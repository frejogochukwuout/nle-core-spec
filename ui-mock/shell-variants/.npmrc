# Storybook 9 peers on Vite ^5||^6||^7 while this app pins Vite 8 (and Vite 8
# itself lists peerOptional @vitejs/devtools that isn't installed). Installs
# therefore run with peer-dep resolution relaxed — same resolution npm's own
# ERESOLVE report recommends for this tree.
legacy-peer-deps=true
