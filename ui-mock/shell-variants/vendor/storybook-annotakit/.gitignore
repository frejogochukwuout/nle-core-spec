node_modules/
# dist IS TRACKED (kit dogfood #6, sibling-stream convention): a fresh clone
# boots Storybook with zero build steps. Rebuild via `npm run vendor:build`
# (or `npm run build` here) + COMMIT when the kit source changes.
examples/nimbus/node_modules/
examples/nimbus/dist-storybook/
examples/nimbus/sb.log
examples/nimbus/sb-debug.log
# annotakit store: threads.db IS tracked (durable feedback — survives disk loss);
# only the sqlite sidecars are volatile
*.db-wal
*.db-shm
*.tsbuildinfo
.DS_Store
*.tgz
