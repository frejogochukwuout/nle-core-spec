# R26-W-A5 — GE (deliver view) audit report

**Agent:** W-A5 (GE — the DELIVER VIEW group). **Threads:** T#34, T#35, T#53, T#71, T#86, T#87 (GH #88, #89, #107, #125, #140, #141) + the three T#1 deliver-page entries (crammed preset tiles · overflowing selects · "repurpose the whole view for export").
**Method:** DESIGN-R26 §3 — ASK (verbatim from digest) → OVERRIDE SCAN → LIVE PROBE (fresh runtime) → CODE CHECK → VERDICT.
**READ-ONLY.** No source file was edited.

## 0. Freshness

- Runtime `http://localhost:3000` index.json = **126 stories** (the post-remediation count). The storybook daemon (PID 936) serves
  `/home/z/my-project/shell-variants`, working tree restored 07:31 from the r25-final bundle (§0.1 remediation); every probe below ran
  on that fresh tree. Spec HEAD `46db450` / repo HEAD `eb9414a`.
- All live probes used the isolated `--session ge-deliver-audit` (no cross-agent interference).

## 1. Verdict table

| Thread | ASK (gist) | Verdict | Evidence |
|---|---|---|---|
| **T#34** (GH #88) | "this view doesn't make sense. video preview should still be here. if you absolutely need to surface these info, well put those in inspector, or as a separate console panel (just like mixer console)" | **PROPER** | LIVE (AppShell--edit → Deliver): the center region boots `[data-testid=shell-deliver-preview]` wrapping the REAL program viewer (`shell-viewer`: zoom select Fit…4×, TC `00:00:16:00`, 1920×1080 · 24 fps, live clip readout "Marina interview · 00:00:03:00–00:00:11:12") — DeliverPage.tsx:327-332 (`<Viewer duration={duration}/>`). BOTH re-homes the reviewer offered are live: the deliver **inspector** (right col = project identity + render settings + CTA, DeliverPage.tsx:335-470) and the **console panel** (the Export tab, see T#86). Queue toggle returns the preview both ways (live: toggle → preview + viewer remount, queue unmounts). |
| **T#35** (GH #89) | "these don't make sense to be here either, left side is related to asset browsing (presets are okay, queues are not, the center view may be better for these queues replacing the video preview when rendering is happening)" | **PROPER** | LIVE: LEFT column = **presets only** (`shell-deliver-presets`, 280px, DeliverPage.tsx:198-233 — the R22-W5 "#89: left side is related to asset browsing (presets are okay, queues are not)" ruling in the header). The QUEUE lives in the CENTER and replaces the video preview exactly "when rendering is happening": queued a master render live → center flipped to the queue view (new row "Beach Doc — Rough Cut — Rough Cut v3 — 1080p · In–Out.mp4" + bundle chip at 0%), header toggle read "Queue · rendering", the store timer walked it to done ("just now"); toggling back remounted the viewer. The view is `showQueue`'s alone (single writer, R24-W4; survives page switches — deliverViewStore.ts). |
| **T#53** (GH #107) | "use block (compact) timeline style, and make the timeline panel slightly narrower to leave more room for the export preview, just the head range selection should be normal height… the shorter timeline area can leave more room for export settings too" | **OVERRIDDEN→T#71** (superseding contract **PROPER**) | The sweep's declared chain: T#53's compact+height ask is superseded/completed by T#71's ruler+range-clamp demand (R24-W4 A3-R7 landed the deliver ruler + range band + in/out clamping). Audited against the CURRENT composition: compact strip on deliver = the 54px head stack (22px ruler + **32px normal-height** range band — D-F1's own "normal height" ruling) and the mainbody rebalances 40%→**50%** while compact (LIVE: 450/900px compact vs 320/800px full — "narrower timeline → more room for the export preview", AppShell.tsx:326-339). **NUANCE (registered, not a defect):** R25-W6's per-page memory law re-truthed the DEFAULT — deliver now boots density `'off'` (FULL timeline; useUiStore.ts:1254, the W6-A pin) and the compact strip is an explicit, remembered per-page choice (ViewOptions → All; LIVE: radio flips mounted the strip + band, Off restored the full Timeline). The reviewer's per-page-memory ask (T#91/th_mtzp94ms, GG group) is the binding later ruling; if the reviewer still wants deliver to boot compact it is a one-line seed change — cite W6-A in the reply, do NOT silently re-default. |
| **T#71** (GH #125) | "under export view timeline is compacted but there's no ruler and no range clamp which is like the BIGGEST if not the only thing we need here" | **PROPER** | **THE BIGGEST THING — live-verified on BOTH surfaces.** (a) Compact strip (ViewOptions All): 22px ruler mounted with **31 tick divs + TC labels** (00:00…00:21+, `shell-timeline-compact-ruler`, rulerTiers grammar) + read-only in/out bracket FLAGS; the **32px range band** below it (`shell-deliver-range-band`, y604 vs ruler y582) with in/out handles as `role=slider` on the **[0, 720]-frame domain** (0…30s × 24fps). **REAL POINTER DRAGS (synthetic PointerEvents, staged evals):** dragged IN past the OUT point (clientX 1345 > out 1318) → preview **PINNED at out** (readout `00:00:28:00 → 00:00:28:00`, never crossed/inverted) and the release **committed exactly the previewed value** (in = 672 frames); dragged OUT past the live IN → **pinned at in** (28:00→28:00); dragged OUT past the right edge (clientX 1600 > area 1412) → **clamped at the timeline duration** `00:00:30:00`. (b) FULL Timeline (the deliver default): the real Ruler mounts with 10 major ticks + 10 labels + **draggable in/out brackets** (`shell-ruler-bracket-in/out`, Loop in 00:00:02:00 / out 00:00:28:00; ordering law R14 + duration cap pinned in Ruler.test:175/193/209). **ONE SEAM:** my band drag moved the full-Timeline bracket readout to `00:00:02:01` — band, brackets and I/O keys are three writers of `s.loop` (RangeBand.tsx:9-15). Clamp laws: RangeBand.tsx:89-106 (keyboard ordering law + mouse honest-preview clamp), :124-125 (opposite-edge pin), [0,duration] domain. Pinned: TimelineCompact.test:230-436 (54px head stack, flags, drag law, `[0, scene duration]` clamp, keyboard law). |
| **T#86** (GH #140) | "this perhaps can be kept in a separate panel the same place we do mixer console etc. and see what else makes sense to fit here, as it is not inspection" | **PROPER** | LIVE: the console row's page-aware tab strip on deliver = **[Timeline \| Export]** (ConsoleTabs.tsx:49-51); clicking Export mounts `shell-deliver-export-console` (DeliverExportConsole) taking the WHOLE row (timeline unmounts — AppShell.tsx:540-550), with the queue-status strip pinned above the scroll ("Render queue · 4 jobs · idle") and the summary card (Timeline / Format / per-preset Codec-or-Schema+Pretty-printed / Resolution / **Duration** / Destination + the live In→Out range block with the **density-aware hint** that names the affordance the CURRENT scope mounts — band copy on the compact strip, ruler-brackets copy on the full Timeline; live-verified both). The "what else makes sense" invite: Duration row + the read-only queue mirror (single-writer law). The right column keeps inspection-family content ONLY (render settings + project identity + CTA); **no generic Inspector rail exists on deliver** (live: `genInspectorMounted=false`; regions = Toolbar/Deliver/Timeline/Status strip/App dock). Settings live in deliverViewStore — the sibling console panel reads the same choice the inspector edits (one-choice law, live: preset click updated the summary's Format row). |
| **T#87** (GH #141) | "add a custom JSON format too" | **PROPER** | LIVE end-to-end: 4th preset tile `shell-deliver-preset-json` ("Custom JSON · NLE project interchange") + the format select's `Custom JSON · project interchange` option; CTA relabels "Export Custom JSON"; clicked → **a REAL download was captured** (spied `URL.createObjectURL` + anchor click): Blob `application/json`, **7613 bytes**, filename `Beach Doc — Rough Cut — Rough Cut v3.json` (project — scene, live-derived). **The JSON is REAL, not a stub** — parsed live: `schema: "nle-interchange/1"`, `project` {name, status "Edited", fps 24, 1920×1080, 48kHz, 2ch}, `timeline` {id sc-1, "Rough Cut v3", duration 30, **range {start 2.0417, end 28} — my own band drag**, markers mk-1…mk-5, tracks overlay 1 / main 4 / audio 1+1 / caption 5 elements carrying the full ElementJSON interchange fields}. Success toast "Export downloaded: … custom JSON interchange (schema nle-interchange/1) — no render queued: the file is already local" — the honest mock/render-real-data split (render presets queue rows; the JSON export actually ran). Builder = pure `buildExportJson` (exportJson.ts:171-185, deterministic key order); download = Blob + object URL + anchor (202-213). Pinned: exportJson.test 8 + DeliverPage.test 16 issue-cites. |
| **T#1** (GH #5) "Deliver page — cloud master preset" | "this whole view feels very crammed the three preset tiles especially" | **PROPER** | LIVE: the preset picker is a 2-col wrapping grid, tiles measured **123×110px** (min-h-[78px] satisfied with room), icon + title + subtitle with breathing-room grammar (th_mto38qzp); four tiles now (json joined) with zero cramping at 1600 and 1280 widths. |
| **T#1** (GH #5) "Deliver page — settings + job queue" | "overflowing" | **PROPER** | LIVE at 1280×800: body row `scrollWidth == clientWidth == 1280` (no overflow), settings column right edge exactly 1280, every `<select>` carries `text-overflow: ellipsis` with `clientWidth == scrollWidth` (min-w-0 + truncate + wrapping field rows — th_mto35hrm); below the ~900px three-region minimum the row scrolls honestly (overflow-x-auto + min-w-0, DeliverPage.tsx:190-196). |
| **T#1** (GH #5) "Full Shell — Deliver" | "this feels tiny, should we repurpose the whole view for export related? like both left (media pool area) and right side, not sure about the timeline perhaps we keep but allow range selection" | **PROPER** | LIVE: DeliverPage owns the WHOLE mainbody (AppShell.tsx:405-420 — the standard left-dock/viewer/inspector trio is not mounted on deliver): presets left 280px / preview-or-queue center / deliver settings right 340px. The timeline question settled exactly as the reviewer leaned ("perhaps we keep but allow range selection"): the timeline stays live below and the loop in/out IS the export range (spec 16 §3.4) — the range select, the Export console's range block and the band/brackets all read/write one seam. |

**Tally: 8 PROPER + 1 OVERRIDDEN→T#71 (contract itself PROPER). ZERO QUICKFIX/BROKEN on the thread contracts.** One collateral polish item found (fix queue F1 — not a violation of any thread above).

## 2. Code check summary (proper-solution test)

- **DeliverPage.tsx** (592 L): the three-region grammar is the R22-W5→R25-W5 composition truth with every ruling cited in-header (#88/#89 split, th_mtzp4arw inspector-vs-console split, th_mtzp4xeb json-real-download, th_mto37ba3 range-reads-the-store-loop, §4.2 honest empty state, R24-W4 store-owned queue). State lives in `deliverViewStore` (jobs + showQueue + the render settings) — queue survives page switches, renders walk on the store timer while unmounted, single-writer laws documented at each toggle.
- **RangeBand.tsx**: local-preview drag grammar (ruling 21 — ONE commit per gesture, no-op release writes nothing, pointercancel/lostpointercapture discard), the F3 P3-2 honest-preview clamp (release commits exactly the previewed value), keyboard ordering law, [0,duration] domain — the interaction LAW, not a visual patch.
- **TimelineCompact.tsx:186-242**: the 54px coexistence head stack (unconditional 22px ruler + deliver-only 32px band) — the A3-R7 ruling that answered both halves of T#71's "no ruler and no range clamp".
- **exportJson.ts**: pure builder (deterministic key order, view-state excluded by law) + guarded DOM wrapper — the testable-seam split; 8 structure/contract pins.
- **AppShell.tsx:509-575**: the console-row wiring — page-aware ConsoleTabs, Export tab takes the row, `TimelineCompact rangeBand={page === 'deliver'}` (the band is deliver-asked only), mixer slot intact on the Timeline tab.
- **Tests (runtime tree):** DeliverPage 34 + Ruler 40 + TimelineCompact 31 + exportJson 8 + ConsoleTabs 8 + deliverViewStore 13 = **134/134 green**; issue-cites verified in all six files (#88/#89/#107/#71/th_mtzp4arw/th_mtzp4xeb).

## 3. Fix queue (for W-F)

- **F1 (P2, one file + pin)** — *collateral of the T#34/T#86 re-homing, not a thread violation:* the toolbar's **Inspector toggle is state-only on deliver**. LIVE (AppShell--deliver): clicking `shell-toolbar-btn-inspector` flips aria-pressed true→false but nothing on the page changes — the deliver settings column is DeliverPage's own always-on region and `panels.inspector` gates nothing on this page (the generic rail exists only in the non-deliver branch, AppShell.tsx:475-482). The house's own precedent is two comments up in the same file: the LEFT toggle was made **DOM-absent on deliver** because "a toolbar toggle there would be the lying control #100 flags" (Toolbar2.tsx:33-38). Fix: gate the Inspector button DOM-absent on deliver the same way (or, if the deliver settings column should be hideable, gate it on `panels.inspector` — pick one; the first matches the #100 ruling). Pin in Toolbar2.test.
- **R1 (P3, reply-wording only — W-R):** T#53's reply must state the R25-W6 evolution honestly: compact is no longer the deliver *default* (every page boots the honest full timeline; compact is a remembered per-page choice with the full 54px T#71 head stack). Offer the one-line seed flip if the reviewer wants the export context to boot compact. Do not code it pre-emptively (W6-A is the binding ruling; GG/T#91 owns the law).

## 4. Probe artifacts (so W-V doesn't re-chase ghosts)

- **Native `agent-browser mouse` drags do NOT route into the range band's React `onPointerMove`** (down+move via the mouse API left the readout untouched). Working recipe = synthetic in-iframe `PointerEvent`s in **staged evals** (pointerdown → wait for React flush → pointermove → wait → read → pointerup) — same class as GC's iframe-drag artifact.
- The ViewOptions popover **stays open after a radio click**; a second click on the toolbar button **toggles it closed** — a "click Off while closed" is a silent no-op that still returns success (cost me one round; re-verify the radio `aria-checked` after any VO interaction).
- The story iframe measures the set viewport honestly (1280×800 and 1600×900 both taken at face value by the app).
- Screenshots for the record: `shots/ge-deliver-strip-band.png` (compact strip: ruler + band), `shots/ge-deliver-full-timeline.png` (deliver default: full Timeline + brackets + preview), `shots/ge-deliver-export-console.png` (the Export tab, json preset).

## 5. Reply evidence for W-R (per §0.2 — reply-first)

- **T#71 (the BIGGEST thing):** cite the live clamp triple — in-past-out pinned at 28:00 with the release committing exactly 672 frames; out-past-in pinned; out-past-edge clamped at the 30:00 duration — plus the 22px ruler (31 ticks + labels) over the 32px band on the compact strip, and the draggable brackets on the full Timeline, all three writers of one seam (band drag moved the bracket readout to 00:00:02:01).
- **T#87:** cite the captured download — 7613-byte `application/json`, filename `Beach Doc — Rough Cut — Rough Cut v3.json`, valid `nle-interchange/1` doc with the LIVE dragged range {2.04→28} and 12 elements across 5 tracks.
- **T#34/T#35:** cite the center-flip sequence (master export → queue replaces the preview at 0% → done "just now" → toggle back → viewer returns) and the presets-only left column.
- **T#86:** cite the [Timeline | Export] strip + the whole-row Export console + the density-aware range hint; the inspector keeps settings-only.
- **T#53:** cite the 50%/40% mainbody arms + the 32px normal-height band, AND the W6-A default note (R1).
- **T#1 (three entries):** tiles 123×110 in a 2-col grid; zero overflow at 1280 (ellipsis selects, scrollWidth==clientWidth); the whole-mainbody 3-region repurpose with the timeline kept + range-selected.
