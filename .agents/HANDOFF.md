# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-23, end of the R35 FINAL SEAL + FLEET RE-HOME round (the register resolved + the aivs-tech re-home + the edit-styles wave + the SF carve + the control plane + battery_r35 ALL GREEN + the adversarial gate's amendments folded). **Read this FIRST.**

## Current state (R35 — COMPLETE: the seal-round directive executed; the user-gated register EMPTY of product calls)

- **THE REGISTER RESOLVED:** fps BOTH 24 AND 30 (a first-class configurable; the `/view` fps param carries either rate) · the playhead FRAME-SNAPPED (W1-a's unanimous-convention verdict; the unquantized path RETIRED; the audio-units display variant = the deferred r2/audio pref-toggle) · the programMonitor viewer-state rows APP-STATE-FIRST (surfaced by default; the surfacing a settings-tier toggle) · the human rounds remain the USER's own lane (never a blocker). The corpus folds: the IP's register + the WALK exit + the app PROTOCOL's three decisions + 18:461/:167's rulings + SKILL #192-193 (the conventions gold standard + the app-state-first law — future product calls resolve by RESEARCH, never by asking).
- **THE FLEET RE-HOMED to github.com/aivs-tech** (origin everywhere; the legacy homes = frozen `legacy` remotes; the GitLab mirrors retained): full-history parity; the CI credentials recreated pre-push (engine's `CI_VENDOR_URL_PREFIX` + test-app's `NLE_GH_PAT`); the re-point commits landed; **the engine CI GREEN on the new home**; **the CONTROL-PLANE REPO `aivs-tech/nle` live** (the shared project skill + plan + handoff + the re-home record — a fresh session starts THERE).
- **THE EDIT-STYLES WAVE LANDED** (shell-mini "fully wired with final production grade code" at the casual bar): the design + batches A (the insertion composites: S1 ripple / S2 overwrite / S3 append / S4 block) + B (the trim styles: the roll/ripple edge session + the wire re-route) + C (the affordances: the nle-ui seam + the 4 pool rows + the F-cluster/E keys + the composition-root registrar + the TimelineView modifier wrappers + the e2e legs) — the census 45 = 33 zero-action + 8 carriers + 4 app-host; the P-tier styles file into SF-2's inventory.
- **THE SF-0..SF-7 FACE CARVE** in the operational plan's §2A (the shell-variant/full mountain's 8 named faces; the r1-r6 rails the execution mechanism; no face mints an engine workstream).
- **THE SPEC CORPUS final at R35**: the wrap re-key (00/09/10/12/17/18's R35 layers; rekey_r35 ZERO + rekey_r34 ZERO) + **battery_r35 247/247 ALL GREEN** (the adversarial gate's amendments folded: the seven vacuous-check P0 + the law-#190 layers + the scanner sets).

## Next session scope (immediate, ranked — ALL itemized in IMPLEMENTATION-PLAN.md's track table)

1. **The re-orient-first law** (SKILL #178): fetch all repos (origin=aivs-tech), run battery_r35 + rekey_r35, read the newest filings — the corpus's "done" state can be invalidated by sibling landings between sessions.
2. **The edit-styles wave's residuals** (if the e2e legs' tail did not land before the session ended — audit src/e2e-crawl.test.tsx for the legs' state; the deadline-recovery pattern applies): the remaining legs + the renderGlued wiring of registerEditStylesSeams + the coverage pins per the design's leg map.
3. **The re-home's verification debts**: the perf-nightly FIRST-RUN dispatch on the NEW engine repo (the 60-day cron auto-disable clock starts fresh; read green before trusting the cron); a fresh-clone submodule e2e (clone from aivs-tech + init via the materialize rewrite — the real proof); the GitLab mirrors' catch-up push (the WAF retry loop).
4. **The parallel NOW-set** (the operational plan's §0): the w2 demo legs (S-app's item 1) · the viewer-state row wiring around ProgramCanvas (the register's ruled disposition — now agent-actionable) · S-ot's L3 data-test census (MUST) · S-engine's W4b the m32 milestone + the r1 Stage-0 prep (the long pole) · S-ui's F3-NEW guard.
5. **The human rounds** (the USER's own lane — fully staged, nothing blocked, nothing agent-blocked; the three decisions RULED; the rounds collect feel-verdicts per the PROTOCOL).

## The process reminders (the round's lessons — READ BEFORE ORCHESTRATING)

- **THE DISPATCH-DEADLINE PATTERN (held 6× this round):** the Task tool's dispatch reports time out but the agents WORK (~15 min each) — the worktree IS the record; audit the diff, verify gates, reconstruct the worklog, keep the work. Six of the round's fourteen agents were recovered this way (C-2a, C-2b, C-1b, C-2, the W1-b research [orchestrator-executed], C-3).
- **THE WORKTREE-PARALLELISM PATTERN:** two implementation agents NEVER share one worktree (their test runs race) — `git worktree add` a branch tree per agent (the batchB pattern: submodules init from local siblings + node_modules symlinked).
- **THE CI-CREDENTIALS-FIRST LAW (the re-home's core lesson):** variables/secrets do NOT transfer with git — recreate BEFORE the first CI-triggering push or the first run goes red.
- **The battery P0 class (GATE-2's catch):** the nested-IIFE check shape `lambda: ((lambda bad: (...))([...]), "tag")` is VACUOUS — ok unpacks a truthy tuple; the R34 bool-tuple law (#191) extends: NEVER nest the verdict lambda; flatten to a named function returning the tuple; run a runtime probe (can any check fail?) after every shape edit.
- **The corpus pin canon lives at ONE place** (00's fleet table) but law #190 binds EVERY row (12/17/18's posture rows too — the GATE-1 P1); the wrap re-key sweeps all of them, not just 00.
- Fetch-first held; zero force-pushes; the two-remote redundancy carried the round.

## VARIANTS-TRACK state (the sibling session's lane — do not collide)

- The shell-variants stream wrapped long ago; its HANDOFF notes live in this file's history (git). The sibling may still be active across the module repos — fetch-first everywhere.
