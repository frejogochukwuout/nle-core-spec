# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-22, end of the R34 SEAL ROUND (the K4 ratification + the fleet gap closes + the wrap re-key + the IMPLEMENTATION-PLAN overhaul + battery_r34 226/226). **Read this FIRST.**

## Current state (R34 — COMPLETE: the seal-round directive executed — every agent-actionable gap closed; only true product calls gated)

- **THE K4 RATIFICATION** closed the crawl: both spec-side conditions landed in the same corpus round (the K2 census+registration flip `0cef3fa` + the K3 VLM-net markers `abd3e14`) and the app's K4 phase row is ticked (`cf4e545`). The K1-K4 tiers are ALL complete/landed; the tier gates are standing regression law (battery-scraped).
- **THE FLEET @ the R34 pins (all coherent, all gates green):** engine `60232ea` **789/789** (the code anchor MOVED — the P-F4 src fix; **the perf nightly + the 5-job CI both GREEN** — the CI_VENDOR_URL_PREFIX variable set [the W0-W4 waves' CI had never run green], perf.yml's parse fix + the two §B.5 steps restored; **P-F4 caught on the FIRST random-seed nightly run + closed same-round** — the exploration venue proven) · OT `ccff397` docs-only over **`8f96ab2` 714/714 @ 83 entries** (the L2 carrier set CR-1/2/3/4/5/8/9 LANDED [M59-M59D] + the M64 /view fps param + the SURFACE_OWNED_KEYS export; the wire census 31=28+3 UNCHANGED — the carriers are props, not verbs) · app `cdc67d8` docs-only over **`d28847c` 426/426** (the R34 re-pin: engine→60232ea + both OT mirrors→05d88d9 + nle-ui→cb04919 + WDC→94f6460; **the CR-2 carrier FLIP** — the census 43 = 33 zero-action + 8 carriers + 2 hosts; scrollAnchorId wired) · nle-ui `cb04919` **747/747** (the DeliverPage supersede contract + the viewer-state-empty CR pair) · WDC `94f6460` 777/777 unmoved.
- **THE SPEC CORPUS final**: the Testing-mirrors + 17 §14.2 repaired; the K2 census registered; rekey_r34 **ZERO LIVE-stale** + rekey_r33 ZERO regression; **battery_r34 226/226 ALL GREEN** (the twelve R34-A..L classes; the adversarial gate's amendments folded + re-gated).
- **THE IMPLEMENTATION-PLAN.md overhauled to the R34 operational world** (`f8f34fb`): every track itemized + gated (the §0 track table = the worklists; the w2 leg inventory + the r1 Stage-0 rows verbatim in §3; the ladder re-based — CRAWL COMPLETE, r1 = EXECUTION; the D40 vocabulary laws preserved + battery-enforced). **THIS DOC + the PLAN are the entry points; the IP is the operational plan.**

## Next session scope (immediate, ranked — ALL itemized in IMPLEMENTATION-PLAN.md's track table)

1. **The re-orient-first law** (SKILL #178): fetch all repos, run battery_r34 + rekey_r34, read the newest filings — the corpus's "done" state can be invalidated by sibling landings between sessions.
2. **The human-rounds track (the USER's — nothing blocks, nothing is blocked)**: run the round-1 scripted edit session per `nle-test-app/docs/human-rounds/PROTOCOL.md` (the annotakit loop live on :6007, the C/G/H/F/Q capture keys, the side-by-side fixtures); each round closes with the round-`<n>`.md register. The three REGISTERED decisions collect verdicts per round.
3. **The parallel NOW-set (agent-actionable, any order):** S-app's value-passing wires (isShellOverlayOpen next; surfaceOwnedKeys UNBLOCKED [OT exports the census]; cancelRegistry the D25.2 mint) + w2 the full-scope demo (~6-9 e2e legs) · S-ot's L3 data-test census (MUST) + L5 the P3 sweep + the deferral rulings · S-engine's W4b (the m32 perf milestone) + the r1 Stage-0 prep (the design v2 complete + all inputs landed — the EXECUTION round is the long pole) · S-ui's F3-NEW sceneDuration guard.
4. **The sibling filings** (absorb via the 5-wave intake pipeline, SKILL #179): the OT L3/L5/L6 waves + the engine's first perf dispositions (the nightly runs accumulate → W5's thresholds unblock).

## The user-gated register (the ONLY items awaiting the user — everything else is agent-actionable)

1. **fps 24-vs-30** — the product frame-rate verdict (the `/view` fps-param MECHANISM landed R34; the product default stays user-gated; until the verdict the app side is the fps-honest rendering).
2. **The playhead cadence** — frame-snapped ~fps publish (current, 18:461-conformant) vs an unquantized display path — recorded each human round (keep = default; switch = registered).
3. **The programMonitor viewer-state rows around ProgramCanvas** — the app-side decision (the nle-ui empty-testid CR landed R34; the app-side row wiring is the gated call).
4. **THE HUMAN ROUNDS THEMSELVES** — the round-1 scripted edit session; fully staged, zero agent-blocked prerequisites.

## Process reminders (the round's lessons — READ BEFORE ORCHESTRATING)

- **THE SELF-LAUNDERING RE-KEY CLAUSE (the W5b P0):** a re-key clause labeled "R34 re-key" is EXEMPT from rekey_r34's own scanner (the label is a historical marker) — so a W2-era clause carrying stale pins survived the wrap re-key labeled as current. The r35 pattern: re-key clauses re-key on CONTENT (the pins must be current), never on their own label; the adversarial reviewer's fresh-context read is the net that catches what the scanner launders.
- **THE DEADLINE-AGENT RECOVERY PATTERN (held 6× this round):** agents that hit report deadlines almost always finished their edits (or pushed!) before dying — the worktree IS the record; audit the diff, verify, reconstruct the worklog, and note the recovery honestly in the commit.
- **THE CI-VARIABLE CLASS (the engine's W0-W4 red X):** a private-submodule workflow needs its credential variable (CI_VENDOR_URL_PREFIX) configured — the workflow's own error message names it, but 6 commits' CI ran red before anyone read the run log. "Check the run before trusting them" (the engine scout's caveat) was RIGHT: the W3/W4 jobs had never run green.
- **THE EXPLORATION-VENUE LAW (P-F4):** the nightly random-seed pass found in ONE run what the fixed seed passed for a week — the exploration venue is the discovery instrument; the deterministic suite holds the class once found. A NEW red nightly is a FINDING, not a regression of the instrument.
- **THE DOCS-ONLY TAIL:** the wrap's sibling HANDOFF truth-ups move the repos past the battery's LIVE pins — re-key the LIVE HEAD checks for the docs-only tail (the substantive pins stay in the corpus layers).
- Fetch-first held; zero force-pushes; the two-remote redundancy carried the round (GitLab's WAF 403 retried through ~50% of pushes).

## VARIANTS-TRACK state (the sibling session's lane — do not collide)

- The shell-variants stream wrapped long ago; its HANDOFF notes live in this file's history (git). The sibling may still be active across the module repos — fetch-first everywhere.
