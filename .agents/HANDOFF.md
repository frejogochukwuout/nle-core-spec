# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-13, end of the R24-mini mini-plus round (through W4+fix). **READ THIS SECTION FIRST — everything below the `-----` divider is R18-era serving/environment detail that is still ACCURATE as background (env layout, :3000 ownership, annotakit kit, mirror repo) but predates R19-R24.**

## Current state (R24-mini — the mini-plus feature-parity round: 6 of 8 features LIVE; W6 mixer + W7 seal remain)

- **The user's directive** (this round): fix the audio-clip box (DONE — the block--audio underlay + ring, live-verified in the browser), then the 8-feature mini-plus push — property editing, effects, transitions (tool mode), the 4 trim modes (Roll/Ripple/Slip/Slide per `../trim_edit_modes.html`), the 6 insert modes (per `../timeline_edit_modes (2).html`), source preview + in/out, track M/S, the mixer — "ideally use app state variant to control these" (the ONE `miniPlus` gate, default ON, additive-by-construction), "aim at 50+ of sub-agent rounds", "iterate till <=p3 issues left".
- **LIVE at `c0f531c` (both mirrors): 495/495 tests / 12 files / tsc 0 / vite + SB builds green / battery_r26 content-green (131/141 — the 10 LIVE fails are the sibling's module repos, un-clonable in this sandbox: environmental).** The waves: R24-1 (the box fix +2), W0 (the model superset + the F1 P0 commit/docChanged deep-clone surgery + the gate + fields primitives +20), W1 (property editing + effects +19), W2 (transitions/fades + the F2 P0 split disposition table +19), W2-fix (+13), W3 (roll/slip/slide + the tool-dispatch seam +13), W5 (track M/S +4), W4 (source mode + the 6-mode insert planner, DELEGATED to a full-stack agent + adversarially reviewed + fixed, +50). Every wave: the count re-pin landed at the push (the battery's scrape + census-math checks caught 4 arithmetic slips — trust them).
- **THE DESIGN IS THE CONTRACT:** `ui-mock/shell-mini/docs/DESIGN-R24-miniplus.md` v2 — D1 the gate (additive-by-construction), D2 the model + the REAL critical path (the commit trio), D3-D9 the features, D10 the keyboard map, D11 the test plan, D12 the wave plan, **D13 the 23 registered deviations** (the sanctioned divergences — read BEFORE reviewing anything). The design was adversarially reviewed (R24-2b: 19 findings, all folded) BEFORE any code; every wave got a fresh-context review (W1+W2: R24-2c FIX-THEN-APPROVE → fixed; W4: R24-2e FIX-THEN-APPROVE → fixed).

## Next session scope (immediate — W6 + W7, then the audit loop)

1. **W6 — the mixer (DESIGN-R24 D9, the design is COMPLETE):** 6a the engine + primitives (`src/lib/meterEngine.ts` ~150 ln: the seeded deterministic walk (mulberry32, the waveform idiom), dB ballistics (attack instant, decay −20 dB/s, peak hold 1s), `useSyncExternalStore` cached-snapshot discipline, the idle+settled rAF stop, `__setLevel`/`__reset` hooks; the `Fader` (taper + gridlines + headroom) + the stereo `StripMeter` ported from the variants' `MixerPrimitives.tsx`) — netted standalone; 6b the dock (the right column becomes flex-column: Inspector over the Mixer; `mixerOpen` **default FOLDED** (28px header / ~220px open); composed-collapse: `viewerMax` hides the rail, `inspectorCollapsed` hides the mixer; strips for BOUND audio tracks only (video-only mode → the honest empty state); view-state fader/pan + master; the M/S dots read D8's `isTrackAudible`). The variants' references: `src/components/mixer/` + `src/lib/meterEngine.ts` + `mockMixer.ts`.
2. **W7 — the seal:** SB stories for the plus surfaces (the 13 stories are pre-plus — add: the Inspector-plus story (a doc with effects/transition/fades), the trim-tools story, the source-mode story, the mixer story); the README deviations register (#40+ — port D13's 23 entries); the LAW-NET Part A/B rows for the W4-W6 families; the full-surface audit loop (fresh-context rounds to ≤P3 — the user's 50+ sub-agent target is at ~14; CONTINUE the rounds: each wave reviewed + at least one full-surface pass + a UX pass with the browser daemon).
3. **The standing conventions that held all round:** push both mirrors at every wave (GIT IS THE DISK; merge-first on the sibling races — 6 this round); the count re-pin per wave (declared == actual at every push — the battery r26's scrape + census-math + suite-pins checks are the arbiter; the variants census re-derives live each time — the sibling keeps moving); the D22 drag-law freeze held (the select branch byte-identical through the tool-dispatch seam — deviation #9; the behavior nets green throughout).
- **Daemons:** SB :3000 + Vite :3001 are live (restarted 09-13 11:08; HMR served all waves; the health check + the audio-box live verification used them). If a fresh sandbox: `ui-mock/shell-mini/scripts/sb3000.py` + `dev3000.py`.
- **Git auth:** GitHub via `GIT_ASKPASS=/tmp/my-project/scripts/git-askpass-gh.sh` (the token source: `/tmp/my-project/scripts/pr69_replies.py`; username `frejogochukwuout`); GitLab via `GIT_ASKPASS=/home/z/my-project/scripts/git-askpass-gl.sh` (the NEW PAT is IN the helper; WAF 403s are probabilistic — retry 2-4×). Both mirrors: origin + gitlab (`https://gitlab.com/ansgareutychisO/nle-core-spec.git`).
- **The PR #69 notices remain staged** (GitHub's repo API surface still dark for REST/GraphQL while git works; `/home/z/my-project/scripts/pr69-r22-notice.py` — fire when it recovers).

## Mini-track R24 state (the feature map)

- **The gate (D1):** `miniPlus` view state (default ON, drag-gated, reset→ON); the tools-row chip `mini-btn-miniplus`; gate-OFF = the R23 surface BY CONSTRUCTION (every plus affordance mints new DOM/testids; netted per feature). Gate-OFF also hands an in-flight source session back to program (the F2 fix).
- **The model (D2):** Clip superset (sourceStart/speed/volume-linear/opacity/fadeIn/Out/effects/transitionOut — all optional, absent = legacy, the absent-is-default write law) + Track.solo. The commit trio: `cloneClip` deep-clone (nested no-alias — netted), the docChanged full superset (deep effect/transition compare), the `sanitizeTransitions` invariant (deviation #15: orphaned transitionOut deleted + duration re-clamped on every commit).
- **The surfaces:** the Inspector plus-groups (Timing/Audio-Video/Effects/Transition/Fades — `fields.tsx` primitives, the F3 settle-display law, the F16 density defaults); the TransitionLayer (wedges + fade triangles + the transition-tool mint zones — the F4 hit-area law); the tool radio (V/T/Y/U/X — the dispatch seam deviation #9); the M/S chips (the anySolo ONE selector, mute-wins); the Viewer dual-mode SourceStage + `SourceRangeBar` (the in<out REFUSAL law) + the pool "open in source" affordance; `lib/insertPlan.ts` (the 6-mode planner — the F1 MIN guards; the F5 delta≥0 invariant) + `insertFromSource` (ONE commit, error toasts on refusal).
- **The keys (D10):** V/T/Y/U/X (tools, gate ON); I/O/,/. (source-mode only, gate ON); the Esc chain: dragActive-cancel → source-exit → deselect. The existing map untouched (S/[/]/Del/±/0/Space/Home/Esc/⌘Z).

## Sibling state (the spec track — R27 in flight)

The sibling is at Round 27 (ARCH-R27 the final-tightness audit; their R25/R26 landed the DaVinci mode-matrix D30-D36 + the three cross-repo executions: engine E1/E2 471/471, WDC 773/773, the app census-CI 206/206; the fleet at engine 0a49286 / OT fdb771c / WDC 387f327 / nle-ui 3026099 / app 3c91ffe). Their races arrive DAILY — always `git fetch origin` + merge-first before pushing. The mini's count claims ride THEIR spec files (00/05/12/13/16/17/18/19 + IMPLEMENTATION-PLAN + REFERENCE-REGISTER) — re-pin on every count change, both directions.

## Standing laws (battery-enforced)

- The posture law (D20): BASE rows pin+suite+count; GAP rows owner+phase+acceptance; count discipline (declared == actual at the pins — the battery scrapes the live suite).
- The D22 drag-law freeze is MINI-ONLY (the user's `.agents/user-msg` ruling: "never apply this to the spec/plan!") — the R18k select-tool law verbatim; the plus tools dispatch around it (deviation #9), the select branch byte-identical.
- The count-coherence convention: land the wave, re-pin once, push (the battery's census-math + scrape checks are the proof — they caught every slip this round).

-----

## What is LIVE right now (the headline — R18 layout; still accurate)

- **The public preview URL IS the full Storybook dev server (R18, user directive):** `https://preview-chat-<chat_id>.space-z.ai/` (the hostname embeds THIS chat's id from the gateway metadata) → edge → Caddy :81 → localhost:3000 → **`storybook dev -p 3000`** (`scripts/sb3000.py`, double-fork daemon, PPID=1, `core.allowedHosts: true`). Same-origin means everything works through the real edge: manager UI, story tree, story selection, iframe canvases, `/index.json`, HMR, deep links (`/?path=/story/…`).
- **The app is the localhost dev surface on :3001** (`scripts/dev3000.py`; `vite.config.ts` port 3001) — for the agent dev loop only, NOT public.
- **Pin-comment review surface LIVE (R18b):** storybook-annotakit v0.5 is vendored at `vendor/storybook-annotakit/` (dist tracked — boots without building), FIRST in `.storybook/main.ts` addons. At the public URL the toolbar carries Pin (⌥C) / Region (⌥R) / Threads (⌥D) / Hide (⌥L), and reviewer pins are same-origin REST through the edge. Store: `.git/annotakit/threads.db` (branch-switch-proof) → orphan `annotakit` branch on GitHub — **SHARED with the sibling stream's env** — plus a 1:1 GitHub-issue mirror. Agent surface: `GET /annotakit/api/health` → agentSurfaces (rest + digests + github); `GET /annotakit/api/threads`, `/export?format=md`, POST/DELETE threads, POST `/sync`. Token: `.env` in shell-mini (gitignored — **recreate after a recycle**: PAT from chat + `ANNOTAKIT_GH_REPO=melodietexoss/shell-mini-review` + `ANNOTAKIT_GH_LABEL=mini` + `ANNOTAKIT_GH_SCOPE=4c1120aa|src/(timeline|shell)/` — see the R18h bullet).
- **The sibling streams own their repos**: nle-engine / opencut-timeline / web-daw-core / nle-ui / nle-test-app all have live `.agents/` docs + worklogs — read THEIR HANDOFFs before touching their domains; file cross-repo work as queues, never unilaterally.
