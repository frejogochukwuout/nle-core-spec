# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-17, end of the R31 DOM-structural-tranche round. **Read this FIRST.**

## Current state (R31 — COMPLETE: the timeline map executed + D51 + the K4 legs begun)

- **The round:** the K3 DOM-structural tranche — the timeline map's 13 RE-EXPRESS-PORT rows ALL LANDED (the app suite **377/377 → 418/418 across 21 files**, +36 tests in 9 new files: dom-structural 8 / scrub-session 4 / clip-lifecycle 3 / trim-gesture 3 / edge-autoscroll 2 / zoom-anchor 2 / dnd-fallback 3 / mid-drag-yield 5 / e2e-crawl 6). **The D26.5 testid mapping row is RATIFIED** (18 §0): the four partial rulings landed as code (the resize-handle `data-element-id` composed-selector grammar + the zoom-slider `aria-valuetext`; the drop-indicator marker ruling + the audiolane divergence registered); the 18 GAP units + the **carrier-reduction work order (the 3-rounds-open ask — NOW FILED)** + the data-test freeze ask all filed in OT's queue @ `31f2764`.
- **D51 the mid-drag keyboard yield LANDED** (the mini's M2 interaction-lock as the engine-world twin): the `isInteractionActive` gate in `use-keybindings.ts` (every verb yields during an active element-interaction session EXCEPT Escape; scrub never engages) — the FULL design pipeline (research → ruling → adversarial review APPROVE-WITH-AMENDMENTS → the five amendments folded → implemented, blast radius zero). **The two-keymap residual is REGISTERED** (the shell's useShortcuts fires wire-invisible ⌘Z/⌘B/brackets mid-drag — probe-proven; the **D52 candidate** = the optional TimelineRouter.isInteractionActive twin). The ruling + review live in the app repo's `docs/decision-mid-drag-keyboard-yield.md` + `docs/review-mid-drag-keyboard-yield.md`.
- **The K4 e2e legs have BEGUN** (`src/e2e-crawl.test.tsx`, 6 tests): import(virtual) → cut(split/ripple/trim) → play(the engine clock) → export(the REAL DeliverPage + deliverService) — **the capstone chain commits exactly six wire verbs, each ok, each origin 'ui'**. The master-export venue gap pinned honestly (jsdom lacks OffscreenCanvas — the browser-venue-only path; the frame preset completes).
- **The nle-ui filings LANDED** @ `dc3644b` (the D29 DECISIONS first-party record + the C0 MiniShell row — the S-package step 2 discharged; the spec's stale §0 stamps re-keyed).
- **Pins:** app `f58147c` (418/418, 21-file roof) · nle-ui `dc3644b` (701, docs-only) · engine `e3f55bd` · OT `31f2764` (code pin `970948a` unmoved) · WDC `94f6460` · spec sealed @ `903cb32`. **battery_r31 178/178.**
- **The row corrections the waves found** (registered in the map's R31 addendum): row 38's Enter law is INVERTED app-side (Enter = gotoStart; the pointer click is the sole selecting surface); row 5's arithmetic slip (CLAMP-never-REJECT + at-bound commit suppression); row 24's first half N-A (the app NEVER reads dataTransfer.getData — the registry IS the design); the trim gesture math is rect-FREE.

## Next session scope (immediate, ranked)

1. **K4's completion** (the crawl's exit — now in sight): extend `src/e2e-crawl.test.tsx` toward the full suite per the K4 exit gate (the routed verbs driven through the real UI end-to-end, zero mock paths, zero human input — the shell-path exception applies identically); the zero-error class + BOTH collector figures recorded. The K4 legs that remain: undo/redo inside the chain, the JKL transport leg, the bookmark leg, the loop-region leg, the multipage navigation leg (the DeliverPage dock switch is already pinned).
2. **w1's entry** (the first WALK rung — per IMPLEMENTATION-PLAN S-app steps (7)-(8)): the engine playhead binding (the w1 approach) + the token wiring (D25.3a — the mini theme mode on OT's CSS-variable surface) + the human side-by-side prep (app-vs-OT-runner, two renderings of ONE tree per D25.4) + the VLM net pointed at both.
3. **The D52 ruling** (the two-keymap yield twin — REGISTERED, not started): the optional `TimelineRouter.isInteractionActive` interface member + the shell useShortcuts' doc-mutating rows consulting it; the C16 both-listeners shape. The evidence: the review's probe C/D/E table (⌘Z/⌘Y/⌘B/brackets live mid-drag, wire-invisible).
4. **The per-element session families** (D51's registered follow-up): the keyframe-drag / keyframe-box-select / volume-line sessions need a scrub-aware interaction registry before they can gate the keymap (the naive `cancelRegistry.hasActive()` over-includes the scrub session).
5. **Standing residuals:** the OT structural-gap components (the D25.3b worklist — crawl-tail, additive-optional); the VLM net (K3's last open half); the per-spec `## Testing` mirrors + 17 §14.2's mapping repair (the maintenance rows); the app hygiene rows (the stale maintainPitch PLAN row tick; the DeliverPage blob contract CR).

## Process reminders (the round's lessons)

- **The design-decision pipeline held end-to-end for D51** (research → drafted ruling → fresh-context adversarial review → amendments folded → implementation → the 10-leg pin): the review found the drafted ruling's TWO overclaims (the two-keymap hole + the scope inconsistency) — never skip the adversarial round on a behavioral ruling.
- **The rect-FREE trim discovery**: the trim gesture math derives delta purely from `clientX − session.startX` (zero getBoundingClientRect reads) — the first gesture family needing NO rect stubs. When a probe fails, read WHICH coordinates the controller actually reads before stubbing.
- **The two-listener world**: the app has TWO keymaps (the port's document-capture listener + the shell's window listener) — any keyboard-law ruling must enumerate BOTH surfaces (the C16 precedent landed in both; D51 landed in one with the residual registered).
- **The window-collapse law re-confirmed**: after any interaction that forces re-measure, el-2 can UNMOUNT (jsdom's zero viewport) — pin unselected-side laws at BOOT, never post-interaction.
- **Fetch-first held** (zero force-pushes; all six repos at the R31 pins; no sibling races this round). The battery re-baselines ONCE per round (battery_r31 is current; the re-key rides the wrap — `scripts/rekey_r31.py` the row-aware scanner, zero residual).

## VARIANTS-TRACK state (the sibling session's lane — do not collide)

- The shell-variants stream (R26-variants) wrapped long ago; its HANDOFF notes live in this file's history (git). The sibling may still be active across the module repos — fetch-first everywhere.
