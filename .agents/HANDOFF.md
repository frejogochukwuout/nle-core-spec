# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-21, end of the R33 INTAKE FOLD round (the 87-row engine register + the OT seal18 wire-reality + the pin re-key + battery_r33). **Read this FIRST.**

## Current state (R33 — COMPLETE: the two post-R32 sibling filings ABSORBED + the corpus re-keyed + battery_r33 214/214)

- **THE 87-ROW ENGINE REGISTER APPLIED 87/87** (audits/ENGINE-SEAL-R28-REVISION-NOTES.md): 84 rows marker-applied ("(R33, engine seal-round register P1/P2/P3-N)" — grep the corpus) + the 3 recorded exceptions (P3-29 discharged-verified [edit-domains.ts matches §5.9F verbatim]; P3-36 discharged upstream [the engine W0 pull_request fold]; P1-6 applied unmarked [verified landed by the W6b audit]). Every premise was re-verified against the CURRENT trees before editing — the register's R31-vintage evidence was re-located by content, and where the trees had moved (the property tier, the seal18 waves, the lockstep re-pin) the rows were applied SHAPED TO THE CURRENT TRUTH, not the register's literal sentences (the W6a audit: "the correct behavior in all three cases").
- **THE OT SEAL18 WIRE-REALITY FOLD**: TRACK_LOCKED is **13 commands** not 12 (insertBatch :1414-1420 + track.remove :2729 join the old 12); F1B-2 is **four verbs** (timeline.trim joined the wire-side overlap validation — `validateWireTrim`); HA-2-7's member-guard generalization; HA-2-11 `upsertKeyframe.value` narrowed to `number`; the 15:14 census at the report-json truth (632 classic incl. M58R-3 + 17 M60 + 11 M61 + 11 M63 + 4 M62 = 675, 77 entries); the bookmark/marker EDIT residual registered (the r1 A2/C7 fold's input).
- **THE PIN WORLD @ R33**: engine `2a0ecf4` **785/785** (749 + 33 property + 3 P-F2; 29 files; the code anchor `28aa387` — the first src/ movement since R28; timeline.ts 9,051 LOC; the W0-W4 waves: CI hygiene + THE PROPERTY TIER + the coverage job + `perf.yml` the fleet's FIRST scheduled workflow; vendors OT→`17a19f8` + WDC→`94f6460`) · OT `008e7f3` **675/675** (the seal18 close: the D42 linkage + D43 edit-domains + D44 ERROR_CODES + the M61/M62/M63 families; the code pin `344123a`) · app `7664603` (426/426 + the 22-file roof UNCHANGED; the lockstep re-pin: vendor engine→`574d8d3` + both OT mirrors→`17a19f8`; census 43 = 32+9+2 @ 17a19f8) · nle-ui `1e0c30b` (743/743) + WDC `94f6460` (777/777) UNCHANGED. **rekey_r33.py: ZERO LIVE-stale residual. battery_r33: 214/214 ALL GREEN** (the D43-A3 + D42 drift-fence classes ACTIVATED — the pre-r1 REGISTERED-SKIP retired).
- **THE ADVERSARIAL GATE PASSED**: W6b APPROVE-WITH-AMENDMENTS (all folded + re-gated: the 4 scanner-missed residuals [03:16/16:15/17:22/18:15], the R33-A distinct-id census, the NEW_PINS laundering fix [1e0c30b/94f6460], the cite nits) + W6a APPROVE (the 6-row sample all TRUE at the live trees).

## Next session scope (immediate, ranked)

1. **THE HUMAN ROUNDS THEMSELVES** (w1's core — the USER's track; everything is staged and nothing in it needs an agent first): run the round-1 scripted edit session per `docs/human-rounds/PROTOCOL.md` in the app repo (import → arrange → trim incl. the 1-frame floor → split/ripple → JKL/loop/audio playback with the Q1 checklist → undo/redo mid-gesture [the D51/D52 yield, live] → save/reload → export), capture via the annotakit pins (C + [feel:*] tags via G — the remapped hotkeys, the loop live on :6007), triage P1/P2/P3, convert EVERY finding to a pin/CR/queue reference, close with the round-`<n>`.md register (census-in/dispositions-out, ZERO UNOWNED FINDINGS). The three REGISTERED open decisions collect their verdicts each round: fps 24-vs-30, the playhead cadence (frame-snapped vs unquantized), the programMonitor viewer-state rows.
2. **THE PENDING GHITHUB RE-PUSH** (item 0 logistics): the round's 6 commits (`c818de7`→`5eb4bb5` + the wrap) are on GitLab + /home/sync but GitHub's PAT died mid-round (401 after the W2 push) — when the user supplies a fresh PAT: update the origin URLs on all 7 repos, `git push origin main` (fetch-first; never force), verify `git log origin/main..HEAD` empty.
3. **The spec-side standing residuals** (autonomous, any order): the /view fps-param resolution (the side-by-side README's open decision); the per-spec `## Testing` mirrors + 17 §14.2's mapping repair; the app hygiene rows (the DeliverPage blob contract CR; the stale maintainPitch PLAN row tick); the OT carrier-reduction program's FILED row consumption.
4. **The sibling lanes' next filings** (absorb as they land, per this round's pattern): the engine's perf.yml item 0 (the push-run red X — a trigger-parse issue) + the first perf disposition + the property-findings ripple; the OT L2 carriers → L3 data-test/view-config → L5 P3/sleeps → L6 expansion waves (their HANDOFF's queue); anything they file arrives as PLAN INCOMING FILING blocks + audits/ artifacts.
5. **w2's entry** (the second WALK rung — the full-scope demo): extends the K4 suite (`src/e2e-crawl.test.tsx`) toward the complete editor journey; re-gates by extension per the K4 row.

## Process reminders (the round's lessons — READ BEFORE ORCHESTRATING)

- **THE RE-ORIENT-FIRST LAW:** before ANY round, fetch all repos + read the register/filings + run the battery — the corpus's "done" state can be invalidated by sibling landings between sessions (this round: the whole R33 queue existed only as two post-R32 INCOMING FILING commits + the battery's 10 pin-lag fails). The user's "wait to confirm... check the full spec plan and fully re-orient" is the standing pattern.
- **THE INTAKE-FOLD SHAPE (this round's reusable pattern):** scanner FIRST (parse the register → extract every file:line cite → read the current corpus at each cite → the evidence pack), then file-disjoint sub-agent batches (each gets: the row text verbatim + the scan-pack + the current pin world + the edit laws + the discharge law), the orchestrator applies the P1s personally, then the re-key scanner + the battery fork, then the adversarial gate (a fresh-context reviewer per facet — the battery audit and the corpus audit are SEPARATE agents).
- **THE DISCHARGE LAW (the register's evidence can be STALE in BOTH DIRECTIONS):** a row's premise may already be fixed at the current trees (P3-36: the engine W0 fold landed AFTER the register) or its revision text may itself be stale (P2-19: "OT has NO linkage field" was false by apply-time; P2-21: the K4 exit sealed past the row's R31 shape) — NEVER apply a register row's literal sentence when the live tree contradicts it; verify, then shape to the current truth, and record the discharge/mutation in the marker note.
- **THE DEADLINE-AGENT RECOVERY PATTERN (held again — 5 of 15 this round):** agents that hit context deadlines almost always finished their edits but died before the worklog; the worktree IS the record — audit the diff, verify the cites yourself, reconstruct the worklog fragment, and note "reconstructed by the orchestrator" honestly.
- **THE ROW-NEW-PIN-FILTER LAUNDERING CLASS (W6b's find):** a multi-repo mega-row (one logical line naming several repos' pins) launders old pins past a row-scoped new-pin filter — the fix: every CURRENT pin (including the UNCHANGED ones like 1e0c30b/94f6460) joins the new-pin set, and the scanner's residual claim is only as strong as its weakest pattern.
- **THE SUBMODULE-INIT ENVIRONMENT TRAP:** a restored sandbox ships with `git submodule status` all `-` (uninitialized) — battery LIVE checks that READ vendored files fail spuriously (R30-3/R30-5 looked like corpus fails; they were environmental). Fix: `git config --global protocol.file.allow always` + point the submodule URLs at the LOCAL sibling repos + `git submodule update --init`. Diagnose BEFORE re-keying a battery check.
- **THE PUBLIC-VS-IMPL CITE RULE (the :6055 lesson):** when re-anchoring line cites, cite the PUBLIC method (`performOverwriteEdit` :6039), not the private impl it delegates to (`_performOverwriteEditImpl` :6055) — two agents "verified" different symbols and both sounded right until the battery's regex forced the distinction.
- **Fetch-first held** (zero force-pushes; GitLab carried the round when GitHub's PAT died — the two-remote redundancy working as designed). The battery re-baselines ONCE per round (battery_r33 current; battery_r32.py untouched as the R32 record — confirmed by the W6b audit).

## VARIANTS-TRACK state (the sibling session's lane — do not collide)

- The shell-variants stream wrapped long ago; its HANDOFF notes live in this file's history (git). The sibling may still be active across the module repos — fetch-first everywhere. The W3-3 agent noted live variants = 1,949/69 vs the declared 1,939/68 (movement in useUiStore/useInsertPreview/useShortcuts, Sep-21 — NOT the color family); the register re-keys at the next WRAP per D35.2-rider.
