# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-10, end of the R26 round (THE EXECUTION round — the three R25 filings executed + the re-pin + battery_r26). **Read this FIRST; everything below the `-----` divider is R18-era serving/environment detail that is still ACCURATE as background (env layout, :3000 ownership, annotakit kit, mirror repo) but predates R19-R26.**

## Current state (R26 — the filings are EXECUTED; the execution continues)

- **The three R25 cross-repo filings are EXECUTED, each adversarially reviewed (PASS, all folds landed):** (1) **the engine E1/E2 linked-companion fixes** @ `8188d4e`+`412693d`+`0a49286` — the P1 linked mid-clip insert overlap (the r1 port's precondition MET) + the overwrite companion asymmetry (the ground-truth discovery: the old cascade was DEAD CODE — the fate-based companion law landed); 471/471, the 13-test pin file, the E1-a..d/E2-a r1 residue register; (2) **the WDC waveform-contract promotion** @ `035afe8`+`387f327` — `WAVEFORM_PEAKS_BUCKETS` + `WaveformPeaks` + the 14-pin contract test; the extraction law held (audio-registry byte-verbatim); 773/773; (3) **the app census-CI** @ `9d310f8`+`3e4f0cd` — the register (41 = 35 zero-action + 5 carriers + host) + the vendored components-tree reference + the checker + the 6-scenario mutation teeth + the `.agents/` bootstrap + the WDC consumer re-pin; 206/206.
- **The R26 pin world (battery-enforced):** engine `0a49286` **471** · OT HEAD `fdb771c` (code pin `c15a629` UNCHANGED) 536 · WDC `387f327` **773** · nle-ui `3026099` 674 (unmoved) · app `3c91ffe` 206 · mini 355 sealed · variants **1,613 it-blocks / 61 files** (the sibling's R24-W1 race absorbed mid-wrap — their round is MID-FLIGHT, more waves coming; fetch-before-push is LAW). **battery_r26 141/141** (the standing battery — run it after every round; the T/U classes verify the filings' landing LIVE).
- **The maintainPitch residual is PRECISELY SCOPED (not a quick fix):** the app-side thread gates on a spec-09 MODEL DECISION (does ElementJSON gain a preserve-pitch field, or does the nle-ui toggle stay mock-local surface state with the engine's default-true standing?) — the design ask files with S-spec FIRST.

## Next session scope (immediate — THE EXECUTION continues)

1. **K3's store/policy halves** (S-app, the critical path, unstarted): the useMini-family re-expression as app tests over the real vendored OT; the targets = the GluedShell store family + `engineService.ts` + `sceneBridge.ts` (CORE-SEAMS §2 is the field map). The audio half now has the named WDC contract (program against `WaveformPeaks`/`getPeaks`/`setAudioPeaks`/`computePeaks`/`WAVEFORM_PEAKS_BUCKETS` — the app's vendored core is re-pinned to it).
2. **The maintainPitch design ask** (S-spec): the model ruling (ElementJSON field vs surface state) — file it as a DECISIONS entry; the app thread follows.
3. **The mode-matrix K3 rows** (S-app) + the **K2 combined-family census registration** (S-spec, at K2 entry — the 10-mode matrix in 06/17 + the zero-orphan-rows law).
4. **The register maintenance** (standing): rows flip LANDED as owning rows land; the variants census re-reads at the sibling's round-wrap; the C-ledger intake.
5. **User-gated decisions to surface when the user next appears:** the r1-entry absent-family scope gate (+1.5-3 wk if all four families in scope); D6 re-affirmation (r4 real media decode); the nle-test-app rename; the seal-snapshot exercise (D28); the D25.2 retirement confirmation.
6. **Process reminders:** fetch-before-push (the sibling raced mid-wrap THIS round); never force push; the census law is standing (a carrier without a rationale is a violation; the landed 24+6 census is NEVER reopened by spec rounds); battery_r26 after every round; the OT r1-port must carry the LINKED-COMPANION LAW (E1/E2 fixed — the residues E1-a..d are registered in the engine's PLAN).

## Standing laws (battery-enforced)

- The posture law (D20), the pin world (D21), the drag law (D22), the single-tree law (D25 + D26's convergence amendment), the plan-executability law (§2A.6), the census law (§2A.7 — now MECHANISM-BACKED: the app's checker + mutation gate in CI), the productization gate (D28.2), the mode-matrix completeness law (§2A.8), the reference-register law (§2A.9) — all as at R25.

## VARIANTS-TRACK state (R24 — 2026-09-10, the full-audit round COMPLETE + reconstructed)

- **The sibling's R24-variants round is COMPLETE** (the mid-flight race R26 absorbed was its W0/W1; the full wrap landed @ `b9cd387`): the 14 reviewer threads #58-#71 researched (A1/A2/A3), audited (the F1-F5 fresh-context family net), re-specced (DESIGN-R24 v2-RECON @ `.agents/design/r24-audit-round.md`), re-implemented W0-W5d after the recycle destroyed the original unpushed waves (SKILL #143-#145 are THAT incident's laws), and resolved 14/14 + GH mirror closed. **The variants census for the register re-read: 64 test files / 1,734 it-blocks / 1740 green / 124 stories** (the R26 register's 1,613/61 figure is the W1-era mid-flight read — supersede at the next spec re-pin).
- **The public review URL serves the full R24 state** (124 stories; the SKILL #122 story-count gate holds; the annotakit store is live with 0 open review threads).
- **The GitLab mirror PAT (glpat-…) died 401 mid-R24** — re-issue before the next mirror push; GitHub + the /home/sync bundles carried durability alone.
- **Next for the variants track:** the user's reaction round on the R24 state (the 14 resolutions + the rebuilt color/FX/deliver compositions); the registered residue list (the Clip context-menu Add-Transition mixed-selection mint; the de-esser/aux polish rows; the r23-analysis re-triage); the 62 legacy open threads.


