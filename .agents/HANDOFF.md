# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-09, end of the R25 round (the DaVinci edit-mode completeness + the reference register + the filings executed). **Read this FIRST; everything below the `-----` divider is R18-era serving/environment detail that is still ACCURATE as background (env layout, :3000 ownership, annotakit kit, mirror repo) but predates R19-R25.**

## Current state (R25 — the ten-mode matrix is law; the four absent families are spec-first; the execution continues)

- **The user's R25 asks are all MET (the integration-review verdict: PASS):** the multi-pass audit ran (34 dispatches across 6 waves — 5 module scouts → 10 one-per-mode auditors → 5 cross-cuts → 2 adversarial ruling reviewers → 6 amendment agents → 5 verification agents + the battery builder + the integration reviewer); the seam updates were scouted at all five repos (the app landed ALL its D30 waves — 206/206, carriers 7→5 at the declared irreducible set; engine/OT/WDC/nle-ui code-sealed); the two DaVinci mocks were deeply analyzed — **the answer to "did we have all these specified?" is NO: REPLACE / APPEND-AT-END / RIPPLE-OVERWRITE / FIT-TO-FILL were absent corpus-wide** (now spec-first, r1-scheduled); the two shell mocks were harvested into the REFERENCE-REGISTER (19 families; the C33-C58 intake complete).
- **The rulings** (`audits/ARCH-R25-edit-mode-completeness.md` v2, adversarially reviewed — 15 + 5 folds): **D30** the mode-matrix law (the 10-mode census in 06 §0; the landed 24+6 wire census NOT reopened — the new verbs are r1-scheduled rows); **D31/31A** the source-edit family restructure (placement-insert vs insert-edit named as distinct ops; the mid-clip split law; 06 §5.9B-F the five family contracts; the dedicated-vs-composite criterion); **D32** the linked-companion law (06 §5.0's 10-mode fan-out table; relink-both-halves; the E1 defect row); **D33** the visual-grammar register (05 §8A structural + 18 §9's 8 tokens; the mocks' green pinned); **D34** the keyboard source-edit family (16 §3.4A/§3.4B: `,` `.` `⇧⌥.` `E` primaries + the F9-F12 alternates + the four-meaning row); **D35** the reference register + adoption tracking; **D36** the execution wiring (the plan's post-D30 re-write + the three cross-repo filings).
- **The R25 pin world (battery-enforced):** engine `3989506` 458 (docs-only over the R24 code state) · OT HEAD `fdb771c` (**code pin `c15a629` UNCHANGED** — src diff empty) 536 · WDC `85b81b0` 759 (zero movement) · nle-ui `3026099` 674 (shell/keymap only, the gesture surface byte-identical) · app `64fb0ab` 206 (D30 complete) · mini 355 sealed · variants 1521+ (1,588 it-blocks). Consumer pins unchanged (OT `c15a629` byte-lock everywhere). **battery_r25 123/123** (the standing battery — run it after every spec round; it includes the mode-matrix/grammar/keyboard/register classes + the census-untouched invariant).
- **The three cross-repo filings LANDED at their venues this round (the first round they actually did):** WDC root-HANDOFF waveform promotion @ `82b3ea9`; engine R9-disposition amendments + the E1/E2/E3 defect filings @ `275bb9d`; app census-CI mechanism block @ `24e1fff`.
- **A P1-class defect is filed and unfixed:** the engine's linked mid-clip insert overlap (`_performInsertEditImpl` Phase 2 — verified live twice; the fix contract + the 4-postcondition pin are in the engine's queue; it is the r1 port's precondition).

## Next session scope (immediate — THE EXECUTION continues, now with the filed work orders)

1. **The engine's E1 fix is the highest-value single item** (P1, pre-r1): the linked mid-clip insert defect — the fix contract is filed in `nle-engine/.agents/PLAN.md`'s E1 block (exclude target-track clips with `from < insertFrame` from the linked expansion; split straddling companions; the 4-postcondition fixture). E2 (the overwrite companion asymmetry) rides the same session. **This is spec-adjacent work in the ENGINE repo — coordinate per the cross-repo protocol (the engine session executes; the spec session verifies + re-pins).**
2. **S-app's queue (the plan's S-app row):** the `.agents/` bootstrap (OVERDUE ×2) + the README refresh; the census-CI execution (the mechanism block is FILED in the app's worklog tail — author `scripts/census-check.mjs` + the register doc + the ci.yml step + the mutation gate); K3's store/policy halves (unblocked, unstarted); the maintainPitch residual (`engineService.ts:477/:486`); the mode-matrix K3 rows.
3. **S-wdc's queue:** the waveform-contract promotion EXECUTION (the filing is item 0 in the root HANDOFF — the typed surface + the pinning test, 759→; it unblocks K3's audio half).
4. **S-spec's standing items:** battery_r25 after every round; the register maintenance (D35.2's per-row pin counts — battery_r26's natural extension; the C-ledger intake; rows flip LANDED as owning rows land); the register's rows 3/4 flip-cells partial (the 16/09 halves + the 15 §13.15 exportMaster.range pointer pending); the K2 census registration at K2 entry.
5. **User-gated decisions to surface when the user next appears:** **the r1-entry absent-family scope gate** (the four mode families are spec-first; implementing all four at r1 adds +1.5-3 wk solo — the user can trim the scope); D6 re-affirmation (r4 real media decode); the nle-test-app → nle-app rename (recommended, non-blocking); the seal-snapshot exercise (D28 — only at the r1-entry/packaging gate); the D25.2 retirement confirmation.
6. **Process reminders:** fetch-before-push (the sibling races recurred twice at R24 — merge-first); never force push; the census law is standing (a carrier without a rationale is a violation; the landed 24+6 census is NEVER reopened by spec rounds — new verbs ride r1 rows); the mode-matrix completeness law (§2A.8 — zero orphan rows at K2).

## Standing laws (battery-enforced)

- The posture law (D20), the pin world (D21), the drag law (D22), the single-tree law (D25 + the D26 amendment), the plan-executability law (§2A.6), the census law (§2A.7), the productization gate (D28.2) — all as at R24.
- **NEW §2A.8 — the mode-matrix completeness law:** the 10-mode matrix in 06 §0 is the edit-mode family's register; at K2 every row is LANDED or carries a live r1-scheduled row with owner+acceptance (zero orphans); at K3 the app column matches the landed set with pins.
- **NEW §2A.9 — the reference-register law:** REFERENCE-REGISTER.md's test-pin counts re-derive live every round (drift without amendment = battery failure); every C-proposal is dispositioned (adopted/queued/declined — none ages untracked).

-----
## What is LIVE right now (the headline — R18 layout; still accurate)

- **The public preview URL IS the full Storybook dev server (R18, user directive):** `https://preview-chat-<chat_id>.space-z.ai/` (the hostname embeds THIS chat's id from the gateway metadata) → edge → Caddy :81 → localhost:3000 → **`storybook dev -p 3000`** (`scripts/sb3000.py`, double-fork daemon, PPID=1, `core.allowedHosts: true`). Same-origin means everything works through the real edge: manager UI, story tree, story selection, iframe canvases, `/index.json` (30 stories), HMR, deep links (`/?path=/story/…`). Verified end-to-end post-switch (browser pass: story DOM renders, zero page errors) + a live kill→restore→public-200 cycle.
- **The app is the localhost dev surface on :3001** (`scripts/dev3000.py`; `vite.config.ts` port 3001) — for the agent dev loop only, NOT public.
- **Pin-comment review surface LIVE (R18b):** storybook-annotakit v0.5 is vendored at `vendor/storybook-annotakit/` (dist tracked — boots without building), FIRST in `.storybook/main.ts` addons. At the public URL the toolbar carries Pin (⌥C) / Region (⌥R) / Threads (⌥D) / Hide (⌥L), and reviewer pins are same-origin REST through the edge — verified live (thread created via the public URL's own API, visible in the manager UI, drawer badge, delete → tombstone → orphan-branch push → GH issue closed). Store: `.git/annotakit/threads.db` (branch-switch-proof) → orphan `annotakit` branch on GitHub — **SHARED with the sibling stream's env** (their `db=annotakit@shell-variants` commits appear in the branch log; the kit's logical merge reconciles both) — plus a 1:1 GitHub-issue mirror. Agent surface: `GET /annotakit/api/health` → agentSurfaces (rest + digests + github); `GET /annotakit/api/threads`, `/export?format=md`, POST/DELETE threads, POST `/sync`. Token: `.env` in shell-mini (gitignored — **recreate after a recycle**: PAT from chat + `ANNOTAKIT_GH_REPO=melodietexoss/shell-mini-review` + `ANNOTAKIT_GH_LABEL=mini` + `ANNOTAKIT_GH_SCOPE=4c1120aa|src/(timeline|shell)/` — see the R18h bullet).
- **R18e/R18f — the feedback wave (annotakit issues #7-#16, ALL fixed + thread-resolved):** ripple edit (toggle, delete/end-trim/start-trim follower-shift laws, snapshot-idempotent previews, delta-quantize + floor overlap guard); RH cut styles `[`/`]` (裁剪开始/裁剪结束, ripple-aware).
- **The sibling streams own their repos**: nle-engine / opencut-timeline / web-daw-core / nle-ui / nle-test-app all have live `.agents/` docs + worklogs — read THEIR HANDOFFs before touching their domains; file cross-repo work as queues, never unilaterally.

-----

## R23.5 — the stale-serving ops fix (2026-09-07 evening, after R23's close)

**Incident:** the user reported the public review URL showed NOTHING from R23. The container had recycled; boot-restore relaunched the daemon from the runtime copy, which was still the R22-era tree (R23 never reached it — the previous session's runtime sync died with its container). GitHub was at f63b15c; the runtime served 117 stories (R23 = 123), no FX files at all.

**Fixed this session (commit 5578391 + the boot-restore patch):**
- Runtime re-synced from origin/main (rsync, runtime-only state protected: .git/annotakit store, .env, node_modules, dist, logs). Live now: 123 stories, 8 FX stories, page switcher Edit/Color/Audio/FX/Deliver, zero console errors, VLM-verified FX composition, edge probe 200.
- The 4 absolute-path self-loop symlinks (r23-analysis/shots/{color,mixer,pages,shell-components} → /home/z/my-project/...) REMOVED from git — they ELOOP-crashed vite's watcher when synced into the runtime (the crash appeared as "Storybook ready" then death).
- boot-restore.sh rewritten: repo restore → best-effort GitHub fast-forward (PAT from .env, never logged) → stamp-gated CODE-SYNC repo→runtime (ELOOP guard, deps-drift npm ci, runtime-only state protected) → health-exit → relaunch. A recycle now self-heals instead of resurrecting stale code.
- /home/sync bundle refreshed (newest nle-core-spec-*.bundle wins the boot restore).
- Pushed: GitHub 5578391 + the follow-up commit; GitLab synced (ansgareutychisO/nle-core-spec — NOTE: the mirror namespace is ansgareutychisO, not the GitHub org).

**Verification law (SKILL #122):** the wrap gate is the live index.json story count vs the repo's, probed through the public URL — not the jsdom suite.
