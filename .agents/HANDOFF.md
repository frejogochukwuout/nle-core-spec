# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-08, end of the R24 round (the spec-track absolute-finality audit). **Read this FIRST; everything below the `-----` divider is R18-era serving/environment detail that is still ACCURATE as background (env layout, :3000 ownership, annotakit kit, mirror repo) but predates R19-R24.**

## Current state (R24 — the spec + plan are ABSOLUTELY FINAL at the R24 pins; the execution continues)

- **The user's R24 asks are all MET** (the integration-review verdict): the three-lens audit ran (5 module ground-truth cards + the 20-agent per-file fleet + the plan/track cold-executor audit); the core modules' collective seam-finalization is reflected (OT's W11 wire-dispatch round — 24 routed verbs + 6 exceptions, the M49C coverage gate — now the plan's K2/K3 instruments per D29); the adopt-wholesale timeline strategy is RULED and in the plan (D26); the topology decisions are RULED (D27 keep nle-ui + keep the app, cloudcut dispositioned; D28 evolve-in-place + the seal-snapshot option at the r1-entry/packaging gate).
- **The rulings** (`audits/ARCH-R24-timeline-strategy-and-topology.md` v2, adversarially reviewed — 17 findings folded): **D26** adopt-wholesale convergence — the app's `timeline-port/` is a census-governed converging mirror (at OT `c15a629`: 40 files = 39 mirrors — 32 zero-action + 7 documented carriers — + the EngineMount host; register-equality is the law, CI-enforced app-side + battery-re-verified; D25.2's two-path vendoring is RETIRED as mechanism; the props list converts to the carrier-reduction program, gating nothing); **D27** the repo topology locked; **D28** evolve in place; **D29** the W11 instruments (M49C = K2's gate; the app's D30 W-F coverage-gate port = K3's routed-verb machine-check, shell-path blind spot compensated via engineService pins; the wire census 24+6; the data-test stability contract; the waveform-contract promotion into WDC's session queue).
- **The R24 pin world (battery-enforced):** engine `5036387` 458 · OT HEAD `ded43c4` (536 = 386 in-page + 150 real-mouse; **code pin `c15a629`** — consumers pin it) · WDC `85b81b0` 759 · nle-ui `fc4cc35` 674 · app `c885ece` 174 · mini 355 sealed · variants 1521+. Consumer pins: engine→OT `c15a629` + WDC `494f6ff`; app→engine `5036387` + OT-lock-copy `c15a629` (byte-exact) + nle-ui `fc4cc35` + WDC `85b81b0`. **battery_r24 85/85** (the standing battery — run it after every spec round; it includes the STRICT mini scrape + the census-coherence + the routed/exceptions + the typo-class corpus-wide).
- **The mockup streams are ACTIVE siblings** (merge-first always): the mini sealed at 355; the variants at 1521+ (their R23.5 ops round landed the boot-restore serving fix). The VLM net is the crawl's visual-verification tooling — adopt, don't fork.

## Next session scope (immediate — THE EXECUTION, per IMPLEMENTATION-PLAN §0)

1. **S-app's D30 completion is the critical path** (the plan's S-app row): step (0) create the app's `.agents/` (HANDOFF + PLAN minimum — OVERDUE, before W-C: the cross-repo lockstep needs it); steps (1) the app's OWN `docs/design-r9-d30.md` waves W-C..W-G (the wire-dispatch port + the interlocked five + the coverage-gate module W-F + the CR filings per the D29.5-AMENDED list — the app's W-G R10 list was amended by ARCH-R24 F1 BEFORE it files: transform-sidecars re-filed as future-extension, maintainPitch closed-LANDED, the scene-grade promotion NOT filed (the decline is the law), the CSS-filter CR re-scoped to the open widening); ∥ (2) K3's store/policy halves (the useMini-family re-expression — CORE-SEAMS §2 + LAW-NET Part A; unblocked NOW). Entry: `/home/z/my-project/nle-test-app` (README + worklog tail until `.agents/` exists).
2. **The five cross-repo filings the plan orders** (each a titled block in the TARGET repo's queue doc, per the cross-project protocol): the OT carrier-reduction work order + the data-test stability contract (S-ot); the WDC HANDOFF waveform-contract promotion (S-wdc — the de-facto getPeaks/setAudioPeaks/computePeaks shape at audio-registry.ts:24-74); the package-side C0 row + the D30 W-D edits + the D29/D30 DECISIONS filings (S-package, in nle-ui); the engine's D29.5 rows (S-engine — the meta-docs lag 3 commits, file against the live tree).
3. **S-spec's standing items:** battery_r24 after every spec round; the C-ledger rolling disposition (`.agents/SPEC-REVISION-CANDIDATES.md` — the ⌘3 collision registration is the open item); the two scope decisions recorded at R24: the key-surface census battery checks were NOT adopted (the 16-agent's suggestion — a scope choice, see the R24 worklog), the K2 census registration still pending at K2 entry.
4. **User-gated decisions to surface when the user next appears:** D6 re-affirmation (r4 real media decode); the nle-test-app → nle-app rename (recommended, non-blocking); the seal-snapshot exercise (D28 — only at the r1-entry/packaging gate, not before); the D25.2 retirement confirmation (an architecture-freeze amendment — done as a reviewed ARCH amendment per the standing protocol; the user may veto).
5. **Process reminders:** fetch-before-push (the sibling races recurred TWICE this round); never force push; the dual phase-vocabulary window is CLOSED (the D24 set only — battery-enforced); the census law is standing (a carrier without a rationale is a violation).

## Standing laws (battery-enforced)

- The posture law (D20): every gap row carries owner + phase + acceptance; BASE rows carry pin+suite+count; the count discipline (declared == actual; the report json / live run is the authority).
- The pin world (D21): HEAD pins AND consumer pins, both classes (battery-checked separately); the plan cites, never re-declares.
- The drag law (D22): the R18k law verbatim is the WHOLE drag law; the retired machinery stays retired.
- The single-tree law (D25 + the D26 amendment): OT's components/ tree is canonical; the app's port is the census-governed converging mirror; register-equality fails builds; reopening any freeze item requires the user or a reviewed ARCH amendment.
- The plan-executability law (§2A.6): every workstream step carries an entry point + a gate; an unexecutable step is a plan bug.
- The census law (§2A.7): the port census is recomputed from the vendored pin vs the port tree (import-specifier-normalized); register-inequality fails the app build; the battery re-verifies at round cadence.
- The productization gate (D28.2): r1-entry, or any consumer outside the six-stream fleet / a distribution request — whichever first; the seal-snapshot option opens there, not before.

-----

## What is LIVE right now (the headline — R18 layout; still accurate)

- **The public preview URL IS the full Storybook dev server (R18, user directive):** `https://preview-chat-<chat_id>.space-z.ai/` (the hostname embeds THIS chat's id from the gateway metadata) → edge → Caddy :81 → localhost:3000 → **`storybook dev -p 3000`** (`scripts/sb3000.py`, double-fork daemon, PPID=1, `core.allowedHosts: true`). Same-origin means everything works through the real edge: manager UI, story tree, story selection, iframe canvases, `/index.json` (30 stories), HMR, deep links (`/?path=/story/…`). Verified end-to-end post-switch (browser pass: story DOM renders, zero page errors) + a live kill→restore→public-200 cycle.
- **The app is the localhost dev surface on :3001** (`scripts/dev3000.py`; `vite.config.ts` port 3001) — for the agent dev loop only, NOT public.
- **Pin-comment review surface LIVE (R18b):** storybook-annotakit v0.5 is vendored at `vendor/storybook-annotakit/` (dist tracked — boots without building), FIRST in `.storybook/main.ts` addons. At the public URL the toolbar carries Pin (⌥C) / Region (⌥R) / Threads (⌥D) / Hide (⌥L), and reviewer pins are same-origin REST through the edge — verified live (thread created via the public URL's own API, visible in the manager UI, drawer badge, delete → tombstone → orphan-branch push → GH issue closed). Store: `.git/annotakit/threads.db` (branch-switch-proof) → orphan `annotakit` branch on GitHub — **SHARED with the sibling stream's env** (their `db=annotakit@shell-variants` commits appear in the branch log; the kit's logical merge reconciles both) — plus a 1:1 GitHub-issue mirror. Agent surface: `GET /annotakit/api/health` → agentSurfaces (rest + digests + github); `GET /annotakit/api/threads`, `/export?format=md`, POST/DELETE threads, POST `/sync`. Token: `.env` in shell-mini (gitignored — **recreate after a recycle**: PAT from chat + `ANNOTAKIT_GH_REPO=melodietexoss/shell-mini-review` + `ANNOTAKIT_GH_LABEL=mini` + `ANNOTAKIT_GH_SCOPE=4c1120aa|src/(timeline|shell)/` — see the R18h bullet).
- **R18e/R18f — the feedback wave (annotakit issues #7-#16, ALL fixed + thread-resolved):** ripple edit (toggle, delete/end-trim/start-trim follower-shift laws, snapshot-idempotent previews, delta-quantize + floor overlap guard); RH cut styles `[`/`]` (裁剪开始/裁剪结束, ripple-aware).
- **The sibling streams own their repos**: nle-engine / opencut-timeline / web-daw-core / nle-ui / nle-test-app all have live `.agents/` docs + worklogs — read THEIR HANDOFFs before touching their domains; file cross-repo work as queues, never unilaterally.

-----

## R23.5 — the stale-serving ops fix (2026-09-07 evening, after R23's close)

**Incident:** the user reported the public review URL showed NOTHING from R23. The container had recycled; boot-restore relaunched the daemon from the runtime copy, which was still the R22-era tree (R23 never reached it — the previous session's runtime sync died with its container). GitHub was at f63b15c; the runtime served 117 stories (R23 = 123), no FX files at all.

**Fixed this session (commit 5578391 + the boot-restore patch):**
- Runtime re-synced from origin/main (rsync, runtime-only state protected: .git/annotakit store, .env, node_modules, dist, logs). Live now: 123 stories, 8 FX stories, page switcher Edit/Color/Audio/FX/Deliver, zero console errors, VLM-verified FX composition, edge probe 200.
- The 4 absolute-path self-loop symlinks (r23-analysis/shots/{color,mixer,pages,shell-components} → /home/z/my-project/...) REMOVED from git — they ELOOP-crashed vite's watcher when synced into the runtime (the crash appeared as "Storybook ready" then death).
- boot-restore.sh rewritten: repo restore → best-effort GitHub fast-forward (PAT from .env, never logged) → stamp-gated CODE-SYNC repo→runtime (ELOOP guard, deps-drift npm ci, runtime-only state protected) → health-exit → relaunch. A recycle now self-heals instead of resurrecting stale code.
- /home/sync bundle refreshed (newest nle-core-spec-*.bundle wins the boot restore).
- Pushed: GitHub 5578391 + the follow-up commit; GitLab synced (ansgareutychisO/nle-core-spec — NOTE: the mirror namespace is ansgareutychisO, not the GitHub org).

**Verification law (SKILL #122):** the wrap gate is the live index.json story count vs the repo's, probed through the public URL — not the jsdom suite.
