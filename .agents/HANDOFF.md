# HANDOFF — Next Session Scope (nle-core-spec)

**Written:** 2026-09-07, end of the R23 round (BOTH tracks: the spec-track separation/fleet + the variants-track FX view + review sweep). **READ THIS SECTION FIRST — everything below the `-----` divider is R18-era serving/environment detail that is still ACCURATE as background (env layout, :3000 ownership, annotakit kit, mirror repo) but predates R19-R23.**

## Variants-track R23 state (the FX/TRANSITION VIEW + the review sweep — COMPLETE, 1595/1595)

- **The user's thrice-iterated FX directive is DONE** (#103/#104/#105): DESIGN-R23 v2 (`.agents/design/r23-fx-view-and-revision.md`, web-researched + 2 adversarial audits + 22 binding amendment rulings in Part IX). THE RULING: **both surfaces, one engine** — the FX page (AppDock 5 pages, ⌘5; FxBrowser left / Viewer center / FxInspector right / the full Timeline in fxMode at 40% mainbody) + the FX tool in the Edit toolbar (Esc exits). Seam/head/tail hit-zones, interactive transition boxes, clip recede, fadeIn/fadeOut model fields, the selectedFxObject domain (7-site law).
- **All six tracks landed** (the 19 issues #90–#108 + #70 + 3 mid-round threads): the color refinements (ScopesDock tabs in the timeline area / nodes-in-viewer / the density toggle everywhere / the Stills Gallery with the clip-level answer IN the UI), the mixer/inspector polish, the per-page shell semantics (leftDockContent table + the D-D2 toolbar matrix), the preview visibility law, the deliver RangeBand.
- **The review sweep (the user's component-at-a-time directive) is the standing instrument**: the VLM net (`scripts/vlm-capture.mjs` + `vlm-review.mjs` + `vlm-run.sh` + `console-sweep.cjs` — 123 stories at the 1280×800 floor; findings in `r23-analysis/`) + the 5 family-review reports (worklog R23-R1..R5) + the fix round (2 P1s + 15 P2s + 4 rulings, `r23-analysis/review-findings.md` is the consolidated ledger). **Adopt the VLM net for every future visual round — it caught real bugs the jsdom suite cannot see (z-order/hit-target collisions, invisible state deltas).**
- **Gates at close:** tsc 0; **1595/1595** (61 files; 1349 → +246 net); vite + SB builds green; live console-sweep 0 errors on all 5 pages; 22/22 threads resolved (GH mirror closed, pending 0).
- **R24 next:** the user's reaction round on the FX view + compositions; the registered-only polish list (`r23-analysis/review-findings.md` — fade-label occlusion, wheel-hue keyboard path, color chrome hex debt, compact-strip playhead follow); the 62 legacy mini-era threads in the shared annotakit db; KEEP the process: design-doc → dual audits → waves → per-wave review → the VLM sweep at wrap.

## Current state (R23 — the plan is SEPARATED, audited, and executable; the execution begins)

- **The user's five R23 demands are all MET** (the integration-review verdict): the implementation plan is its own doc (`IMPLEMENTATION-PLAN.md` at the repo root — NOT a spec); every spec file was audited against the LIVE repos by a 20-agent per-file fleet; the ladder is re-defined (crawl = programmatic verification K1-K4 / walk = the real app with humans w1-w3 / run = the full NLE r1-r6); the timeline-UI bridge question is answered YES (D25); workstreams + parallelism + per-track entry points all answered in the plan's §0.
- **The rulings** (`audits/ARCH-R23-plan-and-bridge.md` v2, twice adversarially reviewed): **D23** the plan/spec separation (spec 14 retired to a redirect stub; the §0 FORWARD INVENTORY posture law stands); **D24** the verification ladder (crawl=K1 module nets / K2 combined nets / K3 app-behavior nets / K4 the automated e2e; walk=w1 grammar+human rounds / w2 media / w3 project; run=r1-r6); **D25** the single-tree bridge (OT's `src/components/timeline/` — 39 files/7,702 LOC, M17-M46-verified, pure React — is THE canonical timeline UI; the app's `timeline-port/` fork retires via props-upstreaming + the two-path vendor extension; the mini grammar lands as the TOKEN half at w1 + the STRUCTURAL half in the crawl window; the gold sample = OT's `/view` runner at mini theme).
- **The fleet re-pinned to the R23 HEADs** (every spec §0 re-verified): engine `b8c6f88` 440 (N2b LANDED `37cdd28`), OT `222532c` 489 (S3/M46 + F1 + M45; the canonical tree), WDC `494f6ff` 759 (W2 soundtouch/varispeed LANDED), nle-ui `85dcf57` 648, app `70e99f0` 117 (W3 JKL). Consumer pins live: engine→OT `a4e971d` + WDC `f446512`; app→engine `4ef0147` (one commit behind) + OT-mirror `a4e971d` + nle-ui `85dcf57`. **battery_r23 75/75** (the standing battery — run it after every spec round; it includes the live mini scrape + the pin-spelling + N2b + seal-note classes).
- **The mockup streams are ACTIVE siblings** (merge-first always): the mini sealed at 355 (R23 seal: CORE-SEAMS + LAW-NET-INVENTORY + otProject); the variants at 1521+ (the R23 W-A FX + W-B color + W-C/D/E waves + the VLM net `scripts/vlm-*`). **The VLM net is the crawl's visual-verification tooling — ADOPT, don't fork.**

## Next session scope (immediate — THE EXECUTION begins)

1. **S-app's K3 store/policy halves — the ONLY critical-path work unblocked NOW** (the plan §0's S-app row, item 1): the useMini-family re-expression as app tests over the real vendored OT, targeting the app's REAL surfaces (the GluedShell store family + `engineService.ts` + `sceneBridge.ts` — CORE-SEAMS §2 is the field-by-field map; LAW-NET Part A §1.2 is the family list). Also: create the app's `.agents/HANDOFF.md` (the fleet convention — the app has none; it becomes the pin-bump notification surface). Prereq: `NLE_GH_PAT` + `npm run bootstrap` (the private submodules). Entry: `/home/z/my-project/nle-test-app`.
2. **File the D25 bridge work order in OT's queue** (the plan §0's S-ot row): a titled block appended to `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol — the PORT-LOCAL props upstreaming (the selection bridge pair, the zoom bridge, `confirmDelete`, `cancelRegistry`, the `timeline-scroll` prop, `EMPTY_CONTEXT_ITEMS` as the bug-fix), the testid-emission convention, the view-config surface, the K2 runner registration, then the structural grammar half. This is the largest single crawl item (2-4 wk incl. OT's review cycle). Entry: `/home/z/my-project/opencut-timeline`.
3. **The standing parallel streams all have live first-actions** (plan §0): S-engine's consumer re-pin wave + the projector parity corpus + the color instruments; S-wdc's 4 deferred test ports + the mixer surface + the waveform seam contract (K3's audio half hard-prerequisite); S-package's C0 MiniShell; S-spec's K2 census registration (at K2 entry) + the C-ledger rolling disposition.
4. **User-gated decisions to surface when the user next appears:** D6 re-affirmation (r4 real media decode — the run total's conditional row); the nle-test-app → nle-app rename (still recommended, still non-blocking); the variants-count central reconciliation (1,470 pin-era vs 1521+ live — the sibling's wrap owns the exact figure).
5. **Process reminders:** fetch-before-push (the sibling races are recurring — this round had 5 merge cycles); never force push; battery_r23 after every spec round; the dual phase-vocabulary window (old C/W/R tags + new K/w/r tags both accepted) TIGHTENS to the D24 set at the next fleet close.

## Standing laws (battery-enforced)

- The posture law (D20): every gap row carries owner + phase + acceptance; BASE rows carry pin+suite+count; the count discipline (declared == actual at the pins; the report json / vitest output is the count authority).
- The pin world (D21): HEAD pins AND consumer pins, both classes (battery-checked separately); the plan cites, never re-declares.
- The drag law (D22): the R18k law verbatim is the WHOLE drag law; the retired machinery stays retired; OT-SEAMS tombstoned rows re-open ONLY by user request.
- The single-tree law (D25): OT's components/ tree is canonical; the app fork is retiring; reopening either freeze item requires the user or a reviewed ARCH amendment.
- The plan-executability law (§2A.6): every workstream step carries an entry point + a gate; an unexecutable step is a plan bug.

-----

## What is LIVE right now (the headline — R18 layout; still accurate)

- **The public preview URL IS the full Storybook dev server (R18, user directive):** `https://preview-chat-<chat_id>.space-z.ai/` (the hostname embeds THIS chat's id from the gateway metadata) → edge → Caddy :81 → localhost:3000 → **`storybook dev -p 3000`** (`scripts/sb3000.py`, double-fork daemon, PPID=1, `core.allowedHosts: true`). Same-origin means everything works through the real edge: manager UI, story tree, story selection, iframe canvases, `/index.json` (30 stories), HMR, deep links (`/?path=/story/…`). Verified end-to-end post-switch (browser pass: story DOM renders, zero page errors) + a live kill→restore→public-200 cycle.
- **The app is the localhost dev surface on :3001** (`scripts/dev3000.py`; `vite.config.ts` port 3001) — for the agent dev loop only, NOT public.
- **Pin-comment review surface LIVE (R18b):** storybook-annotakit v0.5 is vendored at `vendor/storybook-annotakit/` (dist tracked — boots without building), FIRST in `.storybook/main.ts` addons. At the public URL the toolbar carries Pin (⌥C) / Region (⌥R) / Threads (⌥D) / Hide (⌥L), and reviewer pins are same-origin REST through the edge — verified live (thread created via the public URL's own API, visible in the manager UI, drawer badge, delete → tombstone → orphan-branch push → GH issue closed). Store: `.git/annotakit/threads.db` (branch-switch-proof) → orphan `annotakit` branch on GitHub — **SHARED with the sibling stream's env** (their `db=annotakit@shell-variants` commits appear in the branch log; the kit's logical merge reconciles both) — plus a 1:1 GitHub-issue mirror. Agent surface: `GET /annotakit/api/health` → agentSurfaces (rest + digests + github); `GET /annotakit/api/threads`, `/export?format=md`, POST/DELETE threads, POST `/sync`. Token: `.env` in shell-mini (gitignored — **recreate after a recycle**: PAT from chat + `ANNOTAKIT_GH_REPO=melodietexoss/shell-mini-review` + `ANNOTAKIT_GH_LABEL=mini` + `ANNOTAKIT_GH_SCOPE=4c1120aa|src/(timeline|shell)/` — see the R18h bullet).
- **R18e/R18f — the feedback wave (annotakit issues #7-#16, ALL fixed + thread-resolved):** ripple edit (toggle, delete/end-trim/start-trim follower-shift laws, snapshot-idempotent previews, delta-quantize + floor overlap guard); RH cut styles `[`/`]` (裁剪开始/裁剪结束, ripple-aware).
- **The sibling streams own their repos**: nle-engine / opencut-timeline / web-daw-core / nle-ui / nle-test-app all have live `.agents/` docs + worklogs — read THEIR HANDOFFs before touching their domains; file cross-repo work as queues, never unilaterally.
