# IMPLEMENTATION-PLAN — the executable plan (separate from the spec set)

**What this is:** THE plan — the execution runbook for the NLE project. It is deliberately NOT a numbered spec: the spec set (`00`-`20`) owns WHAT and the acceptance criteria (each spec's §0 FORWARD INVENTORY — BASE pinned / GAP with owner+acceptance / TEST PLAN, the R22 posture law); THIS doc owns HOW/WHEN/WHO/WHERE — workstreams, sequencing, parallelism, entry points, gates, estimates, protocol.
**Ruling basis:** `audits/ARCH-R24-timeline-strategy-and-topology.md` (v2, adversarially reviewed — rulings D26 the adopt-wholesale convergence / D27 the repo topology / D28 evolve-in-place + the productization gate / D29 the seam-finalization instruments; D23 plan separation + D24 the ladder stand, D25's mechanism amended per D26 — the lineage in §7). Supersedes spec 14's plan content (that file is retired to a redirect stub; the P→A→C/W/R→K/W/R lineage lives in §7).
**Stage vocabulary (D24):** **CRAWL** = the programmatic-verification layer (no human eyes; K1 module nets → K2 combined nets → K3 app-behavior nets → K4 the automated e2e exit). **WALK** = the real app wiring with humans (w1 grammar+fidelity+human rounds, w2 media, w3 project). **RUN** = the full NLE at DaVinci/web-DAW depth (r1-r6). The stages label VERIFICATION DEPTH — code lands continuously in every stream; a stage's exit gate is what sequences.
**Crawl's purpose (the user's law):** eliminate as many issues as possible before the walk — "ideally everything can continue to be tested programmatically"; human-required gates are registered below WITH reasons.

---

## 0. HOW TO EXECUTE — the entry points (start here)

**If you are starting work:** identify your track in the table, open its repo, orient per its row's ORIENTATION cell (the fleet convention is the target repo's `.agents/HANDOFF.md` + `PLAN.md` — exceptions noted per row; WDC keeps its handoff/plan at the repo ROOT), read this doc's §3 track section, execute the FIRST ACTION, and let the GATES tell you when the step is done. **Environment prerequisites (all repo rows — the R24 cold-executor audit, verified live per repo):** the app's private vendor submodules need `NLE_GH_PAT` + `npm run bootstrap` (README: PAT → non-recursive submodule init → install → annotakit build) before any gate runs; **OT** runs `bun install` AND starts its dev server FIRST (`bun run dev` → :3001 — `bun run test` drives Playwright against `http://localhost:3001/` and does NOT self-start it; Playwright's chromium headless shell must be present — `bunx playwright install chromium`; plain `npx playwright install` rewrites package.json pins + creates package-lock.json, revert both); **WDC** runs `bun install --frozen-lockfile` (its drift gate additionally needs an upstream web-daw clone, `sync --check --source <clone>` — none pending, the lock is clean); **nle-engine** runs `npm install` + `git submodule update --init` with PAT remotes (the SKILL PAT-URL recipe — else the live-run venue is GHA CI); **nle-ui** runs `npm ci` from the committed lockfile (the Storybook build rides CI). Every step ends with: gates green → commit → push → (if module repo) notify the app for the pin bump (the file-queue protocol: a titled block in the target repo's queue doc — for OT, append to `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol: source session, item list, acceptance per item). **A step with no testable gate is not a step — write the gate first.**

**Taxonomy (one sentence):** a TRACK is a repo-owning stream (S-ot/S-app/S-engine/S-wdc/S-package/S-spec — the standing teams); a PHASE is a ladder rung (K1-K4 crawl / w1-w3 walk / r1-r6 run — the sequencing); a WORKSTREAM is a track's worklist slice through the phases (§3).

| Track | Repo (entry dir) | First action (the crawl window's work order) | Inputs (what you need before starting) | Gates (per step) | Done-state (this window) |
|---|---|---|---|---|---|
| **S-ot** | `/home/z/my-project/opencut-timeline` (orient: `.agents/`) | **The carrier-reduction work order** (D26.3 — ONE S-ot work order, filed at S-ot execution in the crawl window as a titled block in `opencut-timeline/.agents/PLAN.md`, the cross-project queueing protocol; **the D25.2 "props upstreaming as the bridge prerequisite" framing is RETIRED — this gates NOTHING, it rides ∥**): (1) **the carrier-reduction program** — upstream the app port's divergence carriers as OPTIONAL injectable props: the selection-bridge pair (`initialSelectedIds`/`onSelectionChange`/`selectionIds`), the zoom bridge (`zoom`/`onZoomChange`), `confirmDelete`, the `timeline-scroll` anchor, `EMPTY_CONTEXT_ITEMS` (the stability bug-fix), `cancelRegistry` injection (TimelineView mints its own today) — additive-optional, classic defaults M-pinned; **each OT landing flips an app carrier toward exact** (census-tracked); cross-reference D30 W-G's R10(h)-(j) as the app's SEPARATE asks (lock/add-track affordance verification, the upstream-back candidates, the insertBatch endorsement) — never duplicate; (2) **the data-test stability contract** (D26.5): freeze OT's 68-site `data-test=` convention AS the stability contract + close the census gaps — **NOT a new `data-testid` layer** (OT emits zero of those); (3) **the view-config surface** (zoom-ladder config + ripple-toggle exposure — the C1 OT-side halves, verified still-absent at `c15a629`); (4) **K2's in-page runner registration** (the runner + the M49C coverage gate EXIST and are green 24/24 at the pin — the registration is the filing as K2's OT-side completeness instrument, not the building); (5) the **structural grammar half** (crawl-tail: the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas — additive components, classic defaults M-pinned); (6) **the token half at w1-entry** (the mini theme mode on `globals.css` `:root` + `theme.ts` — D25.3a; the `/view` gold sample is contingent on both halves) | this doc + `audits/ARCH-R24-timeline-strategy-and-topology.md` (D26/D29) + the app's port census (the carrier list: `nle-test-app/docs/audit-port-convergence-r9.md` + `UPSTREAM.lock.json`) | `bun run dev` (:3001) THEN `bun run test` (536→, the M-report json, 59 entries incl. M49C), `tsc --noEmit`, the real-mouse suites; every prop/contract lands with new M-pins for every classic default | the carrier-reduction landings flip app carriers toward exact (census-tracked); the `data-test` stability contract frozen + its census gaps closed; the view-config surface + the structural components exist additively; the mini theme mode exists at w1 |
| **S-app** | `/home/z/my-project/nle-test-app` (orient: `README.md` + the `worklog.md` tail — the app has NO `.agents/` TODAY (verified at `c885ece`); the README's gate-count line is stale, this row's GATES cell carries the live counts) | (0) **create the app's `.agents/`** (HANDOFF + PLAN minimum, the fleet convention — **OVERDUE per D27.2, BEFORE W-C**: the cross-repo lockstep (the W-D nle-ui edit + re-pin) needs the notification surface; it becomes the pin-bump surface); (1) **complete the app's OWN D30 waves W-C..W-G** (`docs/design-r9-d30.md`, in flight — W-A/W-B landed: the wire-dispatch port (use-wire-dispatch NEW + the interlocked five + EngineMount's wire mint + `engineService.attachWire` + the coverage carry), W-D (keybindings 3W + the ONE nle-ui package commit + re-pin), W-E (the loop bridge + Save/Load + Z2), W-F (**the coverage-gate module** — K3's routed-verb machine-check + the per-op echo pins + Z6), W-G (the CR filings per the D29.5-amended list + the wrap)); (2) **K3's store/policy halves ∥ NOW** (still valid — author against the injection-point contracts): the useMini-family re-expression as app tests over the real vendored OT — **the re-expression target = the app's real surfaces** (the GluedShell store family + `engineService.ts` + `sceneBridge.ts` — CORE-SEAMS §2 is the field-by-field map; each LAW-NET family's tests land in the app's vitest beside the surface they pin) **+ the census register** (D26.2: the port census as a CI-gated app-side script — the vendored pin vs the port tree, import-specifier-normalized, register-inequality fails the build — + the named register doc declaring the per-pin values, currently 40 files = 39 mirrors (32 zero-action + 7 carriers) + the host, `use-wire-dispatch` pending W-C; the battery re-verifies by the same recomputation, never hardcoded constants); (3) **K3's DOM-structural halves AFTER the data-test mapping row + W-C** (D26.5's three-way vocabulary: OT `data-test=` for the timeline units / nle-ui's `shell-*` for the chrome units / the structural-gap worklist for the no-counterpart units; the mini's 59-static + 15-templated census gets the mapping row — mini-unit → target vocabulary + name → present/absent/structural-gap) — **the vendor-extension step is GONE** (never built; superseded by the census discipline) and **the fork-retirement gate = the carrier count reaching the D30-declared irreducible carrier set** (the merge carriers OT's injection points cannot absorb + the 2 documented port-local keeps + the host — tracked by the census, not a file-deletion event); (4) **K4** (the e2e — drives the same routed verbs, the shell-path exception applies); (5) **K3's audio pins + the VLM net port** (the waveform contract's consumer side — the offline-render + call-spy pins authored against S-wdc's stated contract; adopt `ui-mock/shell-variants/scripts/vlm-*` onto the app's `.storybook` + OT's runner) | this doc + ARCH-R24 (D26/D27/D29) + `docs/design-r9-d30.md` (the wave plan) + `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` (the acceptance lists) + `CORE-SEAMS.md` (the seam map) + `NLE_GH_PAT` + `npm run bootstrap` (the private submodules) | the app's vitest (174→), `tsc`, the boundary script, `vite build`, the census script (CI-gated once written) | `.agents/` exists; the D30 waves landed (the coverage gate green; the census at its irreducible target); the K3 corpus row-by-row green (322 authored + 33 projection pins); the audio pins + the VLM net live; **K4: the automated e2e green, zero mock paths** |
| **S-engine** | `/home/z/my-project/nle-engine` (orient: `.agents/` — NOTE: its meta-docs lag the last 3 commits (W2.5/R8-REV/R9-W-A unrecorded in `.agents/`); the fresh D29.5 rows file against the live tree) | The live queue: (0) **the consumer re-pin wave is DONE** (the app pins engine `5036387`, zero lag — R9 W-A landed; N2b/W2/SoundTouch offline pitch stay consumed, not queued); (1) **the D29.5 dispositions** (the app's W-G CR list, amended BEFORE it files): the CSS-filter widening stays **OPEN** (only the Gaussian-Blur subset landed @ `8a0b7fe` + the `4b2dfd1` radius clamp — Vignette/Glow/LUT-preview v1 remain, owner S-engine); the transform sidecars stay the documented **FUTURE-EXTENSION** (re-filed, not a near CR — composition-frame.ts:70-94 carries the slots); `maintainPitch` **CLOSED as landed** (scene-to-segments.ts:246 — the residual is the app's own toggle wiring, app-owned); the scene-grade promotion **DECLINED at the engine seam by design** (the consumer-side final pass; recorded spec-side, not an engine row); (2) the projector parity corpus (the N1 composition-frame family's formal S4 suite — now incl. the W2.5 effects sidecar + `buildElementFilterString`); (3) the color instruments (scopes/qualifier/power-window — spec 08 §0's S-engine row; still queued, zero engine code at the pin; the 4-6 wk estimate is the ARCH-R22 reviewed register); (4) **K2's engine-side pairs**: OT-ops→engine-decode round-trips at the app's seam shapes (extends `tests/vitest/engine/timeline-edit-ops`/`video-sync`) | the queue in the engine's `.agents/PLAN.md` + spec 01/03/04/08 §0s (no D30/R10 section exists engine-side yet — the W-G filing creates it) | the engine's vitest (458→), `tsc`, the vendoring sync-check | the K2 engine-side pairs green; the CSS-filter widening + the parity corpus + the instruments' enumerated milestones each green as queued |
| **S-wdc** | `/home/z/my-project/web-daw-core` (orient: the repo-ROOT `HANDOFF.md` + `PLAN.md` — NOT `.agents/`) | The live queue (the S4 seal round is CLOSED — 759/759, P3-only, zero upstream drift; the SoundTouch offline pitch + varispeed W2 LANDED @ `de09c93`, consumed): (1) **FIRST (D29.4, REQUIRED — K3's audio half HARD prerequisite, currently ABSENT from WDC's own queue — the sharpest spec↔repo mismatch the module cards found): promote the waveform-data seam contract from PLAN's parking lot (PLAN.md:150-152) to the HANDOFF next-session scope + STATE the contract** (the de-facto `getAudioBuffer`/`setAudioBuffer`/`getPeaks`/`setAudioPeaks`/`computePeaks(buffer,buckets) → {min,max,buckets}` shape at `audio-registry.ts:24-74` + the offline-render/call-spy pin shapes the app authors against) **+ pin it**; (2) the mixer surface (spec 20 §0's M2 remainder — EQ/reverb/sends/inserts/sidechain/PDC; design round FIRST, the F6 el.effects-sidecar decision included); (3) the **FIVE** deferred upstream test ports (the label says "4" — an off-by-one carried in WDC's PLAN/HANDOFF and spec 20 §0:21; 5 bodies: engine.test, engine-metronome-automation, engine-param-channel, dsp-aux-sends, wam-effect — nle-engine-side ports onto scene-mixer); (4) the async pre-retime (design-w2 §4; the parallel stream is ACTIVE on nle-engine); (5) the JKL R4 residue (⇧K dead key in the engine world — a P3 doc item) | the WDC root HANDOFF + spec 20 §0 | WDC's vitest (759→), `tsc`, the drift-gate (fails-on-drift; needs an upstream web-daw clone — none pending) | the waveform contract stated + pinned (HANDOFF next-session + spec 20 §0); the mixer + the five ports green as queued |
| **S-package** | `/home/z/my-project/nle-ui` (orient: `.agents/`) | (1) **C0 MiniShell** (the w1-prep chrome family: Topbar/Inspector frame/MediaPool frame/Splitter+R18j laws/Toast role=alert/Viewer frame + tokens/qc- CSS; slots-compatible; package-owned minimal placeholder; zero engine imports — the boundary script); (2) **the filing-gap actions (D27.1 — close them THIS window)**: file C0 in nle-ui's OWN PLAN (no row exists today — the spec's "filed in nle-ui's PLAN queue" expectation is unmet); file the app's D30 W-D package edits PACKAGE-SIDE (currently queued app-side only, `docs/design-r9-d30.md` W-D: R6 the `r` yield-set, Z5 the Inspector link toggle, Z7 the StatusStrip truth rows + the storage prop, the cheat-sheet truth rows, the Viewer scrub-row `data-transport` attr) — the cross-repo queue protocol demands it; close the DECISIONS ledger gap (D29/D30 never filed package-side — the ledger ends at D28); tick the landed-but-unticked OT S-round rows (P-widen + P-lock-route both landed at `fc4cc35`); (3) W-ops→r1 keymap surfaces later | `ui-mock/shell-mini/docs/RH-skin-extraction.md` + the mini's components + spec 18 §0 | the package's vitest (674→), `tsc`, the boundary script, the Storybook build (rides CI) | MiniShell renders in the package Storybook with the chrome laws green; the C0 row + the W-D package edits + the D29/D30 DECISIONS entries filed package-side |
| **S-spec** | `/home/z/my-project/nle-core-spec` (this repo) | (1) **battery_r24's core FIRST** (the successor to battery_r23 — the R24 pin world + the census-coherence checks + the routed/exceptions check + the plan-gate re-points per ARCH-R24's D26 consequences: drop the retired-law checks (the vendoring-mechanics check), re-point the ordering-law/decision-count/seal-census checks to the amended law; the fleet's rounds gate on it; the full extension is §6 step 4); (2) **the K2 combined-family census + spec 12/17 registration at K2 entry** (STILL PENDING — engine-in-the-middle: `timeline-edit-ops`/`video-sync`/`bridge-seams`/`planner`; WDC: `audio-integration`/`dsp-bounce-parity`/`dsp-effects-integration`/`nle-audio-core-derisk`; OT: the 386 in-page + 150 real-mouse runner incl. the M49C gate — battery-checked thereafter); (3) the C-ledger rolling disposition (`.agents/SPEC-REVISION-CANDIDATES.md` §C) + the round's fold of the per-file fleet reports; (4) **the R24 fleet is DONE (this round)** — §8 records it | this doc + ARCH-R24 | the battery (r24 core, then full) after every round; the sibling-race merge-first protocol | battery_r24 green; the K2 census registered at K2 entry; the R24 fleet folded through the integration review |

**The single global entry point:** this table. When in doubt: S-app row → step (0) then step (1) (the `.agents/` surface + the D30 wave completion — the critical path's head; step (2)'s K3 store/policy halves ride ∥ immediately against the injection-point contracts).

---

## 1. The fleet (cited, not re-declared)

The pin world lives in `00-master-spec.md`'s fleet table (D21 — HEAD pins AND consumer pins, both classes; the battery enforces coherence; 00-master is the canon). Current reference set (2026-09-08, the R24 re-baseline; **battery_r24 re-baselines the pin world once, centrally, after the fleet**): engine `5036387` (458) / OT HEAD `ded43c4` (536; **code pin `c15a629`** — the last three commits are docs/runner-artifacts only, src diff empty; consumers pin `c15a629`) / WDC `85b81b0` (759) / nle-ui `fc4cc35` (674) / app `c885ece` (174) / mini 355 (sealed) / variants 1521+. **Consumer pins (live):** engine→OT `c15a629` + WDC `494f6ff` (docs-only behind `85b81b0`); app→engine `5036387` + OT lock-copy `c15a629` (byte-exact minus `testing/`) + nle-ui `fc4cc35` + WDC `85b81b0` (opportunistic). **This plan never re-declares pins — it cites them.**

## 2. The stage ladder (D24) — exit gates + gate classes

Every gate is tagged **[P]** (programmatic — the default; nets, pins, scrapes) or **[H:reason]** (human-required — the registered exception, only at walk for the classes the user named).

### CRAWL — the programmatic-verification layer

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **K1 module nets** | each repo's own suite, extended per its spec §0 GAP acceptance rows (the R24 state, live-verified by the module cards: engine 458/458, OT 536/536 incl. the W11 milestone extensions — M44-M49 + M49T/H/R/G + the M49C gate, 59 entries —, WDC 759/759, nle-ui 674/674, app 174/174) | every repo's suite green at its own gates; every §0 GAP row's owner-repo carries the row's pin | [P] |
| **K2 combined nets** | the named cross-module family: engine-in-the-middle (OT-ops × engine composition), WDC's integration family, OT's in-page UI-over-core runner (pixel reads + real-mouse) — censused + registered in spec 12/17 (the registration is S-spec's pending row); **OT's in-page runner + the M49C coverage gate ARE K2's OT-side completeness instrument** (registered, green 24/24 routed verbs + 6 documented exceptions at `c15a629`) | the family census green; the missing pairs closed (OT-ops→engine-decode round-trips at the app's seam shapes) | [P] |
| **K3 app-behavior nets** | the app wiring driven headlessly: **the routed-verb completeness machine-check = the D30 W-F coverage-gate port** (the module-scope accumulator + the three gesture sims + the final assertion `accumulated ⊇ WIRE_COMMAND_TYPES − exceptions`; drift tsc-enforced through the lock-copy) **+ the shell-path blind spot's compensating pin family** (the trusted-integrator verbs ⌘B/[ ]/⌥[ ]/,/./M/⇧M/⌘M/⌘⇧M are permanently invisible to the gate — D30 R2/R8's owned consequence — drive them directly through `engineService`, owner-tagged app-side); the LAW-NET corpus re-expressed (322 authored + 33 projection pins; **incl. the composed ripple family — the GAP-W-ops rows, 4 units/16 tests, composed app-side over `timeline.trim`/`rippleDelete`; r1 graduates the OT interval-diff family**); **the DOM-structural gate = D26.5's three-way data-test mapping** (OT's frozen 68-site `data-test=` for the timeline units / nle-ui's `shell-*` for the chrome units / the structural-gap worklist for the no-counterpart units; the mini's 59-static + 15-templated census gets the mapping row); the offline audio pins; the VLM visual net (adopted from the variants' R23-G) on the app storybook + OT's runner | the coverage gate green + the compensating pin family green; the corpus row-by-row green (LAW-NET-INVENTORY is the acceptance list); the mapping row enumerated present/absent/structural-gap; the audio pins green (Node-venue law; **hard prerequisite: S-wdc's waveform seam contract — the only K3 sub-half with an external prerequisite**) | [P] |
| **K4 the crawl exit** | the single automated e2e suite: import(virtual) → cut (drag/trim/split/ripple) → play (engine clock) → export — **driving the same routed wire verbs through the real UI (D29.1c; the shell-path exception applies identically — the trusted-integrator verbs driven through engineService)** | **green end-to-end, zero mock paths, zero human input** — the crawl's completion proof | [P] |

**Ordering law (hard edge — RE-STATED per D26, review F4):** K3's DOM-structural half gates on **the data-test mapping row + D30's W-C** — NOT on a swap+testid layer (the D25 ordering is retired with the vendor-extension mechanism). K3's store/policy halves run ∥ D30 against the injection-point contracts. The crawl's structural grammar work (D25.3b's structural half: the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas) lands IN the crawl window as OT additive components — **crawl = structure + behavior; w1 = tokens + fidelity** (the qc- token/theme half is walk).

### WALK — the real app wiring, humans enter

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **w1 grammar + human rounds** | the token half of the grammar (the qc- theme on OT's CSS surface), the MiniShell chrome wiring (C0 consumed), the viewer-frame fidelity, the app-vs-OT-runner side-by-side, the annotakit review loop for the app | the side-by-side VLM pass; **the human test rounds on the complex timeline operations** (the class the user named as needing human testers — drag/trim feel, interaction latency, edit-session flow); every finding lands as a new programmatic pin wherever possible or is registered with owner | [P] + [H:interaction-feel — the user's named class] |
| **w2 media layer** | the MediaRecord registry + probe + lookup, the full event staircase (spec 15 §9.5), telemetry (reference row: cloudcut-nle's hybrid-storage RFC — the three-tier storage + 480p-proxy pattern + the Tauri desktop path, D27.3 — a design input only, NO dependency) | media round-trip pins; every staircase row published + consumed + pinned; the full-scope demo on virtual media re-gates | [P] |
| **w3 project layer** | ProjectJSON persistence (spec 09), multi-scene + scene wire ops, cross-scene-undo + history budget | project round-trip vs per-scene OT toJSON; the scene suite green | [P] |

**Walk exit:** a real edit session — import real media → edit → save → reload → export — human-tested with the findings register closed or owner-tagged. **[H:real-session — the walk's purpose: the human-verifiable product moment; the complex-timeline-operations class extends here]**

### RUN — the full NLE (DaVinci Resolve + web-DAW class, the shell-variants scale)

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **r1 op depth** (was W-ops) | slip/slide/roll/rateStretch (wave 1), retime/freezeFrame/rangeRemoval (wave 2), error-envelope refinement (spec 15 §6.3 first), **C7 rename at END** + the one-business-day app migration + keymap-sync sub-gate | S1 dispatch-complete + typed NOT_IMPLEMENTED honest; OT suite + carried tests green; spec 15 §13.15 rows flip ALIGNED | [P] |
| **r2 audio depth** (was W-audio) | mixer G-wiring full depth, sidechain/PDC/automation shapes (SoundTouch offline pitch + varispeed: **W2 LANDED @ `de09c93`** — consumed, not re-done; N2b keyframed volume: **LANDED @ `37cdd28`** — consumed) | offline parity ≤ −60 dBFS any channel + realtime behavioral pins; the null rig passes | [P] |
| **r3 color** (was W-color) | the grade-math binding + parity; the S-engine instruments consumed (scopes/qualifier/power-window) | grade-math parity pins (max delta ≤ 1 LSB-equivalent); scopes plot engine data + pinned | [P] |
| **r4 real media decode** (was W-n5) | N5: the media registry + real decode → VirtualMediaAsset | **PENDING the user's D6 re-affirmation (deprioritized)** | [P] |
| **r5 interchange + polish** (was R-fcpxml/R-polish) | FCPXML (greenfield; the parser+fixture-corpus choice is the phase-entry artifact), keymap long tail (C22 ledger), i18n, a11y residuals | validates vs the chosen reference parser on the corpus; spec 18 §11 audit; battery | [P] |
| **r6 the non-blocking tail** (was R-engine-p2/R-cloud) | CPU transition renderers, Shape/Lottie/Subtitle/Controller surfacing, other engine P2; cloud render UNCHARTERED unless the user re-scopes | per-item engine milestones green as queued; slips independently by design | [P] |

## 3. The workstreams (the tracks' worklists, keyed to spec §0s)

Each track's worklist rows cite the OWNING spec's §0 GAP register (the posture law — acceptance lives there, not here). Phase tags use the D24 vocabulary; the old→new mapping is §6.

**S-ot (opencut-timeline):** the carrier-reduction program + the data-test stability contract + the view-config surface (spec 05 §0's C1-side rows) — ONE filed work order, riding ∥ (D26.3; gates nothing) → K2's runner registration (M49C, green) → the structural grammar half (crawl-tail: the components per ARCH-R23 D25.3b, the instrument re-pointed to `data-test`) → r1's op families + C7 (spec 06 §0, spec 15 §13.15) → the W-ops→r1 OT queue as it files (the batch-keyframe wire verbs + `timeline.insertBatch` are ENDORSED spec-15 r1-adjacent rows per D29 review F8 — on landing, the routed/exceptions census RE-DECLARES mechanically per the tsc-lockstep law).
**S-app (nle-test-app):** `.agents/` (overdue, before W-C) → the D30 waves W-C..W-G (in flight) + the census discipline (the CI-gated register) → K3 store/policy halves ∥ → K3 DOM-structural (gated on the mapping row + W-C) + K4 → w1 (token wiring + side-by-side + human rounds) → w2 media (spec 15 §9.5 + spec 09's media half) → w3 project (spec 09) → r5 fcpxml.
**S-engine (nle-engine):** the live queue (the D29.5 dispositions: the CSS-filter widening OPEN, the transform sidecars future-extension, maintainPitch closed; the projector parity corpus, the color instruments — spec 01/03/04/08 §0s; N2b/W2/SoundTouch consumed not queued) + K2's engine-side pairs → r2/r3/r6 consumption points as they land.
**S-wdc (web-daw-core):** the waveform seam contract PROMOTED first (D29.4) + the M2 mixer surface + sidechain/PDC/automation (spec 20 §0) + the five test ports + the async pre-retime → r2 depth.
**S-package (nle-ui):** C0 MiniShell (spec 18 §0) + the filing-gap actions (the C0 row, the W-D package edits, the D29/D30 DECISIONS — package-side) + the OT S-round queue (landed — tick) ∥ crawl → w1 consumption → r1 keymap surfaces → r5 polish rows (C12/C11/C14/C15).
**S-spec (this repo):** the audit fleet (§8 — the R24 wave DONE) + battery_r24 + the C-ledger rolling disposition + the mock retirement triggers (post-K4/w1 — the census-discipline law, the law text in 00-master per D26).

## 4. The parallelization map + critical path

**Runs in parallel RIGHT NOW (nothing blocks another):** S-app's D30 waves W-C..W-G (the critical path's head) + its K3 store/policy halves ∥ S-ot's carrier-reduction program (gates nothing) ∥ S-engine's queue (the widening + the parity corpus + the instruments) ∥ S-wdc's waveform-contract promotion + M2 ∥ S-package's C0 MiniShell + the filing gaps ∥ S-spec's battery_r24. Six streams, six repos, six gate sets — the module streams never touch each other's trees (cross-repo work is FILED as queues, never unilateral).

**The dependency graph (the hard edges — re-drawn per D26):**
```
S-wdc: the waveform seam contract (PROMOTED: HANDOFF next-session + stated + pinned) ─────┐ (a hard prerequisite)
                                                                                        ↓
S-ot: carrier-reduction program + data-test contract   (the ∥ stream — gates nothing)  K3's audio-pin half
S-app: .agents/ → D30 W-C..W-G (the census discipline) ──┐                    (K3's store/policy halves ∥ D30,
S-app: K3 store/policy halves (∥, injection-point ctrcts) ┤                     against the contracts)
                                                          ├→ the data-test mapping row + W-C → K3 DOM-structural → K4
S-ot: structural grammar half (crawl-tail) ───────────────┘       (crawl-tail)             (crawl exit)
S-package: C0 MiniShell ─────────────────────────────────┐                                    │
                                                        ↓                                    ↓ (the suite extends)
                  K4 ──→ w1 (tokens + fidelity + human rounds) ──→ w2 ∥ w3 (w2's full-scope demo re-gate = K4's suite extended over the media registry) ──→ r1..r6
                                                                                                               ↑
                                                the productization gate (D28.2) noted at r1-entry
```
**Critical path (RE-STATED):** the app's D30 completion → K3(DOM) → K4 → w1 → (w2/w3 where staffed) → the r-phases. **The OT bridge no longer blocks** — the carrier-reduction program rides ∥ (gates nothing); the structural half is crawl-tail as before. Solo: serial along the path, the ∥ streams ride whenever idle. Two-dev: one on S-app (D30 → K3/K4), one on S-ot/S-package (the carrier-reduction + the structural half + chrome) — the crawl's 5-11/3-6 estimate assumes this split.
**∥-validity laws:** w2 media ∥ w3 project holds (different seams, both app-owned after w1's shell stabilizes); r1 ∥ r2 ∥ r3 hold at run (different repos); the color instruments (S-engine) run ∥ crawl but r3's gate only requires them at consumption time; K2 ∥ everything (it is verification, not construction — it tightens as the pairs land).

## 5. The execution protocol (standing practice)

1. **Push at every micro-milestone** (GIT IS THE DISK); `git fetch` before every push; **never force push** (merge-first on sibling races — they are documented and recurring).
2. **Pin-lockset law:** module lands first under ITS gates; the app pin-bumps within one business day; HEAD-follow opens bump PRs, never pushes main.
3. **The battery runs after every spec round** (battery_r24, the successor — see §6/§8); recalibrate stale checks; exempt windows look BEFORE and AFTER each hit.
4. **Decisions go through adversarial review rounds** (fresh-context sub-agents) before implementation — the ARCH-R22/R23/R24 trail is the pattern.
5. **A gap row with no acceptance is a spec bug; a facet with no coverage-matrix row is a spec bug** (the R22 laws, now in 00-master's standing-laws section).
6. **The census discipline, never swap-then-hope (D26 re-points the port-then-swap law):** the app's port tree stays a converging mirror governed by the census (register-equality, CI-gated recomputation; a carrier without a divergence rationale is a census violation; a census that drifts from the tree is a build failure); upstream waves re-pin via 3-way merge — no-conflict files adopt-wholesale, carriers merge against the documented zone maps with per-wave acceptance pins; the mini mock stays alive until the app matches it (retirement triggers post-K4/w1).
7. **The drag-law freeze (D22)** and the count-discipline law (declared == actual at the pins) are standing — see 00-master.
8. **The productization gate (D28.2):** r1-entry, OR a packaging need — defined as any consumer OUTSIDE the six-stream fleet pinning/consuming a module, or a distribution request landing — whichever PRECEDES r1-entry. At that gate (and only then) the seal-snapshot option opens: tag the sealed shas and cut clean public packages from them (e.g. an `nle-timeline` package from OT's tree, an `nle-shell` from nle-ui) via export-ignore/docs-reorganization — a future user-visible ARCH decision point; the pin ecosystem migrates through the normal pin-bump protocol at that time. Nothing before that gate.

## 6. This round's steps (the S-spec track's work order — the R24 record)

0. **battery_r24's core** (PENDING — the S-spec row's step 1: the R24 pin world + the census-coherence + routed/exceptions checks + the plan-gate re-points; the fleet's rounds gate on it).
1. The adversarial review round on ARCH-R24: **[done — v2, 17 findings folded; transcript agent-eb0816c5]**.
2. The per-file audit fleet + the module-card wave: **[done — 20 per-file agents (00 + 01-13, 15-20) + the 5 module cards (one per core repo, live-verified); the reports live in `audits/fleet-r24/`]**.
3. The plan/track audit wave: **[done — THIS doc at R24-4: the D26/D27/D28/D29 amendments applied + the cold-executor re-simulation folded into §0 (the environment prerequisites + the per-row gate cells); the report lives at `audits/fleet-r24/plan-and-tracks.report.md`]**.
4. battery_r24 full extension (PENDING): the pin set, the plan-doc gates (entry points per track, gate-class tags, workstream completeness), the census-coherence + routed/exceptions checks, the audit residue sweep.
5. The integration review (fresh-context, the whole round) → the wrap (PLAN/HANDOFF/SKILL/worklog + push + /home/sync). PENDING.

*(R23's steps are history: spec 14 → the redirect stub done @ `4878827`; the cross-ref sweep's corpus half rode the R23 per-file fleet.)*

## 7. Lineage (the traceability appendix)

| Era | Plan | Fate |
|---|---|---|
| R9 | P0-P6 phases | retired to git history |
| R15 | A0-A7b assembly | superseded by R22 (content absorbed into the W-rows) |
| R22 | spec 14 v2.1: C0-C4 / W-* / R-* | superseded by THIS doc (D24's mapping table re-homes every row: C1(b,d,e,f)→K3, C1(a)→w1, C2→K3+w1, C3→K3, C4→K4+w1-entry, C0→w1-prep/S-package, W-ops/W-audio/W-color→r1/r2/r3, W-media/W-project→w2/w3, W-n5→r4 (user-gated), R-*→r5/r6; the orphans re-homed per ARCH-R23 review-1 F3) |
| R23 | THIS doc + ARCH-R23 | superseded by the R24 state (the D23/D24 rulings carried forward; the D25 mechanism amended) |
| R24 | THIS doc (the R24 state) + ARCH-R24 | current — D26 amends D25.2's mechanism (the adopt-wholesale convergence + the census/carrier discipline; D25.1 canon + D25.3's two halves stand, 25.3b's instrument re-pointed to `data-test`); D27 the topology (keep nle-ui + the app, evolve in place; cloudcut dispositioned as the dormant predecessor); D28 evolve-in-place + the productization gate (the seal-snapshot option registered); D29 adopts the W11 instruments ladder-wide (the M49C coverage gate, the 24-routed + 6-exceptions census, the data-test contract, the waveform promotion) — D24's ladder carries them |

## 8. The audit fleet (the user's standing directive — per-module, per-file, per-track)

**The R24 wave (this round — DONE):** (a) **the module-card wave** — five agents, one per core repo (opencut-timeline, nle-engine, web-daw-core, nle-ui, nle-test-app), each live-verifying its repo at the R24 pins (HEAD, test counts, tsc, the seam surface, the queue, the spec-facing staleness) — the evidence packs every other wave cites (`audits/fleet-r24/module-*.report.md`); (b) **the per-file fleet** — 20 agents, one per spec file (00-master + 01-13, 15-20), each charged with: BASE claims re-verified at the R24 pins; GAP rows re-checked (landed? still-a-gap? new gaps the W11/D29/D30 rounds created); the D26/D29 amendments reflected (the census register, the wire re-base 24 routed + 6 exceptions, the data-test mapping, the grade decline); phase tags re-based (the dual-vocabulary window closed — the D24 set only); the round's landings promoted (W10F/W11/M44-M49, the engine re-pin + W2.5/R8-REV, nle-ui's D29 seams, the app's R8/R9 waves, WDC's seal wrap); (c) **the plan/track audit wave** — THIS doc (R24-4, the amendments + the cold-executor re-simulation); (d) **the 17-fold adversarial review** on ARCH-R24 (folded to v2 BEFORE the fleet — the review trail in ARCH-R24's §review trail). Agents edit ONLY their file, run NO git commands (the orchestrator commits per batch), and return a findings report. battery_r24 re-baselines the pin world once, centrally, after the fleet.

**The R23 wave (precedent — the original directive's shape):** **The set:** 00-master + the 19 numbered specs (01-20 minus the retired 14). **One agent per file** — each audits its file against the LIVE upstream repos at the current pins, charged with: (a) every BASE claim re-verified (pins/counts/features — the fleet moved under every R22 pin); (b) every GAP row re-checked (landed? still-a-gap? new gaps); (c) every retired-spec-14 §4 row for the domain confirmed present in the §0 register (posture law — a row found nowhere is a violation to fix); (d) phase tags re-based per the D24 mapping; (e) the round's new upstream work reflected (OT S3/F1/M45-46, WDC W2/CR rounds, nle-ui F3/F7, app W3 JKL + RR1-B, engine RR1-A, the R23 seal artifacts).

**Estimates (the honest register — re-based per ARCH-R24 + review F4):** crawl ≈ **5-11 wk solo / 3-6 two-dev** — re-itemed: the app's D30 waves 1-2 (in flight — W-A/W-B landed at the pin; W-C..W-G is the item), K2 1-2, K3 2-4, K4 1-2, **the structural half 1-2 KEPT EXPLICIT** (the migration's second-largest item survives D25.3b — it rides the S-ot stream, counted at the crawl window, not on the critical path); the D25-bridge line-item (2-4) **COLLAPSES into the carrier-reduction program** — riding ∥, off the critical path (its OT review cycles live in the S-ot idle windows). Walk/run UNCHANGED — no walk/run item shifts with the crawl re-base: walk ≈ **10-15 wk solo / 7-10 two-dev** (row sums 7-11 + human rounds 2-4; C0's 1-1.5 rides ∥ crawl per the D24 mapping — off walk's critical path); run ≈ **20-28 wk solo / 13-17 two-dev WITH r4 (the user's D6 re-affirmation); 18-25.5 without**.
