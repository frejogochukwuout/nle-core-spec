# IMPLEMENTATION-PLAN — THE plan (the executable plan, R34 overhaul, separate from the spec set)

**What this is:** the HOW/WHO/WHERE companion to the spec set's WHAT/acceptance (the §0 posture law). The spec corpus (00-19 + 20-audio-core) owns the law + the per-§0 GAP/BASE registers; THIS doc owns execution sequencing, per-track worklists, and the gates table. no time estimates (the D38.1/D40.1 charter: size letters are SCOPE indicators, never calendar promises; a phase exits when its exit gate is green, never on a date); **a gate is a command you run; a row without a machine-checkable gate is a spec bug.**

**Ruling basis:** D1-D41 (the R22-R27 arcs: ARCH-R22 through ARCH-R27, the D40 overhaul's architecture still load-bearing) + D42-D50 (`audits/ARCH-R28-seal-round.md`) + the R29-R33 execution lineage (battery_r28→r33, rekey_r31/32/33, the R33 engine seal round + its 87-row register) + **the R34 charter** (`.agents/PLAN.md`'s R34 entry — the seal-round directive: close every agent-actionable gap, gate ONLY true product calls). The R34 fold commits: `cc17fea` (charter) · `0cef3fa` (the Testing-mirrors repair + **the K2 census/registration flip** — K4-ratification condition #1) · `abd3e14` (**the K3 VLM-net LANDED markers** — condition #2).

**The ladder state (R34):** **CRAWL COMPLETE** (K1 module nets · K2 combined nets — the census+registration LANDED R34 · K3 app-behavior nets · K4 the automated e2e — the exit verdict R32, **RATIFIED R34** when both its spec-side conditions landed) · **WALK**: w1 (the human rounds) STAGED + USER-owned — nothing in it needs an agent; w2 (the full-scope demo) ITEMIZED below · **RUN**: r1 design v2 COMPLETE + audited (`nle-engine/gaps/audit/seal-round/design-r1-port.md`, all 17 amendments folded) → **r1 is now an EXECUTION round** (the leverage map's Stage 0-4, D41 — the trials-and-errors avoidance order; Stage 0 first); r2-r6 unstarted.

**The D12 convergence spine (unchanged, load-bearing):** spec → r1-port → app assembly → K-tier verification → w/r phases. Every lane feeds the spine; nothing on the spine is optional.

**The user-gated register (RESOLVED R35 — the user's four decision inputs received + folded; the register is now EMPTY of product calls; nothing blocks on the user):** (1) **fps 24-vs-30 → RULED R35: SUPPORT BOTH 24 AND 30** — "no NLE can work with ONLY one of them"; fps is a first-class configurable (the `/view` fps-param MECHANISM landed R34 [OT `c4c04f1`'s M64]); the supported default set carries both rates; the either/or framing RETIRED (the app side renders fps-honest at either rate; the human rounds still collect per-round rate preferences as feel-data, never as a gate); (2) **the playhead cadence → RULED R35: FRAME-SNAPPED, per the NLE-convention gold standard** (W1-a's research verdict, `audits/r35/W1-a-playhead-cadence-research.md`: frame-snapped playhead IS the unanimous industry convention — Resolve/Premiere/FCP all frame-quantize; the "smooth playhead" settings move the TIMELINE VIEW, never tween the playhead; the only true flavor-split is sub-frame AUDIO [Resolve's align-audio-to-frames pref, Premiere's Show Audio Time Units, FCP's subframe nav] — the unquantized display path is NOT a convention and is RETIRED; the audio-units display variant registers as the deferred pref-toggle item, r2/audio-era [SF-4's face], per the owner's "if truly different flavor then support all with app setting/user pref toggling"); (3) **the programMonitor viewer-state rows → RULED R35: APP-STATE LEVEL ALWAYS, SURFACING A SEPARATE CALL** (the owner's general principle: "always safe to have it at app state level and then decide whether to surface or not") — the app-side rows land as agent-actionable wiring (S-app's worklist gains the row wiring: loading/error/empty around ProgramCanvas at app-state level, surfaced by default, surfacing itself a settings-tier toggle); (4) **THE HUMAN ROUNDS THEMSELVES** — the round-1 scripted edit session per `nle-test-app/docs/human-rounds/PROTOCOL.md` (the annotakit loop live on :6007, the side-by-side fixtures, the C/G/H/F/Q capture keys; everything staged, zero agent-blocked prerequisites) — the rounds remain the USER's own lane (a lane, never a blocker; the plan never blocks on them).

## 0. HOW TO EXECUTE — the entry points (start here)

**Environment prerequisites (unchanged, verified R34):** the six repos + the vendored mirrors; WDC's `UPSTREAM.lock` `f5011b3`; the engine's `protocol.file.allow` submodule pattern (SKILL law #182); the battery reads the fleet through `REPO/../<name>` sibling clones; the sibling-filing intake protocol (below).

**The taxonomy (standing):** a TRACK is a repo-owning workstream (S-app owns nle-test-app's wiring; S-ot owns the library; S-engine the venue; S-ui the package; S-wdc the audio core; S-spec this corpus) — a lane is a cross-track concern (the human-rounds lane is USER-owned); the mocks are a retired tier.

**The tracks (the R34 worklists — every open item in exactly one home; the eight-stream parallel carve of D40.3/D38.3, the human-rounds lane now first-class):**

| Track | The remaining work (R34) | Gates | Acceptance |
|---|---|---|
| **S-app** (nle-test-app @ `d28847c` — 426/426; census 43 = 33 zero-action + 8 carriers + 2 hosts; the R34 re-pin LANDED (the D-ARCH-6 re-pin + gesture-seam switch line's continuation): engine `60232ea` + OT mirrors `05d88d9` + nle-ui `cb04919` + WDC `94f6460`, the CR-2 carrier FLIP [the program's first full file-flip], scrollAnchorId wired) | (1) **w2 the full-scope demo** (L — ~6-9 new e2e legs / +600-900 lines: the 16 absent routed verbs [track.add/remove/setAllLocked/setAllMuted/toggleLock/toggleMute/toggleVisibility, upsertKeyframe/removeKeyframes/retimeKeyframes, duplicate, delete, insertBatch, moveBookmark, undo/redo] + save/reload round-trip + scene-switch-mid-playback + multi-select pool insert + cross-track arrange + snap + the color page; re-gates by extension per the K4 row — `docs/decision-k4-exit-verdict.md:71`); (2) **the value-passing wires** (M: isShellOverlayOpen absorbs the port's `[data-modal]` guard — the audit's next wire; surfaceOwnedKeys absorbs the Z4 `[data-transport]` member — UNBLOCKED R34 [OT `8f96ab2` exports the upstream census]; cancelRegistry the D25.2 parity mint via the vendable barrel); (2R) **the R35-unblocked pair** (S+M: the programMonitor viewer-state row wiring around ProgramCanvas — loading/error/empty at app-state level, surfaced by default, the settings-tier surfacing toggle [the register's RULED disposition]; + the shell-mini edit-styles wave — the table-stakes insertion/trim set the conventions catalog mandates: insert-at-playhead [ripple] + overwrite + append-at-end + ripple-delete + the modifier-drag pair + ripple-trim + rolling edit + extend/trim-to-playhead [W1-b §3's T-set minus the four already present], each style a wire verb or verb-composite + its keyboard row + its e2e leg, per the owner's "add a few more timeline insertion style + trim / clip editing style"); (3) **M2 Waves 2-3** (M each: Wave 2 the mixer surface — bus reverb mapping + the EQ param editor; Wave 3 PDC — insertLatencySamples + the single subtraction + seekSegment's re-anchor; `docs/design-m2-mixer-surface.md` §8); (4) **CR-F6** (S: the readiness-await flush — Promise.allSettled over the strips' effect-ready promises); (5) **the K3 residual row re-keys** (S: the store/policy phase row + the mode-matrix rows — largely landed across R31-R33, the row not re-keyed); (6) w3 project (spec 09) + r5 FCPXML (post-w2) | vitest 426+ @ the pin; census register-equality; boundary; build | the e2e suite re-gates GREEN by extension; the census register-equality holds at every pin |
| **S-ot** (opencut-timeline @ `8f96ab2` — 714/714 @ 83 entries; the carrier-reduction program's L2 cheap set LANDED R34 [CR-1/2/3/4/5/8/9 — the M59/59A/59B/59C/59D families] + the M64 fps param) | (1) **L3 data-test census** (S-M, MUST — `reviews/seal18-a-datatest.md`: the `scripts/data-test-census.mjs` census check + the frozen manifest [Tier 1 = 59 static + 6 templated; Tier 2 = 10 hosts + 2 families] + the 6 additive gap markers [timeline-element-hit the highest-risk consumer-pinned unmarked surface] + the freeze-contract filing); (2) **L3 view-config** (M, SHOULD — `zoomConfig` {min,max,steps} threading + the ripple triple [initialRippleMode/rippleMode/onRippleModeChange]; the Z1-Z8 + R1-R6 pin lists); (3) **L5 the P3 sweep** (XS-S each: HA-3-4/5/7/10 the UI cluster; HA-3-8 SSR virtualization or its deferral ruling; HA-4-17 dispose try/finally; NEW-2 the trim-sum asymmetry [validateWireUpdates' reshaping filter]; NEW-3 + OV-19 + HA-4-19/20; HA-3-6 a11y depth [integrates SURFACE_OWNED_KEYS]; HA-3-9 the dead CSS; the W8-f + HA-2-12 + HA-4-18 deferral rulings); (4) **the sleeps waves 1-3** (L — m17/m28/m49-transport + the freshPage 150s; the census 372→~300; each wave = one commit + the timeout-proof run); (5) **L6 expansions** (M each, deferrable-with-filed-orders: the census families [controllers/pure/hooks/surfaces] + the 8-row core worklist pins + M25 fuzz expansion [I6/I7/patch/retime/insertBatch needs the Api-level driver] + the mutation harness); (6) **CR-6/CR-7** (M+M, deferrable: the selection bridge [initialSelectedIds/onSelectionChange/selectionIds — the hardened echo-guarded pair] + the zoom bridge [zoom pxPerSec/onZoomChange, PROVENANCE flag, lands LAST alone]); (7) **R31-b the D25.3b 18-GAP worklist** (L, deferrable — the minimized strip ×5, track binding ×2, transition layer ×5, filmstrip, mini-plus/solo ×2, tool radio ×2, zoom wrapper); (8) the bookmark/marker EDIT residual (r1 A2/C7-gated — updateBookmarkInArray exists, no verb consumes it) | the report json (count authority); tsc 0; the M-family pins | every carrier landing green with its pin family; the census moves only by declared milestones |
| **S-engine** (nle-engine @ `60232ea` — 789/789; perf.yml FIXED + the §B.5 steps restored + **the nightly GREEN + CI GREEN** [the CI_VENDOR_URL_PREFIX variable set R34]; **P-F4 caught + closed the same round** — the exploration venue proven on its first random run) | (1) **the r1-port EXECUTION round** (L, multi-session — Stage 0 first: the 15 engine-home §4.1A rows [Effect 5 + Mask 4 + Transition 3 + Export 3]; the OT-side linkedTo consumption + the ElementPatch key; the D43 edit-domains fold [C1/C4 + A-2 fence + check-2 widening]; the fence re-points [§3.2/§3.3 incl. F6's sentence]; rider 9's count reconciliation; the union façade's Stage-A mint; then Stages 1-4 → the r1 END fold [D-r1-1 the three-wave native-Timeline retirement + the r6-entry sunset gate; D-r1-2 staged dispatch; D-r1-3 the av-link fence re-point + P-F3's window-containment question; D-r1-4 the D45 projector five-wave consumption; the 749→~630-640 test-migration map]; the 17 §13A.8 acceptance protocol the contract); (2) **W4b the m32 perf milestone** (M — EXPECTED_MILESTONE_COUNT 31→32 + MIN_TOTAL_TEST_ROWS + the browser pins); (3) **W5 thresholds** (M — the per-directory LINES-only ratchets at measured−3 + the PERF-1 smoke promotion at 10×; evidence-blocked on ≥ a few nightly runs — the nightly is GREEN and accumulating); (4) **NS-1/NS-2/NS-3/NS-5** (M each — the design rounds: transport/render-clock coupling; real-media peaks [the wdc waveform contract]; clip-frame thumbnails; the edit→A/V reschedule classification); (5) the K2 forward row (S-M — the engine-resident round-trip suites beyond the app-carried shapes, honestly registered at the K2 landing) | 789 vitest count gate (both directions); tsc 0; the nightly | the §13A.8 acceptance protocol green per stage; the count gate pins the suite |
| **S-ui** (nle-ui @ `cb04919` — 747/747; the DeliverPage supersede contract + the viewer-state-empty CR pair LANDED R34) | (1) **the F3-NEW sceneDuration guard** (S — the empty-scenes array throws at AppShell.tsx:273; verified absent R34 — the one open row); (2) the value-passing consumers ride S-app's wires (above) | vitest 747+; tsc 0; boundary | the package suite + the boundary green at every pin the package suite + the boundary green at every pin |
| **S-wdc** (web-daw-core @ `94f6460` — 777/777) | (1) **G2 async pre-retime** (M, r2-tagged, engine+WDC — design-first per the M2 §8 map); (2) the M2 Wave 2/3 WDC-side halves (via the file-queue protocol — S-app leads) | 777; the drift-gate; byte-verbatim pins | the drift-gate green; the contract pins byte-verbatim the drift-gate green; the contract pins byte-verbatim |
| **S-spec** (nle-core-spec @ the R34 fold — battery_r33 214 checks; the K2 flip + the Testing mirrors + the K3 markers LANDED R34) | (1) **battery_r34** at the wrap (the pin re-key: engine `60232ea`/789 · OT `8f96ab2`/714-83 · app `d28847c`/census-43=33+8+2 · nle-ui `cb04919`/747 + the R34 check classes); (2) the sibling-filing intake (the standing pattern — absorb as they land); (3) the user-gated register custody (this doc's register + the rounds' verdict recording); (4) the mock-retirement final flips (REFERENCE-REGISTER + 00's rows — post-w1 per D35.4); (5) the wrap re-keys (00-master's fleet table + 12/17's posture rows) | battery_r34 ALL GREEN; rekey ZERO residual | battery_r34 ALL GREEN at every wrap + the rekey scanner ZERO LIVE-stale residual every wrap gates ALL GREEN + ZERO LIVE-stale |
| **The human-rounds lane** (USER-owned — `nle-test-app/docs/human-rounds/PROTOCOL.md`) | The rounds themselves: round-1 the scripted edit session (import→arrange→trim incl. the 1-frame floor→split/ripple→JKL/loop/audio with the Q1 checklist→undo/redo mid-gesture [the D51/D52 yield, live]→save/reload→export) + free-form feel capture (C/G pins, [feel:*] tags); each round closes with the round-`<n>`.md register (census-in/dispositions-out, ZERO UNOWNED FINDINGS); the three REGISTERED decisions collect verdicts each round | the PROTOCOL's own loop; the annotakit health; nothing agent-blocked |
| **Mocks (mini + variants)** | RETIRED-tier records (the port-then-swap law executed: design-reference only; the final REFERENCE-REGISTER flips ride S-spec post-w1) | none — retired-tier | the retirement flips recorded in the battery_r34 wrap |

**The single global entry point:** any track above is independently executable NOW (the table's row = the work order; the First action is each row's item (1); the gates column = the entry gate — **the carve law: every track row's gates column cites a test law; a row that cites no test law is a plan bug**). The current NOW-set (what runs in parallel this instant): S-ot's L3 census + L5 cheap · S-app's w2 demo + the isShellOverlayOpen/surfaceOwnedKeys wires · S-engine's W4b + the r1 Stage-0 prep · S-ui's F3-NEW guard — none collide (disjoint repos/files).

**The sibling-filing intake protocol (standing):** sibling lanes file INCOMING FILING blocks (spec `.agents/PLAN.md` + `audits/` artifacts); the spec round absorbs them via the R33-established 5-wave pipeline (scanner first → file-disjoint batches → the orchestrator applies the delicate rows → the re-key scanner + battery fork → the adversarial gate; SKILL law #179). A filing older than two rounds is a finding.

## 1. The fleet (cited, not re-declared)

00-master's fleet table is the pin canon (D21) — the plan cites pins, never re-declares (the D28 law sentence). **The R34 layer (2026-09-22):** spec (this fold) · **engine `60232ea`** (789/789 = 785 + the P-F4 pins; 29 files; the code anchor MOVED to `60232ea` [the P-F4 src fix]; the 5-job CI **GREEN** + the perf nightly **GREEN** [the first dispatch + the P-F4 catch + the re-baseline]; vendors OT→`17a19f8` + WDC→`94f6460`) · **OT `8f96ab2`** (714/714 @ 83 entries; the fps-param M64 + the CR families M59-M59D; CR-1/2/3/4/5/8/9 LANDED; `SURFACE_OWNED_KEYS` exported) · **app `d28847c`** (426/426; the census 43 = 33 zero-action + 8 carriers + 2 hosts; the R34 re-pin: engine→`60232ea` + both OT mirrors→`05d88d9` + nle-ui→`cb04919` + WDC→`94f6460`; the CR-2 carrier FLIP + scrollAnchorId wired) · **nle-ui `cb04919`** (747/747; the DeliverPage supersede contract + the viewer-state-empty CR pair) · **WDC `94f6460`** (777/777, unmoved). The R28-R33 layers live in the fleet table + 12 §0's layered rows (history, verbatim beneath). Consumer pins re-read live: engine→OT `17a19f8` + WDC `94f6460`; app→engine `60232ea` + the OT mirror lock `05d88d9` + nle-ui `cb04919` + WDC `94f6460`.

## 2. The stage ladder (D24) — exit gates + gate classes

The gate classes (standing): **[P]** = parallel-safe by default; **[H:reason]** = a registered hard dependency, never silently serialized.

### CRAWL — the programmatic-verification layer: **COMPLETE (K1-K4 all landed; the tier gates are now standing regression law)**
- **K1** (module nets) — landed + pinned at every round's battery (the fleet table the canon).
- **K2** (combined nets) — **LANDED R34** (`0cef3fa`; the mode-matrix gate: ZERO ORPHAN ROWS — every mode-family row lands a pin or an explicit exception, never an orphan): the census + register in 12 §0 + 17 §13A.7 (engine-in-the-middle 247 it( cites [bridge-seams 151 / planner 52 / video-sync 31 / timeline-edit-ops 13] @ `d8b97b4`-era engine; WDC's 6-suite family 65 cites @ `94f6460`; OT's runner 687/687@78 at the census pin; M49C green 28/28 @ `344123a`); the missing-pairs status HONEST: app-carried (wire-coverage 26 + engineSeam 7), no engine-resident round-trip suites claimed — the S-engine forward row, not a K2 blocker.
- **K3** (app-behavior nets) — landed R30-R32 (the corpus re-expression + the DOM-structural tranche + the D26.5 data-test mapping row's three-way DOM mapping + the audio pins + the shell-path blind-spot registration + the compensating pin family (F14) + **the VLM net** [7/7, 0 REAL-BUG, GATE=PASS — the K3 VLM-net markers recorded spec-side R34 @ `abd3e14`]); K3's routed-verb completeness instrument is the D29.1/D30 W-F coverage-gate port (the live-registry law); the residual row re-keys ride S-app.
- **K4** (the automated e2e) — **the exit verdict R32 + RATIFIED R34** (both spec-side conditions landed: the K2 flip + the K3 VLM markers; the app's K4 phase row ticked @ `cf4e545`). The e2e drives its 12 wire-visible verbs + 2 shell-path rows; the FULL 28-verb completeness instrument is the wire-coverage gate (the live-registry law). w2 re-gates by EXTENDING the suite (not a freeze).

### WALK — the real app wiring, humans enter
- **w1 (the human rounds) — STAGED, USER-owned.** Everything verified: the PROTOCOL, the annotakit loop (:6007 — the tracked threads.db store, the C/G/H/F/Q remap), the side-by-side fixture pair (byte-stable), the VLM net. Zero agent-blocked prerequisites. The three REGISTERED decisions collect verdicts per round (the user-gated register above).
- **w2 (the full-scope demo)** — S-app's item (1): the ~6-9 leg extension inventory; runs ∥ w1 (user-paced, non-blocking — the w1 verdicts gate only the REGISTERED decisions, never w2's e2e legs).
- **w3 (project)** — stands per spec 09.
- **The WALK exit:** the human sessions run + w2's extension green (the three REGISTERED decisions are RULED R35 — the rounds now collect per-round feel-verdicts + confirmation/flip signals through the registered-decision mechanism, never as blockers).

### RUN — the full NLE (DaVinci Resolve + web-DAW class, the shell-variants scale)
- **r1 (the port)** — **design v2 COMPLETE + audited** → the EXECUTION round: Stage 0 first (the 15 engine-home §4.1A rows; the OT-side linkedTo consumption; the D43 edit-domains fold; the fence re-points incl. F6's sentence + P-F3's window-containment question [D-r1-3]; rider 9's count reconciliation; the union façade's Stage-A mint — `design-r1-port.md` §6 :705-801 the work order) → Stages 1-4 → the r1 END fold (D-r1-1/-2/-3/-4 + the test-migration map 749→~630-640). The 17 §13A.8 acceptance protocol is the contract; battery_r34+ keys the stages.
- **r2 (audio)** — G2 async pre-retime (engine+WDC, design-first) + the M2 Wave 2/3 halves.
- **r3 (color + NS-4)** — the D45-constrained venue table stands.
- **r4 (media)** — deprioritized-by-user standing.
- **r5 (FCPXML)** / **r6 (workers)** — per specs 10/02.

**The decision-points register:** RULED (reversal-registered): the mode-matrix (D30) · the linked-companion law (D32) · the visual-grammar register (D33) · the keyboard family (D34) · the reference register (D35) · the execution wiring (D36) · the topology (D27) + the ARCH-R28 set (D42-D50). **USER-GATED (the R34 register, top of this doc — RESOLVED R35: fps [both 24+30] · the playhead cadence [frame-snapped, convention-ruled] · the programMonitor rows [app-state-first]; the human rounds remain the user's own lane). No agent silently resolved a gated row — the user's R35 decision inputs are the register's resolutions of record.**

### 2A. The shell-variant/full mountain — the SF face carve (R35, the plan's RUN-era product-surface map)

The owner's R35 directive: shell-variant/full is "a much bigger mountain (almost a DaVinci Resolve equivalent)" carved "into sub-sections (per view, ui group, major feature group — e.g. color workflow, NLE editing, sound, effect/transition)." The carve (researched + ratified W1-e, `audits/r35/W1-e-shellfull-decomposition-recon.md`): **a two-level hybrid — VIEW faces at the top level, UI-group + feature-group tracks inside each face** — honoring the closed D50 five-page law (no re-litigation of page shape), absorbing the other two axes where coherent, and keeping the r1-r6 ladder as the EXECUTION rails (the faces are the product-surface map; no face mints a new engine workstream).

| Face | Scope | Gold-standard anchor | Rung rails | Size |
|------|-------|---------------------|------------|------|
| **SF-0 Shell chrome & navigation** | the dock + five-page law, page-scoped state memory, page keys, presets, theming, context menus, toast, a11y floor | Resolve app chrome; Premiere workspace presets | XMOCK-6 (the FX-page absorption) · w2 legs | M |
| **SF-1 Shared panel families** | viewer family (source/program, transport, scrub), Media Pool, the Inspector chassis, the Effects rail, SourceEditBar | Resolve's shared chassis; Premiere's Project/Monitors/Effect Controls | w2 (journey legs) · r1 (source-edit keys) | M |
| **SF-2 EDIT VIEW — NLE editing** | the 10-mode timeline grammar (trim/insert families, slip/slide/roll/replace/append/ripple-overwrite/fit-to-fill), overlays, markers, the edit Inspector tabs, snapping, link/lock | Resolve Edit; Premiere Editing workspace | **r1 (the port — the long pole)** · the human rounds | L |
| **SF-3 COLOR VIEW — color workflow** | grade console (wheels/curves/levels), LUT + color management, qualifier, windows+tracking, node graph, scopes, stills/presets, shot-match, the graded viewer | Resolve Color; Premiere Lumetri as the reduced mode | r3 (the D45 venue table) | L |
| **SF-4 AUDIO VIEW — sound** | the mixer family (strips: fader/pan/meters/EQ/dynamics/sends), bus/aux routing, master, the audio timeline surface, Sound Library, loudness posture | Resolve Fairlight; Premiere Audio + Essential Sound | r2 (G2 + M2 Waves 2/3) | L |
| **SF-5 FX/TRANSITION VIEW** | FxBrowser/FxInspector, fxMode timeline (seam hit-zones, fade zones), the three-doors routing, transition boxes, effect params, masks, keyframes (Fusion-scale nodes OUT, registered) | Resolve Fusion + Effects Library; Premiere Effects | r1 Stage-0 engine-home rows · r6 pipeline · XMOCK-6 | M-L |
| **SF-6 DELIVER VIEW — export/finish** | render settings + presets, the RangeBand export range, queue + progress, FCPXML handoff, cloud master | Resolve Deliver; Premiere/Media Encoder | r5 (+11's cloud path) | M |
| **SF-7 Dropped-pages register (Media & Cut)** | the negative face: the chrome-removal ledger, re-entry costs, the surviving features' homes | Resolve Media/Cut as the contrast set | r4 (deprioritized; the supplier rung if re-litigated) | S |

**The non-collision law:** r1→SF-2 + SF-5's engine-home rows · r2→SF-4 · r3→SF-3 · r4→SF-7 · r5(+11)→SF-6 · r6→SF-5's pipeline depth. Every rung's UI surface lands in exactly one face (or SF-1 for shared panels); new work discovered mid-execution files into its face's forward inventory, never a new mountain. **The sequencing** (W1-e §5): SF-0/SF-1 spine first (hardening + the XMOCK-6 mount) → SF-2 via r1 → SF-5 ∥ SF-4 ∥ SF-3 mid-ladder → SF-6 last of the big faces. The P-tier edit styles (replace/fit-to-fill/place-on-top, slide/slip-pointer/blade-click/nudge, three-point) file into SF-2's inventory — the casual bar carries only the T-set (landed by the R35 edit-styles wave).

## 3. The workstreams (the tracks' worklists, keyed to spec §0s)

§0's track table IS the worklist index (every open item in exactly one home). This section carries the two BIGGEST work orders verbatim (the executing agent's briefing):

**S-app's w2 demo — the leg inventory (extends `src/e2e-crawl.test.tsx`, the K4 harness: renderShell/pressKey from nle-ui/testing, zero module mocks, the honest stub census discipline):** the 16 absent routed verbs cluster as — LEG 10 the track verbs (track.add via the toolbar/Add menu → track.remove → toggleLock/toggleMute/setAllLocked/setAllMuted/toggleVisibility, each the real UI surface, one wire verb each); LEG 11 the keyframe family via the volume-line AR-2 gesture (upsertKeyframe → the drag retimeKeyframes → removeKeyframes batch); LEG 12 the batch verbs (the multi-select pool insert → insertBatch; duplicate ⌘D; delete via the ≥5 confirmDelete gate [CR-5's surface, live]); LEG 13 bookmarks advanced (moveBookmark by drag; the EDIT residual stays r1-gated); LEG 14 undo/redo wire-visible (the routed forms — D22b's shell exception now HAS a wire twin at r1; until then assert the shell-path exception honestly); LEG 15 the save/reload round-trip (Save As → the Load door → the fixture rebuilds byte-identical; scene-switch mid-playback [playing/rate survive]; the autosave clean); LEG 16 the color page (the C45/C46-retired surface — the grade sidecar round-trip + setGrade); + the journey integrations (multi-select cross-track arrange + snap on/off in the same drag). Acceptance: the wire-coverage gate still green (the live registry), the e2e re-gates BY EXTENSION, zero mock paths, zero human input.

**S-engine's r1 Stage 0 — the row list (design-r1-port.md §6 :705-801 the authoritative work order):** the seven work items — (1) the seam-shape pairs (§3.5's table); (2) S-ot's linkedTo field + ops/linked.ts closure + the base-verb consumers + the ElementPatch key (OT-side, the engine consumes); (3) the engine fence re-point (§3.2) + F6's sentence (§3.3); (4) the D43 edit-domains.ts fold (C1/C4 + the A-2 fence + the check-2 widening — the constants one-home lands engine-side); (5) rider 9's count reconciliation (§5.1); (6) the union façade's Stage-A mint — the 15 ENGINE-home §4.1A rows (Effect 5 + Mask 4 + Transition 3 + Export 3); (7) the rider census + the 12/17 Stage-0 registrations. Acceptance: 17 §13A.8's five additions each named with its stage slot; battery_r34+ keys the stage.

## 4. The parallelization map + critical path

**Runs in parallel RIGHT NOW (the NOW-set, §0):** S-ot's L3 census + L5 cheap (OT repo) ∥ S-app's w2 demo + the value-passing wires (app repo) ∥ S-engine's W4b + the r1 Stage-0 prep (engine repo) ∥ S-ui's F3-NEW guard (nle-ui repo) ∥ S-spec's battery_r34 (spec repo). The human rounds run whenever the user is ready (nothing blocks, nothing is blocked).

**The live edge set (the discharged R27 edges retired to the lineage table):** E1' the OT carrier landings → the app's value-passing wires → the census flips (CR-6/7 the last carriers) · E2' the r1 Stage-0 execution → Stages 1-4 → the r1 END fold (the C7 + app migration + keymap sync) · E3' the nightly evidence accumulation → W5's thresholds + the PERF-1 smoke promotion · E4' w2's legs land ∥ w1's verdicts (user-paced) → the WALK exit · E5' the w1 verdicts → the mock-retirement final flips (D35.4) + the user-gated register's resolutions fold to the corpus. Everything else is spurious sequencing, deleted on sight (the edge-set discipline).

**The Critical path:** the r1-port EXECUTION round (Stage 0 → END fold) is the long pole; the w2 demo + the L3/L5 waves are the parallel-covering tracks. The user's rounds are the ONLY user-paced dependency — the plan never blocks on them (the register holds the gates).

## 5. The execution protocol (standing practice)

1. **Push/backup every micro step** (GIT IS THE DISK): GitHub origin + the GitLab mirror (the WAF 403 is probabilistic — retry with backoff) + the /home/sync bundles at milestones. never force push (fetch-first everywhere); the two-remote redundancy is the design (SKILL law #184).
2. **The Pin-lockset + battery per round**: one battery fork per round (battery_r34 current at this wrap; the ancestors are frozen records); the rekey scanner (the K1 layered convention) runs ZERO-residual before every wrap; consumer pins re-read LIVE.
3. **The adversarial gate**: every round's fold passes fresh-context reviewers (the corpus audit + the battery/scanner audit as SEPARATE agents; the R33/R34 precedent) before the wrap commit.
4. **The R22 laws** (standing): no mock-first wiring; the boundary scripts gate every package; the census discipline (declared == scraped, CI-enforced).
5. **The AR-2 gesture-pin law + the drag-freeze law + the count-discipline law** (standing, spec 12/17's rows the home).
6. **The landing-vs-filing line** (OT SKILL law 71): land direct fixes whose gates you can run; file titled queue blocks for work whose acceptance lives in the other repo's machinery; a filing older than two rounds is a finding.
7. **The pin-hygiene law** (spec-19 §5.9): cite CONSTANTS and leaf helpers by file:line; method-family citations in symbol+file form.
8. **The intake protocol** (§0): sibling filings absorb via the 5-wave pipeline (SKILL law #179) — the re-orient-first law (#178) governs every round's start.

## 6. Lineage (the traceability appendix)

| Era | Plan | Fate |
|---|---|---|
| R9 | P0-P6 phases | retired to git history |
| R15 | A0-A7b assembly | superseded by R22 (absorbed into the W-rows) |
| R22 | spec 14 v2.1: C0-C4 / W-* / R-* | superseded by the R24 doc (D24's mapping re-homed every row) |
| R23 | the plan doc + ARCH-R23 | superseded by the R24 state |
| R24 | the R24 state + ARCH-R24 | superseded by R25 (D26-D29) |
| R25 | the R25-5 state + ARCH-R25 | superseded by R27 (D30-D36) |
| R26 | the execution round (amendments executed) | folded through the R26 integration review |
| R27 | the D40 overhaul + ARCH-R27 | superseded by R34 (below) — the architecture PRESERVED (gate-defined phases, the workstream carve, the edge-set discipline, the no-estimates law) |
| R28 | the seal round (D42-D50 + the test-law fold) | the corpus record (audits/ARCH-R28-seal-round.md); the plan rows discharged into the corpus |
| R29-R31 | the execution rounds (the fleet re-pins + the K3 corpus + the DOM-structural tranche + D26.5) | the corpus + battery records (battery_r29-r31) |
| R32 | the K4-exit + w1-entry + D52 round | the corpus record; the K4 verdict conditioned |
| R33 | THE INTAKE FOLD (the 87-row register + the OT wire-reality + the pin re-key) | the corpus record; battery_r33 |
| **R34** | **THE SEAL ROUND — THIS doc (the overhaul) + the charter** (`.agents/PLAN.md`'s R34 entry) | **current** — the K2 flip + the K3 VLM markers (K4 RATIFIED); the fleet gap closes: the engine perf.yml+§B.5+CI-variable+P-F4 chain (the nightly + CI GREEN); the OT L2 cheap carriers CR-1/2/3/4/5/8/9 + the fps param M64 + the SURFACE_OWNED_KEYS export; the nle-ui CR pair; the app re-pin + the CR-2 carrier flip + the census re-declaration; the /view fps mechanism; the Testing-mirrors + §14.2 repair; the ladder re-based (CRAWL COMPLETE; r1 = EXECUTION) |

## 7. The audit fleet (the user's standing directive — per-module, per-file, per-track)

**The standing shape (every round):** the scouts → the mock/regional analysts as the round's questions demand → the per-spec fleet at sub-spec depth → the cross-cuts + the plan → the adversarial review BEFORE the amendments → the battery + the fold. The R24/R25 waves are the precedent records (`audits/fleet-r24/`, `audits/fleet-r25/`).

**The R34 wave record:** W0 the re-orientation (the environment restore + the three sibling-queue scouts) → W1 the six-lane parallel open (the Testing-mirrors audit + the K2 draft + the engine perf fix + the nle-ui CR pair + the OT fps param + the app PLAN ticks) → W2 the spec folds (the mirrors + §14.2 + the K2 flip + the K3 markers + the K4 ratification chain) → W3 the fleet gap closes (the OT carrier waves + the P-F4 catch/fix + the CI-variable + the nightly + the app re-pin + the SURFACE_OWNED_KEYS export) → W4 this overhaul + the adversarial gates → W5 battery_r34 + the wrap. The deadline-agent recovery pattern held throughout (5 agents finished their edits before their report deadlines; the worktrees audited + the worklogs reconstructed honestly — SKILL law #175/#180).
