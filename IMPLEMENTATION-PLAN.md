# IMPLEMENTATION-PLAN — the executable plan (separate from the spec set)

**What this is:** THE plan — the execution runbook for the NLE project. It is deliberately NOT a numbered spec: the spec set (`00`-`20`) owns WHAT and the acceptance criteria (each spec's §0 FORWARD INVENTORY — BASE pinned / GAP with owner+acceptance / TEST PLAN, the R22 posture law); THIS doc owns HOW/WHEN/WHO/WHERE — workstreams, sequencing, parallelism, entry points, gates, estimates, protocol.
**Ruling basis:** `audits/ARCH-R24-timeline-strategy-and-topology.md` (v2, adversarially reviewed — rulings D26 the adopt-wholesale convergence / D27 the repo topology / D28 evolve-in-place + the productization gate / D29 the seam-finalization instruments; D23 plan separation + D24 the ladder stand, D25's mechanism amended per D26 — the lineage in §7) + **`audits/ARCH-R25-edit-mode-completeness.md` (v2 — D30 the mode-matrix law / D31 the source-edit family / D32 the linked-companion law / D33 the visual-grammar register / D34 the keyboard family / D35 the reference register / D36 the execution wiring; the plan-side amendments drafted by `audits/fleet-r25/xcut-plan.md` §2-§3)**. Supersedes spec 14's plan content (that file is retired to a redirect stub; the P→A→C/W/R→K/W/R lineage lives in §7).
**Stage vocabulary (D24):** **CRAWL** = the programmatic-verification layer (no human eyes; K1 module nets → K2 combined nets → K3 app-behavior nets → K4 the automated e2e exit). **WALK** = the real app wiring with humans (w1 grammar+fidelity+human rounds, w2 media, w3 project). **RUN** = the full NLE at DaVinci/web-DAW depth (r1-r6). The stages label VERIFICATION DEPTH — code lands continuously in every stream; a stage's exit gate is what sequences.
**Crawl's purpose (the user's law):** eliminate as many issues as possible before the walk — "ideally everything can continue to be tested programmatically"; human-required gates are registered below WITH reasons.

---

## 0. HOW TO EXECUTE — the entry points (start here)

**If you are starting work:** identify your track in the table, open its repo, orient per its row's ORIENTATION cell (the fleet convention is the target repo's `.agents/HANDOFF.md` + `PLAN.md` — exceptions noted per row; WDC keeps its handoff/plan at the repo ROOT), read this doc's §3 track section, execute the FIRST ACTION, and let the GATES tell you when the step is done. **Environment prerequisites (all repo rows — the R24 cold-executor audit, verified live per repo; re-verified R25 by the scouts):** the app's private vendor submodules need `NLE_GH_PAT` + `npm run bootstrap` (README: PAT → non-recursive submodule init → install → annotakit build) before any gate runs; **OT** runs `bun install` AND starts its dev server FIRST (`bun run dev` → :3001 — `bun run test` drives Playwright against `http://localhost:3001/` and does NOT self-start it; Playwright's chromium headless shell must be present — `bunx playwright install chromium`; plain `npx playwright install` rewrites package.json pins + creates package-lock.json, revert both); **WDC** runs `bun install --frozen-lockfile` (its drift gate additionally needs an upstream web-daw clone, `sync --check --source <clone>` — none pending, the lock is clean); **nle-engine** runs `npm install` + `git submodule update --init` with PAT remotes (the SKILL PAT-URL recipe — else the live-run venue is GHA CI); **nle-ui** runs `npm ci` from the committed lockfile (the Storybook build rides CI). Every step ends with: gates green → commit → push → (if module repo) notify the app for the pin bump (the file-queue protocol: a titled block in the target repo's queue doc — for OT, append to `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol: source session, item list, acceptance per item). **A step with no testable gate is not a step — write the gate first.**

**Taxonomy (one sentence):** a TRACK is a repo-owning stream (S-ot/S-app/S-engine/S-wdc/S-package/S-spec — the standing teams); a PHASE is a ladder rung (K1-K4 crawl / w1-w3 walk / r1-r6 run — the sequencing); a WORKSTREAM is a track's worklist slice through the phases (§3).

| Track | Repo (entry dir) | First action (the crawl window's work order) | Inputs (what you need before starting) | Gates (per step) | Done-state (this window) |
|---|---|---|---|---|---|
| **S-ot** | `/home/z/my-project/opencut-timeline` (orient: `.agents/`) | (1) **the carrier-reduction work order — STILL UNFILED (ordered R24; file it THIS window** as the titled block in `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol — the R9 block at :422-466 is the format demo) — **now MERGED with OT's own R9-c upstream-back hunks (D36.4, the consumer-proven set):** the 6 optional injectable props (the selection-bridge pair `initialSelectedIds`/`onSelectionChange`/`selectionIds`, the zoom bridge `zoom`/`onZoomChange`, `confirmDelete`, the `timeline-scroll` anchor, `cancelRegistry` injection — additive-optional, classic defaults M-pinned) **+ `EMPTY_CONTEXT_ITEMS`** (already D26.3 item 1, the stability bug-fix) **+ the functional `setPos(null)`** (flips the app's TimelineContextMenu carrier) **+ the W8 `!shiftKey` playhead-guard fix — the hole-DOCUMENTATION landed @ `fdb771c` (R9-c item 0, a PLAN-only commit; the filing half is done, do NOT tick the fix); the OT-side code hole is STILL OPEN** (only the app-side Z4 twin + the hole-documentation landed; the OT-side fix stays a live row) **+ the jsdom no-DragEvent guard/docs note**; each landing flips an app carrier toward exact (census-tracked); (1b) **`WIRE_UI_EXCEPTIONS` as a first-class shared export** (OT R9-d — the 6-verb exception registry is test-local today, zero src matches; the export keeps the consumer's ported gate from registry drift; pairs with D29's coverage-gate instrument, tsc-lockstep shape); (1c) **R9-b's keyframe-authoring ruling ASK** (file with the S-spec mode-matrix registration: upstream lane affordance vs consumer-territory — the ruling gates any R9-b work); (2) **the data-test stability contract** (D26.5): freeze OT's 68-site `data-test=` convention AS the stability contract + close the census gaps — NOT a new `data-testid` layer (unchanged, unfiled); (3) **the view-config surface** (zoom-ladder config + ripple-toggle exposure — the C1 OT-side halves, verified still-absent at the code pin `c15a629`); (4) **K2's in-page runner registration** (the runner + the M49C coverage gate EXIST and are green 24/24 at the pin — the registration is the filing as K2's OT-side completeness instrument, not the building); (5) the **structural grammar half** (crawl-tail: the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas — additive components, classic defaults M-pinned); (6) **the token half at w1-entry** (the mini theme mode on `globals.css` `:root` + `theme.ts` — D25.3a). **R9-a (the insertBatch/batch-keyframe endorsement) needs NO action — already registered spec-side (15 §13.15, the D29-F8 endorsement).** | this doc + ARCH-R24 (D26/D29) + ARCH-R25 (D36.4) + the app's port census (`nle-test-app/docs/audit-port-convergence-r9.md` + `UPSTREAM.lock.json`) + OT's R9 queue (`.agents/PLAN.md:432-466`) | `bun run dev` (:3001) THEN `bun run test` (536→, the M-report json, 59 entries incl. M49C), `tsc --noEmit`, the real-mouse suites; every prop/contract lands with new M-pins for every classic default | the carrier-reduction work order FILED (+ the WIRE_UI_EXCEPTIONS export + the ruling ask filed); the carrier-reduction landings flip app carriers toward exact (census-tracked); the `data-test` stability contract frozen + its census gaps closed; the view-config surface + the structural components exist additively; the mini theme mode exists at w1 |
| **S-app** | `/home/z/my-project/nle-test-app` (orient: `README.md` + the `worklog.md` tail — the README's gate-count/import rows are stale (30/596/"not imported yet", all false at 206); refresh it; the `.agents/` below becomes the orientation home) | (0) **create the app's `.agents/`** (HANDOFF + PLAN minimum, the fleet convention — **OVERDUE ×2 (D27.2): ordered BEFORE W-C; W-C..W-G all ran without it**; promote the existing content, don't rewrite it: the worklog's R9 entries + the nle-ui repo's `.agents/` R9-close — the DragEvent trap, the LIFO law, the per-core wire mint, the r-key lockstep) + **the README refresh**; (1) **the app's OWN D30 waves W-C..W-G: DONE @ `64fb0ab`** (206/206 live-run; the coverage gate green 24/24 routed − 6 exceptions; the census at its declared irreducible set **35 zero-action + 5 carriers + host**; 9-G1 REV PROCEED-WITH-FIXES, both P1s fixed; the W-G CRs fired to OT `c37844f` + engine `3989506`) — the fork-retirement gate's carrier-count condition is MET; what remains of the D26 census law is **(1b) the CI enforcement — a QUEUE FILING (D36.1): the spec round FILES the mechanism block into the app's queue (a `scripts/census-check.mjs` step in `ci.yml`, owner S-app, gate = register-inequality FAILS the build; the app session EXECUTES it)** — author the census script + the register doc (the vendored pin `c15a629` vs the port tree, import-specifier-normalized per-file diff; the per-pin declared values 40 files = 35 zero-action + 5 carriers + the host, never hardcoded; the current `ci.yml` runs NO census step — verified R25) + **(1c) the W-G reconciliation filing (D36.2): re-file the D29.5-amended dispositions at `nle-engine/.agents/PLAN.md`'s R9 queue** (the W-G filings landed PRE-amendment — 3-of-4 entries stale; the spec round files the re-file, the engine session executes) **+ land the maintainPitch residual HERE** (`engineService.ts:477/:486` hardcodes `maintainPitch: true` — the Inspector Preserve-pitch toggle is dead app-side; the engine-side threading IS landed at scene-to-segments.ts:246) + **(1d) the E1/E2/E3 defect-row filings into the engine's queue** (per the S-engine row — the spec round files, the engine session executes); (2) **K3's store/policy halves ∥ NOW** (unstarted, unblocked — the useMini-family re-expression as app tests over the real vendored OT; the re-expression target = the app's real surfaces: the GluedShell store family + `engineService.ts` + `sceneBridge.ts` — CORE-SEAMS §2 is the field-by-field map; each LAW-NET family's tests land in the app's vitest beside the surface they pin); (3) **K3's DOM-structural halves** (now gated ONLY on the data-test mapping row — W-C landed; D26.5's three-way vocabulary: OT `data-test=` for the timeline units / nle-ui's `shell-*` for the chrome units / the structural-gap worklist for the no-counterpart units; the mini's 59-static + 15-templated census gets the mapping row); (4) **K4** (the e2e — drives the same routed verbs, the shell-path exception applies); (5) **K3's audio pins + the VLM net port** (the audio pins STILL hard-gated on S-wdc's unlanded waveform promotion — the only K3 sub-half with an external prerequisite; adopt `ui-mock/shell-variants/scripts/vlm-*` onto the app's `.storybook` + OT's runner; REFERENCE-REGISTER.md's rows are the verification worklist); (6) **the mode-matrix K3 rows** (the slip pin slip-G-SLIP-7: a wireLog 'slip' assertion + a D28 dispatch pin for the trimStart/trimEnd patch shape; the composed ripple family ripple-trim-G4: 4 units/16 tests over the app's composition; the K3 selectTool controller layer roll-ROLL-7: the tool radios stop being inert view-state; the group-tightest companion clamp fix slip-G-SLIP-6: mirror `_slipImpl`'s tightest-δ) | this doc + ARCH-R24 (D26/D27/D29) + ARCH-R25 (D36) + `docs/design-r9-d30.md` + `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` (the acceptance list) + `CORE-SEAMS.md` + `NLE_GH_PAT` + `npm run bootstrap` | the app's vitest (**206→**), `tsc`, the boundary script, `vite build`, **the census script (CI-gated once written — the D26.2 law's missing half)**, the wire-coverage gate | `.agents/` exists + README truthful; the census register CI-enforced; the engine R9 queue reconciled + the maintainPitch residual landed; the K3 corpus row-by-row green (322 authored + 33 projection pins **+ the mode-matrix wired-subset rows**); the audio pins + the VLM net live; **K4: the automated e2e green, zero mock paths** |
| **S-engine** | `/home/z/my-project/nle-engine` (orient: `.agents/`) | The live queue: (0) **the consumer re-pin wave: DONE at `5036387`** — engine HEAD `3989506` is docs-only over it (the R9 queue filing); the app's re-pin is optional freshness, not required; (1) **the R9-queue reconciliation (REWRITTEN per D36.2 — the W-G filings landed PRE-amendment; 3-of-4 entries carry stale dispositions; re-file as a titled amendment block in the engine's queue — the spec round files, the engine session executes):** R9-a (transform sidecars) re-labeled **FUTURE-EXTENSION** (the composition-frame.ts:70-94 slots stand; not a near CR); R9-b (maintainPitch) **CLOSED as LANDED engine-side** (scene-to-segments.ts:246 threads `el.retime.maintainPitch` → `SegmentPlayOptions` :518/:611) with the residual **re-filed APP-side** (the app's engineService hardcode — the S-app row); R9-c (scene-grade promotion) **WITHDRAWN** (the DECLINE is the law — consumer-side final pass; Z2's app fix stands; spec 04/08 carry the decision; not an engine row); R9-d (honest-preview widening) **unchanged-OPEN** (Vignette/Glow/LUT-preview v1 remain; only the Gaussian-Blur subset landed @ `8a0b7fe` + the `4b2dfd1` radius clamp); (1b) **the mode-matrix defect rows E1/E2/E3 (pre-r1 — QUEUE FILINGS into the engine's queue doc per D36.2, titled blocks with the fix contracts + pin requirements; the r1 port's PRECONDITIONS — the ports must not carry the defects):** **E1** the linked mid-clip insert corruption (insert-G2 — live-probed, unqueued anywhere: `_performInsertEditImpl`'s Phase 2 pulls the split's LEFT half into the shift set (00:838 no-overlap violation) while the companion shifts whole and unsplit; the D32.6 fix: exclude target-track clips with `from < insertFrame`, split straddling companions; pin = a linked mid-clip insert fixture with the 4 postconditions); **E2** overwrite's companion asymmetry (removed clips cascade; trimmed survivors' companions don't — D32.1's overwrite row: the companion trims to match; pin = an A/V overwrite straddle fixture); **E3** insert's silent transition-blocked abort (R1-B4: empty return, no error surface — re-surface as the error contract); **plus the mode-fleet defect rows:** the roll counterpart clamp (ROLL-2 — no central `keepTightestDelta`, A/V junction desync), the roll/slide/ripple-trim transition+keyframe preservation clamps (ROLL-3, slide-G3, ripple-trim-G3 — the binary-search clamp + `applyTransitionRepairs` are spec'd (06:1145/15:642) but unported), **rippleTrimItem exposure + first tests** (ripple-trim-G3); (2) the projector parity corpus (the N1 composition-frame family's formal S4 suite — incl. the W2.5 effects sidecar + `buildElementFilterString`); (3) the color instruments (scopes/qualifier/power-window — spec 08 §0's S-engine row; the 4-6 wk estimate is the ARCH-R22 reviewed register); (4) **K2's engine-side pairs: OT-ops→engine-decode round-trips at the app's seam shapes (extends `tests/vitest/engine/timeline-edit-ops`/`video-sync`) — GROWN by the mode-matrix wired-subset pairs** (the 6/10 class methods ↔ the K2 mode rows: `rollingTrimItems`/`rippleTrimItem`/`slip`/`slideItem`/`performInsertEdit`/`performOverwriteEdit`) | the queue in the engine's `.agents/PLAN.md` (:135-163) + spec 01/03/04/08 §0s + ARCH-R25 (D36.2 + the E1-E3 fix contracts §5) + the mode reports' G-rows | the engine's vitest (458→), `tsc`, the vendoring sync-check | the R9 queue reconciled (the 4 dispositions textually matching D29.5/D36.2); the E1/E2/E3 + mode-defect filings landed in the queue, then the fixes green pre-r1; the K2 engine-side pairs green (incl. the mode-matrix wired-subset); the widening + the parity corpus + the instruments' enumerated milestones each green as queued |
| **S-wdc** | `/home/z/my-project/web-daw-core` (orient: the repo-ROOT `HANDOFF.md` + `PLAN.md` — NOT `.agents/`, which holds SKILL.md only) | The live queue (the S4 seal round is CLOSED — 759/759, P3-only, zero upstream drift; the SoundTouch offline pitch + varispeed W2 LANDED @ `de09c93`, consumed): (1) **FIRST (D29.4 + D36.3, REQUIRED — K3's audio half HARD prerequisite — NOW 2 ROUNDS UNEXECUTED, the venue settled: promote the waveform-data seam contract into the ROOT `HANDOFF.md`'s next-session scope** (WDC's convention — the repo-ROOT HANDOFF, verified R25: its "Next-session scope" (:32-58) reads exactly the four pre-promotion items — the mixer, the "4" deferred ports, the async pre-retime, P3 — NO waveform row exists anywhere in WDC's queue; the R24 filing stayed spec-carried only) **+ STATE the contract** (the de-facto `getAudioBuffer`/`setAudioBuffer`/`getPeaks`/`setAudioPeaks`/`computePeaks(buffer,buckets) → {min,max,buckets}` shape at `audio-registry.ts:24-74`, `PEAK_BUCKETS = 2000` + the offline-render/call-spy pin shapes the app authors against) **+ pin it** (a vitest contract test in WDC's own suite — 759→); **the row is NOT DONE until the root HANDOFF SHOWS it (the D36.3 filing-verification gate)**; spec-20 §0:25/§0:151 already carries the spec-side half — the WDC-side landing closes the loop and unblocks K3's audio half; (2) the mixer surface (spec 20 §0's M2 remainder — EQ/reverb/sends/inserts/sidechain/PDC; design round FIRST, the F6 el.effects-sidecar decision included); (3) the **FIVE** deferred upstream test ports (the label says "4" — an off-by-one carried in WDC's PLAN/HANDOFF and spec 20 §0:21; 5 bodies: engine.test, engine-metronome-automation, engine-param-channel, dsp-aux-sends, wam-effect — nle-engine-side ports onto scene-mixer); (4) the async pre-retime (design-w2 §4; the parallel stream is ACTIVE on nle-engine); (5) the JKL R4 residue (⇧K dead key in the engine world — a P3 doc item) | the WDC root HANDOFF + spec 20 §0 + ARCH-R25 (D36.3) | WDC's vitest (759→), `tsc`, the drift-gate (fails-on-drift; needs an upstream web-daw clone — none pending) | the waveform contract stated + pinned **at the ROOT HANDOFF (the filing-verified venue)** + the mixer + the five ports green as queued |
| **S-package** | `/home/z/my-project/nle-ui` (orient: `.agents/`) | (1) **C0 MiniShell** (the w1-prep chrome family: Topbar/Inspector frame/MediaPool frame/Splitter+R18j laws/Toast role=alert/Viewer frame + tokens/qc- CSS; slots-compatible; package-owned minimal placeholder; zero engine imports — the boundary script); (2) **the filing-gap actions (D27.1) — state at R25 (D36.4: the still-open filings STAY, not dropped): DECISIONS #30 FILED ✓ (`d22e4e9`); the D30 W-D package edits LANDED AS CODE ✓ (`2646d80` — the package's own W-D wave; the filing is moot, mark done-by-landing); REMAINING: file the C0 MiniShell row in nle-ui's OWN PLAN** (grep-verified R25: no row exists — the spec's "filed in nle-ui's PLAN queue" expectation STILL unmet 2 rounds later) **+ file D29 package-side** (the DECISIONS ledger jumps D28 → #30 — D29's instrument adoptions were never recorded) **+ tick P-widen + P-lock-route** (both verified still unchecked at `.agents/PLAN.md:210/:219` — ordered ticked); (2b) the mode-matrix package rows (r1-adjacent): the ripple-tool inert-for-trim posture (ripple-trim-G7: route the gesture — the variants' `Clip.tsx:586-595` is the reference — or document mode-inert until OT's G2 lands) + the slip keymap law (slip-G-SLIP-5: the runtime-fps resolver or the registered deviation; the tool-gated disambiguation); (3) W-ops→r1 keymap surfaces later | `ui-mock/shell-mini/docs/RH-skin-extraction.md` + the mini's components + spec 18 §0 | the package's vitest (674→), `tsc`, the boundary script, the Storybook build (rides CI) | MiniShell renders in the package Storybook with the chrome laws green; the C0 row + the D29 DECISIONS entry filed package-side + the P-ticks ticked; the ripple-tool + slip-keymap postures registered |
| **S-spec** | `/home/z/my-project/nle-core-spec` (this repo) | (1) **battery_r25's core FIRST** (the R25 pin world + the census-coherence checks + the routed/exceptions check + the plan-gate re-points; **the new check classes per ARCH-R25 §7: the mode-matrix presence checks (06 §0's matrix + the four family sections + the P-list rows gone), the register checks (REFERENCE-REGISTER.md exists; the 19-family census; the C-ledger dispositions complete; the two mocks' suite-count pins — variants 1521+ / mini 355), the keyboard family checks (16's source-edit block + the four-meaning row + the slide rows), the grammar checks (05 §8A's row count + 18 §9's token count), the plan checks (S-app's post-D30 state + the census-CI mechanism row + the WDC root-HANDOFF venue), the pin re-bases (engine `3989506` / OT HEAD `fdb771c` code-pin `c15a629` / WDC `85b81b0` / nle-ui `3026099` / app `64fb0ab`)**; the fleet's rounds gate on it); (2) **the K2 combined-family census + spec 12/17 registration at K2 entry — STILL PENDING, now grown by the mode-matrix registration** (§2's K2 gate: the 10-mode matrix in 06/17 + the D30.3 zero-orphan-rows law); (3) **the C-ledger rolling disposition + the register maintenance (D35.2/D35.3, standing every round): REFERENCE-REGISTER.md's test-pin counts re-derive live (a drift without amendment = a battery failure); every C-row adopted/queued-with-owner or declined — zero un-tagged rows older than two rounds; rows flip LANDED as the owning specs' rows land; the D26.4 retirement triggers key on the flips** + the R25 fold (the 5 scout cards + the 10 mode reports + the 5 cross-cuts); (4) **the mode-matrix S-spec rows THIS ROUND** (the 06/15/16/17/18 authoring rows — ARCH-R25 D30/D31/D32 + the P-list + the D34 keyboard family; the track routing in the xcut-plan §3 table); (5) the mode-family scope-gate preparation (the r1-entry decision material: the mode fleet's 10 verdict cards + the ~70-76 gap rows + the +1.5-3 wk rider) | this doc + ARCH-R24 + ARCH-R25 (D30-D36) + `REFERENCE-REGISTER.md` (D35) + the fleet reports in `audits/fleet-r25/` | the battery (r25 core, then full) after every round; the sibling-race merge-first protocol | battery_r25 green; the K2 census registered at K2 entry; the R25 fleet folded through the integration review; the register current (fresh + intake-complete) |

**The single global entry point:** this table. When in doubt: S-app row → step (0) then step (1b)/(1c) (the `.agents/` surface + the census-CI queue filing + the W-G reconciliation filing — the critical path's head; the D30 waves themselves are DONE @ `64fb0ab`; step (2)'s K3 store/policy halves ride ∥ immediately against the injection-point contracts).

**Cold-executor re-simulation (R25 — the verification notes for this state; the 6 most-changed rows walked as a cold executor would, 6/6 PASS, from `audits/fleet-r25/xcut-plan.md` §4):**

| Row | Entry point (verified live) | Action | Gate (testable) | Blocked? |
|---|---|---|---|---|
| **R1 — S-app step (0), the `.agents/` bootstrap** | `/home/z/my-project/nle-test-app` — README + worklog tail resolve; both content sources named + reachable (the worklog's R9 entries + the nle-ui repo's `.agents/` R9-close) | create `.agents/HANDOFF.md` + `.agents/PLAN.md` (promote, don't rewrite) | `ls .agents/ && git status --short` shows both files tracked | No — **PASS** |
| **R2 — S-app step (1b), the census-CI enforcement** | the app's port tree + `vendor/nle-timeline` (the byte-exact lock-copy @ `c15a629`, pinned by `UPSTREAM.lock.json`); the recomputation method fully specified (import-specifier-normalized per-file diff) | author `scripts/census-check.mjs` + the register doc + the `ci.yml` step (insertion after `npm run boundary` — the current ci.yml read R25: no census step exists) | the script exits 0 at the current tree (40 = 35 zero-action + 5 carriers + host) AND register-inequality fails the build (mutable-mirror check) | No — **PASS** |
| **R3 — S-engine step (1), the R9-queue reconciliation** | `nle-engine/.agents/PLAN.md` R9 queue (:135-163 — the 4 unchecked entries, readable + unambiguous) | apply the four D29.5/D36.2 dispositions (each a one-paragraph doc edit, evidence named: composition-frame.ts:70-94 / scene-to-segments.ts:246 + the app's engineService.ts:477/:486 / the `8a0b7fe` design) + re-file the maintainPitch residual app-side | the queue's dispositions textually match ARCH-R24 D29.5 + ARCH-R25 D36.2, and the app-side row exists (grep both queues) | No — **PASS** |
| **R4 — S-wdc step (1), the waveform-contract promotion** | `/home/z/my-project/web-daw-core` — the ROOT `HANDOFF.md`'s "Next-session scope" (:32-58, items 1-4 enumerated, the waveform row absent) exists and is the correct venue | insert the waveform row (the contract statement pre-drafted in the S-wdc cell + spec-20 §0:25 — the audio-registry.ts:24-74 shape) + author the pin | the row present in the ROOT HANDOFF + `bun run test` green with the new pin (759→) | No — **PASS** |
| **R5 — S-ot step (1), the carrier-reduction filing (merged with R9-c)** | `opencut-timeline/.agents/PLAN.md` (the R9 block at :422-466 demonstrates the titled-block shape to append after) | append the work-order block (the 6 props + the 4 R9-c hardening hunks — every item enumerated with acceptance in the S-ot cell; the hunks' text already written in OT's own PLAN) | the block exists with per-item acceptance; future landings are census-tracked (R2's script is the tracker) | No (a docs commit; the `bun run dev` + test gates apply to the code landings, not the filing) — **PASS** |
| **R6 — K3's amended exit gate, the mode-coverage pin family** | the app's vitest suite + `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` (the acceptance list, cited by the row) + the named mode rows (slip-G-SLIP-7, ripple-trim-G4 — the test specs drafted in the xcut-plan §3 table) | author the slip pin (the wireLog 'slip' assertion + the dispatch pin — the verb is routed app-side today: `store.slipNudge` → the `slip` case in engineService) + the composed ripple family (4 units/16 tests — the `trimToPlayhead(edge, ripple)` path exists, half-pinned at GluedShell :361/:368) | `npx vitest run` green with the new rows (206→) | No — **PASS** |

---

## 1. The fleet (cited, not re-declared)

The pin world lives in `00-master-spec.md`'s fleet table (D21 — HEAD pins AND consumer pins, both classes; the battery enforces coherence; 00-master is the canon). Current reference set (2026-09-09, the R25 re-baseline; **battery_r25 re-baselines the pin world once, centrally, after the fleet**): engine `3989506` (458 — ONE docs-only commit over the R24 pin `5036387`) / OT HEAD `fdb771c` (536; **code pin `c15a629` UNCHANGED — src-diff empty; consumers current without action**) / WDC `85b81b0` (759 — zero movement, the seal held) / nle-ui `3026099` (674 — shell/keymap/docs only, zero `src/components/timeline/` commits) / **app `64fb0ab` (206, LIVE-run this round — the R24 static census 174 superseded)** / mini 355 (sealed) / variants 1521+. **Consumer pins (live):** engine→OT `c15a629` + WDC `494f6ff`; app→engine `5036387` + OT lock-copy `c15a629` (byte-exact minus `testing/`) + **nle-ui `3026099`** (bumped by the W-D lockstep) + WDC `85b81b0`. **This plan never re-declares pins — it cites them.**

## 2. The stage ladder (D24) — exit gates + gate classes

Every gate is tagged **[P]** (programmatic — the default; nets, pins, scrapes) or **[H:reason]** (human-required — the registered exception, only at walk for the classes the user named).

### CRAWL — the programmatic-verification layer

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **K1 module nets** | each repo's own suite, extended per its spec §0 GAP acceptance rows (the R25 state, live-verified by the scouts: engine 458/458 @ `3989506`, OT 536/536 @ `fdb771c` incl. the W11 milestone extensions + the M49C gate 24/24, 59 entries, WDC 759/759 @ `85b81b0`, nle-ui 674/674 @ `3026099`, app 206/206 @ `64fb0ab` — live-run) | every repo's suite green at its own gates; every §0 GAP row's owner-repo carries the row's pin | [P] |
| **K2 combined nets** | the named cross-module family: engine-in-the-middle (OT-ops × engine composition), WDC's integration family, OT's in-page UI-over-core runner (pixel reads + real-mouse) — censused + registered in spec 12/17 (the registration is S-spec's pending row); **OT's in-page runner + the M49C coverage gate ARE K2's OT-side completeness instrument** (registered, green 24/24 routed verbs + 6 documented exceptions at `c15a629`) **+ THE 10-MODE EDIT-MATRIX registered in spec 06/17** (the R25 mode fleet's instrument — the completeness census the way the 24+6 wire census is: mode \| engine math \| OT wire verb \| UI affordance \| pins \| verdict; **baseline at the R25 pins: 6/10 engine-class-only (roll/ripple-trim/slip/slide/insert-edit/overwrite — unwired, thin pins), 0/10 wired, 4/10 absent (replace/append/ripple-overwrite/fit-to-fill)**; the wired-subset pairs are machine-checkable: the engine class methods ↔ the OT runner families) | the family census green; the missing pairs closed (OT-ops→engine-decode round-trips at the app's seam shapes) **+ the mode matrix registered + the wired-subset pairs green (the round-trips extended by the 6 existing modes' math) — the D30.3 K2 form: every matrix row is either LANDED (ported + pinned where r1 has landed it by then) or carries a live r1-scheduled row with owner + acceptance; ZERO ORPHAN ROWS (the completeness is over the REGISTER, not over landed-ness — the ports land at r1 per D30.2, and K2 checks the register)** | [P] |
| **K3 app-behavior nets** | the app wiring driven headlessly: **LANDED @ `64fb0ab`: the routed-verb completeness machine-check** (the D30 W-F coverage-gate port — the module-scope accumulator + the three gesture sims + the final assertion `accumulated ⊇ WIRE_COMMAND_TYPES − 6 exceptions`, through the real mounted UI; the LIFO accumulator law; the jsdom DragEvent solution) **+ the Z6 dead-zone sweep pins**. **OPEN: (a) the compensating shell-path pin family** (the trusted-integrator verbs ⌘B/[ ]/⌥[ ]/,/./M/⇧M/⌘M/⌘⇧M are permanently invisible to the gate — D30 R2/R8's owned consequence — drive them directly through `engineService`, owner-tagged app-side; **PARTIAL: ⌥[/⌥] ripple-trim + ⇧M pinned; slip `,`/`.` + the ⌘B/⌘M/⌘⇧M family unpinned** — the mode-matrix K3 rows enter); **(b) the LAW-NET corpus re-expression** (322 authored + 33 projection pins, unstarted — incl. the composed ripple family, the GAP-W-ops rows, 4 units/16 tests, composed app-side over `timeline.trim`/`rippleDelete`; r1 graduates the OT interval-diff family; LAW-NET-INVENTORY is the acceptance list); **(c) the DOM-structural gate = D26.5's three-way data-test mapping** (OT's frozen 68-site `data-test=` for the timeline units / nle-ui's `shell-*` for the chrome units / the structural-gap worklist for the no-counterpart units; the mini's 59-static + 15-templated census gets the mapping row — **now gated ONLY on the mapping row, W-C landed**); **(d) the offline audio pins** (Node-venue law; **hard prerequisite: S-wdc's waveform seam contract — the only K3 sub-half with an external prerequisite**); **(e) the VLM visual net** (adopted from the variants' R23-G, on the app storybook + OT's runner; the REFERENCE-REGISTER rows are the verification worklist); **(f) the mode-matrix K3 rows** (the app's mode-coverage pin family — the slip pin, the composed ripple family, the selectTool controller, the companion clamp) | the coverage gate green + the compensating pin family green; the corpus row-by-row green; the mapping row enumerated present/absent/structural-gap; the audio pins green (hard prerequisite: S-wdc's waveform seam contract) **+ the app's mode-coverage pin family green for the end-to-end wired subset (split-insert, DnD-insert, slip-keyboard, ripple-trim-keyboard) + the honest-absent verdicts registered (no silent mode absences) — the D30.3 K3 form: the app column matches the LANDED set with pins** | [P] |
| **K4 the crawl exit** | the single automated e2e suite: import(virtual) → cut (drag/trim/split/ripple) → play (engine clock) → export — **driving the same routed wire verbs through the real UI (D29.1c; the shell-path exception applies identically — the trusted-integrator verbs driven through engineService)** | **green end-to-end, zero mock paths, zero human input** — the crawl's completion proof | [P] |

**Ordering law (hard edge — RE-STATED per D26/R25):** K3's DOM-structural half gates on **the data-test mapping row ALONE (D30's W-C LANDED @ `64fb0ab`)** — NOT on a swap+testid layer (the D25 ordering is retired with the vendor-extension mechanism). K3's store/policy halves run ∥ (unstarted, unblocked) against the injection-point contracts. The crawl's structural grammar work (D25.3b's structural half: the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas) lands IN the crawl window as OT additive components — **crawl = structure + behavior; w1 = tokens + fidelity** (the qc- token/theme half is walk).

### WALK — the real app wiring, humans enter

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **w1 grammar + human rounds** | the token half of the grammar (the qc- theme on OT's CSS surface), the MiniShell chrome wiring (C0 consumed), the viewer-frame fidelity, the app-vs-OT-runner side-by-side, the annotakit review loop for the app | the side-by-side VLM pass; **the human test rounds on the complex timeline operations** (the class the user named as needing human testers — drag/trim feel, interaction latency, edit-session flow); every finding lands as a new programmatic pin wherever possible or is registered with owner | [P] + [H:interaction-feel — the user's named class] |
| **w2 media layer** | the MediaRecord registry + probe + lookup, the full event staircase (spec 15 §9.5), telemetry (reference row: cloudcut-nle's hybrid-storage RFC — the three-tier storage + 480p-proxy pattern + the Tauri desktop path, D27.3 — a design input only, NO dependency) | media round-trip pins; every staircase row published + consumed + pinned; the full-scope demo on virtual media re-gates | [P] |
| **w3 project layer** | ProjectJSON persistence (spec 09), multi-scene + scene wire ops, cross-scene-undo + history budget | project round-trip vs per-scene OT toJSON; the scene suite green | [P] |

**Walk exit:** a real edit session — import real media → edit → save → reload → export — human-tested with the findings register closed or owner-tagged. **[H:real-session — the walk's purpose: the human-verifiable product moment; the complex-timeline-operations class extends here]**

### RUN — the full NLE (DaVinci Resolve + web-DAW class, the shell-variants scale)

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **r1 op depth** (was W-ops) | slip/slide/roll/rateStretch (wave 1), retime/freezeFrame/rangeRemoval (wave 2), error-envelope refinement (spec 15 §6.3 first), **C7 rename at END** + the one-business-day app migration + keymap-sync sub-gate **+ (scope-gated) the mode-matrix r1 rows: the insert-edit/overwrite 3-point ports + the four absent families' implementations (replace/append/ripple-overwrite/fit-to-fill) + the OT verb/port family (`timeline.roll`/slide/`trim{ripple}`/insertEdit/overwrite/replace/append/insertBatch)** | S1 dispatch-complete + typed NOT_IMPLEMENTED honest; OT suite + carried tests green; spec 15 §13.15 rows flip ALIGNED | [P] |
| **r2 audio depth** (was W-audio) | mixer G-wiring full depth, sidechain/PDC/automation shapes (SoundTouch offline pitch + varispeed: **W2 LANDED @ `de09c93`** — consumed, not re-done; N2b keyframed volume: **LANDED @ `37cdd28`** — consumed) | offline parity ≤ −60 dBFS any channel + realtime behavioral pins; the null rig passes | [P] |
| **r3 color** (was W-color) | the grade-math binding + parity; the S-engine instruments consumed (scopes/qualifier/power-window) | grade-math parity pins (max delta ≤ 1 LSB-equivalent); scopes plot engine data + pinned | [P] |
| **r4 real media decode** (was W-n5) | N5: the media registry + real decode → VirtualMediaAsset | **PENDING the user's D6 re-affirmation (deprioritized)** | [P] |
| **r5 interchange + polish** (was R-fcpxml/R-polish) | FCPXML (greenfield; the parser+fixture-corpus choice is the phase-entry artifact), keymap long tail (C22 ledger), i18n, a11y residuals | validates vs the chosen reference parser on the corpus; spec 18 §11 audit; battery | [P] |
| **r6 the non-blocking tail** (was R-engine-p2/R-cloud) | CPU transition renderers, Shape/Lottie/Subtitle/Controller surfacing, other engine P2; cloud render UNCHARTERED unless the user re-scopes | per-item engine milestones green as queued; slips independently by design | [P] |

## 3. The workstreams (the tracks' worklists, keyed to spec §0s)

Each track's worklist rows cite the OWNING spec's §0 GAP register (the posture law — acceptance lives there, not here). Phase tags use the D24 vocabulary; the old→new mapping is §6.

**S-ot (opencut-timeline):** the carrier-reduction program (filed THIS window, merged with OT's R9-c hunks) + WIRE_UI_EXCEPTIONS + the data-test contract + the view-config surface → K2's runner registration → the structural grammar half → **r1's op families + C7 (GROWN by the mode-matrix r1 rows: `timeline.roll`/slide/`trim{ripple}`/insertEdit(3-point)/overwrite/replace/append/insertBatch + the M49C census re-declare law — tsc-lockstep, mechanical) + the carried-test families (ROLL-5's three roll laws, slip-G-SLIP-9, slide-G3) + the gesture routing (ripple-trim-G2, ROLL-7's OT half, replace-G-R7)** → the insertBatch/batch-keyframe landings (already endorsed, 15 §13.15).
**S-app (nle-test-app):** `.agents/` (overdue ×2) + the README → **the census-CI enforcement (the D36.1 queue filing) + the engine-R9 reconciliation filing + the maintainPitch residual** → K3 store/policy halves ∥ → K3 DOM-structural (the mapping row only) + K4 + the mode-matrix K3 rows → w1 (token wiring + side-by-side + human rounds) → w2 media (spec 15 §9.5 + spec 09's media half) → w3 project (spec 09) → r5 fcpxml. *(the D30 line drops to DONE.)*
**S-engine (nle-engine):** the R9-queue reconciliation (the D36.2 queue filing) + **the mode-matrix defect rows E1/E2/E3 + ROLL-2/ROLL-3/slide-G3/ripple-trim-G3 (pre-r1)** + the widening (R9-d) + the parity corpus + the instruments + K2's pairs (grown by the mode-matrix wired-subset) → r2/r3/r6 consumption points as they land.
**S-wdc (web-daw-core):** the waveform-contract promotion (STILL first — 2 rounds unexecuted; the ROOT-HANDOFF venue, D36.3) + the M2 mixer surface + sidechain/PDC/automation (spec 20 §0) + the FIVE test ports + the async pre-retime → r2 depth.
**S-package (nle-ui):** C0 MiniShell (spec 18 §0) + the filing-gap residuals (the C0 row, D29 package-side, the P-ticks — still open per D36.4) + the ripple-tool posture + the slip keymap law (r1-adjacent) ∥ crawl → w1 consumption → r1 keymap surfaces → r5 polish rows (C12/C11/C14/C15).
**S-spec (this repo):** the R25 fold + battery_r25 + **the mode-matrix registration (this round)** + the C-ledger rolling disposition + **the register maintenance (D35: REFERENCE-REGISTER.md's freshness pass + the intake discipline, every round)** + the mock retirement triggers (post-K4/w1 — keyed on the register rows flipping LANDED).

## 4. The parallelization map + critical path

**Runs in parallel RIGHT NOW (nothing blocks another):** S-app's `.agents/` + census-CI filings + K3 store/policy halves ∥ S-ot's carrier-reduction filing + program ∥ S-engine's R9 reconciliation + the pre-r1 defect rows ∥ S-wdc's waveform promotion + M2 ∥ S-package's C0 + the filing gaps ∥ S-spec's battery_r25 + the amendment fleet. Six streams, six repos, six gate sets — the module streams never touch each other's trees (cross-repo work is FILED as queues, never unilateral).

**The dependency graph (the hard edges — re-drawn per D26, updated R25):**
```
S-wdc: the waveform seam contract (ROOT-HANDOFF promotion + stated + pinned — 2 rounds unexecuted, D36.3) ─────┐ (a hard prerequisite)
                                                                                                                ↓
S-ot: carrier-reduction program + data-test contract    (the ∥ stream — gates nothing)                  K3's audio-pin half
S-app: .agents/ + census-CI (the D36.1 filing) ──┐              (K3's store/policy halves ∥ — unstarted,
S-app: K3 store/policy halves (∥, ctrcts) ───────┤               unblocked, against the contracts)
                                                 ├→ the data-test mapping row → K3 DOM-structural → K4
S-ot: structural grammar half (crawl-tail) ──────┘                                   (crawl exit)
S-package: C0 MiniShell ─────────────────────────┐                                   │
                                                 ↓                                   ↓ (the suite extends)
               K4 ──→ w1 (tokens + fidelity + human rounds) ──→ w2 ∥ w3 (w2's demo re-gate = K4's suite extended) ──→ r1..r6
                                                                                                    ↑
                                               the productization gate (D28.2) noted at r1-entry
S-spec: the mode-matrix registration (R25, THIS round) ──→ the r1-entry mode-family SCOPE GATE (user-visible) ──→ r1's shape
```
**Critical path (RE-STATED):** the app's `.agents/` + census-CI → K3 (store/policy ∥; DOM after the mapping row) → K4 → w1 → (w2/w3 where staffed) → the r-phases. **The D30 wave no longer heads the path (DONE @ `64fb0ab`); the OT bridge never blocked it** — the carrier-reduction program rides ∥ (gates nothing); the structural half is crawl-tail as before. Solo: serial along the path, the ∥ streams ride whenever idle. Two-dev: one on S-app (K3/K4), one on S-ot/S-package (the carrier-reduction + the structural half + chrome) — the crawl's 5-11/3-6 estimate assumes this split.
**∥-validity laws:** w2 media ∥ w3 project holds (different seams, both app-owned after w1's shell stabilizes); r1 ∥ r2 ∥ r3 hold at run (different repos); the color instruments (S-engine) run ∥ crawl but r3's gate only requires them at consumption time; K2 ∥ everything (it is verification, not construction — it tightens as the pairs land).

## 5. The execution protocol (standing practice)

1. **Push at every micro-milestone** (GIT IS THE DISK); `git fetch` before every push; **never force push** (merge-first on sibling races — they are documented and recurring).
2. **Pin-lockset law:** module lands first under ITS gates; the app pin-bumps within one business day; HEAD-follow opens bump PRs, never pushes main.
3. **The battery runs after every spec round** (battery_r25, the successor — see §6/§8); recalibrate stale checks; exempt windows look BEFORE and AFTER each hit.
4. **Decisions go through adversarial review rounds** (fresh-context sub-agents) before implementation — the ARCH-R22/R23/R24/R25 trail is the pattern.
5. **A gap row with no acceptance is a spec bug; a facet with no coverage-matrix row is a spec bug** (the R22 laws, now in 00-master's standing-laws section).
6. **The census discipline, never swap-then-hope (D26 re-points the port-then-swap law):** the app's port tree stays a converging mirror governed by the census (register-equality, CI-gated recomputation; a carrier without a divergence rationale is a census violation; a census that drifts from the tree is a build failure); upstream waves re-pin via 3-way merge — no-conflict files adopt-wholesale, carriers merge against the documented zone maps with per-wave acceptance pins; the mini mock stays alive until the app matches it (retirement triggers post-K4/w1 — **keyed on REFERENCE-REGISTER.md's rows flipping LANDED, D35.4**). **Enforcement status (the R25 finding): the CI-gated script is ORDERED but NOT YET WRITTEN — until the D36.1 queue filing executes app-side, the census is battery-verified only; the S-app census-script row is the law's missing half.**
7. **The drag-law freeze (D22)** and the count-discipline law (declared == actual at the pins) are standing — see 00-master.
8. **The productization gate (D28.2):** r1-entry, OR a packaging need — defined as any consumer OUTSIDE the six-stream fleet pinning/consuming a module, or a distribution request landing — whichever PRECEDES r1-entry. At that gate (and only then) the seal-snapshot option opens: tag the sealed shas and cut clean public packages from them (e.g. an `nle-timeline` package from OT's tree, an `nle-shell` from nle-ui) via export-ignore/docs-reorganization — a future user-visible ARCH decision point; the pin ecosystem migrates through the normal pin-bump protocol at that time. Nothing before that gate.

## 6. This round's steps (the S-spec track's work order — the R25 record)

0. **battery_r25's core** (PENDING — the R25 pin world + the register checks + the mode-matrix presence checks + the plan checks; the fleet's rounds gate on it; the full check classes are ARCH-R25 §7).
1. The adversarial review round on ARCH-R25: **[done — v2; 2 fresh-context reviewers (design-consistency: 15 findings, 4 P1/6 P2/5 P3; corpus-faithfulness: ~50 claims re-derived live, 5 fold-ins + 6 precision fixes); the reports live at `audits/fleet-r25/review-arch25-design.md` + `review-arch25-faithfulness.md`]**.
2. The R25 fleet: **[done — 20 reports in `audits/fleet-r25/`: the 5 module scouts + the 10 mode audits + the 5 cross-cuts]**.
3. The amendment fleet (6 agents, the ARCH-R25 §6 order): **[06 (D30/D31/D32 + the P-list) · 05 (D33 + P10) · 16 (D34) · 18+09 (D33's token half + D35.4's chrome rows + D32.5) · 00+15 (the decisions + the r1-verb rows + P7) · REFERENCE-REGISTER.md + THIS doc (D35 + D36 — the R25-5 state)]**.
4. battery_r25 full extension + the verification fleet (fresh-context per amended file + a red-team) + **the cross-repo filings THIS round executes** (per the filing protocol — titled blocks in the target repos' queue docs): the WDC root-HANDOFF waveform promotion (D36.3) + the engine R9 re-file + the E1/E2/E3 defect filings + the app's census-CI mechanism block (D36.1). PENDING.
5. The integration review + the wrap (PLAN/HANDOFF/SKILL/worklog + push + /home/sync). PENDING.

*(R24's steps are history — battery_r24 landed, the R24 fleet folded through its integration review; the R24-4 record is in git. R23's steps are history: spec 14 → the redirect stub done @ `4878827`; the cross-ref sweep's corpus half rode the R23 per-file fleet.)*

## 7. Lineage (the traceability appendix)

| Era | Plan | Fate |
|---|---|---|
| R9 | P0-P6 phases | retired to git history |
| R15 | A0-A7b assembly | superseded by R22 (content absorbed into the W-rows) |
| R22 | spec 14 v2.1: C0-C4 / W-* / R-* | superseded by THIS doc (D24's mapping table re-homes every row: C1(b,d,e,f)→K3, C1(a)→w1, C2→K3+w1, C3→K3, C4→K4+w1-entry, C0→w1-prep/S-package, W-ops/W-audio/W-color→r1/r2/r3, W-media/W-project→w2/w3, W-n5→r4 (user-gated), R-*→r5/r6; the orphans re-homed per ARCH-R23 review-1 F3) |
| R23 | THIS doc + ARCH-R23 | superseded by the R24 state (the D23/D24 rulings carried forward; the D25 mechanism amended) |
| R24 | THIS doc (the R24 state) + ARCH-R24 | superseded by the R25 state — D26 amended D25.2's mechanism (the adopt-wholesale convergence + the census/carrier discipline); D27 the topology; D28 evolve-in-place + the productization gate; D29 the W11 instruments ladder-wide |
| R25 | THIS doc (the R25-5 state) + ARCH-R25 | **current** — D30 the mode-matrix law (06 §0's ten-mode census; the four absent families r1-scheduled spec-first); D31 the source-edit family restructure (06 §5.9 + §5.9B-F + the dedicated-vs-composite criterion); D32 the linked-companion law (the E1 defect row); D33 the visual-grammar register (05 §8A + 18 §9); D34 the keyboard source-edit block (16); D35 the REFERENCE-REGISTER (this repo's root — the mock-surface register + the battery freshness + the C-ledger intake); D36 the execution wiring (the plan's post-D30 re-write + the queue filings: census-CI D36.1, the engine R9 re-file + E1/E2/E3 D36.2, the WDC root-HANDOFF venue D36.3, the OT R9 absorption D36.4) |

## 8. The audit fleet (the user's standing directive — per-module, per-file, per-track)

**The R25 wave (this round):** (a) **the module scouts** — five agents, one per core repo (engine/OT/WDC/nle-ui/app), each live-verifying its repo at the R25 pins (HEAD, test counts, tsc, the seam surface, the queue, the spec-facing staleness) — the evidence packs every other wave cites (`audits/fleet-r25/scout-*.md`); (b) **the ten-mode fleet** — one agent per DaVinci edit mode (roll/ripple-trim/slip/slide/insert/overwrite/replace/append/ripple-overwrite/fit-to-fill), each sweeping the spec corpus + the five codebases + the two shell mocks (~76 posture-law gap rows total — the mode-matrix instrument); (c) **the five cross-cuts** (visual-grammar, keyboard, linked-companions, plan, shell-mocks — the last seeded REFERENCE-REGISTER.md); (d) **the 2-fold adversarial review** on ARCH-R25 (folded to v2 BEFORE the fleet); (e) **the amendment fleet** (6 agents, per §6 step 3). Agents edit ONLY their file, run NO git commands (the orchestrator commits per batch), and return a findings report. battery_r25 re-baselines the pin world once, centrally, after the fleet.

**The R24 wave (history):** (a) the module-card wave — five agents, one per core repo, each live-verifying its repo at the R24 pins (the reports in `audits/fleet-r24/module-*.report.md`); (b) the per-file fleet — 20 agents, one per spec file (00-master + 01-13, 15-20), each charged with BASE claims re-verified at the R24 pins, GAP rows re-checked, the D26/D29 amendments reflected, phase tags re-based, the round's landings promoted; (c) the plan/track audit wave (the R24-4 amendments + the cold-executor re-simulation); (d) the 17-fold adversarial review on ARCH-R24 (folded to v2 BEFORE the fleet).

**The R23 wave (precedent — the original directive's shape):** **The set:** 00-master + the 19 numbered specs (01-20 minus the retired 14). **One agent per file** — each audits its file against the LIVE upstream repos at the current pins, charged with: (a) every BASE claim re-verified (pins/counts/features — the fleet moved under every R22 pin); (b) every GAP row re-checked (landed? still-a-gap? new gaps); (c) every retired-spec-14 §4 row for the domain confirmed present in the §0 register (posture law — a row found nowhere is a violation to fix); (d) phase tags re-based per the D24 mapping; (e) the round's new upstream work reflected.

**Estimates (the honest register — re-based R25 per the xcut-plan audit's itemization):** crawl ≈ **5-11 wk solo / 3-6 two-dev — HOLDS** (the D30 item (−1-2) is now DONE, offset by the open items: census-CI script + register ≈0.5, `.agents/` + README ≤0.5, the engine-R9 reconciliation + filing residuals ≤0.5, the mode-matrix registration + wired-subset K3 pins +0.5-1; the mode gaps are **law-authoring (S-spec, cheap doc rows) + r1-ports (scheduled)**, and the engine already holds 6/10 modes algorithmically — no new engine research enters the crawl). Walk ≈ **10-15 wk solo / 7-10 two-dev** UNCHANGED (row sums 7-11 + human rounds 2-4; C0's 1-1.5 rides ∥ crawl — off walk's critical path). Run ≈ **20-28 wk solo / 13-17 two-dev WITH r4 (the user's D6 re-affirmation); 18-25.5 without — WITH the mode-family rider: the 10-mode DaVinci family does NOT currently sit in r1's wave list; IF the user passes the absent-family scope gate at r1-entry (the four absent modes + the insert/overwrite port families), r1 grows ≈ +1.5-3 wk solo / +1-2 two-dev — a user-visible scope gate, paired with the D28.2 productization gate's timing.**
