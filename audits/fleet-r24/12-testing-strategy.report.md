# R24 fleet audit — `12-testing-strategy.md` (the testing-strategy spec — the K-layer's spec-side home + one of D29's two K2-registration homes)

**Agent:** R24-3m (fresh context). **File owned:** `12-testing-strategy.md`. **Date:** 2026-09-08.
**Pin world audited against (all live-verified this audit):** nle-engine `5036387` (458) · opencut-timeline `ded43c4` (code pin `c15a629`) (536) · web-daw-core `85b81b0` (759) · nle-ui `fc4cc35` (674) · nle-test-app `c885ece` (174) · mini 355 · variants 1521+. Consumer pins re-read live: app→engine `5036387`, app→OT lock-copy `c15a629`, app→nle-ui `fc4cc35`, app→WDC `85b81b0`; engine→OT submodule `c15a629`, engine→WDC `494f6ff`. Evidence packs: the five `module-*.report.md` cards + `ARCH-R24-timeline-strategy-and-topology.md` (v2) + the R23 precedent.

---

## LIVE VERIFICATION (this audit's own runs — no git mutations, no test-suite executions)

- **HEADs:** `git rev-parse` at all five repos — 5036387 / ded43c4 / 85b81b0 / fc4cc35 / c885ece, all matching the R24 pin world; `git submodule status` at engine (OT `c15a629` + WDC `494f6ff` gitlinks) and app (engine `5036387` / nle-ui `fc4cc35` / WDC `85b81b0`; lock-copy `UPSTREAM.lock.json` upstreamHead `c15a629`).
- **Engine 458:** static `^\s*(it|test)\(` census over the 13 vitest files = 458 exactly (bridge-seams 113, nle-bridge 96, planner 52, timeline-math 42, transform-resolver 40, load-validation 32, video-sync 31, undo-serialize 15, persistence 13, render-abort 9, realtime-midi 8, timeline-edit-ops 4, api-surface 3). All 11 `tests/vitest/engine/` names + the 2 root files verified present — the R23 enumeration still exact at HEAD.
- **OT 536 = 386 + 150 / 59 entries:** `download/timeline-test-report.json` parsed live → total 536 / passed 536 / failed 0 / milestones 59; per-entry `tests.length` summed by class (M17 + R-suffixed + M49T/H/R/G = real-mouse): **386 in-page tests in 41 entries + 150 real-mouse tests in 18 entries** — the module card's split reproduced exactly. M49C present, 1 test, pass=true. `WIRE_COMMAND_TYPES` verified at `src/lib/timeline/headless/api.ts:182` (tsc-lockstep comment :215-230); the runner's 6-entry exception registry verified at `scripts/run-timeline-tests.mjs:283-300` (selectElements / upsertKeyframe / removeKeyframe / retimeKeyframe / advancePlayhead / trim).
- **WDC 759:** the one commit since `494f6ff` is docs-only (module card ran 759/759 live locally); `src/test/` listed = exactly 18 `.test.ts` suites; all 12 named integration-family suites present (audio-integration, dsp-bounce-parity, dsp-effects-integration, dsp-null-unity, dsp-pdc, dsp-sidechain, nle-audio-core-derisk, offline-parity-wiring, real-audio-e2e, meter-tap, varispeed, fft-analyzer).
- **nle-ui 674:** 37 test files live-counted (`rg --files`).
- **App 174:** static census over 6 suite files = GluedShell 70 + audioService 40 + sceneBridge 25 + persistenceService 17 + deliverService 15 + engineSeam 7 = **174** — the suite inventory grew 4→6 (persistenceService + deliverService landed with the R8 D29 real-wiring wave).
- **Mini/variants:** mini = 8 files / 355 tests (declaration-level, exact — the R23 seal unchanged); variants = 61 test files (decl-level count 1588 — grep over/under-counts; the R24 canon "1521+" kept with the battery-reconciles note).

## EDITS APPLIED (12 charges — minimal-diff, posture law intact: BASE/GAP/ACCEPTANCE triad preserved)

1. **Status header:** R23 → R24 framing — the D29 instrument registration named (M49C = the REGISTERED K2 instrument; the D30 W-F port = the REGISTERED K3 instrument; wire census 24+6); the R24 count set; the dual-tag window declared CLOSED. R23 kept as the supersession record (its own re-key history + superseded pins); R22/Round-8/SCOUT-12 records untouched.
2. **§0 heading + BASE date:** "R23-re-based"/2026-09-07 → "R24-re-based"/2026-09-08 (R23 pins superseded).
3. **The fleet row (charge 1):** engine 440→458 @ `5036387` (13 suites unchanged, +18 = W2.5 + R8-REV; app consumer `4ef0147`→`5036387`, fleet-coherent); OT 489→536 @ `ded43c4`/`c15a629` (359+130/51 → 386+150/59 entries; the W11 extensions named; "14 appended real-mouse suites" → 18 entries; the app's OT mirror `a4e971d`→lock-copy `c15a629`, byte-exact minus `testing/`); WDC @ `85b81b0` (docs-only, 759 held, live-run verified; engine consumer `f446512`→`494f6ff`, docs-only behind); nle-ui 648→674 @ `fc4cc35` (+26 = the D29 seams + R8-REV; consumer at HEAD); app 117→174 @ `c885ece` (4→6 suites enumerated: + persistenceService/deliverService); mini 355 unchanged; variants 1,542-live → 1521+ (60→61 files; 00-master reconciliation note kept).
4. **The D24 ladder tier map (charge 2 — THE round's delta):** K1 row — ALL GREEN at the R24 pins + the OT K1 net grew 489→536 (W11 milestone extensions: M44-M49 + M49T/H/R/G real-mouse + the M49C coverage gate), engine 440→458, nle-ui 648→674, app 117→174. K2 row — **M49C registered as K2's OT-side completeness instrument** (the machine-checked complete-UI claim: accumulated ⊇ `WIRE_COMMAND_TYPES` − exceptions, node-side accumulation across phases, tsc-lockstep both directions; already green 24/24) + the **wire census 24 routed + 6 documented exceptions** as the census the K2/K3 families key on; the family nets' counts keyed to 458/759/386+150. K3 row — **the D30 W-F coverage-gate port named as the REGISTERED K3 instrument** (module-scope accumulator + gesture sims + final assertion = the routed-verb completeness machine-check); **the shell-path blind spot registered verbatim from D30 R2** (the gate measures the PORT surface, excepted per D30 R2/R8) **+ the compensating pin family** (trusted-integrator verbs ⌘B, [ ]/⌥[ ], ,/., M/⇧M, ⌘M/⌘⇧M driven directly through engineService — owner-tagged app-side, D29.1/review F14); the DOM-structural gate re-pointed three-way per D26.5 (data-test 68-site / nle-ui shell-* / structural units — gated on the mapping row + W-C, not a swap+testid layer). K4 row — the e2e drives the same routed verbs; the shell-path exception applies identically (D29.1c).
5. **The K2 census registration state (charge 3):** the GAP row's counts re-based (359+130 → 386+150; the engine 4 within the 458 net, WDC's family within the 759 net) + an explicit **Registration state (R24): still PENDING — K2 entry hasn't happened**; this row (and spec 17 §13A.7) named as the registration home; the D29 instruments pre-register the gate; K2 entry lands the census rows at 458/759/536.
6. **The 5-suite inventories (charge 4):** all re-verified live (see above) — the engine 13-suite enumeration, WDC's 18 + the 12-name integration family, OT's 59-entry M-family + the runner mechanics, nle-ui's 37 files, the app's 4→6 suites. Each still exists at the R24 pins; counts exact.
7. **Phase tags (charge 5):** the transitional dual tags stripped — "was C4" (corpus row), "was C3's audio crawl", "was C4's e2e half", "was W-audio", "was W-color", "was W-n5", "was R-fcpxml" all removed (re-worded as plain D24-split provenance where the sentence needed it); the GAP intro declares the window closed (D24 K/w/r set only). No C/W/R live tags remain.
8. **ACCEPTANCE & TEST PLAN:** battery_r23 → **battery_r24** (keys 5036387/458, ded43c4/536/code-pin c15a629, 85b81b0/759, fc4cc35/674, c885ece/174 + consumer pins **+ the routed/exceptions coherence check per D29**); spec 17 §13A.7 pointer re-pointed to the R24 pins; the K3 tier-gate line names the W-F port.
9. **§13.7 engine annotation (line 1493):** the R23 historical record kept intact + an **R24 re-base appended** (458/458 @ `5036387`, bridge-seams now 113, the OT submodule re-pin a4e971d→c15a629 making the W11 wire surface importable at the engine's test seam).

## REMAINS-OPEN (verified still-open at the R24 pins — no repo landed them)

- **The K2 census + registration itself:** pending K2 entry (this round pre-registers the instruments + the count rows; the register's formal landing is the S-spec work item). The missing cross-module pairs (OT-ops→engine-decode round-trips at the app's seam shapes) unstarted — S-engine's worklist.
- **The K3 instrument (W-F) does not exist app-side yet:** grep-verified per the app module card — no `useWireDispatch`/`WIRE_COMMAND_TYPES`/`wireCoverage` anywhere in app `src/`; the port's 5 W-C/W-D adoptions + the wire mint are D30's in-flight queue. The spec rows register it as the instrument K3 WILL use (the correct tense per D29's "the plan's K3 row WILL name it").
- **K3 corpus (322/115) / offline audio pins / VLM net port / K4 e2e:** all still zero app-side (app card: no LAW-NET re-expression, no e2e). r2-r5 rows unstarted; r4 still D6-gated.
- **The waveform-contract hard prerequisite (K3 audio half):** unchanged in WDC's queue (the R24 D29.4 promotion is spec 20's + WDC's row to carry, not this file's).

## NOTES

- **Count discipline:** OT's 536 keyed to the report-json (parsed live: 536/536, 0 failed); the 386+150/59 split reproduced exactly from the json (module-card parity). Engine 458 = static census match (node_modules absent in sandbox — live-run venue is GHA CI per the card); WDC 759/nle-ui 674 = module-card live runs + this audit's file/suite verification; app 174 = static census (matches commit claim).
- **Variants figure:** "1521+" (the R24 canon) kept over the decl-level 1588 — grep over/under-counts chained forms; the 00-master reconciliation note preserved (60→61 files re-read live).
- **No normative damage:** the R22-posture-law structure (BASE/GAP/ACCEPTANCE) untouched; every historical record (R22/R23 framing, SCOUT-12 Q&A/§14/§15-§16, the §13.7 SCOUT table) kept — this round only re-based pins/counts, registered the D29 instruments, and closed the dual-tag window.
- **Out-of-scope left to owners:** spec 17 §13A.7's own count rows (the sibling 17 round), spec 15's census re-base + C7 lineage (the 15 round), the plan's K2/K3/K4 row re-writes (the plan/track wave), battery_r24's implementation.
