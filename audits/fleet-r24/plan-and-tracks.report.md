# PLAN/TRACK AUDIT — IMPLEMENTATION-PLAN at the R24 state + the cold-executor simulation (R24-4)

**Auditor:** R24-4 (the plan/track audit wave, per ARCH-R24's execution-consequence 3). **Mode:** research + single-file edit — `IMPLEMENTATION-PLAN.md` is the ONLY file touched; this report is the sole other write. NO git commands. **Inputs:** IMPLEMENTATION-PLAN.md (R23 state), ARCH-R24 v2 (the rulings), the five module cards (fleet-r24/module-*), the app's `docs/design-r9-d30.md`, plus live read-only verification of the six repos' orientation surfaces (OT `.agents/` + `scripts/run-timeline-tests.mjs` + `package.json`; the app's `README.md` + `scripts/bootstrap.sh` + the tree (no `.agents/`); the engine's `.agents/HANDOFF.md` + SKILL PAT recipe; WDC's root `HANDOFF.md` + `PLAN.md:140-159`; nle-ui's `.agents/PLAN.md` (grep: no C0/MiniShell row); this repo's `scripts/` (battery_r23.py exists, battery_r24 does not).

**Pin world audited against (the R24 re-baseline):** engine `5036387` (458) / OT HEAD `ded43c4` (536; code pin `c15a629`) / WDC `85b81b0` (759) / nle-ui `fc4cc35` (674) / app `c885ece` (174) / mini 355 sealed / variants 1521+. Consumer pins: engine→OT `c15a629` + WDC `494f6ff`; app→engine `5036387` + OT-lock-copy `c15a629` (byte-exact) + nle-ui `fc4cc35` + WDC `85b81b0`.

---

## 1. THE AMENDMENTS APPLIED (every section, each traced to ARCH-R24)

- **Header:** ruling basis re-based ARCH-R23 → ARCH-R24 (D26/D27/D28/D29 named; D23/D24 stand, D25's mechanism amended).
- **§0 preamble:** the environment prerequisite (one line) expanded to the **per-repo cold-executor set** (see §2 below) — verified live per repo.
- **§0 S-ot row — RE-WRITE (D26.3/D26.5/D29.3):** the D25 bridge work order → **the carrier-reduction work order** (ONE S-ot work order, filed at execution; the six carrier items verbatim; each landing flips an app carrier toward exact; gates nothing) + the data-test stability contract (the 68-site `data-test=` freeze + census gaps, NOT a new testid layer) + the view-config surface (verified still-absent at `c15a629`) + K2's runner registration (runner + M49C exist, green 24/24 — the registration is the filing) + the structural grammar half (crawl-tail) + the token half at w1-entry; D30 W-G's R10(h)-(j) cross-referenced as the app's separate asks (never duplicate). Gate cell fixed: `bun run dev` (:3001) THEN `bun run test` (536→, 59 entries).
- **§0 S-app row — RE-WRITE (D26 + D27.2):** step (0) create `.agents/` (OVERDUE, before W-C) → (1) complete the app's OWN D30 waves W-C..W-G (the design doc cited; W-F = K3's routed-verb machine-check) → (2) K3 store/policy ∥ + **the census register** (CI-gated script + the named register doc, D26.2) → (3) K3 DOM-structural AFTER the data-test mapping row + W-C (the three-way vocabulary; the mini 59+15 mapping) — **the vendor-extension step GONE**, the fork-retirement gate = the D30-declared irreducible carrier set → (4) K4 → (5) the audio pins + the VLM net port. Counts 117→174.
- **§0 S-engine row:** the consumer re-pin wave marked DONE (app @ `5036387`, zero lag — the "one commit behind" text removed); the D29.5 dispositions added (CSS-filter widening OPEN — only Gaussian-Blur landed @ `8a0b7fe`/`4b2dfd1`; transform sidecars future-extension; `maintainPitch` closed; the scene-grade DECLINE recorded); the parity corpus (now incl. W2.5) + the color instruments (zero progress, verified) + K2's engine-side pairs. Counts 440→458.
- **§0 S-wdc row — D29.4 REQUIRED:** the FIRST action is now the **waveform-contract promotion** (from PLAN's parking lot PLAN.md:150-152 → the HANDOFF next-session scope; the contract stated: the de-facto `getAudioBuffer/setAudioBuffer/getPeaks/setAudioPeaks/computePeaks(buffer,buckets) → {min,max,buckets}` shape at `audio-registry.ts:24-74` + the offline-render/call-spy pin shapes; pinned) — K3's audio half hard prerequisite. Then the mixer surface (design-first, F6) + the **FIVE** test ports (the off-by-one named) + the async pre-retime + the JKL R4 residue.
- **§0 S-package row:** C0 MiniShell stays first + the **filing-gap actions** (file C0 in nle-ui's own PLAN — verified absent; file the D30 W-D package edits package-side — R6/Z5/Z7/cheat-sheet/`data-transport`, currently queued app-side only; close the DECISIONS gap — the ledger ends at D28, verified; tick the landed-but-unticked OT S-round rows). Counts 648→674.
- **§0 S-spec row:** battery_r24 core (the successor's re-points per D26 consequences) + the K2 census registration still pending at K2 entry (re-based counts: OT 386+150 incl. M49C) + the R24 fleet marked DONE.
- **§0 entry point:** re-pointed to the S-app row's steps (0)+(1) (the critical path's head).
- **§1:** the reference set re-based to the R24 pins + the consumer pins; 00-master stays the canon; the never-re-declares law kept.
- **§2:** K1 (the R24 counts + the W11 milestone extensions M44-M49/M49T/H/R/G/M49C — 59 entries); K2 (OT's runner + M49C as the OT-side completeness instrument, green 24/24; the missing pairs); K3 (the routed-verb machine-check = the W-F coverage-gate port + the shell-path blind spot's compensating pin family + the three-way data-test mapping + the LAW-NET corpus + the audio pins + the VLM net); K4 (drives the same routed verbs, the exception applies identically); the **ordering law RE-STATED** (K3-DOM gates on the mapping row + W-C, not a swap+testid layer); w2 gains the cloudcut hybrid-storage RFC reference row (D27.3, no dependency).
- **§3:** all six workstreams re-keyed (S-ot/S-app per the §0 re-writes; S-wdc's promotion first; S-package's filing gaps; the OT spec-queue candidates endorsed r1-adjacent per D29/F8).
- **§4:** the graph re-drawn (the "props + testid layer" node → the carrier-reduction ∥ stream; the "vendor swap + fork retirement" node → "the data-test mapping row + W-C → K3 DOM-structural"; the S-wdc waveform hard edge kept; the productization gate noted at r1-entry); the **critical path RE-STATED** (the app's D30 completion → K3(DOM) → K4 → w1; the OT bridge no longer blocks); the two-dev split re-described; the ∥-now paragraph re-pointed.
- **§5:** item 3 → battery_r24; item 4 → the R22/R23/R24 trail; item 6 **re-pointed** (port-then-swap → the census discipline, D26); **item 8 ADDED** (the productization gate, D28.2: r1-entry OR a packaging need — any consumer outside the six-stream fleet, or a distribution request — whichever precedes r1-entry; the seal-snapshot option at the gate only).
- **§6:** re-framed as the R24 record — steps 1-3 done (the review v2, the per-file fleet + module cards, THIS plan audit), battery_r24 core + full extension + the integration review/wrap PENDING.
- **§7:** the R24 lineage row added (R23 → superseded; R24 current).
- **§8:** the R24 fleet recorded (the module-card wave 5 + the per-file 20 + the plan audit + the 17-fold review); the R23 text kept as precedent.
- **Estimates:** crawl **re-itemed** — the app's D30 waves 1-2 (in flight) + K2 1-2 + K3 2-4 + K4 1-2 + **the structural half 1-2 KEPT EXPLICIT (review F4)**; the D25-bridge 2-4 line-item **collapses into the carrier-reduction program** (∥, off the critical path) → **crawl ≈ 5-11 wk solo / 3-6 two-dev**; walk/run unchanged (no item shifts — stated honestly).

## 2. THE COLD-EXECUTOR SIMULATION (the R23 review's instrument, run TODAY at the pins)

Method: for each track row, a fresh executor was simulated — (a) find the entry dir, (b) orient per the orientation cell, (c) execute the first action with what exists at the pins, (d) know the gate — with every orientation surface read live.

| Track | (a) entry | (b) orient | (c) first action @ pins | (d) gate | Verdict |
|---|---|---|---|---|---|
| **S-ot** | ✓ | ✓ `.agents/` exists (HANDOFF/PLAN/DECISIONS/SEAMS/SKILL/OP-COVERAGE) | **BLOCKED in the OLD row**: it ordered the D25 bridge (props-as-prerequisite + a NEW `data-testid` layer) — both superseded by live evidence (TimelineViewProps @ `c15a629` carries none of the props; OT emits ZERO `data-testid`, 68 `data-test=` sites; the app rides OT's own `wire`/`onSaveScene`/`onLoadScene` injection points). A cold executor would have filed a wrong work order. | **BLOCKED in the OLD gate cell**: `bun run test` FAILS without the dev server (the runner targets `http://localhost:3001/` and does NOT self-start it — verified in `scripts/run-timeline-tests.mjs:18,46`) and needs Playwright's chromium headless shell. | **blocked-and-fixed** (row re-written; gate cell now `bun run dev` THEN `bun run test`, 536→) |
| **S-app** | ✓ | ✓ README + worklog — but the README's gate-count line is STALE ("30 glue-law tests" vs 174; the row now carries the live counts); `.agents/` ABSENT (the row's step (0) creates it — executable) | **BLOCKED in the OLD row**: its step (2) prescribed the never-built vendor-extension mechanism (two-path `vendor/nle-timeline/components/` + the `@/lib/timeline` alias) — a cold executor would have burned a wave building a retired mechanism; the actual critical path (the D30 waves W-C..W-G, in flight per the design doc) was invisible to the row. Setup verified: `NLE_GH_PAT` + `npm run bootstrap` (README §Setup; `bootstrap.sh`: PAT → `git submodule update --init` (non-recursive) → `npm install` → `npm run vendor:build`). | ✓ (174→ vitest, tsc, boundary, `vite build` — all runnable post-bootstrap) | **blocked-and-fixed** (steps (0)-(5) re-written) |
| **S-engine** | ✓ | ✓ `.agents/` exists — with the meta-docs LAGGING the last 3 commits (W2.5/R8-REV/R9-W-A unrecorded; flagged in the row) | **BLOCKED in the OLD row**: its queue head ("the consumer re-pin wave — the app's pin `4ef0147` one commit behind `b8c6f88`") is FALSE at the pins (the app pins `5036387`, zero lag, R9 W-A) — a cold executor would hunt a done task. Setup now stated: `npm install` + `git submodule update --init` with PAT remotes (the SKILL recipe; else the live-run venue is GHA CI). | ✓ (458→ vitest, tsc, the sync-check) | **blocked-and-fixed** (queue head removed; the D29.5 dispositions are the real head) |
| **S-wdc** | ✓ | ✓ root HANDOFF + PLAN (verified read: the next-session scope = mixer / "4" ports / pre-retime / P3s — **NO waveform row**) | **BLOCKED (the sharpest)**: the OLD row carried the waveform contract only as item (3) "coordination" while WDC's own queue has ZERO presence for it (PLAN.md:150-152 = an unchecked long-horizon bullet) — K3's audio half would block invisibly at the ladder level. | ✓ (`bun install --frozen-lockfile` verified — 143 pkgs; the drift gate needs `--source <web-daw-clone>` — no upstream clone locally, none pending; now stated) | **blocked-and-fixed** (the promotion is item (1) REQUIRED, contract stated) |
| **S-package** | ✓ | ✓ `.agents/` exists | **EXECUTABLE**: C0 MiniShell's inputs all exist (RH-skin-extraction.md + the mini's components + spec 18 §0); the repo runs live (`npm ci` → 674/674 vitest + tsc + boundary, verified by the module card). The filing gaps (C0 unfiled package-side — verified by grep; the W-D edits queued app-side only; DECISIONS ends at D28; the OT rows landed-but-unticked) were PROCESS debt invisible to the old row. | ✓ | **executable** (the row adds the filing-gap actions) |
| **S-spec** | ✓ (this repo) | ✓ (this doc + ARCH-R24) | **EXECUTABLE**: battery_r24's core is writable from the `scripts/battery_r23.py` template (battery_r24 does not exist yet — that IS the work). | ✓ | **executable** |

**Environment findings folded into the plan (§0 preamble):** the OLD preamble stated only two facts (the app's PAT+bootstrap; OT's `bun install`). The simulation found and the plan now states five: **OT** needs the dev server started first (`bun run dev` → :3001; the runner does NOT self-start; `bunx playwright install chromium` — plain `npx playwright install` rewrites package.json pins + creates package-lock.json, revert both, per the module card's hygiene note); **WDC** needs `bun install --frozen-lockfile` + an upstream clone for the drift gate; **nle-engine** needs `npm install` + PAT-remoted submodule init; **nle-ui** runs `npm ci` (the Storybook build rides CI).

**Final cold-executor posture:** all six rows are now executable TODAY at the pins by a fresh executor — entry dirs exist, orientation surfaces exist (with the two stale-doc flags noted), every first action is either directly executable (S-package, S-spec) or is a filing/creation action whose vehicle exists (OT's `.agents/PLAN.md`, the app's `.agents/` creation, WDC's root HANDOFF, the engine's PLAN queue), and every gate cell names commands that run at the pins once the stated setup is done.

## 3. RESIDUALS (flagged, out of this audit's single-file scope)

1. **Repo-side doc debts** (the plan's rows now ORDER them closed; the owning tracks execute): the app's README gate-count staleness; the engine's `.agents/` meta-doc lag (3 commits — D28.3's hygiene-queue class); WDC's "4 ports / 5 bodies" off-by-one in its OWN PLAN/HANDOFF (spec 20 §0:21 carries it too — the per-file fleet's report owns the spec-side fix); nle-ui's DECISIONS gap (ends at D28) + the unticked landed OT rows + the missing C0 row; OT's `dev` script hardcodes `-p 3001` (the :3000 user-facing topology needs the `daemonize.py` recipe — HANDOFF:54-58; non-blocking for gates, `TIMELINE_TEST_URL` is overridable).
2. **battery_r24 does not exist yet** — the S-spec row's step 1 + §6 step 0; the whole round's gates ride on it. battery_r23.py still carries the R23 pins until then.
3. **00-master's fleet table** (the canon §1 cites) needs the R24 re-baseline — the 00-master per-file report + battery_r24 carry it; the plan cites per the law.
4. **W-C has not landed** (the wire-dispatch port) — the re-stated ordering law's K3-DOM gate depends on it; in flight per D30 (W-A/W-B at the pin).
5. **The cross-repo filings are execution, not plan text**: the S-ot work order, the package-side W-D/C0/DECISIONS filings, the WDC HANDOFF promotion, the engine's D29.5 rows, and the app's `.agents/` creation — the rows now order all five; none existed at the pins (each verified absent).
6. **Estimate honesty note**: the crawl's two-dev 3-6 assumes the second dev absorbs the carrier-reduction + structural + C0 while the first runs D30→K3→K4 serially; if the second dev is unavailable, solo drifts toward the 5-11 top end with the ∥ streams' idle-window spill.

**Bottom line:** the plan is now the R24 executable state — the D25 bridge framing is gone (the carrier-reduction program rides ∥, gates nothing), the app's D30 waves are the critical path's head, the W11 instruments are the ladder's gates (M49C as K2's OT-side instrument, the W-F port as K3's machine-check, the same verbs as K4's vocabulary), the census discipline replaces the swap, the waveform contract is WDC's first action, the productization gate is protocol law, and the crawl re-items to 5-11/3-6 with the structural half explicit. The cold-executor simulation found four blocked rows and two executable ones; all four blocks were fixed in the row re-writes (which is what the re-writes were FOR) plus the five per-repo setup facts now stated in §0.
