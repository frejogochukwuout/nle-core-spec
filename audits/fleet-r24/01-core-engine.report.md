# R24 fleet audit — `01-core-engine.md` (the nle-engine runtime-core spec)

**Agent:** R24-3b (fresh context, task ID R24-3b). **File owned:** `01-core-engine.md`. **Date:** 2026-09-07 (late UTC).
**Repos audited against (read at synced HEADs, NO git mutations, no fetch/clone):** nle-engine `5036387` (clean tree, `main`), nle-test-app `c885ece` (clean, `main`; gitlink pins read live). Both engines' submodules are **unmaterialized in this sandbox** (`git submodule status` prefixes `-`) — pin verification is at the gitlink level (`git ls-tree`), and the W11 surface claims are cited from commit `5036387`'s message + the OT module card (which verified them live in the OT repo). Engine test count is **static-census-verified, not live-run** (node_modules absent) — authority = the exact static match + the commit chain `8a0b7fe` (457/457) → `4b2dfd1` (458) → `5036387` ("458/458").

**Evidence packs consumed first:** fleet-r24 module cards (nle-engine, nle-test-app, opencut-timeline), ARCH-R24 v2 (esp. D29.5), fleet-r23/01-core-engine.report.md (the precedent).

---

## VERIFIED-STRONG (every claim re-checked at the live pins, not trusted from the cards)

- **HEAD + pins:** engine HEAD `5036387c7a9cdb969f5234754c3a012bfed58187`, `git status --short` empty. Submodule gitlinks: OT `c15a6294cee9ac417c3d2aa3661c7120a2e69ba7`, WDC `494f6ff7fadd2f9f71461f3baf0f0f70972e4e71`. The `5036387` commit stat is genuinely SUBMODULE-ONLY (`vendor/opencut-timeline | 2 +-`), message verbatim confirms the W11 widenings list + "458/458 green against the new pin".
- **The 458 count, recomputed:** `it|test(` census across the 13 vitest files = 354 (11 under `tests/vitest/engine/`) + 96 (`tests/vitest/nle-bridge.test.ts`) + 8 (`tests/vitest/nle-bridge-realtime-midi.test.ts`) = **458 exactly**; per-file names/counters match the module card (bridge-seams 113, nle-bridge 96, planner 52, timeline-math 42, transform-resolver 40, load-validation 32, video-sync 31, undo-serialize 15, persistence 13, realtime-midi 8, render-abort 9, timeline-edit-ops 4, api-surface 3).
- **API freeze (D9) UNMOVED:** `api-surface.frozen.ts` = **455** names (`grep -c '^\s*"'`); `buildElementFilterString` absent from it — it lives in `bridge/composition-frame.ts:223`, and the barrel `src/lib/nle/index.ts` has **zero** `bridge/` re-exports (only `audio/realtime-bridge`'s `RealtimeAudioBridge`). The two D9-legal names (`clampAuthorFadeToSpan` :155, `foldVolumeKeyframesToAutomation` :224) are the only post-open additions.
- **Layer fence:** `LAYER-SNAPSHOT.txt` = **52** edge lines (`^(\(root\)|[a-z]+) -> `); `scripts/lint-layering.mjs` wired in `.github/workflows/ci.yml` (:124-130). 9 CI-wired probes (probe-crb/crc/fx2/fx4/fxa/m30-fixture/p114-undo/p2s/rb-p1) — unchanged.
- **The W2.5 landing, code-read:** `CompositionElementParams.effects` (:119) + `CompositionElementEffect` (:124) + `CompositionDrawOp.filter` (:161) + `buildElementFilterString` (:223-238): Gaussian-Blur-only mapping, default radius 4, non-finite→4, honest-skip for everything else, space-join, identity `''`; the R8-REV #5 clamp `Math.max(0, r)` at :234 with the in-code comment; painter applies inside save/restore (header law :85-89); the scene-GRADE consumer-side-final-pass design stated in the commit message. **44-effect registry re-counted:** 43 `id: '` in `effects/pipeline.ts` + `gpu-lut` @ `effects/lut.ts:321` = 44.
- **maintainPitch (D29.5(b)):** `OpencutFlattenOptions.maintainPitch` at scene-to-segments.ts:246 (doc block :239-245) + the threading `el.retime?.maintainPitch === true → source.maintainPitch` at :611 (W2 comment) — engine-side LANDED; the residual (the app's mock-local Inspector toggle) is app-owned per the app card's zero-no-op audit.
- **Transform-sidecar slots (D29.5(a)):** composition-frame.ts:70-71 ("future transform sidecars extend the dest") and :93-94 ("op.dest is v1-inert data kept for the future transform extension") — verbatim present; NOT filed as an engine PLAN row.
- **Zero engine wire mediation:** grep of `src/` for `WIRE_COMMAND_TYPES|HeadlessTimelineApi|applyBatch|lockPreCheck` = 0 hits; zero `wireLog|wireCoverage` in tests; the vitest alias (`opencut-timeline/` → `vendor/opencut-timeline/src/lib/timeline/`, insertion-ordered, vitest.config.ts:40-44) is the test-seam import path for the W11 surface.
- **Color instruments (spec 08's S-engine row) — STILL ZERO:** `gpu-power-window` @ headless/api.ts:1509 + `gpu-secondary-qualifier` @ :1515 are the only hits (the byte-identical FreeCut wire strings, not implementations) + the pipeline.ts:12 planned-params comment; P2 backlog still lists scopes. The GAP row stands.
- **§13A current-state claims:** 62 `.ts` / **54,791 LOC** in `src/lib/nle` (was 54,739); "zero Web Workers" re-verified (0 `new Worker` in src/, AudioWorklets only); the "61 `execute(` call sites" figure reproduces as `this.execute(` = **61** at BOTH `b8c6f88` and `5036387` — stable.
- **Consumer-pin coherence (charge 6):** engine→OT `c15a629` + WDC `494f6ff` (gitlinks, live). App `c885ece` gitlinks read live: **engine `5036387`** (AT HEAD — the R23 "trails by one commit" consumer-lag row is dead), nle-ui `fc4cc35`, WDC `85b81b0` (opportunistic, ahead of the engine's docs-only `494f6ff`), OT lock-copy `c15a629` (UPSTREAM.lock.json upstreamHead; byte-exact minus `testing/` per the app card's live cmp). App 174 static census matches its card.

## FIXED (edits to `01-core-engine.md` — minimal-diff, §0 + §13A only; 2265→2273 lines)

1. **Status line:** R23→R24 framing; engine `5036387` 458/458 with the three new landings named (`8a0b7fe` +17, `4b2dfd1` +1, the submodule-only re-pin); forward list gains the CSS-filter widening; R23 lineage recorded as "same forward set — none landed since; the landings arrived off-forward"; the R22 lineage kept compact; all old C/W/R vocabulary dropped from the status (the dual-vocab window closes).
2. **§0 heading:** re-baselined at R24; "the dual-vocabulary window is closed — D24 tags only" added.
3. **Engine BASE row:** re-pinned `5036387` 458/458 with the 13-file census inline; the freeze row now asserts **unmoved at `5036387`** with the buildElementFilterString-is-bridge-level reasoning (D9 intact); the layer fence pinned at **52 edges, CI-wired**; W2 row extended with the **D29.5(b) maintainPitch LANDED** disposition (scene-to-segments.ts:246/:611/:638, residual app-owned); the **R24 landings block** appended: (1) the W2.5 D29 effects sidecar with the full honest-subset law + the 44-effect registry citation, (2) the R8-REV #5 clamp, (3) the fleet-coherence re-pin with the W11 test-seam surface list + zero-mediation proof; Vendors re-pinned OT `c15a629` / WDC `494f6ff` (with the `5e5bbe4` re-pin provenance). All R23-verified segments (N2b, RR1-A, S3, toSorted, K2 seeds) kept verbatim.
4. **App BASE row:** `c885ece` 174/174 (suite census inline); the R8 REAL-wiring round (W2.1-W2.5) + R8-b1 + R8-REV + R9 W-A/W-B compacted in; consumer pins re-based (engine `5036387` AT HEAD — re-pin wave CLOSED; nle-ui `fc4cc35`; OT lock-copy `c15a629`; WDC `85b81b0` opportunistic); the stale "trailing by one commit / live S-engine queue item" clause removed.
5. **GAP register:** all six rows re-verified still-open at `5036387`; every `(was W-ops|W-n5|W-color|R-engine-p2|∥ crawl|NEW at R23)` dual-vocab parenthetical stripped (D24-only now); the parity-corpus and color-instruments rows gained their re-verification evidence; N5 row cites the P1.9 deprioritization; **NEW GAP row: the CSS-filter preview widening** (Vignette/Glow/LUT-preview v1; D29.5(d); owner S-engine, ∥ crawl; acceptance = each mapping a pinned CSS-filter law or an explicit honest-skip, the W2.5 pin shape).
6. **NEW §0 block — D29.5 DISPOSITIONS** (the engine-repo record vs the app's D30 R10 asks): (a) transform sidecars = FUTURE-EXTENSION, not a current gap (:70-71/:93-94 slots, owner S-engine when queued); (b) maintainPitch = LANDED (cross-ref BASE); (c) scene-grade promotion = DECLINED BY DESIGN (the `8a0b7fe` law — consumer-side final pass; spec 04/08 cross-ref); (d) CSS-filter widening = OPEN GAP (cross-ref the row).
7. **ACCEPTANCE line:** battery_r23 → **battery_r24**.
8. **§13A header:** 54,791 src/lib LOC, 458 vitest, 9 probes @ `5036387`. The §13A table rows left as point-in-time records (per the R23 precedent) — the one live pin reference inside the undo row's supersession note (`61 execute( sites at b8c6f88`) reproduces exactly at both pins and stays.

## REMAINS-OPEN (gaps verified still-open at `5036387` — nothing landed them)

- The projector parity corpus (N1 family's formal S4 suite) — no S4 suite in the repo; not in the engine HANDOFF queue (G1/G2/N5/P3-P2 are the engine's own next steps).
- K2's engine-side combined pairs — the seeds exist (timeline-edit-ops/video-sync), the formal round-trip pairs don't.
- The union façade (r1) — headless api.ts is still the JSON-RPC op dispatch; zero `WIRE_COMMAND_TYPES`-class attach (grep-verified).
- N5 real decode (r4, P1.9-deprioritized, user-gated D6).
- The color instruments (S-engine ∥ crawl, consumed r3) — zero engine code; the row stands with refreshed evidence.
- The r6 tail (ShapeItem/Lottie/Subtitle/Controller + CPU transition renderers).
- The CSS-filter widening (NEW GAP row; D29.5(d)).

## CROSS-FILE RESIDUALS (for other fleet agents / the plan wave — NOT fixed here)

1. **IMPLEMENTATION-PLAN.md S-engine row is now stale in three ways:** (a) its live-queue item 1 is "the consumer re-pin wave (app pin `4ef0147` one commit behind `b8c6f88`)" — CLOSED (app @ `5036387`, AT HEAD); (b) the pin vocabulary (`b8c6f88`, 440→) needs the R24 re-base (the ARCH-R24 D29/F6 K1 re-base covers the §1 reference set — the track rows are the plan-wave's charge); (c) the row should register the CSS-filter widening GAP + the D29.5 dispositions per ARCH-R24's execution consequence 3.
2. **Spec 03/04 §0 rows** still carry `b8c6f88`/440/`a4e971d`/`f446512` pins and miss the W2.5/R8-REV landings (module card §6.1-6.3) — those files' owning agents.
3. **Spec 04/08 must land the scene-grade DECLINE as a decision row** (D29.5(c) says "the spec records the DECLINE as the law" — 01 now carries it; 04/08 must mirror, cross-referenced).
4. **Spec 15 gains the W11 consumer rows** (ARCH-R24 D29.2: "the W11 engine widenings land as BASE rows in spec 15 + spec 01's consumer rows" — the engine-side consumer rows are landed here; 15's own census re-base is its agent's charge).
5. **The engine's own meta-doc lag** (module card §2 note): `.agents/HANDOFF.md`/`PLAN.md` last rewritten at `e0858fa` — no W2.5/R8-REV/R9-W-A records, and HANDOFF still narrates `a4e971d`. Repo hygiene queue item (D28.3: filed as queue, not topology).
6. **battery_r24** should now pin the engine at `5036387`/458 with the frozen-surface 455-unmoved check + the 13-file census class (this file's §0 is now the battery's target).

## NOTES

- **Count discipline:** every number written is recomputed or commit-cited: 458 (static census, exact), 455 (frozen names, exact), 52 (edge lines, exact), 44 (43+gpu-lut, exact), 61 (`this.execute(`, exact at both pins), 9 (probes, exact), 54,791 (LOC, exact), 174/6-suite app census (matches its card), 13 (files, exact).
- **The madge tension** (Testing's `manager-initialization-is-acyclic` property vs §14.16's drop-the-madge correction) — carried forward from R23, still an editorial-pass item, untouched (out of audit scope, no normative damage taken).
- **No D25/D26-class edits needed:** this file makes no view-tree/fork claims (re-grepped: timeline-port/components/timeline — zero hits). The adopt-wholesale strategy touches 01 only through the app BASE row's lock-copy consumer pin, which is now stated.
- **The W11 wire-surface claims' authority chain** is stated inside the BASE row itself (test-seam importable, zero engine mediation, grep-verified) — no over-claim: the engine does NOT attach the wire surface anywhere in its own src (the headless api.ts remains the JSON-RPC 19-op dispatch; the union-façade gap is untouched by the re-pin).
