# 00-master-spec.md — the R24 per-file fleet audit (file 00 of 20)

**Agent:** R24-3a (fresh context, single-file owner; Task ID R24-3a). **File:** `00-master-spec.md` — the master decisions register + the fleet pin canon.
**Date:** 2026-09-08. **Inputs:** the five R24 module cards (all counts verified — OT 536/536 live-run, WDC 759/759 live-run, nle-ui 674/674 live-run, engine 458/458 static-census-matched, app 174/174 static-census-matched, the port census + lock-copy byte-exactness live-verified vs `git archive c15a629`), ARCH-R24 v2 (D26-D29, 17 review findings folded), the R23 precedent report, 00 read in full.
**Method:** no git commands; no battery run; every re-based value traced to a module card or ARCH-R24 before writing.

---

## EDITS MADE (all in `00-master-spec.md`; 16 sites, minimal-diff re-base)

1. **Status header → v9.0 (charge 1):** one-line summaries of D26 (adopt-wholesale convergence amending D25.2 — census-governed converging mirror, 39 mirrors + host, 32 zero-action + 7 carriers; census = the law), D27 (keep nle-ui + keep the app; cloudcut-nle the dormant predecessor, D27.3), D28 (evolve in place; seal-snapshot REGISTERED, trigger = r1-entry or a packaging need), D29 (the W11 instruments entering the ladder — M49C coverage gate K2/K3/K4-wide, the 24 routed + 6 exceptions census, data-test rows, the waveform promotion) + the full R24 re-pin world incl. the consumer pins and the OT code pin `c15a629`. Prior ledger compresses R23 correctly (D25's mechanism clause superseded; the rest stand).
2. **Date ledger:** v9 Round 24 2026-09-08 added.
3. **§0 fleet rows, all five re-pinned (charge 2):** engine 458 @ `5036387` (W2.5 effects sidecar + R8-REV clamp added; consumes OT `c15a629` + WDC `494f6ff` docs-only-behind); OT 536 @ `ded43c4` with the **c15a629 code-pin note** + the W11 complete-UI round description (24 routed + 6 exceptions, M49C, the W11 engine widenings, op-parity 11/11; tree now 40 files / 8,604 LOC, M17-M49); WDC 759 @ `85b81b0` (docs-only over 494f6ff; both consumer pins stated); nle-ui 674 @ `fc4cc35` with the **D29 seam waves** (saveState, exportRequest, per-scene grade sidecar); app 174 @ `c885ece` with the **R8-D29 real-wiring** waves + **R9-D30 mid-flight** (W-C..W-G) + the **adopt-wholesale convergence**; all four app vendor pins re-read live.
4. **NEW §0 row — cloudcut-nle (charge 2):** the DORMANT June-July 2026 predecessor (frejogochukwuout/cloudcut-nle; the freeCut-reskin strategy that never executed; successor notes = D26's evidence; hybrid-storage RFC = a w2/r4 media-design reference — NOT a dependency; D27.3). Cross-referenced with the existing ux-spec-branch row.
5. **§0 posture paragraph:** one sentence added — R24 locks topology (D27) + mechanism (D26) + the seal-snapshot registration (D28).
6. **D10 bracket (living pointer):** canon re-based to engine `5036387` 458 / OT `ded43c4` (code pin `c15a629`) 536 / WDC `85b81b0` 759.
7. **D11 clause-2 bracket (living pointer):** census re-based per D29.2 — 30 names at `c15a629` = **24 routed + 6 documented exceptions**, with the F12 lineage chain (24 R15 → 28 R22 → 30 R23 → the split); 27/78 union counterparts kept (no new evidence).
8. **D12 reasoning:** OT tree current figure → 40 files / 8,604 LOC @ the R24 code pin.
9. **D13 reasoning:** WDC count chain extended — …R22/R23 (759 @ `494f6ff`) **and at R24 (759 @ `85b81b0`)**.
10. **D15 body (charge 4):** the **R24 extension note appended** — seal-snapshot option, EXTENDED-not-superseded, trigger predicate verbatim (r1-entry or a packaging need = any consumer OUTSIDE the six-stream fleet or a distribution request, whichever precedes r1-entry), per-repo artifacts load-bearing until the gate.
11. **D19 body:** an **(R24 note: …)** added after the R23 supersession marker — the dual-vocab window CLOSES at this fleet's end; the record's C/W/R tags are history (not rewritten, per the point-in-time convention).
12. **D21 body:** an **(R24 note: …)** appended after the R23 note — the R23 canon values superseded; the R24 pin world + consumer pins restated; pin-CLASS law unchanged.
13. **D25 body (charge 3):** the **R24 amendment note appended** — D25.2's two-path vendoring + fork deletion superseded by D26's census/carrier discipline (verified: OT upstreamed ZERO of the props at `c15a629`); the census numbers; the carrier-reduction program; "the fork retires" → the D30 irreducible carrier set; **D25.1 canon, D25.3's two halves (25.3b → `data-test`, 68 sites), D25.4 gold sample, D25.5 law re-pointing, D25.6 protocol STAND**; the tree now 40 files / 8,604 LOC.
14. **§2A.5 (charge 5):** freeze item re-scoped to D25's surviving halves (the D25.2 mechanism retired; the amendment executed as a reviewed ARCH amendment per this law).
15. **§2A.7 NEW (charge 5):** the **census law** as a standing law — register-equality, CI-enforced app-side (register-inequality fails the build), battery-re-verified at round cadence (per-pin declared values, never constants); a carrier without a divergence rationale is a census violation.
16. **§3 reading-diagram note (charge 6):** the Timeline UI box now reads as OT's canonical tree (40 files / 8,604 LOC @ `c15a629`) with the app's `timeline-port/` as a **census-governed converging mirror — 39 mirrors + the EngineMount host** (per D26) — NOT "the fork retires".

**Companion edit (the one sanctioned):** `ui-mock/shell-mini/docs/CORE-SEAMS.md` — a 7-line R24 mapping note appended to its header area (the 30-name census → 24 routed + 6 exceptions; the D25-bridge citations → D26's census/carrier discipline; the `data-test` instrument). Nothing else in that file touched.

## VERIFIED-STRONG (re-checked, untouched)

- 25 `### Decision` headers preserved (no headers added/removed — D26-D29's full bodies live in ARCH-R24; 00 carries their canon via the v9.0 header + the amendment/extension notes, per the charge list and the battery's decision-count invariant).
- §2A laws 1-4/6, §2.5 governance, §7 WYSIWYG, §13 tiers, §12 glossary ("78 types as of Round 7" is a correctly-marked point-in-time claim), §5 browser matrix, §6/6A, §8 stream map (18 rows + the spec-14 retirement note), §10 — no staleness found beyond the sweep above.
- D22 drag-law freeze, D23/D24 bodies — stand unchanged (D24's vocabulary is the living set everywhere outside historical records).

## STALE-COUNT/PIN SWEEP RESULT (charge 7)

After the edits: `b8c6c88` — zero occurrences (the pin-typo class stays clean); `b8c6f88`/`222532c`/`85dcf57`/`70e99f0`/440/489/648/117 appear ONLY inside D21's explicitly-marked R22/R23 historical notes (now carrying the R24 supersession note); `a4e971d` appears once as an annotated lineage note ("re-pinned from"); `f446512`/`4ef0147` — gone; `494f6ff` appears only in the LIVE consumer-pin contexts (it IS the engine's current WDC pin, docs-only behind). "440 Hz" (§13.4 tone) and "Chromium 113-117" (§5) are NOT counts — deliberately untouched; battery pin-typo checks should not flag them.

## STALE ITEMS IN *OTHER* FILES' DOMAINS (for the orchestrator)

1. **CORE-SEAMS.md's own target-pin headers** (A: OT @ `05584d8`; B: engine @ `f68ab8c`; C: WDC @ `fe05d85`) — R22-era pins in the seam-table section headers; outside my one-note allowance (mini-docs domain). Same class: `OT-SEAMS.md` and `LAW-NET-INVENTORY.md` still need their F16 R24 mapping notes (I did only CORE-SEAMS, per mandate).
2. **spec 05 + 15** (their auditors, per the OT module card): 489→536 pins, the 40-file/8,604-LOC tree, the W11 wire state, the census register row (D26.2's spec-05 mirror), the 24+6 census re-base, C7's lineage row.
3. **spec 18** (per the app card): the "FORK… ≥2 waves behind" row superseded by the converging-mirror census; nle-ui 648→674 + the four-seam inventory; the three-way data-test mapping row (D26.5).
4. **spec 16**: nle-ui 648→674 pins + the D29 ⌘S semantics.
5. **spec 09**: app/OT/engine pins ×4 + the persistence row rewrite (R8 W2.3 landed REAL localStorage autosave).
6. **spec 01/03/04**: engine `b8c6c88`→`5036387`, vendor pins a4e971d→c15a629 / f446512→494f6ff, the W2.5/R8-REV landings, the consumer-lag note (the 4ef0147 one-commit delta is now a 9-commit, resolved re-pin).
7. **spec 20 + 02**: WDC `85b81b0` pin, the S4 seal round, the waveform-contract promotion action (the K3 hard-prerequisite with no WDC queue presence — the sharpest mismatch), the mis-scoped `collectSidechainWires` claim.
8. **spec 19**: the fleet map row + cloudcut-nle disposition row (its §0 BASE line still carries R22-era figures per the R23 report).
9. **IMPLEMENTATION-PLAN.md** (the plan-track wave, not a spec file): the K1/§1 re-base (458/536/759/674/174/355/1521+), the S-app/S-ot row re-writes per D26, the productization-gate row per D28, the estimate re-items.
10. **battery_r24** (scripts/): the R24 pin world, the census-coherence + routed/exceptions checks, the retired vendoring-mechanics check dropped, the ordering-law/decision-count/seal-census checks re-pointed per D26/D29.

## NEW FINDINGS

1. **The OT split's R23 ±1 edge is superseded:** the fresh live-run report gives **386 in-page + 150 real-mouse** (59 entries) — the corpus-standard 359+130 (and the 360+129 re-count) are both stale; 00 now carries 386+150 per the module card (the count authority).
2. **The dual-vocab close-out is nearly done in 00:** the only surviving old-vocab text is (a) D19's R22 record (explicitly-marked history + the new R24 window-closes note) and (b) the §3 diagram's ASCII "Cloud Render Server (phase 2, optional)" box label — a v1-era vision-record keep per the R23 precedent (D8's body already carries the r6 re-tag). battery_r24's tightening at fleet close should whitelist the diagram label or the orchestrator re-tags it.
3. **"27/78 union counterparts live" (D11 bracket) has no R24 evidence either way** — the module cards re-verify the 30-name WIRE_COMMAND_TYPES (unchanged at HEAD) but not the spec-15 union overlap; I kept 27/78 and flagged it here so the spec-15 auditor confirms or re-bases it.
4. **D25's body now carries a one-round-old census figure pair** (39 files / ~7,700 LOC, M17-M46, "≥2 waves behind") as its R23 record — deliberately NOT rewritten (point-in-time convention); the R24 amendment note directly beneath carries the current values, so no reader can mistake the record for the canon.

**Verdict: FIXED.** The file is R24-current: the v9.0 header + the five re-pinned fleet rows + the cloudcut-nle row carry the round's canon; D25/D15/D21/D19 carry dated amendment/extension/supersession notes; §2A carries the re-scoped freeze item + the new census law; the stale-pin sweep is clean (all R23 pins now live only in marked historical contexts); the battery invariants hold (25 decision headers, the §2A laws present, every sha spelled exactly).
