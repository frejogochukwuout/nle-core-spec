# R24 fleet audit — `17-test-plan.md` (the overall test plan — the acceptance-executability layer: §0A the law + the K-tier map + §13A the facet matrix + §17A the roof suites + §18.5 the engine reference)

**Agent:** R24-3q (fresh context, single-file owner; Task ID R24-3q). **File owned:** `17-test-plan.md`. **Date:** 2026-09-08.
**Pin world audited against (per the R24 re-baseline, every value traced to a module card before writing):** nle-engine `5036387` (458 — static census, live-run venue GHA CI) · opencut-timeline `ded43c4` (code pin `c15a629`; 536 = 386 in-page + 150 real-mouse, 59 milestone entries, live-run at the R24 module audit; M49C green 24/24 + 6 exceptions) · web-daw-core `85b81b0` (759 — live-run, docs-only over `494f6ff`) · nle-ui `fc4cc35` (674 — live-run) · nle-test-app `c885ece` (174 = GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7) · mini 355 sealed · variants 1521+ (61 files). **Method:** no git commands, no battery/test runs; evidence = the five `module-*.report.md` cards + `ARCH-R24-timeline-strategy-and-topology.md` (v2, esp. D29) + the R23 precedent (fleet-r23/17) + the sibling `12-testing-strategy.report.md`.

---

## VERIFIED-STRONG (claims that checked out against the module cards / ARCH-R24)

- **The §0A law's structure:** the four numbered clauses + BASE/GAP inventory intact; the battery-checked strings preserved verbatim (the §0A heading; "BASE rows state their regression role").
- **The R23 BASE row was fully stale but structurally accurate:** every count it declared moved (440→458, 489→536, 648→674, 117→174; 51→59 entries; 14→18 real-mouse entries; variants 1,542-live→1521+) exactly as the module cards record; the engine 13-suite enumeration, WDC 18 `src/test/` suites, OT report-json authority all still exact at the R24 pins.
- **The W11/D29 facts:** M49C real and green 24/24 at `ded43c4` (24 routed + 6 documented exceptions, tsc-lockstep); OT's DOM convention is `data-test=` (68 sites, zero `data-testid`) — the D26.5 three-way mapping target is real; the app's D30 W-F coverage-gate port is verified PENDING (no wire symbols anywhere in app src/); the 9-A2 census (177 controls: ~127 real / 13 degraded / 21 honest-mock / 16 no-op incl. 9 unexpected-dead) + P1-P26 + Z1-Z7 all still PENDING at `c885ece` (only W-A/W-B re-pins since).
- **The app's consumer pins are now fleet-coherent:** engine @ `5036387`, OT lock-copy @ `c15a629` (byte-exact minus `testing/`), nle-ui @ `fc4cc35`, WDC @ `85b81b0` — the R23 "engine @ `4ef0147` behind" row was superseded.
- **The §2-§16 methodology body:** no live fleet-count claims beyond the audited surfaces (sweep: the only "440" hits are the 440-Hz audio tones + the drift ledger).

## FIXED (edits applied to `17-test-plan.md` — 31 edit sites, minimal-diff)

1. **Status header:** v1.4 → **v1.5** — the R24 note prepended (the count set incl. the app's 6-suite inventory; the D29 instrument registrations; the zero-no-op worklist; battery_r24; the dual-tag window CLOSED); v1.4/v1.3/… kept verbatim as the supersession ledger.
2. **Charge 1 — pins/counts re-based everywhere:** §0A.1 example (OT `ded43c4`/`c15a629` 536 = 386+150); §0A.4 (battery_r24; the R23→R24 drift class added — 440→458, 489→536, 648→674, 117→174, 1542-live→1521+, WDC held 759 docs-only; the count-consistency key now engine 458 + OT 536 across 17+19+00); the §0A BASE row (engine 458/458 `5036387` incl. bridge-seams 113 + nle-bridge 96 + W2.5/R8-REV; OT 536 `ded43c4`/`c15a629`, 386+150/59 entries, W11 + M49C; WDC 759 `85b81b0` docs-only; nle-ui 674 `fc4cc35` + the D29 seams; **app 174/174 `c885ece` — the 6-suite roof enumerated: GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7**; variants 1521+; all consumer pins re-read live); §13A.5 (WDC `85b81b0`; the bridge family counts); §13A.7 re-tier row (full R24 re-base, 2026-09-08, battery_r24); §17A intro + S3(a) (the 150-test/18-entry real-mouse family) + S5 (battery_r24); §18.5 (header `5036387` 458/458 + W2.5/R8-REV + vendor pins OT `c15a629`/WDC `494f6ff`; the Tier-1 row 458/458 @ R24).
3. **Charge 2 — the D29 rows:** the ladder intro gains the adopted-not-prescribed framing; **K1** — ALL GREEN at the R24 pins + OT's milestone register grew to **59 entries** (M44-M49 + M49T/H/R/G + the gate); **K2** — **M49C REGISTERED as K2's OT-side completeness instrument** (accumulated ⊇ `WIRE_COMMAND_TYPES` − exceptions, green 24/24); **K3** — **the D30 W-F coverage-gate port REGISTERED (IN FLIGHT)** + the **shell-path blind spot** (the gate measures the PORT surface, excepted per D30 R2/R8) + **the compensating pin family** (⌘B, [ ]/⌥[ ], ,/., M/⇧M, ⌘M/⌘⇧M through engineService — owner-tagged app-side, D29.1b/F14) + the DOM-structural half re-keyed on the **D26.5 three-way data-test mapping row** (OT `data-test=` 68-site / nle-ui `shell-*` / the structural-gap worklist); **K4** — drives the same 24 routed verbs (D29.1c, shell-path exception identical). The GAP register's K2/K3/K4 rows carry the same registrations; the corpus row's ordering law re-stated per D26 (mapping row + W-C, not swap+testid).
4. **Charge 3 — the K2 census twin row re-verified:** the §13A.7 row stands with the new counts (458/759/536 nets; 386+150/59-entry register; M49C as the OT-side instrument; **registration state R24: still PENDING — K2 entry hasn't happened**; spec 12 owns the registration, this row is 17's plan twin) — the R23 arrangement preserved.
5. **Charge 4 — the app's 26-pin plan + Z1-Z7 (NEW row):** the §0A GAP register gains **"the K3 app-side zero-no-op worklist"** (owner nle-test-app/S-app — the D30 W-F/Z6 waves): the 9-A2 census (30 surface groups / 177 controls: ~127 real / 13 degraded / 21 honest-mock / 16 no-op incl. 9 unexpected-dead) + P1-P26 + Z1-Z7 — **ALL PENDING at `c885ece`** (zero landed; only the zero-behavior W-A/W-B re-pins); acceptance = the P/Z-series land with the D30 waves + the census re-run closes the unexpected-dead set. The K3 ladder row cross-references it as the app-side hardening set.
6. **Charge 5 — phase tags stripped to the D24 set:** the GAP intro declares the window CLOSED (D24 K/w/r only; C/W/R tags remain only in the R23 ledger as history); stripped: "pre-C1/pre-C4" (×2), "the old C4 exit is now", "was C4", "was C3's audio crawl", "was C4's e2e half", "was W-audio/W-color/W-n5/R-fcpxml", "the C1/H8-H17" label, "the old C4→K4 mapping" (§18.5 → "the e2e half → K4 per the D24 split").

## REMAINS-OPEN (verified still-open at the R24 pins — no repo landed them)

- **K2 registration:** PENDING K2 entry (the instruments + counts pre-register the gate; the census rows land at 458/759/536). The missing OT-ops→engine-decode pairs unstarted (S-engine).
- **K3 instrument (W-F):** does not exist app-side (grep-verified per the app card); the port's 5 W-C/W-D adoptions + wire mint are D30's in-flight queue. The 322/115 corpus, offline audio pins (waveform contract still absent from WDC's session queue — spec 20 + D29.4's row), the VLM port, and K4 e2e: all zero app-side.
- **The zero-no-op worklist:** all 26 pins + Z1-Z7 pending; the 9 unexpected-dead controls stand.
- **r2-r5:** unstarted; r4 still D6-gated. **The battery's "in 17+19+00" check** now hinges on spec 19's own 458/536 re-baseline (this file's half is done; 00 already carries both per the 00 report).

## NOTES

- **Count discipline:** OT's 536 keys to the report json (live-run at the R24 module audit; 386+150/59 reproduced exactly from the json by the 12-report's parse); engine 458 = static-census match (submodules/node_modules unmaterialized in the sandbox — the authority is marked in the BASE row); WDC 759 + nle-ui 674 = live runs per the cards; app 174 = static census matching the commit claim. The R23-era "359+130" split is superseded by 386+150 (00's R24 finding) — no ±1 note needed this round.
- **Variants:** 1521+ (61 files) kept per the R24 canon; 00-master's reconciliation note preserved.
- **No normative damage:** the posture law's triad (BASE/GAP + acceptance gates), the §17A regression-continuity law, the S-suite size estimates (annotated R15-era), the A3/A4-v1/m24-m26 lineage labels, and the v1.1-v1.4 ledger are all preserved; the "440 Hz" tone fixtures and the R15-era D12.1/D17 citations untouched. battery_r24's expected string checks verified by inspection (heading + regression-role phrase + 458/536 present; stale-pin sweep clean — R23 shas live only inside the line-4 ledger).
- **Out-of-scope, left to owners:** IMPLEMENTATION-PLAN's own K-row re-writes (the plan/track wave); battery_r24's implementation; spec 19's count twin; spec 05/15/18's own D26/D29 rows.
