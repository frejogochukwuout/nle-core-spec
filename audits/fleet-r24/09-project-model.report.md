# R24 fleet audit — 09-project-model.md (ProjectJSON, scenes, media records, persistence)

**Agent:** R24-3j (FLEET AUDIT, fresh context) · **Task:** file-by-file audit + edit of `09-project-model.md` · **No git commands; two writes total: the spec §0 edit + this report.**

**Evidence packs used:** `fleet-r24/module-nle-test-app.report.md` (the app card), `fleet-r24/module-nle-ui.report.md`, `ARCH-R24-timeline-strategy-and-topology.md` (v2), `fleet-r23/09-project-model.report.md` (the precedent). **Live verification (read-only git + file reads):** app HEAD `c885ece` (submodule pins `5036387`/`fc4cc35`/`85b81b0`; `UPSTREAM.lock.json` upstreamHead `c15a629`), OT HEAD `ded43c4` with **empty src diff vs `c15a629`** (code pin confirmed), nle-ui HEAD `fc4cc35`, engine HEAD `5036387`.

## Edits (§0 + header Status only; §0A + §1–§14 + `## Testing` untouched, per the round rules)

1. **Pins re-based everywhere (charge 1).** Header Status + §0 BASE: OT `222532c`/489 → `ded43c4` **536/536** (386 in-page + 150 real-mouse, 59 entries; HEAD docs-only, **code pin `c15a629`**, src-diff-empty verified); app `70e99f0`/117 → `c885ece` **174/174** (with the static census 70+40+25+17+15+7); engine `b8c6c88`/440 → `5036387` **458/458**; nle-ui `85dcf57` → `fc4cc35`; consumer pins `85dcf57`/`a4e971d`/`4ef0147` → **`fc4cc35` + lock-copy `c15a629` (byte-exact, live-verified) + `5036387` + `85b81b0`**; the no-gap ruling re-confirmed at `c15a629`. Status line 4 carries the R24 record; R23/R22 text preserved as lineage (all stale shas now confined to line 4 — the round record, per the posture law).
2. **The persistence contradiction RESOLVED (charge 2, REQUIRED).** New BASE bullet: `persistenceService.ts` (380 LOC + 17-test suite, live-read) — engine-free (nle-ui store + localStorage, synchronous writes); **2s debounce keyed on the IDENTITY of scenes/mixer/masterVolume/masterMuted/activeSceneId (+ `color` since W2.5) — never `docVersion`**; the edit→dirty→saving→saved lifecycle over the package's `saveState` seam (nle-ui `9b0a36c`); ⌘S real-dirty gate → immediate flush; beforeunload sync last-chance flush (dirty/saving gate); **boot `hydrateFromStorage()` at App.tsx:41 MODULE SCOPE, pre-render, no docVersion bump**; baseline law = current state IS the saved state; `loadSnapshotFile`/`saveSnapshotFile`. Citations: **`51592cf`** (W2.3, 132/132) + **`41f5bc9`** (grade-sidecar: identity key gains `s.color`; pre-W2.5 snapshots hydrate neutral). The GAP row rewritten to the **w3 REMAINDER** with the R23 "no localStorage surface exists app-side" claim explicitly RETIRED: OPFS/atomic-write/migrations/locks + the D30 W-E/R7 file affordance (zero `onSaveScene`/`onLoadScene` hits in src/ — live-verified) + format formalization.
3. **sceneBridge re-verified (charge 3).** 503 LOC / 25 tests unchanged at `c885ece` (live wc + grep). R8-b1 fix recorded BASE: `c2be330` — load effect's `core`-dep removed + `useCommittedRef(core)` (EngineMount.tsx:127,162-169 verified in tree); the infinite-rebuild loop fixed; a dedicated real-scene-switch regression pin + the per-scene grade isolation round-trip live.
4. **w3 rows kept honest (charge 4).** Persistence = remainder (above). Multi-scene row: R8-b1 makes the rebuild settle once **but it still resets engine undo per switch** — the lifetime policy stays the REAL w3 slice. Cross-scene-undo + history-budget row: unchanged (still satisfied trivially; the budget law unbuilt). The app's autosave is recorded as the SEED of w3, not its completion.
5. **K3 rows (charge 5).** Zero LAW-NET/e2e app-side (grep live: 0 hits at `c885ece`); the row carries the D26/D29 framing — routed-verb completeness = the D30 W-F coverage-gate port; the DOM half gates on the `data-test` mapping row + W-C, not props-upstreaming. The two C1(d) registered decisions (track-shape mapping; stills synthetic extent) kept.
6. **Phase tags (charge 6).** Live GAP tags stripped to the D24 set: "was W-project per the R22 register; D24: realness = walk" and "W-media per D24" removed → w3/w2/K3 only (r4 for N5 user-gated decode). OT row gains the W11 wire-dispatch note (24 routed + 6 exceptions, M49C; D29's instruments). Battery posture extended: the app's persistence 17-pin suite + the R8-b1 real-switch pin carry the app-side half.

## Next actions for the w3 owner
OPFS layer + migration ladder; the W-E/R7 save/load affordance wiring; the multi-scene lifetime policy + history budget; the persistence-format formalization (nested `SceneTracksJSON` + schema-versioned wrapper vs the traveling REGISTERED-DELTA flat subset).
