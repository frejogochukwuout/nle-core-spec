# fleet-r24 audit report — 20-audio-core.md

**Agent:** R24-3t (spec-file auditor, fresh context). **Date:** 2026-09-08.
**File owned:** `nle-core-spec/20-audio-core.md` (the audio-core spec: WDC + the engine bridge + the app's audio host).
**Pins verified against (the R24 pin world):** WDC `85b81b0` (759/759, 33 test files, tsc 0 — live-run by the module scout) · engine `5036387` (458/458) · OT HEAD `ded43c4` (536, code `c15a629`) · nle-ui `fc4cc35` (674) · app `c885ece` (174). **Zero git commands run** — pins verified by raw file reads (`.git/refs/heads/main`, `.git/logs/HEAD`, `.git/ORIG_HEAD`) + the module cards' gitlink evidence; all code claims verified by reading/grepping the live repos.
**Evidence packs read first:** the WDC module card, the app module card, `ARCH-R24-timeline-strategy-and-topology.md` (v2 — esp. D29.4), the R23 precedent `fleet-r23/20-audio-core.report.md`, the full spec (219 lines → 223).

## VERIFIED LIVE (the load-bearing claims)

1. **The D29.4 mismatch is real.** WDC `PLAN.md:150-152` carries the waveform row ONLY as the unchecked long-horizon bullet ("the peaks→view data flow is wired nle-engine-side — the S-layer data contract"); `HANDOFF.md` "Next-session scope" = mixer surface / 4(=5) test ports / async pre-retime / P3 ledger — **no waveform row, no contract statement**. The de-facto contract shipped at `audio-registry.ts:24-74`: `getAudioBuffer`/`setAudioBuffer`/`getPeaks`/`setAudioPeaks` (the pre-computed-peaks setter — NOTE: the module card and ARCH D29.4 call it "setPeaks"; the live export name is `setAudioPeaks` — the spec records the live name)/`computePeaks(buffer, buckets) → {min, max: Float32Array, buckets}`, fixed `PEAK_BUCKETS = 2000`, mono mixdown, every-sample bucket law, setter-maintained peaks cache.
2. **The collectSidechainWires mis-attribution confirmed.** The helper is WDC's (`channel-strip.ts:63-78`; its docblock's "the ENGINE's rewireSidechains" = WDC's own realtime-engine concept). grep: **zero uses in `nle-engine/src`** (only future-wave `audioDucking` comments in `core/types.ts`); the ONE live consumer is the app's `audioService.ts:56` (`rewireSidechains()` :315, sever-all + collect, after every `materializeStrips`).
3. **S4 seal round:** HANDOFF "the seal round (seam-round S4) CLOSED the seal queue", SEAL-READY P3/P4-only; `.agents/SKILL.md` §11 = six seal-round laws; PLAN's honest ledger. DSP substrate: `EqEffect` (effects.ts:69), **17 `implements EffectNode` classes in effects.ts** (+1 WAM host), ChannelStrip insert/send/§7.3-sidechain topology, PDC.
4. **FIVE test ports, none ported:** PLAN.md:143-149 + HANDOFF name engine / engine-metronome-automation / engine-param-channel / dsp-aux-sends / wam-effect — `nle-engine/tests/vitest/` census: no such bodies. `nle-bridge.test.ts` still **96** `it(` at `5036387` (the +18 engine pins are W2.5/R8-REV composition rounds).
5. **App consumers @ `c885ece`:** U9 ducking (`insertEffects` :110, ducking sidecar → external-sidechain compressor; 5 pins), `deliverService.ts` (:37/:39 imports, :167 `buildAudioSegments`, :191-192 FRESH `SceneMixer` + `renderOffline` — "NEVER the live singleton"), the W2 el.speed→retime + W3 JKL laws re-read live (`lastScheduledTr`, `varispeedRate: segRate * tr` :586, silent-reverse J).

## FIXED (all six charges applied; minimal-diff, posture law kept)

1. **Pins re-based everywhere:** status line → v-next-3 (Round 24, R24 re-pins, R23 lineage kept verbatim); R15 line's "live canon" → `85b81b0` + the consumers-one-docs-commit-behind pattern named at its R24 values (engine `494f6ff`, app `85b81b0` opportunistic); §0 BASE WDC `494f6ff`→`85b81b0` (code-pin distinction drawn); engine `b8c6f88`/440→`5036387`/458 (+ vendor pins OT `c15a629`, WDC `494f6ff`); app `70e99f0`/117→`c885ece`/174; §10 triangle row → `85b81b0`; §5 preamble re-verified note (96 at `5036387`); §0 heading + BASE date → R24.
2. **The D29.4 PROMOTION row (§0 GAP):** the waveform-contract row now carries the full promotion — the parking-lot citation (PLAN.md:150-152), the NO-HANDOFF-row finding, the action (promote to WDC HANDOFF next-session scope + state the contract + pin it), the contract statement (the live `audio-registry.ts` shape incl. `setAudioPeaks` + `PEAK_BUCKETS = 2000`), the plan's S-wdc row pointer, and the acceptance sharpened to "CARRIED IN WDC's OWN HANDOFF QUEUE". §7 gained the matching Round-24 amendment; §11 gained the `audio-registry.ts` canon row.
3. **S4 seal round recorded (§0 BASE WDC):** the 2-audit → 3 P1 + 8 P2 record, SEAL-READY verdict, SKILL §11 six laws, the honest remaining ledger (mixer-surface DSP substrate vs the missing NLE-scene→strip wiring + the F6 `el.effects` sidecar decision, **FIVE** test ports — off-by-one fixed, async pre-retime r2, JKL CLOSED with R4 the ⇧K residue, the P3 ledger).
4. **collectSidechainWires attribution fixed** at both stale sites (§0 mixer row + §7's M2 paragraph): WDC-owned (`channel-strip.ts:63`), app-consumed (U9 rewire), nle-engine wires nothing today (future-wave comments only).
5. **App audio-consumer rows re-verified + extended** (§0 BASE app): the R8/R9 consumers appended — U9 ducking (insertEffects mapping + rewireSidechains via the shared contract, W2.1 @ `efd8243`) and the deliver export's audio leg (fresh `SceneMixer.renderOffline` over the real segment pipeline, W2.4 @ `278b95b`; offline path skips worklet-backed inserts). The W2/W3 rows re-verified live at `c885ece` (the charge's "W2.3→W3 JKL audio half" read as the W2-consumption + W3-JKL rows — both re-read in `audioService.ts`; the app's W2.3 is autosave, out of this file's domain).
6. **Phase tags finalized to the D24 set:** the four transitional parentheticals stripped ("(was W-audio)" ×3, "(was C3)" + "(was C3 (verify))"); the GAP heading now records the window closure.

## REMAINS-OPEN (the §0 GAP register at the R24 pins — 7 rows, zero landings)

Mixer surface (r2, WDC+engine, design-first) · the FIVE deferred test ports (r2) · mixer G-surface full wiring (r2, app+engine) · realtime-vs-offline NULL rig A4-v2 (r2, app) · async pre-retime (r2, engine; WDC design-w2 §4) · **the waveform seam contract (K3 — now PROMOTED; the WDC-side queue presence is the spec's demand, to be executed by the plan's S-wdc row)** · K3's audio pins + worklet asset serving (K3, app — the app's `public/` does not exist at all, re-verified).

## NOTES

- **The pin-mangling incident (SKILL §11 law #1, live again):** this task's briefing says the R23 engine pin is "b8c6c88"; the spec file, the R23 report, and nle-engine's own `.git/logs/HEAD`/`ORIG_HEAD` (raw byte reads) all say **`b8c6f88`** (full: `b8c6f88517ad92e62882bc7cd147cf514be46ad2`) — an f↔c display mangling, the same class as R23's "0ac3c15 vs `0ac3f15`" incident. An early batch edit failed on exactly this; every hash in the edited spec was re-verified byte-level after writing (`[m`-adjacent content included — the pipeline ate it from Bash *display* twice; the Edit/Write paths proved safe).
- **Count discipline:** 759/33/458/96/174/17 EffectNodes/5 test-port bodies — all verified live or module-card-live; "setPeaks" corrected to the live `setAudioPeaks`.
- **No normative damage:** §1-§6, §8-§9, §12 untouched; all Round-15/23 amendments and historical pin chains kept verbatim; the R23 status lineage preserved inside the v-next-3 chain.
