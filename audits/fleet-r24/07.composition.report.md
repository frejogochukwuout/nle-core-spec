# Fleet R24 — spec 07 audit report (composition: scene graph / layer model / blend / transitions)

**File audited:** `nle-core-spec/07-composition.md` (sole file edited; this report is the second write)
**Agent:** R24-3h (SPEC-FILE AUDITOR, Task ID R24-3h) — no git commands, no suites run; counts per the R24 module cards, features verified by live file reads
**Live repos verified (2026-09-08):** nle-engine `5036387` (458/458 static census; submodules OT `c15a629` + WDC `494f6ff`, gitlink-level) · opencut-timeline `ded43c4` (536/536; code pin `c15a629` — verified via the app's byte-exact lock-copy, live-read) · nle-test-app `c885ece` (174/174 static census) · nle-ui `fc4cc35` (674) · WDC `85b81b0` (759) · variants `e89cd65` (1,521, unchanged — no R24 variants pin)
**Evidence packs:** the R24 engine + app module cards, ARCH-R24 v2 (D26/D29), the R23 precedent report, the twin 04-renderer-color R24 report (coherence-checked)

---

## VERIFIED-STRONG (held against the live repos)

1. **The §12.A.1 engine table — ALL 17 rows re-verified line-exact @ `5036387`** (not a sample — every row checked): scene-assembly :16/:1358/:1379/:1870, player :1321/:1575, types :311/:704, registry :2249 (27 presentations re-counted), planner :21/:378, handle-utils :318, transitions/pipeline :90, effects/pipeline :5113/:5148/:5204, mask-manager :671/:723, compositor :33 (25 modes re-counted), texture-pool :5, transform-parent :878. The W2.5/R8-REV rounds touched ONLY `bridge/composition-frame.ts` + `bridge-seams.test.ts` (module-card stat) — zero drift.
2. **The W2.5 delta, verified in full at `5036387`:** `CompositionElementParams.effects` (:119, enabled-only/id-dropped per `CompositionElementEffect` :124 — enabled is the stated consumer domain), `CompositionDrawOp.filter` (:161-165), `buildElementFilterString` (:223 — Gaussian Blur → `blur(radius px)`, default 4, non-finite→4, **negative ≥ 0 per R8-REV #5 `4b2dfd1`** at :231-235; honest skip :229; join-with-spaces; identity `''`), the op threading at :388, the painter's save/restore assignment (:424/:443, Safari<18 graceful), and the grade-DECLINE law in the header (per-element grade ≠ composited grade — D29.5(c)).
3. **The app's composition consumers, verified @ `c885ece`:** ProgramCanvas (242 LOC) — `compositionParamsFromDoc` maps `el.effects` (enabled-only, id-dropped), `buildGradeFilterString` + the scratch-canvas **compose-then-filter final pass**, jsdom pins via `data-ops`/`data-playhead`/`data-filters`/`data-grade`; deliverService (344 LOC) — `renderFrame` = `buildCompositionFrame` + `paintCompositionFrame` on an OffscreenCanvas at spec dims, fed to `renderCompositionCore` (deep import) with **mediabunny@1.50.8** exact (package.json:23); the frame-PNG path paints the same way; `buildAudioSegments` consumed at audioService :469/:515 (mid-entry math); the ducking-sidecar fold (`insertEffects`/`duckCompressorParams` → external-sidechain compressor + `rewireSidechains` over WDC's exported `collectSidechainWires` @ `85b81b0`).
4. **The bridge pin census:** `bridge-seams.test.ts` = **113 `it(` pins in 21 describes** (live count; was 97/18 @ the R23 pin). The three W2.5 describes (:921/:970/:998) carry 20 pins incl. the R8-REV #5 clamp pin (:931). Engine total 458 = the module card's static census exactly.
5. **The Wave-4A P0 rows stay FIXED** (charge 5): `applyToTexture` + legacy `apply` return caller-owned per-call textures (:5148/:5204); `getMaskResult` per-call outputs with invert-exactly-once (:671, :739-757 area) — both re-verified live.
6. **The N1/projector parity corpus: STILL OPEN @ `5036387`** (charge 4) — the engine's HANDOFF next-steps is G1/G2/N5/P3, no S4/parity row; the formal corpus remains unauthored (owner S-engine ∥ crawl).
7. **The OT composition surface @ `c15a629`** (live-read via the app's lock-copy): `ElementTransition` at types :145 (field :190), the 7-way union :231-238, params overwrite-only :166-168, split's left-drop :161 — all exact; placeholder-compositor 175 LOC, header still names spec 04's renderer.

## FIXED (edits to 07-composition.md)

1. **Pins re-based everywhere** (charge 1): header status → R24; §0 header + BASE header + all four rows → engine `5036387` 458/458 · OT `ded43c4`/code `c15a629` 536/536 · app `c885ece` 174/174 · mock `e89cd65` 1,521 (unchanged, no R24 pin); engine vendors OT `c15a629` + WDC `494f6ff`; §5.5 + §6.1A in-law pin bumps; §12.A.1 header 458/458 + 54,791 LOC + an R24 re-verify note. Line-citation drift corrected: OT `ops/timeline-core.ts:2613` → **`:2638`** (the W11-era rounds shifted the file; the R23 "only key-delete path in the patch surface" phrasing softened to the verified law — null DELETES, undefined keeps the prior value; the upsert-path mirrors at :407/:419 carry the same law); placeholder-compositor 176 → 175 LOC; ProgramCanvas 154 → 242 LOC.
2. **The W2.5 delta promoted to BASE** (charge 2): the engine row's "Landed since the R23 pin" block (effects field + filter op + `buildElementFilterString` + the R8-REV clamp + the grade DECLINE as law) + a NEW §12.A.1 row (§7.2 effect → preview resolution, composition-frame.ts:223) + the bridge census 64→97→**113**.
3. **The app row rewritten** (charge 3): both render directions (monitor + export), the grade final pass, mediabunny@1.50.8, the audio legs.
4. **GAP register re-checked** (charges 4-5): parity row re-pinned + the engine-card queue check; the Wave-4A re-verify note in BASE; the v1-exclusions row extended with **the open effects-preview widening (D29.5(d))** and the transform-sidecar future-extension CR (D29.5(a)).
5. **Phase tags stripped to the D24 set** (charge 6): "(was C2-behavior)" / "(was C4)" / "(was R-engine-p2)" parentheticals + the row-label C2/C4 prefixes dropped; the GAP header's "dual-tagged with the retired spec-14 vocabulary" clause removed; the spec-14 §4.2 provenance bracket reduced to the live spec-04 twin cross-ref; K3's "∥ the D25 bridge" re-pointed to the D26 census discipline; battery_r23 → battery_r24.

## FINDING (charge-vs-repo mismatch, honestly handled)

- **`foldSidechains` does NOT exist** in any live repo (grep across engine/app/nle-ui/WDC src, all evidence packs, and the whole spec corpus: zero hits). The charge's name is unverifiable; the surface it points at is real and is what the spec now records: the app's ducking-sidecar fold + `rewireSidechains` + WDC's exported `collectSidechainWires`. The spec text carries only the verified names.
- **Pin-arithmetic caveat:** the engine commit messages claim W2.5 "+17 pins, 440→457" and R8-REV "+1 → 458", while `bridge-seams.test.ts` nets **+16** (97→113; the round's −21 replaced lines evidently folded legacy pins). The live census (113/458) is stated as authority; no decomposition beyond the verified counts is asserted in the spec.

## REMAINS-OPEN (unchanged posture)

The projector parity corpus (S-engine ∥ crawl) · the N1 v1 exclusions + the effects-preview widening (D29.5(d), S-engine) · K3 (the law-net re-expression is still unstarted app-side; the 174-test glue suite is the current approximation) · K4 (no e2e exists app-side, grep-verified) · r6 (presentation depth + P2 surfacing). The K1-set numbers behind these rows are the fleet's (458/536/759/674/174/355/1521+).

---

## VERDICT

**The composition thesis held again and the round's one big delta landed exactly as the module cards record: the W2.5 effects sidecar (`8a0b7fe` + `4b2dfd1`) is now the composition pipeline's honest CSS-filter subset, consumed on BOTH sides (ProgramCanvas's monitor preview + the compose-then-filter grade final pass; deliverService's real mediabunny export painter). Every pin, count, and line citation is re-based and re-verified live (all 17 §12.A.1 rows exact, one drifted OT citation corrected); the Wave-4A P0s stay fixed; the parity corpus stays queued; the phase vocabulary is D24-only now. One charge item (`foldSidechains`) failed name-verification and was replaced with the verified surface.**

*R24-3h — read-only except `07-composition.md` + this report. No git commands, no test suites run.*
