# R27-W3 — spec-04 audit (04-renderer-color.md, the renderer + GPU pipeline)

**Task ID:** R27-W3-04 | **Agent:** spec-04 | **Round:** R27 final-tightness audit, Wave 3 (per-spec fleet)
**Spec audited:** `04-renderer-color.md` (2,398 lines) — section-by-section at sub-spec depth.
**Ground truth:** nle-engine @ `f9ac806` (748/748, tsc 0; `src/lib/nle` 57,064 LOC; timeline.ts 8,997) — live-read: `bridge/composition-frame.ts` (552 L, full read), `gpu/*`, `effects/*`, `playback/player.ts`, `headless/api.ts`, `tests/vitest/engine/bridge-seams.test.ts` (2,584 L, 151 `it(`), `.agents/{PLAN,HANDOFF,DECISIONS}.md`; nle-test-app @ `c020b2a` (deliverService, ProgramCanvas); nle-ui @ `32abd58`; opencut-timeline @ `55c81c0` (placeholder-compositor re-verified); ui-mock (shell-mini color-absence re-verified; variants' `src/lib/color/` 170 `it(`). The mock-color report (`audits/fleet-r27/mock-color.md`) read in full. FreeCut/OpenCut-classic reference repos are NOT in the sandbox — §11/§13.A-C/§15's reference-repo cites are SCOUT-04-era verifications, checked for internal consistency only (noted limitation, not a finding).

---

## §1 The per-section verdict table

| § (lines) | Content | Verdict | Evidence |
|---|---|---|---|
| §0 FORWARD INVENTORY (12-31) | BASE + GAP register | **P1** (one false law statement; plus P2 stale pack) | See §2 rows 1-5 below |
| How to Read / §1-§3 (34-105) | Purpose, goals, problem | **SOUND** | Decision 4/5 refs verified (00-master :145/:158); the 8-bit-problem statement matches the engine's live truth (§13D, below) |
| §4 Architecture (109-177) | The pipeline diagram; the "All grading math happens here" box (04:144-153) | **SOUND + P2 note** | The box is the venue anchor mock-color §5 builds on — coherent with the venue matrix (fragment passes over the scene-linear `rgba16float` working texture). The "Curves (LUT applied in linear)" line needs the CPU-bake qualifier (08 §5.2/§5.3 — the venue-law cross-link, §2 row 6) |
| §5 Texture Format Chain (181-277) | r16uint planes, rgba16float working, canvas feature-detect | **SOUND** | §5.2's rgba16float rationale (04:243-247) unchanged; §5.3's try-configure/fallback consistent with §11.9/§16.4; no live-truth drift |
| §6 Color Space Management (279-460) | YUV→linear WGSL, display transfer, tone map | **SOUND + P3** | §6.4 tone-map law (Reinhard/ACES filmic; v1 filmic HDR→SDR) matches mock-color row 9 ("REPRODUCIBLE — specced; the mock doesn't model it — a parity-fixture note for r3"). P3: 04:296 says "Store this on the `MediaInfo`" — 09's container is `MediaRecord` with `colorInfo: MediaColorInfo` (09:258/:265; the shape at 04:288-293 is field-identical) |
| §7 Renderer Architecture (462-611) | WebGPURenderer, bind-group cache, texture pool, pass-discipline rule | **SOUND + P2 cites** | Pass-discipline cites SURVIVED the engine move: `effects/pipeline.ts:5204` (Wave 4A text, live-read exact) + `mask-manager.ts:647-670` (the getMaskResult Wave 4A block, live-read) — but the §13D table's siblings are stale (§2 row 8). OpenCut `compositor.rs:344-405` cite unverifiable (no reference repo in sandbox — historical) |
| §8 WGSL Shader Inventory (613-668) | Core/composition/effects/JFA/scopes tables | **P2** | Two findings: (a) **§8.5's bit-depth shorthand (04:667)** — "change `255.0` → `1023.0` (10-bit)" conflicts with 08 §18.2's exact law (histogram ×1023 **with `linear_to_srgb` before binning** + 1024 bins; waveform/vectorscope weights ×65535) AND with 04's OWN §11.13 (items 2-3: ×65535) and §14.B (×65535) — a three-way internal disagreement, exactly the drift mock-color §4 row 10 warned about; (b) **the venue-law cross-link is missing** (mock-color §4 row 9's P1 — 04's half is a pointer in §8, §2 row 6) |
| §9 WYSIWYG Contract (671-695) | Browser ≡ cloud, bit-identical | **SOUND** | The `readPixels('rgb24'\|'yuv422p10le')` signature matches 11-cloud-render's contract (11:270 `pixelFormat: 'rgb24' \| 'yuv422p10le'`, 11:326 `renderer.readPixels`) |
| §10 GPU Memory Budget (698-727) | ~340 MB @ 4K | **SOUND** | Superseded-by-§16.6 note present (04:721) |
| §11 Open Questions (729-1074) | Q1-Q13 answered | **SOUND + P2 refresh** | Q1's engine figure **re-verified EXACT**: 46 `rgba8unorm` sites across 10 files @ `f9ac806` (live count: player 7 + text-texture-cache 4 + effects/pipeline 8 + row-alignment 1 + lut 1 + glyph-atlas 3 + compositor 7 + texture-pool 4 + mask-manager 8 + transitions 3 = 46) — the count survived 458→748 tests; the 44-effect registry also EXACT (43 ids in `effects/pipeline.ts` + `gpu-lut` in `effects/lut.ts`). Q13's ×65535 items conflict with §8.5 (above) |
| §12 Test Plan (1076-1110) | Preserved intent list | **SOUND** | Unchanged seed text, no live-truth coupling |
| §13 Code References (1112-1208) | §13A-C reference repos; §13D nle-engine | **P2** | §13D: 9 of 12 rows live-verified exact (compositor.ts:1257/:33, device.ts:79/:67/:59, lut.ts:314, texture-pool.ts:190, transitions/pipeline.ts:116, effects/pipeline.ts:12); **3 rows STALE** (§2 row 8) + the intro's "every file:line below re-verified unchanged" claim is now false (the engine moved 5036387→f9ac806) |
| §14 Corrections (1211-1374) | Seed-spec corrections 14.A-14.I | **SOUND** | Historical corrections; §14.B's ×65535 law is the right one (the §8.5 conflict filed above); §14.A/§14.D/§14.E internally consistent with §6.2/§11.7/§11.9 |
| §15 WGSL Shader Quotes (1377-2062) | The verbatim shader quotes | **SOUND (by history)** | Reference repos absent from the sandbox — the quotes are SCOUT-04 verifications, not re-verifiable; no corpus-internal contradiction found. §15.G's 7-point adoption plan (BT.709 luma, 0.18 mid-gray masks, no-clamp) is consistent with 08 §4.2's ported shader and the mock's gradeMath (mock-color §2) |
| §16 Texture Format Chain (2065-2129) | The canonical format tables + memory budget | **P2** | **§16.5's scope-source row (04:2106)**: "Reads from working linear-light texture" — the measured-signal ambiguity (mock-color §4 row 5: neither 08 nor 04 says PRE- or POST-grade; the mock measures post-grade display-referred = Resolve behavior). §16.4 canvas matrix consistent with §11.9/§11.10 |
| §17 Testing (2132-2395) | Tier 1/2/3 + property tests + assets | **P2** | **04:2301's cite "spec 17 §6.3 interactive latency invariant" is WRONG**: 17 §6.3 is Audio WYSIWYG (17:1162); 17's own perf thresholds are deliberately "50ms not 33ms" (17:1646, 17:2595); the 33ms law's home is **08 §2 goal 4 (08:59) + 08 §14 test 9 (08:970)**. 04's other cites verified: 12 §5 pixelmatch (12:300), 17 §6.1 (17:1078), 16 §3.11 (16:411), spec-15 addEffect/toggleEffect verbs (15:391/:395) |

---

## §2 The fix-list (exact edits, amendment-wave ready)

**Row 1 — [P1] §0 BASE row (04:15): the Gaussian-Blur default is FALSE now.**
Spec: "Gaussian Blur → `blur(radius px)` (radius from params, **default 4**, non-finite→4, negative clamped ≥ 0 per the R8-REV #5 …)".
Engine truth: `GAUSSIAN_BLUR_DEFAULT_RADIUS = 10` (composition-frame.ts:210, "F2-5 … the old fallback (4) previewed a blur the render never shipped — a param-less sidecar … rendered blur(4px) in the viewer while the GPU path shipped radius 10"), pinned at bridge-seams.test.ts:985/:990/:1010 (the F2-5 cross-module coherence pin reads the registry's declared `gaussianBlur.params.radius.default` live).
**Edit:** replace "(radius from params, default 4, non-finite→4, …)" with "(radius from params, **default 10 — the GPU registry's declared default per the F2-5 cross-module coherence pin** (the old default-4 previewed a blur the render never shipped), non-finite→10, negative clamped ≥ 0 per the R8-REV #5 …)". Same-frame row refreshes: "`bridge/composition-frame.ts:223`" → "**:257**" (buildElementFilterString); "(:161)" for `CompositionDrawOp.filter` → "**:179**"; "`bridge-seams.test.ts` now 113" → "**151 @ f9ac806** (the F2-5/F2-6/F2-7/OV-05/P1 hardening waves)"; append to the N1 row: "the transitionOut input law (OV-05: the ENGINE field `el.transitionOut` is the base, the sidecar `params[el.id].transitionOut` overrides — law text at 07 §6.1A; the F2-6 try/finally exception bracket + the F2-7 videoWidth/videoHeight probe are the painter's landed hardening)".

**Row 2 — [P2] §0 GAP row 1 (04:22): the wire-trap cites are stale.**
"`GPU_EFFECT_TYPES` (:1509/:1515 …)" → "**:1492** (the const), **:1529** (`gpu-power-window`), **:1535** (`gpu-secondary-qualifier`)". The trap itself RE-VERIFIED standing @ f9ac806 (the 44-id registry implements neither; the engine P2 backlog still lists "scopes (waveform/vectorscope)" — PLAN.md:94).

**Row 3 — [P2] §0 GAP row 2 (the r3 grade-math binding twin, 04:23): add the sharpened phrase.**
08 §0's r3 row (08:22) reads "the 8-bit→10-bit correction is the binding's substance — the engine's `rgba8unorm` chain, the LUT data path's Uint16Array/rgba16uint port (§16.B/§16.C), and §17.D's LUT linearization fix land with it". 04's twin names the landing surface (§5.2/§16.3 + §6.2) but never the correction itself.
**Edit:** append to the row: "**the 8-bit→10-bit correction is the binding's substance (08 §0's row verbatim): the engine's 46-site `rgba8unorm` chain (§11.1/§13D — re-verified exact @ f9ac806) is the target the §5.2/§16.3 working textures replace.**" This is the mock-color cross-cutting P1's spec-04 half ("every verdict assumes that binding lands").

**Row 4 — [P2] §0 GAP row 3 (the D29.5c DECLINE row, 04:24): two stale clauses.**
(a) "the app's Z2 grade→export fix … stands as the CONSUMER-side open item in the app's D30 W-E queue" — **Z2 LANDED**: deliverService.ts:300-324 @ `c020b2a` ("Z2 (D30 W-E): the scene grade reaches the EXPORT — the same final-pass law ProgramCanvas uses … ctx.filter = the grade string").
(b) "not re-filed engine-side" — **the engine DID re-file it**: PLAN.md's R9-c row (open checkbox): "promote the SCENE GRADE into the shared composition/export law … One grade consumer at the SEAM (the painter's final pass behind a grade param) … Proposal: `paintCompositionFrame(ctx, ops, media, { grade })`". Note: R9-c preserves the compose-then-filter ORDER (the D29.5c math law) and only moves the venue consumer→seam — but 04's row text says the decline is permanent.
**Edit:** flip (a) to "Z2 landed app-side (`deliverService.ts:300-324` — the export now grades with the same final-pass law)"; append to (b): "*(R27 note: the engine's PLAN re-filed a seam-side final-pass proposal (R9-c: `paintCompositionFrame(…, { grade })`) — the compose-then-filter ORDER is preserved, the venue moves consumer→seam; the D29.5c decline stands until that proposal is ratified as its evolution or rejected.)*"

**Row 5 — [P2] §0 GAP row 5 (the projector parity corpus, 04:26): register the NS-4 adjacency.**
The row's promise is still accurate-open (verified: no S4 suite, no parity corpus, no projector row in the engine PLAN @ f9ac806; the N1 family's pins live in `tests/vitest/engine/bridge-seams.test.ts` — 151 `it(`, the N1 describes at :720/:926). But the engine's HANDOFF next-steps #1 (the r1-port design round) names "SA-1's **NS-4** (the SceneTracks→GPU render path)" as a design input — NS-4 (PLAN.md:475: "effects/masks/text at fidelity need either a SceneTracks→render-plan adapter or a new compositor input contract over SceneTracks") will reshape the surface the parity corpus runs against.
**Edit:** append: "*(R27 register: the engine's r1-port design round (HANDOFF #1) carries SA-1's NS-4 SceneTracks→GPU path as an input — the corpus's target contract may be reshaped by that ruling; the corpus itself remains unauthored @ `f9ac806`, engine PLAN has no S4 row — verified.)*"

**Row 6 — [P2] §8 intro (insert after 04:615): the venue-law cross-link (the mock-color P1's 04 half).**
**Edit:** add a paragraph: "**Venue law (cross-link — the answer to 'to what extent can we reproduce these in WebGPU'):** the canonical component × stage × data × fallback venue table lives in **spec 08** (the §0 D-row / new §20 per `audits/fleet-r27/mock-color.md` §5); this inventory is its renderer-side index. Summary: all per-pixel color math (the wheels' 14-step grade, the curve-LUT fetch, the 3D LUT, the qualifier, the power window, tone mapping) runs as **WebGPU fragment passes over the scene-linear `rgba16float` working texture** inside §4's 'all grading math happens here' box — one pass per grade node, ping-ponged (§5.2), composed between composite and display transfer; clip→timeline (and future node-graph) grades are **sequential full passes, never params merges**; **curves + 3D LUTs are CPU-baked at edit time** (1024-entry 1D / 33³ 3D, 16-bit) and uploaded as textures (08 §5.2/§5.3/§7.3) — never evaluated per pixel on the CPU; **scopes are compute-shader reductions over the post-grade signal** (08 §11+§18), rendered on-GPU (readback only as the canvas2d fallback), throttled 10fps (08 §11.4); **UI gesture state is CPU-side** and reaches the GPU only through the 112-byte uniform write per committed grade; the mock's `src/lib/color/` is the **parity oracle** (≤1 LSB-equivalent, shared fixtures, CI), not a production venue — there is no production CPU render path (WebGPU-only, master Decision 4)." (Placement ruling: canonical = 08; 04 = §8 pointer + §4's box stays the anchor — see §4 notes below.)

**Row 7 — [P2] §8.5 (04:667): replace the bit-depth shorthand with the 08 §18 pointer.**
**Edit:** replace "We must change `255.0` → `1023.0` (10-bit) and source-tex format → `rgba16float`. See §14.B." with: "The exact per-scope port is **spec 08 §18** (the canonical table): source texture → `rgba16float`; **`linear_to_srgb` BEFORE binning** (colorists read gamma-encoded); histogram 256→1024 bins at ×1023; waveform/vectorscope accumulator weights **×65535** with clamp (HDR headroom); render-side `refValue` re-scale. See §14.B + 08 §18.2." (Kills the §8.5-vs-§11.13/§14.B-vs-08 §18.2 three-way disagreement; adds the missing encode-before-binning law — 08 §18.2's core, mock-color row 3 risk (a).)

**Row 8 — [P2] §13D (04:1192-1207): refresh the 3 stale engine cites + the header.**
- Row 1: `compositor.ts:1797` → "**:1825**" (the `format: 'rgba8unorm',` inside `uploadRgba`, fn at **:1818**); the intro note's "uploadRgba at :1790" → ":1818", "called from `playback/player.ts:1630`" → "**:1641/:1647**".
- Row 8 (mask invert-once): `mask-manager.ts:724` → "**:542-557**" (the `invertNext` option + flag write; :724 now a getMaskTexture line).
- Row 9 (all item types): `player.ts:1575` → "**:1580**" (the `clip.type !== 'video'` dispatch line, live-read).
- Header (04:1192): "458/458, 54,791 LOC … @ `5036387`" → "**748/748, 57,064 LOC … @ `f9ac806`**"; the claim "the W2.5 round touched only bridge/composition-frame.ts … every file:line below re-verified unchanged" needs the R27 re-anchor note (the E1/E2 + RC/OV waves moved the GPU files).

**Row 9 — [P2] §16.5 (04:2106): rule the measured signal.**
**Edit:** change the scope-source note "Reads from working linear-light texture." → "**Reads from the POST-GRADE linear-light working texture** (the WYSIWYG instrument — the colorist sees the graded signal; the mock measures post-grade display-referred, Resolve's behavior — mock-color §4 row 5), `linear_to_srgb`-encoded before binning per 08 §18.2."

**Row 10 — [P2] §17 Tier 3 (04:2301): fix the 33ms cite.**
**Edit:** "(1 frame at 30 fps; spec 17 §6.3 interactive latency invariant)" → "(1 frame at 30 fps; **spec 08 §2's real-time feedback law** — 08:59/08:970; spec 17 §8.3's CI perf thresholds deliberately use 50ms-not-33ms slack, 17:1646 — the 33ms invariant is 08's, not 17's)". (Same fix needed at 08:2135 — cross-spec note.)

**Row 11 — [P3] §6.1 (04:296): the container name.**
"Store this on the `MediaInfo`" → "Store this on the **`MediaRecord.colorInfo`** (09 §7's container; 09:258/:265 — the `MediaColorInfo` shape is field-identical to §6.1's)".

**Row 12 — [P3] §7.1 (04:494): add the 00 §5 pointer for DegradedRendererBanner** (18:508 carries "DegradedRendererBanner placement (spec 00 §5)").

**Row 13 — [P2] Status header (04:4) + §0 BASE pins: the R27 re-key (mechanical).**
Engine `5036387` 458/458 → **`f9ac806` 748/748** (docs-only head over code anchor `50b91f5`; 57,064 LOC); OT `ded43c4` 536 → **`55c81c0` code-pin `970948a` 632** (placeholder-compositor re-verified present + still names this spec, :13); app `c885ece` 174 → **`c020b2a` 252**; nle-ui `fc4cc35` 674 → **`32abd58` 690**; variants `e89cd65` "1,521" → **census 1,762 it-blocks mid-flight (R25-W1 insert rescue + W2b landed; color-lib tests 167→170) — record-not-chase per the mock-verdicts §3 rule**. Add the D12 ratification note (engine DECISIONS.md:521 — the assembly ruling: the engine's stack IS the render/export venue model; one-directional junction) to §0's engine BASE row — the D-ARCH-6 fact that frames this spec's whole premise.

---

## §3 Focus-item verdicts (the six assigned questions)

1. **Color feasibility reconciliation.** 04's pipeline story is COHERENT with mock-color's verdicts — working format `rgba16float` (§4/§5.2/§16.3), compositing pipeline (§4's box + §7's renderFrame), tone map (§6.3/§6.4 — specced, mock-absent, the r3 parity fixture). The mock-color blocker ("the engine binding — rgba8unorm surface, zero instrument code, the wire trap") is carried by 04's §11.1/§13D (the 46-sites fact — **re-verified exact**) + §0 GAP rows 1-2 — but the r3 twin row never names the **8→10-bit correction** as the binding's substance (08 §0's row does; fix = Row 3). The conflicts with mock-color: **04:667's ×1023 shorthand** (vs 08 §18.2's exact per-scope law AND 04's own §11.13/§14.B — fix = Row 7) and **§16.5's measured-signal ambiguity** (fix = Row 9). The curve composition-order conflict (08:299 vs gradedFrame.ts:83-108) has ZERO 04 exposure (04 §8.3's curves row is order-silent) — 08's to fix.
2. **The OV-05 one-input-source law.** **07 is the right home** — determination made on structure, not just mock-color's say-so: 07 owns the transition model (§6.1A), the seam laws, and the element-anchored-field note (07:367, which currently documents the field + pairing + S3 laws but NOT the params-override convergence); 04's composition exposure is §7's FrameDescriptor consumption (the renderer consumes composition OUTPUT, not the sidecar/field law) + §0's N1 BASE row (zero transitionOut mentions in 04 — grep-verified). Engine ground truth: composition-frame.ts:39-48 (the OV-05 header law), :294-317 (elementTransitionOut — type discriminant "crossfade", ticks→seconds, finite/positive, the audio seam's hygiene), :331-334 (`params[el.id]?.transitionOut ?? elementTransitionOut(el)`), pinned ×3 at bridge-seams.test.ts:869/:886/:901. **04's only edit: the one-phrase pointer in the §0 N1 row (Row 1).**
3. **The projector parity corpus.** 04 promises "the formal parity suite green over the composition-frame family at the app's seam shapes" (S-engine ∥ crawl). Engine current: NO S4 suite, no corpus, no projector row anywhere (PLAN.md grep-verified); the N1 family's live pin surface = bridge-seams.test.ts (2,584 L / 151 `it(` / 30 describes — the N1 pair at :720/:926, W2.5 ×3, F2-6, F2-7, OV-05 ×3). The promise is accurately OPEN — but the engine's r1-port design round (HANDOFF #1) names NS-4 (SceneTracks→GPU) as an input, which may reshape the corpus's target contract (Row 5).
4. **The venue law.** 04 §4's "All grading math happens here" box (04:144-153) REMAINS COHERENT with mock-color's venue matrix (CPU bake + 1D/3D textures + compute scopes + fragment grade) — the box is the anchor the venue law cites. Nothing in 04 contradicts the matrix; what's missing is the explicit cross-link (mock-color §4 row 9's P1 — "neither spec assigns venues"). **Placement ruling: canonical venue table = 08 (§0 D-row or new §20, per mock-color §5's proposal); 04 §8 = the pointer paragraph + §4's box gains the CPU-bake qualifier (Row 6).** 04 should not duplicate the table (one-home law; the amendment wave applies Row 6 + 08's row together).
5. **The pre-R26 pins/line citations.** 13 §13D/engine cites checked live: 9 EXACT (compositor :1257/:33, device :79/:67/:59, lut :314, texture-pool :190, transitions/pipeline :116, effects/pipeline :12) + the two pass-discipline anchors (pipeline.ts:5204, mask-manager.ts:647-670) + the 46-site count + the 44-registry count all hold; **4 STALE clusters** (§0's :223/:161/113; GPU_EFFECT_TYPES :1509/:1515; §13D's :1797/:724/:1575 + the :1790/:1630 note; the header counts) — all refreshed in Rows 1/2/8/13.
6. **Cross-references spot-verified (11 checked).** VALID: 07 §4 FrameDescriptor (07:89); 18 #viewer-panel + §3.3 (18:63/:129); 12 §5 pixelmatch (12:300); 17 §6.1 (17:1078); 16 §3.11 (16:411); 00 D4/D5 (00:145/:158); 03 §14.D (03:1597); 11-cloud-render's readPixels contract (11:270/:326); spec-15's addEffect/toggleEffect (15:391/:395); OT's placeholder-compositor (names this spec, :13). **BROKEN: 04:2301's "spec 17 §6.3 interactive latency invariant"** (Row 10). **DRIFTED: 04:296's `MediaInfo`** (Row 11).

---

## §4 Cross-spec notes (for the cross-cut wave — not my edits)

- **07**: (a) the OV-05 law text belongs at 07 §6.1A + appended to the 07:367 note ("the sidecar `params[el.id].transitionOut` OVERRIDES; the engine field is the BASE — the second authoring home is closed"); (b) 07:13/:1169 carry the same stale "default 4" + ":223/:161" cites (the twin of Row 1) — 07's §0 also says "bridge-seams … 113" (now 151).
- **08**: (a) the canonical venue table's home (Row 6's other half); (b) 08:2135 has the same wrong "spec 17 §6.3" cite (Row 10's twin); (c) the curve composition-order conflict (08:299) + the A3 wheels gesture law + the node-graph model + the C50 sidecar ruling are 08-side P1s (mock-color §4 rows 1-4) — 04 has no exposure; (d) 08 §0's "167 dedicated tests" → 170 live.
- **09**: the `MediaRecord.colorInfo` naming (Row 11) — 04's §6.1 shape is field-identical, only the container name drifted.
- **17**: no interactive-latency invariant exists in §6.3 (it's Audio WYSIWYG); 17's perf thresholds are deliberately 50ms-not-33ms — any spec citing a 33ms invariant should cite 08 §2.
- **00**: §5 owns DegradedRendererBanner placement (04:494's bare mention gets the pointer, Row 12); 00-master's decision table (D4/D5) verified accurate for 04's premise.
- **Briefing facts check for 04**: #1/#2 (wire census) — no exposure (grep-verified: 04 has zero verb-count text); #3 (preservePitch) — no exposure; #6 (OV-05) — 07-side, pointer only (§3.2 above); #7 (WDC diagnostics) — not 04's surface (the audio engine's law); #10/#11 (mock-color/mock-verdicts) — the four 04-side findings are Rows 6/7/9 + the §16.4 consistency check (mock-color's 10-bit-canvas P2 risk cites 04 §16.4 — verified still accurate); #13 (D12) — Row 13's note; #14 (NS queue/r1-port map) — Row 5 registers the NS-4 slice.

## §5 What 04 uniquely sees (the fleet-only findings from this audit)

1. **The engine's 8-bit baseline is FROZEN-EXACT**: 46 `rgba8unorm` sites/10 files, count identical at `f9ac806` despite 458→748 tests and +2,273 LOC — the r3 binding is untouched ground; the same holds for the 44-effect registry (43+1). 04 is the only spec that pins these counts, and they still hold.
2. **The F2-5 default-radius flip (4→10)** — found nowhere else in this wave: the W2.5 BASE row's "default 4" is now a FALSE statement of the engine's pinnable law (the registry-coherence pin at bridge-seams:990 reads the declared default live). The only P1 in this spec.
3. **The Z2 landing + the engine's R9-c re-filing** — the D29.5c venue story moved on both sides (the app closed the export half; the engine re-opened the seam half as a proposal that preserves the math but moves the venue). 04 owns the twin row and must carry both movements.
4. **The NS-4/r1-port adjacency to the renderer contract** — the engine's next design round may put "a new compositor input contract over SceneTracks" (PLAN.md:475) behind this spec's §7 FrameDescriptor seam — the single biggest forward risk to 04's architecture section, invisible from any other spec's angle.
5. **The pass-discipline anchors survived the engine move** (`pipeline.ts:5204`, `mask-manager.ts:647-670`) — the Wave 4A fixes are stable citation bedrock for the amendment wave, while their §13D siblings moved.

*Report ends. Research-only: no repo files modified outside this report + the worklog; no commits/branches (watchdog respected).*
