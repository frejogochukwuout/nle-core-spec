# spec-05 — R27-W3 final-tightness audit (05-timeline.md)

**Task ID:** R27-W3-05 · **Agent:** spec-05 · **Date:** 2026-09-14
**Spec:** `nle-core-spec/05-timeline.md` (v-next, R24-preamble + R25 amendments) — audited section-by-section at sub-spec depth.
**Pins (ground truth, re-derived live this audit):** OT HEAD `55c81c0` / **code pin `970948a`** (632/632; wire census **31 = 28 routed + 3 exceptions**, read from `api.ts:243-275`/`:328-335`); engine `f9ac806` (timeline.ts **8,997 LOC**); app `c020b2a` (252/252; census register `docs/port-census.md` @ the `6e2b91a` mirror: **42 port files = 36 zero-action + 5 carriers + 1 host**); variants in-repo @ `a83d4d2` (**R25-W1 landed past the W5d wrap** — 64 files / 1,762 it-blocks re-counted); mini @ the R24-2 in-flight state (415 it-blocks / 10 files, re-counted). Spec repo @ the fleet pin.
**Method:** every 05 claim re-derived against the live trees (`/home/z/my-project/opencut-timeline`, `nle-engine`, `nle-test-app`, in-repo `ui-mock/*`); the W1/W2 mock reports (mock-trim/mock-variants/mock-verdicts/mock-insert) and scout-ot used as priors, re-verified where load-bearing. No file outside this report + the worklog modified.

---

## §2 The per-section verdict table

| § (lines) | Claim class | Verdict | Evidence (file:line @ pin) |
|---|---|---|---|
| §0 BASE OT row (:14) | pin + census + the exception enumeration | **P1** — the row names verbs that no longer exist and mis-classes one that does | The 6-name exception list (`timeline.{upsertKeyframe, removeKeyframe, retimeKeyframe}` etc.) is false in both directions at `970948a`: `upsertKeyframe` ROUTED (AR-2), `removeKeyframe`/`retimeKeyframe` RETIRED from the union (never-guard `api.ts:2537-2545`; absence pinned `milestones-ar1.ts:202-208`); census 31 = 28+3 (`WIRE_COMMAND_TYPES` `api.ts:243-275`, `WIRE_UI_EXCEPTIONS` `api.ts:328-335` — exported, tsc-asserted, NOT runner-local); pin `ded43c4`/`c15a629`/536 → `55c81c0`/`970948a`/**632** (report json @ `970948a`, 72 entries). [scout-ot #15 confirmed] |
| §0 canonical-tree census (:15) | census mirror | **P2** — every count stale | Live tree: **41 files / 9,176 LOC** (was 40/8,604); TimelineView **1,569L** (1,516), TimelineElementView **950L** (883), TimelineTrack **193L** (184); hooks = **22 files (21 hooks + selection-journal)** (was "20-hook family"), `use-wire-dispatch.ts` **95L** (90); the controllers dir = **11 files** (8 `*-controller.ts` + drop-target + drag-source + event-types; was "10 controllers"). Delta: NEW `hooks/use-keyframe-authoring.ts` (**262L**, AR-2 — the dblclick authoring seam) + the s16/s17 wire switches in use-keyframe-drag/use-timeline-actions/use-wire-dispatch. |
| §0 app-port rows (:16-17) | census mirror | **P2** — two re-reads behind; one resolved note | Live register (`docs/port-census.md`, D26.2's named doc — **exists now**, resolving :17's "verified absent R24 / D27-F17 filing action"): **42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + host** @ the `6e2b91a` mirror (05:16-17 carries 39 mirrors/32+7 → "R25 re-read 35+5"); the carrier list CHANGED — `use-element-interaction` + `TimelineToolbar` converged out; the 5 = TimelineView, use-timeline-actions, use-keybindings, TimelineContextMenu, use-playback-ticker. The lock-copy claim `vendor/nle-timeline @ c15a629 byte-exact` → mirrors **`6e2b91a`** (pre-D-ARCH-6); the `970948a` re-pin is the filed queue (`c020b2a`). |
| §0 law-register row (:18) | D22 drag law + mini counts | **SOUND** (one provenance note) | D22/R18k freeze intact (`shell-mini/docs/OT-SEAMS.md:34/:53/:73/:104`); mini = **415 it-blocks / 10 files** (re-counted — matches), LAW-NET Part A **98 families / 280 tests** still current (its own §:78). Note: 415 is the mid-R24-2 flight figure (briefing: 357→415, W0-W2 landed) — cite as in-flight per the 6-rule coordination law. |
| §0 C7 row (:24) | census | **P2** | "the 30-name wire census at `c15a629` = 24 UI-routed + 6 documented exceptions" → **31 = 28+3** @ `970948a`. The row's own D29-F8 clause ("re-declares mechanically per the tsc-lockstep law") fired — the row must carry the re-declared values + the lineage tail (24→28→30→24+6→25+5→**31=28+3**). |
| §0 DOM half (:30) | the frozen data-test census | **P3** — 68→69 by a selector reference | Live: **69 occurrences** of `data-test=` in `src/components/` (59 unique names; zero `data-testid` — that half holds). The +1 = a SECOND `data-test="timeline-bookmark"` **selector reference** in `hooks/use-keybindings.ts` (not a new DOM site). The app's mapping row state: still an OPEN PLAN checkbox (`nle-test-app/.agents/PLAN.md:66` "K3's DOM-structural half ⟸ the data-test mapping row"). Also :30's "the mini's 59-static + 15-templated census" — the mini now declares **60 static** (59 app-emitted + `mini-clip-harness` story-only; `shell-mini/docs/CORE-SEAMS.md:90`). |
| §1-§4, §5, §6 (purpose/hierarchy/zoom/virtualization) | contract | **SOUND** | Zoom ladder verified against OT `view/scale.ts:6` (`BASE…=50`), pixel-utils, zoom-utils — unchanged in the c15a629→970948a range (grep-verified); no contrary landings. |
| §7.2 Waveform (canvas) | law gap | **P2** — no per-track visibility law | The variants' W5d landed the flag→render gate + the batch: Clip's audio body reads the §4.7 per-track flag (`Clip.tsx:1067-1083` — `waveform === false` renders the flat gradient lane, no strip); `setAllTrackWaveforms` = ONE `withHistory` write converging every audio track (`useUiStore.ts:803` type / `:1737` impl — mock-variants' `:775/:1634` pins are themselves stale post-R25-W1). 05 §7.2/§12.2 state no display-state cross-ref. [mock-variants row 9] |
| §8.1 Tool modes (:341-352) | enum | **P2** — the stale enum §8A keys to | `type ToolMode = 'select' \| 'razor' \| 'hand' \| 'zoom'` (:344) is the pre-R15 four-tool world; §8A (:601) keys its register to "the trim tools of the tool-mode enum §8.1/§4 and 18 §4.5's tool radio share (roll/ripple/slip/slide)" — the enum contains none of them. Real homes: 15 §4.3.45's enum + **18 §4.5's nine-tool radio** (Select V/Razor B/Roll T/Ripple R/Slip Y/Slide U/Rate-stretch/Hand H/Zoom Z — `18-ui-shell.md:202`) + 16 §3.2's T/Y/U/R rows (:180-184). [mock-trim #22/N-7a] |
| §8.2-§8.4 (click/drag/trim) | drag law | **SOUND** | Threshold 5 strict `>` now at `element-interaction-controller.ts:53` (:176 the `>` test); coordinate-space injection unchanged; mixed A/V group rejection + zero-anchor (§14.5A) untouched in the range. The D22 windowed law coheres with OT's current controller (771 LOC — the s16 hardening grew it, the session machine is unchanged). |
| §8.5-§8.7 (razor/playhead/marquee) | contract | **SOUND** | No contrary landings; the razor split + playhead scrub + marquee families match the OT tree at spot depth. |
| §8.8 Drag-drop from library (:559-573) | commit path | **SOUND + P3 gap** | The SINGLE drop still commits `timeline.insert` (`drag-drop-controller.ts:392` → `insertAtTarget:319` → `use-timeline-drag-drop.ts:102` — verified) — §8.8's shape holds. The MULTI-insert is now the insertBatch seam: `view/page.tsx:535-599` — ONE `timeline.insertBatch` per KIND (kind-split F2) + staggered construction (F1: `startTime[i] = playhead + Σ durations[<i]`), one history entry per kind-group. 05 carries no multi-drop row (the §16.5A append row's vehicle note is the nearest cousin). |
| §8.9 Keyboard shortcuts (:575-597) | table | **P3** | The tool rows carry V/B/H/Z/R(+⌥R) but lack **T/Y/U** (roll/slip/slide — 16 §3.2 owns them); same stale-family as §8.1. |
| **§8A the register** (:599-634) — reference-figure + standing-law citations | pins | **SOUND** | All mock-HTML citations verified EXACT: trim `:469/:474` (green edges), `:529/:531` (ripple edge + dimmed inline-geometry neighbor), `:581/:590/:592-594/:596/:599-600` (slip family), `:651/:658/:662/:668-669/:672-677/:680-687` (slide family); insert `:265-266/:279/:305/:316/:325/:327-332/:453-459/:698-716`; the mini's hit-zone law `timeline.css:810-824` exact (incl. the PR69 C11 20px rationale). The z-stack, preview==commit, refusal-no-geometry standing laws hold. :632's "No production repo implements this register today" re-verified TRUE (OT still zero trim-family grammar; R25-W1 did not touch it). |
| §8A rows — interim-carrier citations (:607, :623, :624, :626) | pins | **P2** — the SECOND drift of the carrier line-pins | Live tree (post-`a83d4d2`, R25-W1 landed past the W5d wrap mock-variants audited): ghost `Timeline.tsx:1535`→**:1654**; speed badge `:1573`→**:1692**; arrows `:1654-1670`→**:1776/:1790**; hit zones `Clip.tsx:1359-1408`→**:1493-1510** (the 8px ±4px OUTSIDE law, R15 T4). `insertPlan.ts:83/:306/:498`, `insertPlan.test.ts:514-576`, `editModeIcons.tsx:141` all HOLD. Fix: convert to testid-keyed citations (`insert-preview-ghost` / `-arrow-down` / `-arrow-right-*` / `clip-trim-l/r-*`). [mock-variants row 12c + rec 5] |
| §8A `ghost` row (:624) — content | missing law | **P2** — the split-ghost + preview-visibility family uncarried | The W5d/R23-WE law (`Timeline.tsx:1185-1215`): the straddler's **SPLIT ghost** (dashed right half — `insertPlan.ts:97` `splitGhost?`, `:449` the ripple-split construction) **joins the rAF `scrollIntoView({inline:'nearest'})`** target (`previewGhostRef` + `previewSplitGhostRef`); plus the uncarried siblings: the **zoom floor** (ghost < 24px bumps pps, ONE bump per arm key), the mode-badge layer, and the refusal path **scrolls to the playhead**. Carried NOWHERE in the corpus (18 has no scrollIntoView law either — grep-verified). [mock-variants row 12a] |
| §8A `speed-badge` row (:626) | divergence note | **SOUND** | The one-decimal law stands; the mock still diverges (`Timeline.tsx:1702` `g.speed.toFixed(2)×` — re-verified) — the row's "registered divergence this row corrects" remains accurate. Only the carrier pin is stale (:1692). |
| §8A close-out rulings (:629-633) | completeness | **P3** | The ripple neighbor-width composite (view-ripple `:531`'s shifted-AND-narrowed neighbor = didactic push+pull; the single reading = rigid `from`-only shift) has no D33.4-style one-liner — mock-trim #8/F-8's cheap close. The compact-playhead z-under-sticky-badge (`TimelineCompact.tsx:266-272`, z-[1] under z-[2]) + Ruler t≈0 clamp (`Ruler.tsx:212/:284`) are register-row-class, not §8A rows — recorded as cross-notes only. |
| §9-§11 (snap/track headers/markers) | contract | **SOUND** | §11.1's per-scene markers + ruler-seeks, §11.2's setLoop-halves — cross-refs verify (09 §3.1A exists; 15 §4.3.29 `:1191` exists). |
| §12-§15 (multi-track/perf/test plan) | contract | **SOUND** | No contrary landings; §15's battery framing unchanged. |
| §14 (SCOUT-05 answers) | historical | **SOUND** | OC/FC citations are historical reference (era-qualified where R25 said so); §14.11's pointer-position slip/slide detection correctly cited by 18 §5A:328 (which itself notes its `05:972` anchor is pre-R25 — 05's §14.11 is now at :992; that's 18's stale note, not 05's). |
| §16.4 engine rows (:1216-1227) | line pins | **P2** — all four data-model anchors stale | `timeline.ts:2224` getLinkedClips → **:2569**; `:5546` addMarker → **:7708**; `:5642` setInPoint → **:7850**; headless `api.ts:804` case 'split' → **:830**; `scene-assembly.ts:210/:158` → **:215/:160**; `transform-resolver.ts:462` buildCssTransform → **:303**. (timeline.ts 8,997 LOC @ `f9ac806`.) |
| §16.5 OT rows (:1231-1260) | census + pins | **P1 (one row) + P2 (drifts)** | **P1 — the Headless-API row (:1260):** "30-type prefixed union — 24 UI-routed + 6 documented exceptions (`WIRE_COMMAND_TYPES` `:182-213`)" is FALSE: 31 types, union `api.ts:56-212`, `WIRE_COMMAND_TYPES` `:243-275`, the registry `WIRE_UI_EXCEPTIONS` `:328-335` **exported + tsc-asserted + gate-consumed LIVE** (the "exceptions are a runner-local registry" premise is dead since `40f22af`). **P2 drifts:** element-interaction `:51/:169/:201` (690 LOC) → `:53/:176/:208` (**771 LOC**); resize `:82` (302) → `:81` (**348**); playhead `:102` (312) → `:114` (**378**); zoom `:42` (157) → `:58` (**242**); timeline-core `:209` (986 LOC) → `:317` (**3,360 LOC**); `types/index.ts:95-99` → **:116**; api row `:41-150` → the surface moved (insert at :41 is now an import). SOUND: scale/pixel/zoom/ruler/layout/split/group-resize/group-move/placeholder-compositor/virtual-media anchors; the `TIMELINE_INDICATOR_LINE_WIDTH_PX = 2` known-delta note holds (`pixel-utils.ts:12`). |
| §16.5A op-port table (:1269-1283) | engine line pins | **P2** — the whole pin family stale (timeline.ts 7,502→8,997) | slip `:4143`→**:4956**; slide `:4246`→**:5097**; rollingTrim `:2984`→**:3324**; rateStretch `:3155`→**:3840**; rippleTrimItem `:2839`→**:3132**; `joinItems :7411-7427`→**:8817**; `removeItems :3757`→**:4507**; `_propagateRemovedInterval :3475`→**:4178** (renamed `…ToSyncLockedTracks`); `_propagateInsertedGap :3534`→**:4241** (same rename); `performOverwriteEdit :4860`→**:6007**; wave-2 `:7163/:6158/:7319` → `freezeFrameAtPosition :8652`, `closeGapAtPosition :7463`, `joinItems :8817` — and **no method named "retime" exists in the current timeline.ts** (zero case-insensitive hits; the family is `RetimeConfig`/keyframe-store/`updateElements{retime}` — the pin needs re-derivation, not re-pointing). HOLDS: `calculateSpeed core/timeline-math.ts:133` ✓; `ops/element-utils.ts:71-91` ✓. OT-side: `timeline-core.ts:886-911`→**:887+** (grown by the D-ARCH-6 shared `validateInsertElement` extraction; the intra-batch guard `:922-929`→**~:929-944**). |
| §16.5A append row (:1278) | verb-status note | **P3** | "riding the already-endorsed r1 `insertBatch` composite" — insertBatch **LANDED** (D-ARCH-6 `970948a`): one history entry, the shared validator, F1A-3 kind-split INVALID_PARAMS, intra-batch overlap CONFLICT whole-batch, empty = INVALID_PARAMS, + the library-layer seam (`view/page.tsx:535-599` staggered + kind-split; M49R inverted to the one-entry law). The row should record the landing (mock-insert's AP-1: the one-entry half satisfied, the append-point half not). |
| §16.6 / §17 / §18 / §19 / Testing | historical | **SOUND** | §16.6's skeleton-vs-citation coexistence ruling holds; §17/§18 era-qualified; §18 rows 19-20 (`tool-operation-overlay-utils.ts` `getSlipOperationBoundsVisual`) verify as the REF-only named geometry owner. |

---

## §3 The fix-list (exact edits, amendment-wave-ready)

**P1-1 (§0 :14 + §16.5 :1260) — the exception enumeration + census.** Rewrite both sites to: *"31 verbs = 28 UI-routed + 3 exceptions @ code pin `970948a` (HEAD `55c81c0`, 632/632; lineage 24 → 28 → 30 → 24+6 → 25+5 → **31 = 28+3**). Exceptions: `timeline.selectElements` (selection is VIEW state), `timeline.advancePlayhead` (ticker rAF-local), `timeline.trim` (uniform-delta engine shape; the UI's resize commits through `updateElements` patches). `upsertKeyframe` ROUTED (AR-2); `removeKeyframe`/`retimeKeyframe` RETIRED; `insertBatch`/`removeKeyframes`/`retimeKeyframes` LANDED (one history entry each; the shared `validateInsertElement`; M49C 28/3 machine-checked)." Cite `api.ts:243-275`/`:328-335`; delete the "runner-local registry" premise wherever it survives (§16.5's row note).

**P1-2 (§0 :15) — the tree census re-declaration.** *"41 files / 9,176 LOC (TimelineView 1,569L / TimelineElementView 950L / TimelineTrack 193L + the 22-file hook family incl. `use-keyframe-authoring.ts` (262L — the AR-2 dblclick authoring seam, the first wire command routed from below TimelineView); `use-wire-dispatch.ts` 95L; the controllers dir = 11 files core-side."*

**P1-3 (§0 :16-17) — the app census re-declaration.** *"42 port files = 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers (TimelineView, use-timeline-actions, use-keybindings, TimelineContextMenu, use-playback-ticker) + the EngineMount host, @ the `6e2b91a` mirror (docs/port-census.md — the D27-F17 named doc EXISTS, CI-gated; the `970948a` re-pin filed `c020b2a`)."* Retire the "verified absent R24" note; retire the 7-carrier list (use-element-interaction + TimelineToolbar converged).

**P2-4 (§8.1 :344) — the enum.** Either extend to the nine-tool reality (`'select' | 'razor' | 'roll' | 'ripple' | 'slip' | 'slide' | 'rateStretch' | 'hand' | 'zoom'` — 15 §4.3.45/18 §4.5's set) or re-point: *"ToolMode is 18 §4.5's nine-tool radio (15 §4.3.45's enum; 16 §3.2's keys) — the four-tool listing below is the pre-R15 seed shape, kept for the §4 hierarchy sketch only."* Add T/Y/U rows to §8.9's table (16 §3.2 :180-184). [mock-trim N-7a]

**P2-5 (§8A carrier citations :607/:623/:624/:626) — convert to testid keys.** Replace the line-pinned interim-carrier cites with: ghost → `` `insert-preview-ghost` (Timeline.tsx, testid-keyed; `insertPlan.ts:83` + `splitGhost` :97/:449; preview==commit `insertPlan.test.ts:514-576`) ``; speed badge → `insert-preview-speed-badge` (+ `editModeIcons.tsx:141`); arrows → `insert-preview-arrow-down` / `-arrow-right-*` (+ `insertPlan.ts:498`); hit zones → `clip-trim-l/r-*` (the 8px ±4px-outside law, Clip.tsx R15-T4 block). Line pins have drifted twice in two rounds; testids cannot. [mock-variants rec 5]

**P2-6 (§8A ghost row :624) — add the preview-visibility sub-laws.** Append to the row: *"Visibility (R23-WE ruling 20 + W5d): the straddler's SPLIT ghost (the dashed right half of a ripple-split placement) joins the main ghost as the rAF `scrollIntoView({inline:'nearest'})` target — the union span becomes visible, never a back-slide; a ghost narrower than 24px at the current pps bumps zoom once per arm (re-mints never creep); the refusal path (ok:false) scrolls to the PLAYHEAD instead."* Cite `Timeline.tsx` (the previewGhostRef/previewSplitGhostRef block) + `insertPlan.ts:97/:449`.

**P2-7 (§7.2) — the per-track waveform visibility law.** Add one paragraph: *"Per-track waveform visibility is a VIEW flag (`track.waveform`, default on) read at the clip-body render gate — false renders the flat audio lane (gradient + name), never an empty canvas; the batch convergence (`setAllTrackWaveforms`) is ONE `withHistory` write across every audio track (a converged no-change mints nothing). Display-state cross-ref: 18 §4.7's ViewOptionsPopover row; the height home rides 05 §12.2/C57."* [mock-variants row 9]

**P2-8 (§8 keyframe family — the missing surface).** Add one §8.10 (or a §8A-adjacent row): *"Keyframe gestures (AR-2): double-click on the volume line / expanded lane authors a keyframe at the frame-snapped click time (on-curve seed, dB-clamped, exact-tick = replace); the drag commits `retimeKeyframes` (delta-shaped, one history entry); delete commits `removeKeyframes` (the WYSIWYG bake — an emptied channel bakes the value-at-playhead into the base param)."* Cite `use-keyframe-authoring.ts` (262L), `use-keyframe-drag.ts:105-123`, `use-timeline-actions.ts:401-431`. [scout-ot #16]

**P2-9 (§16.4 + §16.5A) — the engine pin refresh.** Mechanical re-point at `f9ac806` (timeline.ts 8,997 LOC): the §16.4 six anchors (→ :2569/:7708/:7850/:830/:215+:160/:303); the §16.5A family (→ :4956/:5097/:3324/:3840/:3132/:8817/:4507/:4178/:4241/:6007; wave-2 → freezeFrameAtPosition :8652 / closeGapAtPosition :7463 / joinItems :8817) + a re-derived retime anchor (no such method exists — name the family: `RetimeConfig` + the wire's `updateElements{retime}` + keyframe-store). OT-side: timeline-core :887+ / guard ~:929-944. `calculateSpeed :133` and `element-utils.ts:71-91` need no edit.

**P2-10 (§16.5 table) — refresh the drifted OT rows** (element-interaction :53/:176/:208, 771 LOC; resize :81, 348; playhead :114, 378; zoom :58, 242; timeline-core :317, 3,360; types :116) + the C7 note in the §16.5 preamble (:1231 "the 30-name census" → 31 = 28+3).

**P3-11 (§16.5A append row) — record the landing:** insertBatch LANDED @ `970948a` (the shared validator, F1A-3, whole-batch overlap CONFLICT, empty = INVALID_PARAMS; the library seam `view/page.tsx:535-599` kind-split + staggered; M49R inverted) — the append row's remaining ask is only the 6th `PlacementStrategy {type:'append'}` + per-track append point.

**P3-12 (§0 :30) — the data-test census:** "(68 sites)" → "(69 occurrences / 59 unique names — the +1 since the freeze is a selector REFERENCE (`timeline-bookmark` in use-keybindings.ts), not a DOM site; the app's three-way mapping row is still an open K3 checkbox)"; the mini's "59-static" → "60 static (59 app-emitted)".

**P3-13 (§8A close-out) — the ripple composite one-liner:** *"view-ripple's narrowed neighbor (`:531`) is a didactic composite of push+pull; the single implementable reading is a rigid downstream shift (`from` only, duration/source window unchanged)."* [mock-trim F-8/N-7b]

**P3-14 (§16.5A fit-to-fill row) — note the carrier's floor divergence:** the ruled domain [0.1, 5] + refusal (mode-fit-to-fill :118 — the intersection law); the interim carrier enforces [0.01, 5] (`trimLaws.ts:32` RATE_MIN) — the 0.01–0.1 band is the documented engine-unsafe band; the citation should carry the divergence like the badge row does.

---

## §4 Cross-spec notes (for the cross-cut wave — NOT edits to other files)

1. **06** §0/§10.4 carries the same stale engine pin family (mock-trim F-23: `:2984/:2839/:4143/:4246` vs actual `:3324/:3132/:4956/:5097`) + the 24+6 census at six sites — 05's P1-1/P2-9 is the same edit family.
2. **15** §13.15's batch-verb rows + §4.1A's "24+6 / 27 of 78" readings need the flip (scout-ot #4-#8); 05's §16.5A append row cites the same verbs.
3. **19** (v3.2 §3.2 refresh block + §0) carries the same 40-files/8,604-LOC tree census + 24+6 — needs the P1-2 re-pin; it is the corpus's other census-mirror site.
4. **18** §4.7/§4.5: the ViewOptionsPopover contract + the per-track waveform flag law (mock-variants rows 8/9) — 05 §7.2's cross-ref (P2-7) rides it. 18 §5A:328's own "05:972 is the pre-R25 line" note is now doubly stale (05 §14.11 sits at :992) — fold at the 18 pass.
5. **00** §2A.7: the census-register law's app-side doc now exists (`docs/port-census.md`) — 05:17's "verified absent" note retirement (P1-3) should be mirrored in 00's D26.2 row if it carries one.
6. **The AR-2 consumption law** (briefing fact #12): 05's census rows are the natural site to record the app's live-registry consumption (the gate reads `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel) — one clause in P1-3.
7. **The register (REFERENCE-REGISTER.md)**: rows 1/14 re-key after the sibling's trim waves; row 4's compact-family description gains the playhead-z + ruler-clamp notes (my §8A P3 findings are register-row class, not 05 §8A rows).

---

## §5 What 05 uniquely sees

1. **The interim-carrier citation method has now failed twice in two rounds** (W5d moved the pins once, R25-W1 moved them again — :1535→:1654→ the tree will move a third time when the sibling's W2 trim wave lands). 05 §8A is the only spec carrying these pins; the testid-keyed conversion (P2-5) is the only durable fix and should be the fleet's pattern for mock-carrier citations.
2. **The engine has NO method named `retime`** — 05 §16.5A's wave-2 pin (`:7163`) cannot be re-pointed, only re-derived (the family is `RetimeConfig` + the wire's `updateElements{retime}` patches + keyframe-store). No other spec's audit surfaced this; the amendment wave must not blindly sed-replace.
3. **The data-test census moved by a REFERENCE, not a DOM site** (68→69; the delta is a selector in a hook) — the census method needs a unique-names basis (59) or it will drift on every internal grep-usage. The mini's own CORE-SEAMS:36 carries the same now-stale 68.
4. **05 owns the app-port census mirror** — the 42-file/36+5+1 re-declaration + the docs/port-census.md existence resolution land only here (and in 19's v3.2 block).
5. **The fit-to-fill rate-domain carrier divergence** ([0.01, 5] vs the ruled [0.1, 5]) is visible only from 05's §16.5A + the variants' trimLaws — the register's precision-law precedent (the badge row) is the model for carrying it.
6. **The W5d preview-visibility family (split ghost + scrollIntoView + zoom floor + refusal-scroll) is carried NOWHERE in the corpus** — 05 §8A's ghost row is the natural home and the only one that can prevent the r1 grammar port from re-forking it.

## §6 Battery hooks (battery_r27)

- Pin-lag class: `c15a629`/`ded43c4`/536 → `970948a`/`55c81c0`/632; app `c885ece` → `c020b2a`; engine `5036387` → `f9ac806` (05 §0/§16 carry all three).
- Census-coherence class: 24+6 → 31=28+3 (the lineage tail); the tree census 40/8,604 → 41/9,176; the app census 39-mirrors → 42-files.
- New check class worth one line: "the exception-registry export exists" (the §16.5 premise flip — the fleet-twin of scout-ot's proposal).
- The 632 count: cite total only (the in-page/real-mouse split convention changed shape — 452+30+150 per the report json).
