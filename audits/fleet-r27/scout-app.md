# scout-app — R27 final-tightness audit, Wave 1 (the app module card)

Task ID: R27-W1-APP · Agent: scout-app · Scope: nle-test-app movement R26 pin `3c91ffe` → HEAD `c020b2a`.
Research/report-only; every claim carries file:line or commit-hash evidence. Read-order assumed: 00-master, 15, 19, 09, 20, 02, 12, 17, IMPLEMENTATION-PLAN, ARCH-R27.

---

## §1 The commit-range ground truth (verified from code + the register)

**The range is 42 commits, not ~18** (`git rev-list --count 3c91ffe..c020b2a` = 42). `3c91ffe` (2026-09-10, docs-only: the maintainPitch residual scoped) sits before the F3 hardening wave, so the range spans F3 → H2-B → FW-B → RA → RC. `c020b2a` is docs-only (the D-ARCH-6 queue filings); the last code state is `9448667`.

### 1.1 Test-count lineage (commit gate lines + my own static census)

| Wave | Commit | Count (its own gate line) |
|---|---|---|
| R26 pin | `3c91ffe` | 206 (00-master:3 "nle-test-app `3c91ffe` 206") |
| F3 | `ee06732` area | 212/212 |
| H2-B | `44e8049` | 234/234 |
| FW-B | `052c196` | 242/242 |
| RA / RC re-pins | `8b0189f` / `ed33050` | 248/248 |
| AR-2 | `8a3eb35` | 249/249 "(8→9 in wire-coverage)" |
| M2 Wave 1 | `1aebbd5` | 251/251 (248 + 3) |
| fleet-freshness | `1c43897` | 252/252 "(251 + 1: the synced wdc surfaces grew a parametrized case)" |
| HEAD | `9448667` → `c020b2a` | 252/252 |

**Live static census at c020b2a (my count, `it/test(` per file): GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9 = 252.** The roof is now an **8-suite** family (R24's "6-suite roof" in specs 12/17 is two suites behind: wire-coverage + waveformPeaks both exist).

### 1.2 Vendor pins at c020b2a (`git ls-tree HEAD vendor/` + the lock files)

| Pin | Value | Evidence |
|---|---|---|
| vendor/nle-engine (submodule) | **`f9ac806`** | gitlink `160000 commit f9ac8067…`; `9448667` "engine 1064f91→f9ac806" |
| vendor/web-daw-core | **`ec8fd5c`** | gitlink; `9448667` "387f327→ec8fd5c (the S-series upstream wave)" |
| vendor/nle-ui | **`83ff8a8`** | gitlink; `d39d583` "4b0fcbe → 83ff8a8 (the FW-D coalescing CR)" |
| vendor/nle-timeline (lib tree) | **`6e2b91a`** | `vendor/nle-timeline/UPSTREAM.lock.json` upstreamHead |
| vendor/nle-timeline-ui (census reference) | **`6e2b91a`** | `vendor/nle-timeline-ui/UPSTREAM.lock.json` upstreamHead |

Engine lineage inside the range: `2501183` (`1890a3c`) → `f056f02` → `e7b13c9` (`1537eb7`) → `2fd68c6` (`ed33050`) → `1064f91` (`8b0189f`) → `480a216` (`1c43897`) → `f9ac806` (`9448667`). The `1c43897`/`9448667` tsconfig twin: `tsconfig.json:44-45` `"files": ["vendor/web-daw-core/src/types/spessasynth-core.d.ts"]` (the vendored ambient declaration — without it TS2307 + cascading TS2366 fire).

### 1.3 The census register, verified from the tree

`node scripts/census-check.mjs` (run live): *"census: 42 port files vs 41 reference files @ 6e2b91a; computed 6 byte-exact + 30 mechanical-exact (36 zero-action) + 5 carriers + 1 host; register-equality OK"*. The register (`docs/port-census.md:29-37`) declares exactly that: **42 files = 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers + 1 host**, pin `6e2b91a` derived from the reference lock (the H2-B pin-coherence law). The s16 delta (`ed33050`): `hooks/use-keyframe-authoring.ts` PORTED (new mechanical-exact twin — the reference grew to 41), `theme.ts` byte-exact → mechanical-exact (imports the ONE volume-dB law through the barrel).

**The 5 carriers NOW (port-census.md:43-47, the required rationale table):**
1. **`TimelineView.tsx`** — D25 selection/zoom bridges (seed + mirror the shell store), the R3 `confirmDelete` multi-delete confirm gate (≥5 elements), the D29 W2.2 `EFFECT_DRAG_MIME` effects-drop layer, `EMPTY_CONTEXT_ITEMS`, the `useCommittedRef` import. NOT divergent since FW-E2 (the a25d199 HA-3-2 dispatch injection + HA-3-3 selection prune absorbed upstream-verbatim) and since `ed33050` (the 6e2b91a AR-2 `dispatch={dispatch}` pass to TimelineTrackRow absorbed port-verbatim — law 82: the authoring seam must not go dormant).
2. **`hooks/use-timeline-actions.ts`** — the R3 confirm-gate wiring (deletes route through `confirmDelete`) + the S-round `shuttle(direction, fixedRate)` rows (⇧L/⇧J fixed-2×, spec 16's law).
3. **`hooks/use-keybindings.ts`** — the R3 keymap surgery (z/y removal + the `[data-modal]` guard) + the ⇧L/⇧J rows; the e3ecd54 `SURFACE_OWNED_KEYS` yield law is ABSORBED upstream-verbatim (FW-E3, `89d26d6`) with ONE port-local member (the Z4 `[data-transport]` row, D30 W-D).
4. **`TimelineContextMenu.tsx`** — the `setPos` same-reference functional-update guard (the bf9be13 CI-red class; the file's ONLY divergence).
5. **`hooks/use-playback-ticker.ts`** — the S1 engine-owns-transport doc note + the `TICKS_PER_SECOND` import coalescing (order-only).

### 1.4 AR-2 (`8a3eb35`) — the live-registry consumption + the gesture pin

- **The law**: `src/wire-coverage.test.tsx:29` imports `WIRE_COMMAND_TYPES, WIRE_UI_EXCEPTION_VERBS` from `@vendor/timeline`; `:49-59` — *"The exception registry — LIVE from the vendored barrel (RC-V1's P2 fix, 2026-09-13): upstream's AR-1 (D-ARCH-3) exports `WIRE_UI_EXCEPTIONS` as a tsc-asserted seam constant + the derived `WIRE_UI_EXCEPTION_VERBS` Set — this gate now consumes the LIVE registry instead of a hand-mirrored copy (the mirror went stale at the 6e2b91a re-pin: upstream removed timeline.upsertKeyframe when its UI coverage landed — the AR-2 reconciliation law, 6→5 — while this test still excepted it, leaving the authoring seam UNFENCED app-side: deleting the dispatch pass would stay green)"*; `:59` `const WIRE_UI_EXCEPTIONS: ReadonlySet<string> = WIRE_UI_EXCEPTION_VERBS;`.
- **The gesture pin**: `wire-coverage.test.tsx:328-377` — the REAL volume-line double-click through the mounted GluedShell/EngineMount: `[data-test="audio-volume-line-hit"]` dblclick → the wire log's `timeline.upsertKeyframe` entry with `origin: 'ui'` (`:359`, the honest-gate law), `params.propertyPath === 'volume'` (`:362`), the element id, the on-curve/seed value in [−60,+20], the frame-snapped tick, and the coverage-union assertion (`:376`). Proven discriminating: `dispatch={undefined}` → the pin fails loud (commit message).
- **The barrel truth at the app pin** (`vendor/nle-timeline/headless/api.ts`): `WIRE_COMMAND_TYPES` = 30 names (`:197-228`); `WIRE_UI_EXCEPTIONS` = **5** verbs — selectElements, removeKeyframe, retimeKeyframe, advancePlayhead, trim (`:283-294`; upsertKeyframe REMOVED); `WIRE_UI_EXCEPTION_VERBS` the derived Set (`:315-317`); tsc-lockstep asserts both directions (`:230-239`, `:300-311`). So at the app's pin the split is **25 UI-covered + 5 exceptions** (the spec corpus's "24 routed + 6 exceptions" is one step behind; OT HEAD's s17 state is 31 types / 3 exceptions per ARCH-R27:27).

### 1.5 M2 Wave 1, app side (`1aebbd5`)

- **F-1 the bus fold REPLACEMENT**: `src/audioService.ts` `toMixerSceneSettings()` buses now `{ index, name, volume: faderToLinear(aN.returnGain), muted: !aN.on }` — volume travels UNfolded; the R5-amendment-11 app-side on→0 fold is dead; the engine's `applyAuxBusGain` owns mute→0 + clamp at the WRITE site. Pins: the D24c conversion-law bus section re-pinned + the live-toggle pin (one verb, one field).
- **F-2 the single-owner sidechain law**: `rewireSidechains()` + `wiredSidechainSources` DELETED from audioService.ts (the replacement comment at the old site: the BRIDGE — `SceneMixer.materializeStrips` — collects + connects the duck wires through the shared `collectSidechainWires` contract with a per-wire identity diff; realtime + renderOffline share ONE wiring law — "the duck the user hears is the duck the user exports").
- **§1a the flush-args fix**: `src/deliverService.ts:170` `WORKLET_BACKED_INSERT_TYPES` (compressor/gate/deesser + registry-reachable limiter), `:177` `sceneHasWorkletBackedInserts()`, **`:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0`** — the old "offline path skips worklet-backed inserts" premise is dead.
- **The worklet VENUE fix**: `vite.config.ts:28-60` the `serve-vendored-worklets` plugin (dev middleware + `closeBundle` dist copy of `vendor/web-daw-core/public/worklets` — zero byte duplication; `dist/worklets/` carries the three files post-build). Previously NOTHING served `/worklets/dsp-effects-worklet.js` (publicDir is nle-ui's, media only) — every worklet-backed insert silently degraded to PASSTHROUGH, monitor AND export.
- **The CI symlink fix** (`57ba1ca`): the waveform grep-walk in `src/waveformPeaks.test.ts` is symlink-safe (lstat, skip links) — the `8b0189f` reinstall's node_modules symlink red, root-caused.

### 1.6 The maintainPitch engineService residual — RESOLVED at c020b2a

- Edit path: `src/engineService.ts:480-513` — the retime read-merge-write; `:510-513` `maintainPitch = cmd.patch.preservePitch !== undefined ? cmd.patch.preservePitch : f.el.retime?.maintainPitch ?? true` (the hardcode is gone; the retimable-type gate RR1-B-6 at `:488`).
- Load path: `src/sceneBridge.ts:253-265` — `retime: { rate: clamp, maintainPitch: el.preservePitch !== false }` (absent = true, the NLE default).
- Back-projection: `src/sceneBridge.ts:441-449` — `...(el.retime.maintainPitch === false ? { preservePitch: false } : {})` (true/absent project NO doc field).
- Pins: `src/GluedShell.test.tsx:700` (R9-b end-to-end incl. REAL rebuild survival — `d8d1b2b` made the rebuild leg REAL) and `:761` (the Inspector's REAL Preserve-pitch button clicked — `2de34ac`, the `2de34ac`/`d8d1b2b` "R9-b round-trip pins" of the task brief). The prerequisite nle-ui CR landed at `af32e69` (ElementJSON + patch widening + the toggle), consumed at `d39d583`.
- **BUT the app's own queue doc is stale**: `.agents/PLAN.md:47-52` still lists "maintainPitch threading … `engineService.ts:477/:486` + `sceneBridge.ts:212` hardcode + the `:384-389` back-projection hole; ⟸ nle-ui CR" as an OPEN row — last edited `7fd3eb6` (the .agents bootstrap, before the H2-B wave); `c4f8d7d` updated only the H2-B + audio-pins rows. The residual is CLOSED in code; the row was never ticked.

---

## §2 The current queue state (what the app's own docs say is next)

`HANDOFF.md` is H2-B-stale (234/234; "M2 Wave 1 + the re-pin freshness next" — both done). The live truth is `.agents/PLAN.md` (last touched `c020b2a`) + the worklog tail (the RC-app entry, ends `8a3eb35`; the last 3 commits have no worklog entry).

**The standing queue (PLAN.md:45-93, priority order)**: the (stale) maintainPitch row · **K3's store/policy halves** (322 authored + 33 projection pins over the real vendored OT — NOT started) · the mode-matrix K3 rows (slip-G-SLIP-7, ripple-trim-G4, ROLL-7, slip-G-SLIP-6) · the compensating shell-path pin family (⌘B/⌘M/⌘⇧M/`,`/`.`) · K3's DOM-structural half (⟸ the D26.5 data-test mapping row) · K3's audio pins + VLM net (the audio half DONE @ `44e8049` — `src/waveformPeaks.ts` consumes the EXPORTED contract names; the VLM net still open) · K4 e2e · w1→w3 · the F3-NEW residual list (closed by FW-B except as noted) · the CR list (setElementFieldAll — the coalescing landed at nle-ui `83ff8a8`, the flip pin landed `d39d583`; the ElementJSON maintainPitch widening — LANDED `af32e69`; the DeliverPage blob contract — still open).

**The D-ARCH-6 filings (PLAN.md:104-147, filed from OT's s17 session — the cross-project queueing protocol):**
1. **The timeline vendor re-pin + the port's gesture-seam switch** (the gate-breaking duty): re-pin to the s17 tip; the port carries the SAME direct-core gesture seams the upstream switched — **`src/timeline-port/hooks/use-timeline-actions.ts:452` `core.removeElementKeyframes` + `src/timeline-port/hooks/use-keyframe-drag.ts:96` `core.retimeElementKeyframes`** (both verified live in the tree) — the ported coverage gate will FAIL against the 31-verb/3-exception registry until they switch to the batch wire verbs (`removeKeyframes`/`retimeKeyframes` origin "ui"; upstream's use-keyframe-drag.ts takes an OPTIONAL `dispatch` prop — "pass it and the switch is one line"). Acceptance: the ported gate green against the live export; the singular verbs absent from the vendored WIRE_COMMAND_TYPES.
2. **The pool multi-insert rides `insertBatch`** (the queued `docs/design-r9-d30.md:210/269` design): ONE `timeline.insertBatch` per KIND (the spec 15 §13.15 kind-split rule), the STAGGERED construction `startTime[i] = base + Σ durations[<i]`, firstAvailable, one history entry per kind-group. Acceptance: the app's pool insert pins mirror upstream's M49R pair.
3. **The nle-ui re-pin absorbs the AW1-2 ceiling fix** (nle-ui `32abd58`: the Inspector Gain max +4 → +20, the fleet [−60,+20] domain).
   ⚠️ Filing nit: item 1's baseline reads "a25d199 → the s17 tip" but the register's actual pin is **6e2b91a** (the s16 re-vendor, `ed33050`); the s17 tip is `55c81c0` per ARCH-R27:27.

**Also queued**: G2 the async pre-retime (engine-owned design first — HANDOFF item 3); M2 Waves 2-3 (the reverb/authoring surfaces + PDC); the RC worklog's registered next-round item — "the transitionOut sidecar drop (the engine field is now the base — the app's legacy bag is a redundant override)" (app worklog.md:697).

---

## §3 The spec-impact register

| spec §site | current text (quote) | new truth (file:line evidence) | sev | proposed amendment |
|---|---|---|---|---|
| **00-master:3** (status v11.0 fleet ledger) | "nle-test-app `3c91ffe` 206 (the census-CI + folds + the maintainPitch scoping; the WDC consumer pin bumped to `387f327`)" + the census-CI record "(41 port files = 35 zero-action (7 byte-exact + 28 mechanical-exact) + 5 carriers-with-rationales + the host…)" + consumer pins "app→engine `5036387` (the E1/E2 bump rides the K3 wave) + OT lock-copy `c15a629`… nle-ui `3026099` + WDC `387f327`" | App @ **c020b2a 252/252** (§1.1; static census matches); census **42 = 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers + 1 host** @ `6e2b91a` (port-census.md:29-37 + census-check live run); pins **engine `f9ac806` + OT mirrors `6e2b91a` + nle-ui `83ff8a8` + WDC `ec8fd5c`** (§1.2 gitlinks/locks) | **P1** | Re-pin the fleet row + the census-CI record + the consumer pins; fold the RA/RC deliverables (AR-2 fence, M2 Wave 1, the 8-suite roof) |
| **00-master:19** (the repo-map app row) | "**206/206 — LIVE-run (the R24 static census was 174… the port census 41 = 35 zero-action + 5 documented carriers + host, REGISTER-EQUALITY CI-ENFORCED)… consumes engine @ `5036387` + the OT lock-copy @ `c15a629`… + nle-ui @ `3026099` + WDC @ `387f327` (re-pinned R26) @ `3c91ffe`" | Same as above: 252/252, census 42, pins f9ac806/6e2b91a/83ff8a8/ec8fd5c; the roof is 8 suites; the AR-2 authoring gesture + the M2 bus-law/sidechain-parity landed | **P1** | Same re-pin; optionally register the AR-2 gesture pin + the live-registry law in the row's evidence |
| **00-master:467** (Decision 36.2, D36.2) | "R9-b maintainPitch → LANDED engine-side (the residual is the app's engineService hardcode — re-filed app-side)" | The app-side residual is **LANDED** (`94d5a2e` threading + `d39d583`/`2de34ac` UI legs; engineService.ts:510-513 read-merge-write; sceneBridge.ts:262/:447) | P2 | Fold the R26 execution note for 36.2's R9-b half (mirror the E1/E2 fold pattern already used in that same decision text) |
| **00-master:477** (§2A.7 census law, R26 note) | "(the register (41 = 35 zero-action (7 byte-exact + 28 mechanical-exact) + 5 carriers… The prior 'currently 39 mirrors' reading is the R24 record — the R26 register declares 40 mirrors + host at the D30 irreducible set.)" | The register re-declared at `6e2b91a`: **42 port files = 41 mirrors + host** (36 zero-action + 5 carriers); `use-keyframe-authoring.ts` the new twin; theme byte→mechanical | P2 | Append the R27 re-declare note (the law itself — register-equality, CI-enforced — is unchanged and HELD: census-check green live) |
| **05:17** (the REQUIRED spec-side census mirror row) | "at `c15a629`: 40 port files = 39 mirrors (32 zero-action + 7 carriers) + the host; `use-wire-dispatch` pending W-C *(the R24-declared values; R25 re-read @ the app pin `64fb0ab`: W-C landed, the set is 35 zero-action + 5 carriers + host — **the next register amendment carries the re-declared values})*" | The next register amendment HAPPENED (`ed33050`, 6e2b91a): 42 = 36 zero-action + 5 carriers + host — the spec-side mirror row is now a full amendment behind its own promised re-declare | **P1** | Re-declare: 42 port files @ `6e2b91a` (6 byte + 30 mechanical + 5 carriers + host); keep the promise-cycle (amend at every pin bump) |
| **05:16** (the app BASE row) | "nle-test-app @ `c885ece` — 174/174… the port's 40 files = 39 mirrors of OT's 40… 7 documented carriers" | R24-era: three pins + ~78 tests behind (§1.1/§1.3) | P2 | Re-pin with the §1 evidence |
| **15:14** (the BASE census + the test-local note) | "the census RE-BASED to the W11 truth (D29.2): 24 UI-routed verbs + 6 documented exceptions (the 30-name census split at `c15a629`)… **NOTE…: the exception registry is TEST-LOCAL upstream — OT exports NO exception constant**" | At the app's own pin `6e2b91a`: `WIRE_UI_EXCEPTIONS` is a **first-class exported seam constant** (api.ts:283, AR-1/D-ARCH-3) + the derived `WIRE_UI_EXCEPTION_VERBS` Set (api.ts:315) + tsc-lockstep asserts; exceptions 6→**5** (upsertKeyframe removed when its UI coverage landed — the AR-2 reconciliation); at OT HEAD (s17): 31 types / 3 exceptions | **P1** | Strike the TEST-LOCAL note; re-base the census to the 6e2b91a consumer truth (25 covered + 5 exceptions) + register the owner-HEAD state (31/3) with the consumer re-pin duty (D-ARCH-6 item 1) |
| **15:30** (the R8 coverage-gate row) | "the final assertion is `accumulated ⊇ WIRE_COMMAND_TYPES − WIRE_UI_EXCEPTIONS` (**the exception registry re-declared app-side, TEST-LOCAL — upstream exports no exception constant; drift is tsc-enforced at the re-exported constant**)… *(R25 status: LANDED… 206/206 live-run; the row is history.)*" | The gate consumes the **LIVE registry** (wire-coverage.test.tsx:29/:49-59, `8a3eb35`) — the hand-mirrored copy went stale at exactly the re-pin the spec said it wouldn't survive; 252/252; the AR-2 gesture pin (volume-line dblclick → `timeline.upsertKeyframe` origin 'ui') is the keyframe-authoring leg the row never had | **P1** | Amend R8: the gate consumes the exported registry (never a copy) + register the AR-2 gesture pin. **The general law is worth its own 00-master §2A row: "a consumer gate consumes the exported registry, never a hand-mirrored copy"** — the same register-law class as 2A.7, cross-repo (any consumer of any exported constant) |
| **15:4905** (the C7 corrective row) | "the 6 exceptions (the runner's registry, `scripts/run-timeline-tests.mjs:283-300` — test-local upstream, no exported constant…)" | Same as 15:14: exported since AR-1; 5 at the consumer pin, 3 at the owner HEAD | P2 | Fold with the 15:14 amendment; the C7 rename worklist count needs the 31-name refresh at the s17 tip |
| **15:4915 + 15:4916 + 15:21** (batch-keyframe verbs + `timeline.insertBatch` — QUEUED r1-adjacent) | "the wire exposes only the singular verbs… the batch wire forms collapse N singular commands → 1 history entry | **QUEUED (r1-adjacent)**" / "the library multi-insert currently runs `applyBatch([insert × N])`… | **QUEUED (r1-adjacent)**" | **LANDED at the owner** (OT s17, `ab7727c..5f8b6d9`: `timeline.insertBatch`/`removeKeyframes`/`retimeKeyframes`, one history entry each; the singular verbs RETIRED — WIRE_COMMAND_TYPES 30→31, WIRE_UI_EXCEPTIONS 5→3; 632/632 + M49C 28-routed/3-exceptions per ARCH-R27:27 + the app PLAN.md:108-114 filing) | **P1** | Flip both rows QUEUED → LANDED-at-owner with the consumer-lag record (the app's re-pin + gesture-seam switch = the D-ARCH-6 duty; the pool multi-insert design rides it); the census re-declare is mechanical per the tsc-lockstep law as written |
| **09:337 + 09:177** (B2, the ElementJSON audio fields) | "`ElementJSON.pan?: number` (−100..100) and `preservePitch?: boolean` join `volume`; volume's persisted unit is LINEAR 0..1…" (R15 text, nothing since) | The **R9-b model ruling is NOT in the spec**: preservePitch is the retime's COMPANION — **ABSENT = true** (the NLE default); explicit false = pitch-affected varispeed; the doc field is a PROJECTION of the engine retime (the engine field is the SSOT; back-projection false-only); the identity law (a dormant false at rate 1 drops at reload — the load-bridge identity law). Evidence: sceneBridge.ts:228-265/:435-449, engineService.ts:498-513, GluedShell.test.tsx:700-760, `94d5a2e` | **P1** | Add the R9-b ruling to §7.2 B2 (an amendment marker in the established pattern): the default, the projection semantics, the identity law — one home in 09, consumed by engine + app + nle-ui |
| **02:17** (the app row, workers/threading) | "nle-test-app @ `c885ece` — 174/174… the W2.4 deliver export running the engine's main-thread offline mixdown — **whose offline path SKIPS worklet-backed inserts entirely (`deliverService.ts`: 'no worklet-backed insert effects in the offline path')**, the K3 row's offline-side cost surface" | The premise DIED: `deliverService.ts:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0` (the §1a flush-args fix) + the vite worklet-venue plugin serves the worklets (vite.config.ts:28-60) — worklet-backed inserts no longer skip OR silently passthrough offline; and the pin/count is 3 states behind | **P1** | Re-write the app row's deliver leg: flush > 0 when worklet-backed inserts present; the venue law (the app serves the vendored worklets at their expected URL, zero byte duplication); re-pin to c020b2a/252 |
| **20:17** (the app's WDC audio host row) | "@ nle-test-app `c885ece` — 174/174… **U9 sidechain ducking**… `rewireSidechains()` after every `materializeStrips` — sever-all zero-arg + `collectSidechainWires`, the shared contract, **the helper's ONE live consumer**" | `rewireSidechains` is **DELETED** (`1aebbd5` F-2): the BRIDGE (`SceneMixer.materializeStrips`) owns the sidechain wires on BOTH paths (single-owner law, per-wire identity diff); the bus on/off toggle maps to the bridge's `muted` field (F-1 — the engine's `applyAuxBusGain` owns mute→0 + clamp; the R5-amendment-11 app-side fold dead); pin/count stale | **P1** | Re-write the U9 evidence (the tap's connection graph is the witness, venue-agnostic to who wired it) + the bus-law fold; re-pin. Also **20:20/145/151** ("missing = the NLE-scene→strip wiring" / "the helper's one live consumer is the APP's U9 duck-wire rewire") — M2 Wave 1 landed the wiring (bridge-side); the M2-REMAINDER rows need the Wave-1 fold to BASE + the Waves 2-3 remainder re-split |
| **19:5 + 19:13 + 19:18 + 19:216 (§3.3C)** | Status/§0 BASE/§0 GAP D26 row/app row: "app `c885ece` 174/174"; census "40 files = 39 mirrors… 32 zero-action + 7 documented carriers… `use-wire-dispatch` pending W-C"; §3.3C "The app has NO `.agents/` yet" | `use-wire-dispatch` is a zero-action mechanical-exact mirror since W-C/s16; census 42 = 36+5+host; `.agents/` landed `7fd3eb6` (R26); app 252 @ c020b2a; the vendoring record (engine `5036387`/nle-timeline `c15a629`/nle-ui `fc4cc35`/WDC `85b81b0`) is 4 pins stale | **P1** (19 is the code-reference canon) | Full app re-baseline: pins, count, census arithmetic, the `.agents/` fact, the AR-2/M2 movement |
| **12:14 + 12:36; 17:4 + 17:20 + 17:39 + 17:2359 + 17:2730** | The R24-pins blocks: "app 174/174 @ `c885ece` — the 6-suite roof: GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7" (repeated in the §17A roof-suite law) | 252 @ c020b2a, **8 suites**: GluedShell 104 + audioService 42 + sceneBridge 41 + deliver 20 + persistence 20 + **wire-coverage 9** + engineSeam 7 + **waveformPeaks 9** (static census §1.1); 17:39's K3 instrument row predates the AR-2 gesture pin | P2 | Re-baseline the roof-suite inventory + the K1 pins in both files; register the AR-2 gesture pin under 17's K3 instrument row (the routed-verb completeness machine-check's keyframe-authoring leg) — currently registered NOWHERE in the spec corpus |
| **IMPLEMENTATION-PLAN:19 (S-app) + :33 (R3 verify) + :85** | "**land the maintainPitch residual HERE** (`engineService.ts:477/:486` hardcodes `maintainPitch: true` — the Inspector Preserve-pitch toggle is dead app-side…)"; "the census at its declared irreducible set **35 zero-action + 5 carriers + host**"; "the register (41 = 35 zero-action (7 byte-exact + 28 mechanical-exact)…)"; "206/206" | The residual LANDED (94d5a2e + the 2de34ac/d8d1b2b legs); census 42 = 36+5+host; 252 tests (§1.1/§1.3/§1.6) | **P1** (the plan drives work — a stale "land it here" row invites a duplicate landing) | Tick the maintainPitch item DONE with the commit evidence; re-base the S-app row's counts; add the D-ARCH-6 filings as the new S-app head |
| **battery_r26.py:490-491** | `check("LIVE: the app repo HEAD is 3c91ffe…")` | HEAD is c020b2a — the check fails (the known pin-lag class; ARCH-R27's "battery_r26 136/141 = pure pin-lag") | P2 | battery_r27 re-bases the app class to c020b2a/252 + the new census arithmetic + the register pin 6e2b91a |

Not stale (verified held): the §2A.7 register-equality LAW itself (census-check + mutation gate 8/8 green live); 02:302's worklet census (the venue fix adds no worklet code — it serves WDC's existing 3 files); 20:104/106's send/return + authoring-contract laws (the F-1 fold is their enforcement, not their contradiction); the D24c conversion law (re-pinned inside 1aebbd5, not spec-side).

---

## §4 Open questions

1. **The D-ARCH-6 re-pin baseline**: PLAN.md:117 says "a25d199 → the s17 tip" but the register's pin is `6e2b91a` (s16) — confirm the intended target is `55c81c0` (ARCH-R27's OT HEAD) and restate the baseline when the filing is executed; the spec-side census row should then declare the consumer truth (6e2b91a: 25 covered / 5 exceptions) AND the owner truth (s17: 28 routed / 3 exceptions) with the re-pin duty named.
2. **The live-registry law's home**: fold into 15 §R8 only, or promote to a 00-master §2A standing law ("a consumer gate consumes the exported registry, never a copy")? It generalizes beyond OT (the app's `waveformPeaks.ts` already follows the same pattern against WDC's exported contract — 44e8049; the census register's own law 2A.7 is the same class). Recommend the standing law + the R8 fold.
3. **The R9-b ruling's spec home**: 09 §7.2 B2 (recommended, the field's home) vs a 06 retime-section cross-cite. The ruling is consumed by three repos (engine scene-to-segments, app bridge/service, nle-ui ElementJSON) — one canonical home needed before the corpus drifts further.
4. **M2's split**: spec 20's M2-REMAINDER rows predate Wave 1. Should R27 fold Wave 1 (bus-law + sidechain-parity + the flush law + the venue law) to BASE and re-split the remainder as Waves 2-3 (reverb/authoring surfaces + PDC), or hold the fold until the D-ARCH-6-era re-pin lands the engine's m2 surfaces at the app? The app's own design doc (design-m2-mixer-surface.md v1.1) is the wave map.
5. **The app's PLAN.md hygiene**: the stale maintainPitch row (PLAN.md:47-52, untouched since 7fd3eb6) and the HANDOFF's H2-B-era state are app-internal, but the spec round's cross-repo filings ride those docs — flag to the S-app session to tick the row (evidence: this report §1.6) rather than re-file it.
6. **K3's store/policy halves remain the S-app head** (unstarted at c020b2a) — confirm the R27 plan keeps them behind the D-ARCH-6 re-pin (the gate-breaking duty must land first or the ported gate goes red at the re-pin), per the PLAN's own ordering.
