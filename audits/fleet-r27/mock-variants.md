# R27-W2 — The shell-variants R24-wrap delta harvest (mock-variants)

**Task ID:** R27-W2-VARIANTS · **Agent:** mock-variants · **Date:** 2026-09-14
**Scope:** the sibling stream's R24-variants full-audit round (W0–W5d, reconstructed, wrapped at `3db130f`) + the two round docs already in the spec repo (`.agents/design/r24-audit-round.md` v2-RECON, `.agents/design/r25-reaction-round.md` v2-FINAL). Research-only: no spec/mock file modified; every claim carries file:line or commit evidence.
**Tree state audited:** the spec repo's `ui-mock/shell-variants` @ main (HEAD carries the sibling's W5d `b9cd387`; the wrap `3db130f` is docs-only). Live census re-derived: **64 test files / 1,733 it-blocks** (line-start method; the sibling's wrap declares 1,734 by its own method — `3db130f`). **No R25-variant wave has landed in code** (verified: `git log --oneline --since=2026-09-11 -- ui-mock/shell-variants/src/` → the last src commit is W5d `b9cd387`); the R25 A1/A2/A3 verdicts are therefore **round-doc law, pre-implementation** — harvested as LAW, not as code.

---

## §1 The round docs' verdict extraction

**IMPORTANT disambiguation:** the two round docs REUSE the labels A1/A2/A3 for different topics. DESIGN-R24 §1 (`.agents/design/r24-audit-round.md:70-196`) = transitions-DnD / color-composition / mixer-toolbar-deliver. DESIGN-R25 §6 (`.agents/design/r25-reaction-round.md:178-213`, commit `a85c051`) = source-transport / color-layout / **wheels**. Both sets extracted below, labeled R24-A* / R25-A*.

### 1.1 DESIGN-R24 §1 (the research verdicts that drove W0–W4)

**R24-A1 — transitions & DnD (#58/#59)** [r24-audit-round.md:72-104]:
- **One transition per seam, drag-to-replace** — FCP/Premiere/Resolve unanimous canon; presentation+type swap **retains duration and alignment** (the R23 default-duration overwrite dies).
- Click on an occupied seam = **select-to-edit** — never a dialog (no NLE uses dialogs here).
- No data-model change: `transitionOut` stays a single object per clip; effects stay a stack (duplicate OFX instances legal).
- The **routing table** (frozen `application/x-nle-effect` row): empty seam → new mint; occupied seam → REPLACE (identical presentation short-circuits BEFORE `setTransition` with an "Already X" toast); clip body → OUT seam (transition rows) / effect stack (×N toast) / `setFade` (fade rows); transition row on a clip with no butt-spliced neighbor → refusal toast (1ms epsilon); **empty lane → pure no-op** (no highlight, no toast, no commit).
- Drag-over visuals on the SeamZone (HTML5 drag never fires hover): 24px zone + `--transition-mark` 26% fill + '+' on empty seams; the occupied seam's TransitionBox widens to the 24px drop floor + swaps the glyph to ⇄.
- **Double-click a browser row** = apply to the single selected clip; zero/multi selection = honest "Select one clip" toast.

**R24-A2 — color composition (#67–#70)** [r24-audit-round.md:106-147]:
- **GREP-VERIFIED: ZERO curves-editor DOM in all five reference HTMLs** — the Curves panel is rebuilt from Resolve research (BMD: per-channel custom curves + live histogram; the histogram overlay arrived in Resolve 16).
- **Nodes (#67):** timeline-area console dock (F6 console row slot [6]), NEVER a viewer swap; own 26px nodeviewer header + 38px toolbar + natural-size 706×268 scroll-both workspace.
- **Scopes (#68):** ~160px pane UNDER the viewer inside region [2]'s column; reference-exact labels Parade/Waveform/Vectorscope/Histogram; 2×2 four-up = one-shot deferred toast, not a mode.
- **NO nodes/scopes tabbed union** (this R24 ruling is later SUPERSEDED by R25-A2 below).
- **Curves rebuild (#69):** `CurveSet {y,r,g,b}` + Y-applies-to-all-3 composition order in `bakeLinearCurveLuts`; [Y|R|G|B] radiogroup w/ roving; ~64-bin channel histogram BEHIND the grid; solid 25%-white reference line; 10px white handles; single-click insert w/ 8% near-band; Delete/right-click removal; D3 gesture law + lost-pointer-capture commit.
- **Stills → Gallery (#70):** rename; per-card delete/.drx buttons DELETED (Resolve has zero per-card buttons); a ContextMenu (Apply/Delete/Export PowerGrade); **stills apply = REPLACE not merge** (reset-then-set in ONE `setGrade` patch).

**R24-A3 — mixer / toolbar / deliver (#60–#66, #71)** [r24-audit-round.md:149-196]:
- **Mixer toggle (#65/#66): binary toggle WITH lastVisual memory** — `cycleMixerState`/`mixerStateLabel`/`mixerFloorWarned` all DELETED with store-level deletion pins; the button renders on the AUDIO page ONLY.
- **Floor (#60):** <280px = SILENT render-level fallback (the container measures itself and pure-renders MetersDock; store untouched).
- **View options (#64/#62):** a REAL APG popover (`ViewOptionsPopover.tsx`, ContextMenu-family): Compact tracks (menuitemcheckbox), Clip style Filmstrip|Block (menuitemradio), Audio waveforms (menuitemcheckbox, honest aria-disabled + reason while compact).
- **Edit dock glyph (#61):** Clapperboard.
- **Deliver (#71):** the 22px read-only ruler UNCONDITIONAL in TimelineCompact + the 32px RangeBand BELOW it on the deliver branch (54px head stack); ruler gains read-only in/out bracket FLAGS; band grammar (solid accent-tint fill, ~40% dark mask OUTSIDE in→out, 12px bracket handles, live TC readout, keyboard sliders); **mouse preview CLAMPS against the opposite live edge — release commits exactly the preview** (the inverted-preview defect dies; the loop seam law unchanged).

### 1.2 DESIGN-R25 §6 (the verdicts that drive the sibling's NEXT round — binding law, not yet code) [r25-reaction-round.md:178-213]

**R25-A1 — the source viewer (feeds their W1):** Resolve treats STILL sources as normal clips (default 5s) with the FULL transport; the honest mock = a moving playhead + running TC over the poster. **I/O marks = bracket flags ON THE SCRUB STRIP + out-of-range dimming ON THE STRIP (never on the image)**; duration readout switches to the range. Transport row: left TC/jog · center transport · right Mark In/Mark Out. The **SourceRangeBar merges into a real scrub strip** (playhead + flags + dimming); the 7 edit-mode buttons stay in the row as the PRIORITY cluster [r25-reaction-round.md:180-190].

**R25-A2 — the color layout (feeds their W3):** **"scopes react to the PLAYHEAD FRAME (always-on analysis, not selection)"** → the console-row TAB ruling CONFIRMED: the color console row = `[Timeline | Nodes | Scopes]` thin tabs; **the under-viewer pane RETIRES** (supersedes R24-W2's placement). Target grammar: a 3-chip breadcrumb `[clip name · track] ▸ [Clip grade | Timeline grade] ▸ [Node n · label]` with the node chip clickable; **timeline-grade copy: "applies to every clip in this timeline, after clip grades"** (never "one track" — Resolve has no track grade). Orange-dot affordance on inspector tabs holding adjustments in the current node [r25-reaction-round.md:192-200].

**R25-A3 — the wheels (feeds their W3):** **disc drags are RELATIVE/ACCUMULATING** (trackball-style: grab anywhere, `v += Δpointer`, clamp at rim) with LIVE preview + live YRGB readouts — **"our absolute+commit-on-release model is WRONG and gets rewritten."** Center = neutral; crossing = complementary hue (≤3% dead zone optional). **Master luma = a HORIZONTAL DIAL below the wheel + Ctrl/Cmd+drag inside the disc = master adjust + Shift+drag = absolute jump. Dbl-click disc = color-only reset; per-wheel corner button = color+master reset. 4 YRGB numeric fields per wheel, live, EDITABLE** (dbl-click-to-type, ↑/↓ nudge, drag-to-scrub). Hue ring: vectorscope orientation (red upper-left, 60° spacing). One undo entry per gesture [r25-reaction-round.md:202-213].

### 1.3 The wave law summaries (commit evidence; all messages read in full)

| Wave (commit) | The established law |
|---|---|
| **W0** `1dd0cf8` | Store prep: setPage clears `selectedFxObject`; `setTransition` identical-patch no-op guard (never fires on fresh-mint); `splitElement` clears a fx-selection pointing at the split element. |
| **W1** `645981f` | Binary mixer toggle w/ memory (store deletion pins); <280px pure-render floor (`mixer-dock` wrapper testid); ViewOptionsPopover (APG menu, first-enabled focus, rove w/ wrap, Esc/Tab/outside + focus restore); Clapperboard; the #62 icon-audit table (every TimelineToolbar control verdicted); "no Media Pool on any page ≠ edit" pin. |
| **W2** `0196c22` | Viewer-swap DELETED (region [2] ALWAYS Viewer-led; AppShell.tsx:17/:397); ColorNodeGraph → console-row slot [6]; ~160px scopes pane under viewer (ColorPage.tsx:9, AppShell.tsx:449); reference-exact scope labels (ScopesDock.tsx:11-12); Curves YRGB rebuild (curveMath.ts:62-96 — CurveSet + splitChannels/joinChannels; frozen {master: tagged points} storage); Gallery rename (leftDockContent.ts:45); stills apply = replace-not-merge. |
| **W3** `0a45d17` | ONE shared parser in Clip.tsx (`applyFxRowToSeam`/`applyFxRowToClip`/`buttSplicedFollower`, `SEAM_EPSILON = 0.001` at Clip.tsx:215); replace retains duration+alignment; three doors (clip body, SeamZone, occupied TransitionBox); effect rows stack w/ ×N toast; empty-lane pure no-op; SeamZone dragOver visuals; occupied-seam ⇄ surface (14→24px floor); dbl-click apply (FxBrowser.tsx:109 imports the parser, Clip.tsx:175 owns `EFFECT_DRAG_TYPE`); TransitionBox wrapper/visual split (un-clipped wrapper + overflow-hidden visual child, handles hang 3px outside); seam WITH a transition renders NO SeamZone. |
| **W4** `6b0bd91` | Ruler+band coexistence (22px ruler UNCONDITIONAL + 32px band BELOW on deliver, 54px stack; `shell-timeline-compact-flag-in/out` — TimelineCompact.tsx:182); band grammar; DeliverPage reads s.loop; `deliverViewStore.ts` (module zustand: jobs/showQueue single-writer, store-owned 4×500ms mock render timer, unmount-survival); mouse-clamp + keyboard-keeps-R14; story net 123→124. |
| **W5a** `e6de8ca` | **THE P1 CSS fix:** unlayered `input[type="range"]{height:14px}` outranked Tailwind's `@layer utilities` (the Inspector EQ's vertical h-[88px] sliders collapsed to 14×11 stubs) → the whole range grammar block moved INTO `@layer base` (app.css:44-47, :184-190, :256-261 — three blocks verified in-tree); NumberField TC width w-[84px]; ClipParamRow stable slider keys + controlled value; MicroSlider `resetTo` (default, never midpoint); Levels\|EQ tablist arrow roving (reconciled: lives in Inspector.tsx's AudioSection); WheelsPanel Temp/Tint REAL bar-sliders; TransitionSection honest mixed state ("Mixed — N of M") + fan-out Add + no-op-never-mints. |
| **W5b** `72b2248` | SourceEditBar ONE ROW always (h-8, flex-wrap deleted, overflow-x-auto, 24px hit floor, 7 buttons; the R22-#83 narrow-width wrap law superseded in-file); MediaPool preview hairline sweeps; honest trim-in/out (head/tail reset; aria-disabled + reason until a range exists); plain F6 trapped in the CheatSheet (window-capture + dialog stopPropagation); hideOverlays covers ALL 4 groups; SourceRangeBar B7/B8 gesture laws (pointercancel/lostpointercapture + aria-orientation) + absolute drag + Home/End on either handle; ConfirmDialog focus restore. |
| **W5c** `af8c1c9` | Parade `sharedMax` USED by the painter (honest cross-channel density); parade label dedup (one graticule); unguarded `setPointerCapture` ×3 → the try/catch guard (MicroSlider, WheelsPanel puck, QualifierPanel RangeWidget); **AuxStrip T0 header ID-only** (bus name once, in the title row); **ChannelEditor Aux-returns covers BOTH buses a1+a2** (ChannelEditor.tsx:430-434, `setAuxBus`); **De-esser Freq row LOG-domain** (position map `value = min·(max/min)^(pos/100)`, geometric midpoint ≈4.24 kHz — ChannelEditor.tsx:54-57, :78, :101-109); setActiveScene clears stripFocus; QualifierPanel resetTo ×5. |
| **W5d** `b9cd387` | Splitters tabIndex=0 (§11 ladder real); the ruling-22 Delete rung scoped (`page==='fx' \|\| fxMode`); inspector dbl-click reset un-pins `inspectorWUserSet`; `toggleEffect`/`removeEffect` no-op law; Escape clears marker/fx-object domains; **W1's waveform flag→Clip-body render gap PAID** (Clip's audio body reads the §4.7 per-track view flag); **`setAllTrackWaveforms`** store batch (ONE `withHistory` write converges every audio track's flag — useUiStore.ts:775/:1634); insert-preview SPLIT ghost joins the rAF scrollIntoView; compact playhead z drops UNDER the sticky badge chrome; Ruler t≈0 pin clamps x ≥ 0. |
| **WRAP** `3db130f` | 1,740/1,740 green, tsc + build clean, 124 stories; census 64 files / 1,734 it-blocks (their method); 14/14 threads resolved; SKILL #143-#145 (PUSH-AFTER-EVERY-WAVE; [recon] provenance; bundle-HEAD law). |

---

## §2 The harvest map

Severity: **P1** = an established mock/round-doc law the spec lacks or contradicts · **P2** = stale/underdefined spec prose vs the rebuilt mock · **P3** = citation/count drift + polish.

| # | Surface | The mock's new law (evidence) | Spec / register current state | What must absorb | Sev |
|---|---|---|---|---|---|
| 1 | **Deliver range band placement** | The 22px read-only ruler is UNCONDITIONAL in TimelineCompact's head stack + the 32px RangeBand mounts BELOW it on the deliver branch (54px total; `rangeBand={page==='deliver'}`); the ruler gains read-only in/out bracket FLAGS (`shell-timeline-compact-flag-in/out`, TimelineCompact.tsx:182) — W4 `6b0bd91` | **18 §4.8:222 says "mounted in place of the compact ruler's head row"** — the exact opposite of the coexistence law; REFERENCE-REGISTER row 4 carries "in place of the compact ruler's head row" verbatim | 18 §4.8's RangeBand row + register row 4: replace the placement clause with the coexistence law (ruler unconditional + band below + flags + 54px stack) | **P1** |
| 2 | **08 §0's color-wave description (3 stale claims)** | (a) ScopesDock is NOT in the console row next to MixerDock — it is the ~160px pane under the viewer inside region [2] (ColorPage.tsx:9, AppShell.tsx:449), with reference-exact tabs Parade/Waveform/Vectorscope/Histogram (ScopesDock.tsx:11-12); (b) the node graph does NOT swap the viewer — region [2] is ALWAYS Viewer-led, the graph re-homes to console-row slot [6] (AppShell.tsx:17/:397), ruling-14 stale-frame law DELETED; (c) density is NOT the timelineCompact auto\|on\|off toggles — it is the ViewOptionsPopover's Compact-tracks menuitemcheckbox (ViewOptionsPopover.tsx:201-209) — W2 `0196c22` + W1 `645981f` | **08 §0:16 describes the R23 W-B state verbatim on all three counts** ("in the timeline-area console row next to the MixerDock… Luma WFM / RGB Parade / Vector / Histogram"; "the AppShell swaps the viewer for the graph… ruling 14"; "the timelineCompact auto\|on\|off density toggles (D-B3)") | 08 §0's W-B row: re-truth all three clauses to the W2/W1 state, and REGISTER the R25-A2 supersession (scopes → console-row TAB `[Timeline\|Nodes\|Scopes]`, pane retires — §1.2 above) so the row does not go stale twice | **P1** |
| 3 | **FX/transitions DnD routing (A1 canon)** | Replace-never-stack (presentation swap retains duration+alignment; identical short-circuits with the "Already X" toast); one law, three doors; effect rows stack w/ ×N; empty-lane pure no-op; adjacency refusal (SEAM_EPSILON 1ms); occupied-seam ⇄ surface; dbl-click apply; click-occupied-seam = select — W3 `0a45d17`, Clip.tsx:173-222, FxBrowser.tsx:24/:109 | **18 §4.11:249 has only:** "transition rows issue `updateTransition` on the clip's boundary… One drop = one undoable command" + the MIME contract; register row 7 names the MIME + Fade-rows routing only | 18 §4.11 (or a new 18 §4.11a routing table): absorb the A1 routing table + replace-never-stack + the three doors + refusal/no-op laws; grow register row 7's citation list | **P1** |
| 4 | **Wheels gesture law (R25-A3)** | Disc drags RELATIVE/ACCUMULATING (`v += Δpointer`, clamp at rim) + live preview + live YRGB readouts; master = horizontal dial + Ctrl/Cmd+drag; Shift = absolute jump; dbl-click = color-only reset; corner button = color+master reset; 4 YRGB numeric fields per wheel, live, EDITABLE; vectorscope-oriented hue ring; one undo per gesture — r25-reaction-round.md:202-213 (BINDING for the sibling's W3; **current code is still the old model**: WheelsPanel.tsx:12-14 "ONE undoable setGrade per drag… commit on pointer-up", :22-24 YRGB read-only) | **08 §4.3:262 describes the FreeCut absolute puck** ("puck (drag to set hue+amount)") and has NO gesture law; 08 §16.A verifies the model as (hue, amount) pairs + one scalar f32 per wheel — **A3's "editable" YRGB fields CONTRADICT that verified model** (no per-channel fields exist to edit) | 08 §4.3: add the wheel gesture contract (relative drags, dial, modifiers, resets); FILE THE MODEL QUESTION as an OPEN decision (editable YRGB requires widening WheelsParams beyond 08 §4.2's verified 28-f32 layout, or narrowing the verdict to derived readouts) — see §4 rec 5 | **P1** |
| 5 | **Source viewer/transport (R25-A1 + W5b)** | Stills get the FULL transport (5s pseudo-duration playhead scrub + play; play stops at out); I/O = bracket flags + dimming ON THE STRIP, never the poster; SourceRangeBar merges into a real scrub strip — r25-reaction-round.md:180-190 (binding, unimplemented). Landed now: SourceEditBar one-row law (h-8, no wrap, overflow-x-auto, 24px floor, 7 buttons — W5b `72b2248`); honest trim-in/out (head/tail reset) | 18 §4.3:174 describes the plain `<video>` fallback ("plays the raw asset… no timeline state involved"); 18 §4.3:175-176 (the R25-landed SourceEditBar/SourceRangeBar rows) carry the chrome contract but NOT the one-row responsive law, NOT the transport, NOT the strip-flags law | 18 §4.3: extend the SourceRangeBar row with the W5b gesture laws (B7 pointercancel/lostpointercapture, aria-orientation, Home/End both handles) now; register R25-A1 as the forward contract (their W1 will implement) — one amendment, two phases | **P1** (A1 is binding round-doc law; the spec's fallback description is superseded) |
| 6 | **Band drag semantics (mouse vs keyboard split)** | The MOUSE preview clamps against the opposite LIVE edge and the release commits exactly the preview; the KEYBOARD path keeps the R14 drag-along ordering verbatim — W4 `6b0bd91` (both directions pinned) | 18 §4.8:222 presents R14 drag-along ("writing an edge past the other DRAGS the far edge along, never inverts") as the band's law, unqualified; register row 4 repeats it | 18 §4.8 + register row 4: split the clause (mouse = clamp-to-live-edge; keyboard = drag-along) | **P2** |
| 7 | **Color page composition** | Color page = Gallery in the LEFT dock (leftDockContent.ts:45), ColorInspector in the right rail (AppShell.tsx:338), console-row node graph slot [6], scopes pane under the always-led viewer — W2 `0196c22` | 18 §4.8:220: "Color (grading workspace… in a simplified single-column layout; enters a color-focus mode that swaps the inspector for the grading panel stack…)" — no single-column stack exists | 18 §4.8's Color one-liner: re-truth to the W2 composition (and pre-note R25-A2's TAB strip) | **P2** |
| 8 | **ViewOptionsPopover (timeline view state)** | APG menu: Compact tracks (menuitemcheckbox, DOM-ABSENT on fx), Clip style Filmstrip\|Block (menuitemradio), Audio waveforms (menuitemcheckbox, honest aria-disabled + reason while compact); the standalone density button is DEAD — W1 `645981f`, ViewOptionsPopover.tsx:57/:201-209 | 18 §4.5 (timeline-toolbar) describes no view-options control at all; 18 §4.7:216 has only "the mock's per-track waveform/clip-view toggle is a UI pref"; register has no row | 18 §4.5/§4.7: add the view-options popover contract; NEW register row (see §3) | **P2** |
| 9 | **Per-track waveform render + the batch law** | Clip's audio body READS the §4.7 per-track view flag (flag false → no waveform band; thumbnail + name stay) — W5d `b9cd387`; `setAllTrackWaveforms` = ONE `withHistory` write converging every audio track (useUiStore.ts:775/:1634); the popover's convergeWaveforms rides it | 18 §4.7's "UI pref" one-liner; 05 §7.2 (waveform rendering) states no per-track visibility law; 05 §12.2/18 own the display-state split (C57's B3 height-home rides the same seam) | 18 §4.7: upgrade the one-liner to the flag+batch law (the N6 lock-all batch is the spec's own precedent — 18 §4.5:207); 05 §7.2: add the per-track-flag render gate as the display-state cross-ref | **P2** |
| 10 | **Curves YRGB rebuild** | CurveSet {y,r,g,b} (frozen {master: tagged points} storage, splitChannels/joinChannels); Y-OUTER LUT composition (y∘r, y∘g, y∘b); [Y\|R\|G\|B] radiogroup w/ roving; ~64-bin histogram BEHIND the grid at the family throttle; solid 25% reference; 8% near-band insert; Delete/right-click removal; lost-pointer-capture commit — W2 `0196c22`, curveMath.ts:62-96 | 08 §5.4:401 describes FreeCut's panel (Master/R/G/B switcher, 230×230, 16 points, monotone spline) — no histogram law, no gesture grammar; register row 16 = DESIGN-REFERENCE, no pins | 08 §5.4: add the mock's YRGB channel model + the histogram-behind-grid law (the Resolve-16 research finding) + the insert/remove gesture grammar as reference rows; register row 16 grows pins (ColorPage.test 43, curveMath via gradedFrame.test 21) | **P2** |
| 11 | **Mixer family (W1 + W5c)** | Binary audio-page-only mixer toggle w/ memory (glyphs AudioLines/SlidersVertical); <280px pure-render floor; AuxStrip T0 header ID-only; ChannelEditor aux-returns BOTH buses (a1+a2, −60..+6, `setAuxBus` — ChannelEditor.tsx:430-434); de-esser Freq log-domain (geometric midpoint 4.24 kHz — ChannelEditor.tsx:54-78); setActiveScene clears stripFocus — W1 `645981f` + W5c `af8c1c9` | **18 §8.8:387/§4.1:139 DROPPED the mixer panels entirely** (the 3-page world — the registered 3-vs-5 OPEN decision, register row 8); **20 §7:143 defers all mixer UI to the M2 design round; the register has NO mixer family row** — the W1/W5c laws are indexed nowhere | Register: NEW mixer family row (mock homes + MixerDock.test 35 / ChannelEditor.test 27 live; owning spec 20-M2-pending; divergence MOCK-YIELDS-REGISTERED per row 8's posture); 20 §7's M2 roadmap row gains the mock-law pointer | **P2** |
| 12 | **05 §8A grammar deltas** | (a) The insert-preview SPLIT ghost joins the rAF scrollIntoView (the straddler visible at arm time — W5d); (b) the waveform flag→Clip-body render (row 9); (c) carrier line pins DRIFTED: ghost `Timeline.tsx:1535`→**:1654**, speed badge `:1573`→**:1692**, arrows `:1654-1670`→**:1776/:1790** (verified in-tree) — W3/W5d edits | 05 §8A:623-627 cites the old line numbers; the `ghost` row has no scrollIntoView clause; no waveform-flag clause | 05 §8A: refresh the interim-carrier citations (or convert to testid-keyed: `insert-preview-ghost`/`insert-preview-arrow-down`/`insert-preview-arrow-right-*` — drift-immune); add the split-ghost-scrollIntoView sub-law to the `ghost` row | **P2** (a,b) / **P3** (c) |
| 13 | **Inspector/widgets micro-laws (W5a)** | `@layer base` range-reset law (unlayered author CSS outranks Tailwind utilities — app.css:44-47, verified); NumberField TC w-[84px]; MicroSlider resetTo default; Levels\|EQ arrow roving; honest mixed-state TransitionSection ("Mixed — N of M" + aria-disabled Add + fan-out); Temp/Tint real bar-sliders — W5a `e6de8ca` | 18 §4.4:189 has commit-on-release/enter + "multi-selection shows the common-parameter subset with 'mixed' indicators" — directionally consistent, no layer law, no mixed-state no-op law | 18 §4.4: fold the mixed-state honesty law + the reset-to-default (not midpoint) law as one amendment; the CSS layering law belongs in 18 §9's theme/token section as a house rule | **P3** (mock-house implementation class, one fold) |
| 14 | **Store-hygiene laws (W0/W5d)** | setPage clears fx-selection; setTransition no-op guard; split clears fx-selection; toggleEffect/removeEffect no-op on unknown ids; Delete rung scoped to page==='fx'\|\|fxMode; Escape ladder — W0 `1dd0cf8` + W5d `b9cd387` | 18 §6 (state binding) has no page-exit/no-op-guard family; these are view-state laws | One 18 §6.2 bullet family (the "honest view-state" clauses) or register-row notes | **P3** |
| 15 | **Count pins across the corpus** | Live census: 64 files / 1,733 it-blocks (line-start) | **12-testing-strategy.md:14 + 17-test-plan.md:20 still say "61 test files"**; register header already carries 1,733/64 | 12/17 §0: 61→64 (the battery's central re-baseline class) | **P3** |
| 16 | **Register row pin-count drift** | SourceEditBar.test 16 (was 15); SourceRangeBar.test 12 (was 8); TimelineCompact.test 28 (was 24); DeliverPage.test 24 (was 20); FxBrowser 12 + FxInspector 14 (was 5+12); useShortcuts.test 59 (was 56); insertPlan.test 31 HOLDS | Register rows 2, 3, 4, 7, 17 carry the old counts (35.2: drift without amendment = battery failure) | §3 worklist | **P3** |

---

## §3 The register-maintenance worklist (concrete REFERENCE-REGISTER.md edits)

1. **Row 2 (Source-preview chrome):** pins → **16** SourceEditBar.test + **59** useShortcuts.test; append to the mock-home cell: "+ the W5b one-row law (h-8, no wrap, 24px hit floor) + the R25-A1 forward contract (full transport on stills; SourceRangeBar merges into the scrub strip)".
2. **Row 3 (Source in/out marks):** pins → **12**; append the B7/B8 gesture laws + "Home/End commit on either handle; the drag is absolute" + the R25-A1 merge verdict (REGISTERED FORWARD).
3. **Row 4 (Deliver RangeBand):** pins → **28** TimelineCompact.test; rewrite the family description: "the 22px read-only ruler UNCONDITIONAL + the 32px band BELOW on the deliver branch (54px head stack; in/out bracket FLAGS); mouse-clamp + keyboard-drag-along split" — replaces "in place of the compact ruler's head row".
4. **Row 7 (FX page):** pins → **26** (FxBrowser 12 + FxInspector 14) + TimelineCompact density note re-keyed to the popover; grow the MOCK-IS-REFERENCE content: "replace-never-stack (retained duration/alignment), three drop doors, effect-stack ×N, empty-lane pure no-op, adjacency refusal (1ms ε), occupied-seam ⇄ surface, dbl-click apply".
5. **Row 16 (Color surfaces):** add pin counts (ColorPage.test **43**, ScopesDock.test **22**, StillsPanel.test **19**, ColorInspector.test 10, WheelsPanel.test 6, controls.test 6, mockGrades.test 21 — all live-counted) + the W2 rebuild description (console-row node graph slot [6]; scopes pane under the viewer; Gallery; YRGB curves; replace-not-merge stills); **REGISTER the R25-A2 supersession** (scopes → `[Timeline|Nodes|Scopes]` TAB strip; pane retires; 3-chip breadcrumb; playhead-frame analysis) as a dated forward verdict so the row survives the sibling's W3.
6. **Row 17 (Deliver page):** pins → **24** DeliverPage.test + **10** deliverViewStore.test (NEW module `src/state/deliverViewStore.ts`); add "the store's unmount-survival + single-writer + mock-render-timer laws".
7. **NEW ROW — Mixer family** (after row 15): mock homes `src/components/mixer/{MixerDock,ChannelEditor,ChannelStrip,MixerPrimitives,StripGraphs,SoundLibrary}.tsx` + `src/state/mockMixer.ts`; pins MixerDock.test **35** + ChannelEditor.test **27** + ChannelStrip + MixerPrimitives; owning spec **20 (M2 design round, §7)**; citation EXECUTABLE-WITNESS + PROPOSAL; divergence **MOCK-YIELDS-REGISTERED** (rides row 8's 3-vs-5 OPEN decision). Laws to name: the binary audio-page toggle, the <280px pure-render floor, the AuxStrip ID-only header, aux-returns both buses, the de-esser log-domain slider, stripFocus clearing.
8. **NEW ROW — Timeline view-state / ViewOptionsPopover:** mock home `src/components/timeline/ViewOptionsPopover.tsx` (+ TimelineToolbar.test 43, AppShell.test's 7 re-pointed density describes); owning spec **18 §4.5/§4.7**; citation PROPOSAL (C57's B3 height-home rides); divergence MOCK-IS-REFERENCE. Laws: the APG menu contract, Compact-tracks re-home, clip-style radio, the waveforms flag + `setAllTrackWaveforms` batch.
9. **Header suite-count pin:** holds at 1,733/64 (verified live; the wrap's 1,734 = their method — the existing note already covers it). No edit needed unless the battery's line-start method is re-run.
10. **Companion spec-prose edits (register-adjacent, not register rows):** 12/17 §0 "61 test files" → 64 (P3); 08 §0:15-16 the color-count sentences (ColorPage 33→43, ScopesDock 20→22, StillsPanel 14→19, mockGrades 18→21 — live-counted) — fold with the §2 row-2 re-truth.
11. **OPEN decisions table:** add one row — **"A3 wheels YRGB-editable vs 08 §16.A's (hue,amount)+scalar model"** (register row 16 + owner S-spec 08; §4 rec 5).

---

## §4 Recommendations

1. **Absorb the R25 A1/A2/A3 verdicts as REGISTERED FORWARD law now** (they live in `.agents/design/r25-reaction-round.md:178-213`, in-repo and binding for the sibling). The cheapest correct move: one 08 §4.3 amendment (the wheel gesture contract) + one 18 §4.3 extension (source transport + strip flags) + register rows 2/3/16 forward-verdict notes — harvesting the LAW from the docs while the code is still pre-rewrite. Re-read the CODE at the sibling's R25 wrap (the register's 35.2 pass) rather than pinning to `WheelsPanel.tsx`'s current absolute model.
2. **Fix the two hard P1 contradictions in this round's spec edits** (they are cheap, mechanical): 18 §4.8:222's "in place of the compact ruler's head row" → the coexistence law; 08 §0:16's three stale color clauses → the W2 state + the R25-A2 supersession note.
3. **File the A3-vs-08 §16.A model question as an OPEN decision before any wheels spec prose lands.** A3's "4 YRGB numeric fields per wheel, live, editable" requires per-channel fields that 08 §4.2/§16.A's grep-verified uniform layout (hue/amount pairs + one scalar f32) does not have; either the model widens (a 08 §4.2 change with shader consequences — §17.A-port's 28 f32s) or the verdict narrows to derived readouts. Writing the gesture law before this is ruled would bake the contradiction into the corpus.
4. **Register maintenance is ~a 30-minute edit** (§3): two new rows (mixer, view-state), six count refreshes, three description rewrites. Doing it this round retires the 35.2 battery-failure exposure before the sibling's R25 waves move the counts again.
5. **Convert 05 §8A's interim-carrier citations to testid keys** (`insert-preview-ghost`, `insert-preview-arrow-down`, `insert-preview-arrow-right-*`) — the W3/W5d drift (:1535→:1654 etc.) is the second drift of these pins; testids are the mock's own stable contract and cannot drift.
6. **The 18 §4.11 DnD routing table is the single highest-value spec add** (§2 row 3): the A1 research (FCP/Premiere/Resolve unanimous canon) is exactly the class of law the corpus exists to carry, it is fully test-pinned mock-side (FxBrowser 5→12, FxInspector 12→14), and the r1 port will need it.

---

*Research-only audit: no file outside `audits/fleet-r27/mock-variants.md` + the worklog was modified; no commits or pushes; stayed on main.*
