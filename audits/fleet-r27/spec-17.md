# R27-W3 — spec-17 (the overall test plan) — the final-tightness audit

**Task ID:** R27-W3-17 (retry — the prior dispatch failed mid-run; NO partial report existed at dispatch time; this file is the first) · **Agent:** spec-17 · **Date:** 2026-09-14
**Scope:** `17-test-plan.md` (2,819 lines) audited section by section at sub-spec depth against the R27 pin world + the live repos.
**Method:** full spec read (all 19 sections + 0A/13A/17A/18.5) + live re-derivation of every count/census row in the five repos + the two mocks + the sibling reports (scout-engine/ot/wdc/nle-ui/app, mock-variants/mock-verdicts, spec-05/06/09 audits where they touch 17's rows) + the coordination law (mock-verdicts §3.2, the six rules).
**Ground truth re-derived live (this session):** engine @ `f9ac806` (748/748 attested worklog:5806 + commit `480a216`; my static census 665 `it(` + loop-generated families = consistent; **25 test files**: 21 under `tests/vitest/engine/` + 4 under `tests/vitest/`; `timeline-linked-source-edit.test.ts` = 13 tests ✓, `upstream-ports.test.ts` = 25 ✓, `bridge-seams` = 151 ✓, `nle-bridge` = 99 ✓, `m2-wave1-mixer-surface` = 9 ✓); OT @ `55c81c0` (report json live-parsed: **632/632, 72 milestone entries = 481 in-page + 151 real-mouse**; `WIRE_COMMAND_TYPES` = 31 names, `WIRE_UI_EXCEPTIONS` = **3** — selectElements/advancePlayhead/trim, api.ts:328-335; M58+M58R both in the register); WDC @ `ec8fd5c` (34 `.test.ts` files = 19 in `src/test/` + 15 colocated; `waveform-contract.test.ts` = 14 pins ✓; `src/lib/daw/diagnostics.ts` exists, no test file names it); nle-ui @ `32abd58` (**690 it-blocks static — matches its own gate line exactly**); app @ `c020b2a` (**252 = 8 suites**: GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9; the AR-2 gesture pin verified live at `wire-coverage.test.tsx:328-377`; the census-mutation-gate = clean + M1–M6b = 8 scenarios); mini in-flight (**441/441 @ W3 `285eca5`; census 149 units / 136 GAP / 408 authored**); variants in-flight (**my re-read 1,796 it-blocks / 65 files; the R24-wrap census 1,733/64; the sibling's own re-read 1,762→1,773 — still moving**).

---

## §1 Per-section verdicts

| § | Content | Verdict | Evidence / finding |
|---|---|---|---|
| Status header (:4) | the R24 pin block | **P1** | All six repos' pins/counts 2–3 rounds stale: engine 458→**748** (`5036387`→`f9ac806`), OT 536→**632** (`ded43c4`→`55c81c0`/`970948a`), WDC 759→**777** (`85b81b0`→`ec8fd5c`), nle-ui 674→**690** (`fc4cc35`→`32abd58`), app 174→**252** (`c885ece`→`c020b2a`), variants "1521+ (61 files)"→**1,733/64 at the R24-wrap, higher now** (mock-variants:5; my live read 1,796/65). The "61 test files" phrase is the mock-variants report's named twin-site (row 15: "12-testing-strategy.md:14 + 17-test-plan.md:20"). |
| §0A row 1 (BASE-row law) | the regression-role law | SOUND | The law holds; the OT example count inside it ("536/536 — 386 in-page + 150 real-mouse") rides the F1 re-pin. |
| §0A row 3 (law-net corpus) | the LAW-NET census | **P3** + coord note | The census numbers (10 files / 441 tests / 149 units / 408 authored / 136 GAP) are CURRENT — re-keyed by the sibling's own W3 commit (`285eca5` diff verified: 428→441 with the census re-pin). But the mini is MID-ROUND (R24-2 W0–W3 landed; W4+ pending) — per the coordination law the register re-keys at wrap only; the row should carry the REGISTER-PENDING marker (mock-verdicts rule 3). The "60th static id harness-only" clause also moved (05's audit: the mini now declares 60 static = 59 app-emitted + `mini-clip-harness` story-only). |
| §0A row 4 (count discipline) | battery_r24 | **P2** | → battery_r27; the drift lineage stops at R24 — the R24→R27 drift (engine 458→748, OT 536→632, WDC 759→777, nle-ui 674→690, app 174→252, variants 1521→1733+) is exactly the class the row enumerates; the count-consistency key ("engine 458 + OT 536 across 17+19+00") needs the re-key. |
| §0A BASE para (:20) | the K1 module-net proof | **P1** | Everything stale: engine "13 vitest suites / 11 listed" → **25 test files** (12 new suites since: audio-codec-ladder, compositor-clear, mask-manager-view, media-registry, stable-video-grouping, **timeline-linked-source-edit (the E1/E2 13-test pin file, R26 `8188d4e`)**, timeline-locked-track, timeline-mode-clamps, timeline-nan-guard, timeline-relink-fixes, **upstream-ports (G1 @ `3a28b6e`, 25 tests)**, **m2-wave1-mixer-surface (M2 Wave 1 @ `50b91f5`, 9 pins)**); "nle-bridge (96 pins)" → **99**; "bridge-seams now 113" → **151**; OT "386 in-page + 150 real-mouse across 59 milestone entries" → **481 + 151 across 72**; "24 routed wire verbs + 6 documented exceptions" → **28 routed + 3 exceptions** (api.ts:328-335 verified); WDC "18 `src/test/` suites" → **19 + the colocated family (34 total)** + the S-series unregistered (incl. the diagnostics/heartbeat law — briefing fact 7); nle-ui's R9-b/F4/FW-D pins unregistered; app "6-suite roof… 174" → **8-suite, 252**; consumer pins: app→OT **`6e2b91a`** (not `c15a629`), engine→OT **`6e2b91a`**, WDC **`ec8fd5c`** (scout-app §1.2). |
| §0A GAP row 23 (LAW-NET) | LANDED R23 | SOUND | Held. |
| §0A GAP row 24 (K2 census twin) | the combined-family census | **P1** | (a) M49C's registered form is stale: "24 routed + 6 documented exceptions… green 24/24 at `ded43c4`" → **31 = 28 routed + 3 exceptions, green at `970948a`**; (b) the family counts (458/759/536) → 748/777/632, and the engine-in-the-middle suite list grew by the 12 new suites; OT's register "59 entries" → 72; (c) **the 10-mode matrix registration is MISSING from 17 entirely** — the plan's K2 gate (IMPLEMENTATION-PLAN:53) requires "THE 10-MODE EDIT-MATRIX registered in spec 06/17 (… the D30.3 zero-orphan-rows law)"; 06 owns the matrix (§0, D30.1, verified R25) but 17 registers nothing (grep: zero "ten-mode"/"mode matrix" hits). |
| §0A GAP row 25 (K3 corpus authoring) | the store/policy halves | **P2** | Still correctly a GAP row (the halves are UNSTARTED at `c020b2a` — app PLAN:52-54 unchecked ✓). Missing: the **D-ARCH-6 re-pin + gesture-seam switch is now the gating duty** (the ported coverage gate goes red at the re-pin until `use-timeline-actions.ts:452` + `use-keyframe-drag.ts:96` switch to the batch wire verbs — scout-app §2 item 1 / OQ6); the mini's in-flight census movement recorded (149/136/408, REGISTER-PENDING). |
| §0A GAP row 26 (K3 offline audio pins) | waveform/mute pins + prerequisite | **P1 — FLIP** | The row reads "hard prerequisite: S-wdc's waveform seam contract". **MET and LANDED**: the WDC waveform contract survived byte-verbatim (`waveform-contract.test.ts` = 14 pins at `ec8fd5c`) AND the app's K3 audio pins LANDED @ `44e8049` — `src/waveformPeaks.test.ts` (9 tests, header: "the K3 AUDIO PINS (the S-wdc waveform-promotion…"), consuming WDC's EXPORTED contract names. Spec-20's audit already flipped its row; 17's is the twin flip. |
| §0A GAP row 27 (K3 VLM net) | the visual net | **P2** | Correctly still open (app PLAN unchecked: "the VLM net still open") — but the row predates the sibling's grid: their W7+ fleet is **7 views × 12 UI groups × 8 aspects + a VLM net over 124 stories** (r25:147-169), and the coordination law (mock-verdicts rule 4) makes OUR row descriptions THEIR VLM expectations — our amendments must land (or be register-noted) before their VLM pass, else their net verifies dead law (the stills-full-transport row is the live example). |
| §0A GAP row 28 (K3 zero-no-op worklist) | "ALL PENDING at `c885ece` (zero landed)" | **P1 — FALSE now** | The work LANDED at the app's R9 round (the R26 pin, 206/206): "W-F: THE COVERAGE GATE (src/wire-coverage.test.tsx — M49C's law app-flavored… incl. the 3 NEW gesture sims) + the Z6 dead-zone sweep (P8/10/11/12/13/14/19/22/25 pinned)" and "the app flows swept (zero-no-op: 9 unexpected-dead → 4 closed by the waves, 4 pinned honest, 1 CR'd)" (app worklog:413-420). Since then the gate was re-fenced to the **LIVE registry** (`8a3eb35`, RC-V1's P2 fix: consumes `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel — the hand-mirror went stale at exactly the re-pin the old text predicted) + the **AR-2 gesture pin** (the REAL volume-line dblclick → `timeline.upsertKeyframe` origin 'ui', `wire-coverage.test.tsx:328-377`). The census-CI mutation gate now runs **8 scenarios** (`census-mutation-gate.mjs`: clean + M1–M6b — was 6 at the D36.1 filing). |
| §0A GAP rows 29-34 (K4, r2-r5, battery) | | **P2** | K4's "drives the same 24 routed wire verbs" → **28**; r2/r3/r4/r5 SOUND (still pending); the battery row → battery_r27. |
| §0A K-tier map K1 (:37) | module nets | **P2** | "59 entries (M44-M49 + M49T/H/R/G)" → **72 entries (M50-M58 + M51R/M55R/M57R/M58R added)**; "engine net 440→458" → 458→**748**; "nle-ui 648→674" → **690**; "the app roof 117→174" → **252 (8 suites)**. |
| §0A K-tier map K2 (:38) | M49C instrument | **P1** | Same 24/6→28/3 flip as row 24 (the map and the GAP register must move together). |
| §0A K-tier map K3 (:39) | coverage gate IN FLIGHT | **P1** | "the D30 W-F coverage-gate port (REGISTERED, D29.1b — IN FLIGHT)" → **LANDED** (wire-coverage.test.tsx, 9 tests, LIVE-registry law, AR-2 gesture pin); "the offline audio pins" row-part → LANDED @ `44e8049`; "OT's `data-test=` 68-site convention" → stale (05's audit re-derived **69 occurrences / 59 unique names** and recommended the unique-names basis; my read at `55c81c0`: 63 static occurrences + dynamic brace forms in the component tree, 73 unique names across all src — the sites grow with every M-wave); the compensating shell-path pin family correctly still queued (app PLAN unchecked ✓). |
| §0A K-tier map K4 (:40) | the crawl exit | **P2** | "the same 24 routed verbs" → 28. |
| §0-§2 (how-to/purpose/methodology) | | SOUND | The three-tier model, trade-offs, tier map (§2.3), error-path-census + NFR rules (§2.5) — no live contradiction; the D12 ratification does not change the tier shape. |
| §3.1 matrix + §3.2-3.4 | feature × tier | **P2** | The matrix is pre-wire-census in shape: no row for the **batch verbs** (`insertBatch`/`removeKeyframes`/`retimeKeyframes` — landed, D-ARCH-6), no row for the **diagnostics/heartbeat** facet (briefing fact 7 — missing from ALL 21 specs; a §13A.7-eligible NF facet with no verification row anywhere = the §13A.7 enforcement rule's own "spec bug" class), no row for the keyframe-authoring gesture (AR-2). The existing rows are not false. |
| §4 (template + worked example) | | SOUND | Template discipline intact; §4.2's fixture names match §5.3. |
| §5 (asset design) | fixture canon | **P3** | §5.3's project-JSON table is the D-HB2 mirror's natural home — 09's audit (§3 row 9 + :122) asks 17 to "mirror one line in spec 17's round-trip matrix row": the scene-load boundary (project JSON validates+migrates at ProjectManager, then **each scene re-validates at `fromJSON` on engine mount**, OT `ops/timeline-core.ts:2443-2537` @ `970948a`). Currently absent. |
| §6 (WYSIWYG) | three invariants | SOUND | Unchanged by the round (M2 Wave 1 STRENGTHENS audio WYSIWYG: realtime + renderOffline now share ONE wiring law — a §6.3-applicable fact worth a note when the audio row is touched, not a defect). |
| §7 (property strategy) | | SOUND | §7.7's Project-model round-trip row is the D-HB2 mirror target (F8). |
| §8-§13 (perf/CI/regen/assets/manual/triage) | | SOUND | Methodology intact; §9's CI graph is structure-level (the OV-12 engine gate is a K-tier registration, not a §9 defect). |
| §13A.1-13A.4 (NFR/a11y/fixtures/wire conformance) | | SOUND | §13A.4's "78 union members" is spec-15's EngineCommand union (still 78; the 31-verb wire census is the separate D-ARCH-6 re-declaration — 15's audit carries it). |
| §13A.5 (audio-domain seam properties) | WDC/engine citations | **P2** | "759/759 @ `85b81b0`… the R22-era 740" → **777/777 @ `ec8fd5c`** (the S-series: diagnostics + the rebuilt dsp-effects-worklet + the sample-catalog split — none registered); "nle-bridge… 96 pins" → **99**; "bridge-seams… 113 pins" → **151**; the **m2-wave1-mixer-surface** suite (the bus-law + sidechain-parity pins, M2 Wave 1) is unregistered. |
| §13A.6 (shell facets) | | SOUND | (18's own audit carries its P1s; 17's rows point correctly.) |
| §13A.7 (facet matrix) | the re-tiering row + K2 twin | **P1** | The master count row (line 2359): every figure stale (see Status header F1); the K2 twin row (line 2360): M49C "green 24/24" + "(the census rows land at 458/759/536)" → 28/28 + 748/777/632 + **the 10-mode matrix row missing** (the K2 acceptance form per the plan: "every matrix row LANDED or carries a live r1-scheduled row — ZERO ORPHAN ROWS"). |
| §14.1-14.4 (per-spec contract) | | SOUND + **P3** | §1.4's split-table verified live (12's §5/§7/§8/§9/§11/§12/§14/§15/§17.1-17.5/§3.2/§14.A all exist ✓). §14.2's ≈ line numbers drifted: 01 2108→**2184**, 02 2493→**2539**, 03 2371→**2429**, 04 2084→**2132**, 05 1428→**1593**, 06 2908→**3188** (+280), 07 1655→**1758**, 08 2056→**2102**, 09 2465→**2556**, 10 1863→**1937** (and it is "§16. Testing"), 11 2385→**2440**, 12 2362→**2420**. |
| §15 (open questions) | | SOUND | All still open with live mitigations. |
| §16 (checklist) | | SOUND | Unchanged. |
| §17 (glossary) | | SOUND | — |
| §17A (the roof) | the app-tier suite law | **P1** | The intro's inventory: "174 seam/wired-whole tests — the 6-suite inventory: GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7" → **252 — the 8-suite inventory: GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9** (suite names verbatim from the tree; two new suites: the coverage gate + the waveform peaks). §17A.1 S3(a): "OT's 150-test real-mouse family (the 18-entry family)" → **151 tests / 22 entries** (M51R/M55R/M57R/M58R added). §17A.1's S-suite sizes are R15-era estimates (marked as such — acceptable). §17A.2/17A.3 SOUND in law; §17A.3 could register the OV-12 pattern (engine CI's vendored-tree re-pin gate, ci.yml:216) as the roof's own pin-integrity precedent — optional. |
| §18.1-18.4 (meta) | | SOUND | — |
| §18.5 (engine references) | the R24 re-baseline para | **P2** | "458/458 vitest (13 suites incl. the nle-bridge pair…)" → **748/748 @ `f9ac806` (25 test files; code anchor `50b91f5`)**; the table's R7-era citations are correctly labeled CORRECTIVE/superseded (history, not stale law). |

**Counts: P1 = 8 · P2 = 8 · P3 = 3** (P1s: the fleet re-pin block, M49C's form ×2 (GAP row + K2 map), the K3 coverage-gate flip, the K3 audio-pins flip, the zero-no-op FALSE row, the missing 10-mode registration, the 8-suite roof, the §13A.7 master rows).

---

## §2 The fix-list (exact edits, mechanically appliable)

**F1 (P1) — the fleet re-pin.** Four sites carry the R24 pin block (Status :4; §0A BASE :20; §13A.7 re-tier row :2359; §17A intro :2730) + §18.5 (:2794). Replace the R24 parenthetical with the R27 pin world (one text, four paste sites; §18.5 gets the engine-only variant):

> **R27 pins:** engine **748/748 @ `f9ac806`** (code anchor `50b91f5`; 25 vitest test files — the 13 R24 suites + audio-codec-ladder, compositor-clear, mask-manager-view, media-registry, stable-video-grouping, timeline-linked-source-edit (the E1/E2 13-test pin file), timeline-locked-track, timeline-mode-clamps, timeline-nan-guard, timeline-relink-fixes, upstream-ports (G1, 25), m2-wave1-mixer-surface (M2 Wave 1, 9); bridge-seams 151, nle-bridge 99; tsc 0, fence 52, probes 9/9; the OV-12 vendored-tree re-pin gate in CI) — consumes OT @ `6e2b91a` + WDC @ `ec8fd5c`; **OT 632/632 @ `55c81c0` / code `970948a`** — 481 in-page + 151 real-mouse across **72 milestone entries** (M50-M58 + M51R/M55R/M57R/M58R since R24; `download/timeline-test-report.json` the authority; **WIRE_COMMAND_TYPES = 31 = 28 routed + 3 exceptions** — selectElements/advancePlayhead/trim — M49C machine-checked; the M58/M58R batch-verb pin family: insertBatch/removeKeyframes/retimeKeyframes, one history entry each, the singular verbs RETIRED); **WDC 777/777 @ `ec8fd5c`** — 19 `src/test/` suites + the colocated family (34 `.test.ts` total; the S-series upstream wave: the diagnostics EngineDiagnostic bus + heartbeat monitor, the rebuilt dsp-effects-worklet, the rt-safety-gate heritage fix; `waveform-contract.test.ts` 14 pins byte-verbatim); **nle-ui 690/690 @ `32abd58`** (AW1-2 the Gain ceiling +4→+20 — the [−60,+20] fleet domain; F4 identity guards; FW-D `setElementFieldAll` @ `83ff8a8`); **app 252/252 @ `c020b2a` — the 8-suite roof** (GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9; the AR-2 gesture pin + the LIVE-registry law @ `8a3eb35`; the census register 42 = 36 zero-action + 5 carriers + 1 host @ `6e2b91a`; the census-CI mutation gate clean+M1-M6b); **mini 441 in-flight (R24-2 W3; census 149/136/408 — REGISTER-PENDING, re-keys at the sibling's wrap)**; **variants 1,733/64 at the R24-wrap `3db130f`, higher and moving (REGISTER-PENDING)**.

**F2 (P1) — M49C's current form.** Every "24 routed wire verbs + 6 documented exceptions" and "already green 24/24 at `ded43c4`" (sites: :4, :20, §0A GAP row 24, §0A K2 :38, §13A.7 K2 twin :2360) → "the 31-name census = **28 routed + 3 exceptions** (selectElements / advancePlayhead / trim — api.ts:328-335), M49C green 28/28 at `970948a`".

**F3 (P1) — the K3 instrument flips (§0A GAP rows 26 + 28, K-tier K3 :39).**
- Row 26 → move to BASE-side or flip the row: "**LANDED**: the WDC waveform contract landed byte-verbatim (14 pins, `ec8fd5c`) AND the app's pins landed @ `44e8049` (`waveformPeaks.test.ts`, 9 tests, consuming the exported contract names — the live-registry law's second application)".
- Row 28 → re-write: "**LANDED at the app's R9 round (206/206)**: the W-F coverage gate (`wire-coverage.test.tsx`) + the Z6 dead-zone sweep + the zero-no-op closure (9 unexpected-dead → 4 closed, 4 pinned honest, 1 CR'd); re-fenced R27 to the LIVE registry (`8a3eb35`) + the AR-2 gesture pin. Remainder: the census re-run's residual set rides the D-ARCH-6 re-pin round."
- K3 map (:39): "the D30 W-F coverage-gate port (…IN FLIGHT)" → "**LANDED** — the gate consumes the LIVE `WIRE_UI_EXCEPTION_VERBS` export (never a hand-mirrored copy — the 00 §2A standing law) + the AR-2 keyframe-authoring gesture pin (the volume-line dblclick → `timeline.upsertKeyframe` origin 'ui')".

**F4 (P1) — the roof inventory (§17A intro + §0A BASE + §13A.7).** "the 6-suite roof: GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7" → "the **8-suite** roof: GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + **wire-coverage 9** + engineSeam 7 + **waveformPeaks 9**" (252 @ `c020b2a`). §17A.1 S3(a): "150-test real-mouse family (the 18-entry family…)" → "151-test real-mouse family (the 22-entry family — M17 + the *R suites + M49T/H/R/G + M51R/M55R/M57R/M58R)".

**F5 (P1) — the 10-mode matrix registration (§0A GAP row 24 + §13A.7 K2 twin row).** Append to both rows: "**+ THE 10-MODE EDIT-MATRIX registration (D30.1/D30.3, the plan's K2 gate: 'registered in spec 06/17'): 06 §0's ten-mode matrix (the completeness census — mode | engine math | OT wire verb | UI affordance | pins | verdict; baseline at R25: 6/10 engine-class-only, 4/10 absent) is 17's K2 census input; the K2 acceptance form is ZERO ORPHAN ROWS (every matrix row LANDED or carrying a live r1-scheduled row with owner + acceptance), and the wired-subset pairs (the 6 engine class methods ↔ the OT runner families) extend the missing-pairs worklist.**"

**F6 (P2) — the VLM row (§0A GAP row 27).** Append: "the sibling's W7+ grid is 7 views × 12 UI groups × 8 aspects + a VLM net over 124 stories (r25:147-169); **the register rows are the VLM expectations — their net consumes OUR row descriptions** (mock-verdicts §3.2 rule 4): our facet amendments must land or be register-noted before their VLM pass, else their net verifies dead law (the stills-full-transport row is the live example)."

**F7 (P2) — the diagnostics facet row (§13A.7).** Add one row: `| WDC diagnostics/heartbeat (S-series) | 20 §7.4 (02's audit draft) | NF | T1 | the EngineDiagnostic bus + the heartbeat monitor + the stall bypass-to-passthrough + the in-process() protocol (256-quantum heartbeats, NaN/silence scans) + the rt-safety-gate transitive-heritage fix | the heartbeat fires under load; a stalled processor bypasses to passthrough; the heritage gate fails on un-gated transitive imports |` (source: briefing fact 7; WDC `src/lib/daw/diagnostics.ts` + the rebuilt worklet).

**F8 (P3→P2 for coherence) — the D-HB2 mirror (§7.7's Project-model row or §5.3's note).** Add one line to §7.7's "09 Project model" row: "round-trip is TWO-GATED per D-HB2: project JSON validates + migrates at the ProjectManager, then **each scene re-validates at `fromJSON` on engine mount** (OT `ops/timeline-core.ts:2443-2537` @ `970948a`) — the property test asserts both gates, not just the project-level parse" (source: spec-09's audit :122 — 17 was named the mirror's home).

**F9 (P2) — §13A.5's citation refresh.** "759/759 @ `85b81b0` — the R24 re-verify" → "777/777 @ `ec8fd5c` — the S-series wrap (diagnostics + the rebuilt worklet + the sample-catalog split)"; "96 pins at the R24 pin" → "99"; "113 pins" → "151"; append "+ the **m2-wave1-mixer-surface** family (9 pins: the bus return-gain law via upstream `applyAuxBusGain`, the sidechain single-owner parity law, the per-wire identity-diff sever — M2 Wave 1 @ `50b91f5`)".

**F10 (P2) — the battery row (§0A row 4 + GAP row 34).** battery_r24 → battery_r27; extend the drift lineage: "the R24→R27 drift — engine 458→748, OT 536→632, WDC 759→777, nle-ui 674→690, app 174→252, variants 1521+→1733-wrap (+in-flight)"; re-key the consistency check to "engine 748 + OT 632 across 17+19+00".

**F11 (P2) — K4/K1 verb + register counts.** K4 (:40): "the same 24 routed verbs" → "the same 28 routed verbs"; K1 (:37): "59 entries" → "72 entries (M50-M58 + M51R/M55R/M57R/M58R)".

**F12 (P3) — the variants/mini re-key policy.** In the Status header and §0A: "variants 1521+ (61 files)" → "variants 1,733/64 at the R24-wrap (`3db130f`), R25 waves in flight — REGISTER-PENDING (re-key at the sibling's round-wrap only, per the coordination law; my live re-read 1,796/65 at audit time)". Mini's "441" number is current (the sibling re-keyed it at W3) but its parenthetical ("the R23 seal 355 + the R24-mini nets: clip-box + mini-plus W0/W1/W2/W3") conflates two rounds — the real path is R24-2 W0→W3 (357→377→396→415→428→441); fix the annotation, keep the number, add REGISTER-PENDING.

**F13 (P3) — §14.2's ≈ line numbers** re-keyed to the R27 values (see the §1 table; 06's drift is +280 — recommend testid-style "§N-anchored" citations or a battery-scrapeable convention per scout-engine OQ6).

**F14 (P3) — the OV-12 registration.** Add to the K-tier map's K1 or §17A.3: "the engine's vendored-tree re-pin gate (OV-12, ci.yml:216 — the vendored OT suite runs whenever the gitlink moves) is the pin-integrity pattern the roof's S5 lockset assertion generalizes."

**Not defects (verified held):** §1.4's 12-split (all cited sections exist); §5.1/§5.2's ffmpeg recipes (12 §15-verified); §13A.3's absorbed-semantics fixtures (T1, still OT-true); §13A.4's 78-member sweep (15's union is still 78 — the 31-verb census is the separate re-declaration); §17A.2's port-then-swap law; the §2.5 error-path-census + NFR rules; §12's manual matrix; §13's triage tree.

---

## §3 Cross-spec notes (for the cross-cut wave — NOT edits to other specs)

1. **12 (the twin):** every 17 site fixed here has a 12-site twin — mock-variants row 15 already names "12-testing-strategy.md:14 + 17-test-plan.md:20" for the 61-files class; scout-app :109 names "12:14 + 12:36; 17:4 + 17:20 + 17:39 + 17:2359 + 17:2730" for the app/roof class. The two files must move together (the battery keys them).
2. **15:** the 31-verb/28+3 re-declaration landed spec-side in 15's audit (PASTE-READY per `5ecdce5`); 17's M49C rows quote OT's gate — the two texts must stay byte-coherent on the exception set (3 at owner HEAD vs 5 at the app's `6e2b91a` consumer pin — the D-ARCH-6 re-pin duty closes the gap).
3. **19:** the consolidated line-pin re-base (~45 pins) is 19's audit deliverable; 17's §18.5 re-baseline rides it (engine 748/25 files).
4. **05:** the data-test census method (69 occurrences / 59 unique; unique-names basis recommended) — 17's K3 DOM-structural row cites the same 68-site convention and should adopt 05's method when re-keyed.
5. **06:** the ten-mode matrix law lives in 06 §0 (D30.1); the E1/E2 13-test pin file (`timeline-linked-source-edit.test.ts`) is registered in 06's audit — 17's K1/K2 rows should name it too (F1/F5).
6. **09:** the D-HB2 mirror line (F8) is 09's audit ask; also 09's preservePitch ruling (absent≡true) has a §5.12 companion law in 06 — 17's §3.1 matrix gains nothing here (feature-level), but the K3 corpus rows inherit the field when the store/policy halves author.
7. **20:** the M2 Wave 1 fold + the K3-audio LANDED flip landed in 20's audit; 17's rows 26 + §13A.5 are the twin flips (F3/F9).
8. **00:** the fleet pin canon (00's audit carried the pin-world P1s) + the AR-2 consumption law as a 00 §2A standing row — 17's K3 instrument row should cite it once it lands.

---

## §4 What 17 uniquely sees

1. **The verification ladder is the corpus's only cross-repo acceptance map** — six gate-status flips exist nowhere else: M49C's 28/3 form, the coverage-gate LANDED (+LIVE-registry law), the K3 audio pins LANDED, the zero-no-op row's false "ALL PENDING", K4's verb count, and the K1 milestone register (59→72). Every other spec's GAP rows lean on this map for their acceptance gates — a stale ladder misdirects the whole fleet's work ordering (the K3 store/policy vs re-pin sequencing is decided by these rows).
2. **The AR-2 gesture pin + the census-CI mutation gate (8/8) have no spec home at all** (scout-app: "currently registered NOWHERE in the spec corpus") — 17's K3 instrument row + §17A roof law are the natural homes (F3/F4).
3. **The 10-mode K2 registration is 17's co-duty with 06** (the plan's K2 gate names both) — 17 is currently the missing registrant (F5); the D30.3 zero-orphan-rows form is the K2 entry's acceptance shape.
4. **The VLM coordination law runs through 17's rows**: the sibling's 124-story net consumes our facet/row descriptions as expectations — 17's row text is load-bearing for a fleet it does not own (F6; the stills row is the collision example already live).
5. **The count-discipline key ("17+19+00")** makes 17 one of three battery-anchored files — its counts must be re-keyed in the same pass as 00/19, else battery_r27's coherence checks fail by construction (F10).
6. **The roof law (§17A) is 17's own legislation** — the 6-suite inventory was superseded by two suites the spec never saw land (wire-coverage, waveformPeaks); the roof is the app's CI shape, so the spec's suite list is an executable contract that is currently two suites short.

---

## §5 Verification log (the spot-checks behind the table)

- OT report json parsed live (632/72/481+151; ids enumerated — M58+M58R present); `WIRE_UI_EXCEPTIONS` read at api.ts:328-335 (3 names); `WIRE_COMMAND_TYPES` at api.ts:243-277 (31 names).
- Engine: 25 test files enumerated; per-file it-counts (bridge-seams 151, nle-bridge 99, upstream-ports 25, timeline-linked-source-edit 13, m2-wave1 9); the 748/748 attestation cross-checked (worklog:5806 + `480a216` gates line; static 665 + parametrized families consistent — scout-engine §1.3).
- WDC: 34 `.test.ts` found; waveform-contract = 14; diagnostics.ts exists; no diagnostics-named test file (the F7 gap).
- nle-ui: 690 it-blocks static == its own gate line; AW1-2 commit message verified at `32abd58`.
- App: 8 suites enumerated with per-file counts (sum 252 == the gate line); the AR-2 pin read at wire-coverage.test.tsx:328-377 (origin 'ui' at :359); the mutation gate's 8 scenarios enumerated (clean + M1-M6b); app PLAN's K3 queue read (store/policy, mode-matrix rows, compensating family, DOM half, VLM, K4 — all unchecked).
- mini/variants: live it-counts re-derived (441 / 1,796-65) + the sibling's own re-pin commits read (`285eca5` diff on 17 verified — the mini numbers are sibling-maintained, the variants numbers are not).
- Cross-refs: 12's §5/§7/§8/§9/§11/§12/§14/§15/§17.x + §3.2/§14.A verified present; 05 §16.5, 06 §5.12, 00 §6A, 19:136 (455 freeze) verified; 12 per-spec Testing locations re-derived (all drifted, F13).

*Report-only: no spec files were modified; this report + the worklog entry are the only writes.*
