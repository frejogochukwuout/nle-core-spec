# The Leverage Map — R27 W2 (mock-leverage)

**Task ID:** R27-W2-LEVERAGE · **Agent:** mock-leverage · **Round:** R27 final-tightness audit, Wave 2 · **Date:** 2026-09-13
**The user's question:** OT is "incredibly limiting" vs the Resolve target — are we in no-man's-land on advanced timeline edit features? Does FreeCut have more? Should we implement ourselves while fully leveraging what exists, to reduce trials-and-errors?

**Method:** every claim read from code at OT HEAD `55c81c0` (code pin `970948a`), engine HEAD `f9ac806` (src anchor `50b91f5`), nle-ui `32abd58`, spec corpus @ the R27 W1 fleet state, or the quoted reference sections of spec 06. No file outside this report + the worklog was touched. Companion reads: the five W1 scout reports (`audits/fleet-r27/scout-{ot,engine,nle-ui,app,wdc}.md`), the R25 mode fleet (`audits/fleet-r25/mode-*.md`, 10 reports), `ARCH-R25-edit-mode-completeness.md` (Decisions 30-36), OT's SA-1 (`reviews/audit-s16-SA-1.md`), the engine's collision map (`gaps/audit/ra-round/audit-AW1.md` §3).

**The headline answer:** (1) The "lame" claim is **verified and precisely scoped** — OT is the OpenCut-classic parity surface (11/11 classic verbs, `.agents/OP-COVERAGE.md`), and classic *never had* the Resolve-class trim family (spec 06 §11.8, R15-verified); OT adds batch keyframes/insertBatch/ripple-delete but **zero** of the ten-mode advanced surface. (2) **Yes, FreeCut has more** — the spec's §9 Q1-Q15 quotes preserve the full FreeCut trim family (rolling-edit, slip, slide, rate-stretch, freeze-frame, range-removal, sync-lock, the binary-search constraint engine) — but the FreeCut/OpenCut-classic checkouts are GONE (`/tmp/opencut-classic`, `/tmp/freecut` both absent, verified this session); the *executable* FreeCut lineage lives in **nle-engine's `timeline.ts` at `f9ac806`** — complete, E1/E2-fixed, test-pinned, and deliberately UNWIRED (venue-internal per the engine's D12). (3) We are **not** in no-man's-land: the algorithms exist (engine), the interaction/gesture/wire plumbing exists (OT), the visual grammar + composite planners exist (the variants mock), and the specs' Decisions 12.3/30-36 already rule the convergence. What's missing is the **port itself** plus three cross-cutting mechanism decisions (the linkage model, the constants lattice, the error contract) — exactly the trials-and-errors the user wants to avoid.

---

## §1 The OT census at HEAD `55c81c0` (code pin `970948a`) — the "lame" verification

### 1.1 What OT verifiably HAS

**The wire (31 verbs = 28 routed + 3 exceptions).** `WIRE_COMMAND_TYPES` at `src/lib/timeline/headless/api.ts:243-275` (tsc-lockstep-asserted against the `TimelineCommand` union at `:56-212`); `WIRE_UI_EXCEPTIONS` exported at `api.ts:328-335` (selectElements / advancePlayhead / trim). Machine-checked three ways (AR1-5 registry pin `milestones-ar1.ts:180-191`; the M49C gate reading both registries LIVE via `run-timeline-tests.mjs:390-399`; M55R arithmetic pin). **632/632 tests** (`download/timeline-test-report.json` @ `970948a`: 452 in-page + 30 DragEvent/keyboard + 150 real-mouse). (scout-ot §1.2/§1.5, re-verified here.)

**The core op layer** (`ops/timeline-core.ts`, 3,360 LOC, class `TimelineCore` @ `:317`): insert `:994`, move `:1088`, split `:1234`, delete `:1258`, **rippleDelete `:1288`** (decomposes delete → `computeRippleAdjustments` `:1304` → `applyRippleAdjustments` `:1308`), duplicate `:1324`, updateElements `:1390`, upsertKeyframe `:1653`, removeKeyframes `:1791`, retimeKeyframe(s) `:1915/:1987`, track toggles `:2080-2298`. All through `withUndo(label, mutate)` `:826` over the `UndoStack` `:183-316` (beginTransaction, suspended eviction, 100-cap) — **the history-batching seam every new op reuses free**.

**The placement engine** (`placement/index.ts` 543 LOC + `apply.ts` 332): 5 strategies (explicit/firstAvailable/preferIndex/aboveSource/alwaysNew), **REJECT-NOT-SHIFTED** overlap policy, main-track zero-anchor — ported verbatim from OpenCut-classic `apps/web/src/timeline/placement/` (module header `placement/index.ts:1-16`). **The ripple diff engine** (`ripple/index.ts`, 395 LOC): `rippleShiftElements :16` / `applyRippleAdjustments :40` / `computeRippleAdjustments :121` — the OpenCut-classic `apps/web/src/ripple/` diff ported (header `:1-13`; full algorithm quoted in spec 06 §12). **Group ops**: `group-move.ts` (814 LOC — anchor+member, snap on any edge, mixed A/V rejection on the new-track path, `canApplyMovesToExistingTracks` virtual-removal revalidation), `group-resize.ts` (448 LOC — `buildResizeMembers :54` / `computeGroupResize :152`, snap-once invariant, neighbor+source bounds, min-1-frame), `split.ts` (222 LOC, retainSide both/left/right).

**The gesture pipeline** (the controller seam family, `controllers/`): `element-interaction-controller.ts` (771 LOC — idle→pending→dragging state machine, commits `timeline.moveElements` with resolved group-move results + pre-minted stable new-track ids), `resize-controller.ts` (348 LOC — the trim gesture; injected `previewElements`/`commitElements` callbacks, NO engine import, spec 05 §14.4's pattern), `drop-target.ts` (312), `drag-drop-controller.ts` (394), `keyframe-drag-controller.ts` (402), `box-select-controller.ts` (280), `playhead/seek/zoom` (378/240/242). Plus the hook layer (`components/timeline/hooks/` — 20 hooks incl. `use-wire-dispatch.ts`, `use-timeline-actions.ts`, `use-keyframe-authoring.ts`).

**The shared validation seam**: `validateInsertElement` (`api.ts:634-809`, ~175 lines — ONE writer for the element-field law, extracted D-ARCH-6); the `insertBatch` arm `api.ts:1262-1370` (ONE history entry wrapping ONE `core.insertElements`, intra-batch self-overlap → CONFLICT whole-batch atomic `:1365-1369`, kind-split duty `:1326-1331`, staggered construction demo'd app-side `view/page.tsx:535-599`); the `timeline.trim` arm `api.ts:1527-1596` (uniform-delta, side left|right, `lockPreCheck`, the F1B-2 wire-side overlap simulation over `buildResizeMembers+computeGroupResize`); `fromJSON` the validating scene-load boundary (`timeline-core.ts:2443-2537`, D-HB2).

### 1.2 What OT verifiably LACKS (the Resolve-target gap) — grep-verified at `970948a`

Zero hits in `src/` (outside testing-milestone names) for: **roll / ripple-trim (as a trim variant) / slip / slide / insert-edit splice / overwrite-edit / replace / append / ripple-overwrite / fit-to-fill / sync-lock / rate-stretch command / freeze-frame / range-removal / source-edit 3-point family**. Concretely:

| Missing family | The OT fact (file:line) | Spec's own census row |
|---|---|---|
| Roll / Slip / Slide | absent — "OpenCut never had them; still absent at `c15a629`, R24-verified" (spec 06 §10.5 row); re-verified absent at `970948a` | 06 §10.5 "OT-GAP — nle-engine is the executable reference" |
| Ripple-trim (the trim variant) | `timeline.trim` carries NO ripple param (`api.ts:1527-1596`); `ripple/` lib consumed by `rippleDeleteElements` ONLY (`timeline-core.ts:1288-1308`) | 06 §5.2A note (the W11 exception; `⌥[/⌥]` composed-over-patches app-side) |
| Insert-edit splice | `insert`/`insertBatch` = placement-insert (reject-not-shift) — the two-semantics law D31.1: "the placement surface places; the source-edit surface splices" | 06 §5.9 preamble (R25) |
| Overwrite / Replace / Append / Ripple-overwrite / Fit-to-fill | absent; the phantom `placement:'overwrite'` rows retired R25 (P2/P6); replace is the deliberate classic-parity no-op (OT DECISIONS SD-5) | 06 §5.9B-F GAP rows (D31.3-31.7, spec-first R25) |
| Sync-lock | `track.locked` exists (S2); NO sync-lock track groups, no propagation | 06 §6; collision-map row 6 |
| Rate-stretch command / freeze-frame / range-removal | retime MATH only (`ops/retime.ts:12-20` clampRetimeRate [0.01,5]) — no command surface | 06 §10.5 OT-GAP rows |

**The verdict on "lame":** fair as a description of the *advanced-edit* surface, but the limitation is LINEAGE, not quality — OT is a faithful, hardened, wire-covered port of OpenCut-classic's editing model (11/11 classic verbs, `.agents/OP-COVERAGE.md` at `ded43c4`), and the ten-mode family is the FreeCut-side surface that classic never had (spec 06 §11.8: "OpenCut-classic does NOT implement roll/slip/slide/track-lock/freeze-frame/range-removal"). What OT has is exactly the **seam set** the ports need: gesture controllers, the wire registry + its tsc-lockstep discipline, `withUndo` batching, `validateInsertElement`, placement, the ripple diff, and 632 tests + the M49C coverage-gate machinery that mechanically absorbs every new verb (the D29.2 pattern: census re-declares itself).

---

## §2 The FreeCut / OpenCut-classic reference state (what the spec quotes preserve)

**The checkouts are gone:** `/tmp/opencut-classic` and `/tmp/freecut` both absent (verified this session). The reference lives in (a) the spec's quotes, (b) what OT's tree retains of classic, (c) the engine's FreeCut-lineage ports.

**FreeCut (spec 06 §9 Q1-Q15 + §10's table) — the "does freecut have more?" answer: YES, decisively, for the trim family:**

- **Q2 trim family**: `trim-actions.ts` (878 LOC — `trimItemStart/End`, `rippleTrimItem`, **`rollingTrimItems`**, **`slipItem`**, **`slideItem`**), `trim-utils.ts` (274 — the 3-layer clamp: source bounds / adjacent overlap / min duration), **`trim-edit-constraints.ts` (213 — `clampRollingTrimDeltaToPreserveEditState` + `clampRippleTrimDeltaToPreserveEditState` via binary search to the last delta that preserves transitions + keyframes)**, `applySynchronizedTrim` (tightest delta across linked companions, then per-member `skipAdjacentClamp`).
- **Q4 sync-lock**: `sync-lock-ripple.ts` (469 LOC — `propagateRemovedIntervalsToSyncLockedTracks` + `propagateInsertedGapToSyncLockedTracks`, full interval arithmetic quoted in 06 §6) + `track-sync-lock.ts` (17 LOC — `!track.locked && track.syncLock !== false`).
- **Q5-Q7 the edit overlays**: rolling-edit (2-up OUT/IN panels, **shrink-first-then-extend**, `neighborDelta`), slip (4-up, `computeClampedSlipDelta` 33 LOC, virtual-item source shift), slide (4-up, `slideItem` `trim-actions.ts:651-878`, `computeSlideContinuitySourceDelta` 42 LOC, min/max delta stores, keyframe constraints).
- **Q8-Q9**: `rate-stretch-actions.ts` (393 — `rateStretchItem` + `resetSpeedWithRipple`; [0.1,16]), `use-rate-stretch.ts` (772 — `resolveDurationAndSpeed` 5-iteration snap-once normalization), freeze-frame (mediabunny extraction, 2s default), range-removal (354 — three variants funneling into `removeTimelineRangesFromItems`).

**OpenCut-classic (spec 06 §10 + §12):** the ripple diff (`diff.ts` 279 LOC — the vacated−joined interval arithmetic, fully quoted in 06 §12.3), the placement family (6 files, 5 strategies), `group-move/` + `group-resize/`, `retime/` (rate.ts [0.01,5], resolve.ts the source↔clip maps, audio-stretch.ts SoundTouch), `commands/timeline/element/` (the Command/BatchCommand/TracksSnapshotCommand pattern, 06 §4). **What OT retains:** placement/ripple/group-move/group-resize/split ported VERBATIM (module headers cite classic paths file-by-file); the command class layer was re-implemented as `TimelineCore` + `withUndo` + the `UndoStack` (the spec-06 §4.6 design over classic's §4.2-4.5 shapes). **The classic→OT port is the proven precedent for the engine→OT port the ten modes need** — same one-directional discipline, same "carry the law not the defect" rule (engine PLAN `:209`).

**What the engine holds of FreeCut:** the entire quoted family, ported, hardened (E1/E2 fixed R26, mode clamps, N3 lock guards, N4 NaN guards) and test-pinned — §3 below. **Conclusion for the user's plan:** we implement ourselves (no third codebase to adopt — FreeCut itself is unreachable and would be a second model), but we are NOT starting from zero anywhere except replace.

---

## §3 The engine's unwired inventory (verified at `f9ac806`; timeline.ts 8,997 LOC)

**The class methods, line-verified this session** (the R24-pinned §10.4/§16.5A/§0-matrix numbers are ALL stale — the W3 re-pin batch's charge):

| Method | line @ f9ac806 | (was @ R24 pin) | State |
|---|---|---|---|
| `splitClip` | `:2620` | :2367 | complete, wired (19-op RPC) |
| `rippleTrimItem` | `:3132` | :2839 | **complete, UNWIRED** |
| `rollingTrimItems` | `:3324` | :2984 | **complete, UNWIRED** |
| `rateStretchItem` / `rateStretchWithRipple` | `:3840` / `:7645` | :3155 / :6351 | **complete, UNWIRED** |
| `applySyncLockRipplePatch` | `:4218` | :3511 | complete (sync-lock twin below) |
| `removeItems` | `:4507` | :3757 | wired (`removeItems` RPC arm, api.ts:822) |
| `moveItems` | `:4823` | :4035 | wired (`moveItem` RPC) |
| `slip` | `:4956` | :4143 | **complete, UNWIRED** |
| `slideItem` | `:5097` | :4246 | **complete, UNWIRED** |
| `performInsertEdit` | `:5630` | :4702 | **complete, E1 FIXED, UNWIRED** |
| `performOverwriteEdit` | `:6007` | :4860 | **complete, E2 FIXED, UNWIRED** |
| `closeGapAtPosition` | `:7463` | :6180 | complete, UNWIRED |
| `removeSilenceFromItems` (+filler/transcript) | `:8511`+ | :7057+ | complete, UNWIRED |
| `removeRangesFromClip` | `:8235` | :6910 | complete, UNWIRED |
| `freezeFrameAtPosition` | `:8652` | :7185 | complete, UNWIRED |
| `joinItems` | `:8817` | :7341 | complete, UNWIRED (the N3 gate at `:8820-8824`) |
| `calculateSpeed` (pure) | `core/timeline-math.ts:133` | same | pure leaf, consumed by rateStretch |
| `sync-lock.ts` (728 LOC) | `getSyncLockedTracks :176` / `propagateRemovedIntervalsToSyncLockedTracks :579` / `applyRippleToSyncLockedTracks :703` | sync-lock.ts:503 | complete, UNWIRED |

**"UNWIRED" verified:** the engine's `headless/api.ts` exposes exactly 19 RPC case arms (`grep "case '"` = 19) with ZERO occurrences of slip/rolling/slide/insertEdit/overwrite/rateStretch/rippleTrim/freezeFrame/joinItems/closeGap. The ten-mode family is reachable only as direct `Timeline` methods — engine-internal venues (its browser runner, probes, vitest). Per the engine's **D12** (`.agents/DECISIONS.md:521-584`, landed `1064f91`): opencut is THE editing engine; the engine's freecut-lineage stack is the render/export venue model; engine-native edit verbs are **FROZEN venue-internal** — "the offline reference + the parity twin that pins r1". So the unwired-ness is BY DESIGN: these methods exist to be ported, not wired.

**Exact preconditions for reuse (the r1-port contract):**

1. **E1 FIXED (R26, `8188d4e` + review folds `412693d`)** — the linked mid-clip insert defect (Phase 2 pulled the split's LEFT half into the shift set → exact-overlap violation). Fix: the linked expansion EXCLUDES target-track clips with `from < insertFrame`; straddling linked companions are SPLIT there, only their right half shifts. Pinned: `tests/vitest/engine/timeline-linked-source-edit.test.ts` (**26 tests now**: the 4-postcondition pin, re-pair pin, EXCLUSION-clause pin, companion-abort, `linked:false` severance, unlinked regressions — grew from 13 at the fix via the H/F review folds). Residues filed as the port register: **E1-a** companion-track downstream does not ripple (spec 06 §5.0's "downstream" reads target-track-only today); **E1-b** a companion on a LOCKED track is still mutated (same gap in removeItems); **E1-c** N-group orphaning on the ≥2 relink (spec-silent); **E1-d** legacy-path (originId) companions get no fan-out (engine PLAN `:221-231`).
2. **E2 FIXED (R26, same commits)** — the overwrite companion asymmetry was UNDERSTATED in the filing: the old companion cascade block was DEAD CODE. Replacement = the fate-based companion law: cascade groups (fully-removed targets) remove companions whole; trim groups (split survivors) carve the region out of the companion; ≥2-splits relink re-pairs both tracks' survivors by side; a transition-blocked companion carve ABORTS the whole edit. Residue: **E2-a** mixed-fate N-group (cascade wins — a trimmed survivor in a cascaded group loses its audio; spec-silent, r1 clarification) (engine PLAN `:232-250`).
3. **E3 OPEN (P3, rides the r1 port)** — both source-edit ops return empty on a transition-window split with NO error surface; must re-surface as `INVALID_PARAMS`-class per spec 06 §5.9B's error-contract row (engine PLAN `:250-255`).
4. **The port must carry the LAW, not the defect** (PLAN `:209`) — i.e., the E1-a..d/E2-a residues are clarifications to resolve AT the port, and the mode clamps + lock guards + relink laws travel with the algorithms.
5. **Domain lattice (D11)** — four retime domains ([0.01,5] OT wired SSOT / [0.1,4] nle-ui authoring / [0.1,16] engine-native venue / [1/32,32] WDC DSP) + the volume-dB [−60,+20] one-home (OT `core/audio-params.ts`, OV-01/02 VALUE-imported by the engine's `bridge/opencut-laws.ts:54-79`). The port lands ONE constants module (collision-map row 5).
6. **The carried test corpus** (the port's acceptance = these green in OT): `timeline-linked-source-edit.test.ts` (26), `timeline-edit-ops.test.ts` (25 — E1/E2/N2/R1-B4 describes), `timeline-mode-clamps.test.ts` (20 — **ROLL-2/ROLL-3, ripple-trim-G3, slide-G3, slip-G3**: the F5 transition-preserving clamp family), `timeline-locked-track.test.ts` (14 — N3), `timeline-relink-fixes.test.ts` (F6 relink).

---

## §4 THE LEVERAGE MAP — `mode | OT seam | FreeCut reference | engine method | UI surface | risk | order`

**Per Decision 12.3 (spec 00) + spec 06 §10.5's home ruling: the algorithm home for every port is OT's ops layer, as pure functions over SceneTracks, wrapped in `withUndo`, exposed through the spec-15 wire, absorbed by the M49C coverage gate + the tsc-lockstep census.** The engine methods are the algorithm sources (read-only reference + carried tests); the variants mock is the UX/grammar reference; FreeCut's quoted algorithms are the semantic cross-check. Waves per spec 06 §0's GAP register + 05 §16.5A's port table (both r1 wave 1).

| # | Mode | OT seam to extend | FreeCut algorithm to consult | Engine method to port (@ f9ac806) | App/UI surface (variants mock components) | Risk (the trials-and-errors) | Order |
|---|---|---|---|---|---|---|---|
| 1 | **Roll** | `resize-controller.ts` (348 LOC — add a junction-drag session kind beside `side` trim; the `previewElements`/`commitElements` injection seam stays); `ops/group-resize.ts` for bounds math; new `ops/roll.ts` + `timeline.roll` wire verb | `rollingTrimItems` (06 §5.5's full quote): **shrink-first-then-extend**; `clampRollingTrimDeltaToPreserveEditState` binary search; `neighborDelta` preview store | `rollingTrimItems :3324` (+ROLL-2/ROLL-3 clamps pinned in `timeline-mode-clamps.test.ts:128-316`) | `trimLaws.ts` neighbor/source bounds (already the mock's law); the roll cursor/grammar (05 §8A register; `trim_edit_modes.html` §view-roll) | adjacency discovery (the facing pair, N-groups — the ROLL-2/N6 window-matched-pair law); linked-counterpart tightest delta; OT has NO transitions on the trim path (the clamp must carry or be documented absent — G-SLIP-8 class) | **1** |
| 2 | **Ripple-trim** | the `⌥[/⌥]` composed-over-patches app precedent exists; OT: extend `timeline.trim` with a ripple variant OR compose trim + `rippleShiftElements` (`ripple/index.ts:16`); the `ripple/` diff is the wrong tool (per-track delete-shaped) — the engine's signed-shift is the law | `rippleTrimItem` + `clampRippleTrimDeltaToPreserveEditState` (binary search) | `rippleTrimItem :3132` (ripple-trim-G3 pinned :318-374) | variants' `ripple.ts` (the diff port — already exercises the shift family); ripple amber-edge grammar | push/pull asymmetry (the tail extends → downstream shifts; the head retracts → downstream pulls); the E1-a companion-track-downstream question lands HERE first; transition preservation | **2** (shares the roll clamp infra) |
| 3 | **Slip** | greenfield op (`ops/slip.ts`) — no gesture seam exists; wire = `SlipCommand` §4.3.6 already in the 78-union (the C7 problem inverts: OT gets the verb, the union already has it); keyboard `,/.` law exists (16 §3.4A) | `computeClampedSlipDelta` (33 LOC — the whole clamp); the 4-up overlay shape; companion same-δ fan-out | `slip :4956` (speed/fps-aware; clamped-0 = silent no-op — the law the v1 crash taught, 06 §12 note) | shell-variants R15-T4 keyboard slip (the working precedent); the white-outline/diverging-arrows grammar (G-SLIP-1, unpinned) | **the delta-space trap** (timeline-frames vs source-frames vs ticks — five conventions today, G-SLIP-4); the field-vocabulary trap (`sourceStart/End` +δ/+δ vs `trimStart/End` +δ/−δ — the sign flip); non-matching linked companions excluded not desynced | **3** |
| 4 | **Slide** | greenfield op (`ops/slide.ts`); the 3-clip chain needs neighbor discovery (buildResizeMembers' neighbor bounds `ops/group-resize.ts` is the reusable finder) | `slideItem` (`trim-actions.ts:651-878`); `computeSlideContinuitySourceDelta` (42 LOC — the split-contiguous chain gate); `clampSlideDeltaToPreserveKeyframes`; min/max-delta stores for the constrained cursor | `slideItem :5097` (slide-G3 pinned :376-456) | variants' gesture+bounds surface (the registered source-capped divergence) | the continuity-shift law (sourceStart is NOT unchanged — P4's phantom); keyframe preservation; neighbor-trim effects on transitions | **4** |
| 5 | **Insert-edit (3-point splice)** | **`insertBatch`** (`api.ts:1262-1370`) is the composite carrier for multi-clip; the placement layer stays OUT (D31.1's two-semantics law — the new op is `ops/insert-edit.ts`, NOT a placement strategy); `splitElementsOnTracks` (`ops/split.ts:43`) is the straddle-split primitive | `source-edit-actions.ts:233-294` (the algorithm the engine ported; quoted at `performInsertEdit :5630`'s header) | `performInsertEdit :5630` — **E1 FIXED**, 26-test pin file; the mid-clip split law D31.2 | `SourceEditBar.tsx` `,` button + `SourceRangeBar.tsx` (in/out marks — the 3-point family's missing UI primitive, D35.4) + `useInsertPreview`/`insertPlan.ts` (the pure planner, preview==commit by construction, 31 tests — "the most faithful, honestly-labeled" per R25) | the E1-a..d residue set; **the linkage model absent in OT (the biggest hole — §5)**; E3 error contract; the C7 param alignment (`insertBatch` shape vs the union's `insert{elements[]}` superset — the open r1 decision) | **5** |
| 6 | **Overwrite-edit** | composite over landed verbs first (`split + delete + insert`, one `applyBatch`, one history entry — the variants' documented mapping `insertPlan.ts:26`), OR port the op; the covered-clip law is spec'd (D31.3) | `source-edit-actions.ts:296-376` (the 4-case straddle taxonomy) | `performOverwriteEdit :6007` — **E2 FIXED** (fate-based companion law), E3 open | `SourceEditBar` `.` button + R20-W2 (the only full impl in the fleet — variants `insertPlan`'s overwrite branch) | E2-a mixed-fate N-group; transition drops on removed clips; zero downstream movement must hold against the reject-not-shift instinct | **6** |
| 7 | **Replace** | **NEW dedicated op** (`ops/replace.ts` — D31.4/D31A: the ONE dedicated op of the four; single-op atomicity for the transition-remap + sever semantics) | none dedicated in FreeCut (gap) | ABSENT in the engine — the transition law = `joinItems :8817`'s id-remap pattern (NOT removeItems' cascade-drop); build new | variants implements it **WRONG** (3× specified, 0× correct per R25 mode-replace) — re-implement per D31.4 | greenfield BOTH sides (the only mode with no algorithm anywhere); the sever-not-delete companion law (`linked:false`); unfillable-source refusal (`INVALID_PARAMS`, never clamped) | **7** |
| 8 | **Append at end** | **the 6th `PlacementStrategy {type:'append'}`** riding `insertBatch` (already-endorsed composite, ONE history entry free — `timeline-core.ts:887-994`); optional `timeline.append` wrapper (the rippleDelete pattern); append point = target track's last-element end (per-track law; `ops/element-utils.ts` has the span helpers) | n/a (no FreeCut row) | NONE by design (placement-level composite — 05 §16.5A row) | variants + shell-mini (real per-track-end composites already shipped both mocks) | LOWEST of the ten: kind-split duty (F1A-3), linked-append A/V pair synced at the video track's point, audio-track conflict refuses atomically | **8** (cheapest; can ride any wave) |
| 9 | **Ripple-overwrite** | composite `delete + move + insert` (3 ROUTED verbs, ONE `applyBatch`, one history entry — **the order law**: delete-then-move-then-insert never overlaps, F1A-2's intra-batch guard passes by trajectory) | none (gap) | REFERENCE ONLY — `rippleTrimItem :3132`'s private signed-shift machinery (push AND pull) + `_propagateRemovedInterval`/`_propagateInsertedGap` are the delta-law precedent | variants push-only (the pull unreachable — must be re-aligned per mode-ripple-overwrite G-RO-5) | the delta law (push if positive, PULL if negative, zero-floor on downstream min-duration); **the classic §12 diff is asymmetric — pull-only** (freed = ∅ on push; the intermediate state is overlap-rejected) — do NOT build it as "overwrite + ripple flag" | **9** |
| 10 | **Fit-to-fill** | composite `insert + updateElements{retime}` (both verbs landed + UI-routed; one `applyBatch`); first-class verb optional at r2 | the rate-stretch family (`resolveDurationAndSpeed`'s snap-once normalization) | the pure speed law `calculateSpeed` (`core/timeline-math.ts:133`) + the fixed-span derivation inside `rateStretchItem :3840`; the 3-point family's optional target-span overload at `performOverwriteEdit :6007` | variants `view-fitfill` + SourceEditBar button + **37 test pins** (the tested planner branch `insertPlan.ts:342-381`) | the acceptance domain **[0.1, 5]** (the three-domain intersection) with INVALID_PARAMS refusal — **never clamps** (a clamped fit silently violates exact-fill); the badge one-decimal law; audio half is DONE (WDC W2 varispeed — no new audio work) | **10** ("the cheapest landing of the four gaps" per mode-fit-to-fill) |

### 4.1 The adjacent advanced-edit surface (the user's implied scope beyond the ten)

| Surface | OT seam | FreeCut reference | Engine method | UI surface | Risk | Order |
|---|---|---|---|---|---|---|
| **Sync-lock multi-track** | NONE — `track.locked` only; needs a `track.syncLock` field + candidate discovery + the two propagations; collision-map row 6 says "decide: opencut op family or bridge-side expansion" | `sync-lock-ripple.ts` (469 LOC) fully quoted in 06 §6 (the removed-interval + inserted-gap propagations, candidate discovery `getCandidateTrackIdsFromState`) | `sync-lock.ts` (728 LOC): `getSyncLockedTracks :176` / `propagateRemovedIntervalsToSyncLockedTracks :579` / `applyRippleToSyncLockedTracks :703` + `applySyncLockRipplePatch :4218` | the track-header lock/sync-lock affordances (variants TrackHeader) | the precedence law D32.2 (`lock > sync-lock > link`) must be enforced in EVERY new op, not bolted on; straddle-split semantics during propagation | after the trim family (it wraps them) |
| **The companion fan-out (linked A/V)** | **THE BIGGEST HOLE — OT has NO linkage model at all** (AW-1 R7: "no linkage field; app dispatch pairwise"; av-link.ts:39-43). Decision needed FIRST: an opencut linkage field/registry OR dispatch-level + port the relink policy into the app's bridge | `applySynchronizedTrim` (tightest-δ across companions); the split-bookkeeping relink pairs (06 §9 Q1) | `linkedGroupId` + the E1/E2/F6 relink laws (per-group, by-side) — complete + pinned; D32.1 is the ten-mode fan-out table (the law is SPEC'D, only the model is absent) | the app's `linkedTo` sidecar (pairwise, dispatch-level) + variants' link grammar | the D32.4 split-link ruling (relink-both-halves is the law); pairwise-at-rest → groups-at-runtime (D32.5); N-groups and post-split relink exist only engine-native | **0 — the mechanism decision that unblocks 5/6/7/9** |
| **Transition-preserving trims** | OT's transitions live on `element.transitionOut` (the OV-05 one-input-source law — the engine field is BASE, the consumer sidecar OVERRIDE); NO transition-awareness on the trim path today | `trim-edit-constraints.ts:126-213` — **the binary-search-to-last-valid-delta engine** (the single most valuable FreeCut algorithm to port: one generic `clampDeltaToPreserveEditState` serves roll + ripple-trim) | the F5 mode-clamp family: ROLL-3 / ripple-trim-G3 / slide-G3 / slip-G3, 20 tests pinned (`timeline-mode-clamps.test.ts`) | the constrained-cursor/color grammar (roll amber-constrained states, 05 §8A) | whether OT's `transitionOut` model can host the "transition blocked" state the clamps need (E3's abort case); binary search determinism at frame quantization | **with modes 1-2** (it IS their clamp layer) |
| **Rate-stretch command / freeze-frame / range-removal / close-gap / join** (the wave-2 r1 families) | `ops/retime.ts` math landed; the command surface is the gap | Q8/Q9 (rate-stretch-actions, use-rate-stretch, freeze-frame, range-removal — all quoted) | `rateStretchItem :3840` / `rateStretchWithRipple :7645` / `freezeFrameAtPosition :8652` / `removeRangesFromClip :8235` + the silence/filler/transcript trio `:8511+` / `closeGapAtPosition :7463` / `joinItems :8817` | Inspector retime surface (nle-ui, landed); the freeze-frame insert flow | the [0.01,5] OT domain vs [0.1,16] engine-venue (D11 lattice — one constants module); looping-media duration law; silence threshold 0.75 | r1 wave 2 (per 06 §0) |

---

## §5 The collision-map reconciliation + the recommended sequencing

### 5.1 The two filings agree (no conflicts with this map)

**The engine's r1-port collision map** (`gaps/audit/ra-round/audit-AW1.md` §3, `:193-204`; registered in the engine PLAN `:12` and its "r1-port open questions" `:433-437`): (1) **E1/E2 linked-companion + relink — NONE in OT — "the biggest single hole"**; (2) E3 error contract → the port's envelope; (3) F5 mode clamps → "port the clamps with the ops"; (4) N3 locked-track guards (36 ops) → "port op-by-op (opencut's `setTracksLocked` batch is the family precedent)"; (5) the constants/domain lattice → one constants module; (6) sync-lock groups → "op family or bridge-side expansion"; (7) the two headless wires → **spec-15 wins**.

**OT's SA-1** (`reviews/audit-s16-SA-1.md` — the OV-01..OV-21 register + NS-1..NS-5): the overlapping rows with this map are OV-09 (two timeline models — RESOLVED by the D-ARCH-1/D12 assembly ruling, both signatures), OV-13 (their edit verbs = venue-internal twins, "freeze them so nobody fixes one side expecting the other to follow"), and OT's own next-horizon queue (PLAN `:550-556`): "**The r1-port design round** — our side is the linkage-model row (E1/E2 linked companions — the biggest hole) + the constants-module lattice decision + the wire convergence (spec-15 wins, our dialect)."

**Reconciliation: this map is consistent with both filings by construction** — every mode's port home is OT's ops layer (Decision 12.3), the engine methods stay venue-internal (the parity twin; D12 bilateral freeze), the wire is spec-15 (the C7 rename at r1 END), and the mechanism decisions (linkage/constants/E3/N3) are sequenced BEFORE the family ports. The one place this map EXTENDS the filings: append (collision-free — pure placement composite) and fit-to-fill (composition over landed verbs) are not in the collision map at all, which is exactly why they're the cheapest landings.

### 5.2 The recommended sequencing (the trials-and-errors avoidance plan)

**Stage 0 — the mechanism decisions (cross-cutting, before any port):**
1. **The linkage-model ruling** (the collision map's biggest hole): opencut linkage field/registry vs dispatch-level + bridge-side relink policy. Everything in modes 5/6/7/9 + the companion fan-out hangs on it. Spec-side owner: 06 §5.0 + 09 §B1 (D32.5's pairwise-at-rest/groups-at-runtime).
2. **The constants module** (D11's lattice + volume-dB): OT's `core/audio-params.ts` is the proven one-home pattern (OV-01/02 VALUE-import precedent) — extend it with the retime-domain law.
3. **E3's error contract** + the N3 guard survey (36 ops): the wire envelope (`INVALID_PARAMS`-class, never silent no-op) + `lockPreCheck` extension per new verb — OT's `lockPreCheck` (`api.ts` family) is the seam.

**Stage 1 — the trim family (roll → ripple-trim → slip → slide):** shared infrastructure first (the binary-search clamp engine + the junction session in `resize-controller.ts`), then the greenfield slip/slide ops with the delta-space + field-vocabulary conventions pinned ONCE (G-SLIP-4's five-convergence fix). Engine algorithms complete + clamped + 20-test carried corpus. The `trimLaws.ts` mock is the UI-side cross-check.

**Stage 2 — the source-edit family (insert-edit → overwrite):** the E1/E2-tested engine algorithms with the 26-test pin file carried; `SourceEditBar`/`SourceRangeBar`/`insertPlan` as the UX reference (preview==commit by construction — the planner pattern is worth porting into OT's preview/commit seam).

**Stage 3 — the composites (append → fit-to-fill → ripple-overwrite):** all three ride LANDED verbs (`insertBatch`, `updateElements{retime}`, the delete+move+insert trajectory); append and fit-to-fill are the cheapest of the ten; ripple-overwrite needs only the order-law discipline.

**Stage 4 — replace (the one greenfield) + the wave-2 families** (rate-stretch command, freeze-frame, range-removal, close-gap, join) + sync-lock (the op-family decision resolved by then).

**Throughout:** every port lands with (a) the carried engine tests green in OT, (b) the census self-re-declaring (D29.2: tsc-lockstep + M49C — no spec amendment per verb), (c) the mode's 05 §8A grammar row + the D32 fan-out row, and (d) the battery's mode-matrix check re-derived (D30.1).

### 5.3 The stale-pins this audit surfaced for the W3 re-pin wave (P2, one edit family)

Spec 06 §0's ten-mode matrix + §10.4's table + 05 §16.5A's port table all cite the R24-era line numbers (`rollingTrimItems :2984` etc.) — all moved at `f9ac806` (§3's table is the fresh set; the engine scout filed the same family as its P2). The R25 mode reports' pins are era-correct but superseded. Nothing semantic moved — timeline.ts's ten-mode methods were untouched by the R26/R27 engine range except E1/E2 fixes + hardening (the H/F waves' mode-clamp family).

### 5.4 The bottom line for the user

- **"Lame"**: accurate but mis-scoped — OT is the right *substrate* (wire + gesture + history + validation + placement + 632 tests + the self-re-declaring census machinery) with the wrong *verb set* (classic-lineage only).
- **"Does FreeCut have more?"**: yes — the whole Resolve-class trim family — but the executable FreeCut is already inside **our own engine**, ported, fixed, and pinned; the original FreeCut/OpenCut-classic checkouts are gone, so the spec quotes + the engine are the reference pair.
- **"No-man's land"**: no — the specs have ruled the convergence (D12.3/D30-36), both repo-sides have filed the same collision map, the UX/grammar + composite planners exist in the variants mock, and the engine holds 6 of 10 modes algorithmically complete (roll/ripple-trim/slip/slide/insert/overwrite) with 4 spec'd-first composites (replace/append/ripple-overwrite/fit-to-fill). The genuine unknowns are exactly three: the linkage model, the constants lattice, and replace's greenfield op — which is why Stage 0 leads the plan.
