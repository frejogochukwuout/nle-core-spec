# R27-W3-01 — spec `01-core-engine.md` audit (Round 27, Wave 3, per-spec fleet)

**Agent:** spec-01 · **Ground truth:** nle-engine `f9ac806` (docs-only head over code anchor `50b91f5`; timeline.ts **8,997 LOC**; src/lib/nle **63 files / 57,064 LOC**; **748/748, tsc 0, fence 52, probes 9/9** per engine `.agents/HANDOFF.md:14-15` + worklog:5806 — live-run venue is GHA CI; the sandbox clone's submodules are unmaterialized so vitest re-runs ERR_MODULE_NOT_FOUND, expected). Live re-greps this session: bridge/ `wc -l` (12 files = 4,645), LAYER-SNAPSHOT (52 edges), api-surface.frozen.ts (455 names), applyOp switch (19 cases), probe scripts (9), all line pins below ⭐-verified at `f9ac806`.

---

## §1 Per-section verdict table

| 01 section (lines) | Verdict | Evidence |
|---|---|---|
| Header/Status :1-6 | **P2** | The head still reads "Round 24 … engine core @ `5036387` 458/458"; needs the R27 re-audit stamp (`f9ac806`, 748/748) appended to the lineage chain (R23/R22 lineage rows are history, fine). |
| §0 FORWARD INVENTORY :10-31 | **P1** (cluster) | (a) **The D12 venue ruling is missing entirely** — the ratified form (engine `.agents/DECISIONS.md:521-584`) never appears; §0's BASE narrative stops at the R24 re-pin. (b) **The OV-01/OV-02 one-home collapse is missing** — `bridge/opencut-laws.ts` (122 LOC) is named NOWHERE in 01; §0 still describes the bridge as "app-level G-layer code per D8" with hand-synced value-laws implied. (c) Test-state row stale: "458/458 … 13 vitest files: bridge-seams 113 + nle-bridge 96 + …" → **748/748, 25 files** (live list: bridge-seams 151 + nle-bridge 99 + planner 52 + timeline-math 42 + transform-resolver 40 + load-validation 33 + video-sync 31 + timeline-nan-guard 31 + upstream-ports 25 + persistence 25 + timeline-mode-clamps 20 + undo-serialize 15 + timeline-linked-source-edit 13 + timeline-edit-ops 13 + timeline-locked-track 12 + timeline-relink-fixes 11 + m2-wave1-mixer-surface 9 + render-abort 9 + realtime-midi 8 + stable-video-grouping 5 + media-registry 5 + audio-codec-ladder 5 + mask-manager-view 4 + compositor-clear 3 + api-surface 3 `it(`-sites; table-driven rows expand at runtime). (d) Vendor pins stale: OT `c15a629`→**`6e2b91a`**, WDC `494f6ff`→**`ec8fd5c`** (`git ls-tree HEAD vendor/`). (e) App pin stale: `c885ece` 174/174 → **`c020b2a` 252/252, census 42 = 36+5+1**. (f) **No E1/E2/E3 register pointer** (01 never carried the defect rows; the FIXED state + the E1-a..d residue register exist only engine-side + in 06). **HOLDS:** 455 frozen names (FROZEN_API_SURFACE count ⭐, both D9 additive names present :155/:224); "52 pinned cross-layer edges" (LAYER-SNAPSHOT ⭐); the barrel's only bridge-adjacent export is `RealtimeAudioBridge` (index.ts:45-48 ⭐); the GAP rows all re-verified still-open (no `src/lib/nle/projector/`, no S4 parity corpus, no scopes code — the wire-schema strings verified live at api.ts:1529/:1535). |
| §0 D29.5 dispositions :25-29 | **P2** | (a) transform-sidecar slot cites drifted: ":70-71"→**:80** ("future transform sidecars extend the dest"), ":93-94"→**:106** ("op.dest is v1-inert data") ⭐. (b) **maintainPitch's "residual is the app's own toggle wiring" is RESOLVED in code** — R9-b: `el.preservePitch` is ElementJSON law (absent≡true, nle-ui @ `32abd58`); the app threads it at engineService.ts:510-513 + sceneBridge.ts:262/:447 (fleet scout-app card). (c) The maintainPitch engine cites :246/:611/:638 → **:596/:623** (the `source.maintainPitch` threads) + the type fields :207-214/:273 ⭐. (d) `buildElementFilterString` composition-frame.ts:223→**:257** ⭐ (spec-04's report carries the same). |
| §1 Purpose :35-37 | **P2** | "Define the engine's public API surface… every other stream depends on it" — post-D12 the *editing* stream depends on opencut; the purpose needs the venue framing sentence (fix F-2 inserts it). |
| §2 Goals :41-49 | **SOUND** | Framework-agnostic / automation-ready / two-entry / native-portable — unaffected by R25-R27. |
| §3.1 Layered structure :55-91 | **SOUND** | Spec-side design diagram; the D12 note lands at the §3 head (F-2), not here. |
| §3.2-3.4 EditorCore + managers :93-406 | **SOUND** | Teacher-derived design (OpenCut-classic 12-manager roster); untouched by the engine's R25-R27 waves. The engine's live concrete-class form is §13A's business. |
| §3.5 Contract seams :408-475 | **SOUND** | FreeCut `deps/` pattern; unchanged. |
| §3.6 Two entry points :477-566 | **P2** | The WYSIWYG invariant stands, but post-D12 **both entries are the render/export VENUE's internals** — the editing entry is opencut's wire (the one-directional junction). Needs the one-sentence venue framing + the 19-op RPC note: **the 19-case dispatch still exactly 19** (applyOp switch verified ⭐: addText/addItem/updateItem/moveItem/removeItems/split/trimStart/trimEnd/addTransition/updateTransition/removeTransition/addTrack/addClip/addKeyframe/removeKeyframes/setTransformParent/addEffect/removeEffect/setTransform) — the class methods stay off the RPC **by design** (venue freeze); the union façade arrives at r1; **E3's INVALID_PARAMS error contract rides the r1 port, class-level empty return pinned** (engine PLAN :251/:361). |
| §3.7 Separation rules :568-583 | **SOUND** | Lint rules unchanged in kind; the engine's live fence (52 edges, lint-layering.mjs) matches §0's claim. |
| §4 Command pattern :586-675 | **SOUND** | Teacher-derived; the engine-side `execute<T>` supersession lives in §13A (re-pin under F-8). 61 `this.execute(` call sites — **still 61 at `f9ac806`** ⭐. |
| §5 EngineDeps & lifecycle :679-723 | **SOUND** | Unchanged. |
| §6 Module structure :726-811 | **P1** | The tree omits the **bridge/ family entirely** — now 12 files / 4,645 LOC (av-link 203, composition-frame 552, conversions 228, fade-curve 174, mixer-track-model 198, **opencut-laws 122**, realtime-engine 234, scene-mixer 494, scene-to-segments 634, scored-notes 400, segment-strip-adapter 1,092, transition-inputs 314) — the D12 junction's PERMANENT seam (SceneTracks → scene-to-segments / transition-inputs / composition-frame → the venue stack) and a FENCED layer (LAYER-SNAPSHOT carries bridge→audio, bridge→core, audio→bridge, export→bridge among the 52). The OV-01/OV-02 one-home leaf (`opencut-laws.ts`) belongs on this map. |
| §7 Q1-Q7 :815-1916 | **SOUND** | Immutable teacher-repo evidence (OpenCut-classic/FreeCut line quotes); not re-litigated. |
| §8 Test plan :1918-1926 | **SOUND** | Intent list; the 78-union dispatch tests reference spec 15 §4.1 (r1 façade design — coherent). |
| §13 Code references :1930-1992 | **SOUND** | Teacher tables, historical record. |
| §13A nle-engine map :1995-2011 | **P2** | Intro figures stale: "54,791 src/lib LOC, 458 vitest + 9 CI-wired engine probes @ `5036387`" → **57,064 LOC / 63 files, 748 vitest / 25 files, 9/9 probes @ `f9ac806`**. Row pins drifted: timeline.ts:1980→**:1894** ⭐; :5225→**:7321** ⭐; core/types.ts:749→**:1030** ⭐ **and the quote is stale** (`VideoClip | AudioClip | ImageClip | AdjustmentItem | CompositionItem | TextItem` — **6 of 8** item types now, not "only 3 of 8"); headless/api.ts:99→**:122** ⭐; gpu/compositor.ts:981→**:1130** (+:1174/:1205) ⭐; playback/player.ts:1038→**:1580** ⭐. The CORRECTIVE verdicts themselves stand. |
| §14 Corrections :2014-2180 | **SOUND** | Teacher-derived corrections; unchanged. |
| Testing section :2184-2268 | **SOUND** | Executable contract (Tier 1/2/3 + property + fixtures + commands); matrix rows live in 17. |

**Counts: P1 = 2 sections (§0's missing-law cluster; §6's missing bridge layer) · P2 = 7 · SOUND = 13.**

---

## §2 Findings on the seven focus items

**1. The venue model post-D12 — P1, the ratification note is drafted (F-2).** Engine-side D12 (`.agents/DECISIONS.md:521-584`, dated 2026-09-13) ratifies exactly what 19 §2.4 records spec-side: opencut-timeline is THE editing engine (SceneTracks SSOT + spec-15 wire dispatch); the engine's freecut-lineage stack (timeline.ts + TimelineData + the JSON-RPC wire + Player/scene-assembly/gpu-compositor/export) is the **render/export venue model** — venue-internal like an IR, NOT a second user timeline; the junction is **one-directional** (SceneTracks → bridge → venue stack, edits never flow back); engine-native edit verbs are **frozen as venue-internal**; the engine Timeline is NOT deleted — the offline reference + the r1 parity twin. 01 states NONE of this: §0's BASE narrative ends at the R24 re-pin, §1/§3.6 carry the pre-D12 "the engine is the backbone" framing. No contradiction — pure absence. The drafted note (F-2) is a dated insert.

**2. The bridge family — P1 on the module map.** The bridge is 12 files/4,645 LOC at `f9ac806` (⭐ wc -l), a fenced layer (4 of the 52 edges), and now contains the OV-01/OV-02 one-home leaf: `opencut-laws.ts` **deep-imports opencut's pure leaves** (`opencut-timeline/core/audio-params` clampVolumeDb/volumeDbToLinear/VOLUME_DB_MIN/MAX/DEFAULT + `ops/retime` clampRetimeRate behind the flat-number adapter — verified imports at opencut-laws.ts:52-58/:120), the volume-dB + retime twins are GONE, and the ticks law stays a replicated twin + drift fence (the branded-MediaTime cast problem, opencut-laws.ts:18-24). 01's only bridge text is §0's N1-N4/W1/W2/N2b/RR1-A seam narrative (pre-RC, accurate as history) + the D8 "app-level G-layer" framing — which **survives** (opencut-laws.ts:38-39: "NOT exported through the src/lib/nle/index.ts barrel… consumers deep-import per the D24e convention") but needs the RC-era amendment (the family's own one-home law now exists). §6's tree has no bridge at all.

**3. The test-state rows — P2.** 458/458/13-files → 748/748/25-files (evidence in §1c). Fence 52 **held exactly** (⭐); probes 9/9 (⭐ the 9 scripts: p114-undo, p2s, rb-p1, fxa, fx2, fx4, crb, crc, m30-fixture); the CI also gained the OV-12 `vendored-timeline` re-pin job (ci.yml:216-297 ⭐) — worth one clause in §0's fence sentence.

**4. The E1/E2 fix state — P2 (pointer only; 01 has no register to flip).** Verified: E1+E2 FIXED at `8188d4e`+`412693d` (commit read live — the Phase-1b companion splits, the ≥2-splits relink, the from<insertFrame exclusion, the fate-based overwrite companion law replacing DEAD CODE); the pin file `tests/vitest/engine/timeline-linked-source-edit.test.ts` (13 `it(`-sites, 26 runtime tests per the fleet's mock-leverage card); the F1a twin reconciliation kept their implementation as base; the **E1-a..d residue register** lives in the engine PLAN (E1-a companion-track downstream non-ripple; E1-b locked-companion closure gap — since narrowed by F1a-merged's lock>link delta + F1c's N3 fleet; E1-c N-group orphaning on relink; E1-d legacy originId fan-out absence); **E3 rides r1** (PLAN :251 "E3's INVALID_PARAMS error contract rides the r1 PORT; class-level empty return pinned"). 06:438 already carries the flipped row — 01 should POINT, not duplicate (F-9).

**5. The diagnostics law — cross-spec note only.** 01's only diagnostics surface is the OpenCut-classic `DiagnosticsManager` teacher sketch (§3.2/:117, Q2, §13) — a UI-side runtime-check registry, a different animal from WDC's EngineDiagnostic bus + heartbeat monitor + DspDiagnosedProcessor in-process() protocol + rt-safety-gate heritage fix (the corpus-wide fact #7). 01 has no engine telemetry/health section that is now false; the WDC law belongs to 02/20. Recorded in §4 — nothing to flip in 01.

**6. The op-surface — SOUND with a framing update.** The 19-op dispatch is verified still exactly 19 (⭐); the venue freeze (class methods off the RPC by design) and the r1 union façade are consistent with 01's §0 GAP row ("the 78-union service slice, JSON-RPC re-typed internal per D12.2 — r1"). §13A's "19-op JSON-RPC vs spec 15's 78-type union | CORRECTIVE" row needs the venue-freeze sentence + E3's rides-r1 status (F-2c/F-8), else a reader concludes the engine "owes" the union today.

**7. Pre-R26 pins/citations — spot-verified 8 cross-refs + 01's own engine cites.** Cross-refs verified: →06 (op inventory; 06:438's E1 flip accurate), →15 §4.1/§4.2 (78-union manager mapping, r1 design), →02 §8.6/§8.7 (workers delegation; engine zero-workers still true at `f9ac806`), →19 (§13A pointer; 19's own F-1 re-base in flight), →03 §5.2 (mediabunny probe), →09 §7.4 (thumbnail overload), →18 §6 (UI shell), →00 §2.5 (rename history). 01's OWN engine line pins (all re-grepped ⭐, consolidated with 19 §2A): timeline.ts:1980→:1894; :5225→:7321; core/types.ts:749→:1030 (+6-type quote); headless/api.ts:99→:122; compositor.ts:981→:1130; player.ts:1038→:1580; scene-to-segments :246/:611/:638→:596/:623 (+:207-214/:273); composition-frame :223→:257, :70-71→:80, :93-94→:106; api.ts:1509/:1515→:1529/:1535. The color-instruments GAP row's substance verified live (no scopes code; the two wire-schema strings at :1529/:1535 ⭐).

---

## §3 Fix-list (mechanically appliable)

- **F-1 (P1, §0 BASE row :13-14 + header :4).** Re-base the pin/count cluster: `nle-engine @ f9ac806` (docs-only head over code anchor `50b91f5`) — **748/748 vitest, tsc 0, 25 vitest files** (fence **52 held**, probes **9/9**, + the OV-12 `vendored-timeline` CI re-pin job); "Vendors OT @ `6e2b91a` (re-pinned c15a629→…→6e2b91a), WDC @ `ec8fd5c` (re-pinned 494f6ff→…→ec8fd5c by the S-series wave `480a216`)". Same row: refresh the 13-file enumeration to the 25-file list (§1c) or replace with "25 files; bridge-seams 151 + nle-bridge 99 + planner 52 + … (the R25-R27 waves: E1/E2, N3/N4, F5/F6, the RC round, M2 Wave 1)". Header :4: append "R27 re-audit: engine @ `f9ac806` 748/748 — the D12 venue ratification + the OV convergence absorbed (this round's fix-list)".
- **F-2 (P1, the D12 ratification note).** Insert after §0's BASE rows (before **GAP**), and add the italic sentence at §3's head:
  > **D12 VENUE RATIFICATION (ratified engine-side 2026-09-13, engine `.agents/DECISIONS.md:521-584`; spec-side record 19 §2.4, Decisions 11/12/16):** opencut-timeline is THE editing engine (the SceneTracks SSOT + the spec-15 wire dispatch). This engine's freecut-lineage stack (`timeline/timeline.ts` + `core/types.ts` TimelineData + the JSON-RPC wire + Player/scene-assembly/gpu-compositor/export over TimelineData) is the **render/export venue model** — venue-internal state like an IR, NOT a second user timeline. The junction is ONE-DIRECTIONAL: `SceneTracks → (bridge: scene-to-segments / transition-inputs / composition-frame) → the venue stack`; edits never flow back through a second wire. Engine-native edit verbs (overlap clamps, split bookkeeping, sync-lock ripple, E1/E2/F6 relink) are FROZEN as venue-internal (the engine's own test-runner/headless/persistence/Player venues); the engine Timeline model is NOT deleted — the offline reference implementation + the parity twin that pins the eventual r1 port (§0's union-façade GAP row). The 19-op headless dispatch (§13A) is the venue's frozen internal RPC — the class methods stay off it by design; **E3's INVALID_PARAMS error contract rides the r1 port** (the class-level empty-return abort is the pinned disposition, engine PLAN :251).
  
  §3 head sentence: *Post-D12 this spec's "engine" is the render/export venue — the editing engine is opencut-timeline (spec 05/15); the architecture below is the venue's internal design + the r1 façade's manager vocabulary.*
- **F-3 (P1, §6 module map).** Append to the §6 tree an engine-reality overlay:
  > **Engine-reality overlay (nle-engine `src/lib/nle/`, @ `f9ac806` — 63 files / 57,064 LOC):** the live tree carries `bridge/` — the D12 junction's permanent seam, **12 files / 4,645 LOC**: scene-to-segments (634, the flattener), transition-inputs (314), composition-frame (552, the venue painter + `buildElementFilterString` :257), av-link (203), scene-mixer (494), mixer-track-model (198), segment-strip-adapter (1,092), realtime-engine (234), conversions (228), fade-curve (174), scored-notes (400), **opencut-laws (122 — the OV-01/OV-02 one-home leaf: VALUE-imports opencut's pure leaves `core/audio-params` + `ops/retime`; the volume-dB + retime twins are GONE; the ticks law stays a replicated twin + bridge-seams drift fence; zero-import leaves only, never the barrel; bridge-internal, NOT in the D9-frozen barrel)**. `bridge/` is a fenced layer (bridge→audio, bridge→core, audio→bridge, export→bridge among the 52 pinned edges).
- **F-4 (P2, §0 line pins).** :13 "scene-to-segments.ts:246/:611/:638" → "scene-to-segments.ts:596/:623 (the `source.maintainPitch` threads; the segment type fields :207-214/:273)"; "composition-frame.ts:223" → ":257"; :26 D29.5(a) ":70-71"→":80", ":93-94"→":106"; :21 color-instruments GAP "headless/api.ts:1509/:1515"→"api.ts:1529/:1535".
- **F-5 (P2, §0 app row :14).** `nle-test-app @ c885ece — 174/174 (static census: …)` → `c020b2a — 252/252 (census 42 = 36 zero-action + 5 carriers + 1 host @ OT mirror 6e2b91a; AR-2 wire-coverage gate live; maintainPitch threaded at engineService.ts:510-513 + sceneBridge.ts:262/:447)`.
- **F-6 (P2, §0 D29.5(b) :27).** Replace "the residual is the app's own toggle wiring (owner S-app…)" with "**RESOLVED end-to-end (R9-b, R27)**: `el.preservePitch` is ElementJSON law (absent≡true; nle-ui `32abd58`); the app threads it (engineService.ts:510-513, sceneBridge.ts:262/:447); the engine's `retime.maintainPitch` (scene-to-segments :596/:623) is the runtime projection."
- **F-7 (P2, §0 landings paragraph).** Add one R25-R27 sentence to the BASE narrative: "Post-R24 engine landings absorbed by this audit: the E1/E2 linked-companion fixes (`8188d4e`+`412693d`, the 13-pin file — see 06 §0's flipped row + the engine PLAN's E1-a..d residue register; E3 rides r1); the N3/N4 guard fleets; the RC round (D12 ratification, OV-01/OV-02 one-home collapse, OV-05 one-input-source transitions, OV-12 CI re-pin gate, M2 Wave 1 bus-law + sidechain parity); the NS-1..NS-5 design queue + the r1-port collision map (engine-side filings — the plan should carry them, 00's row)."
- **F-8 (P2, §13A re-base).** Intro: "54,791 src/lib LOC, 458 vitest + 9 CI-wired engine probes @ `5036387`" → "57,064 src/lib LOC / 63 files, 748 vitest / 25 files + 9 CI-wired probes @ `f9ac806`". Rows: `timeline.ts:1980`→`:1894`; `timeline.ts:5225`→`:7321` (61 execute( sites — still 61 ⭐); `core/types.ts:749`→`:1030` + quote → `export type Clip = VideoClip | AudioClip | ImageClip | AdjustmentItem | CompositionItem | TextItem;` and note "6 of 8 item types now (Wave-4C P1.3 + text/adjustment)"; `headless/api.ts:99`→`:122`; `gpu/compositor.ts:981`→`:1130`; `playback/player.ts:1038`→`:1580`. Add the venue-freeze clause to the §3.6 row per F-2.
- **F-9 (P2, §0 one-line pointer).** After the BASE rows: "Defect-register pointer: the engine-side E1/E2/E3 filings + their R26 dispositions live in 06 §0's register (flipped FIXED) + the engine's `.agents/PLAN.md` (E1-a..d/E2-a residue register :221-231; E3 rides r1 :251) — 01 does not duplicate them."
- **F-10 (P3, §0 GAP row wording).** The union-façade GAP row may add "(post-D-ARCH-6: OT's op families include the batch verbs — `insertBatch`/`removeKeyframes`/`retimeKeyframes` — the graduation set is live; see 15's census rows)".

## §4 Cross-spec notes (recorded, not actioned)

1. **00/19:** 01's F-1/F-2 joins the fleet re-pin family (battery_r27 keys on the 748-count line); the D12 ratification note should be mirrored by 19 §2.4's dated stamp (19's F-9 already drafts it).
2. **02:** the zero-workers fact 01 §13A relies on held at `f9ac806` — 02's own re-stamp covers 01 (no separate edit).
3. **04:** `buildElementFilterString` :257 is 04's pin too (its report carries it) — same edit, two specs, no conflict.
4. **06:** 06:438's E1 FIXED row is accurate against the engine (commit + pins + residues verified this session — corroborates spec-06's audit).
5. **15:** the 78-union façade references in 01 (§0 GAP, §8) remain r1-design-coherent; the D-ARCH-6 batch verbs change 15's census rows, not 01's.
6. **20/02:** the WDC diagnostics law (EngineDiagnostic bus, heartbeat monitor, DspDiagnosedProcessor 256-quantum protocol, rt-safety-gate heritage fix) is missing corpus-wide (fact #7) — the natural engine-side pointer, IF the cross-cut wave wants one in 01, is a single sentence in §3.5's contracts list (a future `Diagnostics` adapter alongside Audio/Clock); recommend leaving to 02/20 (01's DiagnosticsManager is OpenCut-classic teacher material, a different concept — do not conflate).
7. **00 (plan):** the NS-1..NS-5 queue + the r1-port collision map (engine-side filings, `gaps/audit/ra-round/audit-AW1.md` §3) appear nowhere in the spec corpus — 01's F-7 records the pointer; the plan's S-engine row should carry them (00's business).

## §5 What 01 uniquely sees

1. **The manager-architecture vocabulary** — 01 is the only spec carrying the 12-manager/EditorCore design language; post-D12 this becomes the r1 union façade's dispatch vocabulary (15 §4.2's manager-method column maps onto it). No other spec can carry the D12 "venue internals" framing for the engine's own API — 01 is where it lands.
2. **The D9 freeze accounting** — 01's §0 is the corpus's only name-level audit trail of the frozen barrel (453→455 via `clampAuthorFadeToSpan` + `foldVolumeKeyframesToAutomation`, both verified present at `f9ac806`; the `buildElementFilterString` NOT-in-barrel ruling re-verified — the bridge's one-home leaf keeps the same discipline).
3. **The two-entry-point WYSIWYG contract** — 01 owns the interactive/render entry invariant; D12 re-frames both entries as venue internals (F-2), which no other spec states.
4. **The bridge family's spec-side home** — 04 (painter), 20 (audio seams), 19 (line pins) each carry slices; 01 owns the bridge as ARCHITECTURE (the junction + the module map + the fence edges) — the missing map row is uniquely 01's to fix.
5. **The engine's teacher lineage in full** (Q1-Q7 + §13 + §14) — immutable evidence that anchors WHY the venue model looks FreeCut-lineage; the D12 ruling's "the engine Timeline stays as offline reference + parity twin" (01 F-2) is the natural continuation of §13A's supersession-note style.
