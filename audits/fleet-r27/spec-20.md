# R27-W3-20 — spec-20 (audio core): the per-spec final-tightness audit

**Task ID:** R27-W3-20 · **Agent:** spec-20 · **Date:** 2026-09-13
**Spec:** `20-audio-core.md` (222 lines, v-next-3/R24 head — R25 filed + R26 stamped in-place; no version bump since) — audited SECTION BY SECTION.
**Ground truth (all re-derived live in this sandbox):** WDC `ec8fd5c` (clean checkout; manifest/lock/tests/line-pins re-read; the 777/777 + tsc 0 attested by scout-wdc's live run + the BRIEFING pin world), engine `f9ac806` (code anchor `50b91f5`; submodule status re-read: OT `6e2b91a` + WDC `ec8fd5c`), app `c020b2a`, OT `55c81c0`/`970948a`, nle-ui `32abd58`. Scouts: scout-wdc / scout-engine / scout-app in full; spec-02/spec-09/spec-00/spec-05/spec-04/spec-08 reports read for the cross-spec layer.

---

## §1 Per-section verdicts

| §site (line) | Verdict | Finding (evidence) |
|---|---|---|
| Header/Status :1-8 | **P2** | The version ledger head is v-next-3 (R24); R25's D36.3 filing + R26's EXECUTED stamp were folded into :25's W-row in-place (`git log 0fc7653`) with no v-next-4 entry. :8's "the live canon is the R24 pins: WDC @ `85b81b0` 759/759" is FALSE (777/777 @ `ec8fd5c`). The R27 entry must be drafted (FIX-7a). |
| §0 BASE — WDC row :15 | **P2** | Every count/pin stale: 759→**777**, 33→**34 test files**, lock `eeb3b24`→**`f5011b3`**, consumers engine `494f6ff`+app `85b81b0`→**both `ec8fd5c`** (engine `480a216`, app `9448667`; `git submodule status` both repos re-read live), "no pending porting duty"→the S-series WAS the duty, discharged BY `ec8fd5c` (+9,678/−489, 45 files). The BASE description carries NONE of the S-series surfaces (diagnostics/heartbeat, wam-instrument+state, the MIDI family, the catalog split, the instrument factory/registry/param-path) — the diagnostics omission is the corpus-wide fact #7, graded P1 under §11/FIX-4. The "17 DSP `EffectNode` implementations" figure: **22** exported effect classes at `494f6ff`, `387f327` AND `ec8fd5c` (`grep -c "export class .*Effect"` × 3 pins; 15 direct `implements EffectNode` + 3 `DspWorkletEffect` + 4 `WorkletEffect` subclasses) — stale at its own R24 pin (P3, FIX-7d). |
| §0 BASE — engine row :16 | **P2** | 458→**748** @ `f9ac806`; "nle-bridge.test.ts still 96"→**99** (static count re-derived); the R27 audio movement absent (G1 the test ports, FW-C D7/D11, OV-01/OV-02, **M2 Wave 1** `50b91f5`). |
| §0 BASE — app row :17 | **P1** | Two live-FALSE clauses: (a) "`rewireSidechains()` after every `materializeStrips` — sever-all zero-arg + `collectSidechainWires`, the shared contract, the helper's ONE live consumer" — **DELETED** at M2 Wave 1 (`1aebbd5`; the replacement note sits at `audioService.ts:319-322`; the BRIDGE's `materializeStrips` is now the one consumer, `scene-mixer.ts:222-225`); (b) "the offline path skips worklet-backed inserts" — **FALSE** since the §1a flush-args law (`deliverService.ts:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0`; `WORKLET_BACKED_INSERT_TYPES` :170; pins `deliverService.test.ts:429-471`). Pin/count 3 waves stale (174/`c885ece`→252/`c020b2a`). FIX-3a. |
| §0 GAP — M2 mixer surface :20 | **P1** | "**the engine's sidechain wiring does not exist yet**" is FALSE: M2 Wave 1 (`50b91f5`) landed it — `collectSidechainWires` consumed INSIDE `materializeStrips` after `applySettings` (`scene-mixer.ts:222-225`, `:314-339`), the per-wire IDENTITY-DIFF sever law, both consumption paths (realtime + `renderOffline`) build through it — "the duck the user hears is the duck the user exports" (previously the offline path NEVER wired sidechains). The row needs the Wave-1 fold to BASE + the Waves 2-3 remainder re-split (the app's wave map: `docs/design-m2-mixer-surface.md` v1.1 — Wave 1 bridge laws+venue / Wave 2 authoring surfaces / Wave 3 PDC, read live). FIX-3b. [scout-engine P1 row] |
| §0 GAP — the FIVE test ports :21 | **P1** | "none ported yet — re-verified R24, no such bodies in `nle-engine/tests/vitest/`" is **FALSE**: G1 (`3a28b6e`, inside the R27 range) landed `tests/vitest/upstream-ports.test.ts` — all FIVE bodies (engine.test / engine-metronome-automation / engine-param-channel / dsp-aux-sends / wam-effect) re-expressed against the bridge's composition, 25 its porting 42 upstream laws, **"5 upstream files, 106 its total → 42 PORTED + 6 ALREADY-COVERED + 58 NOT-PORTABLE (reasoned) — nothing silently dropped"** (the file's own disposition table, `upstream-ports.test.ts:223-226`). **Not in any scout register** — the spec-20-owned flip. FIX-1. |
| §0 GAP — Mixer G-surface :22 | **P2** | "today: `bridgeSceneSettings` materializes unity faders + solo only" still TRUE for the ENGINE's default path (re-verified: `conversions.ts:201-233` — `defaultMixerTrackSettings`, unity faders, solo only) but the sentence now under-states the landed G-slice: the APP's `toMixerSceneSettings()` materializes real inserts (Comp/Gate/De-esser/EQ) + buses with volume+muted (`audioService.ts:146/:175-176`). Re-scope the honesty note to "the engine's DEFAULT `bridgeSceneSettings`" (FIX-3e). |
| §0 GAP — A4-v2 NULL rig :23 | SOUND | Still open, accurately stated (no realtime-vs-offline e2e null rig exists — grep-verified; the strongest gate 29.1 compares two offline recipes). |
| §0 GAP — async pre-retime :24 | SOUND | Still open in BOTH owning queues (engine G2 / WDC HANDOFF item 3 — both re-read live); the r2 phasing + acceptance accurate. |
| §0 GAP — waveform seam contract (the W-row) :25 | **P2** | The R26 EXECUTED stamp's facts hold but the R27 tail is missing: the contract SURVIVED the S-series refresh byte-verbatim (audio-registry disk hash `fc5168f6eef91684` == lock pin `UPSTREAM.lock.json:18`; `waveform-contract.ts:44/:47` + the 14 pins re-read green inside the 777; the module header still cites "spec 20 §0:25"); the count 773→**777**; the app consumer pin `387f327`→**`ec8fd5c`**. Mechanical (FIX-6c). The K3-prerequisite clause "the only K3 sub-half with an external prerequisite" is now SPENT — the prerequisite LANDED, and :26's row LANDED with it. |
| §0 GAP — K3's audio pins :26 | **P1** | **LANDED, unflipped**: app `44e8049` — `src/waveformPeaks.ts` programs against WDC's EXPORTED contract names (`WAVEFORM_PEAKS_BUCKETS`/`WaveformPeaks` from `@/lib/daw/waveform-contract`; `getPeaks`/`setAudioPeaks`/`computePeaks` from `@/lib/daw/audio-registry` — the AR-2 consumption law) + `waveformPeaks.test.ts` **9 pins** (the contract shape; the 2000 drift tripwire; the read-through cache identity; the independent min/max re-derivation; mediaTone's real virtual-audio specs through the seam; the pre-computed setAudioPeaks law; the distinct-media keying; the empty-buffer 0-law; the never-a-copy grep tripwire — all read live). The mute-law half pinned in `audioService.test.tsx:104-195` + `GluedShell.test.tsx:1289` (S3-C2). Acceptance ("the pins green in the app's vitest") MET at 252/252. The VLM net — the OTHER half of the plan's item 5 — remains open. FIX-2. |
| §0 GAP — worklet serving :27 | **P2** | The mechanism is superseded, the acceptance is not: the ENGINE's venue is now the `public/worklets` git **SYMLINK** → `../vendor/web-daw-core/public/worklets` (`50b91f5`, `ls -la` re-read); the APP's venue is the vite `serve-vendored-worklets` plugin (dev middleware + `closeBundle` dist copy, **zero byte duplication** — `vite.config.ts:38/:45/:57`; the app's `public/` still does not exist, live-verified). "The 3 files … copied to the app's `public/worklets/`" is no longer the plan of record; what remains K3 is the real-BROWSER venue pin (spec-02's FIX-3 owns the 02-side wording; 20's row mirrors it). FIX-6d. |
| §0 retired rows + register :28-30 | SOUND | The N2b/W2 strikethroughs accurate; the posture law + the plan pointer accurate. |
| §0 ACCEPTANCE :32 | **P2** | "BASE acceptance = the cited suites at the cited pins" — the pins are the R24 set (rides FIX-6). |
| §1 Purpose :36-40 | SOUND | The not-owned list's cross-refs re-verify live: 03 §3 (Clock, `03:67`), §9.2 (streaming chunks, `03:925`), §8 (Varispeed worklet, `03:773`). |
| §2 Why web-daw-core :44-48 | SOUND (+P3) | The capability-gap paragraph's "20+ DSP effects" holds; "17 DSP `EffectNode` implementations" is the one stale figure (22 at all three pins — see :15 row). The AudioMixer tombstone language correctly historical. |
| §3 track model :52-70 | SOUND (+P3) | No S-series delta touches the S/G/E layering (scout-wdc §3 "not impacted" — re-verified: the bridge files untouched by the engine's R27 range; `docs/track-model.md` is normative-adjacent per 00:485). P3: the engine's D12 ratification (`1064f91`, DECISIONS.md:521-584) deserves a one-line dated note at the seam-law paragraph (the junction's one-directionality is now bilateral law). |
| §4.1 Layer S :76-98 | **P2** | The field table + merge consequences all re-verify (scene-to-segments read live at `f9ac806` — `maintainPitch` :508, `volumeDb` fold :509-511, `gainAutomation` :512). MISSING: the `volumeDb` row's **domain coherence row** — the [−60, +20] domain, the ONE HOME (opencut `core/audio-params`, deep-imported by `opencut-laws.ts:54-79`, `clampVolumeDb`/`VOLUME_DB_MIN/-MAX`/`DEFAULT_VOLUME_DB` re-exported), NaN→DEFAULT-then-read-as-absent, absent≡0 dB≡unity. Spec-09's B2 rewrite (spec-09.md:107/:166) expects exactly this row — one author should land 09 B2 + 20 §4.1 together. FIX-8. |
| §4.2 Layer G :100-106 | **P1** | The landed bus-law is missing: `AuxBusSettings` gains the additive-optional **`muted?: boolean`** (`mixer-track-model.ts:89-102`, read live: "the bus mute folds into the return gain — the write law is `muted ? 0 : clamp(volume, 0, 1)`, upstream's own `applyAuxBusGain`"); bus return gains write through upstream `applyAuxBusGain` — **one law, three consumers** (DAW `syncAuxBuses` / the offline renderer / the NLE bridge; `scene-mixer.ts:277-289`); the raw `gain.value = volume` write retired. §4.2's shape line still says "per-bus 1..4: return gain + return chain" — add muted. FIX-3c. |
| §4.3 Layer E :108-110 | P3 | Accurate but understated (scout-wdc's P3 row): the tree now carries the full per-channel MIDI state machinery (`midi/MidiChannelState` 128-CC Int8Array + 14-bit pitch wheel, NRPN/SysEx GS/XG/GM2 decoding, drum-params, the boot-pinned spessasynth constants behind the async boundary, `attachMidiState()` at `instrument-interface.ts:36`) — the natural W7/M3 upstream reference; one registering sentence, no scope change (Decision 13's deferral stands). |
| §5 bridge laws :114-125 | **P1 (+P2/P3)** | The pin list + laws 1-8 re-verify (H1/H1b/H8/W2/N2b all live at `f9ac806`); but **the M2 Wave 1 laws are missing entirely**: (9) the F-1 bus return-gain law; (10) the F-2 single-owner sidechain parity law (the per-wire identity diff, the after-applySettings re-collect — duck params change wires WITHOUT strip rebuilds); (11) the worklet-flush law (worklet-backed inserts need a macrotask flush before `startRendering`; `renderOffline`'s `workletFlushMs ?? 50` default, `scene-mixer.ts:444-448`; the app's 250-when-present). Also: the pin-home list lacks `tests/vitest/m2-wave1-mixer-surface.test.ts` (**9 pins** read live: F-1 ×4 incl. the additive-optional + re-materialization laws; F-2 identity ×3; F-2 parity ×2 — the REAL offline duck + the no-declaration control); "96 bridge tests … re-verified still 96" → **99**. P3: the RC-V1 residuals on the N2b row (the flattener ramps LINEARLY between bezier keys — mid-segment audio can deviate from the drawn envelope; the `conversions.ts` `dbToLinear` replica is a conscious decline, venue-internal per D12). FIX-3d + FIX-6e. |
| §6 M1.5 convergence :129-139 | **P2** | :133 "worklet bundles served from the app's `public/worklets/`" — stale venue (see :27 row). The rest re-verifies: the bridge relocation facts, the retirement rows (AudioMixer deleted `20fa266`, direct-mix retired `abdf9ee` — tombstones stand), the export Stage-2 law. NEW consumption fact missing: the vendored **ambient declaration** (`vendor/web-daw-core/src/types/spessasynth-core.d.ts`) must be in every consumer's tsconfig `files` or TS2307+TS2366 fire (engine `480a216` + app `9448667`; app `tsconfig.json:45`) — the pin-law sentence (scout-wdc's 00 D15 row, P3-grade; fold into FIX-6d). |
| §7 M2/M3 roadmap :143-153 | **P1** | :145's parenthetical ("the helper is the CORE's, its one live consumer is the app's U9 rewire — NOT an engine surface; **the engine's sidechain wiring does not exist yet**") is FALSE on both clauses (the bridge is the consumer; the wiring EXISTS). :147's honest-gap note needs the Wave-1 nuance (see :22). :149's M2 REMAINDER (1) "the 4 deferred upstream test ports … r2" — LANDED as G1; (2) the mixer surface needs the Wave-1 fold; (3) the async pre-retime stands. :151's R24 stamp + :153's M3 paragraph SOUND (the WAM/`signalLatency` PDC contract untouched by the S-series). FIX-3e. |
| §8 wire surface :157-159 | SOUND | No M2 Wave 1 verb or protocol shape landed (the bus/sidechain laws are materialization-side, not wire-side) — the section's "adds no protocol shapes of its own" holds; 15 §4/§6/§13.15 cross-refs verified. |
| §9 boundaries :161-169 | **P2** | The 02 row ("worker/abstraction discipline | the audio worklet specifics that run under it") is the corpus's ONLY 20→02 link, and 02 has ZERO references back (grep "spec 20\|20-audio" over `02-workers-threading.md` = empty) — **the two audio specs are effectively unlinked** while they now share the diagnostics/heartbeat + rt-safety + worklet-venue laws. Upgrade the row to the two-way §-level link (FIX-4c). |
| §10 testability :172-189 | **P2** | :184 "all green (759 suite — WDC @ `85b81b0` …)" → 777 @ `ec8fd5c` (+ the R26/R27 supersession chain note). MISSING facet rows: the M2 Wave 1 family (bus return-gain law; sidechain single-owner parity incl. the offline-duck REAL-DSP pin; the flush-args law) — the natural §13A additions; the diagnostics-protocol rows are 02's FIX-1 note (17 §13A's "AudioWorklet message protocol" facet at 17:546 gains the heartbeat/NaN/silence messages). |
| §11 code references :191-215 | **P1 ×2 (+P2)** | (a) **:209 "`src/lib/daw/instruments/*` (shims)" is FALSE** — the dir is 7 shims + **3 verbatim upstream copies** (`instrument-factory.ts` 201L / `instrument-registry.ts` 251L / `param-path.ts` 183L — manifest `copy` rows re-read live; the L4/L5 synth-seam split + the §1.3 dotted-path grammar) [scout-wdc P1]. (b) **The diagnostics/heartbeat row is MISSING** — a landed observability law with zero spec-20 trace (the `EngineDiagnostic` bus `diagnostics.ts:1-57`+, the `DspDiagnosedProcessor` mixin `dsp-effects-worklet.js:315-372`, the stall bypass-to-passthrough ladder `effects.ts:441-507`, the rt-safety-gate `scripts/rt-safety-gate.ts:140-187`) [briefing fact #7; scout-wdc P1]. P2s: :202 "33-file closure" → the manifest census (**copy 93 + copyRepoRoot 4 + shims 11 + coreOwned 14** — recomputed live from `extraction-manifest.json`); the S-series closure families missing rows (midi/*, wams/wam-instrument+wam-state, sample-catalog-core + the IR JSON, library-audio-proxy, stall-race, spessasynth-core.d.ts); :211 "96 bridge tests" → 99 + the m2-wave1 + upstream-ports test homes. SOUND: :206 audio-registry pins EXACT at `ec8fd5c` (PEAK_BUCKETS :20, getAudioBuffer :24, getPeaks :48, setAudioPeaks :58, computePeaks :74 — re-read live); :208 the D24e barrel law (still zero importers — re-verified via both consumers' deep-import aliases); :210/:212-214 (the derisk home, the harness, the sync mechanism) hold. |
| §12 open questions :217-222 | **P2 (+SOUND)** | §12.1 accurate (the N2b precedent + `model/automation.ts` upstream reference); §12.2's premise "the scene-level helper must choose a sidechain source track" is half-landed (the ducking sidecar IS the scene-level helper; the strips' own declarations are collected bridge-side — the remaining question is the spec-18 mixer-panel UX); §12.3/§12.4 SOUND (the C9 closure verified). |

**Totals: P1 ×6 (the app BASE row's dead clauses; the M2 Wave-1 bus/sidechain/flush cluster missing across §0/§4.2/§5/§7; the FIVE-test-ports row FALSE; the K3-audio-pins row unflipped; the instruments "(shims)" label FALSE; the diagnostics/heartbeat law missing) · P2 ×13 · P3 ×6.** No settled D-law needs re-litigation; every fix is additive or mechanical.

---

## §2 The fix-list (exact edits, for the amendment wave)

### FIX-1 (P1) — :21 flip the FIVE-test-ports row to LANDED

Replace the row's tail ("…; none ported yet — re-verified R24, no such bodies in `nle-engine/tests/vitest/`). WDC+engine; **r2** (acceptance: the ported bodies green in the engine's vitest).") with:

> "— ~~none ported yet~~ **LANDED R27 (engine G1 @ `3a28b6e`)**: `nle-engine/tests/vitest/upstream-ports.test.ts` — all FIVE bodies (engine.test / engine-metronome-automation / engine-param-channel / dsp-aux-sends / wam-effect) re-expressed against the relocated bridge's composition surface (SceneMixer + ChannelStrip + master-chain + aux buses), with the honest per-it disposition table in the file header: *5 upstream files, 106 its total → 42 PORTED (by the file's 25 tests) + 6 ALREADY-COVERED + 58 NOT-PORTABLE (each reasoned — e.g. the metronome click has no NLE surface; the reactive-host contract has no bridge analog)* — nothing silently dropped. 670/670 at landing; green in the engine's vitest at `f9ac806` (748). Flipped to BASE; WDC's own HANDOFF item 2 still carries the stale '4 deferred' row (the S-series docs wrap never landed — scout-wdc §2; cross-spec note)."

### FIX-2 (P1) — :26 flip the K3-audio-pins row to LANDED

Replace the whole row with:

> "- ~~**K3's audio pins**~~ — **LANDED (app @ `44e8049`, the R26 promotion's unblocked execution + the M2 Wave 1 pins @ `1aebbd5`)**: `src/waveformPeaks.ts` programs against WDC's **EXPORTED contract names** — `WAVEFORM_PEAKS_BUCKETS`/`WaveformPeaks` from `@/lib/daw/waveform-contract`, `getPeaks`/`setAudioPeaks`/`computePeaks` from `@/lib/daw/audio-registry` — never a copy (the AR-2 live-registry consumption law); the read-through cache (registry get → core computePeaks at the CONTRACT constant → registry set), the mediaId→clipId keying. `src/waveformPeaks.test.ts` — **9 pins** (the contract shape; the constant-as-tripwire; the read-through cache identity; the independent min/max re-derivation; mediaTone's real virtual-audio specs through the seam; the pre-computed setAudioPeaks law — render-before-audio-init; the distinct-media keying; the empty-buffer 0-law; the never-a-copy grep tripwire). The mute laws pinned in `audioService.test.tsx` (the D24c derive + the M2 bus-mute folds :122-143) + `GluedShell.test.tsx` S3-C2 (toggleMuteAll one-entry); the deliver-flush pins at `deliverService.test.ts:429-471`. **Acceptance MET** — the pins green in the app's vitest (252/252 @ `c020b2a`). The VLM net — the other half of the plan's S-app item (5) — remains open (K3)."

### FIX-3 (P1) — the M2 Wave 1 amendment cluster (six coordinated edits)

**(3a) :17 app BASE row** — re-pin to `c020b2a` 252/252 + replace the U9 clause's tail and the deliver clause:

> "…**U9 sidechain ducking** (W2.1 @ `efd8243`; re-shaped by M2 Wave 1 @ `1aebbd5`: the app's `rewireSidechains()` DELETED — the BRIDGE (`SceneMixer.materializeStrips`) collects + connects the duck wires through the shared `collectSidechainWires` contract on BOTH paths, the single-owner law; `insertEffects` still maps the ducking sidecar → the external-sidechain compressor insert (−30 dB, ratio 1+amount×7); the live-wire pin + the 5 U9 pins now read the tap's connection graph as the witness) + **the deliver export's audio leg** (W2.4 @ `278b95b` + the M2-§1a flush law: `deliverService.ts:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0` — a worklet-backed insert whose module load races `startRendering` renders PASSTHROUGH, so the offline path FLUSHES when the family is present; the app-reachable family = compressor/gate/deesser + the registry-reachable limiter) + **the worklet VENUE fix** (the vite `serve-vendored-worklets` plugin, `vite.config.ts:28-60` — dev middleware + `closeBundle` dist copy of the vendored worklets, zero byte duplication; previously nothing served `/worklets/dsp-effects-worklet.js`, so every worklet-backed insert silently degraded to passthrough, monitor AND export)."

**(3b) :20 M2 mixer-surface row** — replace the attribution parenthetical + the r2 tail:

> "…external sidechain ducking (BGM under dialogue — **the scene-level helper + the ENGINE-side wiring LANDED as M2 Wave 1 @ `50b91f5`**: `collectSidechainWires` consumed INSIDE `materializeStrips` after `applySettings` (`scene-mixer.ts:222-225`), the per-wire IDENTITY-DIFF sever law (`:314-339`) — a re-materialization severs exactly the vanished/identity-changed wires, live wires survive param-only syncs; BOTH paths (realtime monitor + `renderOffline`) build through it — *the duck the user hears is the duck the user exports*; 9 pins in `tests/vitest/m2-wave1-mixer-surface.test.ts` incl. the REAL offline-duck DSP pin), PDC coordination across strips, automation shapes (the VOLUME lane landed as N2b; the other param surfaces remain — §12.1). WDC(+engine); **r2, Waves 2-3** (Wave 1 = the bridge laws + the venue, LANDED; Wave 2 = the authoring surfaces — the bus reverb mapping + the EQ param surface + the `el.effects` sidecar mapping decision F6; Wave 3 = PDC; the app's `docs/design-m2-mixer-surface.md` v1.1 is the wave map) (acceptance: offline parity pins ≤ −60 dBFS any channel + realtime behavioral pins)."

**(3c) §4.2 :102** — extend the shape sentence:

> "Scene-level: `tracks[]`, `buses: AuxBusSettings[]` (per-bus 1..4: `index`/`name?`/`volume` 0..1/`muted?` — the additive-optional M2-Wave-1 field/`effects` the return chain), `masterVolume`." + append after the H11d paragraph: "**The bus return-gain law (M2 Wave 1, F-1):** bus gains write through upstream `applyAuxBusGain` — `muted ? 0 : clamp(volume, 0, 1)` — ONE LAW, THREE CONSUMERS (the DAW engine's `syncAuxBuses`, the offline renderer, the NLE bridge); the raw `gain.value = volume` write is retired. Buses start SILENT in the engine (gain 0) — the mixer opens them explicitly (H4)."

**(3d) §5** — append two laws after item 8:

> "9. **Bus return gain (M2 Wave 1, F-1):** the bridge's `wireBuses` writes every bus gain through the shared upstream law (above) — muted reads 0 on the gain node and WINS over volume; out-of-range volumes clamp at the write site; the absent `muted` keeps the legacy meaning (additive-optional); re-materialization re-applies the fold live. Pinned by m2-wave1 F-1 (4 pins).
> 10. **Sidechain single-owner parity (M2 Wave 1, F-2) + the worklet-flush venue law:** the sidechain wires are collected from the strips' OWN declarations and connected INSIDE `materializeStrips` (after `applySettings` — duck params change wires without strip rebuilds), with the per-wire identity diff as the sever law; realtime and offline share the ONE wiring — an offline export can never be silently duck-inert. Worklet-backed inserts need a macrotask flush before `startRendering` (the SKILL §3 port-message law): `renderOffline` defaults `workletFlushMs` 50; the app's deliver path passes **250 when the worklet-backed family is present, 0 otherwise** (the fast path preserved). Pinned by m2-wave1 F-2 (5 pins) + `deliverService.test.ts:429-471` (the flush-args law — the jsdom venue pins the ARGUMENTS; the DSP amplitude half lives in the engine's venue)."

Also update §5's pin-home sentence: "96 bridge tests at the R23 pin, grown from the R15-era 37; re-verified still 96 at the R24 pin `5036387`" → "…99 at the R27 pin `f9ac806` (+3: the M2-era bus/sidechain laws live in `m2-wave1-mixer-surface.test.ts`; the FIVE-body ports live in `upstream-ports.test.ts`)".

**(3e) §7 :145-149** — replace :145's parenthetical with the Wave-1 clause (same text as 3b's core) + re-scope :147's honesty note to "the ENGINE's DEFAULT `bridgeSceneSettings` materializes unity faders + solo only (`conversions.ts:201-233` — verified R27); the app's A4 G-slice (`toMixerSceneSettings`, `audioService.ts:146`) carries the real inserts + buses; the M2 Waves 2-3 remainder is the authoring surfaces + PDC" + flip :149's remainder item (1) to LANDED-per-G1 (same text as FIX-1).

**(3f) §10** — add the facet rows:

> "| Bus return gain (M2 W1 F-1) | F | T1 | `m2-wave1-mixer-surface.test.ts` (4 pins) | muted→0 wins; clamp at write; additive-optional; live re-fold |
> | Sidechain single-owner parity + flush args (M2 W1 F-2/§1a) | F | T1 | m2-wave1 (5 pins: connect/re-point/prune + the REAL offline duck + control) + `deliverService.test.ts:429-471` | offline export = realtime monitor (no silent duck-inertness); flush 250-when-present / 0 fast path |"

### FIX-4 (P1) — the diagnostics/heartbeat registration (the corpus-wide fact #7, audio-side)

**(4a) §11 file-map rows** (insert after the channel-strip row):

> "| `src/lib/daw/diagnostics.ts` (S-series, manifest copy) + the `DspDiagnosedProcessor` mixin (`public/worklets/dsp-effects-worklet.js:315-372`) + the bypass ladder (`effects.ts:441-507`) | the audio-thread observability law: the `EngineDiagnostic` bus (7 kinds: bootstrap-failed/node-create-failed/processor-error/nan-output/silence-suspected/heartbeat-stalled/recovered; console-silent; WeakMap context binding), the off-thread heartbeat monitor (armed-by-first-beat; a stall fires only while `isRunning()`; stall >3 s → **bypass-to-passthrough** — audio keeps flowing), the in-`process()` protocol (256-quantum heartbeats, NaN/silence scans, ONE preallocated message object). The THREADING-side law text is spec 02 §7.4 (this round's 02 amendment) — the two rows cite the same files/lines |
> | `scripts/rt-safety-gate.ts` (S-series fix) | the static hot-path analyzer over every `process()` transitively reachable — **seeds = classes whose heritage reaches `AudioWorkletProcessor` DIRECTLY OR TRANSITIVELY through LOCAL base classes** (the mixin made the direct-only match find 0 seeds — a VACUOUS PASS; the cycle-safe `isWorkletSubclass` walk fixed it, `rt-safety-gate.ts:140-187`); lethal (exit 1), TS sources AND built bundles both analyzed |"

**(4b) §0 BASE WDC row** — append one sentence to the S-series re-pin (FIX-6a): "the S-series upstream wave (19 locked files refreshed + the 18-file closure extension: the MIDI family, wam-instrument+state, the instrument factory/registry/param-path, the catalog split, library-audio-proxy, stall-race, the ambient type seam) **incl. the diagnostics/heartbeat law — §11's new rows; the threading-side statement is 02 §7.4**."

**(4c) §9 boundaries 02 row** — upgrade to the two-way link:

> "| 02 workers | the worker/abstraction discipline + **§7.4 the audio-thread observability & realtime-safety law** (the EngineDiagnostic bus, the in-`process()` protocol, the stall bypass ladder, the rt-safety-gate — WDC `ec8fd5c`) | the audio worklet specifics that run under it — §11's diagnostics/rt-gate rows + the worklet venue laws (§0's worklet-serving row, §6.1) |"

### FIX-5 (P1) — :209 split the instruments row

Replace "| `src/lib/daw/instruments/*` (shims) + `setInstrumentProvider` | provider injection seam (Phase B upstreaming) |" with:

> "| `src/lib/daw/instruments/*` — 7 shims (mini-synth, provider, fm-synth, sampler, sf2-sampler, hq-sf2-sampler, subtractive-synth) + `setInstrumentProvider` | provider injection seam (Phase B upstreaming); the shim defaults are upstream-canonical (the S-series alignment round caught 3 real default drifts via the factory's module-load cross-check) |
> | `src/lib/daw/instruments/{instrument-factory, instrument-registry, param-path}.ts` (S-series, manifest `copy` — 3 verbatim upstream files, NOT shims) | the L4/L5 synth-seam split: the registry holds pure param metadata (L5-safe, no class imports), the factory dispatches construction + the module-load cross-check, `setParamPath` implements the dotted-path grammar (plain key = byte-identical spread; numeric segment indexes arrays only when the parent IS one; collision/malformed → unchanged + warn-once, never a throw at tick rate) |"

### FIX-6 (P2) — the mechanical re-pin batch

- **6a :15** — "759/759, 33 test files" → "777/777, 34 test files"; "UPSTREAM.lock `eeb3b24`, no pending porting duty" → "UPSTREAM.lock `f5011b3` (the S-series wave WAS the pending duty — discharged by this commit)"; "consumers: engine @ `494f6ff`, app @ `85b81b0`" → "consumers: engine AND app both @ `ec8fd5c` (engine `480a216`, app `9448667` — the docs-wrap never landed in WDC, so the repo's own HANDOFF/PLAN still say 759/'4 ports'/coreOwned 5; scout-wdc §2 files the docs-wrap queue item)"; append FIX-4b's sentence.
- **6b :16** — "458/458 … `5036387` … nle-bridge.test.ts still 96" → "748/748 … `f9ac806` (code anchor `50b91f5`) … nle-bridge.test.ts now 99; vendors WDC `ec8fd5c` + OT `6e2b91a`"; append "R27 movement: G1 (the FIVE-body test ports), FW-C (D7 volume-dB [−60,+20] amendment + D11 the retime-domain lattice), OV-01/OV-02 (opencut-laws value-imports the ONE-HOME leaves), M2 Wave 1 (the bus law + sidechain parity + the worklet venue)".
- **6c :25** — append the R27 tail: "**R27 survival note:** the contract survived the S-series refresh byte-verbatim (audio-registry disk hash == the new lock pin `f5011b3`-era entry; the 14 pins green in the 777; the consumer pin is now `ec8fd5c`)" + fix "773/773" → "777/777 (R27)" in the stamp's count only where it states the live figure (the historical 773 stays in the R26 record).
- **6d :27 + :133** — the venue rewrite per the :27 verdict row: "the 3 files … copied to the app's `public/worklets/`" → "served from the VENDORED tree at the `/worklets/` root-base paths — the ENGINE's venue is the `public/worklets` git symlink to `../vendor/web-daw-core/public/worklets` (M2 Wave 1); the APP's venue is the vite `serve-vendored-worklets` plugin (dev middleware + dist copy, zero byte duplication; the app's own `public/` still does not exist — live-verified R27); the varispeed worklet still needs NO asset (blob-URL). **Consumer pin-law:** every consumer adds `vendor/web-daw-core/src/types/spessasynth-core.d.ts` to its tsconfig `files` (else TS2307 + cascading TS2366 from the vendored MIDI modules). app; **K3** (acceptance unchanged — the browser paths load + the strips' worklet effects run app-side; the remaining pin is the real-BROWSER venue — spec 02 §7/FIX-3's wording)". Mirror the same one-liner at :133.
- **6e :116 + :211** — the 96→99 count (in both §5's pin list and the §11 row) + add the two new test-home rows (`m2-wave1-mixer-surface.test.ts`, `upstream-ports.test.ts`) to §11.
- **6f :184** — "all green (759 suite — WDC @ `85b81b0` …)" → "all green (777 suite — WDC @ `ec8fd5c` …)" + extend the supersession bracket.
- **6g :202** — "verbatim, 33-file closure" → "verbatim — the manifest census at `ec8fd5c`: 93 `copy` + 4 `copyRepoRoot` + 11 shims + 14 `coreOwned` (the S-series 18-file closure extension grew copy 75→93; see `extraction-manifest.json`)".
- **6h :8** — "the live canon is the R24 pins: WDC @ `85b81b0` 759/759" → "the live canon is the R27 pins: WDC @ `ec8fd5c` 777/777 (see §0)".
- **6i §12.2** — re-word the premise (the sidecar helper landed; the open question is the spec-18 mixer-panel UX + the W7-era source-selection surface).

### FIX-7 (P3) — polish

- **7a** Draft the v-next-4 status entry (Round 27 — the fleet audit: the R27 re-pins BASE — WDC 777/777 @ `ec8fd5c` (the S-series + the waveform-contract survival), engine 748/748 @ `f9ac806`, app 252/252 @ `c020b2a`; the M2 Wave 1 laws folded; the FIVE ports + the K3 audio pins flipped LANDED; the diagnostics/heartbeat registered; the instruments row split).
- **7b** §3's seam-law paragraph: add the dated D12-ratification note (engine `1064f91`, DECISIONS.md:521-584 — the junction is one-directional law BOTH sides now).
- **7c** §4.3: one sentence registering the vendored MIDI-state family as the W7/M3 upstream reference (`midi/*` — MidiChannelState, NRPN/SysEx, drum-params, spessa constants behind the async boundary).
- **7d :15** — "17 DSP `EffectNode` implementations" → "22 exported effect classes (15 CPU DSP + 3 `DspDiagnosedProcessor`-backed + 4 worklet-family; unchanged at the last three pins)".
- **7e** §5.8's N2b law: append the two RC-V1 residual notes (the bezier→linear flattener divergence — boundary-eval parity with upstream's treated-as-linear, mid-segment audio can deviate from the drawn envelope, registered not blocking; the `conversions.ts` `dbToLinear` replica = conscious decline, venue-internal per D12).

### FIX-8 (P2) — §4.1's `volumeDb` coherence row (the 09-B2 pairing)

Extend the row's cell:

> "| `volumeDb?` | folded clip+track volume in dB (Wave-5A P1.4; converted to a linear `level` on the segment) — **the domain is [−60, +20], ONE HOME: opencut `core/audio-params` (`VOLUME_DB_MIN/-MAX`, `DEFAULT_VOLUME_DB = 0`, `clampVolumeDb`, `volumeDbToLinear`) — the engine bridge deep-imports the leaves (`opencut-laws.ts:54-79`, OV-01/02); NaN → DEFAULT at the clamp, read-as-absent at the fold; absent ≡ 0 dB ≡ unity; host-built sources clamp the same domain in defense-in-depth (`scene-to-segments.ts:509-511`)** | **a volume boundary never merges** (the 0.0001 dB law) |"

(Cross-spec: spec-09's B2 row — one author lands 09 B2 + this row together; spec-09.md:107/:166 already reserves the pairing.)

---

## §3 The focus-item rulings (the brief's eight)

1. **The scout-wdc P1 set** — (a) :209 CONFIRMED FALSE (7 shims + 3 copies, manifest-verified — FIX-5); (b) the diagnostics law missing CONFIRMED (FIX-4 registers it audio-side; 02's §7.4 draft owns the threading text; the rows must cite the same files/lines: `diagnostics.ts:1-57`+, `dsp-effects-worklet.js:315-372`, `effects.ts:441-507`, `rt-safety-gate.ts:140-187`); (c) the count re-pins 759→777 land at :4/:8/:15/:25/:184 (FIX-6) + 34 files + the 19→`src/test/`-files fact (12/17's rows — cross-spec).
2. **The M2 mixer bus-law** — CONFIRMED the P1 cluster; the amendments drafted as FIX-3 (six coordinated edits: the app BASE row's two dead clauses; the GAP row's Wave-1 fold + Waves 2-3 re-split; §4.2's `AuxBusSettings.muted` + the F-1 law; §5's laws 9-10 incl. the workletFlushMs 250-when-present law; §7's FALSE clauses; §10's facet rows). The `AuxBusSettings.muted` rows missing = confirmed (spec had only "return gain + return chain").
3. **The waveform contract state** — 20's W-row (:25) carries the accurate R26 EXECUTED stamp but lacks the R27 survival tail (byte-verbatim through the S-series; 777; consumer `ec8fd5c`) — FIX-6c. **The K3 audio-pin prerequisite is MET and the row itself LANDED** (`44e8049`) — FIX-2 flips :26; the PLAN's S-app row (5) ("the audio pins STILL hard-gated on S-wdc's unlanded waveform promotion") and the :54 K3 row (d) both need the same flip (cross-spec).
4. **The S-series closure** — the manifest census (copy 93 + 4 root, shims 11, coreOwned 14) replaces :202's "33-file closure" (FIX-6g); the vendoring-law row :213 stays accurate (the file-class law held through the wave — scout-wdc §2 "the law needs no amendment; only the census numbers are stale"); the rt-safety-gate fix's law text = FIX-4a's second row (the transitive-heritage law).
5. **The W2/N2b consumption state** — both correctly LANDED in 20 (the :28-29 strikethroughs, §5.3/:120, §5.8/:125, §10's rows — all re-verified live); the r2 depth rows (A4-v2 NULL rig, async pre-retime) both still accurately OPEN; the ONE stale r2 row is the test-ports row (FIX-1) and the G-surface nuance (FIX-3e).
6. **The async pre-retime queue** — what 20 promises (:24) is accurate and unchanged (r2, engine-owned, design-w2 §4; both queues still carry it — re-read live). The "4 deferred tests" residue lives only inside WDC's own stale HANDOFF (the S-series docs wrap never landed) — spec-side the count was already fixed R24; the ROW flips LANDED (FIX-1). The JKL audio half is correctly recorded CLOSED (the S4 seal; the R4 ⇧K dead-key residue a P3).
7. **The dB-domain coherence** — 20's domain statements: §5.8 carries the [−60, +20] clamp + NaN→unity (accurate); §4.1's `volumeDb` row LACKS the domain + the one-home statement — FIX-8 adds it; the one-home law verified live at `opencut-laws.ts:54-79` (the OV-01/02 value-imports of `core/audio-params`). 09's B2 wants ONE author with 20 §4.1 — the pairing is reserved at spec-09.md:166; the W3 fleet should dispatch them together.
8. **The pre-R26 pins/citations + cross-refs** — spot-verified 8: 03 §3/§8/§9.2 (exist), 09 §3.1's TrackJSON mute/solo (exists, A4), 15 §13.15 (exists), 17 §13A + 17:2308's audio-seam-properties row (valid, cites 20 §5), 00 §2.5/§2.5.3 + D13/D14 (exist), 12:14 + 17:4/:20 (carry the stale 759/18-suites — cross-spec notes), 02↔20 (CONFIRMED effectively unlinked — 02 has zero references to 20; 20's only 02 link is the §9 boundaries row — FIX-4c proposes the two-way §-level link rows).

---

## §4 Cross-spec notes (for the cross-cut wave)

1. **02** — 02's FIX-1 (§7.4, the audio-thread observability law) and 20's FIX-4 MUST cite the same four file:line anchors; 02's FIX-2/FIX-3 (the app deliver/sidechain rows + the worklet venue) are the 02-side twins of 20's FIX-3a/6d; the 02↔20 link rows (FIX-4c) close the corpus's only same-domain unlink. 02's FIX-4 re-pins mirror 20's FIX-6.
2. **09** — the B2 rewrite (spec-09.md:107) + 20's FIX-8: ONE author, one landing (the dual-representation law + the one-home statement byte-coherent). 09's preservePitch absent≡true ruling (spec-09.md:105) touches 20 §4.1's `maintainPitch` row — 20's row already says "the element's `retime.maintainPitch`"; a one-phrase "absent ≡ true (the NLE default, R9-b)" addition rides the same edit.
3. **12 + 17** — the WDC rows (759→777, `ec8fd5c`, 18→19 `src/test/` files, the tempo-map.test +4, the S-series families) are scout-wdc's rows, confirmed here; 17:546's "AudioWorklet message protocol" facet gains the diagnostics messages (02's FIX-1 note); 17's K3 instrument row should register the AR-2 gesture pin + the app's 8-suite roof (scout-app's row); 20's §10 facet additions (FIX-3f) should be mirrored in 17 §13A's audio block.
4. **15** — no audio wire shapes landed in R27 (M2 Wave 1 is materialization-side); 15 §13.15's audio-command family unchanged — no edit from 20.
5. **00** — the fleet rows + 2A.10 are spec-00's P1s (already filed); the D29.4/D36.3 waveform-promotion records verified accurate; the K3-audio-pin flip propagates: 00's D36/D29 notes + the plan.
6. **IMPLEMENTATION-PLAN** — S-app row (5): "the audio pins STILL hard-gated on S-wdc's unlanded waveform promotion" is FALSE (the promotion LANDED R26 — the plan's own S-wdc row says so — AND the audio pins landed @ `44e8049`); flip with the VLM-net-only remainder. S-wdc row: item (2) the mixer surface needs the Wave-1 fold; item (3) the FIVE ports flip LANDED (G1 `3a28b6e`); the S-wdc row's "zero upstream drift" clause is FALSE (scout-wdc's row) + the missing S-series docs-wrap queue item. The :54 K3 row (d) "the offline audio pins (hard prerequisite: S-wdc's waveform seam contract)" needs the landed note.
7. **19** — the WDC vendored-census rows (copy 93+4 / shims 11 / coreOwned 14) + the "upstream unchanged since the sync" historical note are scout-wdc's rows, confirmed by my manifest re-count.
8. **04/05/06/08** — no audio-side coupling found beyond the already-filed venue/measured-signal rulings; no 20-side edits needed.

---

## §5 What 20 uniquely sees (not in any scout or sibling report)

1. **The FIVE-test-ports row flip (:21, FIX-1)** — the G1 landing (`upstream-ports.test.ts`, the 106-it disposition table) is recorded in NO scout register and NO spec; 20 is its only home (scout-wdc's register has no :21 row; scout-engine's G1 row mapped to no spec site).
2. **The K3-audio-pins row flip (:26, FIX-2)** — scout-app's register covers 20:17 + 20:20/145/151 but not :26; the acceptance-met flip (9 pins + the mute/flush families) is this audit's.
3. **The 22-effect census** — the "17 DSP `EffectNode` implementations" figure was verified stale at its OWN R24 pin (22 at `494f6ff` AND `387f327` AND `ec8fd5c`; the 15+3+4 classification), resolving scout-wdc's open question #3 with a count that predates the S-series.
4. **The worklet-venue re-shape (:27 + :133, FIX-6d)** — the engine-symlink + app-plugin venue pair (and the ambient-declaration tsconfig pin-law) stated as ONE law for both sites; 02's FIX-3 owns the 02 wording, 20's is the audio-side twin.
5. **The MIDI-family W7/M3 registration (§4.3, FIX-7c)** — the vendored `midi/*` family as the natural upstream reference for the deferred scope, in the same pattern as `model/automation.ts` (§12.1).
6. **The 09-B2/§4.1 one-author pairing (FIX-8)** — the volume-dB coherence row 20 owes the fleet's single-B2-author plan; drafted so the two specs can land byte-coherently in one edit.

---

## §6 Verification register (every load-bearing claim, re-derived live)

- WDC @ `ec8fd5c` (clean; `git status` shows only `bun.lock`): manifest census **copy 93 / copyRepoRoot 4 / shims 11 / coreOwned 14** (python re-count of `extraction-manifest.json`); instruments = **7 shims + 3 copies** (dir listing + manifest cross-check); `UPSTREAM.lock.json` sha `f501b3` (read); `src/test/` = **19** `*.test.ts` (34 repo-wide); `diagnostics.ts` API surface (`:139/:175/:296/:335/:344`); `dsp-effects-worklet.js:315-372` (`DspDiagnosedProcessor`, 3 processors extend it); `rt-safety-gate.ts:141-182` (the transitive fix, the in-code M3-REV-DOCS comment); `audio-registry.ts` line pins (:20/:24/:48/:58/:74) EXACT; `waveform-contract.ts:44/:47` + the 14-pin test (3 describes :94/:130/:204); `effects.ts` — 22 exported effect classes, the stall-bypass ladder :460-461/:505; barrel :81/:87/:102-103.
- Engine @ `f9ac806`: submodule status (`-6e2b91a…` OT, `-ec8fd5c…` WDC); `scene-mixer.ts:42-59` (the Wave-1 law header), `:65` (the import), `:222-225` (syncSidechainWires after applySettings), `:277-289` (applyAuxBusGain), `:314-339` (identity-diff sever), `:444-448` (workletFlushMs ?? 50); `mixer-track-model.ts:89-102` (AuxBusSettings.muted); `nle-bridge.test.ts` = **99** its; `m2-wave1-mixer-surface.test.ts` = **9** its (F-1 ×4, F-2 ×5); `upstream-ports.test.ts` = 25 its + the 106-it disposition table (:223-226); `opencut-laws.ts:54-79` (the one-home value-imports); `scene-to-segments.ts:164-175/:506-511` (the volume folds); `conversions.ts:201-233` (the unity-fader default); `public/worklets` symlink (ls -la).
- App @ `c020b2a`: `audioService.ts:146/:175-176` (the bus fold — volume UNfolded + `muted: !aN.on`), `:319-322` (the rewireSidechains replacement note); `deliverService.ts:170-181/:252` (the flush law); `vite.config.ts:38/:45/:57` (the worklet plugin); `waveformPeaks.ts:24-41` (the exported-contract consumption) + `waveformPeaks.test.ts` (9 its, read); `audioService.test.tsx:104-195/:998-1030` (the mute/bus/U9-single-owner pins); `GluedShell.test.tsx:1289` (S3-C2); `44e8049` + `1aebbd5` + `9448667` commit messages read; `public/` absent (live).
- Cross-refs: 03 §3/§8/§9.2, 09 §3.1 (A4 mute/solo), 15 §13.15, 17 §13A/:2308, 00 §2.5/:485/D13/D14 — all resolve; 02↔20 link state (grep-verified both directions); 12:14 + 17:4/:20 (the stale 759/18-suite rows); spec-09.md:107/:166 (the B2 pairing reservation); spec-02.md FIX-1/2/3 (the twin amendments).

**Research-only: no spec/repo files modified outside this report; no commits/branches; stayed on main.**
