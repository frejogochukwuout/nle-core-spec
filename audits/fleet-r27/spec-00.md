# R27-W3 spec-00 — the master spec audit (00-master-spec.md, section by section)

**Task ID:** R27-W3-00 · **Agent:** spec-00 · **Round:** R27 final-tightness, Wave 3 (per-spec fleet) · **Date:** 2026-09-13
**Spec audited:** `/home/z/my-project/nle-core-spec/00-master-spec.md` (863 lines, v11.0/R26 state) at repo HEAD `ee7958a`.
**Method:** full section-by-section read of the file; every pin/count/census claim re-derived against the live trees (`/home/z/my-project/{nle-engine,opencut-timeline,web-daw-core,nle-ui,nle-test-app}` — SHAs, submodule gitlinks, register files, gates lines re-read directly); the W1 scout reports (`audits/fleet-r27/scout-*.md`) cited as the count authority where suites were not re-run in this sandbox; cross-references into the other 20 specs spot-verified by grep/read (12 checked, all listed in §1). Report-only: no spec-corpus file touched outside this report + the worklog.

**Ground truth re-verified live for this audit:** engine HEAD `f9ac806` (src/ unchanged since code anchor `50b91f5` — `git diff --stat 50b91f5 f9ac806 -- src/` empty; timeline.ts 8,997 LOC; `performInsertEdit` @ :5630; the 13-test pin file `tests/vitest/engine/timeline-linked-source-edit.test.ts` present) · OT HEAD `55c81c0`, code pin `970948a` (`git diff 970948a..55c81c0 -- src/ -- scripts/` empty; the D-ARCH-6 commit family `75b72b7→ec86ad3→970948a` read in full; the canonical tree `src/components/timeline/` = 41 files / 9,176 LOC) · WDC `ec8fd5c` (one sync commit over `387f327`) · nle-ui `32abd58` (parent `83ff8a8`) · app `c020b2a` (submodules: engine `f9ac806`, nle-ui `83ff8a8`, WDC `ec8fd5c`; the vendored components mirror + the engine's OT submodule both @ `6e2b91a`; `docs/port-census.md:29-37` declares 42 port files = 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers + host @ `6e2b91a`) · the engine's submodules: OT `6e2b91a` + WDC `ec8fd5c` (the D-ARCH-6 absorption of `970948a` is the engine's queued next — engine `.agents/PLAN.md:481`, `HANDOFF.md:49`) · the engine's D12 ratification read in full (`.agents/DECISIONS.md:521`) · the engine's SKILL law 88 read in full (`.agents/SKILL.md:1167` — the live-registry consumption law) · the app's maintainPitch resolution read in full (`src/engineService.ts:481-513`, `src/sceneBridge.ts:229-262/:435`).

---

## §1 The per-section verdict table

| # | Section (00 line span) | Verdict | The finding (evidence) |
|---|---|---|---|
| 1 | Status header + date ledger (:3-:6) | **P2** | v11.0/R26 ledger is the dated record (fine) but the round is over: needs the **v12.0 R27 entry**; the date ledger (:4) is missing `v11 Round 26 2026-09-10` (the R26 wrap `0fc7653` is 2026-09-10 — the ledger stops at v10/R25); :6's `Consumers:` line names only engine/OT/WDC in a seven-repo pin world (D21). |
| 2 | §0 fleet rows — engine (:16) | **P1** | The live canon row states the R26 pins as current: `0a49286` **471/471**, consumes OT `c15a629` + WDC `494f6ff`. FALSE now: **`f9ac806` 748/748** (gates lines at `c01a187` worklog + `480a216`; ARCH-R27's "(739)" is the mid-round reading — scout-engine §6.1), consumer pins OT **`6e2b91a`** + WDC **`ec8fd5c`** (submodule gitlinks re-read live). The RC-round law set (D12 ratification, OV-01/02/05/12, M2 Wave 1, the 8,997-LOC warning) is absent from the row. |
| 3 | §0 fleet rows — OT (:17) | **P1** | `fdb771c`/code pin `c15a629` 536/536 + "the M49C coverage gate — **24 routed wire verbs + 6 documented exceptions**". FALSE now: **HEAD `55c81c0`, code pin `970948a`, 632/632** (452 in-page + 30 DragEvent/keyboard + 150 real-mouse — the report json committed in `970948a` is the authority) and the census is **31 verbs = 28 routed + 3 exceptions** (D-ARCH-6, session-17; three-way verified — scout-ot §1.2). AR-1/AR-2/D-HB2 + the batch-verb law missing from the row; "40 files / 8,604 LOC" canonical-tree figure stale (41 files / 9,176 LOC live-counted). |
| 4 | §0 fleet rows — WDC (:18) | **P1** | `387f327` 773/773. FALSE now: **`ec8fd5c` 777/777** (34 test files, tsc 0, re-run live by scout-wdc; the +4 = the tempo-map family 68→72) and the **S-series law set is missing entirely** (briefing fact 7): the EngineDiagnostic bus + heartbeat monitor (DspDiagnosedProcessor, 256-quantum, NaN/silence scans, stall bypass-to-passthrough), the rebuilt dsp-effects-worklet bundle, the rt-safety-gate transitive-heritage fix, wam-instrument+state, the instrument factory/registry/param-path, the MIDI family, the sample-catalog split; the waveform contract survived byte-verbatim (the 14 pins inside the 777). |
| 5 | §0 fleet rows — nle-ui + app + mocks (:19) | **P1** | nle-ui `3026099` 674/674 → FALSE: **`32abd58` 690/690** (R9-b `el.preservePitch` ElementJSON — the R26 model-decision ask RESOLVED in code; FW-D setElementFieldAll; F4 identity guards/SEVERS; AW1-2 the Gain ceiling +4→+20). App `3c91ffe` 206/206, census "41 = 35 zero-action + 5 carriers + host", consumer pins `5036387`/`c15a629`/`3026099`/`387f327` → ALL FALSE: **`c020b2a` 252/252**, census **42 = 36 zero-action + 5 carriers + host @ `6e2b91a`** (`docs/port-census.md:29-37` + census-check live), consumer pins **engine `f9ac806` + nle-ui `83ff8a8` + WDC `ec8fd5c` + the OT mirror `6e2b91a`** (gitlinks re-read). The AR-2 consumption landing (the gate consumes the LIVE registry + the volume-line dblclick → `timeline.upsertKeyframe` origin 'ui') + M2 Wave 1 app-side + maintainPitch-RESOLVED missing from the row. |
| 6 | §0 fleet rows — mini + variants (:19 tail) | **P2** | mini reads as settled ("415 (the R23 seal 355 + the R24-mini nets — …W0/W1/W2; …145 census units / 10 files)") but **R24-2 is IN FLIGHT** (W0/W1/W2 landed mid-audit 2026-09-13, W3+ pending — the W2 commit's tool radio says "W3 extends"; lineage 355+2(clip-box)→377→396→415; census 145 units / **132 GAP** / **382 authored**). variants cites "the R25 census reads 1,588 it-blocks" — its own register re-derived **1,762 it-blocks / 64 test files** at the 2026-09-13 re-read (REFERENCE-REGISTER.md:13). |
| 7 | §0 companion-files table (:26-:50) | **SOUND** | 15's "78 `EngineCommand` types" ✓ (15:285 "Total: 78 command types"); 16's "181 bindings" ✓ (16:42/:2346); 19's "33 rows / C1-C9" ✓ (19:13/:303-:311); 14's RETIRED-at-R23 note ✓ (the stub exists); ui-mock row ✓; 20's row present in the stream map (§8) ✓. |
| 8 | §1 Product Vision (:54-:93) | **SOUND** | No pins/counts; scope statements intact. |
| 9 | §2 D1-D9 (:100-:233) | **SOUND** | Historical records with reasoning preserved; no corpus-fact collisions. |
| 10 | §2 D10 (:289-:298) | **SOUND** | Counts explicitly superseded with the pointer to §0's fleet rows — the layered-card convention working as designed (the R26 readings it names are themselves now history; the §0 re-pin fixes the chain). |
| 11 | §2 D11 (:300-:314) | **P2** | Clause 2's census-lineage parenthetical stops at "the routed/exceptions split at `c15a629`"; the lineage continued **25+5 at AR-2 (s16) → 28+3 at D-ARCH-6 (s17, `970948a`)**. |
| 12 | §2 D12 (:316-:336) | **P2** | **Missing the engine-side ratification note** (briefing fact 13): nle-engine `.agents/DECISIONS.md:521` (D12, `1064f91`, 2026-09-13) RATIFIES the spec's Decision 12/16 junction — "opencut-timeline is THE editing engine; nle-engine's freecut-lineage stack is the render/export venue model (venue-internal state like an IR, not a second user timeline); the junction is ONE-DIRECTIONAL"; "the cross-repo ruling now has both signatures". The convergence deliverables (OV-01/02/05/12, M2 Wave 1) belong in the same note. |
| 13 | §2 D13 (:338-:346) | **P3** | The WDC count-supersession chain stops at R24 (`85b81b0` 759); R26's 773 and R27's 777 are §0's fleet rows' job — the chain could carry the two-step tail for the same one-line reason it carries the R22/R23/R24 steps. |
| 14 | §2 D14-D17 (:348-:384) | **SOUND** | Supersession notes intact and accurate (D14's body note, D15's R24 extension, D16/D17 unchanged). |
| 15 | §2 D18-D23 (:386-:408) | **SOUND** | D18's "spec 18 §16.1" ✓ (18:549 `### 16.1 Positioning`); D23's "the fleet pin table stays HERE" verified against the plan (the plan cites pins, never re-declares). |
| 16 | §2 D24-D28 (:410-:431) | **SOUND** | Ladder/topology/evolution records intact; D28's gate unchanged. |
| 17 | §2 D29 (:433-:435) | **P2** | Three stale clauses: (a) "(green, 24/24)" — the M49C gate now reads **28 routed + 3 exceptions PASS** (`970948a`'s report json); (b) "the exception registry is test-local upstream" — **FALSE since AR-1** (`d0db321`, s16: `WIRE_UI_EXCEPTIONS` + `WIRE_UI_EXCEPTION_VERBS` exported from the barrel; the app's gate consumes the LIVE export); (c) "OT's spec-queue candidates (insertBatch + batch-keyframe verbs) endorsed as r1-adjacent" — **LANDED as OT's D-ARCH-6** (session-17). |
| 18 | §2 D30 (:437-:439) | **P2** | "The landed C7 census (24 routed + 6 exceptions, Decision 29.2) is NOT reopened" — the law HELD exactly as written (the re-declaration fired mechanically per its own clause), but the row needs the dated firing note or a reader concludes the census is still 24+6. |
| 19 | §2 D31 + 31A (:441-:447) | **P2** | "**`performInsertEdit` :4702**" — stale line pin: timeline.ts is 8,997 LOC at `f9ac806`; `performInsertEdit` is **:5630** (`_performInsertEditImpl` :5647). The fresh-grep discipline (§2.5.2) requires the refresh. |
| 20 | §2 D32 (:449-:451) | **P3** | 32.6's defect row is the R25 record (the E1 fix is recorded in the status header + D36.2's R26 note) — the row head could carry the one-tag "(FIXED at R26)" so a reader landing on D32 doesn't re-derive the fix state. |
| 21 | §2 D33-D35 (:453-:463) | **SOUND** | 05 §8A ✓ (05:599); 18 §9 ✓ (18:396 `## 9. Theming & Design Tokens`); 16 §3.4A ✓ (16:32/:196); REFERENCE-REGISTER.md ✓ exists at the root, C-ledger **C33-C58 still the full range** (register :56-:85, C58 the tail row) — 2A.9's "today" reading is accurate. |
| 22 | §2 D36 (:465-:467) | **P2** | 36.2's maintainPitch clause — "the residual is the app's engineService hardcode — re-filed app-side" — **RESOLVED at R27**: the app's H2-B/R9-b wave landed the full threading (`engineService.ts:481-513` — preservePitch given, else the engine retime's maintainPitch, else true; `sceneBridge.ts:229-262` — `maintainPitch: el.preservePitch !== false`; :435 the back-projection note). 36.4's "the W8 `!shiftKey` playhead-guard hole is STILL OPEN OT-side" — **LANDED**: the absorbed F4 wave (`9ee3537`..`3be2dea`, inside `fdb771c..55c81c0`) carried the OT-side guard; "WIRE_UI_EXCEPTIONS-export is a new small row" — **LANDED** as AR-1 (`d0db321`). |
| 23 | §2A standing laws (:469-:479) | **P1** | **The AR-2 live-registry consumption law is missing entirely** (briefing fact 12): a landed, cross-repo, battery-relevant law with three independent codifications (the app's gate consuming the LIVE export — `wire-coverage.test.tsx:29/:49-59` @ `8a3eb35`; the engine's SKILL law 88 @ `f9ac806`; OT's D-ARCH-6 registry shrink 5→3 that a stale mirror would have missed). Also: 2A.7's R26 note needs the R27 re-declare (42 = 41 mirrors + host @ `6e2b91a`); 2A.8's census sentence needs the firing note (28+3). 2A.1-2A.6, 2A.9 SOUND. |
| 24 | §2.5 Document Governance (:481-:487) | **SOUND** | The single-file canon + citation rules intact; the repo-side adjacency (OT SEAMS.md, WDC docs/track-model.md) unchanged. |
| 25 | §3 High-Level Architecture (:489-:560) | **P2** | The live-fleet reading note carries R24-record values as the live reading: "39 mirrors + the EngineMount host; 32 zero-action + 7 documented carriers, per Decision 26" → now **41 mirrors (36 zero-action + 5 carriers) + host**; "40 files / 8,604 LOC at the R24 code pin `c15a629`" → the canonical tree is **41 files / 9,176 LOC at `970948a`** (live-counted). |
| 26 | §4 Technology Stack (:562-:587) | **SOUND** | mediabunny/MPL note intact; wire-protocol row consistent with spec 15. |
| 27 | §5 Browser Matrix (:589-:608) | **SOUND** | Unchanged law. |
| 28 | §6 + §6A (:610-:641) | **SOUND** | NFR table's citations verified (spec 17 §13A.1 exists; 17's §13A matrix present at 17:4 ff.). |
| 29 | §7 WYSIWYG Contract (:643-:671) | **SOUND** | The three-invariant contract unchanged; consistent with §13.6. |
| 30 | §8 Stream Map (:673-:701) | **SOUND** | 18 rows; every named spec file exists (ls-verified); the spec-14 redirect note accurate; 20-audio-core row present. |
| 31 | §9 Testability summary (:703-:715) | **SOUND** | Summary consistent with §13 + spec 12/17. |
| 32 | §10 Implementation Plan summary (:717-:733) | **SOUND** | `IMPLEMENTATION-PLAN.md` at the repo root ✓ exists; D23's separation respected. |
| 33 | §11 Open Questions (:735-:758) | **SOUND** | Historical scout round record; the R7 status note accurate. |
| 34 | §12 Glossary (:760-:784) | **SOUND** | No stale claims vs the live specs (78-type + 15-pointer rows verified). |
| 35 | §13.1-§13.4 (:790-:832) | **SOUND** | Tier table, UI-tax rules, blend assets, tone assets intact. |
| 36 | §13.5 NLE op verification — the 00:838 invariant class (:833-:846) | **SOUND** | **The invariants are coherent post-E1/E2-fix**: the no-overlap line (:838 "No overlaps on any track after ripple/move/insert") was VIOLATED by E1 and is now RESTORED and pinned (`8188d4e`+`412693d`, the 13-test pin file — re-verified present in the live tree); the property list needs no change. Optional P3: the E1-fix pin cross-ref. |
| 37 | §13.6-§13.7 (:847-:861) | **SOUND** | The three WYSIWYG invariants + asset SoT law unchanged. |

**Cross-references spot-verified for this audit (12):** 06 §0 mode matrix (06:14) · 05 §0 census mirror row (05:17) · 05 §8A (05:599) · 18 §9 (18:396) · 18 §16.1 (18:549) · 16 §3.4A (16:32) · 15 §4.1A (15:18) · 15 §13.15 (15:4/:18/:31) · 17 §13A (17:4 ff.) · 19 §2.4 (19:68) · REFERENCE-REGISTER.md (root, :13 suite pins) · IMPLEMENTATION-PLAN.md (root, the S-rows). All resolve; the STALE CONTENT inside them is each owning spec's W3 report (see §3).

---

## §2 The fix-list (exact edit proposals — mechanically appliable)

### P1 — the fleet-row canon is FALSE now (all four module cards + both consumer-pin sets)

**P1-1 · §0:16 — the engine row's R27 card.** Find the tail of the engine row: `…+ the PLAN tick \`0a49286\`; **471/471** (458 + 13) LIVE-run this round against the exact pinned submodule SHAs; consumes OT @ \`c15a629\` (UNMOVED since the R24 re-pin) + WDC @ \`494f6ff\` (docs-only behind \`85b81b0\`) as submodules); the RUNTIME-domain core` — insert the R27 card between `as submodules)` and `; the RUNTIME-domain core`:

> `@ `f9ac806` (R27 — the RC convergence round: the docs-only head over the code anchor `50b91f5` (src/ unchanged — the corpus's every pre-R26 engine line number is suspect: **timeline.ts is 8,997 LOC**); **748/748** (471 + 277: the F3 hardening + the FW-A/C/E1 waves + **OV-01/OV-02 the volume-dB + retime-clamp one-home collapse** (bridge/opencut-laws.ts VALUE-IMPORTS opencut's pure leaves — core/audio-params + ops/retime) + **OV-05 el.transitionOut** the visual seam + **OV-12 the vendored-tree re-pin gate in engine CI** + **M2 Wave 1 the bus-law + sidechain-parity** (materializeStrips) + the WDC S-series re-pin), tsc 0, fence 52, probes 9/9 (the gates lines, NOT ARCH-R27's "(739)" — that is the mid-round OV-05 reading); **DECISIONS D12 (:521) RATIFIES the spec's Decision 12/16 junction engine-side** (opencut is THE editing engine; the engine's stack is the render/export venue model; one-directional junction); consumes OT @ `6e2b91a` (session-16; **the D-ARCH-6 absorption of the s17 code pin `970948a` is the engine's queued next**) + WDC @ `ec8fd5c` (the S-series re-pin `480a216`) as submodules)`

**P1-2 · §0:17 — the OT row's R27 card.** Find: `…536/536 + the M49C gate 24/24 + 6 exceptions re-run LIVE this round; R25 — the fleet re-pin; consumers pin \`c15a629\` — code-current without action); the EDITING-domain core` — insert between `code-current without action)` and `; the EDITING-domain core`:

> `@ `55c81c0` / **code pin `970948a`** (R27 — the two sessions: **s16** the hardening + architecture round (616/616; **AR-1** core/audio-params.ts the volume-dB ONE HOME + the `WIRE_UI_EXCEPTIONS`/`WIRE_UI_EXCEPTION_VERBS` live-read barrel export; **AR-2** the keyframe-authoring double-click → `timeline.upsertKeyframe` origin 'ui' + the registry reconciliation 6→5; **D-HB2** fromJSON the scene-load boundary) + **s17 D-ARCH-6 the wire widenings** (`timeline.insertBatch`/`removeKeyframes`/`retimeKeyframes` — one history entry each, the shared validateInsertElement helper, F1A-3, intra-batch overlap CONFLICT whole-batch, empty INVALID_PARAMS, the WYSIWYG bake, the kind-split + staggered construction at the library layer; the singular removeKeyframe/retimeKeyframe RETIRED — **`WIRE_COMMAND_TYPES` 31 verbs = 28 routed + 3 exceptions, re-declared MECHANICALLY per the tsc-lockstep/M49C law (D29 F8)**; census lineage 24+6 @ `c15a629` → 25+5 @ AR-2 → **28+3 @ `970948a`**); **632/632** (452 in-page + 30 DragEvent/keyboard + 150 real-mouse — the report json the authority) + the M49C gate 28 routed + 3 exceptions PASS; consumers: engine @ `6e2b91a` (the s17 absorption queued), the app's vendored components-tree mirror @ `6e2b91a`)`

Also in the same row: `**+ the CANONICAL React timeline UI tree** (\`src/components/timeline/\`, 40 files / 8,604 LOC, M17-M49 + real-mouse + coverage-gate-verified — Decision 25)` → append ` (41 files / 9,176 LOC at the R27 code pin — the AR-2 `use-keyframe-authoring.ts` addition)`.

**P1-3 · §0:18 — the WDC row's R27 card.** Find: `…773/773 = 759 + 14 LIVE-run; consumed by engine @ \`494f6ff\` (docs-only behind) + app @ **\`387f327\`** (re-pinned this round)); three-layer track model` — insert between `(re-pinned this round))` and `; three-layer track model`:

> `@ `ec8fd5c` (R27 — **the S-series upstream wave**: ONE sync commit (the fork @ `f5011b3`, our `eeb3b24` fix preserved) — 19 locked files refreshed + the 18-file closure: **the diagnostics EngineDiagnostic bus + heartbeat monitor** (DspDiagnosedProcessor: 256-quantum heartbeats + NaN/silence scans in process(); the stall bypass-to-passthrough), **the rebuilt dsp-effects-worklet bundle**, **the rt-safety-gate transitive-heritage fix**, wam-instrument+state, the instrument factory/registry/param-path, the pure-logic MIDI family, the sample-catalog split; **the waveform contract survived byte-verbatim** (the 14 pins inside the 777); **777/777** (773 + 4: the synced tempo-map family 68→72), 34 test files, tsc 0 — re-run live; consumed by engine @ `ec8fd5c` (`480a216`) + app @ `ec8fd5c` (`9448667`); WDC's own docs never absorbed the S-series wrap — the root HANDOFF is stale, their next session's problem)`

**P1-4 · §0:19 — the nle-ui + app R27 cards.** Find the nle-ui card tail: `…role LOCKED per Decision 27.1) + **nle-test-app**` — insert before `+ **nle-test-app**`:

> ` @ `32abd58` (R27 — 8 commits over `3026099`: **R9-b `el.preservePitch` joins ElementJSON (absent≡true; the engine's retime.maintainPitch is the runtime projection — the R26 model-decision ask RESOLVED in code)**, FW-D setElementFieldAll (one user verb = one undo unit per owning domain), F4 the identity guards (law-73 class) + kind-aware toggleMuteAll + duplicateElements SEVERS linkedTo, AW1-2 the Inspector Gain ceiling +4→+20 (the fleet-coherent [−60,+20] domain, opencut's core/audio-params.ts the one home); **690/690** live-run + boundary PASS; consumed @ `83ff8a8` — the app is ONE commit behind (the AW1-2 ceiling fix absorbs at the freshness re-pin, the app's `c020b2a` filing))`

Find the app card tail: `…(re-pinned R26) @ \`3c91ffe\` (the census-CI + folds + the maintainPitch scoping — docs-only over \`3e4f0cd\`) — the R26 fleet re-pin (Decisions 18/21/25/26-28)` — insert after `(Decisions 18/21/25/26-28)`:

> ` @ `c020b2a` (R27 — 42 commits over `3c91ffe` (F3 → H2-B → FW-B → RA → RC): **252/252**, tsc 0, census OK, boundary OK, mutation 8/8; **the census register re-declared at the `6e2b91a` re-pin: 42 port files = 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers + the host** (the AR-2 `use-keyframe-authoring.ts` twin; `theme.ts` byte→mechanical via the shared volume-dB law); **AR-2 consumption: the wire-coverage gate consumes the LIVE `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel** (2A.10) + the real volume-line dblclick → `timeline.upsertKeyframe` origin 'ui'; M2 Wave 1 app-side (the bus fold REPLACEMENT, single-owner sidechain, the worklet VENUE fix); **maintainPitch RESOLVED in code** (engineService.ts:481-513 + sceneBridge.ts:229-262 — the PLAN row's hardcode residual is dead text); consumes engine @ `f9ac806` + nle-ui @ `83ff8a8` + WDC @ `ec8fd5c` + the OT components-tree mirror @ `6e2b91a`)`

**P1-5 · §0:19 — the mini/variants tail.** Find: `the in-repo \`ui-mock/shell-variants\` @ 1521+ tests (live; 1,470 at the 2026-09-07 pin-era reading; the R25 census reads 1,588 it-blocks — the canon holds; the R23 W-A FX surface + W-B color wave + the VLM net), the design reference; \`ui-mock/shell-mini\` 415 (the R23 seal 355 + the R24-mini nets — the clip-box fix + the mini-plus feature-parity round W0/W1/W2; the law register re-censused: 145 census units / 10 files), the crawl's law register` → replace with:

> `the in-repo \`ui-mock/shell-variants\` @ 1521+ tests (live; **the register re-derived 1,762 it-blocks / 64 test files at the 2026-09-13 re-read** over the R25 census's 1,588 — R25-W1 (the insert rescue wave) landed, more waves coming; the sibling's round-wrap declares the final figure, per-wave movements recorded not chased; the R23 W-A FX surface + W-B color wave + the VLM net), the design reference; \`ui-mock/shell-mini\` **415 — R24-2 IN FLIGHT** (the R23 seal 355 + clip-box (+2) + the R24-2 mini-plus waves W0 (+20) / W1 (+19) / W2 (+19) landed mid-audit 2026-09-13, W3+ pending; the law register re-censused: 145 census units / 132 GAP / 382 authored / 10 files), the crawl's law register`

**P1-6 · §2A — the new standing law 2A.10.** Insert after the 2A.9 block (before `### 2.5 Document Governance`):

> `10. **The live-registry consumption law (the AR-2 ruling, R27): a consumer gate consumes the exported registry, never a hand-mirrored copy.** When a core exports a registry/constant as a seam surface (opencut-timeline's `WIRE_UI_EXCEPTIONS` + `WIRE_UI_EXCEPTION_VERBS` from the barrel, per their AR-1), every consumer gate — the app's wire-coverage gate, the engine's vendor fences, any future consumer — reads the LIVE export. A hand-mirrored list goes stale at exactly the re-pin that changes it (the RC-V1 P2: the app's gate had mirrored the 6-verb exception list test-locally; OT's AR-2 removed `timeline.upsertKeyframe` from the registry in the SAME session its UI coverage landed, the mirror went stale at the re-pin, and the authoring seam was left unfenced — and OT's D-ARCH-6 then shrank the registry 5→3, which a stale mirror would have missed again). The same register-law class as 2A.7, cross-repo: any consumer of any exported constant. Engine-side twin: nle-engine SKILL law 88 (`f9ac806`); the app's landing: `wire-coverage.test.tsx:29/:49-59` (`8a3eb35`).`

### P2 — stale citations / missing dated notes / stale line pins

**P2-1 · Status header (:3) + date ledger (:4).** Promote the status to **v12.0 (Round 27 — the final-tightness round…)** with the R27 summary (the five re-pins + the census firing + 2A.10 + the D12 ratification note + the sibling-race facts), and demote the current v11.0 text to "Prior ledger:" (the established convention). Append to :4's date ledger: `; v11 Round 26 2026-09-10; v12 Round 27 2026-09-13` (the v11 date is MISSING today — the ledger stops at v10/R25).

**P2-2 · D21 (:400) — the R27 note.** Append to the R26 note's close (`…the pin-CLASS law stands unchanged.)*`):

> ` *(R27 note: the R26 canon values above are superseded — the live canon is §0's fleet rows re-pinned at R27: engine `f9ac806` 748 / OT `55c81c0` (code pin `970948a`) 632 / WDC `ec8fd5c` 777 / nle-ui `32abd58` 690 / app `c020b2a` 252; consumer pins re-read live: engine→OT `6e2b91a` (the D-ARCH-6 absorption queued) + WDC `ec8fd5c`; app→engine `f9ac806` + nle-ui `83ff8a8` (one behind HEAD — the AW1-2 freshness re-pin queued) + WDC `ec8fd5c` + the OT components mirror `6e2b91a` — the pin-CLASS law stands unchanged.)*`

**P2-3 · D29 (:435) — the three clauses.** (a) `(green, 24/24)` → `(green, 24/24 at \`c15a629\` — R27: 28 routed + 3 exceptions PASS at \`970948a\`)`; (b) after `the exception registry is test-local upstream.` insert `*(R27: superseded by AR-1's barrel export — the live-registry consumption law, 2A.10.)*`; (c) after `endorsed as r1-adjacent (the census re-declares mechanically per the tsc-lockstep law).` insert `*(R27: LANDED — OT's D-ARCH-6, session-17; see 2A.8's firing note.)*`.

**P2-4 · 2A.8 (:478) + D30 (:439) — the census-firing note.** Append to 2A.8's final sentence (`…re-declare mechanically per the tsc-lockstep/M49C law (Decision 29 F8) when they land.`):

> ` *(R27 note: the mechanical re-declaration FIRED twice without reopening the census — OT's AR-2 (s16): `timeline.upsertKeyframe` left the exceptions on its UI-coverage landing (25 routed + 5 exceptions); OT's D-ARCH-6 (s17): the three batch verbs landed + the singular pair RETIRED — **31 verbs = 28 routed + 3 exceptions at the code pin `970948a`**, M49C 28/28 PASS, the report json the authority. The tsc-lockstep law held; no spec amendment was required — exactly this law's design.)*`

Insert the same note (shortened) into D30's body after `is NOT reopened` sentence: `*(R27: fired — see 2A.8's note: 28+3 at `970948a`.)*`.

**P2-5 · 2A.7 (:477) — the R27 re-declare.** Append to the R26 note's close (`…the R26 register declares 40 mirrors + host at the D30 irreducible set.)*`):

> ` *(R27 note: the register re-declared at the `6e2b91a` re-pin: **42 port files = 41 mirrors (36 zero-action: 6 byte-exact + 30 mechanical-exact) + 5 carriers) + the host** — the AR-2 `use-keyframe-authoring.ts` twin added; `theme.ts` byte-exact → mechanical-exact (imports the shared volume-dB law through the barrel); the carrier set unchanged at the D30 irreducible set. The law HELD — census-check green live at `c020b2a`.)*`

**P2-6 · D12 (:316-:336) — the ratification note.** Append after D12's `**Implication:**` paragraph:

> `> **R27 ratification note:** the engine RATIFIED this ruling engine-side — nle-engine `.agents/DECISIONS.md` D12 (`1064f91`, 2026-09-13): opencut-timeline is THE editing engine; nle-engine's freecut-lineage stack (timeline.ts + TimelineData + the JSON-RPC wire + Player/scene-assembly/gpu-compositor/export) is the render/export venue model — venue-internal state like an IR, NOT a second user timeline; the junction is ONE-DIRECTIONAL; engine-native edit verbs are FROZEN as venue-internal (the offline reference implementation + the r1 port's parity twin). "The cross-repo ruling now has both signatures." The convergence landed with it: OV-01/OV-02 (the volume-dB + retime one-home collapse), OV-05 (el.transitionOut), OV-12 (the vendored-tree re-pin gate), M2 Wave 1 (the bus-law + sidechain parity).`

**P2-7 · D31.1 (:443) — the stale line pin.** `the engine's 3-point family \`performInsertEdit\` :4702` → `the engine's 3-point family \`performInsertEdit\` :5630 (the R27 re-anchor — timeline.ts is 8,997 LOC at `f9ac806`; every pre-R26 engine line pin is suspect)`.

**P2-8 · D36.2 (:467) — the maintainPitch residual.** After `R9-b maintainPitch → LANDED engine-side (the residual is the app's engineService hardcode — re-filed app-side)` insert: `*(R27: the residual RESOLVED — the app's H2-B/R9-b wave landed the full preservePitch threading: engineService.ts:481-513 (the patch merge: preservePitch given, else the engine retime's maintainPitch, else true) + sceneBridge.ts:229-262 (the back-projection: `maintainPitch: el.preservePitch !== false`); the app's PLAN row is dead text — the plan wave flips it)*`.

**P2-9 · D36.4 (:467) — the two landed rows.** After `WIRE_UI_EXCEPTIONS-export is a new small row)` insert: `*(R27: BOTH LANDED — the OT-side W8 guard rode the absorbed F4 wave (`9ee3537`..`3be2dea`, inside `fdb771c..55c81c0`); the WIRE_UI_EXCEPTIONS export landed as AR-1 (`d0db321`) and is CONSUMED live by the app's gate — 2A.10)*`.

**P2-10 · §3 (:~:556-:558) — the live-reading figures.** `(39 mirrors + the EngineMount host; 32 zero-action + 7 documented carriers, per Decision 26)` → `(41 mirrors + the EngineMount host at the `6e2b91a` re-pin — 36 zero-action + 5 documented carriers, per Decision 26 + the census register; the canonical tree is 41 files / 9,176 LOC at `970948a`)`.

**P2-11 · D11 clause 2 (:305) — the lineage tail.** `lineage: 24 at the R15 charter → 28 at the R22 pin → 30 at R23 → the routed/exceptions split at \`c15a629\`` → append ` → 25+5 at AR-2 (s16) → **28+3 at D-ARCH-6 (s17, `970948a`)**`.

### P3 — polish

**P3-1 · D32.6 (:451).** After `**32.6 — the defect row (S-engine, pre-r1, P1):**` insert `(FIXED at R26 — `8188d4e`, the 13-test pin file; see D36.2's R26 note)`.
**P3-2 · D13 (:342).** The count-chain tail: `and at R24 (759/759 @ \`85b81b0\` — §0's fleet rows)` → `and at R24 (759/759 @ `85b81b0`) and at R26 (773/773 @ `387f327`) and at R27 (777/777 @ `ec8fd5c`) — §0's fleet rows)`.
**P3-3 · §13.5 (:838).** Append to the no-overlap bullet: ` *(the E1 linked-insert defect that violated this invariant was fixed at R26 — `8188d4e`+`412693d`, the 13-test pin file `tests/vitest/engine/timeline-linked-source-edit.test.ts` at the engine)*`.
**P3-4 · :6 Consumers line.** `the nle-engine / opencut-timeline / web-daw-core workstreams` → `the seven-repo fleet workstreams (nle-engine / opencut-timeline / web-daw-core / nle-ui / nle-test-app / the in-repo mocks)` — the header metadata still names 3 of 7 (D21).

---

## §3 Cross-spec notes (for the cross-cut wave; recorded here, NOT edited by this audit)

1. **05:17 (the spec-side census mirror row)** — a full amendment behind its own promise cycle: still says "the next register amendment carries the re-declared values"; the amendment HAPPENED (`ed33050`/`6e2b91a`: 42 = 36+5+1). Scout-app filed it P1. 00's 2A.7 note (P2-5 above) is the master-side half.
2. **15's census sites** (§0:4, :14, §13.15, the §4.1A-adjacent rows) — the 24+6 readings + the QUEUED "wire exposes only the singular verbs" rows are FALSE now (briefing facts 1-2); 15's agent owns them; 00's four sites are P2-3/P2-4 above.
3. **06** — the E1 row flip + the stale engine line pins (:4719/:4702-class); 00's D31.1 (P2-7) is the same class.
4. **09 + 06 §5.12** — the `el.preservePitch` ElementJSON ruling (absent≡true; the engine's retime.maintainPitch the runtime projection) is THEIR law to carry; 00's piece is D36.2's R27 note (P2-8) + the D37 option (§4.4 below).
5. **20** — the WDC rows (773→777; the S-series surfaces; the lock sha `f5011b3`; the code pin IS `ec8fd5c`) — scout-wdc's register; 00's WDC row (P1-3) is the master half.
6. **18** — the preservePitch mock-local ledger row (stale), the "Gain dB is withdrawn" FALSE claim (the [−60,+20] dB rail is live — AW1-2), the 08-side color cluster (the wheels/absolute-vs-relative, the composition-order conflict at 08:299) — their agents.
7. **17** — the module-count re-tier row (202/297/737-class readings) re-pins to 748/632/777/690/252.
8. **19** — the fleet re-base (§0 BASE + §3 rows carry the R24 pins; its §0:13 was already re-keyed by the sibling waves to mini 415/variants-1521+).
9. **IMPLEMENTATION-PLAN.md** — two rows are stale: S-app's maintainPitch residual clause ("land the maintainPitch residual HERE — engineService.ts:477/:486 hardcodes maintainPitch: true") is DEAD TEXT (the code landed); S-engine's row lacks the RC-round queue (the **NS-1..NS-5 design queue + the r1-port collision map appear NOWHERE in the spec corpus** — grep-verified empty; the engine's `.agents/PLAN.md` RC section carries them; briefing fact 14 says the plan should).
10. **battery_r27** — the re-pin classes (all five modules + both consumer-pin sets + the census 42@6e2b91a + the 28+3 census-firing class + the 2A.10 live-registry class); also guard the **ARCH-R27 "(739)" trap** (the engine's true figure is 748 — cite the gates lines).

---

## §4 What the briefing missed that 00 uniquely sees

1. **The sibling edits 00's own fleet rows mid-flight — including the v11.0 dated record.** The mini R24-2 waves (`e391239`/`1e21511`/`2420d7e`, all 2026-09-13) each re-pinned 00's §0 mini row (357→377→396→415) AND **retroactively amended the v11.0 R26 status header's mini figure** (the R26 wrap `0fc7653` originally recorded "mini 355 (sealed)" — `git show 0fc7653:00-master-spec.md` — the header now reads 415). The same commits touched 19's §0, the plan, the register, and battery_r26. This collides with §2.5.1's revision-ledger design (the status header is the point-in-time record) and with mock-verdicts §3 rule 3 as written ("re-key only at their round-wrap declaration, never mid-flight" — true for OUR fleet's register keys, but the SIBLING re-keys the canon per-wave under "the register's own derivation law"). Recommendation: accept the living-ledger practice (the R27 card re-declares everything anyway — P1-5 marks the in-flight state honestly), but the v12.0 entry should note the practice once so future readers know pre-R27 header figures were re-keyed in-flight by the sibling stream. P3-class hygiene, 00-unique.
2. **The engine's OT vendor pin is a session behind the code pin** (`6e2b91a` vs `970948a`) with the D-ARCH-6 absorption as the engine's QUEUED next (engine `.agents/PLAN.md:481`, `HANDOFF.md:49`) — the briefing's vendor-coherence list names only the app's pins. Not a violation (the re-pin duty is filed at both ends), but 00's fleet row must record it or the engine→OT consumer reading looks incoherent with the OT module card. Same class: **the app's nle-ui pin `83ff8a8` is one commit behind HEAD `32abd58`** (the AW1-2 ceiling fix), with the absorption filed in the app's `c020b2a` queue.
3. **D29's "the exception registry is test-local upstream" clause is now FALSE** — AR-1 (`d0db321`) exported `WIRE_UI_EXCEPTIONS`/`WIRE_UI_EXCEPTION_VERBS` from the barrel and the app's gate consumes the LIVE export. The briefing's fact 12 covers the LAW; 00 additionally carries the now-false test-local claim as a D29 record clause (P2-3b).
4. **The ARCH-R27 "(739)" figure is a trap for the amendment wave** — scout-engine §6.1 proved 748 is the final (the gates lines at `c01a187`/`480a216` attest twice). 00's R27 card must quote 748 with the gates-line citation, not ARCH-R27's tables.
5. **The D37+ namespace is open — 00 sees exactly two candidate registrations, both already resolved-in-code model laws** (registering them is the W4/W5 waves' call, recorded here for the decision): (a) the **preservePitch dual-representation ruling** (ElementJSON field absent≡true; the engine's retime.maintainPitch the runtime projection; the app threading landed) — its law home is 09+06 §5.12, but a D37 registration would put the model decision in the D-index where the R26 HANDOFF's "model-decision ask" lived; (b) the **volume-dB [−60,+20] one-home law** (opencut's core/audio-params.ts the ONE HOME — the D7 amendment + AW1-2 + the engine's OV-01 value-import + the mini's audioDb pair). Both fit the D-register's "stated once, referenced everywhere" pattern (the 2A.10 route is the alternative — 2A.8/2A.9 paired with D30/D35 suggests standing laws get D-numbers when they carry a ruling).
6. **00's own layered-card convention is straining under three superseded engine cards** (458 R24 → 471 R26 → 748 R27 in one row). The convention (D10/D13/D21) is sound; the R27 amendment should not compress history, but the W6 wave may want to trim the pre-R24 cards to the D10-style "retired to history, see §0's fleet rows" pointer if the row length keeps growing — a presentation decision, not a defect.

**Bottom line:** 00 is structurally SOUND everywhere except the places the R26→R27 movement makes it FALSE: the five fleet-row module cards + both consumer-pin sets (P1), the missing 2A.10 standing law (P1), and a tight band of dated-note/line-pin refreshments (P2). The invariants (§13.5's 00:838 class) are coherent post-E1/E2 — the fix RESTORED the no-overlap law and pinned it. The D-register D1-D36 is faithful to the live truth everywhere except the five clauses P2-3/P2-4/P2-6/P2-8/P2-9 refresh. No re-litigation of settled law is needed; every fix is mechanical.
