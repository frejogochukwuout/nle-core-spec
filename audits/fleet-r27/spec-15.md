# Spec-15 audit — R27 Wave 3 (final-tightness, per-spec fleet)

**Task ID:** R27-W3-15 · **Agent:** spec-15 · **Date:** 2026-09-13
**Spec:** `nle-core-spec/15-wire-protocol.md` (5,229 lines, R24/R25-era text; the R25 amendment's D30-D32 content stands) — audited section by section at sub-spec depth.
**Ground truth (the R27 pin world):** opencut-timeline HEAD `55c81c0` / **code pin `970948a`** (632/632; `git diff 970948a..55c81c0 -- src/ scripts/` EMPTY — verified; the four trailing commits are docs-only) · nle-engine `f9ac806` (748/748; timeline.ts 8,997 LOC) · nle-test-app `c020b2a` (252/252; OT mirror `6e2b91a`, the D-ARCH-6 re-pin QUEUED — `vendor/nle-timeline/UPSTREAM.lock.json` read) · WDC `ec8fd5c` · nle-ui `32abd58`.
**Method:** every census/verb/law claim re-derived against the live tree — OT `api.ts` read in full at the relevant arms (union `:56-212`, `WIRE_COMMAND_TYPES` `:243-275`, tsc-asserts `:277-297`, `WIRE_UI_EXCEPTIONS` `:328-335` + derived Set `:356-358`, `validateInsertElement` `:634-809`, insertBatch arm `:1262-1370`, removeKeyframes arm `:2188-2285`, retimeKeyframes arm `:2286-2390`, never-guard `:2537-2545`, applyBatch `:2559-2616`); the pin files (`milestones-w11.ts:252-254` the 31-count pin, `:257+` the batch-atomic TRACK_LOCKED rewrite; `milestones-ar1.ts:180-229` the AR1-5 registry pin; `milestones-darch6.ts` the M58 family header); the gesture seams (`use-keyframe-drag.ts:105-123`, `use-timeline-actions.ts:401-431`, `view/page.tsx:535-599`); the core ops (`timeline-core.ts:887/:994` insertElements+withUndo, `:1726/:1791/:1863` removeElementKeyframes+bake, `:1966/:1987/:2037-2044` retimeElementKeyframes+clamp); the runner gate (`run-timeline-tests.mjs:390-425`); the report json (632/632). Scout-ot consumed in full; the engine/app/wdc scouts for the pin rows.

---

## §2 Per-section verdict table

| § | Section | Verdict | Evidence (file:line @ `970948a` unless noted) |
|---|---|---|---|
| 0 | FORWARD INVENTORY | **P1 ×2 + P2 ×6** | The census block (:14) states "**24 UI-routed verbs + 6 documented exceptions**" with an exception list naming `upsertKeyframe` (ROUTED since AR-2 `30bfe2a`) and `removeKeyframe`/`retimeKeyframe` (**RETIRED** at D-ARCH-6) — FALSE now (live: 31 = 28+3, `api.ts:243-275`/:328-335). The battery-relevant NOTE (:14) "OT exports NO exception constant; the registry is TEST-LOCAL upstream" — **FALSE since `40f22af`**: `WIRE_UI_EXCEPTIONS` is an exported tsc-asserted `Record<verb→law>` (`api.ts:328-335`, the `_WireUiExceptionsAreRealVerbs` assert `:341-352`) + the derived `WIRE_UI_EXCEPTION_VERBS` Set `:356-358`, barrel-re-exported, page-exposed (`view/page.tsx:494`), consumed LIVE by the M49C gate (`run-timeline-tests.mjs:390-399`) and by the app's ported gate (`nle-test-app/src/wire-coverage.test.tsx:29,59` — `import { WIRE_COMMAND_TYPES, WIRE_UI_EXCEPTION_VERBS } from '@vendor/timeline'`). P2s: all pins stale (OT `ded43c4`/`c15a629` 536 → `55c81c0`/`970948a` 632; app `c885ece` 174 → `c020b2a` 252; engine `5036387` 458 → `f9ac806` 748); the app's vendored lock-copy "byte-exact @ `c15a629`" (:15) → mirrors `6e2b91a` (pre-D-ARCH-6; re-pin filed `c020b2a`); :21 C7 GAP row "**30** prefixed wire names — 24 routed + 6 exceptions" → 31/28+3; :27-30 the D30 W-C..W-G rows are history (R25 already notes it); :31 "the landed 24 routed + 6 exceptions census is the whole truth" → false; :33 acceptance counts (OT 536/59 entries; app 174; engine 458) → 632 (452 in-page + 30 DragEvent/keyboard + 150 real-mouse; report json authority) / 252 / 748. |
| 0A | TL;DR | SOUND | No live-tree claims. |
| 1 | Purpose + unifying abstraction | SOUND | Layer framing unchanged; Decision 9 adoption text intact (00 §2 :222). |
| 2 | Goals | SOUND | The 10 goals unaffected by the round (goal 6's 1:1 law is union-side). |
| 3 | The Three Layers | SOUND | Unchanged. |
| 4.1 | The discriminated union | SOUND | 78 members counted independently (17+7+5+7+4+4+4+2+4+3+5+4+3+4+3+2+1+3 = 78 ✓). The union is spec-side law; the batch verbs' landing is an OT-side surface event (r1 param-alignment is the open decision — §4 fix-list). |
| 4.1A | Routing-disposition table | **P1 + P2 ×3** | The Keyframe row (:320) "per-key singular forms implemented for upsert/remove/retime" — FALSE now: the wire exposes `upsertKeyframe` (singular, ROUTED), `removeKeyframes`/`retimeKeyframes` (plural batch), and the singular remove/retime are GONE (`api.ts:255-256`; absence pinned `milestones-ar1.ts:201-210`). P2s: the :296 R24-reading preamble (census 30=24+6, "27 of 78 counterparts") needs the R27 re-read + the counterpart re-derivation (see §3.3 — the counting-convention ruling); :302's engine anchors `timeline.ts:2984/4143/4246/3155` (roll/slip/slide/rateStretch) are pre-R26 — live: rollingTrimItems `:3324`, slip `:4956`, slideItem `:5097`, rateStretchItem `:3840` (engine `f9ac806`, 8,997 LOC); :304's `ElementPatch muted/hidden @ timeline-core.ts:126/:140` — live: `hidden?: boolean` at `:141`, and `muted` rides `params?: ElementParams` (shallow-merge), not a top-level patch field — precision note (P3-grade, same edit). |
| 4.1B | NOT_IMPLEMENTED code | SOUND | Registered into §6.3 ✓ (both sites agree). |
| 4.2 | Command → manager mapping | SOUND | Spec-side mapping table; untouched by the round. |
| 4.3.1-4.3.78 | Command definitions | SOUND (with 2 cross-notes) | Union-side contracts unchanged. Spot-verified: §4.3.2 TrimCommand `{elementId, edge, delta}` (the OT group+side controller shape remains a §13.15 CONVERGENT row — wire `api.ts:94-99` unchanged); §4.3.5 RollCommand / §4.3.6 Slip (the R25/P7 `composition` fix stands) / §4.3.7 Slide — none landed on the OT wire (correctly r1 wave 1); §4.3.9's R25/P11 re-key (insert{ripple} never produces an insert-edit push; D31.6 delta-vs-full law) verified against 06 §5.9E — SOUND. Cross-notes (not 15's edits): §4.3.66's singular-absolute `retimeKeyframe` now has NO OT counterpart (the plural delta-shaped `retimeKeyframes` is an OT EXTENSION — `api.ts:177-197` names it); §4.3.65's per-element `{elementId, keyframeIds[]}` vs OT's cross-element flat member lists is the r1 param-alignment decision (§4-A P1-3 carries it). |
| 4.4 | apply() dispatcher | SOUND + P3 | Exhaustive-switch law intact. P3: the example emits `NO_ELEMENTS_AT_TIME` (:2241) — not in §6.3's code list ("extend as needed" covers it; register or genericize at the r1 envelope refinement). |
| 5 | Command examples | SOUND | Unchanged; the §5.10 batch examples remain valid. |
| 6 | CommandResult | SOUND; §6.3 **P2** | §6.1/6.2 unchanged. §6.3's code list is spec-side law — SOUND; the P2 is its R24 annotation's OT-side citations (:22 GAP row + §13.15 :4910): "TRACK_LOCKED … extended by W11 to the singular keyframe verbs (R-A P3-9: `removeKeyframe`/`retimeKeyframe` … api.ts:1462/:1517/:1549; 12 commands total)" — the singulars are retired; the live law is **batch-atomic TRACK_LOCKED** on `removeKeyframes` (`api.ts:2239-2246`) + `retimeKeyframes` (`:2337-2344`) + upsertKeyframe (`:2139`); the 12-command lock-pre-check count SURVIVES the retirement (insert :1202, insertBatch :1290, move :1497-1499, trim :1554, split :1616, delete :1671, rippleDelete :1691, duplicate :1711, updateElements :1897, upsert :2139, removeKeyframes :2239, retimeKeyframes :2337 — counted live). The F1A-3/F16/CONFLICT codes: the landed reality MATCHES the envelope (INVALID_PARAMS/NOT_FOUND/CONFLICT/NOOP/TRACK_LOCKED/INTERNAL_ERROR — `api.ts:217-223`, the exact 6-code set §13.15 already records). |
| 7 | Command Batch | SOUND; §7.1A **P2 ×2** | The five invariants + the R23 F1B-7 controlling note stand. P2s: (a) the V-S16 NEW-1 extension is missing — the intra-batch overlap guard (invariant 5) is now per-command on the move/updateElements/trim VALIDATORS (the shared `timeSpansSelfOverlap`, `2492a24`, M56-pinned; the save-brick class) — one sentence extends §7.1A; (b) the R23 note's line refs drifted (`api.ts:1758-1771` → `:2596-2608`; `:1767` → `:2604`; `timeline-core.ts:2389-2391` clearRedo → `:2429`; clearFuture `:262` ✓ endTransaction `:243` ✓ — hold). |
| 8 | Wire Protocol (HTTP) | SOUND | Untouched by the round; §8.10's export-artifact law unchanged. |
| 9 | Event Stream | SOUND (§9.5 spot-verified) | §9.5's staircase register carries NO census/pin claims — the w2 GAP status unchanged (the M2 Wave 1 bus-law landed engine/app-side but the §9.5 mapping-register row is the app adapter's charge, correctly phased). P3: "the app's `nle-app/src/events/` adapter" — the repo is `nle-test-app`. |
| 10 | Versioning | SOUND | Unchanged. |
| 11 | Zod schemas | SOUND | §11.1 mirrors §4.3 exactly (all 78 present, incl. the keyframe quartet `:4241-4280` and the export trio); §11.2-11.4 unchanged. |
| 12 | Test Harness | SOUND | No pin/census claims inside §12. |
| 13.1-13.13 | Relationships | SOUND | Spot-verified: §13.1 (01's apply/exec split), §13.2 (06's 14-op inventory), §13.5 (16's 181 bindings + §8.3 resolver — cross-note: 16 §3.12's keyframe-nudge rows cite the singular `retimeKeyframe` absolute form, 16's own edit), §13.11 (10's un-gating), §13.12/§13.13 all resolve. |
| 13.14 | Code refs — nle-engine | **P2** | The R24 note's pin (`5036387`, 458/458, "455 names", "vendored OT submodule pins `c15a629`") → `f9ac806` 748/748; the 19-case dispatch STILL exact (`applyOp` `headless/api.ts:785`, header `:773` — re-verified live); the R7-era table line refs drifted badly (undo `:5247`→`:7350`, snapshot `:5292`→`:7395`, splitClip `:2275`→`:2620`); "37,958 LOC" stale; the api-surface freeze exists (`tests/vitest/engine/api-surface.frozen.ts` + `.test.ts` ✓ — re-freeze count needs the re-pin); the engine's OT vendor pin is now `6e2b91a` (one session behind the code pin — the queued absorption, engine PLAN:481). |
| 13.15 | Code refs — opencut-timeline | **P1 ×3 + P2 ×4** | P1s: (1) the preamble (:4901) + discriminator row (:4905) census/registry claims — "30 types", "24 UI-routed + 6 exceptions", "the 6 documented exceptions live in the RUNNER's registry, test-local upstream — no exported constant", `api.ts:41-151`/`:556-1716`/`:1717`/`:182-213`/`:216-230`, runner `:283-300`/`:301-336` — ALL stale/false (live: 31 types; union `:56-212`; `WIRE_COMMAND_TYPES` `:243-275`; asserts `:277-297`; the exported registry `:328-335`; gate live-read `:390-425`; M49C 28/3 PASS per the report json); (2) the §4.3.64-65 QUEUED row (:4915) — "the wire exposes only the singular verbs" FALSE (the batch verbs LANDED; the singulars RETIRED); (3) the §4.3.9-batch QUEUED row (:4916) — "the library multi-insert currently runs applyBatch([insert × N]) — N history entries" FALSE (ONE `insertBatch` per kind-group, staggered construction, `view/page.tsx:535-599`; M49R inverted to the one-entry law). P2s: :4910 (§6.3 row — the batch-atomic TRACK_LOCKED re-key, above); :4912 (applyBatch line refs `:1739-1779`→`:2559-2616`, `data.results` `:1753-1775`→`:2573-2612`); :4914 (command-coverage row "27 of 78" → re-derive, §3.3); :4918-4921 (the four R25 r1-scheduled rows' census clauses "the landed 24 routed + 6 exceptions stand untouched" — false as census statements; the SCHEDULING itself remains correct — none of replace/append/ripple-overwrite/fit-to-fill landed, verified against the 31-verb array). The C7 disposition rows for `advancePlayhead`/`setAllLocked`/`setAllMuted` — UNCHANGED ✓ (scout-ot "not impacted" confirmed). |
| 14 | Open Questions | SOUND | §14.11's output-exception ruling stands; no question is invalidated by the batch-verb landing. |
| 15 | Implementation Notes | SOUND | P3: the `src/engine/...` layout is greenfield-side (fine as spec). |
| 16 | Summary | SOUND | The 78/55/23 accounting re-verified ✓. |

**Counts: P1 ×5 · P2 ×16 (6 edit families) · P3 ×4.** (P1s: the §0 census block; the NO-exception-constant claim family; the §13.15:4915 QUEUED row; the §13.15:4916 QUEUED row; the §4.1A:320 Keyframe row.)

---

## §3 The 31-verb re-declaration (focus #1 — the full draft)

### 3.1 The verified census (read from code three ways)

`WIRE_COMMAND_TYPES` at `api.ts:243-275` = **31 verbs** (tsc-lockstep asserts `:277-297` hold it == the `TimelineCommand` union `:56-212`, both directions; the w11 count pin `milestones-w11.ts:252-254` asserts `length === 31`). Verified three ways (scout-ot §1.2 + re-verified here): (a) the array counted live; (b) the M49C gate artifact — `download/timeline-test-report.json` (committed in `970948a`) records "fired 28 distinct command types through the REAL UI (origin:\"ui\" dispatches only …) across the phases; 3 documented exceptions; no gaps" PASS; (c) the M55R AR2R-6 arithmetic pin (`scripts/m55-ar2-real-mouse.mjs:525-567` — 31 = 28+3, page-local).

**The routed 28:** `timeline.{delete, duplicate, insert, insertBatch, move, moveBookmark, pause, play, redo, removeBookmark, removeKeyframes, retimeKeyframes, rippleDelete, seek, setLoopRegion, setPlaybackRate, split, toggleBookmark, undo, updateElements, upsertKeyframe}` (21) + `track.{add, remove, setAllLocked, setAllMuted, toggleLock, toggleMute, toggleVisibility}` (7).

**The 3 exceptions** (`WIRE_UI_EXCEPTIONS` `api.ts:328-335`, an exported tsc-asserted `Record<verb→law>`): `timeline.selectElements` (selection is VIEW state; remote-consumer verb) · `timeline.advancePlayhead` (the ticker is rAF-local) · `timeline.trim` (uniform-delta engine shape; the UI's resize commits through `updateElements` patches).

**The lineage:** R15 charter 24 → 28 actual at R22 `05584d8` → 30 at R23 `222532c` → **24 routed + 6 exceptions** at R24 `c15a629` → **25 routed + 5 exceptions** at AR-2 `30bfe2a` (upsertKeyframe ROUTED) → **31 = 28 routed + 3 exceptions** at D-ARCH-6 `970948a` (+3 batch verbs, −2 retired singulars, 1 re-classified).

### 3.2 The batch verbs' exact semantics (all code-verified)

- **`timeline.insertBatch`** (type `api.ts:66-88`; arm `:1262-1370`): ONE history entry (wraps ONE `core.insertElements`, `timeline-core.ts:887`/`withUndo("Insert elements", …)` `:994`); first element at `startTimeTicks`, later members keep RELATIVE offsets; per-member validation through the **shared `validateInsertElement`** (`api.ts:634-809` — the singular insert's ~170-line block extracted, one writer incl. SEAL SE-1 type discriminant + W9 E9 graphic/effect fields); **F1A-3 mixed-kind → INVALID_PARAMS naming the kind-split duty** (`:1326-1331`); **intra-batch self-overlap or placement failure → CONFLICT, whole-batch atomic** (`:1365-1369`); **empty → INVALID_PARAMS** — the deliberate divergence from delete's NOT_FOUND-for-empty, pinned M58-1 (`:1269-1282`); explicit-onto-locked → TRACK_LOCKED (`:1290-1296`); success echoes the minted refs `data.elements` (`:1354-1364`); placement REJECTED-NOT-SHIFTED (the group lands as-requested or one new track is minted).
- **`timeline.removeKeyframes`** (type `:156-176`; arm `:2188-2285`): cross-element flat member lists (marquee spans elements); ONE history entry (classic's batch-manager law, `timeline-core.ts:1726`/`withUndo("Remove keyframes")` `:1791`); **the WYSIWYG bake rides the core op through the wire** (channel-empties → value-at-playhead baked into the base param, `timeline-core.ts:1834-1863` + the E3 base-resolution `:3249+`); wire-side dedup by the 4-tuple (`:2229-2235`); batch-atomic TRACK_LOCKED (`:2239-2246`); **F16 pre-resolution against committed state — a stale ref is a NAMED NOT_FOUND** (`:2252-2263`); post-state echo = one `{elementId, keyframes}` per distinct element (`:2271-2278`).
- **`timeline.retimeKeyframes`** (type `:177-197`; arm `:2286-2390`): **delta-shaped** (shift every selected key by `deltaTicks`, each clamped to its element's `[0, duration]` — `timeline-core.ts:2037-2044`, the clamp observable through the wire); cross-element; ONE history entry (`withUndo("Retime keyframes")` `:1987`); **the F4 benign zero-delta — `delta 0 → {ok:true, data:{changed:false}}`, NEVER the NOOP failure code** (`api.ts:2358-2366`); non-integer delta → INVALID_PARAMS (`:2320-2327`); dedup + batch-atomic lock + F16 as removeKeyframes. OT itself names the union divergence: "an OT EXTENSION pending the r1 §4.1A union decision" (`:183-186`) and "the spec's per-element {elementId, keyframeIds[]} shape is the r1 param-alignment decision, not ours to pre-empt" (`:163-166`).
- **The retirement:** the singular `removeKeyframe`/`retimeKeyframe` are GONE from the union (a stale singular dispatch falls to the never-guard's typed INVALID_PARAMS "unhandled command", `api.ts:2537-2545`; absence pinned `milestones-ar1.ts:201-210`); the absolute-retime capability composes as `deltaTicks = target − key.time` (the harness migration, `ec86ad3`).
- **The three gesture-commit seam switches** (origin "ui"): keyframe-drag → `retimeKeyframes` (`use-keyframe-drag.ts:105-123`); deleteSelected keyframe branch → `removeKeyframes` (`use-timeline-actions.ts:401-431`); library multi-insert → ONE `insertBatch` per KIND with **staggered construction** (F1: `startTime[i] = playhead + Σ durations[<i]`) + **kind-split at the library layer** (F2: the `byKind` map, `view/page.tsx:535-599`); M49R's N-entries pin INVERTED to the one-entry law (homogeneous pair = ONE entry; mixed = N-kinds).
- **The origin-attribution law (HA-4-2):** the M49C gate counts ONLY origin:"ui" dispatches (`api.ts:366-380` the `WireLogEntry.origin`; the runner's gate phase prints "origin:\"ui\" dispatches only — test-handle dispatches never count") — the spec's "machine-checked" claims predate it; fold one clause into the re-base.

### 3.3 The counterpart arithmetic (the counting-convention ruling — flagged for the W4 cross-cut)

Spec 15 §4.1A:296/§13.15:4914 say "27 of 78 union members have live OT wire counterparts" (@ `c15a629`). At `970948a` the moved members: `upsertKeyframes` ← `timeline.upsertKeyframe` (AR-2's routing made the counterpart unambiguous), `removeKeyframes` ← `timeline.removeKeyframes` (the plural landed where the singular once stood), `retimeKeyframe` (§4.3.66 singular absolute) ← **no counterpart** (the plural delta form is the OT extension), `insert` ← `timeline.insertBatch` (the batch form of an already-counted member, or the §4.3.9-superset slot). **Natural reading: 29 of 78** (27 + upsert-reclassified-routed + removeKeyframes; insertBatch counted toward insert's existing slot). This is a counting-convention ruling (scout-ot open Q2): the W4 keyboard+wire-census cross-cut owns the fleet-coherent number; my fix-list proposes the 29-reading with the convention stated inline so the amendment wave can apply it and W4 can flip the convention in one place if it rules otherwise.

---

## §4 Fix-list — the exact edits (mechanically applicable)

**P1-1 — §0:14, the census block re-declaration.** Replace from "The wire surface (`src/lib/timeline/headless/api.ts`): **the census RE-BASED…" through "…3 OT-side verbs have no union member yet (…)" with:

> The wire surface (`src/lib/timeline/headless/api.ts`): **the census re-declared at D-ARCH-6 (session 17, code pin `970948a`): 31 verbs = 28 UI-routed + 3 exceptions** (machine-checked three ways: the tsc-lockstep asserts; the M49C coverage gate, 28/28 PASS; the M55R arithmetic pin). The routed 28 (`WIRE_COMMAND_TYPES`, api.ts:243-275; tsc-lockstep both directions at :277-297): `timeline.{delete, duplicate, insert, insertBatch, move, moveBookmark, pause, play, redo, removeBookmark, removeKeyframes, retimeKeyframes, rippleDelete, seek, setLoopRegion, setPlaybackRate, split, toggleBookmark, undo, updateElements, upsertKeyframe}` (21) + `track.{add, remove, setAllLocked, setAllMuted, toggleLock, toggleMute, toggleVisibility}` (7). The 3 exceptions (`WIRE_UI_EXCEPTIONS`, api.ts:328-335 — **an exported tsc-asserted `Record<verb→law>`**, see the AR-2 consumption-law row below): `timeline.selectElements` (selection is VIEW state — the remote-consumer surface), `timeline.advancePlayhead` (the ticker is rAF-local), `timeline.trim` (the uniform-delta engine shape; the UI's resize commits as `updateElements` patches). The census lineage: the R15 charter's 24 → 28 actual at the R22 pin `05584d8` → 30 at the R23 pin `222532c` → the routed/exceptions split at `c15a629` (24+6) → **25 routed + 5 exceptions at AR-2 `30bfe2a`** (upsertKeyframe ROUTED by the dblclick authoring) → **31 = 28+3 at D-ARCH-6 `970948a`** (+3 batch verbs; −2 retired singulars — `removeKeyframe`/`retimeKeyframe` GONE, a stale dispatch hits the never-guard's typed INVALID_PARAMS at api.ts:2537-2545). The **M49C coverage gate** machine-checks the split (runner :390-425: reads BOTH registries LIVE from the page — no driftable copy; **origin-attributed — only origin:"ui" dispatches count**; 28/28 + 3 exceptions PASS, the report json the authority).

Also in the same block: update the W11-widenings citation set (`api.ts:182-213`→`:243-275`; `:216-230`→`:277-297`; the attach/recorder cites `:241-258`/`:260`/`:277-300`/`:305-320`/`:324-329` — re-anchor at the re-pin pass; `applyBatch data.results` `:1753-1775`→`:2573-2612`; the keyframe-verb TRACK_LOCKED sentence → "the batch-atomic keyframe-verb TRACK_LOCKED (R-A P3-9, rewritten batch-atomic at D-ARCH-6: `removeKeyframes`/`retimeKeyframes`/`upsertKeyframe` lock-pre-check whole-batch, api.ts:2139/:2239-2246/:2337-2344 — still 12 commands total, counted live)"). And: "27 of the 78 union members now have live OT wire counterparts" → "29 of 78 (the natural reading: +`upsertKeyframes` via the routed singular, +`removeKeyframes` via the landed plural; `insertBatch` counts toward insert's slot; `retimeKeyframe`'s singular-absolute form has NO counterpart — the plural delta form is an OT extension pending the r1 §4.1A decision)".

**P1-2 — the AR-2 consumption-law row (three sites: §0:14 NOTE, §0:30 R8 clause, §13.15:4901/:4905).** Replace the NOTE with:

> **NOTE (battery-relevant): the exception registry is an EXPORTED ENGINE SYMBOL since `40f22af` (AR-1 D-ARCH-3): `WIRE_UI_EXCEPTIONS` is a first-class tsc-asserted `Record<verb→law>` (api.ts:328-335 — drift is a compile error via the `_WireUiExceptionsAreRealVerbs` assert at :341-352) + the derived `WIRE_UI_EXCEPTION_VERBS` Set (:356-358), barrel-re-exported, page-exposed (view/page.tsx:494), consumed LIVE by the M49C gate (run-timeline-tests.mjs:390-399) and by the app's ported gate (nle-test-app src/wire-coverage.test.tsx:29 — `import { WIRE_COMMAND_TYPES, WIRE_UI_EXCEPTION_VERBS } from '@vendor/timeline'`). The runner's test-local copy was DELETED. Consumption law (the 00-master §2A.10 standing law): a consumer gate consumes the exported registry, never a hand-mirrored copy.

And the R8-row clause "(the exception registry re-declared app-side, TEST-LOCAL — upstream exports no exception constant; drift is tsc-enforced at the re-exported constant)" → "(the gate consumes the LIVE `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel — the AR-2 consumption law; the app's R27-era gate already does, `wire-coverage.test.tsx:29/:59`)".

**P1-3 — §13.15:4915, the §4.3.64-65 row flips to LANDED.** New row text:

> | §4.3.64-65 keyframe batch forms — **LANDED (D-ARCH-6, BASE @ `970948a`)** | `timeline.removeKeyframes` (api.ts:156-176, arm :2188-2285) + `timeline.upsertKeyframe` UI-ROUTED (AR-2) — ONE history entry each; the wire's cross-element flat member lists (`{elementId, trackId, propertyPath, keyframeId}[]`); the WYSIWYG bake through the wire (timeline-core.ts:1834-1863); wire-side dedup; batch-atomic TRACK_LOCKED; **F16: a stale ref is a NAMED NOT_FOUND** (api.ts:2252-2263); the singular `removeKeyframe`/`retimeKeyframe` RETIRED (never-guard :2537-2545; absence pinned milestones-ar1.ts:201-210) | **BASE** | The census re-declared MECHANICALLY as D29-F8 promised (no census amendment); the r1 param-alignment decisions the landing forces: (a) `retimeKeyframe` (§4.3.66, singular absolute) vs OT's plural delta-shaped `retimeKeyframes` (+ the benign zero-delta `{changed:false}` law); (b) param grouping: the union's per-element `{elementId, keyframeIds[]}` vs OT's cross-element flat lists; (c) 16 §3.12's nudge rows ride decision (a). OT deliberately did not pre-empt (api.ts:163-166/:183-186).

**P1-4 — §13.15:4916, the insertBatch row flips to LANDED.** New row text:

> | §4.3.9 InsertCommand batch form — **LANDED (D-ARCH-6, BASE @ `970948a`)** | `timeline.insertBatch` (api.ts:66-88, arm :1262-1370): ONE history entry; the shared `validateInsertElement` (api.ts:634-809 — one writer for the element-field law); F1A-3 mixed-kind INVALID_PARAMS; intra-batch overlap CONFLICT whole-batch; empty INVALID_PARAMS (the deliberate divergence from delete's NOT_FOUND-for-empty, M58-1); TRACK_LOCKED; the minted-refs echo; the library multi-insert switched to ONE insertBatch per kind-group with staggered construction (view/page.tsx:535-599); M49R inverted to the one-entry law | **BASE** | The C7 rename absorbs it as bare `insertBatch`; the **bare-verb vs `insert {elements[]}` superset decision stays OPEN** (the r1 param-alignment wave's charge); the 06 §5.9D kind-split + append-riding rows point here |

**P1-5 — §4.1A:320, the Keyframe row re-key.** Replace with:

> | **Keyframe** | `upsertKeyframes`, `removeKeyframes`, `retimeKeyframe`, `updateKeyframeCurves` (4) | OT | wire @ `970948a`: `upsertKeyframe` (singular, UI-ROUTED since AR-2), `removeKeyframes`/`retimeKeyframes` (plural batch verbs, D-ARCH-6 — ONE history entry each; the singular remove/retime RETIRED); `retimeKeyframe` (§4.3.66's singular-absolute form) has NO OT counterpart — the plural delta form is the OT extension pending the r1 union decision; `updateKeyframeCurves` still no OT form → **r1**; batch engine-ops (timeline-core.ts:1726/:1966, R27 re-pinned) |

**P2-1 — the pin re-base family (§0:4/:14-16/:33, §13.14 header, §13.15:4903).** One edit family: OT → HEAD `55c81c0` / code pin `970948a`, **632/632** (cite total only — the in-page/real-mouse split convention changed shape: 452 + 30 DragEvent/keyboard + 150 real-mouse per the report json); app → `c020b2a` **252/252** + the vendored mirror `6e2b91a` (the D-ARCH-6 re-pin QUEUED, `c020b2a`'s filing — the byte-exact-`c15a629` claim retires); engine → `f9ac806` **748/748** (timeline.ts 8,997 LOC; the 19-case dispatch still exact — re-verified); WDC `ec8fd5c` 777/777; nle-ui `32abd58` 690. §13.14's R7-era table: undo `:5247`→`:7350`, snapshot `:5292`→`:7395`, splitClip `:2275`→`:2620`; "37,958 LOC" → the live figure at the re-pin.

**P2-2 — §4.1A:296/:302/:304 + §13.15:4905-4914 re-anchor.** The :296 preamble gets the R27 re-read (census 31=28+3; counterparts 29-of-78 per §3.3 with the convention stated); :302's engine anchors → `rollingTrimItems timeline.ts:3324 / slip :4956 / slideItem :5097 / rateStretchItem :3840`; :304 → `hidden` at timeline-core.ts:141 + `muted` via `params?: ElementParams` (the shallow-merge). §13.15:4905 the discriminator row re-write (31 types; union `:56-212`; WIRE_COMMAND_TYPES `:243-275`; asserts `:277-297`; never-guard `:2537-2545`; the exported registry `:328-335`; the gate at runner `:390-425`; the rename-pass cell: `removeKeyframes`/`retimeKeyframes` map to their union names directly now — the per-key-singular mapping sentence retires). §13.15:4912 applyBatch `:1739-1779`→`:2559-2616`.

**P2-3 — §13.15:4910 (the §6.3 row's OT-side cell).** "extended by W11 to the singular keyframe verbs (R-A P3-9: `removeKeyframe`/`retimeKeyframe` report TRACK_LOCKED … api.ts:1462/:1517/:1549; 12 commands total)" → "rewritten batch-atomic at D-ARCH-6: `removeKeyframes`/`retimeKeyframes` lock-pre-check the WHOLE batch (api.ts:2239-2246/:2337-2344, + upsert :2139); still 12 lock-checked commands (counted live); the w11 P3-9 pin itself was rewritten batch-atomic (milestones-w11.ts:257+)".

**P2-4 — §7.1A + its R23 note.** Add one sentence to invariant 5: "**V-S16 NEW-1 (R26, `2492a24`, M56-pinned): the intra-batch overlap guard is PER-COMMAND — any wire param carrying N spans (move, updateElements, trim) validates its own list through the shared `timeSpansSelfOverlap`; a committed intra-batch overlap previously poisoned the scene AND bricked the save file at the HB-2 load boundary.**" And refresh the note's line refs (`api.ts:1758-1771`→`:2596-2608`; `:1767`→`:2604`; `timeline-core.ts:2389-2391`→`:2429`).

**P2-5 — the census-clause sweep in the R25 r1-scheduled rows (§13.15:4918-4921) + §0:21/:31.** Each "the landed 24 routed + 6 exceptions census is UNTOUCHED / stays exactly as-is" clause → "the landed census (31 = 28+3 since D-ARCH-6) is unaffected by this row" — the scheduling verdicts themselves all STAND (replace/append/ripple-overwrite/fit-to-fill: none landed, verified against the 31-verb array; the §4.3.9 ripple-flag D31.6 law unchanged).

**P3-1..4.** (a) §9.5: "nle-app/src/events/" → "nle-test-app's events adapter". (b) §4.4: register `NO_ELEMENTS_AT_TIME` in §6.3's list or genericize. (c) §4.1A:304: the muted-via-params precision (folded into P2-2). (d) File upstream (OT-side, NOT a spec edit): `api.ts:235`'s header comment still says "the 30 wire command types" over a 31-entry array; SEAMS.md:184-185 retains a historical 24+6 line.

---

## §5 Cross-spec notes (for the cross-cut wave — NOT my edits)

1. **The stale-census family (fleet-wide, one edit family each):** 00 (5 sites incl. :17 the pin-canon fleet row — drives battery_r27's pin-lag checks), 05 (§0:14 exception enumeration names retired verbs — P1-grade; §16.5:1260 "30-type … 24+6"), 06 (§0:12/:29/:32 + §5.9D AP-1 GAP row :1638 — "applyBatch([insert × N]) stacking" now FALSE, insertBatch + kind-split + staggered construction landed; §10.4 note/:2596, §10.5 W11 row/:2616), 12 (§0:4 "the census both key on" — the K2/K3 instruments re-key to 28+3 via the exported registry), 17 (:4/:15/:38/:2359/:2360 — "24 routed wire verbs", OT 536), 18 (:514 the OT row), 19 (:5/:13/:26/:48/:72-73/:137/:309/:352/:365/:413 — 22 `c15a629` hits + "all 30 headless types" + the C7 row).
2. **16 §3.12's keyframe-nudge rows (:439-440)** cite the singular `retimeKeyframe` absolute-time form — the OT wire now exposes only the plural delta form; the annotation belongs to 16, the decision to 15's r1 (my P1-3 carries it).
3. **05 §8 / 18 §4.2 / 16 §3.12** carry no keyframe-gesture rows (the AR-2 dblclick authoring + the drag/delete/multi-insert seam switches) — the scout's #16 minimal-carrier recommendation stands; 15's flipped rows + the registry-export note carry the wire-side truth.
4. **The counting convention (29-of-78)** — W4's keyboard+wire-census cross-cut must rule it fleet-coherently (my §3.3 proposes the natural reading).
5. **battery_r27 classes (echoing scout-ot §4.8):** (a) census-coherence 24+6 → 31/28+3 with the lineage tail; (b) a "the exception-registry export exists" class (the P1-2 flip's fleet twin); (c) cite the 632 total only (the split convention changed shape).

---

## §6 What 15 uniquely sees

1. **The census re-declaration is 15's alone to draft** — every other spec consumes it. The full 31-verb text (§3.1) is ready to paste; the lineage tail (…→ 25+5 at AR-2 → 31=28+3 at D-ARCH-6) is the single sentence the whole fleet's re-keys will cite.
2. **The two QUEUED→LANDED flips are the round's only verdict inversions in the corpus** — the §13.15 rows were written as endorsements of NOT-yet-landed verbs; D29-F8's "no spec amendment for the census itself" held exactly as designed, but the rows' OT-state cells are now false, and the flip text must carry the three r1 param-alignment decisions (singular-absolute vs plural-delta retime; per-element vs cross-element flat param grouping; bare insertBatch vs the insert{elements[]} superset) because OT deliberately left them open (`api.ts:163-166/:183-186`) — this is the one place the corpus records them.
3. **The AR-2 consumption-law row (P1-2) is 15's highest-leverage edit after the census**: the "OT exports NO exception constant" NOTE was battery-relevant boilerplate cited by the app-side rows of other specs; its flip (the exported tsc-asserted Record + the deleted runner copy + the app's live-barrel import) is the concrete instance of 00's proposed §2A.10 standing law.
4. **The 12-command TRACK_LOCKED count SURVIVES the retirement** — the batch verbs replaced the singulars one-for-one in the lock pre-check (12 sites counted live) — a non-obvious continuity the §6.3 row's re-key must preserve rather than re-derive.
5. **The r1-scheduled verb rows' phasing is verified INTACT** — none of replace/append/ripple-overwrite/fit-to-fill landed at `970948a`; only their embedded census clauses are stale. 15 is the only spec whose edit is a clause-sweep rather than a verdict change there.
6. **The §4.1A Keyframe row is the one normative-table P1** — the routing-disposition table is the app bus's implementation map; a consumer compiling against the retired-singulars reading would mis-scope its keyframe dispatch.
