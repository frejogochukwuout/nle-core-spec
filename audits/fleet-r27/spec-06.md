# R27-W3-06 — spec-06 (06-nle-ops.md) per-section audit

**Agent:** spec-06 (Wave 3, per-spec fleet) · **Round:** R27 final-tightness
**Spec:** `/home/z/my-project/nle-core-spec/06-nle-ops.md` (3,670 lines) — audited section by section.
**Ground truth re-derived live:** nle-engine `f9ac806` (timeline.ts **8,997 LOC**, 748/748 per `.agents/HANDOFF.md:14`), opencut-timeline HEAD `55c81c0` / code pin `970948a` (**632/632**, `WIRE_COMMAND_TYPES` = **31 verbs** at `api.ts:243-275`, `WIRE_UI_EXCEPTIONS` = **3** at `:328-335`), nle-ui `32abd58` (690/690), nle-test-app `c020b2a` (252/252), WDC `ec8fd5c` (777/777). Predecessors consumed: all six Wave-2 mock reports + all five Wave-1 scouts (their 06-side rows re-verified, not re-derived).

**Method:** every §10.4/§10.5/§0 engine + OT line pin re-grepped at HEAD; the E1/E2 fix commits (`8188d4e`+`412693d`) and the pin file (`timeline-linked-source-edit.test.ts`, 26 it-blocks) read live; the E3 silent returns confirmed at `:5726/:5772/:5855/:6181` (+`:6270`); the D-ARCH-6 census verified at the OT barrel; the R9-b/F4/FF-3 laws read in nle-ui/app/variants code; the /tmp reference checkouts confirmed GONE.

---

## 1. Per-section verdict table

| § (lines) | Verdict | Findings (all evidence re-derived this audit) |
|---|---|---|
| Header/Status (:1-8) | **P2** | R25-round record; fine as history, but it pins the "24 routed + 6 exceptions" census (:4) — stale (31 = 28+3, corpus-wide fact 1). |
| **§0 matrix + BASE/GAP (:10-48)** | **P1 ×2 + P2 ×4** | **(P1a)** Matrix engine column ALL stale pins: Roll `:2984`→**:3324**, Ripple trim `:2839`→**:3132**, Slip `:4143`→**:4956**, Slide `:4246`→**:5097**, Insert `:4702`→**:5630**, Overwrite `:4860`→**:6007** (rg at f9ac806). **(P1b)** The Insert row's cell still says "the E1 linked defect open" (:22) and the Overwrite row "E2/E3 open" (:23) — **both FALSE now**: E1 FIXED R26 (`8188d4e`, §5.0's own E1 row :438 is flipped) and E2 FIXED R26 (verified: the fate-based companion law + the straddle/cascade/carve/re-pair pins live in `timeline-linked-source-edit.test.ts`; commit `8188d4e` "E1+E2 FIXED"). The matrix contradicts its own §5.0/§5.9B — the E2 flip is **half-done** (body :1590 yes; GAP row OW-2 :1599, matrix :23, §10.4 row :2584 no). **(P2a)** Wire column: no insertBatch anywhere — the matrix Insert row's wire cell says "insert verb (placement-only)" and the Append row "r1: … + insertBatch" (:25) — insertBatch **LANDED** (OT `api.ts:1262-1370`; one history entry; shared `validateInsertElement` :634; F1A-3; intra-batch CONFLICT whole-batch; empty INVALID_PARAMS). **(P2b)** Matrix header pins `3989506`/`c15a629`/`64fb0ab` = R25 census pins; now `f9ac806`/`970948a`(HEAD `55c81c0`)/`c020b2a`. **(P2c)** Engine BASE row :34 — 458/458 → **748/748**; timeline.ts 7,502 → **8,997 LOC**; the six algorithm-source pins stale (`rateStretchItem :3155`→**:3840**, `freezeFrameAtPosition :7185`→**:8652**, `removeRangesFromClip :6910`→**:8235**). **(P2d)** "24 routed + 6 exceptions" ×4 sites (:12/:29/:32/:42) + "the 24 routed verbs" (:1658 RO-1) + "`WIRE_COMMAND_TYPES` = 30 names" (:1619 RE-1) — the census is **31 = 28 routed + 3 exceptions** (selectElements/advancePlayhead/trim; the singular keyframe verbs RETIRED — `api.ts:305-311`'s reconciliation note). |
| §0A What Changed (:51-66) | **P2** | The `/tmp/opencut-classic/` + `/tmp/freecut/` citations are **unqualified** — both checkouts are GONE (verified this session; `mock-leverage.md` §2). Needs the 20:8-style provenance note (fix F-9). |
| §1-§4 Purpose/Goals/Inventory/State Model (:69-404) | **SOUND** | The classic command-model census is accurate as a preserved quote; §3's "102 public methods" note (:115) → the class now has 108 two-space methods (P3; fold into F-1's re-anchor). |
| **§5.0 fan-out law (:407-438)** | **P2 ×2** | **(a)** No **Duplicate row** — the F4 law (duplicateElements SEVERS `linkedTo` on the copy; the original keeps its pair) is missing from the fan-out table, the base-verb list (:428 lists split/trim/delete/range-removal/move/rate-stretch — no duplicate), AND §5.10. Evidence: nle-ui `useUiStore.ts:937-945` (`delete copy.linkedTo` — "the splitElement R14 law's twin"); routed: sever-by-construction (new ids carry no sidecar meta). **(b)** Engine anchors drifted: `getSynchronizedLinkedItems :751-767`→**:856-873**, `relinkSplitSegments timeline.ts:668-690`→**:773-816**, "16 public methods" for `options.linked`→ **32 sites** now (the N3 locked-track/fan-out waves grew it). The `av-link.ts:10-24/:26-37/:141-151/:160-201` anchors verified STILL EXACT. E1 row (:438): the R26 flip verified accurate against the engine (commit, pins, residues all match `8188d4e`/`412693d` + PLAN's E1-a..d register). |
| §5.1-§5.8 Split/Trim/2A/Move/Ripple/Roll/Slip/Slide/Delete (:440-1483) | **SOUND** (+2 P2 pins) | The mode-law family verified by mock-trim (zero P1s). Residual pins: §5.2A's OT refs `timeline-core.ts:1017/:1344`→**:1032/:1384**; §5.9's §11.3 cross-annotations consistent with 15's P11. |
| **§5.9 Insert (:1484-1570)** | **P2 ×2** | (a) `ops/timeline-core.ts:872`→**:887** (`insertElements`). (b) The two-semantics preamble is exactly what insertBatch implements (scout-ot: "strengthen, don't change") — add the LANDED note. Mid-clip law's engine refs stale: `_splitClipPure :4742-4774`→**:5479**; Phase-2 shift → `_performInsertEditImpl :5647+`. |
| **§5.9B Overwrite (:1571-1600)** | **P1 ×1 + P2** | **OW-2/E2 GAP row (:1599) NOT flipped** — it still describes the pre-fix asymmetry as live work ("pre-r1 → r1"), contradicting the body's "E2 FIXED (R26)" (:1590) and the engine. The residue (E2-a: the mixed-fate N-group case, cascade wins, spec-silent) needs the row's DONE-tail. Pins: `_performOverwriteEditImpl :4876-5051`→**:6023-6270**; `_buildSourceEditClip :5061-5116`→**:6341**. E3/OW-3: **correctly OPEN** — the silent empty returns live at `:5726/:5772/:5855/:6181/:6270`; the engine PLAN carries the filing (:251-252 "E3 [P3, rides the r1 port]"; :361). |
| §5.9C Replace (:1602-1620) | **P2** | Pins: `joinItems :7411-7427`→**:8817+**; `removeItems :3757/:3775-3777`→**:4507+**; RE-1's `timeline.ts:4513-4515`→ the 3-point family now :5630/:6007. |
| **§5.9D Append (:1622-1639)** | **P1 ×1** | **AP-1's OT-fact cell is FALSE** (:1638): "OT's pool batch is playhead-anchored `applyBatch([insert × N])` stacking with N undo entries (`view/page.tsx:515-555`)" — the pool multi-insert now fires ONE `timeline.insertBatch` per kind-group with staggered construction (`view/page.tsx:535-603` @ `970948a`); the N-entries divergence narrowed to N-kinds (M49R inverted the old pin). The r1 append-strategy half stands. Also :1632's "already-endorsed r1 `insertBatch` composite (15 §13.15, D29 F8)" → LANDED. |
| §5.9E Ripple Overwrite (:1641-1660) | **P2** | RO-2's refs: `rippleTrimItem :2925-2950`→**:3132+**; sync-lock propagations `:3475/:3534`→ timeline.ts `:4218+` / sync-lock.ts `:579+`. RO-1's "the 24 routed verbs"→28. |
| **§5.9F Fit to Fill (:1662-1679)** | **P2 ×2** | (a) **FF-3 unregistered** (mock-insert's new finding): the variants' fitToFill clamps `[RATE_MIN=0.01, RATE_MAX=5]` (`insertPlan.ts:354` + `trimLaws.ts:32-33`) and refuses only outside — the law pins [0.1,5] + refuse-never-clamp (:1668); the 0.01–0.1 band is accepted by the mock, refused by the spec. Plus the branch's stale "spec 06 §5.8" toast citation (§5.9F is the home) and the `insertPlan.ts:22-27` header still teaching the retired composite map ("insert = insert{ripple:true}", "rippleOverwrite = rippleDelete+insert" — both non-law per 15's P11 + 06:1651). (b) `rateStretchItem :3202-3211`→**:3840+**. FF-1/FF-2 verified sound. |
| §5.10 Duplicate (:1681-1692) | **P2** | No link law — the F4 sever (see §5.0a). |
| §5.11 Rate Stretch (:1694-1802) | **SOUND** | Classic census accurate; the `shouldMaintainPitch` quote here is the §5.11/§5.12 split flag's other half (P3, F-6). |
| **§5.12 Retime (:1804-1921)** | **P1 ×1** | **The R9-b companion-field law is MISSING** (corpus-wide fact 3): `el.preservePitch` ElementJSON (09 §3.1A B2), **absent ≡ true**; the engine's `retime.maintainPitch` is the runtime projection — absent ≡ false there (classic `shouldMaintainPitch` requires `=== true`), the BRIDGE converts (`sceneBridge.ts:262` writes `maintainPitch: el.preservePitch !== false`; back-projection :435-447 writes only explicit false; the dormant-false-at-rate-1 reload drop is the APP load-bridge identity law — the engine stores a committed `{rate:1}` retime verbatim, `scene-to-segments.ts:596/:623` read `retime?.maintainPitch === true`). A preservePitch-only patch keeps the current rate (read-merge-write, `engineService.ts:510-513`); writing the default onto an absent field is identity (nle-ui `DEFAULTED_PATCH_KEYS`). The section is currently a pure classic code census with no doc-field law. |
| §5.13-§5.16 Freeze/Range/Mute-Lock/Snap (:1923-2226) | **SOUND** | Optional P3 (scout-nle-ui): the kind-aware mute-all convergence note (the mock's `isMuteableTrack` shared law). |
| §6 Sync-Lock (:2227-2279) | **SOUND** | classic/freecut census accurate. |
| §7-§8 Composition/Invariants (:2281-2338) | **SOUND** | Optional P3: the FW-D undo-group contract (one user verb = one undo unit per owning domain) could be cross-noted. |
| **§9 Q1-Q15 (:2340-2477)** | **P2** | The FreeCut quotes are now **the only preserved reference** — `/tmp/freecut` GONE — but the path citations read as live trees. Needs the provenance qualification (the 20:8 precedent: "point-in-time R15-era record — the live canon is…"). |
| **§10.1/§10.2 tables (:2479-2557)** | **P2** | Same /tmp qualification (both headers unqualified). |
| **§10.4 engine op-coverage (:2559-2604)** | **P2 (fleet-scale)** | The ENTIRE table is ~1,500 lines stale: header claims "line numbers re-verified R24 @ `5036387` — UNCHANGED" — **FALSE at f9ac806** (timeline.ts grew 7,502→8,997 through the H/F/FW/RC waves: E1/E2, N3 locked-track fleet, F5 mode clamps). Counts 458/458→**748/748**; OT consumer pin `c15a629`→`6e2b91a`; every row's line pin moved (full re-anchor map in F-1). `snapshotsEqual :1521`→**:1642**; `_commit :1844`→**:1969**; the insert row's "the E1 linked defect (§5.0) is the pre-r1 precondition" → met (E1 fixed); the overwrite row's "the E2 companion asymmetry + the E3 silent abort are the fix rows" → E2 fixed. |
| **§10.5 OT op-coverage (:2594-2626)** | **P1 ×1 + P2 ×5** | **(P1)** Post-D-ARCH-6 the table is silent: no insertBatch row, no removeKeyframes/retimeKeyframes rows, no singular-retirement note — while three rows reference the retired singular verbs as live ("the W11 lockPreCheck extends TRACK_LOCKED to the singular keyframe verbs (`api.ts:340/:849/:904`)" — those verbs are GONE; the batch verbs carry TRACK_LOCKED batch-atomically, M58). **(P2)** Header: 536/536 @ `c15a629`/`ded43c4` → **632/632** @ `970948a`/`55c81c0`; census "30 names split 24+6" ×2 (:2596/:2616) → 31 = 28+3; `WIRE_COMMAND_TYPES api.ts:182-213`→**:243-275**. **(P2)** Method pins drifted: `duplicateElements :1278`→**:1318**, `rippleDeleteElements :1242`→**:1282**, `toggleElementsMuted :1365`→**:1405**, `toggleElementsVisibility :1409`→**:1449**, `setTracksLocked :2101`→**:2141**, `setTracksMuted :2145`→**:2185**, `previewElements :2271`→**:2311**, `commitElements :2317`→**:2357**. (split/group-move/ripple/placement/retime/UndoStack rows verified STILL EXACT.) |
| **§11.7 rate bounds (:2695-2700)** | **P2** | The two-domain text ("FreeCut 0.1-16 vs OpenCut 0.01-5; adopt OpenCut's") is superseded by **D11's four-domain lattice** (engine DECISIONS `:474-519`, ratified FW-C): [0.01,5] opencut wired SSOT · **[0.1,4] nle-ui authoring subset (stated NOWHERE in 06)** · [0.1,16] engine venue-internal · [1/32,32] WDC DSP + the dead zone [0.01,1/32) + the authoring path. §5.9F (:1668) carries a three-domain-intersection framing that also predates the lattice. |
| §11.1-§11.6/§11.8-§11.9 | **SOUND** | Seed corrections accurate as records. |
| §12 ripple quote (:2716-3139) | **SOUND** + P2 | The 373-line preserved quote — same /tmp qualification note applies. |
| §13 summary + Testing (:3141-3670) | **SOUND** | The R7-era fixes verified landed (fixture registration in 17 §5.3 :957/:958 ✓; `window.__engine` ✓; no false blend-test claim ✓). The Tier-1/2/3 plan is current law. |

**Headline verdict:** the R25 mode-matrix + D31/D32 law family HOLDS (mock-trim/mock-insert: zero P1 spec gaps; the mode law is verified at sub-spec depth). The P1s are all **freshness/registration** failures, not law failures: the E2 half-flip (3 un-flipped sites), AP-1's insertBatch cell, §10.5's D-ARCH-6 silence, §5.12's missing R9-b law, and the census count family.

---

## 2. Fix-list (exact edits, in priority order)

**F-1 (P2, the big mechanical re-anchor) — §0 matrix + §10.4, all engine pins at `f9ac806`.**
Matrix (:18-27) + BASE (:34) + §10.4 table (:2563-2590):
`:2984`→`:3324` (Roll), `:2839`→`:3132` (Ripple trim), `:4143`→`:4956` (Slip), `:4246`→`:5097` (Slide), `:4702`→`:5630` (Insert edit), `:4860`→`:6007` (Overwrite), `:3155`→`:3840` (rateStretch), `:6351`→`:7645` (rateStretchWithRipple), `:2367`→`:2620` (splitClip), `:2576`→`:2840` (splitAll), `:2646`→`:2920`/`:2742`→`:3026` (trimHead/tail), `:3574/:3606`→`:4286/:4320` (rippleDelete×2), `:3757`→`:4507` (removeItems), `:3812`→`:4575` (duplicate), `:3945/:4035`→`:4715/:4823` (move), `:6910/:7057/:7098/:7124`→`:8235/:8511/:8557/:8587` (range family), `:7185`→`:8652` (freeze), `:7341`→`:8817` (join), `:6180/:6284`→`:7463/:7574` (closeGap×2), `timeline.ts:3511`→`:4218` + `sync-lock.ts:503`→`:579` (sync-lock), `:6070/:6115`→`:7350/:7395` (undo/snapshot), `:1844`→`:1969` (`_commit`), `:1521`→`:1642` (snapshotsEqual). Counts: 458/458→**748/748**, 7,502→**8,997 LOC**, OT pin `c15a629`→**`6e2b91a`** (consumer), "102 public methods"→108. Add one dated line to §10.4's header: "*R27 re-anchor @ `f9ac806` (docs-only head over code anchor `50b91f5`): the R24 'UNCHANGED' note is superseded — the H/F/FW/RC waves (E1/E2, the N3 locked-track fleet, the F5 mode clamps) moved every line below; table re-verified at HEAD.*" Same treatment for the matrix header (:14) + OT/app pins → `970948a`/`55c81c0`, `c020b2a`.

**F-2 (P1) — the E2 flip's three missing sites.**
(a) Matrix Overwrite cell (:23): "…E2/E3 open" → "…E2 FIXED (R26, `8188d4e` — the fate-based companion law); E3 the silent abort open (rides the r1 port)".
(b) Matrix Insert cell (:22): "the E1 linked defect open" → "E1 FIXED (R26) — the r1 precondition met".
(c) OW-2 GAP row (:1599): strikethrough-flip mirroring the E1 row's form — "**FIXED (R26, engine `8188d4e`): the fate-based law** (cascade groups remove companions whole; trim groups carve the region out; the by-side ≥2-splits relink re-pairs both tracks' survivors; pins in `timeline-linked-source-edit.test.ts`) … the residue: the mixed-fate N-group case (cascade wins) is the E2-a r1 clarification" — phase "pre-r1 → **DONE** (E2-a open at r1)".
(d) §10.4's overwrite row (:2584): "the E2 companion asymmetry + the E3 silent abort are the fix rows" → "E2 fixed R26; E3 the silent abort remains the r1 fix row". §10.4's insert row (:2582): "the E1 linked defect (§5.0) is the pre-r1 precondition" → "(met — E1 fixed R26)".

**F-3 (P1) — AP-1's OT-fact cell (:1638).** Replace the cell's OT fact with: "insertBatch LANDED (D-ARCH-6 @ `970948a`: `timeline.insertBatch` `api.ts:1262-1370` — ONE history entry, the shared `validateInsertElement` helper, F1A-3 heterogeneous→INVALID_PARAMS, intra-batch overlap→CONFLICT whole-batch, empty→INVALID_PARAMS); OT's pool multi-insert now rides it (one entry per kind-group, staggered per-kind construction, `view/page.tsx:535-603`; the N-entries divergence narrowed to N-kinds, M49R inverted) — **the append placement itself remains open** (2-of-5 strategies on the InsertCommand surface)". :1632's "the already-endorsed r1 `insertBatch` composite (15 §13.15, D29 F8)" → "the LANDED `insertBatch` verb (D-ARCH-6)".

**F-4 (P1) — §5.12's companion-field law (R9-b).** Append to §5.12 (after the classic census, before "Algorithm"):
> **The companion-field law (R9-b, registered R27):** the doc model's pitch flag is `ElementJSON.preservePitch` (09 §3.1A B2) — **absent ≡ TRUE** (the NLE default: a speed change keeps pitch); explicit `false` = pitch-affected varispeed. The engine's `RetimeConfig.maintainPitch` is the runtime **projection**, and its absent-≡-false convention is the *classic* one (`shouldMaintainPitch` requires `=== true`) — the consumer bridge converts: `maintainPitch: el.preservePitch !== false` on load (app `sceneBridge.ts:262`); back-projection writes only an explicit `false` (:435-447); a dormant `false` at rate 1 does not survive reload — that drop is the APP load-bridge identity law, not engine law (the engine stores a committed `{rate:1}` retime verbatim; `scene-to-segments.ts:596/:623` read the explicit-true projection). A preservePitch-only patch keeps the current rate (read-merge-write, `engineService.ts:510-513`); writing the default onto an absent field is identity (no history entry — the absent-≡-default class). The UI home is 18 §4.4's Video-tab Speed row (the honest mixed-selection tri-state).

**F-5 (P1) — §10.5's D-ARCH-6 absorption.** (a) Header (:2594) + W11 row (:2616): 536→632 @ `970948a`/`55c81c0`; "the census now 30 names split 24 UI-routed + 6 documented exceptions" → "**31 verbs = 28 routed + 3 exceptions** (`selectElements`/`advancePlayhead`/`trim`; D-ARCH-6 @ `970948a`: the singular `removeKeyframe`/`retimeKeyframe` RETIRED — their gesture seams cross the wire through the batch forms; `timeline.upsertKeyframe` is UI-routed since AR-2 — no keyframe exception remains; the census re-declared mechanically per D29 F8)". `WIRE_COMMAND_TYPES api.ts:182-213`→`:243-275`. (b) The F1B row's singular-verb clause → "the batch keyframe verbs carry TRACK_LOCKED batch-atomically (M58); the singular verbs are retired". (c) Add three rows to the table: **§5.9 Insert-batch** | `headless/api.ts:1262-1370` | `timeline.insertBatch` | ALIGNED (D-ARCH-6; one entry; shared validator; F1A-3; intra-batch CONFLICT whole-batch; empty INVALID_PARAMS) · **§5.12/15 keyframe batch verbs** | `api.ts` (removeKeyframes/retimeKeyframes case arms) | one-entry each + the WYSIWYG bake-through-the-wire | ALIGNED (M58) · **the singular retirement** | — | `removeKeyframe`/`retimeKeyframe` gone | the retire-don't-route ruling. (d) Re-pin the eight drifted method pins (F-1's OT list).

**F-6 (P2) — the §5.0 duplicate sever (F4's law).** (a) Add to §5.10: "**The link law (F4):** duplicate SEVERS — the copy carries no `linkedTo`; the original keeps its pair (a verbatim copy would form a one-way pair and the next companion edit would fan out to a clip the user never linked). Mock: `useUiStore.ts:937-945` (`delete copy.linkedTo`); routed: sever-by-construction — new ids carry no sidecar meta." (b) Extend §5.0's base-verb sentence (:428): "…, duplicate (§5.10's sever law — the copy severs `linkedTo`, the original keeps its pair)". (c) Optional: the fan-out table gains the row "Duplicate | SEVERED on the copy; the original keeps its pair (F4) | n/a (new track) | n/a".

**F-7 (P2) — §11.7 → the D11 four-domain lattice.** Replace the "Decision:" paragraph (:2700) with the lattice table: [0.01,5] opencut `ops/retime` (wired SSOT; the engine bridge VALUE-IMPORTS `clampRetimeRate` per OV-02) · **[0.1,4] nle-ui Inspector (authoring subset — authoring ⊂ model, the volume-law lattice)** · [0.1,16] engine-native `core/timeline-math.ts` (venue-internal, off the wired path) · [1/32,32] WDC `varispeed.ts` (DSP) · dead zone [0.01,1/32) (opencut-legal, WSOLA-illegal → the pitch-affected fallback, loud warn) · the authoring path (Inspector→derive clamp→flattener→WSOLA). Cross-ref §5.9F (closes FF-2). Add the [0.1,4] subset mention to :1668's intersection sentence.

**F-8 (P2) — the FF-3 GAP row (§5.9F's table, after FF-2).**
> | FF-3 | the variants' fit-to-fill domain: `insertPlan.ts:354` clamps at `[RATE_MIN=0.01, RATE_MAX=5]` (`trimLaws.ts:32-33`) and refuses only outside — the 0.01–0.1 band is accepted by the mock, REFUSED by the law (:1668, engine-freecut-clamp-unsafe); the branch's toast cites "spec 06 §5.8" (stale — §5.9F is home); the `insertPlan.ts:22-27` header still teaches the retired composite map ("insert = insert{ripple:true}", "rippleOverwrite = rippleDelete+insert" — non-law per 15's P11 + §5.9E) | S-app (mock owner) | crawl (r1-adjacent, with RE-2/RO-3's wave) | the fitToFill branch uses a fit-specific floor 0.1 (generic retime keeps 0.01) + a 0.05-ratio refusal test; toast → §5.9F; header rewritten to the landed composite law |

**F-9 (P2) — the /tmp provenance qualification.** One note after §10's heading (:2479), applying to §0A/§4/§5 quotes/§9/§10.1/§10.2/§12 (the 20:8 precedent):
> **R27 provenance note:** the `/tmp/freecut/` + `/tmp/opencut-classic/` checkouts are **GONE** (verified absent R27; `audits/fleet-r27/mock-leverage.md` §2). The quotes in this file are the **preserved reference** — point-in-time R15-era records, not live trees. The executable FreeCut lineage lives in nle-engine's timeline.ts @ `f9ac806` (E1/E2-fixed, test-pinned, venue-internal per D12); the classic lineage survives in opencut-timeline's ops layer (11/11 parity, `.agents/OP-COVERAGE.md`). Re-derivation requires re-cloning the original remotes.

**F-10 (P3s, fold-in):** the §5.11/§5.12 citation split (pick §5.12 as the preservePitch home; cross-ref from §5.11's `shouldMaintainPitch` quote; align nle-ui's code cites + 18:184's pointer — cross-spec); §0 GAP row 2's app pin `c885ece` → `c020b2a` (the "still unstarted" verdict re-verified TRUE — grep-empty for LAW-NET/cutHead at c020b2a); §5.2A's `timeline-core.ts:1017/:1344`→`:1032/:1384`; §5.9's `:872`→`:887`; §5.0's "16 public methods"→"32 sites"; the kind-aware mute-all note (§5.15).

---

## 3. Cross-spec notes (for the cross-cut wave; NOT my edits)

1. **15 §13.15 :4905/:4916/:4919** — the QUEUED insertBatch row + "24 routed + 6 exceptions" census cells are FALSE post-D-ARCH-6 (scout-ot P1). 06's AP-1 flip (F-3) should land in the same amendment wave.
2. **15 §4.1A** — the batch keyframe verbs' union members (`removeKeyframes`/`retimeKeyframes` plural-form) + `insertBatch` are wire-census facts 06 §10.5 now absorbs (F-5); the C7 rename map at 15:4905 still lists "singular per-key forms".
3. **09 §3.1A B2 + §7.2** — the R9-b ruling fold (the model home; scout-nle-ui's P1) pairs with 06's F-4 — the same amendment should carry both, one DECISIONS entry.
4. **18 §4.4 :184** — "Speed: rate % + preserve-pitch toggle (→ retime family, spec 06 **§5.11**)" → §5.12 after the split ruling (F-10); the Audio-tab Gain-dB row is 18's own P1 (scout-engine #91-92).
5. **05 §16.5A's op-port table** carries the same stale engine pins (slip `:4143`, slide `:4246`, roll `:2984`, rateStretch `:3155`, `:2839`, `:4860`, `:7411-7427`, `:3757`) — the F-1 re-anchor's sibling edit; plus 05:1280's `rateStretchItem :3404-3408`.
6. **05 §17/:47 census mirror + 00-master fleet rows** — 06's pins move with the fleet re-pin (the battery_r26 pin-lag class).
7. **19-code-references** — the §10.4 re-anchor (F-1) should be reflected in 19's engine card (458→748, the consumer pin `6e2b91a`).
8. **The corpus-wide "24+6" family** — 06's nine sites (F-1/F-5) are the single largest concentration; 15/05/09/12/17/18/19 carry the rest.

---

## 4. What 06 uniquely sees

1. **The E2 half-flip class** — a fix landed in the code + flipped in the §5.9B body but NOT in the same file's GAP row, matrix cell, or §10.4 row. Battery candidate: an intra-file consistency check (matrix cells ↔ §5.x GAP rows ↔ §10.4 verdicts must agree on FIXED/open).
2. **06 is the corpus's quote-preservation burden** — §9's Q1-Q15 + §12's 373-line ripple quote + the §4/§5 FreeCut/classic excerpts are now the ONLY preserved FreeCut reference (the /tmp checkouts are gone; the executable lineage lives in the engine's venue-internal ports). The provenance note (F-9) is 06's to carry; the risk (a future agent re-verifying against a nonexistent tree) is 06-specific.
3. **The rate-domain lattice's FOURTH venue** (nle-ui [0.1,4]) is visible only from 06's §11.7 + §5.9F — no other spec hosts the retime-domain table; D11's ratification note belongs here.
4. **The F4 duplicate-sever law has no other natural home** (05 is view, 09 is the field) — §5.0's fan-out table is the law's owner.
5. **D-ARCH-6 invalidates a GAP row's FACT cell while leaving its r1 work intact** (AP-1: insertBatch landed ≠ the append strategy landed) — the only spec where the batch landing changes a row's evidence but not its phase. The kind-split rule's normative home is 06 §5.9D (:1632), which OT's code already cites — the citation becomes true once 15 §13.15 flips (scout-ot #27).
6. **The r1-port leverage map converges on 06's GAP register** (mock-leverage §4-§5): the ten-mode sequencing + the three mechanism decisions (linkage model, constants module, E3+N3 error contract) — 06's GAP rows are the port wave's worklist of record; the E1/E2 FIXED states are the port's green precondition set.
7. **E3's engine-side filing exists** (PLAN :251-252, downgraded P3, rides the r1 port) — 06's OW-3 row is accurate but can now cite the filing (the "pre-r1 queue filing" phase is DONE).

**Report-only compliance:** no spec/engine/OT/ui/app files modified; only this report + the worklog entry. Every claim above carries file:line evidence re-derived this session at the R27 pins.
