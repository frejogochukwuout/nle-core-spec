# R27-W1 scout — web-daw-core: the S-series upstream wave (`387f327` → `ec8fd5c`)

**Agent:** scout-wdc · **Round:** R27 final-tightness, Wave 1 (module scouts) · **Date:** 2026-09-13
**Range audited:** exactly ONE commit — `ec8fd5c` "sync: the S-series upstream wave (fork @ f5011b3, our eeb3b24 fix preserved) — 19 locked files refreshed + the 18-file closure extension" (`git log --oneline 387f327..ec8fd5c`).
**Method:** everything below re-derived from the tree at `ec8fd5c` (clean checkout; `git status` empty), not from commit messages alone. Suite + tsc re-run live in this sandbox.

---

## §1 The commit-range ground truth

### 1.1 The sync commit, mechanically

`ec8fd5c` = 45 files changed, **+9,678 / −489** (`git show ec8fd5c --stat`): 26 M + 19 A.

**The 19 locked files refreshed** (M-status files that are manifest `copy`/`copyRepoRoot`) — verified against the name-status list and `UPSTREAM.lock.json`:
`effects.ts, channel-strip.ts, types.ts, offline-render.ts, aux-buses.ts, ir-presets.ts, sample-catalog.ts, sample-loader.ts, project-factory.ts, model/index.ts, model/parameter.ts, model/tempo-map.ts, wams/wam-effect.ts, wams/host.ts, wams/registry.ts, ir-presets.test.ts, model/tempo-map.test.ts, scripts/rt-safety-gate.ts, public/worklets/dsp-effects-worklet.js`. The other 7 M files: the lock, the manifest, the barrel (`src/index.ts`), and 4 shim alignments.

**The 18-file closure extension** (new manifest `copy` entries; manifest diff `387f327..ec8fd5c` = +18 rows after `test/real-audio-e2e.test.ts`):
`diagnostics.ts` (376L) · `stall-race.ts` (30L) · `library-audio-proxy.ts` (188L) · `sample-catalog-core.ts` (196L) + `data/sample-catalog-ir.json` (494L) · `wams/wam-instrument.ts` (943L) + `wams/wam-state.ts` (231L) · `instruments/instrument-factory.ts` (201L) + `instrument-registry.ts` (251L) + `param-path.ts` (182L) · `midi/instrument-interface.ts` (78L) + `midi-channel-state.ts` (896L) + `midi-channel-state-map.ts` (149L) + `midi-constants.ts` (169L) + `drum-params.ts` (359L) + `nrpn-handler.ts` (444L) + `sysex-parser.ts` (513L) + `spessa-midi-constants.ts` (1,679L). Plus the **type seam** `src/types/spessasynth-core.d.ts` (22L, `coreOwned` — the ambient `MIDISystem`/`SysExAcceptedArray` declaration so the type-only `import type { … } from 'spessasynth_core'` inside the vendored constants resolves without the 105 KB-gzip runtime dep; `src/types/spessasynth-core.d.ts:1-22`).

**Lock arithmetic (verified from the tree, not the message):** `UPSTREAM.lock.json` now pins **97 files** (93 `copy` + 4 `copyRepoRoot`), `repo https://github.com/bearachprema/web-daw`, **sha `f5011b3…`** (`UPSTREAM.lock.json:2-4`), syncedAt 2026-09-13T10:27Z. Previous lock: sha `eeb3b24…` (R26, `git show 387f327:UPSTREAM.lock.json:3`) — i.e. "our eeb3b24 fix preserved" = the fork's `main` advanced from our own upstream push `eeb3b24` to `f5011b3`, and the refreshed files keep the fix. **All 97 disk hashes == lock pins (recomputed live, sha256[:16] per `scripts/sync-from-upstream.mjs:198`)** — zero lock↔disk drift. (Upstream↔lock re-sync not runnable here — no token/clone — same limitation R26 recorded; the disk half is what the drift gate protects.)

**Shim alignments** (the loud-failure design working): `mini-synth.ts:38,50` + `provider.ts:25` `connectInput: GainNode|null → AudioNode|null`; `fm-synth.ts:48` `masterGain 1 → 0.5`; `subtractive-synth.ts:50-51` `masterGain 0.8 → 0.4` + `lfo.rate 0 → 5` (upstream's canonical defaults — caught by the factory's module-load registry cross-check, `instrument-factory.ts:8-10`).
**Barrel re-points:** `getAllIRPresets → ./lib/daw/ir-presets`, `resolveIRUrl → ./lib/daw/sample-catalog-core` (`src/index.ts:102-103`) — upstream moved their homes.

### 1.2 What upstream wave was absorbed (headline items, read from code)

- **diagnostics.ts** — the structured error-event spine + off-thread liveness monitor that replaced upstream's deleted ARCHITECTURE §3.9 watchdog (`src/lib/daw/diagnostics.ts:1-38`): `EngineDiagnostic` events (kinds incl. `bootstrap-failed`, `node-create-failed`, `processor-error`, `nan-output`, `silence-suspected`, `heartbeat-stalled`, `recovered`; `diagnostics.ts:41-57`), `reportDiagnostic` (`:139`), `subscribeDiagnostics` (`:175`), `registerHeartbeat`/`unregisterHeartbeat`/`registerPoll` (`:296/:335/:344`). Console-silent bus; heartbeat armed-by-first-beat; stall only fires while `isRunning()`.
- **The rebuilt dsp-effects-worklet bundle** — a new local mixin base `DspDiagnosedProcessor extends AudioWorkletProcessor` (`public/worklets/dsp-effects-worklet.js:322`): heartbeat every 256 quanta (`:315`), NaN scans + silence detection per block, preallocated message objects — the in-`process()` instrumentation of the diagnosed-DSP family.
- **The rt-safety-gate transitive-heritage fix** — `scripts/rt-safety-gate.ts:149-182`: `findProcessSeeds` previously matched only DIRECT `extends AudioWorkletProcessor`; the new mixin broke it → **0 seeds → the whole diagnosed-DSP call graph walked nothing, the gate's PASS was vacuous for those files** (the in-code comment says the M3-REV-DOCS review caught it, `rt-safety-gate.ts:141-146`). Fixed with the cycle-safe `isWorkletSubclass` walking LOCAL base classes transitively (`:162-172`).
- **wams/wam-instrument.ts + wam-state.ts** — the WAM→Instrument adapter behind the EXISTING `Instrument` contract (`wam-instrument.ts:1-33`: notes become `wam-midi` events via the SDK's `scheduleEvents`; re-entry ladder mirroring WamEffect; instrument-PDC documented-not-consumed) + the `WamStateEnvelope` (wamId-keyed, v1, opaque family-shaped blob; sanitize caps depth 8 / keys 1024 / 64 KB — cap violation NULLS the whole blob; `wam-state.ts:21-46`).
- **instruments/{factory,registry,param-path}** — the synth-seam L4/L5 split: the registry holds pure param metadata (L5-safe, no class imports; `instrument-registry.ts:1-30`), the factory dispatches construction + module-load cross-check (`instrument-factory.ts`), and `setParamPath` implements the §1.3 dotted-path grammar (plain key = byte-identical spread; numeric segment indexes arrays only when the parent IS one; collision/malformed → input unchanged + warn-once, never a throw at tick rate; `param-path.ts:6-23`).
- **The pure-logic MIDI family** — `MidiChannelState` (all 128 CCs 7-bit in an Int8Array, 14-bit pitch wheel, poly pressure; `midi-channel-state.ts:1-10`), NRPN/SysEx decoding for GS/XG/GM/GM2 (`nrpn-handler.ts:1-5`, `sysex-parser.ts:1-9`), GS/XG per-drum-note overrides (`drum-params.ts:1-6`), the boot-pinned spessasynth constants vendored so the 105 KB runtime dep stays behind the async boundary (`spessa-midi-constants.ts:1-9`), and `instrument-interface.ts`'s `attachMidiState()` contract (`:36`).
- **sample-catalog split** — `sample-catalog-core.ts` (zero-data pure helpers + `SampleQuality`; the 123 KB catalog stays out of the boot graph; `sample-catalog-core.ts:1-14`) + the IR-catalog JSON split (`data/sample-catalog-ir.json`).
- **library-audio-proxy.ts** — the same-origin `/api/library/audio` proxy for degraded-network fetches (CN GFW conditions), client map + server allowlist, fail-open client-side (`library-audio-proxy.ts:1-23`).
- **stall-race.ts** — `withStallTimeout`, a rejection-leak-free Promise.race (load-resilience hardening; `stall-race.ts:1-8`).

### 1.3 The waveform contract — INTACT through the refresh (the R26 promotion survived)

- `src/lib/daw/waveform-contract.ts` present, unchanged, coreOwned-registered (`extraction-manifest.json:146`); exports `WAVEFORM_PEAKS_BUCKETS = 2000` + `WaveformPeaks` (`waveform-contract.ts:48-55`); the barrel re-exports via `export * from './lib/daw/waveform-contract'` (`src/index.ts:87`).
- **`audio-registry.ts` byte-verbatim: disk sha256[:16] `fc5168f6eef91684` == lock pin** (`UPSTREAM.lock.json:18`) — it was NOT one of the 19 refreshed files; the extraction law held. The `audio-registry.ts:24-74` line citations in spec 20 remain exact (getAudioBuffer@24, getPeaks@48, setAudioPeaks@58, computePeaks@74; `PEAK_BUCKETS = 2000` @:20).
- The 14-pin contract test `src/test/waveform-contract.test.ts` — 14 `it`s across 3 describes (`:94/:130/:204`) — ran green inside the live 777. The module header's own cross-citation ("spec 20 §0:25") still points at the right row.

### 1.4 Test counts at `ec8fd5c` (re-run live in this sandbox)

**vitest 777/777, 34 test files, 53.3 s** (`bun run test`; `bun install` fresh) + **`tsc --noEmit` exit 0**. Delta over R26's 773: **+4 pins, all in the synced `model/tempo-map.test.ts` family (68→72 it-blocks)**; `ir-presets.test.ts` refreshed without net test growth (33 it-blocks at both pins). File count unchanged (34; 19 in `src/test/` — note `waveform-contract.test.ts` already made `src/test/` 19 files at R26, not 18).

### 1.5 Consumer re-pins (verified in the sibling repos)

Both consumers moved to `ec8fd5c` BEFORE this audit: engine `480a216` ("vendor/web-daw-core 387f327 → ec8fd5c …+ the tsconfig 'files' entry for the vendored ambient declaration"; submodule status `-ec8fd5c…`) and app `9448667`/`1c43897` (submodule `-ec8fd5c…`; `tsconfig.json:45` carries `"vendor/web-daw-core/src/types/spessasynth-core.d.ts"`). **New consumption fact: every consumer must add the vendored ambient declaration to its tsc `files` array or the vendored MIDI modules fail TS2307 + a cascading TS2366** (the app commit's own gate note).

---

## §2 The current law state (WDC's own docs)

**CRITICAL FINDING — the S-series docs wrap NEVER LANDED in WDC.** `ec8fd5c` touches code + lock + manifest + shims + barrel only; `git log -1` per file: `HANDOFF.md`/`PLAN.md` last touched `387f327` (R26), `worklog.md`/`.agents/SKILL.md` last touched `85b81b0` (S4 wrap, 2026-09-07); no dangling commits (`git fsck` clean); working tree clean. So at HEAD:

- **HANDOFF.md:12** "web-daw-core @ `494f6ff`" — stale (HEAD `ec8fd5c`).
- **HANDOFF.md:29-30** "Upstream web-daw: NO new commits since our eeb3b24 (checked all branches). `sync --check` clean. Upstream porting duty: NONE pending." — **now false**: the fork advanced 175 files/+27 k lines (commit message; merge-base `913d0d7`) and the S-series was exactly that porting duty — now discharged, but the doc still says there is none.
- **HANDOFF.md:73-77** "The 4 deferred upstream tests (`engine.test.ts`, `engine-metronome-automation`, `engine-param-channel`, `dsp-aux-sends`, `wam-effect.test.ts`)" — the known off-by-one (5 bodies under a "4" label; spec 20 corrected its own copy at R24, `20:151`) still uncorrected in WDC's own docs (also `PLAN.md:145`).
- **SKILL.md §1:25-26** describes `coreOwned` as 5 files — the manifest now lists 14 (`extraction-manifest.json:141-156`).

**The file-class law (the vendoring law, WDC's own statement)** — `.agents/SKILL.md:9-30`: `copy` = VERBATIM upstream bytes, never hand-edit (a hand edit is silently reverted by the next sync; the drift check catches it); `shims` = core-owned files at the SAME import paths upstream's DAW-app machinery occupies (the export surface is a contract; tsc is the enforcer); `coreOwned` = the package surface. Path-mapping law `apps/web/src/** → src/**` with `copyRepoRoot` + audited `rewrites` for repo-root escapes (`SKILL.md:32-40`). **The S-series is this law working at scale** — the manifest-extension decision path ("shim or manifest-extension or defer-the-test", `SKILL.md:28-30`) was exercised for the 18-file closure, and the module-load registry validation caught 3 real default drifts. The law needs no amendment; only the census numbers in the docs are stale.

**The D24e law** (a SPEC-20 label, `20-audio-core.md:208`; WDC-side it landed as the A6 round `494f6ff`): the barrel has ZERO importers today — both consumers deep-import the vendored tree via their own `@/lib/daw/*` alias; the barrel is the FUTURE web-daw-migration surface (UPSTREAMING Phase C). **Still true at `ec8fd5c`** — the S-series only re-pointed two of the barrel's own export sources (`src/index.ts:102-103`); consumers still deep-import (both re-pin commits alias through `vendor/web-daw-core/src`).

**WDC's live queue (per the unchanged root HANDOFF, re-derived):** item 0 [DONE R26] the waveform-contract promotion; then (1) the M2 mixer surface (live parametric EQ / reverb sends / inserts / sidechain / PDC — design round FIRST, the W2 process); (2) the FIVE (doc says "4") deferred upstream test ports (nle-engine-side ports of the upstream bodies against the relocated bridge); (3) the async pre-retime queue item (the adapter's O(span) sync WSOLA on the play edge; design-w2 §4); (4) the P3 ledger (upstream-class notes, F12, F9, F10/F11, R4, F4). **Plus the missing S-series docs wrap** (the staleness list above) — nothing in any WDC queue carries it.

---

## §3 The spec-impact register

| spec §site | current text (quote) | new truth (file:line evidence) | sev | proposed amendment |
|---|---|---|---|---|
| **20 §status :4** | "WDC 759/759 @ `85b81b0` (…the S4 SEAL round CLOSED…)" (v-next-3, R24) | `ec8fd5c`, **777/777**, 34 test files, tsc 0 — re-run live (§1.4); the S-series upstream wave absorbed | P2 | v-next-4 status line: WDC 777/777 @ `ec8fd5c` (the S-series: 19 refreshed + 18-file closure + the rebuilt worklet bundle + the rt-gate fix); engine `480a216` + app `9448667` both consume `ec8fd5c` |
| **20 §0 BASE :15** | "**759/759, 33 test files**, tsc 0 … drift-gate green (UPSTREAM.lock `eeb3b24`, no pending porting duty) — the code pin is `494f6ff`" | 777/777, **34 test files**; lock sha **`f5011b3`** (`UPSTREAM.lock.json:4`); the code pin IS `ec8fd5c` (the sync is code, not docs); porting duty discharged BY this wave | P2 | Re-write the BASE row to the R27 truth + add the S-series surfaces (diagnostics/heartbeat, wam-instrument+state, the MIDI family, the catalog split, the instrument factory/registry) to the BASE description |
| **20 file-map :209** | "`src/lib/daw/instruments/*` (shims) + `setInstrumentProvider` \| provider injection seam (Phase B upstreaming)" | **FALSE label now**: `instruments/` = 7 shims + **3 verbatim upstream copies** (`instrument-factory.ts` 201L, `instrument-registry.ts` 251L, `param-path.ts` 183L — manifest `copy` list, `extraction-manifest.json:90-92`) | **P1** (states something false) | Split the row: shims (7, incl. the provider seam) vs the vendored factory/registry/param-path copies (the L4/L5 synth-seam + the §1.3 dotted-path grammar) |
| **20 file-map (missing rows) — diagnostics** | (no row; the only diagnostics-adjacent text anywhere is none — grep "heartbeat\|liveness\|diagnostic" over 02/20/12/17 returns nothing spec-side) | A landed observability law with NO spec trace: the `EngineDiagnostic` bus + heartbeat monitor (`src/lib/daw/diagnostics.ts:1-57`), the worklet-side mixin protocol (`dsp-effects-worklet.js:315-372`), the strip's context binding + **bypass-to-passthrough on heartbeat-stall** (effects.ts delta: "stopped rendering (no heartbeat >3 s …) — bypassed to passthrough") — an audible-degradation real-time policy the K3 app consumer will see | **P1** (a landed law missing from spec) | Add a file-map row + a §0 BASE sentence; decide at the fold whether a thin spec-02 §7 note carries the in-`process()` protocol law (see the 02 row below) |
| **20 file-map (missing rows) — the rest of the closure** | (no rows for: wam-instrument/wam-state, the midi/* family, sample-catalog-core + the IR JSON, library-audio-proxy, stall-race, spessasynth-core.d.ts) | All are manifest-registered copies at `ec8fd5c` (§1.1); spec 20's file map is "canon for this stream per Decision 13" (:191) | P2 | Extend the map (one row per family, the WAM-state envelope + the spessasynth type-only async-boundary law are the two with cross-repo relevance) |
| **20 file-map :202** | "`src/lib/daw/` engine + effects + model + offline-render (verbatim, **33-file closure**)" | The vendored closure grew 75 → **93 src copies + 4 root** (97 lock entries; §1.1) — the "33-file closure" was the M1-era engine BFS, already historical | P2 | Re-word to the manifest census (93+4) or drop the count for the manifest pointer |
| **20 §4.3 :110 (MIDI paragraph)** | "Instruments (the MIDI path) inject via `setInstrumentProvider()` … The strip's instrument slot + `noteOn`/`noteOff` IS the MIDI pipeline … a MIDI file importer becomes a host-side concern that flattens to `NoteEvent`" | Still TRUE (provider shim + `Symbol.hasInstance` dispatch intact; `provider.ts:22`), but understated: the tree now carries the full per-channel MIDI state machinery (`MidiChannelState`, CC7/CC11/pitch-bend/sustain via `attachMidiState` — `midi/instrument-interface.ts:36-78`; NRPN/SysEx/drum-params/spessa constants) — the natural upstream reference for the deferred W7/M3 scope, like `model/automation.ts` already is for M2 (:219) | P3 | One sentence registering the vendored MIDI-state family as the W7/M3 upstream reference; no scope change (spec-scope deferral per Decision 13 stands) |
| **20 :25 (the W-row, R26 EXECUTED stamp)** | "…the pinning test … 14 pins … **773/773** + tsc clean; the app's consumer pin re-pinned `85b81b0`→`387f327`" | The contract itself SURVIVED VERBATIM (§1.3 — hashes + line citations + 14 pins green in the 777); only the count and consumer pin are stale: 777/777, app @ `ec8fd5c` (submodule verified, §1.5) | P2 (mechanical) | Update the stamp's tail (777; consumer `ec8fd5c`) + add "survived the S-series refresh byte-verbatim" so the survival is on record |
| **20 :206** | "`audio-registry.ts` (lines 24-74 …)" | Verified exact at `ec8fd5c` (getAudioBuffer@24 … computePeaks@74; §1.3) | — (accurate) | none |
| **02 §7 :257-262 + :16 + :26** | §7 states the audio-thread facts (hard RT deadlines, per-quantum, separate memory) + WDC row "759/759 @ `85b81b0` … the 3 `public/worklets/` files … re-verified unchanged" | (a) pins stale: 777/777 @ `ec8fd5c`; (b) "`public/worklets/` … unchanged" is now FALSE — `dsp-effects-worklet.js` was REBUILT (+75 lines, the `DspDiagnosedProcessor` mixin, `:315-372`); (c) **spec 02 has NO audio-thread law for what may run inside `process()`** — zero corpus mentions of the rt-safety-gate (grep over all 21 specs), and no coverage of the new sanctioned in-`process()` diagnostics pattern (bounded, preallocated `dsp-hb`/NaN/silence messages) | **P2** (a false "unchanged" claim + a missing mechanism) | §7 amendment: (1) re-pin; (2) name the WDC rt-safety-gate as the audio-thread discipline's enforcement mechanism + the **transitive-heritage law** (worklet processors may extend local base classes — the gate must walk the chain; the direct-only match made the gate's PASS vacuous, `rt-safety-gate.ts:141-146`); (3) register the heartbeat/NaN/silence in-`process()` protocol as the sanctioned bounded-messaging pattern. Cross-ref spec 17 §13A's "AudioWorklet message protocol" facet row |
| **19 :204** | "**copy** (75 src files + 4 root assets…) **shims** (11 files…) **coreOwned** (**5** files…)" | copy = **93 src + 4 root**; shims 11 (unchanged); coreOwned = **14** (`extraction-manifest.json:141-156`) | P2 | Refresh the three-class census |
| **19 :202** | "Extracted from web-daw `main@913d0d7` (…upstream unchanged since the sync, ls-remote verified 2026-09-04)" | Superseded: the repo re-synced at the fork `f5011b3` (2026-09-13) | P3 | Re-word the historical note (the "only LIVING ancestor" claim at 00:22 / 19:50 stays TRUE — the fork IS the living venue and the S-series proves the flow) |
| **19 :49 (repo table 3A) + :5 status** | "759/759, tsc 0, zero runtime deps — R24 @ `85b81b0`" | 777/777 @ `ec8fd5c` (zero runtime deps still true — package.json devDeps only) | P2 | Re-pin |
| **12 :14 + 17 :4/:20/:2308** | "web-daw-core 759/759 @ `85b81b0` … **18 `src/test/` suites**" | 777/777 @ `ec8fd5c`; **19 `src/test/` files** (34 total; 19 since the R26 promotion — the "18" was already stale at R26) + the synced `model/tempo-map.test.ts` family +4 | P2 | Re-pin + suite-list refresh |
| **00 :3/:18/:400 (fleet rows + D21 R26 note)** | "WDC `387f327` **773** … consumed by engine @ `494f6ff` + app @ `387f327`" | WDC `ec8fd5c` **777**; engine AND app both consume `ec8fd5c` (§1.5); the R27 pin-world note appends | P2 | The round's central re-pin (known pin-lag; battery_r26 136/141) — the WDC row's S-series one-liner should name the 18-file closure + the ambient-declaration consumption law below |
| **00 D15 :372 (the vendoring law) — NEW consumption fact** | "vendors … as pinned git submodules (…pins travel as a lockset — the app's OT/WDC pins never advance beyond the engine's validated combination without a compat run)" | The law held (both consumers advanced together, compat-run gates green) — but the S-series adds a MECHANICAL requirement the law doesn't state: consumers must add `vendor/web-daw-core/src/types/spessasynth-core.d.ts` to their tsconfig `files` or the vendored tree fails tsc (TS2307 + cascading TS2366; engine `480a216` + app `9448667` both carry the entry; app `tsconfig.json:45`) | P3 | One sentence in the vendoring law / spec 20's integration notes: the vendored ambient declaration is part of the pin |
| **IMPLEMENTATION-PLAN S-wdc :21** | "The live queue (the S4 seal round is CLOSED — **759/759**, P3-only, **zero upstream drift**; …)" | "Zero upstream drift" is now false — a 175-file/+27 k-line upstream wave was absorbed; WDC's own HANDOFF is materially stale (§2) and the docs wrap is in NO queue | P2 | Re-write S-wdc's queue from the tree (the §2 re-derivation here is the input): the live head = the S-series absorbed + the missing docs wrap (file it) + M2 mixer + the FIVE test ports + async pre-retime + the P3 ledger |
| **20 :27 + 02 :22 (worklet asset serving)** | "the app's `public/` does not exist at all — verified R23, re-verified R24" | **Still TRUE** at app `c020b2a` (verified live: no `public/` dir) — the K3 worklet-serving row remains a live gap; unchanged | — (accurate) | none (record so W3 doesn't re-derive) |

**Not impacted / verified stable:** the three-layer track model (Decision 13) — no S-series delta touches the S/G/E layering; the D24e barrel law (still zero importers); the sync/vendoring mechanism description at 20:213 + 00 D13:346 (accurate, proven by this very wave); spec 02's worklet census claim ("two Blob-URL processors + the three served assets" — still exactly true; the rebuilt bundle is one of the three); the "17 DSP EffectNode implementations" figure at 20:15 is UNCHANGED by this wave (22 exported Effect classes at BOTH pins — pre-existing discrepancy, see §4).

---

## §4 Open questions

1. **Where is the S-series docs wrap?** The sync commit is code-only; HANDOFF/PLAN/worklog/SKILL are untouched and now materially false in three claims (§2). Lost (unpushed/destroyed — the R24-variants recycle precedent class) or deliberately pending in a live parallel session? Either way a WDC-side queue item must exist — recommend the plan's S-wdc row files it ("the S-series doc wrap: HANDOFF Where-things-stand rewrite + the 4→5 test-port off-by-one + SKILL §1 coreOwned census + the worklog entry").
2. **Does the diagnostics/heartbeat system become spec LAW or stay WDC-internal machinery?** The bypass-to-passthrough-on-stall policy is an audible-degradation law the app-side K3 consumer will observe; the rt-safety-gate has been WDC-internal to date (imported by `test/dsp-bounce-parity.test.ts`, runs in-suite). Ruling needed at the fold: a thin spec-02 §7 note + spec-20 row (my P1 rows above), or a deliberate "machinery, not law" disposition recorded so the W3 per-spec fleet doesn't re-litigate.
3. **The "17 DSP EffectNode implementations" figure (20:15)** vs 22 exported `class …Effect` in `effects.ts` at both `387f327` and `ec8fd5c` — unchanged by this wave, but which count is meant (insert-chain nodes vs all effect classes incl. helpers)? A W3 verification item.
4. **The spec-side S-series absence:** NO spec or plan row anywhere mentions the S-series (grep "ec8fd5c|f5011b3|S-series" over the corpus returns nothing except the just-landed ARCH-R27 skeleton §1 row). The battery's live checks will flag the pins, but the 18-file closure's content (the MIDI family, the WAM instrument path) has never been intake-reviewed against the spec posture — this report's §3 is that intake; the W3 spec-20 owner should treat it as the seed.
5. **WDC's SKILL.md coreOwned census (5 listed vs 14 actual)** — was already stale at R26 (the promotion's 2 files + meter-tap/soundtouch/varispeed + the type seam); rides the same docs-wrap queue item as #1.
