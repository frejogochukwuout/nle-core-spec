# R27 WAVE 3 — SPEC-11 AUDIT (cloud render: the unchartered stream, the two-halves truth)

**Agent:** spec-11 (R27-W3-11) · **Date:** 2026-09-14 · **Scope:** `11-cloud-render.md` (2,557 L), section-by-section, against the R27 pins — engine `f9ac806` (748/748 per the fleet pin), OT `55c81c0`/`970948a` (632/632), WDC `ec8fd5c` (777/777), nle-ui `32abd58` (690/690), app `c020b2a` (252/252), cloudcut-nle ON DISK at `/home/z/my-project/cloudcut-nle` (the dormant predecessor — the reference row's evidence base is LIVE), FreeCut teacher GONE from disk.

**Headline:** ZERO P1s. The two-halves truth, the zero-cloud-service grep, and the r6-unchartered register all RE-VERIFY at the R27 pins. The findings are one coherent P2 cluster — the spec's live line-pins/LOC counts drifted in the RC round (four moved pins), the round-notes stop at R24, one Tier-3 test row carries a wrong method+section pointer (`GET`/§8.5 for what spec 15 §8.7 defines as `POST`/subscribe), and the §14 dependency-LOC column is three fleet-rounds stale. Plus the focused check's yield: the engine's D12 ratification names THIS spec's subject ("the RENDER/EXPORT venue model") and 11 never registers it; the M2 Wave 1/Z2 landings corroborate 11's WYSIWYG contract in code before the stream is chartered.

---

## §2 The per-section verdict table

| Section (line) | The claim checked | Verdict | Evidence (live re-derivation) |
|---|---|---|---|
| Status :4 + §0 BASE :12-:17 | The two-halves truth + zero cloud-service at HEAD | **P2** | Zero-cloud RE-VERIFIED by grep at all five R27 pins: no `spawn('ffmpeg'`/S3/RunPod/`/api/render`/Dockerfile anywhere in engine/OT/WDC/UI/app; engine's only `child_process` uses are the Xvfb runner (`scripts/run-nle-tests.mjs:93`) + two verify scripts; the false-positive classes re-confirmed in place (WDC's FFmpeg-bake design comments `bake-cache-key.ts:32`/`types.ts:58`; the engine's S3A wave-tags `av-link.ts:143`). Both halves still landed engine-side: `NleHeadlessApi` :111 + frame-grab doc :118 + `HARNESS_READY_PREDICATE` `api.ts:2771` + the 9-method surface all re-read. **But the live pins drifted:** `headless/api.ts` 2,757→**2,777** LOC; `export/orchestrator.ts` 936→**957** (settings 368 / audio-mixdown 369 / contracts 92 / index 20 HOLD; dir total 1,785→**1,806**); `createRenderAdapter` :524→**:544**; `.agents/DECISIONS.md` :226→**:242**. The newest round-note is R24 — the whole RC round (D12 ratification, OV-01/02/05/12, M2 Wave 1, NS queue) is unregistered |
| §0 cloudcut row :15 | D27.3 historical context: render-worker 419 LOC, 10s heartbeat, timeout-kill/retry, `/api/render` routes, successor notes | **SOUND** | All verified ON DISK today: `mini-services/render-worker/index.ts` = exactly **419 L**; `:27` `import { spawn, ChildProcess } from 'child_process'`; `:239` `spawn(ffmpegStatic as string, args, …)` (ffmpeg-static binary); `:7` "Heartbeat every 10s via PATCH /api/render/heartbeat"; `:6` atomic claim; `:3` "retry, heartbeat, and graceful shutdown"; `docs/planning/NOTES_FOR_SUCCESSOR.md` present ("Beyond Hybrid Storage", 2026-06-30); `docs/planning/HYBRID_STORAGE_RFC.md` carries the three tiers (§3.1 :54), the 480p/720p proxy (:36/:125), the Tauri path (:17/:44/:50) |
| §0 GAP row :20 | r6 unchartered; the plan quote; the stub re-point; the battery exemption | **SOUND** | Plan §2 :78 reads "**r6 the non-blocking tail** (was R-engine-p2/R-cloud) \| … cloud render UNCHARTERED unless the user re-scopes \| … slips independently by design" — 11's quoted phrase VERBATIM; the plan's r6 row is broader than cloud (CPU transition renderers, Shape/Lottie/Subtitle surfacing, other engine P2) and 11's framing ("D24's non-blocking tail") agrees. Stub 14 §-redirect: "§3.3 (the RUN) → The plan §2 r5/r6" ✓. `scripts/battery_r26.py:186` `if not rows and n not in (11, 13)` — the no-gap-rows exemption VERIFIED in code |
| §0 ACCEPTANCE :22 | 17 §13A facet rows present | **SOUND** | 17:538 (Cloud render WYSIWYG facet), :1514 (Memory 8K <4GB), :1515 (4K <10min), :1516 (8K <30min), :2396 (the spec-11 facet registry row) all live |
| §1 Purpose / §2 Goals :26-:40 | WYSIWYG contract; 4GB ceiling | **SOUND** | Matches 00-master Decision 6 (:178 "One engine, two entry points") + the NFR table :634 "Memory ceiling <4 GB per renderer process (Decision 6 constraint)" |
| §3 Architecture :43-:116 | Seed diagram + SCOUT-11 refinement (download-event pattern) | **SOUND** (P3 rider) | Design coherent; the refinement note's premise (FreeCut's mediabunny download pattern) is a historical verification — the teacher repo is GONE (both `/tmp/freecut` and `/home/z/my-project/freecut` absent), so every FreeCut file:line citation in §3/§8/§12/§14/§16 is now record-not-re-derivable |
| §4 Headless Chrome :119-:246 | Flag set; Xvfb fallback; the engine cross-refs | **SOUND** | External-source material (tigerabrodi blog) — unverifiable in-sandbox, consistent as cited. The engine pins re-verify: `run-nle-tests.mjs:8` ("headless Chrome REQUIRES Xvfb") ✓, `:66` `'--use-webgpu-adapter=swiftshader',` ✓ + the flag list :66-72 ✓; the Xvfb decision quote now at `DECISIONS.md:242` (the §14R table's `:226` is STALE — see §14 row) |
| §5 Render Engine :249-:385 | `createRenderEngine`; two-entry-point; memory/VRAM budgets | **SOUND** | Spec-01 citations live: §3.6 two entry points :477, `createRenderEngine` :516, the §3.3/§14.4 SceneManager/getActiveState consumer-responsibility note :275-277 + §14.4 :2046. P3: §5.1's audio sketch predates the landed sidechain law (see §5 below) |
| §6 ffmpeg :388-:486 | Subprocess, mux, formats table | **SOUND** | Internally consistent with §17's CLI reference; ASWF-sourced profile table consistent |
| §7 GPU readback :489-:603 | No zero-copy output path; pipelining | **SOUND** | Research claims as cited; no live-tree conflict (readback is Path-B-only, still SPEC-ONLY) |
| §8 Storage :605-:705 | S3 design; mediabunny UrlSource Range behavior | **SOUND** (P3 rider) | UrlSource behavior cited from `/tmp/freecut/node_modules/mediabunny/src/source.ts:719-998` — teacher gone (record). The engine's landed counterpart (UrlSource via the app's mediabunny install) unaffected |
| §9 Parallelism & Queue :708-:787 | RenderQueue; progress | **SOUND** | Spec 15 §8.10's exportMaster row maps "1:1 onto spec 11 §9.2's `RenderQueue.enqueue()` → jobId contract and §9.3's progress reporting" — both sections exist as described; the mapping re-verifies coherent (15 :3309-3316) |
| §10 Error Handling :790-:821 | Device loss / ffmpeg / Chrome crash / timeout | **SOUND** | SPEC-ONLY re-confirmed: no queue/supervisor anywhere in the five repos (grep-zero) |
| §11 WYSIWYG :824-:1105 | The contract; two entry points one EditorCore; the test | **SOUND** | 01 §3.6 ✓; the landed halves CORROBORATE the contract in code: MASTER 5B rows (decode-verified, pixel parity vs `renderFrameOffscreen`), **Z2 (D30 W-E)** — the scene grade reaches the export (`nle-test-app/src/deliverService.ts:300`, `buildGradeFilterString` shared preview/export), **M2 Wave 1** — both audio paths build through `materializeStrips` (`nle-engine/src/lib/nle/bridge/scene-mixer.ts:50-51` "the same fold reaches the realtime monitor AND renderOffline"), the fresh-mixer offline law (`deliverService.ts` header: "a FRESH SceneMixer.renderOffline pass over the app's REAL segment pipeline (flatten → foldSidechains → transition windows → segments) — NEVER the live mixer"). P3: "same managers" wording vs the one-engine law (F-8) |
| §12 Q1-Q12 :1108-:1347 | The answered open questions | **SOUND** | Historical verification record (teacher gone — same P3 rider) |
| §13 :1350-:1375 | The superseded informal test plan | **SOUND** | Self-consistently superseded by `## Testing` (:2440's header says so) |
| §14 Code References :1378-:1462 | FreeCut tables; URL table; dependency-specs table; **14R engine table + the R23/R24 round-notes** | **P2** | The 14R engine table: `api.ts:111` ✓, `:118` ✓, `MASTER.md:166` ✓ (P3 HTTP-driver row re-read at :164-168), `run-nle-tests.mjs:66` ✓, `G-test-coverage.md:26` ✓, `DECISIONS.md:29` ✓ — but `DECISIONS.md:226` STALE (→**:242**; the RC round's D11/D12 entries pushed it). The R23 note's pins: `orchestrator.ts:524` STALE (→**:544**), api.ts "2,757" →2,777. The R24 note's "every quoted line-pin re-verified in place" was true then, false now for the same four pins + the LOC counts. The dependency-specs LOC column is three rounds stale: 00 466→**863**, 01 1962→**2272**, 02 2478→**2649**, 03 2360→**2792**, 04 2076→**2398**. The `render-abort` pins live (`tests/vitest/engine/render-abort.test.ts`, `:156`/`:236` 'Render cancelled'); the m24.9/m26.10 mounts HOLD at `src/app/page.tsx:8117`/`:9382` (comments re-read; `createRenderAdapter` calls :8120/:9385). "Full reconciliation: 19-code-references.md" pointer live ✓ (19's own R27 report owns its re-base) |
| §15 Corrections :1465-:1591 | The 15 correction rows | **SOUND** | Internal register; no live-tree conflicts |
| §16 FreeCut quote :1594-:2058 | The quoted harness code | **SOUND** | Historical record (teacher gone); the quotes are internally consistent with §12/§14 |
| §17 ffmpeg CLI :2061-:2234 | Per-format exact CLI | **SOUND** | External-source consistency (ASWF/ffmpeg wiki); no live-tree surface |
| §18 Dockerfile :2237-:2437 | §18.1 FreeCut Dockerfile; §18.2 our UNTESTED RunPod image; §18.3 checklist | **SOUND** | The §18.2 promotion caveat's pointer re-verifies: `audits/11-cloud-render.audit.md` Issue 3 exists (:475-480 — the apt-key deprecation + gpg --dearmor recommendation) |
| Testing :2440-:2553 | Tier 1/2/3 + property + manual + assets + commands | **P2** | Tier 1's ffmpeg-argv rows consistent with §17; Tier 2's `POST /api/engine/render-frame` per "spec 15 §8.4" ✓ (15 :3197); `varispeed.json` "registered in spec 17 §5.3" ✓ (17 §5.3's table row "Audio-WYSIWYG realtime-vs-offline render (spec 11 Testing)"); 4k-5min/8k-1min fixtures ✓ in 17 §5.3. **The P2:** the Tier-3 websocket row (:2501) says the dialog "subscribes to `GET /api/engine/subscribe` (SSE/WebSocket per spec 15 §8.5)" — spec 15 defines subscribe at **§8.7** and as **POST** (`:3267-3273`); §8.5 is `render-audio` (:3228). P3 riders: 17:2396's "≈2385" pointer for this section is now :2440; the Tier-3 rows describe the chartered world while the PRESENT DeliverPage master flow routes through the D29 `exportRequest` seam → in-browser `deliverService` (verified: nle-ui `DeliverPage.tsx:135` the optional seam, the app's in-browser render) — needs the two-world sentence (F-10) |

**Totals: 21 section groups — 17 SOUND, 4 P2, 0 P1, +7 P3 riders (F-6..F-11).**

---

## §3 The fix-list (exact edits, amendment-wave-ready)

**F-1 (P2 — §0 BASE :16, the live LOC pins).** Replace `(2,757 LOC; the 9-method` with `(2,777 LOC at the R27 pin \`f9ac806\`; the 9-method` — and replace `(orchestrator 936 / settings 368 / audio-mixdown 369 / contracts 92 LOC; Wave 5B` with `(orchestrator 957 / settings 368 / audio-mixdown 369 / contracts 92 + index 20 LOC — 1,806 total; Wave 5B`. In the §0 BASE header (:12), append after `2026-09-08):` → `2026-09-08; the R27 re-verify at engine \`f9ac806\` moved four pins — DECISIONS :226→:242, orchestrator :524→:544, api.ts 2,757→2,777, export/ 1,785→1,806 — see §14R's R27 note):`.

**F-2 (P2 — §14R, add the R27 round-note after the R24 note).** New blockquote after :1461:
> **R27 re-verification (2026-09-14, engine `f9ac806` 748/748 · OT `55c81c0`/`970948a` · WDC `ec8fd5c` · nle-ui `32abd58` · app `c020b2a`):** zero cloud-service code re-verified by grep across all five R27 pins (same false-positive classes: the S3A wave-tags, WDC's FFmpeg-bake design comments, the mock DeliverPages); the RC round's engine deltas (the D12 ratification, OV-01/02/05/12, M2 Wave 1's materializeStrips, the NS queue) are laws + docs, NO cloud code. The two browser-side halves unchanged and landed: headless/api.ts **2,777** LOC (NleHeadlessApi :111, frame-grab :118, HARNESS_READY_PREDICATE :2771 — frozen in api-surface.frozen.ts:62); export/ 5 files **1,806** LOC (orchestrator **957** — `createRenderAdapter` now **:544**, `renderCompositionCore` :187; settings 368 / audio-mixdown 369 / contracts 92 / index 20). Moved pins: `.agents/DECISIONS.md` :226→**:242** (the D11/D12 entries); `run-nle-tests.mjs:8/:66`, `MASTER.md:166`, `G-test-coverage.md:26`, `DECISIONS.md:29`, the page.tsx m24.9/m26.10 mounts (:8117/:9382) all HOLD. Browser-side additions to the Path-A-adjacent surface: the app's `deliverService.ts` (418 L, D29 W2.4 export host — in-browser mediabunny, blobUrl, no server) now carries **Z2** (D30 W-E: the scene grade reaches the export, :300) and consumes the M2 Wave 1 audio law (a FRESH SceneMixer.renderOffline over the real segment pipeline incl. foldSidechains; both paths build through `materializeStrips` — scene-mixer.ts:50-51). The activation-time ENGINE-GAPs unchanged (P3 HTTP driver, Stage-3 worker/OPFS, Stage-5 smart-copy per `gaps/audit/C-export-encode.md`).

**F-3 (P2 — §14R table row, the Xvfb pin).** In the §14R table, replace `` `.agents/DECISIONS.md:226` `` with `` `.agents/DECISIONS.md:242` `` (the quote itself unchanged — re-read at :242).

**F-4 (P2 — §14 dependency-specs table :1434-:1440, the LOC column).** Replace the five LOC values: `466`→`863` (00), `1962`→`2272` (01), `2076`→`2398` (04), `2478`→`2649` (02), `2360`→`2792` (03) — or strike the column to `LOC (at authoring; R27 count in brackets)` per the amendment wave's preference.

**F-5 (P2 — Testing :2501, the websocket row's wrong method + section).** Replace `the dialog subscribes to \`GET /api/engine/subscribe\` (SSE/WebSocket per spec 15 §8.5)` with `the dialog subscribes via \`POST /api/engine/subscribe\` (SSE/WebSocket per spec 15 §8.7)`.

**F-6 (P3 — §0 GAP row :20, the D6/r4 adjacency sentence).** Append to the GAP row: `[Dependency note (R27): the plan's r4 row — real media decode, was W-n5 — is "PENDING the user's D6 re-affirmation (deprioritized)" (plan §2:76); D6 is 00-master's one-engine-two-entry-points decision, i.e. THIS spec's foundation — the r6 re-scope ask and the D6 re-affirmation are the same user gate at two ladder ends; r6 stays independently slippable either way (plan §2:78 "slips independently by design").]`

**F-7 (P3 — §0 or §14R, the D12/D16 venue ratification note).** One sentence in §0 BASE (after the two-halves bullet): `[Venue note (R27): the corpus Decision 12/16 junction is now RATIFIED engine-side (engine DECISIONS D12, :521 — "nle-engine's freecut-lineage stack … is the RENDER/EXPORT venue model … The junction is ONE-DIRECTIONAL"); Decision 16 law 2 re-types the engine's JSON-RPC+$ref surface as "INTERNAL transport (the headless/cloud venue)" — this spec's cloud path is a venue-internal consumer of that stack, never an editor (§5.1's render-only design already conforms).]`

**F-8 (P3 — §11.2, the one-engine-law clarification).** After "The only differences are the clock, the output target, and the audio context." add: `[Landed refinement (R27): the audio half's concrete shape is the one-engine law — a FRESH SceneMixer materialization over the same segment pipeline for offline render, never the live mixer instance (a mixer materialized on the realtime AudioContext throws on renderOffline; both paths build through materializeStrips — engine scene-mixer.ts:50-51, app deliverService.ts header). "Same managers" means same graph topology, not shared instances.]`

**F-9 (P3 — §0 :15, the heartbeat disambiguation clause).** After `10s heartbeat` insert `(a job-claim liveness heartbeat — NOT web-daw-core's EngineDiagnostic bus/heartbeat law, the S-series diagnostics landing that no spec carries yet; see fleet-r27/spec-02.md)`.

**F-10 (P3 — Testing Tier-3 intro :2494-:2496).** After the spec-18 ownership sentence add: `[Two-world note (R27): the PRESENT DeliverPage 'master' flow routes through the D29 exportRequest seam to the app's in-browser deliverService (blobUrl result — Path-A-adjacent, verified at c020b2a); the rows below describe the CHARTERED cloud world (POST /api/render + signed URL) and are activation-time tests per §0's ACCEPTANCE row.]`

**F-11 (P3 — §14 header, the teacher-provenance line).** Add under "### FreeCut source files read in full": `[Provenance (R27): the FreeCut teacher repo is no longer on disk (both /tmp/freecut and /home/z/my-project/freecut absent at the R27 audit) — the file:line citations in §3/§8/§12/§14/§16 are the verification record as of SCOUT-11 (2026-09), not re-derivable; cloudcut-nle IS on disk (/home/z/my-project/cloudcut-nle) and its citations re-verify live.]`

---

## §4 Cross-spec notes (for the cross-cut wave — NOT this spec's edits)

1. **17 §13A:2396** pins 11's `## Testing` at "≈2385" — now **:2440** (11 grew in the R22-R25 amendment rounds; the pointer is section-named so it still resolves). 17's amendment wave re-pins.
2. **15 §8.10's exportMaster ↔ 11 §9.2/§9.3 mapping** verified coherent BOTH directions — no edit needed on either side; the job-envelope/jobId/progress mapping is design-consistent with 11's queue.
3. **19-code-references.md** is the R24 register (engine 458/458 @ `5036387` etc.); 19's own R27 report (spec-19.md) owns the re-base — 11's F-2/F-3 pins should be absorbed by it, not duplicated.
4. **02's report owns the WDC diagnostics/heartbeat landing** (corpus fact #7); 11's only touch is F-9's disambiguation — without it, the corpus grep "heartbeat in all 21 specs → only 11:15" misleads (it's cloudcut's job-claim heartbeat, not the EngineDiagnostic liveness monitor).
5. **09/06 preservePitch**: when the `el.preservePitch` ruling lands (absent≡true), 11's varispeed fixtures inherit it — 17 §5.3's `with-varispeed.json`/`varispeed.json` descriptions ("pitch preserved") are already consistent with the absent≡true default; no 11 edit needed, just awareness in that wave.
6. **00-master** carries the D27.3 cloudcut primary row (:21) + Decision 27 (:425) — 11's "primary row lives in 00-master §0" pointer verified ✓; the D12-ratification note (corpus fact #13) is 00-master's edit, 11's F-7 is the domain-side cross-ref.
7. **The plan (§2:64, w2 media layer)** still carries the cloudcut hybrid-storage RFC reference row verbatim ("a design input only, NO dependency") — verified consistent with 11's D27.3 framing; no drift.
8. **battery_r27** can add two cheap checks: (a) the spec-11 exemption pair `(11, 13)` carries forward (battery_r26.py:186 pattern); (b) 11's §0 GAP row still quotes the plan's r6 phrase "cloud render UNCHARTERED unless the user re-scopes" verbatim (plan §2:78) — a drift-canary for the r6 ruling.

---

## §5 What 11 uniquely sees (the cloud-lens findings)

1. **The two-halves truth is the fleet's cleanest unchartered-stream register — zero P1s across three re-pin rounds (R23/R24/R27).** The design-of-record + honest-mock + battery-exemption discipline held; the only maintenance this spec ever needs is pin-drift + round-notes. The R27 delta is exactly four moved pins and one wrong test-row pointer.
2. **cloudcut-nle is on disk and its §0 row re-verifies line-for-line TODAY** — 419 LOC render-worker, the 10s heartbeat, ffmpeg-static spawn (:239), the `/api/render` claim/heartbeat routes, NOTES_FOR_SUCCESSOR, the hybrid-storage RFC (three tiers/480p proxy/Tauri). This is the corpus's only LIVE as-built server-side render path — the r6 activation design's only real reference; no other spec can say "the dormant half exists and I checked it today."
3. **The heartbeat collision is 11's alone to defuse:** 11:15 is the corpus's ONLY "heartbeat" — and it is cloudcut's job-claim heartbeat, not WDC's EngineDiagnostic liveness monitor. The corpus fact #7 grep reads wrong without F-9's clause.
4. **The engine's D12 ratification names 11's subject and 11 is silent on it:** "nle-engine's freecut-lineage stack … is the RENDER/EXPORT venue model" + D16's "JSON-RPC = the headless/cloud venue's internal transport" — the venue vocabulary the rest of the corpus now uses for the engine was coined over exactly this spec's domain; registering it (F-7) makes 11 the place the ratification is cross-referenced from the cloud side.
5. **The WYSIWYG contract is being corroborated in code BEFORE the stream is chartered:** Z2 (the grade reaches export — deliverService.ts:300) + M2 Wave 1 (both audio paths through materializeStrips — scene-mixer.ts:50-51) + the fresh-mixer one-engine law mean the §11.1 contract's preview/export parity now has three landed enforcement points, all in the browser half. The cloud half inherits them for free at r6 entry.
6. **The r4/D6 adjacency:** the plan gates real media decode on "the user's D6 re-affirmation" — D6 is 11's own foundational decision. The user's r6 re-scope and the D6 re-affirmation are one question asked at two ladders' ends; 11 is the only spec positioned to say so (F-6), and until answered, r6 "slips independently by design" (plan §2:78) — 11's unchartered register agrees exactly.
