# R27-W3 — spec 18 (UI shell) final-tightness audit

**Task ID:** R27-W3-18 · **Agent:** spec-18 · **Date:** 2026-09-14 · **Round:** R27 Wave 3 (per-spec fleet)
**Spec:** `18-ui-shell.md` v1.7 (663 lines), read §0→§16 in full, every named claim re-derived against the live trees.
**Pins (re-derived live):** nle-ui **`32abd58`** (690/690; R9-b preservePitch + FW-D setElementFieldAll + AW1-2 Gain ceiling verified in-tree); variants mock **`685edba`** (R25-W2 landed — **the mock now carries the R24-W5d state + R25-W1 + R25-W2**; suite 1780/1780 green per the wave's gates; census re-key at wrap per the coordination law); mini @ R24-2 in-flight (415); OT `55c81c0`/code `970948a` (632/632; wire census **31 = 28 routed + 3 exceptions**, `api.ts:243-275`/`:328-335`); engine `f9ac806`; app `c020b2a` (252/252).
**Method:** the mock reports (mock-variants/mock-verdicts/mock-trim/mock-insert/mock-color) + the BRIEFING's 15 corpus facts applied §-by-§; every finding below carries file:line evidence re-read in-tree this round. Report-only.

---

## §1 The per-section verdict table

| § (line) | Claim class | Verdict | Evidence (live, this audit) |
|---|---|---|---|
| §0 BASE pins (:14-15) | pins | **P2 — all five pins moved** | nle-ui `3026099`/674 → **`32abd58`/690** (git log; AW1-2 commit gates "vitest 690/690"); app `64fb0ab`/206 → **`c020b2a`/252**; OT `fdb771c`/`c15a629`/536 → **`55c81c0`/`970948a`/632**; engine `3989506` → **`f9ac806`** (748/748). |
| §0 wire-census cells (:25) | census | **P2 — "24 routed + 6 at `c15a629`" is STALE** | **31 = 28 routed + 3 exceptions** (`selectElements`/`advancePlayhead`/`trim`) at `970948a`; the singular `removeKeyframe`/`retimeKeyframe` RETIRED, `timeline.insertBatch`/`removeKeyframes`/`retimeKeyframes` live (BRIEFING fact 1-2; spec-05 §1 rows 14/18 verify the same numbers — consistent). |
| §0 honest-mock ledger (:17) | ledger | **P1 — one clause FALSE** | "Inspector Pan/Transform/**preserve-pitch** (mock-local)" — preserve-pitch is MODEL-BACKED since R9-b: `Inspector.tsx:729-733` writes `el.preservePitch` (`setFieldAll({preservePitch})`), the absent≡true tri-state at `:765-769`, pinned `Inspector.test.tsx:254-268`; the header comment `:676` itself says "update this list with R9-b". Pan + Transform stay mock-local (verified `:993-996`, `:672-703`) — only the preserve-pitch clause is stale. |
| §0A/§1/§2 | framing | SOUND | The 3-page posture is the registered OPEN decision (§4.8 row below). |
| §3 layout/§3.2 (:119) | sizing | SOUND + P3 | The 1280×800 floor vs the mock's responsive sub-floor behavior (the reviewer canvas 700–1100px; W1's ladder law operates below the spec's floor) — mock-verdicts OQ-4 stays the registered open question; the mock's sub-1280 responsive behavior is a REGISTERED divergence, not a defect. |
| §4.1 toolbar2 (:139) | chrome | SOUND | Verified vs nle-ui `Toolbar2`/`AppShell`; the mixer divergence is §8.8's row to carry (below). |
| §4.2 MediaPool | contract | SOUND | Import flow, sort modes, state rows spot-checked against the variants mock + nle-ui; no drift found. |
| **§4.3 Viewer — fallback row (:174)** | source preview | **P1 — superseded by LANDED law** | "it plays the raw asset via a plain `<video>` element swapped over the canvas" — **zero `<video>` elements exist in the variants tree** (grep-verified); the reference source viewer is the asset POSTER + moving playhead + running TC (Viewer.tsx:111-140, :408-412; W1 `a83d4d2`). The spec's interim describes an implementation neither reference ships. |
| **§4.3 SourceRangeBar (:176) — stills clause** | stills law | **P1 — FALSE vs landed code** | "stills keep the honest static band (no duration → no range)" vs W1: stills = NORMAL clips, **5s pseudo-duration** (`SOURCE_STILL_PSEUDO_DUR`), full transport, trimmable domain — `SourceRangeBar.tsx:17-27` ("A1 stills: a still = a normal 5s clip in Resolve… gains the transport strip"), `Viewer.tsx:138-140`, `useUiStore.ts` sourcePlayhead family. mock-verdicts V1's CONTRADICTION is now a code contradiction (mock-verdicts wrote when the code agreed with 18 — W1 flipped it). |
| **§4.3 SourceEditBar (:175)** | responsive law | **P1 — the landed law missing** | :175 carries the D35.4 one-shot/roving/refusal contract (all CONFIRMED by `SourceEditBar.tsx:8-24`) but NOT the W1 **priority ladder** (the flex-starvation root cause; the bar measures its own width via ResizeObserver, full→icon-only, labels to aria/data-tip; duration readout degrades first, trim second; overflow = LAST resort — `SourceEditBar.tsx:24-35`, mock-verdicts V6) nor the W5b gesture riders (B7 pointercancel/lostpointercapture, aria-orientation, Home/End — `SourceRangeBar.tsx:29-33`). |
| §4.3 transport/scrub rows (:172-173) | program viewer | SOUND | The loop-seam/inverted-window laws verified against the mock's scrub row; 16 §3.1's I/O→setLoop rows exist (:157-161). |
| **§4.4 Audio tab (:189)** | field set | **P1 — FALSE vs the BASE** | "'Gain dB' is withdrawn — no dB field exists in 09" — the BASE ships the **[−60,+20] Gain dB rail** writing `el.volume` through the D28-A2 law (`nle-ui Inspector.tsx:997-1008`: `min={-60} max={20}`, commit `10^(dB/20)` clamped ≥0.001; AW1-2 raised the ceiling +4→+20). **09 §3.1A B2 (:337) itself cross-cites "18 §4.4's 'Gain dB' row is the display form"** — the two specs now disagree about what 18 says; the R15 withdrawal created the circular contradiction. "Volume %" is equally false (the Levels group = Gain dB + Pan + Fades). |
| §4.4 preservePitch (:184-189) | cross-ref | P3 | :184's "→ retime family, spec 06 §5.11" — §5.11 is **Rate Stretch**; the retime/pitch-preservation family is **06 §5.12** (`RetimeConfig.maintainPitch`, 06:1731/:1793-1804). Plus the absent≡true default law (R9-b) is unstated (09's ruling is this round's spec-09 amendment; 18 should cite it, not re-derive). |
| §4.4 refresh law | missing law | **P2** | No refresh law anywhere in §4.4 ("on any view/mode transition the inspector re-resolves; unresolvable → empty state, never stale data") — mock-verdicts V19-R4; the mock's W6 carries it (r25:143-144, pending). C44 rides the same seam. |
| **§4.5 tool cluster (:202)** | tool union | **P2 — the FX tool missing** | 16 §3.2's R23-WA note: "the variants' FX tool joins the edit-tool radio… **Spec 18 §4.5's tool union is the owner of the seventh member**" — 18 §4.5 lists only 15 §4.3.45's nine (no fx; verified `15-wire-protocol.md:1476`). The variants' `ToolId` = select/blade/roll/ripple/slip/slide/stretch/**fx** (useUiStore.ts:53). 16 delegated the home to 18; 18 never took it. |
| §4.6 scene tabs | contract | SOUND | Verified against the variants' SceneTabs. |
| §4.7 view flags (:216) | render gate | **P2** | "the mock's per-track waveform/clip-view toggle is a UI pref" — now a **render gate + batch law**: the flag false → no waveform band (variants `Clip.tsx:1067-1083` per spec-05's re-read), `setAllTrackWaveforms` = ONE withHistory write (useUiStore.ts:803/:1737); + the ViewOptionsPopover chrome (APG menu: Compact checkbox DOM-absent on FX page, Clip-style radio, waveforms checkbox w/ honest aria-disabled — `ViewOptionsPopover.tsx:201-249`) is uncarried (mock-variants rows 8-9). |
| **§4.8 deliver band (:222)** | placement | **P1 — FALSE** | "mounted in place of the compact ruler's head row" — W4 made it **coexistence**: the 22px read-only ruler is UNCONDITIONAL + the 32px band mounts BELOW (54px stack; `rangeBand={page==='deliver'}` — `TimelineCompact.tsx:202`, comment :51) + the ruler gains read-only in/out bracket FLAGS (`shell-timeline-compact-flag-in/out`, `TimelineCompact.tsx:170-190`). |
| §4.8 band drag law (:222) | semantics | **P2** | :222 presents R14 drag-along as the band's law unqualified — W4 split it: **mouse = clamp against the opposite live edge (release commits exactly the preview); keyboard = R14 drag-along verbatim** (mock-variants row 6). |
| §4.8 Color page (:220) | page row | **P2** | "simplified single-column layout" — the W2 composition: Gallery in the LEFT dock (leftDockContent.ts:45), ColorInspector in the right rail (AppShell.tsx:338), node graph in console-row slot [6] (:397-401), scopes pane under the always-led viewer; R25-A2 (W3-pending) will re-home scopes to the `[Timeline|Nodes|Scopes]` console-row TAB. |
| §4.8 page census / D35.4 OPEN | open decision | **P2 — the decision's scope is stale** | The divergence is now **three-way**: spec 3 pages (Edit/Color/Deliver) · **BASE nle-ui 4** (`Page = 'edit'|'color'|'audio'|'deliver'`, useUiStore.ts:17 — Audio page = ChannelEditor + SoundLibrary, AppShell.tsx:308-330) · variants mock **5** (`'edit'|'color'|'audio'|'fx'|'deliver'`, useUiStore.ts:57). "3-vs-5" mis-names the BASE's own divergence. The OPEN posture stays right (no resolution this round — a future design round's call) but the registered row must name all three sets. |
| §4.9 context menus | contract | SOUND | Clip/track/ruler/pool/empty menus verified against 15 §4's types + the variants' ContextMenu; N10's ruler-seeks law holds in-tree. |
| §4.10 sample project | contract | SOUND | — |
| **§4.11 EffectsPanel (:249)** | routing canon | **P1 — the A1 canon missing** | :249 carries only the two-line drag contract (MIME + addEffect/updateTransition). LANDED mock-side (W3 `0a45d17`): **replace-never-stack** (presentation swap RETAINS duration+alignment; identical short-circuits with the "Already X" toast — `Clip.tsx:261`, `:271` "A1-R1 replace-never-stack"), one-law-three-doors, effect-stack ×N, empty-lane pure no-op, 1ms adjacency refusal (`SEAM_EPSILON = 0.001`, Clip.tsx:219), dbl-click apply. mock-variants row 3 called this "the single highest-value spec add". |
| **§5 gesture table (:264-265)** | normative rows | **P2 — 3 of 4 tool rows missing** | The table has trim + roll only; ripple/slip/slide tool gestures are spec'd nowhere normative (mock-trim F-6/N-3; 05 §8A is register-class). The mock implements all three (variants `Clip.tsx:834-861`: roll = tool or select+Alt; ripple returns `ripple-{edge}`; slip/slide body drags). The table's own "This table is normative" makes the gap the Tier-3 contract gap. nle-ui's tool radio is still INERT (`Clip.tsx:524/:536` `if (tool !== 'select') return` — re-verified; G7-ripple/N-5). |
| §5A cursor table (:311-313) | layer-1 law | **P2** | "Clip left/right edge (hit zone) \| `ew-resize`" — W2's ladder is **directional**: single edges = `w-resize`/`e-resize`, the ROLL junction = `ew-resize` (`Clip.tsx:834` `handleCursor`). The spec as written renders every edge bidirectional; the reference grammar (the single-bracket push) is directional. |
| §5A dbl-click resets (:301) | blanket law | **P2 — the wheels exception (registered forward)** | The blanket reset-to-default law needs R25-A3's exception: dbl-click a wheel DISC = color-only reset (master survives); the per-wheel corner button = full reset (mock-verdicts V14). A3 is binding round-doc law; the mock's WheelsPanel is still the pre-A3 absolute model (WheelsPanel.tsx:12-14/:148 — the rewrite is their W3). |
| §5A cursor layering (:328) | D33.1 ruling | **CONFIRMED by landed code** | W2 implements exactly the ruling: drag-only SVG glyphs + CSS hover cursors (`Clip.tsx:834`, TrimAffordances.tsx pointer-dead, z-10); the 05 §8A:622 row cross-cites it coherently. No edit needed beyond the :311 rows + the A3 exception. |
| §6 state binding | view-state family | **P2** | The W0/W5d "honest view-state" family is uncarried: setPage clears fx-selection, setTransition identical-patch no-op, split clears fx-selection, no-op guards on toggleEffect/removeEffect, Escape ladder — one §6.2 bullet family (mock-variants row 14, P3 there but it is spec-18's home). |
| §6.3/§6.4 | UX | SOUND | Save chip + toast/error tables verified against nle-ui StatusStrip/ToastRegion. |
| §7 rendering | contract | SOUND | — |
| **§8.4/§8.8 removal ledger** | ledger | **P1 — contradicted by the accepted BASE** | §8.8 "Mixer / Metadata panels… **Removed**" + §8.4's Fairlight/audio-post removal vs the BASE's **Audio page + full mixer family** (nle-ui `AppShell.tsx:310` audio → ChannelEditor, `:330` SoundLibrary, `:373` MixerDock — the F3 real-gain slider family). The register carries the mixer row (owning spec 20-M2, MOCK-YIELDS-REGISTERED); the LEDGER row carries no divergence note, so the spec contradicts its own accepted BASE. |
| **§9 token table (:407)** | token law | **P1 — the playhead parenthetical is FALSE** | "`--accent-selection` \| `#e8b34b` (**mock playhead gold**) \| selection, active tab, **playhead**" — the davinci mock's playhead is **RED `#fa1024`** (`davinci_resolve_ui_mock.html:535` `.playhead-line background: #fa1024`; also :961), and the variants' token law is "**Playhead: dedicated token (mock-authentic red #fa1024) — time ≠ state**" (tokens.css:8, :59). No reference paints the playhead gold; implementing the row as written forks the reference. |
| §9 grammar tokens (:412-423) | D33.1 half | **P2 — two gaps** | (a) The W2 implementation paints the grammar over HOUSE tokens with a documented inline map (reference `#e2403c` → `--danger #e5484d`; the `#8ce22e` greens → `--mk-green #46a758`; SOLID 4px `#fbfdff` boxes → the 2px-dashed white ghost grammar — TrimAffordances.tsx header); the spec's "implemented as-is, no fork" needs the registered-divergence note. (b) The mock's W5 surface waves added the 08-domain color tokens (`--scope-*`/`--nodegraph-*`/`--node-*`/`--wheel-*`, tokens.css:316-338) — uncarried anywhere (08's home, cross-noted). (c) The `@layer base` CSS-layering house rule (W5a's range-collapse P1 fix — unlayered author CSS outranks Tailwind utilities; app.css:44-47) is uncarried (mock-variants row 13). |
| §10 testids | contract | SOUND | The flag/band/popover testids ride the family conventions. |
| §11 a11y | floor | SOUND | F6 ladder, roving, Shift+F10 all verified against the mock's implementation (W5d's splitter tabIndex=0 wave). |
| §12 testing | plan | SOUND | The Tier-3 contract rows align with the mock's pin families. |
| **§13 code refs (:513-514)** | pins | **P2 — both rows stale** | :513 nle-ui `fc4cc35`/"674 tests (37 suites) + 74 stories" → **`32abd58`/690** (R9-b + FW-D + AW1-2; 37 test files, live-counted). :514 OT "`ded43c4` [code pin `c15a629`] — 536/536… **24 routed + 6 exception verbs**" → **`55c81c0`/`970948a` — 632/632, 31 = 28 routed + 3 exceptions** (the D-ARCH-6 verbs; the row's exception names are counts-only, so P2 not P1). |
| §14 relationships | cross-refs | SOUND | The 15 §4 "78 types" claim holds (15:285 "Total: 78 command types"; the 78→79 replace-verb row is r1-SCHEDULED, not landed). |
| §15 open questions (:532-534) | staleness | P3 | Q1's framing ("does v1 need media-asset playback before timeline insert?") is answered mock-side by A1/W1 — restate as the v2 command-surface question; Q3's color-layout question is superseded by R24-W2 + R25-A2 (08's W-B row owns it). |
| §16 shell-mini | census | SOUND | The R24-2 waves land mid-audit (415 at last count); per the register's coordination law the mini counts re-key at the sibling's wrap — no edit this round. |

**Tally: 7 × P1 (§0 ledger clause, §4.3 ×3 [fallback/stills/EditBar-law], §4.4 Gain-dB, §4.8 placement, §8.8 mixer-removal, §9 playhead) + ~13 × P2 + 4 × P3.** (The §4.3 cluster counts as one finding-complex with three sub-rows; total distinct P1 fix items = 7.)

---

## §2 The fix-list (exact edits, amendment-wave ready)

**F-1 (P1) — §4.8:222, the RangeBand placement + drag split.**
Replace "a 32px interactive in/out band mounted in place of the compact ruler's head row, with full-height 12px bracket handles (thin-glyph law: the hit target ≠ the visual size), a TC readout, and a dim-not-erase fill over the excluded span" with:
> "a 32px interactive in/out band mounted **BELOW the compact ruler's head row on the Deliver page — the 22px read-only ruler is UNCONDITIONAL in the compact head stack (ruler + band = the 54px deliver head; W4 `6b0bd91`)**, and the ruler itself gains read-only in/out bracket FLAGS (`shell-timeline-compact-flag-in/out` — the loop seam's brackets in miniature, pointer-events-none), with full-height 12px bracket handles (thin-glyph law: the hit target ≠ the visual size), a TC readout, and a dim-not-erase fill over the excluded span."

In the same block, split the drag law:
> "**the drag law (ruling 21)** — LOCAL preview state during the drag, ONE commit on release, a no-op release writes nothing, pointercancel discards; **the mouse path (R24-W4) CLAMPS against the opposite live edge — the release commits exactly the preview**; **the ordering law (R14) governs the keyboard path verbatim** — writing an edge past the other DRAGS the far edge along, never inverts (the same law as §4.3's scrub-row inversion guard);"

**F-2 (P1) — §4.3, the A1 source-viewer law (one amendment, replaces the fallback row's last sentence + the stills clause + extends :175-176).**
(a) :174, replace "it plays the raw asset via a plain `<video>` element swapped over the canvas — media-asset playback, no timeline state involved" with:
> "the source monitor shows the raw asset's **poster** (letterboxed, object-contain) with a **moving playhead + running TC over the poster as the honest playback** — media-asset playback, no timeline state involved, no second decode surface"
(b) :176, delete "stills keep the honest static band (no duration → no range)" and append to the SourceRangeBar row:
> "**R25-A1 (round-doc binding; landed W1 `a83d4d2`):** stills are sources too — a still loads with a **5s pseudo-duration** (Resolve's default; the trimmed [0, 5s] domain satisfies 16 §3.4A's C46 gate — 09's synthetic-extent ruling carries the default), the full transport (play 'plays' the frozen frame; **play stops at out**; J/K/L drive the source transport while the source monitor is open — fixed ±1×, the program accel ladder stays program-only; Space/Home/End/←→ join the program-mode grammar, gated on source mode), and the **no-trim law survives** (a still has no real source range — no handles; inserts stay the fixed-length still). **I/O marks render as bracket flags ON THE SCRUB STRIP with out-of-range dimming ON THE STRIP — never on the image** (the poster is never dimmed; its playing state = the live TC + a 2px playhead progress line); **the duration readout switches to the [in, out] range**, and with a range set the playhead's domain IS [in, out] (scrub, Home/End, and playback clamp to the trimmed span). The SourceRangeBar is a REAL SCRUB STRIP — the dual in/out handles + range band + the source playhead marker (B7 pointercancel/lostpointercapture discipline, B8 aria-orientation, absolute drag, Home/End on either handle)."
(c) :175, append to the SourceEditBar row:
> "**Responsive priority, not overflow (R25-W1):** the edit bar is the transport row's PRIORITY consumer — it measures its OWN width and degrades **full-labels → icon-only** below the label floor (labels live in aria-label + data-tip); the duration readout degrades first, the trim cluster second; **all 7 buttons remain visible at every usable width** — a horizontal-scroll bar is the LAST resort, never the primary means (the W5b overflow-x-auto escape is demoted). Source transport row: left = current TC/jog · center = go-to-start / step / play / step / go-to-end / loop · right = Mark In / Mark Out; the 7 edit-mode buttons stay in the row behind a clear divider, as the PRIORITY cluster (Premiere's Source-Monitor placement)."

**F-3 (P1) — §4.11, append the A1 routing canon** (after the drag-to-clip contract, before the click-fallback row):
> "**The routing table (R24-A1, landed W3 `0a45d17` — the FCP/Premiere/Resolve unanimous canon):** one transition per seam, drag-to-REPLACE — **never stack** (a presentation+type swap RETAINS duration and alignment; an identical presentation short-circuits BEFORE the write with an 'Already X' toast). One law, three doors: an **empty seam** mints; an **occupied seam** REPLACES; a **clip body** routes transition rows to the OUT seam, effect rows to the effect stack (a ×N stack is legal — duplicate instances), fade rows to `setFade`. A transition row on a clip with no butt-spliced neighbor (1ms epsilon) = a refusal toast; a drag to an **empty lane** = pure no-op (no highlight, no toast, no commit). **Double-click a browser row** = apply to the single selected clip (zero/multi selection = the honest 'Select one clip' toast); **click on an occupied seam = select-to-edit, never a dialog.**"

**F-4 (P1) — §4.4:189, the B2 rewrite.** Replace "the audio tab's field set is Volume % + pan −100..100 + fades — reading 09 §3.1's `volume` (linear 0..1, presented as %) and `pan` (−100..100, ADDED to ElementJSON by the parallel R15 09-side amendment, cross-ref); 'Gain dB' is withdrawn — no dB field exists in 09 and dB conversion is display-side only" with:
> "the audio tab's Levels group is **Gain dB** — the **[−60, +20] fleet-coherent domain** (opencut's `core/audio-params.ts` the ONE home; AW1-2 raised the authoring ceiling +4→+20) — authoring `el.volume` through the D28-A2 dual representation (display 20·log10, commit 10^(dB/20) clamped ≥ 0.001; 09 §3.1's linear 0..1 is the PERSISTED unit, the dB rail the authoring+display form — 09 §3.1A B2's cross-ref expects exactly this row) + pan −100..100 (ElementJSON-backed per 09 §3.1; the BASE's per-element pan authoring is still mock-local — nle-ui's strip-layer pan is the real pan, the register's C40 family) + fades"
and fix :184's citation: "→ retime family, spec 06 **§5.12**" (was §5.11 = rate stretch), appending "(`preservePitch` is ElementJSON law — absent≡true; the engine's `retime.maintainPitch` the runtime projection: R9-b)".

**F-5 (P1) — §8.8 + §4.8's OPEN decision, the registered divergences.**
(a) §8.8's Rationale cell, append:
> "**REGISTERED DIVERGENCE (R27):** the accepted BASE ships an Audio page (`Page = 'edit'|'color'|'audio'|'deliver'` — nle-ui useUiStore.ts:17) with the full mixer family (ChannelEditor/ChannelStrip/MixerDock — AppShell.tsx:308-330/:373), and the variants mock adds an FX page (5-page set); the register's mixer-family row owns the law (spec 20's M2 design round; MOCK-YIELDS-REGISTERED)."
(b) §4.8: after "three pages, not seven", re-scope the registered note: "the page-set divergence is **three-way** — this spec's 3 (Edit/Color/Deliver) vs the BASE's 4 (+Audio) vs the mock's 5 (+Audio +FX) — registered OPEN (D35.4; a future design round's call, not this round's)". §8.2-8.4's rows keep their rationale text.

**F-6 (P1) — §9:407, the playhead token.** Replace the row:
> "| `--accent-selection` | `#e8b34b` | selection, active tab, CTA — **state only, never time** |"
and add the row the reference implements:
> "| `--playhead` | `#fa1024` (the mock's red — davinci `:535`; variants tokens.css:59) | the playhead + time indicators — **time ≠ state** (the dedicated-token law, tokens.css:8) |"

**F-7 (P2) — §9's grammar-token block, three appends.** (a) The W2 house-map note:
> "**R27 note — the implemented mapping (R25-W2 `685edba`):** the variants' TrimAffordances paint the grammar over HOUSE tokens (reference `#e2403c` → `--danger #e5484d`; the `#8ce22e` greens → `--mk-green #46a758`; the SOLID 4px `#fbfdff` boxes → the 2px-dashed white ghost grammar — the map documented inline in TrimAffordances.tsx). The A1-A8 values above stay the PINNED reference; the house map is the registered divergence, not a fork of the law."
(b) The CSS-layering house rule (one line, in the v1.1 deepening block): "**CSS layering:** control-reset CSS lives in `@layer base` — unlayered author CSS outranks Tailwind's `@layer utilities` (the W5a range-collapse lesson, app.css:44-47); never re-emit an unlayered `input[type=range]` rule."
(c) Cross-note: the mock's color-page tokens (`--scope-*`/`--nodegraph-*`/`--node-*`/`--wheel-*`) are 08's half — pointer only, no 18 rows.

**F-8 (P2) — §5, add the three tool rows** (after the Roll row, :264):
> "| Ripple tool + edge-drag | downstream-shift preview (dim ghosts + the sign-following push/pull arrow) | `trim {ripple}` (the engine's rippleTrimItem composite; the G1 wire shape pending r1) | 05 §8A's ripple grammar; the displaced predicate (same track, start ≥ orig end) |
> | Slip tool + clip-body drag | the white full-source outline + BRIGHT in-point preview — the slot FROZEN | `slip` | source window slides inside a fixed slot; position/duration unchanged |
> | Slide tool + clip-body drag | the neighbors' shrink boxes + outward arrows | `slide` | the 3-clip roll; span invariant |"
with the note: "the variants' Clip.tsx:834-861 is the reference implementation; the router/15-union verb names finalize at the r1 ports (06 §5.4-5.7's families; D33.5's sequencing law)."

**F-9 (P2) — §5A.** (a) :311-313: "Clip left/right edge (hit zone)" → "`w-resize` / `e-resize` (directional — the single-edge push; R25-W2, Clip.tsx:834)"; "Roll tool on cut point" stays `ew-resize` re-worded "(the ONLY bidirectional edge cursor — the two-way junction)". (b) :301, append: "**EXCEPTION (R25-A3, registered forward — the mock's W3 implements):** double-click on a color wheel's DISC resets the COLOR component only — the master/luma value survives; the per-wheel corner button is the full (color+master) reset (08 §4.3 cross-ref)."

**F-10 (P2) — §4.4, the refresh law** (one bullet in the v1.1 field contracts): "**Refresh law (R25-W6, registered forward):** on any view/editor-mode/page transition the inspector re-resolves its target entity; an unresolvable target shows the empty state, never stale data (C44's empty-state → active-track fallback rides the same seam)."

**F-11 (P2) — §4.5, the FX tool** (append to the tool-cluster sentence): "**the FX tool (R23-WA ruling 18):** a seventh radio member (select/blade/roll/ripple/slip/slide + fx — the variants' ToolId) with NO plain key (V/B/T/Y/U/R own the six rungs); reachable via the radio + the ⌘5 FX-page chord; implies fxMode; leaving the FX page re-seats a stranded fx tool to select. 15 §4.3.45's nine-member enum does not carry it — either the enum grows the member at r1 or the fx tool stays a 16 §0.2 UI-layer extension (registered divergence)."

**F-12 (P2) — §4.7, the view-flag upgrade.** Replace "the mock's per-track waveform/clip-view toggle is a UI pref" with: "the per-track waveform flag is a RENDER GATE (flag false → no waveform band; thumbnail + name stay) + the `setAllTrackWaveforms` ONE-batch write (one undoable write converges every audio track — the N6 lock-all precedent, §4.5:207); the ViewOptionsPopover (APG menu: Compact tracks checkbox — DOM-absent on the FX page; Clip style Filmstrip|Block radio; Audio waveforms checkbox, honest aria-disabled + reason while compact) is the owning chrome (18 §4.5/§4.7; the register's view-state row)".

**F-13 (P2) — the pin family (§0:14-15/:25 + §13:513-514).** Mechanical re-pin: nle-ui `32abd58` (690/690; R9-b + FW-D + AW1-2 named), app `c020b2a` (252/252), OT `55c81c0`/`970948a` (632/632; **31 = 28 routed + 3 exceptions** — selectElements/advancePlayhead/trim; the singular keyframe verbs RETIRED, the batch verbs live; `WIRE_COMMAND_TYPES api.ts:243-275`, `WIRE_UI_EXCEPTIONS api.ts:328-335`), engine `f9ac806` (748/748); §0's honest-mock ledger deletes the preserve-pitch clause.

**F-14 (P3) — §15.** Q1: restate as "the v2 question is the COMMAND-SURFACED source monitor (the mock-side A1 grammar is landed law — §4.3)"; Q3: re-point at 08 §0's W-B row (the R24-W2 composition + the R25-A2 TAB supersession own the layout question).

---

## §3 What 18 uniquely sees (the fleet-level facts only this audit surfaces)

1. **The variants mock has become a THIRD implementation of the trim-mode grammar** (W2's TrimAffordances.tsx, 313 LOC — all four modes' affordances, drag-only, z-10, pointer-dead, the house color map). This falsifies mock-trim's "P1 code-reality" family (its rows 7/10-13/16-18) **for the variants tree** and softens 05 §8A's "No production repo implements this register today" — true for production trees, false for the mock tree. D33.5's carrier-designation (OT's canonical tree) is untouched, but the register should note the variants implementation exists as a second reference ahead of the r1 ports — the port now has TWO references to lift from (engine tests + the variants paint layer).
2. **The spec's own honest-mock ledger is the canary for BASE drift** — the preserve-pitch clause went stale within one round because R9-b landed mid-fleet. The ledger needs the standing rule: re-truth at every fleet re-pin (F-13), not only at amendment rounds.
3. **The 09↔18 circular contradiction on Gain dB** (09:337 cites "18 §4.4's Gain dB row" as the display form; 18:189 withdraws the row; the BASE ships the rail) — resolved by F-4 in ONE direction only: the dB rail is law. This is the briefing's fact 4 landing in 18's text.
4. **nle-ui's internal [−60,+4] residual**: AW1-2 raised the INSPECTOR ceiling but ChannelEditor.tsx:91/:99 still carries `max={4}` on the mixer's clip-gain row — the BASE contradicts the fleet domain internally. Not an 18 edit (it's the C40/mixer row's domain), but the amendment wave + the register's mixer row should carry it (the H1-E #7/#8 domain-drift pairs already track the class).
5. **The "usable width" phrasing vs the 1280×800 floor** (OQ-4): the W1 ladder law operates below the spec's floor because the mock serves the review canvas. F-2c deliberately says "usable width" and leaves the floor question OPEN — the registered posture (mock = registered deviation), consistent with mock-verdicts' recommendation.
6. **The A1 landing validates the mock-verdicts convergence direction** (their §3.2 rule 5): the spec absorbs first, the mock implements the amended rows — W1/W2 are the mock implementing DESIGN-R25's A1 nearly verbatim. The remaining forward registrations (A2's TAB strip, A3's wheels rewrite, W4-W6) should follow the same one-amendment-two-phases pattern F-2 uses.

---

## §4 Cross-spec notes (for the cross-cut wave; no edits proposed outside 18)

- **05:** §8A's census sentence + mock-trim's code-reality verdicts need the W2 re-read (§3.1 above); 18 §5A's layering ruling is CONFIRMED by W2's implementation — no 05-side change implied by 18. 18:328's own admission that "the report's `05:972` line anchor is stale" is noted — 05's current §14.11 anchor is 05:992 (verified).
- **08:** the color-page tokens' home (tokens.css:316-338) + the A3 wheels exception (F-9b) + 18:220's Color one-liner (F-2 of mock-variants row 7, folded into my §4.8 verdict) — all ride 08's W-B row amendments; the A2 TAB supersession registration is mock-variants §3 item 5's work.
- **09:** the stills 5s pseudo-duration default + the C46 gate note (F-2b) are 09-side companions (mock-verdicts V1); the absent≡true preservePitch ruling is 09's this-round draft (per the W3 batch-A commit). 09:337's B2 cross-ref resolves once F-4 lands.
- **16:** §3.2's FX-tool note expects 18 §4.5 to own the seventh member — F-11 satisfies it from 18's side; the enum-grows-or-UI-extension decision is 15/16's r1 call. Optional 16 §3.1 rider: "JKL drive the source transport in source mode" (V4's one-liner).
- **15:** the 78-count holds (:285); the replace verb (78→79) is r1-scheduled — 18 needs no edit. 18's §0/§13 census cells join the corpus-wide 28+3 re-declaration (F-13).
- **12/17:** the variants census re-keys at the sibling's wrap only (1780 green at W2 per their gates; the briefing's 1,762 was the W1 re-read — both recorded, neither chased; the coordination law).
- **00:** the AR-2 standing law (§2A.10 draft): 18's §0 K3 row consumes the exception registry — once law, 18:25's "documented OT fact cited at the runner's location" framing inherits the exported-`WIRE_UI_EXCEPTIONS` correction (spec-05's finding, cross-noted).
- **20:** the mixer-family register row + §7's M2 pointer carry nle-ui's audio page + the [−60,+4] ChannelEditor residual (§3.4 above).

---

## §5 Summary for the orchestrator

Spec 18 is structurally SOUND (layout/state/a11y/testing contracts all verified live) but sits **one mock-round behind its own references**: the R25-W1/W2 landings flipped four mock-verdicts rows from "registered forward" to "landed code" that 18 contradicts outright. **7 P1s**: §4.8:222's "in place of the compact ruler's head row" (W4 coexistence + flags + 54px stack, TimelineCompact.tsx:170-202); §4.3's `<video>` fallback + "stills keep the honest static band" + the missing priority ladder (W1's poster transport, SOURCE_STILL_PSEUDO_DUR 5s, strip-dimming-never-image, priority-not-overflow); §4.4:189's "'Gain dB' is withdrawn" (the BASE's live [−60,+20] rail; 09:337 cross-cites the row 18 deleted — the circular fix is F-4); §8.8's mixer "Removed" vs the BASE's Audio page + mixer family (the 3-vs-5 OPEN decision is really 3-vs-4-vs-5 — three-way); §9:407's "(mock playhead gold)" (every reference paints #fa1024 red — time ≠ state); §0's preserve-pitch "mock-local" ledger clause (R9-b made it real). **~13 P2s** headline: §4.11's missing A1 routing canon (the highest-value add — replace-never-stack, three doors, 1ms refusal, all landed Clip.tsx:179-321), §5's missing ripple/slip/slide tool rows (the normative Tier-3 contract; nle-ui's radio still dead at Clip.tsx:524/:536), §5A's directional-cursor ladder + the A3 wheels dbl-click exception, §4.4's refresh law, §4.5's FX tool, §4.7's waveform render gate, the §9 house-token map, the 28+3 wire-census cells, the stale pin family. 14 exact edits drafted (F-1..F-14); cross-spec notes recorded for 05/08/09/15/16/20/00. No settled law re-litigated; the D35.4 OPEN posture and the r1 sequencing laws stand.

*Report-only: no file outside this report + the worklog was modified; no commits; stayed on main.*
