# R27-W3-12+13 — spec 12 (testing strategy) + spec 13 (sub-agent scout plan) — the combined per-spec audit

**Task ID:** R27-W3-12+13 · **Agent:** spec-12-13 · **Round:** R27 final-tightness, Wave 3 · **Date:** 2026-09-13
**Ground truth (live-verified this audit, ⭐ = re-derived by direct read/run):** engine `f9ac806` **748/748** (25 vitest test files ⭐, bridge-seams 151, nle-bridge 99; vendor pins OT `6e2b91a` + WDC `ec8fd5c` ⭐ via `git submodule status`); OT HEAD `55c81c0` / code pin `970948a`, **632/632 ⭐** (report-json: total 632/passed 632/failed 0 ⭐; 72 entries = 452 in-page + 30 REAL-DragEvent/keyboard + 150 REAL-mouse; **`WIRE_COMMAND_TYPES` = 31 verbs @ headless/api.ts:243-275 ⭐, `WIRE_UI_EXCEPTIONS` exported @ :328-335 ⭐**; M49C 28/3; the gate counts ONLY `origin:"ui"` dispatches — run-timeline-tests.mjs:59-64 ⭐); WDC `ec8fd5c` **777/777** (19 `src/test/` files ⭐, 34 total); nle-ui `32abd58` **690/690** (37 test files ⭐ — count unchanged); app `c020b2a` **252/252 ⭐** (8 suites ⭐: GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9; census 42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ `6e2b91a`); mini **441 it-blocks / 10 files ⭐ (live-current)**, 149 census units / 33 HOLDS / 408 authored / 136 GAP ⭐ (LAW-NET-INVENTORY §2.3), R24-2 still in flight; variants **64 test files ⭐ / 1,796 it-blocks live ⭐** (register declares 1,773/64; the sibling's waves still moving). **battery_r26 re-run live: 135/141 ⭐** (the 6 fails: the 5 HEAD/consumer pin-lags + the variants it-census). battery_r27 not yet built.

**Method:** both specs read in full at sub-spec depth (12 = 2,831 L; 13 = 943 L); every load-bearing claim re-derived against the live trees (the repos at /home/z/my-project/*); the five W1 scouts + scout-app/scout-ot/scout-engine/scout-wdc/scout-nle-ui + mock-verdicts + mock-leverage + spec-19/spec-16/spec-00 reports consumed for the module narratives (their claims spot-verified, not re-derived); SKILL.md laws #106-#145 read for the 13-side process audit. Research-only: no spec/repo file touched outside this report + the worklog.

**The headline (both specs):** 12's LAW layer (the tier mechanics §3-§11, the meta-tests, the D24 K-ladder shape) is SOUND — every failure is in the REGISTRATION layer: the whole spec is pinned at R24 (two fleet rounds + three battery generations behind), the K2/K3 instrument rows predate D-ARCH-6/AR-2/R25's mode-matrix growth, and the battery-posture row still names battery_r24. 13 is the corpus's only process-law record and its history **stops at R24** — three entire fleet rounds (R25's 33-agent mode fleet, R26's execution round, R27's 55+-dispatch final-tightness grid) and the process laws they proved (the briefing-hypothesis law, the report-json count authority, the multi-pass wave law, the cross-repo filing protocol) are recorded nowhere in it. Neither spec carries a law-text P1; both carry freshness/registration P1s, all mechanically fixable.

---

## §1 SPEC 12 — the per-section verdict table

| # | Section (line) | Verdict | Evidence + the gap |
|---|---|---|---|
| 1 | Status header :4 | **P1** | The whole R24 pin block: "engine 458 / OT 536 / WDC 759 / nle-ui 674 / app 174 / mini 355 / variants 1521+; the wire census **24 routed + 6 exceptions** is the census both key on" — ALL superseded (748/632/777/690/252/441-in-flight/1,773+; **31 = 28 routed + 3 exceptions** per headless/api.ts:243-275/:328-335 ⭐, briefing fact #1). "mini 355" is doubly stale (441 now ⭐). The K2/K3 instrument registration phrase predates AR-1's exported registry. |
| 2 | §0 BASE fleet row :14 | **P1** | The full re-pin family, every sub-row: engine "13 vitest suites" + the 11+2 name listing → **25 test files ⭐** (missing 12: m2-wave1-mixer-surface, upstream-ports, timeline-linked-source-edit, timeline-locked-track, timeline-mode-clamps, timeline-nan-guard, timeline-relink-fixes, compositor-clear, mask-manager-view, audio-codec-ladder, media-registry, stable-video-grouping); OT "386 in-page + 150 real-mouse across 59 milestone entries… 18 appended real-mouse entries" → 452+30+150 / **72 entries** (the split convention changed shape at R26/R27 — scout-ot #139c recommends citing the total only); WDC "18 `src/test/` suites" → **19** ⭐ (34 total; "18" was already stale at R26 — scout-wdc #88); nle-ui "674 @ fc4cc35" → 690 @ 32abd58 (37 test files still EXACT ⭐); app "**6 suites**: engineSeam / sceneBridge / GluedShell / audioService + persistenceService / deliverService" → **8 suites ⭐** (wire-coverage 9 + waveformPeaks 9 both exist — the "6-suite roof" is two suites behind; scout-app #109); all consumer pins stale (app→engine `5036387`→`f9ac806`; app OT lock-copy `c15a629`→`6e2b91a`; engine OT submodule `c15a629`→`6e2b91a` ⭐; engine WDC `494f6ff`→`ec8fd5c`; nle-ui consumed `fc4cc35`→app vendored `83ff8a8` with AW1-2 absorb queued); variants "1521+ (**61 test files**)" → **64 files ⭐ / 1,773 registered / 1,796 live** (the mock-variants finding — see §6-U1); mini 441/10 — numbers LIVE-CURRENT ⭐ but framed as settled while R24-2 is in flight (briefing: "more waves coming"). |
| 3 | §0 seal artifacts :15 | **P3** | The LAW-NET census decomposition (149 units / 10 files / 441 tests; 33 HOLDS / 408 app-side authoring / 136 GAP) re-verified **EXACT** against the live LAW-NET-INVENTORY §2.3 ⭐. Only missing: the in-flight marker + the re-key rule for the mini (the sibling's R24-2 waves keep moving the corpus; the register re-keys at their WRAP — mock-verdicts §3.1/§3.2). |
| 4 | §0 K1 row :18 | **P2** | "ALL GREEN at the R24 pins — OT 489→536, engine 440→458, nle-ui 648→674, app 117→174" → the R27 deltas: engine 458→**748**, OT 536→**632**, WDC 759→**777**, nle-ui 674→**690**, app 174→**252** (all green ⭐-verified per repo). |
| 5 | §0 K2 row :19 | **P1** | Three failures: (a) "M49C… **(already green, 24/24)**" → **28 routed + 3 exceptions**; (b) "the wire census 24 routed verbs + 6 documented exceptions (`WIRE_COMMAND_TYPES` @ OT `api.ts` **+ the runner's exception registry**)" — the "runner's exception registry" reading is **FALSE since `40f22af` (AR-1/D-ARCH-3)**: the registry is a first-class **exported** tsc-asserted `Record` (headless/api.ts:328-335 ⭐) read LIVE by both gates; (c) the row misses the **origin-attribution clause** (HA-4-2: the gate counts ONLY `origin:"ui"` dispatches — test-handle dispatches never count; run-timeline-tests.mjs:59-64 ⭐) — the exact law the app's ported gate keys on. Plus the missing R25 growth (→ row 10). |
| 6 | §0 K3 row :20 | **P1** | The D30 W-F coverage-gate port "(D29)… the REGISTERED K3 instrument" — the port **LANDED at R25** (the wire-coverage suite, 9 tests, in the 252 ⭐) and 12 never flipped the row from registered-in-flight to landed; missing the **AR-2 live-registry consumption law** (the gate consumes `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel — wire-coverage.test.tsx:29/:49-59; the hand-mirrored copy went stale at exactly the `6e2b91a` re-pin) and the **AR-2 gesture pin** (the REAL volume-line dblclick → `timeline.upsertKeyframe` origin 'ui' — registered NOWHERE in the corpus per scout-app #109). The shell-path exception + the compensating trusted-integrator family remain correct law. |
| 7 | §0 K4 row :21 | SOUND | The e2e remains K4-pending; "the same routed wire verbs" holds under the 28+3 census. |
| 8 | §0 Walk/run row :22 | SOUND | The D24 depth ladder; no fleet facts. |
| 9 | §0 GAP LAW-NET row :25 | SOUND | The LANDED flip + the census decomposition re-verified exact ⭐ (§2.3 live). |
| 10 | §0 GAP K2 census+registration row :26 | **P1** | "Registration state (R24): **still PENDING**" — still true at R27 (K2 entry hasn't happened), but the row is missing the **R25 growth the plan declares**: "the K2 combined-family census + spec 12/17 registration… **now grown by the mode-matrix registration** (§2's K2 gate: the **10-mode matrix in 06/17** + the **D30.3 zero-orphan-rows law**)" (IMPLEMENTATION-PLAN.md:23) — 12's twin row carries neither (the matrix lives in 06 §0 per D30.1; the K2 gate form is the plan :53's "every matrix row LANDED or a live r1-scheduled row with owner + acceptance; ZERO ORPHAN ROWS"). The counts "(458/759/536)" → 748/777/632; "M49C… 24/24" → 28/3; the "24 routed + 6 exceptions" → 28+3. |
| 11 | §0 GAP K3 corpus authoring :27 | SOUND | PENDING is honest (scout-app :79: "322 authored + 33 projection pins… NOT started"); the 322/115 counts are the R23-seal census decomposition the live inventory still declares ⭐. |
| 12 | §0 GAP K3 audio pins :28 | **P2** | "**hard prerequisite: S-wdc's waveform seam contract**" — the prerequisite **LANDED at R26** (`WAVEFORM_PEAKS_BUCKETS` + `WaveformPeaks` exported, the 14-pin contract test green inside the 777; the app's `waveformPeaks` suite consumes it). The row should record the prerequisite as met, not open. |
| 13 | §0 GAP VLM net :29 / K4 e2e :30 / r2-r5 :31-34 | SOUND | All still-pending rows; no false claims. |
| 14 | §0 ACCEPTANCE & TEST PLAN :36 | **P1** | "**battery posture: the R24 suite pins above stay green (the K1 regression role — battery_r24 keys** engine `5036387`/458, OT `ded43c4`/536… WDC `85b81b0`/759, nle-ui `fc4cc35`/674, app `c885ece`/174 + the consumer pins + the routed/exceptions coherence check per D29)" — **three battery generations stale**: battery_r25 (123 checks) → **battery_r26 (141 checks, live-run this audit: 135/141 — the 6 fails pure pin-lag) ⭐** → battery_r27 (built this round). 12 assigns the battery exactly three roles today (the central once-per-round pin re-baseline; the K1 regression pins; "battery-checked thereafter" for the K2 family) — all still right as LAW but the keying row must move to battery_r27 + the new check classes (see §6-U3 + F-6). "17 §13A.7 re-tier row to the R24 pins" → the R27 pins. |
| 15 | §1 Purpose / §2 Goals :40-54 | SOUND | Stream definition unchanged. |
| 16 | §3 Test Infrastructure :57-187 (stack, framebuffer, structure) | SOUND | The mechanics layer; §3.2's lavapipe/SwiftShader dual-flavor note (:102) still the operative law (engine validated both). |
| 17 | §4-§7 mechanics :189-583 (assets, pixel, audio, WYSIWYG) | SOUND | §5.3's linear-light math, §6.4's varispeed law, §7's WYSIWYG invariant re-verify as written; the fixture registrations (17 §5.3 round-trip) intact. |
| 18 | §8 property-based :585-692 | SOUND | The invariant catalogue; model-based extension at §12 Q5. |
| 19 | §9-§10 matrix + perf :694-813 | SOUND | The smoke/full/nightly budgets; no fleet facts. |
| 20 | §11 CI Configuration :815-903 | SOUND | Config templates (actions@v7 verified at SCOUT-12 era). |
| 21 | §12 Open Questions :906-1383 | SOUND | Historical research records (Q1-Q12, resolved + dated). |
| 22 | §13 Code References :1384-1509 | **P2** | §13.1's context rows ✓ (00/18/19 all exist); §13.7's R24 re-base annotation :1493 is stale on every figure: "13 suites, **458/458 @ `5036387`**… bridge-seams now 113… the engine's OT submodule re-pinned `a4e971d`→`c15a629`" → 25 test files / **748/748 @ `f9ac806`** / bridge-seams **151** / OT submodule **`6e2b91a`** ⭐. The annotation chain (R23→R24) needs its R27 entry. |
| 23 | §14 Corrections :1512-1632 | SOUND | Historical corrections (14.A-14.G); all stand. |
| 24 | §15-§16 scripts + reference-render :1633-1976 | SOUND | Verified ffmpeg command set; no fleet facts. |
| 25 | §17 CI configs :1977-2401 | SOUND | 17.1-17.5 templates; no fleet facts. |
| 26 | §18 meta plan + `## Testing` :2404-2829 | SOUND | The meta-test catalogue (canary/negative/self-test/flaky-detector) is 12's unique contribution and needs no re-key; the "17 §9.3 CI ordering" (:2432) and "17 §11.3 manifest" (:2448) refs both resolve ✓ (17:1726 / 17:2020). |

## §2 SPEC 13 — the per-section verdict table

| # | Section (line) | Verdict | Evidence + the gap |
|---|---|---|---|
| 1 | Round-7 note :3 | SOUND | The /download/nle-spec/ paths correctly labeled historical; `audits/ROUND-7-AUDITS.md` exists ✓. |
| 2 | Status header :4 | **P1** | "v-next (**Round 24** — the R24 re-baseline: the scout protocol is the operating practice, executing per round…)" — the recorded process history **stops at R24**. Three fleet rounds are missing entirely: **R25** (the edit-mode completeness round — 33 sub-agents: 5 module scouts → 10 one-per-mode agents → 5 cross-cuts → 2 adversarial reviewers (15+11 findings, folded to ARCH-R25 v2) → 6 amendment agents → 5 verification agents + 3 cross-repo filings + the integration review + battery_r25 123 checks; `audits/fleet-r25/` = 27 reports ✓); **R26** (the execution round — the three R25 filings EXECUTED at their owners, each adversarially reviewed (PASS), battery_r26 141 checks; note: **no `fleet-r26/` directory** — the round's shape was execute+review, not a per-file fleet — itself a process fact worth recording); **R27** (the final-tightness round — the 55+-dispatch grid: W1 5 scouts → W2 6 mock analysts → W2b the briefing fold → W3 18 per-spec → W4 5 cross-cuts → W5 3-4 adversarial → W6 6-10 amendments + battery_r27; ARCH-R27 §3 ✓). The current protocol facts absent as law: the briefing-hypothesis law, the report-json count authority, the multi-pass wave law, the cross-repo filing protocol (see §6-U6). |
| 3 | §0 BASE protocol row :12 | **P2** | The R23/R24 fleet description is accurate as history (the 20-agent per-file fleet ✓, the module-card wave ✓ `audits/fleet-r24/module-*.report.md` exists, the track/plan audit ✓, the ×17 adversarial fold ✓) — but "the R24 execution wave is its newest form" is three rounds false. The evidence-citation / census-counting / split rules (:13) all still hold and GENERALIZED at R25+ (the absence-proof discipline, SKILL #131). |
| 4 | §0 BASE downstream-product pins :14 | **P1** | The full R24 pin block (engine `5036387` 458 / OT `ded43c4` 536 / WDC `85b81b0` 759 / nle-ui `fc4cc35` 674 / app `c885ece` 174 "39 mirrors (32 zero-action + 7 carriers)" / mini 441 / variants 1,521+) — every figure superseded (see §1 row 2; the app census arithmetic is now **42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ `6e2b91a`**). The R22/R23 lineage tails stand as lineage (arrow-adjacent exempt, SKILL #126). |
| 5 | §0 GAP no-standing-gap ruling :17 | SOUND (P3) | "re-verified at the R23 and R24 fleet audits" — R25/R26/R27 each exercised the protocol without adding a standing gap; "battery_r24, S-spec's row" → the battery lineage (r25 123 → r26 141 → r27). |
| 6 | §0 ACCEPTANCE :19 | **P2** | "battery posture (**battery_r24**, the successor): the pins above stay green, the live suites SCRAPED not substring-matched, declared == actual" — the LAW holds (and was proven again: the strict-scrape pattern, the report-json authority) but the keying is two generations stale; the dual-vocabulary window note is correctly closed (D24 set only ✓). |
| 7 | §1 Purpose :23-25 | SOUND | Correctly scoped to the Rounds-1-6 record ("refine the seed specs (files 01 through 12)"). |
| 8 | §2 Stream Breakdown :29-53 | **P2** | The 12-stream table + "**Total: 12 streams. Recommended parallelism: 3-4 scouts at a time**" — accurate as the R1-6 record, but §0 declares the protocol "IS the operating practice" while the operating grids live only in the ARCH-R2x docs: the modern shape is 20-21 per-file agents dispatched in waves of ~5 with **disjoint file ownership** (SKILL #120), R25's 33 agents, R27's 55+ dispatches. 13 records none of the modern grids (the task's "fleet grids vs the current practice" finding). |
| 9 | §3 Scout Workflow :57-97 | SOUND | The historical 3-phase record (scout → audit → integration review) — the seed of the modern multi-pass form; no false claims (the modern extension is §0's job, not §3's — §3 is dated prose by design). |
| 10 | §4 Per-Stream Prompts :100-824 | SOUND | The 12 historical prompts, self-contained as designed; the /tmp reference-repo paths correctly historical (the repos are gone — spec 06 + the engine are the reference pair now, per mock-leverage §5.4). |
| 11 | §5 Parallelism Strategy :828-881 | **P2** | §5.2's audit-pass + §5.3's integration-review checklist are the R1-6 form; the modern additions (the adversarial-review-fold cycle; the verification pass; the orchestrator's integration round as a budgeted non-optional step — SKILL #114 "per-file agents cannot see cross-file contradictions"; the cross-repo filing wave) are unrecorded. Same fix family as row 8. |
| 12 | §6 Deliverables :884-910 | SOUND | The file inventory + the audits/*.audit.md naming ✓ (all 12 audit files exist). |
| 13 | §7 Worklog Protocol :914-940 | SOUND | The template IS the live practice (this audit's entry follows it; the worklog carries 41 Task IDs / 86 entries ⭐ — every dispatched round left its trail, the acceptance test passing). |
| 14 | End pointer :943 | SOUND | "Next: 14-implementation-phases.md (retired at R23 — the redirect stub; THE plan is `IMPLEMENTATION-PLAN.md` per D23)" ✓ both verified. |

---

## §3 THE FIX-LIST — spec 12 (exact edits; F-1..F-8)

**F-1 (P1, Status :4) — the R27 re-pin block.** Replace the Round-24 status parenthetical's pin tail with: "Round 27 — the fleet re-pin + the instrument re-key: engine **`f9ac806`** 748/748 (25 test files; bridge-seams 151) / OT HEAD **`55c81c0`** (code pin **`970948a`**) 632/632 / WDC **`ec8fd5c`** 777/777 / nle-ui **`32abd58`** 690/690 / app **`c020b2a`** 252/252 (8-suite roof) / mini 441 (R24-2 in flight) / variants 1,773 registered (64 files; 1,796+ live, in flight — re-keys at the sibling's WRAP); the wire census is now **31 verbs = 28 routed + 3 exceptions** (D-ARCH-6 `970948a`; the exported registry consumed LIVE by both gates); battery_r26 141 checks (135/141 at the R27 pins — pure pin-lag) → battery_r27 this round; the K2/K3 instruments re-keyed (M49C 28/3, origin-attributed; the W-F port LANDED R25 with the AR-2 live-registry consumption + gesture pin)." Keep the R22/R23/R24 lineage tails as lineage.

**F-2 (P1, §0 BASE :14) — the full fleet row re-write.** Per sub-row: engine "748/748 @ `f9ac806` (code anchor `50b91f5`) — **25 vitest test files** (the 13 R24 suites + the R25-R27 growth: timeline-linked-source-edit, timeline-locked-track, timeline-mode-clamps, timeline-nan-guard, timeline-relink-fixes, upstream-ports, m2-wave1-mixer-surface, compositor-clear, mask-manager-view, audio-codec-ladder, media-registry, stable-video-grouping; bridge-seams 151, nle-bridge 99) — the app consumes @ `f9ac806`; the engine's OT submodule @ **`6e2b91a`** + WDC @ **`ec8fd5c`**"; OT "**632/632 @ `55c81c0` (code pin `970948a`)** — 72 report entries = 452 in-page + 30 REAL-DragEvent/keyboard + 150 real-mouse (cite the total; `download/timeline-test-report.json` the count authority); the app consumes the vendored lock-copy @ **`6e2b91a`** (census 42 = 36 zero-action + 5 carriers + host, register-equality CI-enforced)"; WDC "777/777 @ `ec8fd5c` (the S-series upstream wave) — **19 `src/test/` files, 34 total** (the waveform-contract promotion made src/test 19 at R26)"; nle-ui "690 @ `32abd58` — 37 test files (the app vendored @ `83ff8a8`, AW1-2 absorb queued)"; app "252/252 @ `c020b2a` — **8 suites: GluedShell 104 / audioService 42 / sceneBridge 41 / deliverService 20 / persistenceService 20 / wire-coverage 9 / engineSeam 7 / waveformPeaks 9** (the R8 D29 real-wiring + R9/D30 W-A/W-F waves + the RA/RC rounds)"; mini "**441 (10 test files / 149 census units) — live-current at the R27 read; the sibling's R24-2 waves in flight (W0-W3 landed: 357→377→396→415→428→441; more waves coming — the register re-keys at their WRAP, per-wave movements recorded not chased)**"; variants "**1,773 it-blocks / 64 test files registered (1,796 live at this audit's read — in flight, re-keys at the sibling's WRAP)** (the R24-variants full-audit round W0-W5d + the R25-W1 insert rescue; supersedes 61 files / 1,521+)".

**F-3 (P1, K1 :18) —** the growth sentence: "ALL GREEN at the R27 pins — the K1 net grew engine 458→748 / OT 536→632 / WDC 759→777 / nle-ui 674→690 / app 174→252 across R25-R27 (the E1/E2 fix + FW/OV/RC/M2 waves at the engine; D-ARCH-6 + session-16 at OT; the S-series at WDC; R9-b/F4/FW-D/AW1-2 at nle-ui; the RA/RC rounds at the app)."

**F-4 (P1, K2 :19) —** rewrite the D29-instrument sentence: "OT's M49C coverage gate — the machine-checked complete-UI claim, accumulated ⊇ `WIRE_COMMAND_TYPES` − exceptions, **origin-attributed (ONLY `origin:"ui"` dispatches count — HA-4-2; test-handle dispatches never count)**, node-side accumulation across the phases, tsc-lockstep both directions — is K2's OT-side completeness instrument (**green, 28 routed + 3 exceptions**); the wire census **31 verbs = 28 routed + 3 exceptions** (`WIRE_COMMAND_TYPES` @ OT `headless/api.ts:243-275` + the **EXPORTED** `WIRE_UI_EXCEPTIONS` registry @ `:328-335`, an AR-1/D-ARCH-3 tsc-asserted export consumed LIVE by both gates — never a hand-mirrored copy, the AR-2 consumption law) is the census the K2/K3 families key on." 

**F-5 (P1, K3 :20) —** append to the W-F sentence: "**(LANDED R25** — the app's `wire-coverage` suite, 9 tests in the 252; the gate consumes the LIVE `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel — the hand-mirrored copy went stale at the `6e2b91a` re-pin, the live-registry consumption law — and carries the AR-2 gesture pin: the REAL volume-line double-click → `timeline.upsertKeyframe` origin 'ui'**)**."

**F-6 (P1, ACCEPTANCE :36) —** the battery-posture re-write: "battery posture: **the R27 suite pins above stay green** (the K1 regression role — **battery_r27** keys engine `f9ac806`/748, OT `55c81c0`/`970948a`/632, WDC `ec8fd5c`/777, nle-ui `32abd58`/690, app `c020b2a`/252 + the consumer pins + the census-coherence check re-keyed 24+6 → **31 = 28+3** (lineage: 24→28→30→24+6→25+5→28+3) per D29/D-ARCH-6; the new classes per this round: the **registry-export check** (the AR-2 live-registry consumption law — a consumer gate consumes the exported registry, never a copy), the **line-pin re-base map as ONE consolidated battery-able input** (spec-19's §2 — the engine timeline.ts family + the OT headless family + the count families), the app census arithmetic 42 = 36+5+1 @ `6e2b91a`, and the origin-attribution clause on the M49C/W-F instrument checks)".

**F-7 (P1, K2 GAP row :26) —** the R25 growth + the re-key: after "close the missing cross-module pairs" insert "; **the mode-matrix registration rides the same K2 entry (D30.1/D30.3)**: the 10-mode edit-matrix (registered in spec 06 §0 + 17's K2 gate row) joins the census the family keys on, and the K2 gate's form is the **zero-orphan-rows law** — every matrix row either LANDED (ported + pinned) or carrying a live r1-scheduled row with owner + acceptance (the completeness is over the REGISTER, not over landed-ness)"; and re-key the parenthetical counts "(458/759/536)" → "(748/777/632)" + "M49C… 24/24" → "28/3" + "24 routed + 6 exceptions" → "28 routed + 3 exceptions".

**F-8 (P2, K3 audio pins :28) —** flip the prerequisite: "hard prerequisite: S-wdc's waveform seam contract — **MET at R26** (`waveform-contract.ts` exporting `WAVEFORM_PEAKS_BUCKETS`/`WaveformPeaks`, the 14-pin contract test; the app's `waveformPeaks` suite consumes the exported names)". **(P2, §13.7 :1493)** — append the R27 annotation: "**R27 re-base:** 25 test files, 748/748 @ `f9ac806` (code anchor `50b91f5`; bridge-seams 151, nle-bridge 99, upstream-ports 25, m2-wave1 9); the OT submodule @ `6e2b91a` + WDC @ `ec8fd5c`."

## §4 THE FIX-LIST — spec 13 (exact edits; F-9..F-13)

**F-9 (P1, Status :4) — the R25/R26/R27 process-history entry.** Prepend to the status parenthetical: "Round 27 — the final-tightness fleet: the protocol's largest execution — **W1 module scouts (5) → W2 mock analysts (6) → W2b the briefing fold (the BRIEFING.md pattern: the fleet's facts distilled once, consumed by every per-spec agent) → W3 per-spec (18-21 agents, one per file) → W4 cross-cuts (5) → W5 adversarial review (3-4) → W6 amendments + battery_r27** — 55+ dispatches (`audits/fleet-r27/`); the coordination law with the sibling mock stream recorded (the register re-keys at their WRAP; per-wave movements recorded, not chased). Round 26 — the execution round: the R25 cross-repo filings EXECUTED at their owners (the E1/E2 fixes, the waveform promotion, the census-CI mechanism), each adversarially reviewed by a fresh-context agent, folded + re-pinned; battery_r26 (141 checks); no per-file fleet — the round's shape was execute + review. Round 25 — the mode fleet: 33 sub-agents (5 scouts → 10 one-per-mode agents → 5 cross-cuts → 2 adversarial reviewers → 6 amendment agents → 5 verification agents + 3 cross-repo filings + the integration review); battery_r25 (123 checks)."

**F-10 (P1, §0 BASE :14) — the downstream-product pin block.** Same re-key family as 12's F-2 (engine `f9ac806` 748 / OT `55c81c0`+`970948a` 632 / WDC `ec8fd5c` 777 / nle-ui `32abd58` 690 / app `c020b2a` 252 with census "42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ `6e2b91a`, register-equality CI-enforced" / mini 441 in flight / variants 1,773 registered (64 files), live 1,796); keep the R22/R23/R24 lineage tails as lineage.

**F-11 (P2, §0 protocol row :12) — the process-law absorption (what the R25-R27 practice proved, as permanent law).** Append to the BASE row: "**The standing process laws the R25-R27 fleets proved (absorbed from the SKILL learnings + the ARCH rulings): (1) the briefing-hypothesis law — a fleet briefing is a hypothesis; every agent verifies it against code, the code wins, discrepancies flagged (SKILL #107; the R27 BRIEFING.md embodies it); (2) the count-authority law — the artifact's own runner output is the count authority; scrape the report json, never the prose (SKILL #115; OT's `timeline-test-report.json`); (3) the multi-pass wave law — one agent per FEATURE for reference-material audits, the orchestrator reads every artifact BEFORE dispatching, multi-pass = verification + depth (SKILL #130); (4) the integration-round law — per-file agents cannot see cross-file contradictions; the integration/adversarial round is budgeted, not optional (SKILL #114); (5) the cross-repo filing protocol — a spec session FILES titled queue blocks, it never ORDERS another repo's code; FILE → EXECUTE → adversarial review → fold → re-pin, and a filing is a hypothesis the execution session tests first (SKILL #135/#136/#138); (6) the live-scrape battery discipline — declared == actual, scrape-don't-substring, and every check must distinguish 'instrument broken' (FAIL) from 'instrument absent' (visible skip) (SKILL #124).**"

**F-12 (P2, §0 GAP :17 + ACCEPTANCE :19) —** the battery lineage re-key ("battery_r24, S-spec's row" → "battery_r27 (the lineage: r15→r22→r23→r24→r25 123→r26 141→r27; `scripts/battery_r2N.py` the succession)").

**F-13 (P2, §2 :53) —** after "Total: 12 streams." append: "*(The R1-6 record. The modern operating grids — the per-file fleet (20-21 agents in waves of ~5, disjoint file ownership), the module-card wave, the mode fleet, the R27 six-wave grid — are §0's lineage; the parallelism law that survived: parallel waves are safe IF the file sets are disjoint and the orchestrator runs the full gates after the wave (SKILL #120).)*"

---

## §5 CROSS-SPEC NOTES (recorded for the cross-cut wave; not actioned here)

- **X-1 (17, the twin):** 12:14/:18/:36 and 17:4/:20/:24/:37/:2360/:2359/:2730 share the same stale family (the fleet pins, the 6-suite roof, the K2 twin row, the 24+6 census) — one re-key family across both; 17's §13A.7 re-tier row also needs the R27 counts. 17's K3 instrument row (:39) should gain the AR-2 gesture-pin registration (scout-app #109: registered NOWHERE).
- **X-2 (00, the pin canon):** 12:14's "00-master's fleet table is the pin canon, D21" — 00's v11.0 rows carry the same R26-stale pins (spec-00's report P1s); the W6 wave re-pins 00 first, then the siblings cite.
- **X-3 (19, the reference authority):** 12's §13.7 annotation chain and 19's own register layer share the re-base (spec-19's F-1/F-5); the consolidated line-pin re-base map (19 §2) is the single input 12's battery-posture row (F-6) should cite.
- **X-4 (15, the wire census):** 12's 24+6 → 28+3 re-key rides 15's census re-base (scout-ot #1/#3/#5); the AR-2 consumption law's proposed 00 §2A.10 standing-law home (spec-00's report) — 12/17's instrument rows point at it once it lands.
- **X-5 (plan):** the K2 registration growth (F-7) mirrors the plan's S-spec row (:23) and §2 K2 gate (:53) — the plan's "still PENDING, now grown by the mode-matrix registration" is the authority 12's twin row must match; the plan's own pin block (:42) re-keys in the same wave.
- **X-6 (06/16):** the 10-mode matrix registration (12's F-7) points at 06 §0's matrix (landed R25, D30.1) — no 06 edit needed; 16's "M49C 24/24" family re-keys with 12's F-4.
- **X-7 (the coordination law):** the mock-verdicts §3.2 six rules + the suite-count re-key discipline (register :13) are the standing law for 12's variants/mini rows — the W4 xcut-register agent owns the re-key at the sibling's WRAP; 12's F-2 text pre-registers the rule.

## §6 WHAT 12/13 UNIQUELY SEE

- **U1 (12 — the variants census, the mock-verdicts finding):** 12:14's "**61 test files**" is the corpus's stale variant file-count (17:4 carries the twin); the live tree reads **64 files / 1,796 it-blocks ⭐** (register declares 1,773/64; ARCH-R27 opened at 1,734; the sibling's waves keep moving it). The coordination law (mock-verdicts §3): record the movement (1,521+ → 1,588 R25 → 1,613/61 R26 → 1,733/64 W2 → 1,773 registered → 1,796 live), **re-key ONLY at the sibling's round-WRAP** — never mid-flight; the declared AND scraped figures both recorded (the census-method divergence: their method vs the line-start method differ by ~1).
- **U2 (12 — the mini is live-current by accident of timing):** 12's mini row (441/10/149; 33/408/136) re-verified EXACT against the live LAW-NET-INVENTORY §2.3 ⭐ — the one fleet row that needs no number edit; only the in-flight marker + the re-key rule (the sibling's R24-2 waves landed W0-W3 through 2026-09-13 and continue). 12 is the only spec that carries the census decomposition at all.
- **U3 (12 — the battery law's home):** 12 is the natural registration home for the battery's role/classes (the posture row :36) — today it names battery_r24 with three roles (the central re-baseline, the K1 pins, the post-registration "battery-checked thereafter"). The R27 growth it must register: the census-coherence re-key (28+3, with the full lineage tail), the registry-export law, the line-pin re-base map as ONE consolidated battery-able input (spec-19 §2), the app census arithmetic, the origin-attribution clause — the exact input battery_r27's build consumes (F-6).
- **U4 (12 — the instrument rows are 12/17's twin charge):** the M49C/W-F instrument registrations are carried ONLY by 12 §0 and 17 §0A — both predate D-ARCH-6 (28/3), AR-1 (the exported registry), AR-2 (the live-registry consumption + the gesture pin), and the origin-attribution law (HA-4-2). No other spec sees this row; if 12/17 don't re-key it, the corpus's only instrument registration is three laws behind.
- **U5 (12 — the mode-matrix registration gap):** 12's K2 twin row predates D30 entirely — the plan (:23) grew the K2 registration duty at R25 ("now grown by the mode-matrix registration… the 10-mode matrix in 06/17 + the D30.3 zero-orphan-rows law") and neither 12 nor 17 absorbed the growth. K2 entry is still pending, so nothing is false YET — but the registration home itself is missing the row it will need to register.
- **U6 (13 — the process-history monopoly):** 13 is the ONLY spec that records the fleet protocol as law — and its record stops at R24 while the practice ran three more rounds (R25: 33 agents/5 waves; R26: execute+review, NO fleet directory — a shape 13 never names; R27: 55+ dispatches/6 waves + the briefing fold). The six process laws the R25-R27 practice proved (F-11) live only in SKILL.md + the ARCH docs — outside the spec corpus. 13's absorption is the difference between the corpus's process law being findable in the corpus vs. tribal.
- **U7 (13 — the worklog protocol is the proven invariant):** 41 Task IDs / 86 entries ⭐, every dispatched round leaving its trail before the phase's code work — 13's §7 acceptance test has now passed ~15 consecutive rounds; the one thing in either spec that needs NO edit.
- **U8 (both — zero law-text P1s):** every P1 in both specs is a freshness/registration failure (pins, counts, instrument keys, process history) — mechanically fixable by the amendment wave; no settled D-law is contradicted, and 12's mechanics layer (the meta-tests, the tier structure) + 13's historical record both re-verify clean.

---

*Research-only agent: no files modified outside this report + the worklog; no commits, no branches; stayed on main.*
