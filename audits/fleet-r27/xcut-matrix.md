# xcut-matrix — R27 W4: the ten-mode matrix re-derivation (Decision 30.1's census, the R27 fleet pass)

**Task ID:** R27-W4-MATRIX · **Agent:** xcut-matrix · **Round:** R27 final-tightness, Wave 4 (cross-cuts) · **Date:** 2026-09-13.
**Method:** every cell re-derived by direct code reads at the R27 pins (not from the reports): engine `f9ac806` (src anchor `50b91f5`; timeline.ts **8,997 LOC live-counted**; 748/748; the 19-op headless surface grep-verified — zero ten-mode arms), OT HEAD `55c81c0` / code pin `970948a` (632/632; `WIRE_COMMAND_TYPES` **31 verbs read from the array** api.ts:243-275 = 24 `timeline.*` + 7 `track.*`; `WIRE_UI_EXCEPTIONS` :328-335 = 3), app `c020b2a` (252/252; submodules f9ac806/83ff8a8/ec8fd5c + the OT mirror `6e2b91a` — the re-pin queued), nle-ui `32abd58` (690/690), variants in-repo @ **R25-W3 `b08b83a`** (W1 a83d4d2 + W2 685edba landed past the W5d wrap — the brief's "W2-in-flight" is superseded; the mode-relevant waves are W1+W2), mini in-repo (R24-2 mid-flight). Companion evidence: mock-trim/mock-insert/mock-leverage/spec-06/spec-05/spec-15/scout-ot (all re-verified, not re-derived). Spot-verified live this session: 22 cells/anchors (§0.1 below).

---

## §1 The re-derived ten-mode matrix (paste-ready for 06 §0 — replaces :14-29)

**The ten-mode matrix (Decision 30 — the edit-mode completeness census; the R27 fleet pins: engine `f9ac806` (docs-only over code anchor `50b91f5`; timeline.ts 8,997 LOC — every pre-R26 engine line pin in the corpus is stale) / OT code pin `970948a` (HEAD `55c81c0`, docs-only past the code tip; the wire census **31 = 28 routed + 3 exceptions**, D-ARCH-6) / app `c020b2a` / nle-ui `32abd58` / the variants @ R25-W3; the spec column is post-R25-amendment + R27-verified (mock-trim/mock-insert: zero P1 law gaps), the code columns re-read live at the R27 pins):**

| Mode | 06 spec home | 15 wire | engine | OT ops+UI | app | reference |
|---|---|---|---|---|---|---|
| Roll | §5.5 COVERED | `RollCommand` §4.3.5 | `rollingTrimItems` :3324 — complete, UNWIRED (off the 19-op INTERNAL surface; D12 venue-internal twin) | ABSENT (grep-verified `970948a`) | ABSENT | variants LIVE (ops+gesture+grammar: R25-W2's `TrimAffordances.tsx` RollTrimAffordance; the R15-T4 commit path; rollDeltaBounds useUiStore.ts:2114) |
| Ripple trim | §5.2A COVERED (wire/keys only) | `TrimCommand{ripple}` §4.3.2 | `rippleTrimItem` :3132 — complete, UNWIRED | ABSENT (ripple-delete only; `timeline.trim` carries NO ripple param — api.ts:1527-1540, the W11 exception) | nle-ui `⌥[/⌥]` composed-over-patches (shortcutMap.ts:70-71, pinned useShortcuts.test.tsx:275 — alt combos read e.code); the app's rippleMode routes delete only | variants (the only DaVinci-faithful mouse surface; R25-W2's RippleTrimAffordance + the `ripple.ts` interval model) |
| Slip | §5.6 COVERED | `SlipCommand` §4.3.6 | `slip` :4956 — complete, UNWIRED | ABSENT | `,`/`.` engine-true via the router slip verb (app engineService.ts:403 — ONE `updateElements` batch, the app-inline law, unpinned; nle-ui's slipNudge hardcodes 24fps — the registered divergence) | variants R15-T4 keyboard slip (+W2's SlipTrimAffordance: the white source outline + the bright in-point preview) |
| Slide | §5.7 COVERED | `SlideCommand` §4.3.7 | `slideItem` :5097 — complete, UNWIRED | ABSENT | ABSENT | variants (gesture+bounds, the source-capped divergence registered; +W2's SlideTrimAffordance shrink boxes) |
| Insert edit | §5.9 (the two-semantics preamble + the splice law, D31.1/31.2 — R25) | `insert` verb (placement-only; the P11 re-key) | `performInsertEdit` :5630 — **E1 FIXED** (R26 `8188d4e`+`412693d`; the 26-test pin file), UNWIRED | placement-insert only (reject-not-shift, `insertElements` :887) + **`insertBatch` LANDED** (api.ts:1262-1370 — ONE history entry, the shared `validateInsertElement`; the composite carrier) | pool-DnD = single-asset `timeline.insert` (use-timeline-drag-drop.ts:89-133; NO insertBatch consumer — the design filed c020b2a) | variants `insertPlan` :424-468 (the pure planner, preview==commit, the most faithful, honestly-labeled) |
| Overwrite edit | §5.9B (D31.3 — R25) | none | `performOverwriteEdit` :6007 — **E2 FIXED** (R26, the fate-based companion law; pinned in the 26-test file); E3 the silent abort open (rides r1); UNWIRED | ABSENT (the phantom strategy rows retired R25, P2/P6) | ABSENT | variants R20-W2 (`insertPlan` :469-485 — the only full impl) |
| Replace | §5.9C (D31.4 — R25, spec-first) | none (r1: `timeline.replace` — the NEW 79th union member, 15:4918) | ABSENT (no `performReplaceEdit`; the transition law = `joinItems` :8817's id-remap pattern, NOT removeItems' cascade-drop) | ABSENT (drop-on-clip = the deliberate SD-5 no-op) | ABSENT | variants implements the WRONG law (RE-2 — remove+overwrite at the source's own duration; 3× specified, 0× correct) + the reference card's own geometry defect (P3) |
| Append at end | §5.9D (D31.5 — R25, spec-first) | **`insertBatch` LANDED** (D-ARCH-6); the 6th `PlacementStrategy {type:'append'}` + the resolver = the r1 scope (15:4919) | ABSENT by design (the D31A placement-level composite — no engine op) | ABSENT as a mode (2-of-5 strategies exposed, api.ts:1297-1310: firstAvailable \| explicit only; the pool multi-insert = playhead-anchored insertBatch per kind, staggered — view/page.tsx:535-603) | ABSENT | variants `insertPlan` :413-414 + mini (useMini.ts ~:1649-1670 + geometry.ts:50 `contentEnd` — real per-track-end composites; the R25-era :1055 pin drifted) |
| Ripple overwrite | §5.9E (D31.6 — R25, spec-first) | none dedicated (composite over 3 ROUTED verbs: delete+move+insert, ONE `applyBatch` — the order law) | ABSENT (primitives private: `rippleTrimItem` :3132's signed shift + the sync-lock propagations timeline.ts:4218+ / sync-lock.ts:579+) | ABSENT | ABSENT | variants push-only (RO-3 — the pull unreachable; must re-align per mode-ripple-overwrite G-RO-5) |
| Fit to fill | §5.9F (D31.7 — R25, spec-first) | none dedicated (composite over routed verbs: insert + `updateElements{retime}`; first-class verb optional r2) | ABSENT (math only: `calculateSpeed` timeline-math.ts:133 + `rateStretchItem` :3840's fixed-span derive :4076-4110) | ABSENT (math only: `retime.ts:51` the inverse; no verb computes rate from a duration) | ABSENT | variants tested planner branch (`insertPlan` :342-381, 37 pins) — **FF-3 open**: the mock clamps [0.01,5] (:354, trimLaws.ts:32-33) where the law pins [0.1,5] + refuse-never-clamp (06:1668) |

*Decision 30.2: the four absent families (replace / append / ripple-overwrite / fit-to-fill) are r1-scheduled spec-first families — owner + phase + acceptance per their §5.9C-F GAP rows; the landed C7 census is **31 = 28 routed + 3 exceptions** (D-ARCH-6 @ `970948a`; the R25-era "24 routed + 6 exceptions" reading retired) and is NOT reopened — the new family verbs re-declare it mechanically per the tsc-lockstep/M49C law (D29.2) when they land. Decision 30.3 (the ladder gate): at K2 every matrix row is LANDED or carries a live r1-scheduled row with owner + acceptance — zero orphan rows (the completeness is over the REGISTER, not over landed-ness); at K3 the app column matches the LANDED set with pins.*

**The R27 cell-deltas vs the R25 census** (all freshness, zero law changes — the mode law itself re-verified at sub-spec depth by mock-trim/mock-insert): (1) the engine column re-anchored ~1,500 lines down (timeline.ts 7,502→8,997); (2) the insert row's E1 cell and the overwrite row's E2 cell flip open→FIXED (R26); (3) the append row's OT cell: insertBatch LANDED — the pool batch is no longer "applyBatch([insert × N]) with N undo entries" but one-insertBatch-per-kind, staggered, one history entry per kind-group (AP-1's old OT-fact FALSE — spec-06 F-3); (4) the ripple-trim app cell attributed: the `⌥[/⌥]` keys are nle-ui's, the app itself has no Alt bindings (use-keybindings.ts:230 guards altKey out); (5) the reference column gains the R25-W2 conformance wave (TrimAffordances.tsx — the 05 §8A grammar now HAS a rendering implementation in the variants; N-2's OT-side duty unchanged); (6) the census footnote 24+6 → 31=28+3.

### §0.1 The live spot-verification register (22 anchors, this session)

engine@f9ac806: `rollingTrimItems` :3324 · `rippleTrimItem` :3132 · `slip` :4956 · `slideItem` :5097 · `performInsertEdit` :5630 · `performOverwriteEdit` :6007 · `rateStretchItem` :3840 + `rateStretchWithRipple` :7645 · `closeGapAtPosition` :7463 · `removeRangesFromClip` :8235 · `freezeFrameAtPosition` :8652 · `joinItems` :8817 · `calculateSpeed` core/timeline-math.ts:133 · timeline.ts 8,997 LOC · headless api.ts = 19 case arms, zero ten-mode mentions (UNWIRED) · the carried corpus live-counted 26/20/25/14 it-blocks (linked-source-edit/mode-clamps/edit-ops/locked-track). OT@970948a: the 31-verb array read verbatim (24 timeline + 7 track) · the 3-exception Record · the insertBatch arm :1262 · the strategy whitelist :1297-1310 · `insertElements` :887 · the trim arm's no-ripple shape :1527 · the pool multi-insert switch view/page.tsx:535. app@c020b2a: grep-empty for insertBatch across src/ · the DnD single-asset insert path · the slip router verb engineService.ts:403 · the altKey guard use-keybindings.ts:230. nle-ui@32abd58: ToolId includes roll/ripple/slip/slide (useUiStore.ts:16) · the router's slip-only verb (timelineRouter.ts:93) · the `⌥[/⌥]` pins (useShortcuts.test.tsx:275). variants@b08b83a: the insertPlan mode branches :312/:343/:413/:424/:469/:472 + arrows :498 · the fitToFill clamp :354 + trimLaws.ts:32-33 · TrimAffordances.tsx (NEW, 313 lines, W2) · the R25-W3 color wave confirmed (past the mode surface). mini: `contentEnd` geometry.ts:50 + the append target useMini.ts ~:1649-1670.

---

## §2 The D30.3 ladder-gate audit (the register's current state at the R27 pins)

### K2 — the zero-orphan-rows check: **10/10 rows carry live r1-scheduled registrations. ZERO ORPHAN ROWS — the K2 condition holds.**

| Mode | The live scheduled row(s) | Owner | Phase | Freshness verdict |
|---|---|---|---|---|
| Roll | 06 §0 GAP row 1 (r1 wave 1) + §10.4 :3324 PORT-SCHEDULED + the plan's r1 op-depth row | S-ot (the D12.3 home) | r1 | SOUND |
| Ripple trim | §0 GAP row 1 + the S-ot r1 verb list (`trim{ripple}`) + 15 §4.3.2 | S-ot | r1 | SOUND |
| Slip | §0 GAP row 1 + 15 §4.3.6 | S-ot | r1 | SOUND |
| Slide | §0 GAP row 1 + 15 §4.3.7 | S-ot | r1 | SOUND |
| Insert edit | §5.9's two-semantics preamble (r1 per D12.3) + the E1 row (flipped DONE R26 — the precondition MET) | S-ot | r1 | SOUND (the :4702-era line pins stale — F-1) |
| Overwrite | OW-1 (§5.9B) | S-ot | r1 | OW-1 SOUND |
| — | OW-2 (E2) | S-engine → the port | pre-r1 → r1 | **STALE FACT CELL** — the E2 half-flip: the body :1590 says E2 FIXED (R26) while OW-2 :1599 still describes the asymmetry as live work; its acceptance ("a linked A/V overwrite-straddle fixture… pinned engine-side") is now MET by the 26-test file. Needs the F-2c flip; the r1 carry-into-port half stands |
| — | OW-3 (E3) | S-engine + S-ot | pre-r1 (the filing) / r1 (the port) | SOUND — the engine-side filing EXISTS (PLAN :251-252, P3, rides r1); the row can now cite it |
| Replace | RE-1 + RE-2 (§5.9C) | S-engine + S-ot / S-app | r1 / r1-adjacent | SOUND |
| Append | AP-1 + AP-2 (§5.9D) | S-spec-15 + S-ot / S-ot | r1 | **FALSE OT-FACT CELL** — insertBatch LANDED (D-ARCH-6); the pool batch is now the staggered per-kind insertBatch (one history entry per kind-group), NOT "N undo entries". The r1 work (the 6th strategy + the resolver) STANDS. Needs the F-3 rewrite; "rides insertBatch's endorsed slot" → "rides the LANDED insertBatch" |
| Ripple overwrite | RO-1 + RO-2 + RO-3 (§5.9E) | S-ot + S-spec / S-engine + S-ot / S-app | r1 / r1 / crawl | SOUND |
| Fit to fill | FF-1 + FF-2 (§5.9F) | S-ot / S-spec | r1 (riding the wave-1 rateStretch row) | SOUND — **FF-3 UNREGISTERED** (mock-insert's new P2: the variants' [0.01,5] clamp domain vs the law's [0.1,5] refuse-domain); needs the F-8 row |

**K2 verdict:** the register is complete — every row resolves to a live scheduled row with owner + acceptance, not prose (the ARCH-R25 30.3 machine-check form). Three freshness defects ride the register without orphaning it: the OW-2 half-flip (P1), AP-1's false OT-fact (P1), FF-3's missing row (P2) — all three already drafted verbatim by spec-06 (F-2/F-3/F-8); the amendment wave applies them mechanically. One P3: the E1 row's "13 tests" pin (era-correct at the R26 flip) — the file now holds 26 it-blocks (E1's 13 + E2's 13).

### K3 — the app-column-matches-LANDED check: **vacuously satisfied — zero rows LANDED.**

No mode is ported+pinned app-side at R27 (the ports are r1 work per D30.2). The app column's current truth is recorded in §1 (the two partial surfaces: the slip router verb + the pool-DnD placement; nle-ui's `⌥[/⌥]` + the dead tool radio G7-ripple). The K3 instrument to arm at r1: the app's AR-2 wire-coverage gate (LANDED, consuming the live registry) + the census-CI mechanism (D36.1's queue filing) + the per-mode pins the mode reports' acceptance rows name. One forward note: when the first mode lands, the matrix's app column must flip cell-by-cell with pins (the D30.3 form), and the app's OT mirror (`6e2b91a`, one session behind `970948a`) must re-pin first — the re-pin queue is already filed at `c020b2a`.

---

## §3 The D30.2 scope-gate card (the r1-entry decision material — paste-ready for the user)

**The decision:** which of the four absent families enter r1's scope (ARCH-R25 36.6: r1 grows **+1.5-3 wk solo** ONLY IF you pass this gate; the families are spec-first since R25 — their §5.9C-F contracts + GAP rows are DONE, so this is purely an implementation-scope call).

**The Stage-0 preconditions (the leverage map's cross-cutting decisions — make these FIRST regardless of the gate's outcome; they gate 5 of 10 modes + the companion fan-out):**
1. **The linkage model** (the biggest hole): OT has NO linkage field at all (the app dispatches pairwise, av-link.ts:39-43). Rule: an opencut `linkedTo`-style field/registry in OT's model vs dispatch-level composition + the bridge-side relink policy. Spec homes: 06 §5.0 + 09 §3.1A B1 (D32.5's pairwise-at-rest/groups-at-runtime). Everything in insert/overwrite/replace/ripple-overwrite + every companion fan-out hangs on it.
2. **The constants lattice** (D11's four retime domains + the volume-dB one-home): OT's `core/audio-params.ts` is the proven one-home pattern (OV-01/02 VALUE-imported by the engine) — extend it with the retime-domain law before any retiming family lands (fit-to-fill's [0.1,5] refusal domain rides it).
3. **E3's error contract + the N3 guard survey**: the transition-blocked abort re-surfaces as INVALID_PARAMS-class at the wire (never the engine's current silent empty return); `lockPreCheck` extends to every new verb (36 ops surveyed engine-side).

**The per-family material (leverage → build → risk → cost):**

| Family | What exists to reuse | What must be built | The risk register's headline | Cost rank |
|---|---|---|---|---|
| **Append at end** | The LANDED `insertBatch` (ONE history entry free) + both mocks' real per-track-end composites + `insertElements`' relative-offset batch (the primitive) | The 6th `PlacementStrategy {type:'append'}` + the resolver (per-track t₀, sequential accumulation, the linked-append A/V law, TRACK_LOCKED refusal) + the optional wrapper | LOWEST of the ten: kind-split duty (F1A-3), the linked pair synced at the video track's point, audio-conflict atomic refusal | **1 — the cheapest landing of the four** |
| **Fit to fill** | `calculateSpeed` (pure, timeline-math.ts:133) + `rateStretchItem`'s fixed-span derive + the variants' 37-pin tested planner + WDC's landed varispeed (the audio half is DONE — no new audio work) | The composite's OT-side landing (insert + `updateElements{retime}` under the 4-point contract) riding the wave-1 rateStretch port | The acceptance domain **[0.1, 5]** with INVALID_PARAMS refusal — NEVER clamps (a clamped fit silently violates exact-fill); FF-3's mock-side divergence to fix at the same crawl | **2** |
| **Ripple overwrite** | 3 ROUTED verbs (delete/move/insert) + `applyBatch` atomicity + `rippleTrimItem`'s private signed-shift machinery as the algorithm reference | The composite (delete→move→insert, the ORDER LAW) + the verb decision (dedicated vs the replace-placement delta param — the law-split stated at 15:4920) | The delta law's PUSH half cannot come from the classic §12 diff (freed = ∅, the intermediate state is overlap-rejected) — do NOT build it as "overwrite + ripple flag"; the pull's zero-floor law | **3** |
| **Replace** | `joinItems` :8817's id-remap pattern (the transition law) + the D31.4 contract + the 3-point family's `_buildSourceEditClip` | The ONE greenfield: `performReplaceEdit` + `timeline.replace` (the 79th union member) + the consumer re-implementation (the variants' current law is WRONG — RE-2) | Greenfield BOTH sides (the only mode with no algorithm anywhere); the sever-not-delete companion law (`linked:false`); the unfillable-source refusal; single-op atomicity (D31A) | **4 — the most expensive** |

**The recommendation:** pass the gate for all four — the leverage verdict (mock-leverage §5.4) is that this is "a port with three named mechanism decisions to make first", not no-man's-land: 6/10 modes are algorithmically complete engine-side (E1/E2-fixed, 85 carried tests), and 4 modes are spec'd composites over LANDED verbs. If you must trim: append + fit-to-fill are the cheap pair (both ride already-routed verbs); replace is the one defensible cut (greenfield both sides) — but its absence leaves the SourceEditBar's F11 button honest-refusing forever, and the D31A dedicated-op criterion was ruled precisely because composites cannot carry the transition-remap + sever semantics. Sequencing inside r1 per the leverage map: Stage 0 → trim family (roll→ripple-trim→slip→slide) → source-edit (insert→overwrite) → composites (append→fit-to-fill→ripple-overwrite) → replace + the wave-2 families.

---

## §4 The convergence check + the D39-candidate rulings

### §4.1 The three orderings vs each other — COHERENT, two rulings needed

| The ordering | Its source | The order |
|---|---|---|
| The plan's r1 op-depth row | IMPLEMENTATION-PLAN.md:73 | wave 1 = slip/slide/roll/rateStretch → wave 2 = retime/freezeFrame/rangeRemoval → error-envelope → C7 rename at END + the scope-gated mode-matrix rows (insert/overwrite 3-point ports + the four absent families + the OT verb family) |
| 06 §0's GAP register | 06:40-42 | the same wave 1/2 split + the four families r1-scope-gated (D30.2) + the C7 rename at r1 END |
| The leverage map's stages | mock-leverage §5.2 | Stage 0 (the mechanism decisions) → Stage 1 trim (roll→ripple-trim→slip→slide) → Stage 2 source-edit (insert→overwrite) → Stage 3 composites (append→fit-to-fill→ripple-overwrite) → Stage 4 replace + the wave-2 families + sync-lock |

**The verdict: one coherent sequencing.** The map's Stage 1-4 refines the plan's "scope-gated mode-matrix rows" into a dependency-true order (shared clamp infra first, the E1/E2-tested algorithms before the composites that reference them, the one greenfield last); the plan's wave 1/2 split is untouched; D33.5's grammar-carrier law (land N-2's overlay grammar WITH the op ports, not after) is consistent with both. **No conflicts of substance — two loose ends:**

1. **rateStretch's wave assignment** (the one real cross-document discrepancy): the plan + 06 §0 GAP row 1 + FF-1's dependency all put rateStretch in **wave 1** (with slip/slide/roll); the leverage map's Stage-4 list names "rate-stretch command" among the wave-2 families (while its own FF row says "the r1 vehicle is the wave-1 rateStretch port"). → **Ruling D39-B below.**
2. **sync-lock has NO r1-scheduled row in 06 §0's GAP register**: it is named in D12.3's port list (§10.4 :2561) and §10.5's OT-GAP row (:2612), but neither the §0 GAP row 1's wave lists nor the plan's r1 rows schedule it. Out of the ten-mode scope (K2 does not orphan over it — it is §6's family), but the D32.2 precedence law (lock > sync-lock > link) must be enforced IN every new op, so its absence from the schedule is a real sequencing hole the leverage map exposes ("after the trim family — it wraps them"). → **Ruling D39-C below.**
3. (P3, no ruling needed) the map's Stage-4 breadth (close-gap/join) exceeds the plan's wave-2 naming (retime/freezeFrame/rangeRemoval) — all engine-complete per §10.4, port-scheduled but not wave-assigned; the plan overhaul (xcut-plan's charge) can absorb the naming.

### §4.2 The wire-column coherence (task items 4a/4b — verified)

- **insertBatch LANDED changes the append row's wire form:** the R25 cell ("none (r1: the 6th `PlacementStrategy` + `insertBatch`)") understates the landed state — the composite carrier is LIVE (`timeline.insertBatch` @ api.ts:1262-1370, one history entry, the shared validateInsertElement, F1A-3 kind-split, CONFLICT whole-batch, refs echo), so the r1 scope shrinks to the 6th strategy + the append resolver (AP-2) + the optional wrapper. §1's matrix carries the corrected cell; spec-06 F-3 + spec-15's :4916 QUEUED→LANDED flip land the same fact in the amendment wave. The insert row's "placement-only" cell is UNTOUCHED by the landing (the two-semantics law holds: insertBatch is placement-insert, reject-not-shift — verified at the arm).
- **The singular keyframe retirement touches NO mode row — verified:** the ten rows reference zero keyframe verbs (the wire cells are RollCommand/TrimCommand{ripple}/SlipCommand/SlideCommand/insert/none×5); the retirement's spec impact lives entirely in 15 §4.1A/§13.15 + 05 §16.5/§14.1 (spec-15's P1-1/P1-5 family) — the matrix needs NO edit for it. The census footnote (24+6 → 31=28+3) is the only retirement-driven matrix change, carried in §1.

### §4.3 The D39-candidate rulings (for the W5/W6 fold — the D37/D38 slots are claimed by the preservePitch + wire-census/AR-2 candidates per spec-00/spec-15)

- **D39-A — the Stage-0 mechanism-decision set becomes the r1-entry preconditions:** the three decisions (the linkage model; the constants lattice extending OT's `core/audio-params.ts` one-home; E3's error contract + the N3 lockPreCheck extension) are ruled and filed BEFORE any family port, per the leverage map's Stage 0 — the collision map's biggest hole (no linkage model in OT) gates modes 5/6/7/9 + the companion fan-out. Registration home: 00's D-register + 06 §0's GAP register gains the three mechanism rows (owner S-spec-06 + S-ot; acceptance = the decision text + the constants module landed in OT).
- **D39-B — rateStretch stays wave 1:** three corpus sites agree (06 §0 GAP row 1, the plan's r1 row, FF-1's "riding 15 §13.15's wave-1 rateStretch row"); the leverage map's Stage-4 mention is the loose phrase (the first-class command vs the `updateElements{retime}` composite distinction). The fit-to-fill composite (Stage 3) rides it either way — `updateElements` is already routed, so only the authoring-verb half waits on wave 1.
- **D39-C — sync-lock gains its r1-scheduled row:** 06 §0's GAP register adds the row (owner S-ot; phase r1, sequenced after the trim family per the leverage map's dependency — it wraps them; acceptance = the carried sync-lock.ts tests green in OT + the `track.syncLock` field + the two propagations + the D32.2 precedence enforcement verified in every then-landed mode op). Without it, the D32.2 precedence sentence is law with no scheduled enforcement surface.

### §4.4 The battery hook (for battery_r27)

The mode-matrix check class: (a) the intra-file matrix↔GAP-row↔§10.4 consistency sweep (the E2 half-flip class — spec-06's candidate, confirmed here: the insert row's E1 cell and the overwrite row's E2 cell must match §5.0's E1 row and §5.9B's body/OW-2 within one edit round); (b) the engine line-pin liveness check (every `:NNNN` in 06 §0/§10.4/05 §16.5A greps true at the pinned engine commit); (c) the wire-census single-source (the 31=28+3 reading asserted once per spec file that names a census).

---

**Report-only compliance:** no spec/engine/OT/ui/app files modified; no commits, branches, or pushes; stayed on main. Deliverable: this report + the worklog entry. Every claim carries file:line evidence re-derived live this session at the R27 pins.

— end of report —
