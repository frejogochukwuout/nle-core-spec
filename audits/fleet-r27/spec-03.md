# R27-W3-03 — spec-03 (Playback Engine) — the per-section audit

**Agent:** spec-03 · **Round:** R27 final-tightness, Wave 3 (per-spec fleet)
**Spec:** `03-playback-engine.md` (2,792 lines, read in full, section by section)
**Ground-truth pins:** engine `f9ac806` (code anchor `50b91f5`, 748/748 — RC-V1's live `bunx vitest run` at `gaps/audit/rc-round/audit-RC-V1.md:115`, static it-census 664 + loop-generated rows), WDC `ec8fd5c` (777/777 per fleet), app `c020b2a` (252/252; `git submodule status` → engine `f9ac806`, nle-ui `83ff8a8`, WDC `ec8fd5c`), OT HEAD `55c81c0` (engine vendors OT `6e2b91a`). OT's WIRE census read live from `src/lib/timeline/headless/api.ts`.
**Method:** every load-bearing claim re-derived against the live trees (reads/greps cited); the six focus areas verified in code; 8+ cross-refs spot-verified.

---

## §1 The per-section verdict table

| § (lines) | Content | Verdict | Evidence |
|---|---|---|---|
| Status :4 | "v-next (Round 24 …)" — the 2026-09-08 pin block (`5036387`/458, OT `ded43c4`/`c15a629`/536, WDC `85b81b0`/759, nle-ui `fc4cc35`/674, app `c885ece`/174) | **P2** | Three fleet rounds stale (R25-R27). The R24 narrative (W11, S4, D30 "in flight") is history; every pin/count moved. Needs the R27 re-pin sentence + the "D30 LANDED" flip |
| §0 :15 (engine BASE row) | 458/458; "13 files — bridge-seams 113, nle-bridge 96, planner 52…"; "clock.ts 764 / player.ts 3,656 / video-sync.ts 1,234 LOC — byte-identical… the playback core SOURCE-STABLE"; "the only src/ deltas since `b8c6f88` are composition-frame.ts + bridge-seams.test.ts" | **P2** | Test-state rows: **748/748, 25 test files, bridge-seams 151, nle-bridge 99, load-validation 33** (live recount; the rest of the top rows exact: planner 52, timeline-math 42, transform-resolver 40, video-sync 31). clock.ts 764 ✓ and video-sync.ts 1,234 ✓ still exact, but **player.ts is 3,695** (+39: `0212fe3` HW1-4 collision-free registry auto-ids + epoch-keyed still-texture cache, `e9f2d91` HW1-10 groupStableVideoItems loop) — the SOURCE-STABLE claim died in the RA round. The row's since-R24 delta is missing entirely: the RC round (OV-01/OV-02 opencut-laws one-home `b5f39cb`, OV-05 `9850231`, M2 Wave 1 bus/sidechain/flush `50b91f5`, D12 ratification `1064f91`, OV-12 CI gate `2fd68c6`), the vendor re-pins (OT `6e2b91a` `55e5365`, WDC `ec8fd5c` `480a216`), the NS-1..5 queue + the r1-port collision map |
| §0 :16 (OT BASE row) | "24 UI-routed verbs + 6 documented exceptions, M49C the coverage gate machine-checking the routed set, tsc-lockstep both directions — the transport verbs routed: `timeline.{play, pause, seek, setLoopRegion, setPlaybackRate}`"; the S1 transport policy; the M28R ladder; `use-timeline-actions.ts:55-62/:178-197`; m28 real-mouse `:271-338`; 536/536 | **P1** | The census is the corpus-wide stale family (fact #1): live at `970948a`→`55c81c0`, **`WIRE_COMMAND_TYPES` = 31 verbs (api.ts:243-275), 28 routed + 3 exceptions (`WIRE_UI_EXCEPTIONS` api.ts:328-335: selectElements/advancePlayhead/trim — now an EXPORTED tsc-asserted Record, killing 15's old "test-local" note)**; D-ARCH-6's `insertBatch`/`removeKeyframes`/`retimeKeyframes` landed, the singulars RETIRED. Everything else re-verifies EXACT: S1's `setPlaybackRate` (timeline-core.ts:613-619 — finite nonzero, notify-once, NOT undoable), `setLoopRegion` (:637-675 — validated/reconciled S1-F4/crossing-only S1-F3/in-region bounded), `advancePlayhead` (:685-755 — unsnapped origin, committed-duration S1-F2, atomic pause, S1-F7 cadence); the M28R pins `:55-62/:178-197` byte-exact; m28-actions-real-mouse.mjs exists with the :271 JKL test. Count: 632/632. P3: "the headless `…/getLoopRegion` command surface" — `getLoopRegion` is the core getter + the setLoopRegion data echo (api.ts:1986), NOT a dispatch arm |
| §0 :17 (WDC BASE row) | "`85b81b0` — 759/759 … the delta vs the R23 pin is ONE docs-only commit"; the W2 varispeed law set; the S4 seal; "the async pre-retime STILL QUEUED … WDC's HANDOFF #3 = the engine's G2" | **P1** | Pin/count stale → **`ec8fd5c` 777/777**; the "ONE docs-only commit" framing is dead — ec8fd5c is the S-series upstream wave (19 locked files + the 18-file closure: the **EngineDiagnostic bus + heartbeat monitor**, the **rebuilt dsp-effects-worklet** with the `DspDiagnosedProcessor` mixin, the **rt-safety-gate transitive-heritage fix**) — NONE of it in the row (fact #7, missing from all 21 specs). The W2 varispeed law set itself re-verifies byte-exact: `RETIME_RATE_MIN/MAX = 1/32/32` at varispeed.ts:36-37, the identity/channel/F1-flush laws in the header (:1-24), `VARI_SPEED_PROCESSOR_NAME` :30. The S4 seal + the G2-queue claims hold (engine PLAN:388/:396 "G2: the async pre-retime, design-first"). WDC's own docs never absorbed the S-series (HANDOFF stale — their next session) |
| §0 :18 (app BASE row) | "`c885ece` — 174/174 … W3 (the JKL AUDIO half) LANDED — the six pinned laws … **still the tap-accel ladder at `c885ece`** … superseded IN FLIGHT by D30 R5 … D30 R4 re-bases the loop … W-E, in flight" | **P1** | The W3 six laws all re-verified LIVE at `c020b2a` (audioService.ts: `lastScheduledTr` :450/:605, the store-echo edge :381-388 BEFORE the jump heuristic, composed `varispeedRate: segRate * tr` :585, the #24 element-domain offset :571, fades ÷ tr :582-583, silent-reverse :534-538, the routed setShuttle engineService.ts:113-135; N2b glue :591/:446-495; ProgramCanvas.tsx exists). But the row's two "in flight" clauses are FALSE now: the **M28R stateless ladder LANDED (W-D `f681d34`** — use-keybindings.ts:37-46 "the port's old tap-accel window ladder is retired" + use-timeline-actions.ts:223-240) and **R4's two-way echo-guarded loop bridge LANDED (W-E** — EngineMount.tsx:238-294 + the GluedShell "D30 n" describe). Count: 252/252 (8 suites: GluedShell 111 it-census, audioService 42, sceneBridge 41, deliver 20, persistence 20, wire-coverage 9, engineSeam 7, waveformPeaks 9). Missing landed surface: the W-C wire-dispatch port (`use-wire-dispatch.ts` + `engineService.attachWire` :613), the **HA-3-2 seek-commit seam** (FW-E2 `354cdfd`), the Z4/W-D lockstep family, the R7 r-key yield, the **preservePitch↔maintainPitch bridge** (engineService.ts:500-530, sceneBridge.ts:262/:447 — briefing fact #15) |
| §0 :19 (consumer-pin note) | app engine pin `5036387`; OT mirror `c15a629`; nle-ui `fc4cc35`; WDC `85b81b0`; engine's vendors OT `c15a629` + WDC `494f6ff` | **P2** | Every pin stale: app engine → `f9ac806`, OT mirror → `6e2b91a`, nle-ui → `83ff8a8` (one docs-only commit behind the repo's `32abd58` — the AW1-2 absorb is queued at the app's freshness re-pin), WDC → `ec8fd5c`; engine's vendors OT `6e2b91a` + WDC `ec8fd5c` (all live via `git submodule status`) |
| GAP :22 (event staircase, w2) | posture row | SOUND | No falsification; the w2 phase stands |
| GAP :23 (K3 playback-behavior corpus) | posture row | SOUND (P3 note) | Partial landing evidence exists (GluedShell.test.tsx:107-110 the D2a engine-wrap/reverse-clock pins); the row's 12-test corpus as such not found app-side — posture stands, but the row's "D30's W-C in flight" clause is stale (landed) |
| GAP :24 (the D30 JKL/transport-mirror convergence) | "verified PENDING at `c885ece`: the tap-accel ladder still live, no `data-transport` guard, no wire symbols in src/" | **P1** | ALL THREE claims FALSE at `c020b2a`: (a) the tap-accel ladder is RETIRED (W-D landed — use-keybindings.ts:37-46 + use-timeline-actions.ts:223-240); (b) the `[data-transport]` guard is LIVE app-side (use-keybindings.ts:139-150, the SURFACE_OWNED_KEYS member with the 9-G1 no-shiftKey-carve-out note) AND package-side (nle-ui `755235c`: Viewer.tsx:294-309 + Ruler.tsx:230-248, both `e.stopPropagation()` + the Z4 law comment); (c) wire symbols EXIST (`use-wire-dispatch.ts`, `engineService.attachWire` :613, the wire-coverage gate consuming the live barrel). The R6 r-yield landed too (nle-ui `2646d80` + the app `78e7283`). The row is FINISHED WORK needing the LANDED flip |
| GAP :25 (viewer frame visuals, w1) | posture row | SOUND | w1 phase; no falsification |
| GAP :26 (K3 audio pins) | "hard prerequisite: S-wdc's waveform seam contract … STILL unstated WDC-side — the D29.4 promotion action moves it … into the HANDOFF next-session scope" | **P2** | The prerequisite clause is FALSE since R25: the waveform contract LANDED as WDC's named cross-repo surface (waveform-contract.ts:44 `WAVEFORM_PEAKS_BUCKETS = 2000`, :47 `WaveformPeaks`; the three functions in audio-registry) and the app's consumer landed with **9 pins** (waveformPeaks.ts + waveformPeaks.test.ts — "the S-wdc/R26 promotion's unblocked execution"). The row needs the re-scope: what remains is the offline-render/call-spy pin audit, not the contract |
| GAP :27 (reverse-transport audio depth, r2) | "FreeCut's reverse-shuttle grain plan (§8.4) remains the reference" | SOUND | Re-verified open: no grain-plan port anywhere (engine grep: zero reverse-shuttle sites); the app's design doc itself says "Reverse audio (baked reversed buffers) is a queued M2-class capability, not this round" (design-jkl-audio-follow.md:78-82). The r2 row accurately pending |
| GAP :28 (decode/scrub perf, r4/r6) | posture row | SOUND | D6 re-affirmation still pending; the single-tier scrub cache + prewarm remain the BASE (player.ts:280 comment verified exact); the 3-tier cache still unbuilt |
| GAP :29 (K1-continuous re-pin law) | the closed W-A row + the standing law | SOUND | The app is at engine HEAD `f9ac806` — zero lag NOW; the law's next re-arm is the OT re-pin (`6e2b91a`→`55c81c0` queued at c020b2a) |
| :31 (acceptance row) | "battery_r24's posture checks … spec 17 §13A facet rows (the §3.1 'Playback (frame-accurate)'…)" | **P2** | battery_r24 → battery_r27; 17 §13A's 03 row exists (:143) but cites "≈2371" — 03's `## Testing` is at **:2429** (cross-noted to 17); the quoted matrix-row names drift from 17's actual text ("Playback frame accuracy (30/24/23.976 fps)", "Varispeed (0.5x, 2x, with SoundTouch pitch preservation)", "Scrubbing latency (p50, p95, p99)" — 17:525-527) |
| §1-§2 (:51-65) | Purpose / Goals | SOUND | Target-state |
| §3 (:67-176) | The Clock — FreeCut reference + the engine-port note | SOUND | The reference quotes are the historical verification record (FreeCut tree gone — see §13.A row); the engine port re-verified EXACT at `f9ac806`: clock.ts:576/:591/:638/:687/:92 all byte-quoted correctly, 764 LOC, R1-B7's law comment at clock.ts:249. P3: the §3.4 note's parenthetical attributes R1-B8/R2-5 to clock.ts — they live in **player.ts:793/:801** (the scrub-implies-pause + statechange laws, verified live) |
| §3.5 (:154-176) | StaticClock | SOUND | SPEC-ONLY re-confirmed: ZERO `class StaticClock` in engine src at `f9ac806` |
| §4 (:179-292) | MediaTime/FrameRate | SOUND | Reference math (OpenCut-classic Rust — historical); the internal 23-format recount (§5.3's enum list) exact |
| §5 (:294-471) | Decode pipeline | SOUND | mediabunny reference + §5.2's VideoSinkDecoderOptions correction — the 02-audit resolution already reflected here (no pixelFormat; 10-bit = I420P10-family). P2/P3: the `/tmp/freecut/node_modules/...` cites are dead paths now (provenance note needed — see F-12) |
| §6 (:473-633) | Six sync plans | SOUND | The engine-port note verified: video-sync.ts:926/:181/:225/:1041 ALL EXACT, 1,234 LOC unchanged — the six plans + drift constants hold at `f9ac806` |
| §7 (:635-771) | Scrubbing — seekGenerations, prefetch chain, 3-tier cache, prewarm | **P1** | The reference material stands, but §7 carries **none of the landed gesture-side seek/commit law**: the HA-3-2 seam (app FW-E2 `354cdfd` absorbing OT `a25d199`) — during a ruler scrub, per-frame preview seeks stay core-direct (ZERO wire entries — the flood law) and the **mouseup COMMIT crosses the wire as exactly ONE `timeline.seek`** at the final position; the empty-area click-seek's mousedown→pending/click→commit venue (the click IS the commit, M51R's pin shape); the dispatch-optional commit seams in use-timeline-playhead/use-timeline-seek. Pins: GluedShell.test.tsx:2350 ("FW-E2 b … the click IS the commit, no preview phase") + the FW-E2 a row. 03 owns scrubbing — this is its missing law |
| §8 :773-885 (8.1/8.2) | Varispeed — FreeCut SoundTouch reference | SOUND (as reference) | The §8.2 FreeCut quote-fidelity stands (historical); the VENUE statement is live in §13E's row (the full WSOLA port landed in WDC W2) — but see P1-6 for the missing law |
| §8 (normative body) | **The composed-rate + pitch-projection law** | **P1** | The corpus's own pointer says the audio half lives HERE: 06's §10.5 map row — "§5.12 Retime pitch … Audio half lives in **spec 03** / nle-engine / WDC's W2" — yet 03's §8 body states NEITHER (a) the **composed transport×element rate law** (`varispeedRate = segRate × tr`; wall = timeline/r; spans/anchors/fades ÷ tr; the #24 element-domain offset `offsetSec = trim + into × segRate`; one `lastScheduledTr` idempotence key per rate change; silent-reverse J; the composed domain `[0.01,5] × {1,2,4} ⊂ [1/32,32]`) — it exists only as the §0:18 posture row, while 06:1670's pair law + D31.7's fit-to-fill audio half (06:426: "one rate law both streams … audio in `varispeedRate = segRate × transportRate`") consume it; NOR (b) the **preservePitch projection chain** (R9-b: `el.preservePitch` absent≡true → `retime.maintainPitch` → `seg.maintainPitch` → the adapter's WSOLA pre-retime branch — the field appears ZERO times in 03). `preservePitch` grep in 03: 0 hits |
| §8.3 (:886-889) | Varispeed in cloud render | **P2** | "the same worklet runs but processes faster than real-time" needs the M2 Wave-1 flush caveat: worklet-backed inserts in the offline path need a macrotask flush — engine `WORKLET_BACKED_INSERT_TYPES` (scene-mixer.ts:170) + the 50ms default (:444) + the app's 250-when-present (deliverService.ts:252); without it the insert's effect vanishes from the export |
| §8.4 (:890-914) | Reverse playback audio (grain plan reference) | SOUND | The reference + the r2 GAP row carry the pending status correctly |
| §9 (:917-957) | Audio pipeline + the round-9 domain note | SOUND + **P1 (the missing robustness law)** | The domain note's pointer verified (20-audio-core.md §4.3 exists at :108 — Layer E). But 03's playback-path robustness carries NONE of the landed WDC S-series law (fact #7): the **stall bypass-to-passthrough** (effects.ts:441-507 — stall >3 s while running → bypassed to the ctor's passthrough wire, audio keeps flowing; NaN → permanent bypass; graceful recovery without charging the state-restore budget), the **EngineDiagnostic bus** (diagnostics.ts — 7 kinds, the 64-event ring, `HEARTBEAT_STALL_MS = 3000` at :212, isRunning-gated), the **in-process() protocol** (the rebuilt dsp-effects-worklet.js: DspDiagnosedProcessor — 256-quantum heartbeats, NaN/silence scans, preallocated messages). 02's §7.4 (per spec-02's FIX-1) is the drafting home — 03 needs the playback-continuity cross-ref row |
| §10 (:960-971) | Frame rendering loop | SOUND | The R23-update note re-verified EXACT: scene-assembly.ts:868-877 (the per-type gate) + the video-only `continue` at :1894 (`collectFrameVideoCandidates`) both live at `f9ac806` |
| §11-§12 (:974-1429) | The 13 answered questions + the seed test plan | SOUND | Historical verification record; preserved verbatim |
| §13.A/B/C (:1435-1484) | Reference tables + the MPL-2.0 license note | **P2** | All `/tmp/freecut` + `/tmp/opencut-classic` paths are DEAD (the trees are gone — 06's finding; re-confirmed). The tables + §15's full quote are now the corpus's ONLY preserved FreeCut reference — the provenance note (06's 20:8 precedent) needs the 03 twin |
| §13.D (:1486-1522) | Key file:line references | **P2** | Same /tmp provenance family |
| §13E (:1524-1544) | The engine register, "re-verified at `5036387`" | **P2** | **All 13 engine line cites re-verified EXACT at `f9ac806`** (clock.ts:576/:591/:638/:687/:92; video-sync.ts:926/:181/:225/:1041; player.ts:280; scene-assembly.ts:868-877/:1894; soundtouch-processor.worklet.ts:13) — the register's pins are stable; only the framing drifts: the pin (`5036387`→`f9ac806`), the count (458→748), player.ts LOC (3,656→3,695 — the drift family 02 found at :1629→:1646 does NOT touch 03's cites; 16's :994-995→:999-1000 is 16's own §17), and the missing RC-round register rows (the opencut-laws one-home, the M2 Wave-1 mixer surface, the E1/E2 26-it pin file, the vendored re-pins) |
| §14 (:1547-1762) | Corrections A-J | SOUND | Historical; all ten corrections stand |
| §15 (:1765-2411) | Clock.ts full quote | SOUND | The preserved reference — value RAISED by the /tmp trees' death |
| Testing (:2429-2792) | The formalized per-module test section | **P2** | (a) The two NEW fixtures (`10s-test-pattern-1080p.mp4`, `10s-10bit-hevc.mp4`) were NEVER registered in 17 §5 — the "to be registered … before merge" promise (:2714-2716) is unfulfilled (TEST-INTEGRATION #3 class; 17 carries only 10s-red/10s-smpte/10s-red-10bit + the sine family); (b) the Tier-3 JKL row states the pre-M28R law — "K → paused (rate preserved internally as 0 but last-rate remembers -1)" contradicts the landed S1/M28R law (K = pause-ONLY, the rate STAYS −1; setPlaybackRate rejects 0), and "debounced per spec 16 §3.1 multi-tap semantics" is the retired tap-accel framing; (c) the matrix-row names quoted at the section head drift from 17's actual row text |

**P1 ×6 · P2 ×8 families · P3 ×5.** The reference body (§3-§15, ~85% of the file) is SOUND and its engine pins are the fleet's most stable; the entire P1 load sits in the §0 posture layer (4 of 6) plus the two missing normative laws (§7's commit seam, §8's composed-rate/pitch chain) — exactly the surfaces the R25-R27 fleet movement landed on.

---

## §2 The six P1s, in detail

**P1-1 — the D30 flip set (§0:24).** The GAP row's "verified PENDING at `c885ece`" triple is now three FALSE statements. Landing commits: the app `f681d34` (W-D: the M28R ladder + the S1 pin rewrite), `78e7283` (Z4 app-half + the K2 un-withheld + the r/⇧R rows), `0328bc0`/`f681d34` (the wire-dispatch port W-C), and the nle-ui package `755235c` (Z4 stopPropagation) + `2646d80` (R6 r-yield). The loop two-way bridge (R4/W-E) landed in EngineMount.tsx:238-294 with the GluedShell "D30 n" describe. The row's acceptance criteria are all met — it needs the LANDED flip (or retirement into the BASE app row's history).

**P1-2 — the app BASE row's dead clauses (§0:18).** "still the tap-accel ladder at `c885ece` … superseded IN FLIGHT by D30 R5" and "D30 R4 … W-E, in flight" both describe a dead world. The row also misses five landed surfaces it should carry as BASE: the W-C wire-dispatch port, the FW-E2/E3 carrier absorptions (the HA-3-2 seam + the HA-1 keybindings yield), the Z4 family, the R7 r-key yield, and the preservePitch bridge (the read-merge-write law at engineService.ts:500-530 + the load/save projections at sceneBridge.ts:262/:447 — `maintainPitch: el.preservePitch !== false` / `retime.maintainPitch === false → preservePitch: false`, absent≡ON both directions).

**P1-3 — the stale wire census (§0:16).** Corpus fact #1. Live: 31 = 28 + 3 (api.ts:243-275/:328-335), the batch verbs landed, the singulars retired, the exceptions now an exported tsc-asserted Record. 03's one census mention joins the fleet-wide re-declaration (15's drafted 31-verb block is the paste source).

**P1-4 — the WDC S-series absence (§0:17 + §9).** Corpus fact #7. The diagnostics/heartbeat/bypass mechanism is a PLAYBACK-continuity law (a stalled DSP insert must not stop the transport): the stall bypass-to-passthrough restores the ctor's passthrough wire while the clock keeps running — 03 is the spec whose §2 Goal is "any stall is immediately visible". 02's §7.4 is the drafting home (per spec-02's FIX-1); 03 needs the §9 registration + the two-way link.

**P1-5 — the missing seek-gesture COMMIT seam (§7).** The FW-E2 law: scrub previews are core-direct (the flood law — zero wire traffic), the release COMMIT is exactly one `timeline.seek`; the click-seek venue is mousedown→pending/click→commit. Landed app-side `354cdfd` (absorbing OT `a25d199`'s dispatch-optional seams in use-timeline-playhead/use-timeline-seek); pinned GluedShell.test.tsx:2350+. §7 currently documents only the DECODE-side invalidation (seekGenerations) — the gesture-side commit semantics (what crosses the wire, when, and how many times) is the other half of the scrubbing law and has no home in the corpus's playback spec.

**P1-6 — the missing composed-rate + pitch-projection law (§8).** 06's own map row points here ("Audio half lives in spec 03"); 06:1670's linked-pair law and D31.7's fit-to-fill audio half consume `varispeedRate = segRate × transportRate`; the app's six pinned laws (audioService.ts:498-606) + the design doc (docs/design-jkl-audio-follow.md §1) are the reference implementation; the engine's scene-mixer/realtime-bridge threads carry the element half (scene-mixer.ts:365) and the transport half (realtime-bridge.ts:328) separately — the COMPOSITION is app-side today. And the pitch half: R9-b's `el.preservePitch` absent≡true ruling has zero presence in 03 (grep: 0 hits), while 03 §13E is where the SoundTouch venue is stated. §8 needs a normative §8.5.

---

## §3 The fix-list (exact edits, mechanically applicable)

**F-1 (§0:16 census + counts).** Replace "24 UI-routed verbs + 6 documented exceptions, M49C the coverage gate machine-checking the routed set" with: "**31 wire verbs = 28 UI-routed + 3 documented exceptions** (`WIRE_COMMAND_TYPES` @ api.ts:243-275 + `WIRE_UI_EXCEPTIONS` @ :328-335 — the exceptions now an EXPORTED tsc-asserted Record: selectElements/advancePlayhead/trim; **D-ARCH-6**: `timeline.insertBatch`/`removeKeyframes`/`retimeKeyframes` landed one-history-entry-each, the singular remove/retime RETIRED — the coverage gate consumes the exported constant live)". Replace "536/536 (386 in-page + 150 real-mouse across 59 milestone entries" with "632/632 (M49C 28/3; OT HEAD `55c81c0`, code pin `970948a`)". P3 rider: "the headless `timeline.setPlaybackRate`/`setLoopRegion`/`getLoopRegion` command surface" → "…`setLoopRegion` command surface (+ the `getLoopRegion` getter echoed in setLoopRegion's result)".

**F-2 (§0:17 WDC re-pin + the S-series).** Replace "`85b81b0` — 759/759 vitest (verified locally this round; the delta vs the R23 pin `494f6ff` is ONE docs-only commit — the seal-round wrap), tsc 0" with: "`ec8fd5c` — 777/777 vitest, tsc 0 (the S-series upstream wave: 19 locked files + the 18-file closure — **the EngineDiagnostic bus + heartbeat monitor** (diagnostics.ts — 7 event kinds, a 64-event ring, `HEARTBEAT_STALL_MS = 3000`, isRunning-gated, graceful recovery), **the rebuilt dsp-effects-worklet bundle** (the DspDiagnosedProcessor mixin: 256-quantum in-`process()` heartbeats + NaN/silence scans, preallocated rt-safe messages), **the stall bypass-to-passthrough ladder** (effects.ts:441-507 — the ctor's passthrough wire restored on stall; NaN bypasses permanently), **the rt-safety-gate transitive-heritage fix**; WDC's own HANDOFF never absorbed the wrap — their next session's problem)". Keep the W2 law-set sentence (re-verified byte-exact at varispeed.ts:36-44).

**F-3 (§0:18 app re-pin + flips).** Replace "`c885ece` — 174/174 vitest (GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliverService 15 + engineSeam 7; …)" with "`c020b2a` — 252/252 vitest (GluedShell 111 + audioService 42 + sceneBridge 41 + deliver 20 + persistence 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9; …)". Replace "**still the tap-accel ladder at `c885ece`**: … superseded IN FLIGHT by D30 R5, which ADOPTS upstream's M28R stateless ladder" with "**the M28R stateless ladder LANDED (D30 W-D, `f681d34`)**: use-keybindings.ts:37-46 + use-timeline-actions.ts:223-240 — the tap-accel window RETIRED, K = pause-only, Space resumes at the persisted rate, setPlaybackRate BEFORE play; ⇧L/⇧J fixed-2× stay port-local". Replace "D30 R4 re-bases the loop to the echo-guarded two-way bridge — W-E, in flight" with "the echo-guarded two-way loop bridge LANDED (W-E — EngineMount.tsx:238-294, tick-domain echo keys both sides)". Append to the row: "Plus since R24: the W-C wire-dispatch port (`use-wire-dispatch.ts` + `engineService.attachWire` + the coverage gate over the live barrel), the FW-E2/E3 carrier absorptions (the HA-3-2 seek-commit seam — §7.7; the HA-1 focused-bookmark yield), the Z4 `[data-transport]` guard both halves, the R7 r/⇧R yield, and the **preservePitch bridge** (engineService.ts:500-530 the read-merge-write with absent≡true; sceneBridge.ts:262/:447 the load/save projections)".

**F-4 (§0:19 consumer pins).** Replace the five pins: app engine `5036387`→`f9ac806` (re-pinned `9448667`, the RC wrap); OT mirror `c15a629`→`6e2b91a` (the re-pin to `55c81c0` queued at c020b2a); nle-ui `fc4cc35`→`83ff8a8` (the AW1-2 ceiling absorb rides the freshness re-pin); WDC `85b81b0`→`ec8fd5c`; engine's vendors → OT `6e2b91a` + WDC `ec8fd5c`.

**F-5 (§0:24 the GAP flip).** Rewrite the row's tail: "— **K3 (app; LANDED R25-R27**: W-C `use-wire-dispatch` + attachWire; W-D `f681d34` the M28R ladder + `78e7283` the Z4/K2/r-yield rows + nle-ui `755235c`/`2646d80`; W-E the loop two-way bridge EngineMount.tsx:238-294; the coverage gate accumulated ⊇ WIRE_COMMAND_TYPES − exceptions, live) — the row retires into the BASE app row; the r1 residue is the OT mirror re-pin".

**F-6 (§0:26 waveform prerequisite).** Replace "hard prerequisite: S-wdc's waveform seam contract — the only K3 sub-half with an external prerequisite; the contract is STILL unstated WDC-side — the D29.4 promotion action moves it from WDC's parking-lot bullet into the HANDOFF next-session scope" with "prerequisite MET (R25): WDC's waveform-contract.ts (WAVEFORM_PEAKS_BUCKETS + WaveformPeaks + the three audio-registry functions) is the named cross-repo contract; the app's consumer landed with 9 pins (waveformPeaks.ts) — remaining: the offline-render/call-spy pin audit over the mute laws under the Node venue".

**F-7 (new §7.7 — the seek-gesture COMMIT seam).** Insert after §7.6: "**§7.7 The gesture COMMIT seam (HA-3-2, LANDED app FW-E2 / OT a25d199).** A scrub gesture has two halves with different venues: the per-frame PREVIEW seeks stay core-direct (zero wire traffic — the flood law; the playhead follows the pointer through the engine clock) while the gesture's COMMIT (mouseup at the final position, or the click-seek's click — mousedown→pending, click→commit, M51R's venue) crosses the wire as **exactly ONE `timeline.seek`**. The seams are dispatch-optional in `use-timeline-playhead`/`use-timeline-seek` (a view that injects no dispatch keeps the legacy core-direct behavior). Pins: GluedShell.test.tsx FW-E2 a/b (the scrub-commit = 1 wire seek at 240000; the click-seek = 1 at 300000)." Cross-ref 15 §4.3 (timeline.seek) + 18 §4.11's routing canon.

**F-8 (new §8.5 — the composed-rate + pitch law).** Insert after §8.4: "**§8.5 The composed transport×element rate law (W3/S3-C4, LANDED).** During transport-rate playback (JKL), every schedule term composes the transport rate `tr` with the element's own retime rate `segRate`: **`varispeedRate = segRate × tr`** (content per wall second); future anchors `(S−P)/tr`; played spans `remaining/tr`; **fades are timeline-authored but the envelope sweeps wall seconds — ÷ tr** (the R3-3 clamp computed in the timeline domain first); the mid-entry content offset is ELEMENT-domain (`offsetSec = trim + into × segRate` — the transport rate moves how fast you traverse, not WHERE in the clip you are); **one reschedule per rate change** (the `lastScheduledTr` idempotence key — the mirror's playRate echo must not re-fire); **`tr ≤ 0` is the honest silent-reverse degradation** (WebAudio cannot play backward; the clock runs backward in silence); the composed domain stays in `[1/32, 32]` by construction (element clamp [0.01,5] × ladder {1,2,4}) with the adapter's domain guard the safety net. Reference implementation: `nle-test-app/src/audioService.ts:498-606` + `docs/design-jkl-audio-follow.md` §1; engine halves: scene-mixer.ts:365 (element) + realtime-bridge.ts:328 (transport). **The pitch half (R9-b):** `el.preservePitch` is ElementJSON law (absent≡true) → the bridge projects `retime.maintainPitch` (scene-to-segments.ts:596/:623) → `seg.maintainPitch` → the adapter's WSOLA pre-retime branch (segment-strip-adapter.ts:526-547; the [1/32,32] guard + the >2ch fallback); preservePitch=false leaves the legacy pitch-affected path. The SoundTouch VENUE is WDC's W2 (soundtouch.ts + varispeed.ts `retimeChannels`) — see §13E."

**F-9 (§8.3 flush caveat).** Append: "**M2 Wave-1 flush law:** a worklet-backed insert in the offline path needs a macrotask flush before `startRendering()` or its effect vanishes from the render — engine `WORKLET_BACKED_INSERT_TYPES` (scene-mixer.ts:170) + the `workletFlushMs` default 50 (:444); the app's deliver venue flushes 250-when-present (deliverService.ts:252)."

**F-10 (§9 robustness registration).** Append to §9.2 (or as §9.2A): "**Playback-path stall law (WDC S-series, LANDED `ec8fd5c`):** a DSP insert that stalls (>3 s without a heartbeat while the context runs) or renders non-finite samples is **bypassed to the passthrough wire** — audio (and playback) continues; the NaN bypass is permanent, the stall bypass recovers gracefully. The mechanism: the EngineDiagnostic bus + the DspDiagnosedProcessor's in-`process()` heartbeats. Full law: spec 02 §7.4 (the drafting home); this section owns the playback-continuity consequence."

**F-11 (§13E re-pin).** Replace the header's "(re-verified @ `5036387`)… clock.ts 764 LOC; player.ts 3,656; video-sync.ts 1,234, all UNCHANGED at the live pin" with "(re-verified @ `f9ac806`, R27): clock.ts 764; video-sync.ts 1,234 — unchanged; **player.ts 3,695** (the RA round's HW1-4 registry-auto-id/epoch-keyed still cache + HW1-10 grouping loop moved it; the cites below are stable — all 13 re-verified exact). 748/748 at the pin (RC-V1's live run). New since the R24 register: bridge/opencut-laws.ts (the OV-01/02 one-home), scene-mixer.ts's M2 Wave-1 bus/sidechain/flush laws, the vendored re-pins OT `6e2b91a` + WDC `ec8fd5c`."

**F-12 (§13.A/C/D provenance).** Prepend to §13.A: "> **Provenance (R27):** the `/tmp/freecut` and `/tmp/opencut-classic` trees are GONE — the quotes in §3-§15 are now the corpus's ONLY preserved FreeCut/OpenCut-classic reference (the 06:20:8 precedent). Do not re-verify against the paths; re-derive only from the quotes."

**F-13 (Testing section).** (a) Register the two fixtures in 17 §5 (cross-spec note below) and change 03's ":2714 to be registered … before merge" to "registered in 17 §5.1/§5.2 (R27)". (b) Rewrite the Tier-3 JKL row's law: "J → rate −1, K → paused (the rate PERSISTS at −1 — K is pause-only; setPlaybackRate rejects 0), L → rate +1; multi-press doubles capped ±4 per spec 16 §3.1's M28R ladder (the tap-accel window is retired)". (c) Align the quoted matrix-row names with 17's actual text.

**P3 batch (optional riders):** the §3.4 note's R1-B8/R2-5 re-attribution to player.ts:793/:801; §0:31's battery_r24→battery_r27 + the ≈2371→:2429 pointer (17's row); a D12-ratification note at §13E's standing-law sentence ("the spec wins — RATIFIED engine-side at DECISIONS #12, 2026-09-13: opencut is THE editing engine, the engine's stack is the render/export venue model, the junction one-directional"); the NS-1..5/r1-port queue pointer (the plan's job, not 03's).

---

## §4 Cross-spec notes (for the cross-cut wave)

1. **15** — 03's census cell joins the fleet-wide 31=28+3 re-declaration (15's drafted block is the paste source); 03 §7.7's `timeline.seek` commit law should cross-ref 15 §4.3's seek row.
2. **06** — §8.5's composed-rate law must land byte-coherently with 06 §5.12's R9-b companion (spec-06's F-10) and 06:1670/:426's pair-law citations; one author, one landing. 06's §10.5 "audio half lives in spec 03" map row then resolves.
3. **02** — the §9.2A stall-law row pairs with 02's §7.4 (the drafting home); 02's FIX-4 mechanical batch covers 03's nothing (03's player.ts cite :280 held), but 02's §13B player.ts:1629→:1646 drift confirms the family 03's §13E re-pin sentence must now acknowledge.
4. **09/18** — the preservePitch chain: 09's B2 ruling is the field law; 18 §4.4's honest-mock ledger row + 18's F-4 (the Gain-dB circularity) already cite the Inspector seam; 03's §8.5 is the playback-side third leg. The A1 play-stops-at-out family: 18 §4.3 is the home (mock-verdicts V4); 03 needs NO law change — its §3.4/§11.1 range-playback law (play stops at the out point via actualLastFrame) is the clock-side twin and should be cross-noted when 18's F-2b lands.
5. **17** — the fixture registration (F-13a) is 17's §5 edit; 17's §13A 03-row pointer ≈2371→:2429; the quoted matrix-row names.
6. **19** — the consolidated re-base map (spec-19's F-5) should absorb 03's §13E re-pin sentence + the R1-B8/R2-5 re-attribution.
7. **00** — the battery_r24→r27 key; the D12 note's master home.

---

## §5 What 03 uniquely sees

1. **The engine's playback core is the fleet's stability anomaly** — while timeline.ts churns ~1,500 lines/round, ALL 13 of 03's §13E line cites held across three fleet rounds (458→748 tests, +290 pins, and the clock/video-sync/scene-assembly pins byte-stable). Only player.ts's LOC moved (+39, RA round) and none of 03's cites sit in the moved region — the symbol+file citation convention 19 recommends is ALREADY proven by this spec's register.
2. **The composition venue gap**: the composed `varispeedRate = segRate × tr` law exists ONLY app-side (audioService.ts:585) — the engine's bridge threads carry the two halves separately (scene-mixer.ts:365 element / realtime-bridge.ts:328 transport) and never compose them. The law's only normative homes are 03's §0 posture row + 06's pair-law citations; P1-6's §8.5 gives it a body. The D31.7 fit-to-fill audio half inherits it — if the engine ever authors fit-to-fill, the composition must move engine-side or the export path will diverge from preview.
3. **The HA-3-2 seam is a two-venue seek law no other spec carries whole**: 18 has the routing canon, 15 the verb, 16 the guard family — but the flood-law/one-commit gesture semantics (preview core-direct + one wire seek on release) is playback law. 03 is its home.
4. **The /tmp reference death raises §15's stakes**: 03's Clock.ts full quote + §13.A/B tables are now the ONLY preserved FreeCut reference for the clock/sync/scrub families (06 holds the ops families; 02 the workers). The provenance note is a preservation duty, not hygiene.
5. **The r2 row is the fleet's most honest GAP row** — re-verified genuinely open (no grain port; the app's own design doc defers reverse audio to an M2-class capability), while two sibling rows in the same §0 (the D30 and waveform rows) were falsified by landings. The audit's flip-list touches every row around it.
6. **The Z4 double-venue pattern**: the guard exists in BOTH a package surface (nle-ui Viewer/Ruler stopPropagation) and a keymap registry member (the app's `[data-transport]` SURFACE_OWNED_KEYS row with the 9-G1 no-shiftKey-carve-out note) — 03's GAP row described them as "one nle-ui package commit"; the landing is actually a two-repo lockstep, the third proven execution of the D28-A5 keymap-lockstep law (16's finding).

---

*Report-only: no spec/engine/app/OT/WDC files touched outside this report; no commits, no branches; stayed on main.*
