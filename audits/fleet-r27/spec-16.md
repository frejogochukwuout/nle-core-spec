# spec-16 — R27-W3 final-tightness audit (16-keyboard-shortcuts.md)

**Task ID:** R27-W3-16 · **Agent:** spec-16 · **Date:** 2026-09-14
**Spec:** `nle-core-spec/16-keyboard-shortcuts.md` (2,451 lines; v-next, R24 re-baseline + the R25 D34 amendment) — audited section-by-section at sub-spec depth.
**Pins (ground truth, re-derived live this audit):** app `c020b2a` (252/252; the W-D waves LANDED — see F-1); nle-ui `32abd58` (690/690; the r-yield + Z4 + F4-7 waves landed in the `3026099..32abd58` range); OT HEAD `55c81c0` / **code pin `970948a`** (632/632; `WIRE_COMMAND_TYPES` = **31 = 28 routed + 3 exceptions** @ `api.ts:243-275`/`:328-335`); engine `f9ac806`; WDC `ec8fd5c`; mini = the R24-2 in-flight state (428 @ the `fb05540` re-pin; census 147 units / 134 GAP / 395 authored; W0+W1+W2 landed); variants in-repo past the R25-W1 wrap (1,762 it-blocks in flight).
**Method:** every load-bearing 16 claim re-derived against the live trees (`/home/z/my-project/nle-ui`, `nle-test-app`, `opencut-timeline`, `nle-engine`, in-repo `ui-mock/shell-mini` + `ui-mock/shell-variants`); the W1/W2 reports (scout-app, scout-nle-ui, mock-trim, mock-verdicts, mock-insert) used as priors and re-verified at the sites I rely on. No file outside this report + the worklog modified.

---

## §2 The per-section verdict table

| § (lines) | Claim class | Verdict | Evidence (file:line @ pin) |
|---|---|---|---|
| §0 header (:4) | round pin | **P2** | The R24 audit-round paragraph pins engine `5036387` 458 / OT `ded43c4`/`c15a629` 536 / WDC `85b81b0` 759 / nle-ui `fc4cc35` 674 / app `c885ece` 174 / mini 355 sealed / variants 1521+ — all seven stale at the R27 pins (engine `f9ac806` 748; OT `55c81c0`/`970948a` 632; WDC `ec8fd5c` 777; nle-ui `32abd58` 690; app `c020b2a` 252; mini 428 in-flight; variants 1,762). Historical-lineage convention applies, but the R27 round note must be appended. |
| §0 BASE app row (:15) | keymap census + JKL state | **P1** | (a) "the 25-row port map (OT's 28 at `c15a629` minus the 3 undo rows)" → the port map is **27 rows** (the `r`/`⇧R` loop rows added, `use-keybindings.ts:59-60`); (b) "at `c885ece` the port still ships tap-accel + K = togglePlay (use-keybindings.ts is one of the census's 3 documented merge carriers, W-D pending)" → **FALSE**: the tap-accel ladder is RETIRED ("W11 (M28R rewrite, D30 R5)… The port's old tap-accel window ladder is retired", `use-keybindings.ts:38-42`; `k` = `jkl-pause` :43); K = pause-ONLY pinned (`GluedShell.test.tsx:1866`); use-keybindings is one of **5** carriers (`port-census.md:43-47`); (c) "SUPERSEDED-IN-FLIGHT by D30 R5" → the supersession LANDED (`f681d34` W-C + `0328bc0`'s "the S1 pin's comment+name rewritten to the W11 stateless-ladder law (D30 R5's promised delivery)"). |
| §0 BASE mini row (:16) | key census | **P2** | "useKeys: Space/S/[/]/Del/±/0/Home/Esc + the form-control skip + the C1/C16 laws" omits the landed **V/X tool-radio rows** (R24-2 W2, gate-ON: `useKeys.ts:1-2` "+ R24-miniplus (gate ON): V/X = the tool radio's select/transition", :55-70; `useMini.ts:320` `trimTool: 'select'|'roll'|'slip'|'slide'|'transition'`). "428 tests — the R23 seal 355 + the R24-mini nets" happens to match the live count again (428 @ `fb05540`) but via a different composition (W0-W2 + the W2-fix review round; census 147/134/395); the mini's own S9 register (`CORE-SEAMS.md:53`) has not yet re-keyed either. |
| §0 BASE nle-ui row (:17) | yield-set law | **P1** | "The yield-set's THIRD row is PENDING (D30 R6, W-D): `'r'` is in NEITHER yield set today — `useShortcuts.ts:417` maps plain r → ripple tool unconditionally in both worlds (verified live at `fc4cc35`)" → **FALSE at `32abd58`**: `'r'` IS in `engineOwnedPlain` (`useShortcuts.ts:69`) AND `shiftedYields` (:85) — landed by the lockstep commit `2646d80` ("the cross-repo lockstep package wave… (1) useShortcuts yield-set gains 'r'"). The `useShortcuts.ts:417` ripple-tool pin still exact (case 'r' verified :417). Also the row's test count 674→**690** and pin `fc4cc35`→`32abd58`. |
| §0 BASE variants row (:18) | census | **P2** | The grammar claims re-verify SOUND (⌘1-⌘5 incl. ⌘5 FX :326-333; FX tool no-plain-key; Escape exits; `shortcutMap.ts` = **56 rows** re-counted live) — only "1521+ tests" is stale (1,762 it-blocks in flight, R25-W1 landed past the W5d wrap). |
| §0 BASE OT row (:19) | pin + keymap census | **P2** | Pin `ded43c4`/`c15a629`/536 → `55c81c0`/`970948a`/**632** (docs-only commits above the code tip — the same shape as the R24 pin; `git log 970948a..55c81c0` = 4 docs commits). The 28-row map re-verifies EXACT (`use-keybindings.ts:23-81`: 28 rows incl. r/⇧R at :47-49, b=bookmark :81); the M28R ladder rows + M49T/M49C claims unchanged. |
| §0 C22 ledger (:20) | counts | **SOUND + P2 pin** | "53 rows (nle-ui) / 56 rows (variants) of this spec's 181-row Appendix A universe" — re-verified live at both pins (nle-ui `shortcutMap.ts` = 53 `action:` rows; variants = 56); Appendix A = **181 rows** re-counted (grep `^kbd-` = 181). Only the pin labels are stale. |
| §0 GAP K3 row (:23) | status | **P1** | "in flight: the D30 W-C/W-D absorption waves" → **LANDED**: W-C `f681d34` (the wire-dispatch seam + the M28R ladder), W-D `78e7283` (r/⇧R + the Z4 keymap guard) — both verified in the app tree; the app's PLAN now queues the D-ARCH-6 re-pin instead. |
| §0 GAP K3/OT row (:24) | divergence census | **P2 (one clause dead)** | "K and J/L RESOLVED OT-side… the JKL divergence MOVED to the app side (the port still ships tap-accel + K = togglePlay until the D30 W-D adoption)" → the app-side divergence is **CLOSED** (F-1 evidence). The rest re-verifies: **B remains the sole live divergence** (b = bookmark OT :81 + app :81 vs razor/blade per §3.2 — nle-ui :413/variants :442, re-verified live); the **C16 `e.repeat` gate is STILL in neither the OT nor the app-port keymap** (grep `\.repeat` over `opencut-timeline/src/components/timeline/hooks/` + both app port hook files = zero hits — re-verified at `55c81c0`/`c020b2a`; also absent in nle-ui useShortcuts) — the 16:24 hazard claim re-pins, only its grep pins (`c15a629`/`c885ece`) are stale. OT's B law is now a recorded intentional divergence (DECISIONS audit-note **U-14**: "ours-only `B` bookmark keybinding… Intentional fixture affordance, pinned by M24R") — the "silent divergence" half of the disposition is satisfied OT-side. |
| §0 GAP r5/r1/carrier rows (:25-27) | register | **SOUND** | r5 (53/56 of 181) + r1 (no source-edit verb in 15's union — Appendix A carries no `kbd-source-*`/`kbd-slide-*` rows, consistent) + the carrier-reduction "re-verified ABSENT at `c15a629`" (rippleMode still internal; zoom continuous) — no contrary landings found in the range. |
| §0 GAP page-key row (:28) | ⌘3 collision | **SOUND (still pending)** | Re-verified: the variants' `⌘3` = **Deliver** (`useShortcuts.ts:313-317`), §3.8's `⌘3` = "Effects workspace" still collides, and **SPEC-REVISION-CANDIDATES still carries no ⌘3 registration** (grep-empty) — the R23-WA promise "lands in SPEC-REVISION-CANDIDATES at wrap" (`useShortcuts.ts:326-329` comment) never landed. The row stands as written. |
| §0 GAP R5 row (:29) | the M28R adoption | **P1 — flip to DONE** | All three deliverables landed: the port's M28R ladder (`use-keybindings.ts:38-47` + `use-timeline-actions.ts:223-238` — stateless: `core.getPlaybackRate()`/`isPlaying()`, same-direction doubling capped at 4, opposite/paused → 1, K = pause-only, `setPlaybackRate` BEFORE play); ⇧L/⇧J fixed-2× port-local (`use-timeline-actions.ts:268-275`, `shuttle(direction, fixedRate)`); the S1 pin rewrite (`0328bc0`: "P2-2 the S1 pin's comment+name rewritten to the W11 stateless-ladder law (D30 R5's promised delivery)" — the pins at `GluedShell.test.tsx:470/:484`). |
| §0 GAP R6 row (:30) | the r-key yield | **P1 — flip to DONE** | The lockstep pair landed: nle-ui `2646d80` (yield-sets gain 'r', `useShortcuts.ts:69/:85`; the shortcutMap dual-world tool-ripple row) + the app `78e7283` ("K2 un-withheld (r/⇧R loop-from-selection rows; the yield-set carries 'r' now)", `use-keybindings.ts:56-60`); the acceptance pin is live ("r/⇧R engine region + no tool switch", `GluedShell.test.tsx:1850-1851/:1940`). The D28-A5 pattern (one package commit, app re-pin last) executed exactly as the row demanded. |
| §0 GAP Z4 row (:31) | the scrub-row guard | **P1 — flip to DONE** | Both halves landed: **S-package** `755235c` — `e.stopPropagation()` on the scrub row (`Viewer.tsx:296-307`) + ruler root (`Ruler.tsx:233-245`), the `data-transport` marker already at `Viewer.tsx:317`; the acceptance pin at `AppShell.test.tsx:505-517` ("Z4 focused-surface arrows — one key, one owner": ArrowLeft = exactly 1 frame, ⇧ArrowLeft = exactly 10, read live). **S-app** `78e7283` — the `[data-transport]` member in `SURFACE_OWNED_KEYS` (`use-keybindings.ts:139-150`, yielding before binding resolution :211-214, NO shiftKey carve-out per 9-G1 P1-1); the app pin at `GluedShell.test.tsx:1876` ("plain ArrowLeft steps EXACTLY one frame; ⇧ArrowLeft is the row OWN ×10 nudge"). Note the row's premise detail: the scrub row never needed to "gain" `data-transport` (it carried it since R9 `2646d80`); the package half was the stopPropagation leg. |
| §0 GAP D34 row (:32) | source-edit family | **SOUND + P1 clause (→§3.4A)** | The landed-law claims verify (§3.4A/§3.4B/§6.1 #2 + #19/#20 all present; §3.2's boundary sentences; P11's 16-half). The r1 half correctly still pending. BUT the row's own "zero live-behavior risk" echo (via :263) is dead — see §3.4A. |
| §0 ACCEPTANCE (:34) | pins | **P1 (mechanical)** | "app 174; mini 428; nle-ui 674; the OT in-page M24/M28R real-keyboard suites at `ded43c4`, code tip `c15a629`" → app **252 @ c020b2a**; nle-ui **690 @ 32abd58**; mini 428 (in-flight composition, see :16); OT **632 @ `55c81c0`/`970948a`**. |
| §0A (:38-67) | TL;DR + alignment | **P3** | "resolves **18 conflicts**… (12 original + 6 audit-flagged… #13–#18)" ×3 sites (:44, :55, :2061) predate R25's #19/#20 — the §6.1 table has **20 rows**; the three summary sites say 18. The 78-type spec-15 union citation (:61) + the normative-priority rule (:65) verify against 15:14/:320. |
| §1-§2 (:71-107) | purpose/principles | **SOUND** | No contrary landings; the Shift=more/Cmd=global grammar still matches every shipped surface. |
| §3.1 Playback (:129-170) | law + census | **P2** | The binding rows hold. The **R24 live-surface census paragraph (:170)** is half-stale: "The app's port (`c885ece`) still ships the W3 tap-accel ladder… — D30 R5 adopts M28R in the W-D wave (in flight…)" → LANDED (F-1 evidence); "The nle-ui/variants mocks… yield to the port in the engine world (D22b)" still true; "K-then-J/L ½× slow-mo remains unimplemented everywhere (ledger C20)" re-verified true (M28R rate ∈ {0,±1,±2,±4} only). The **JKL audio half**: re-verified SEALED — WDC `ec8fd5c` carries the W2 varispeed surface (`src/lib/daw/varispeed.ts`, RETIME_RATE domain [1/32,32]) and the app composes the transport rate with exactly one reschedule per rate change (`audioService.ts:373-388`, the S3-C4 law: "one JKL rate change = exactly one reschedule"; `seg.varispeedRate ?? 1` at :551). Missing (mock-verdicts V4): the **source-viewer JKL context** — the variants' W1-B law "while the SOURCE viewer is open, J/L/K drive the SOURCE transport instead (fixed ±1×…; K stops the SOURCE transport)" (`useShortcuts.ts:60-94`) is law nowhere in 16. |
| §3.2 Tools (:172-196) | tool census | **SOUND + P2 owner-pointer** | The eight keyed rungs + ⌥R + N + Escape verify against every surface (nle-ui `useShortcuts.ts:412-417`; variants :441-446; 18 §4.5's nine-tool radio incl. rate-stretch toolbar-only). **P2 — :194's "Spec 18 §4.5's tool union is the owner of the seventh member"** (the FX tool): 18 §4.5 (`:202`) lists the nine tools with **no fx member** (grep `fx` in 18 = zero) — the R23-WA registration landed 16's row but the 18-side owner row never followed; the pointer dangles. The mini's `transition` tool (useMini.ts:320) is a mini-surface (R18k/D6-D5), not a 16 §3.2 census member — noted for §0's mini row only. |
| §3.3-§3.4 (:198-257) | selection + editing | **SOUND** | The C2-extension registration (:219) still accurate (the mock's ArrowUp/Down single-level focus only); the slip-vs-nudge law + N15 targeting unchanged; the A1 delete chords + 18 §4.9 alignment verified in nle-ui (`useShortcuts.ts` Delete/Backspace branch). |
| §3.4A (:259-285) | source-edit family | **P1 (one clause) + SOUND law** | The law text is coherent and re-verifies against the corpus (06 §5.9B-§5.9F pins check: §5.9F's acceptance domain [0.1, 5] at 06:1668 ✓; the D34.1 chord math — E free in every corpus keymap: re-verified no `e`/`E` binding in nle-ui/variants/OT/mini keymaps; the F-block + preventDefault law; Shift+F10 unavailable per 18 §4.9 ✓). **The dead clause — :263's phasing note**: "The rows are dormant in every shipped shell by construction (**no source viewer exists** — 18 §8.5 defers dual viewers to v2; zero live-behavior risk)" → **FALSE since the variants' R25-W1** (`a83d4d2`): the source-mode viewer EXISTS (`viewerMode: 'program'|'source'`, `useUiStore.ts:659`, enter/exit :1352/:1358 — a single-viewer mode SWAP, so 18 §8.5's *dual*-viewer deferral technically survives, but "no source viewer exists" does not) and the `,`/`.` rows are **LIVE** there: `viewerMode === 'source'` → `insertMediaAt(sourceMediaId, ','?'insert':'overwrite')` (`useShortcuts.ts:460-475`). The C46 gate itself is implemented exactly as specced — the ratification is vindicated, the risk note is stale. The mock-verdicts V1 rider (the **5s pseudo-duration satisfies the C46 gate** — "a source with duration is loaded" :261) is still unabsorbed (their OQ2). |
| §3.4B (:287-298) | slide-by-frame | **SOUND (spec) / registered code lag** | The XKB-6 trap law + the r1 phasing verify; the registered code lag re-verifies at `32abd58`: nle-ui's `,`/`.` fire `slipNudge` on ANY tool with a selection, un-gated (`useShortcuts.ts:431-432`), and at hardcoded 24fps (`useUiStore.ts:957-959`, `deltaSec: frames / 24`) — the N-4/G-SLIP-5 deviation stands unchanged in both shells (variants :475 same). |
| §3.5 Track ops (:300-318) | mute/lock family | **P2** | (a) The kind-aware law is missing: the shipped `⌘⇧M` reads the **MUTEABLE set** — `isMuteableTrack` (`nle-ui/useUiStore.ts:332-333`: `t.kind === 'main' || t.kind === 'audio' || !t.elements.every(e => e.type === 'text')`), shared by BOTH toggleMuteAll halves (:504 routed, :517-518 mock; text-only/empty overlays never flip — F4-7 `5801079`); §3.5's "Mute all tracks… `<allTracks>.map`" batch-composite is the pre-F4 form. (b) The shipped form is ONE convergence chord (⌘⇧M = set-all toggle, target = `!every(muteable muted)` — `useShortcuts.ts:265`) vs the spec's mute/unmute two-chord split (⌘⇧M / ⌘⇧⌥M); the mock's ⌘M is also extended (any-kind focused track, master fallback — shortcutMap row 'audio-mute'). (c) The wire grew the set-all verbs: `track.setAllMuted`/`track.setAllLocked` (OT `api.ts:199/:201`; the S3 `setTracksMuted` — ONE history entry, A9 `{changed, muted}` echo) — the N-toggleTrackMute batch is superseded by a single set-all command for the rows that have it. |
| §3.6-§3.13 (:320-458) | the remaining tables | **SOUND** | §3.6's fps conversion note, §3.7's A2 marker family (09 §3.1A/15 §4.3.49 alignment verified; `addMarker` canonical), §3.9's export attribution (15 §4.3.74-76 + §13.11/§14.11 — verified at 15:280/:2117 for the project-verb cross-refs), §3.12's spec-15 alignment, §3.13 — all re-verify; the only cross-cutting item is §3.12's `retimeKeyframe` singular (see F-6). |
| §4-§5 (:462-624) | test patterns + multi-select | **SOUND** | No stale pins found in §4/§5 (the patterns are engine-agnostic; §5.1-5.4 unchanged). |
| §6 (:626-679) | conflict table | **SOUND** | The 20-row table + §6.2 order + §6.3 precedence re-verify internally and against the shipped surfaces; only the three "18 conflicts" summary counts (P3 above) predate #19/#20. |
| §7 (:682-727) | a11y + cheat sheet | **SOUND** | The single-source discipline holds in both shells (nle-ui CheatSheet renders the 53-row map; variants the 56-row). |
| §8.1-§8.3 (:729-1512) | implementation | **P3** | The union block (:870-966) reflects the R7-era fixes (setLoop start/end, trim delta, selectElements elements, paste atTime, tool 'roll' — all verified as fixed at :877-:950). The Round-7 note (:67) still accurate: the illustrative resolver bodies still show `engine.command.execute({command: new XCommand})` (e.g. :1098 et al., 10+ sites) — flagged for the seal-round migration, unchanged since. The §8.3 comment's spec-15-only type list (:955-964) is still a faithful 15-mirror (15's D-ARCH-6 fold will touch `retimeKeyframe` — F-6). |
| §8.4-§8.6 (:1513-1729) | input/layout/map | **SOUND** | The R23 live-guard census (:1552) re-verifies: SELECT skip-list ✓, C16 gate still absent (re-grepped), C1 native yield ✓ (mini `useKeys.ts:97-104`), the `[data-modal]`/overlay gate ✓ (app `use-keybindings.ts:191-199` + OT's overlayDepth law), the Alt guard ✓ (nle-ui/variants swallow ⌘/⌥ before the plain-key switch), the focused-playhead arrow ownership ✓ (app SURFACE_OWNED_KEYS `:128-137`). §8.6's multi-descriptor law + the source-edit descriptor phasing (:1563) coherent. |
| §8.7 (:1731-1753) | JKL multi-tap | **P2** | The closure sketch + its R24 census note ("the live implementations compute the ladder STATELESSLY… the app post-D30-W-D") — the note's conditional is now unconditional truth (the app ladder verified stateless, F-1); the sketch is correctly labeled the spec-side form. One-word re-truth. |
| §9-§10 (:1757-2036) | tests + open questions | **SOUND** | §9's recipes/coverage goal unchanged; §10's open questions still open (none contradicted by landings). |
| §11 (:2038-2062) | 05 §19 reconciliation | **SOUND** | The 05 §19.1 re-anchor (":1537-1538 post-R25") + the D34.2 ratification row verify; 05 §11.2's N12 SUPERSEDED note confirmed in the tree (05:724). The "18 conflicts" count here is the third stale site (:2061). |
| §12 (:2065-2131) | manager-method cross-ref | **SOUND + F-6 rider** | Spot-verified against 15 §4.2 + 01 §14 (saveProject/closeProject/rename rows; exportFCPXML chain). The `retimeKeyframe` row (:2120) rides 15's fold. |
| §13 (:2135-2149) | phasing lineage | **P2** | :2141 "LIVED: the port's **24-row** map" — internally inconsistent with §0:15's "25-row" (both now 27) + stale; :2148's K3 gate evidence (S1/S2 at 174, "the M28R-law pins land with D30 W-D") → the pins are live at 252. |
| §14 Appendix A (:2152-2346) | registry | **SOUND** | 181 rows re-counted (grep `^kbd-` = 181); the r1-scheduled absences (`kbd-source-*`/`kbd-slide-*`) correctly still absent; the A1/A6/A8 footers coherent. |
| §15-§16 Appendices B/C (:2350-2418) | impl checklist + test matrix | **SOUND** | Consistent with §12/Appendix A; the "Tests written: 0" K3/r5 framing still honest. |
| §17 code refs (:2421-2433) | engine pins | **P2** | The R24 re-verify note pins `5036387`; engine now `f9ac806` (748/748). Re-verified live: api.ts:793 (`case 'addText': {`) ✓ exact, api.ts:919 (`actions.addKeyframe({`) ✓ exact, run-nle-tests.mjs:167 ✓ exact, page.tsx:5794 (`compositionKeyframe pass=${pass}`) ✓ exact; **player.ts:994-995 → drifted to :999-1000** (`setPlaybackRate(rate: number): void {` / `this._clock.playbackRate = rate;`) and the ratechange cite :606-607 → the event map sits at :196 with the clock-forwarding comments at :603-605. The row verdicts (SPEC-ONLY/ENGINE-GAP/ALIGNED/CORRECTIVE) all stand. |

---

## §3 The fix-list (exact edits, amendment-wave-ready)

**F-1 (P1) — §0's D30 W-D family flips (R5 :29, R6 :30, Z4 :31) + the three BASE rows that carry their pre-W-D state (:15 app, :17 nle-ui, :23 K3).**
- **R5 → LANDED:** *"D30 W-D — R5, the JKL adoption. LANDED (the app's W-C/W-D waves `f681d34`/`0328bc0`, re-pinned `c020b2a`): the port ships the M28R stateless ladder (rate ∈ {0,±1,±2,±4}; same-direction doubling capped; opposite reset; K = pause-ONLY; Space resumes at the persisted rate; setPlaybackRate BEFORE play — `use-keybindings.ts:38-47` + `use-timeline-actions.ts:223-238`); the tap-accel closure is RETIRED (upstream DECISIONS #25 ruling 4); ⇧L/⇧J fixed-2× stay PORT-LOCAL (`shuttle(direction, fixedRate)`); the S1 pin rewritten to the new law, assertions surviving (`GluedShell.test.tsx:470/:484`; K-pause-only pinned :1866)."*
- **R6 → LANDED:** *"D30 W-D — R6, the `r`-key coordination. LANDED as the cross-repo lockstep pair: nle-ui `2646d80` (yield-sets gain 'r' — `useShortcuts.ts:69` engineOwnedPlain + `:85` shiftedYields; the shortcutMap tool-ripple dual-world row) + the app `78e7283` (K2 un-withheld — the r/⇧R loop-from-selection rows, `use-keybindings.ts:56-60`; acceptance pinned `GluedShell.test.tsx:1850-1851/:1940`: engine world — one r-press = one loop-set, no tool change; mock world — the ripple tool fires). The D28-A5 pattern (one package commit, the app re-pin last) executed as specified."*
- **Z4 → LANDED:** *"D30 W-D — Z4, the scrub-row double-apply guard. LANDED, both halves: S-PACKAGE `755235c` — stopPropagation on all four nudge branches (Viewer scrub-row `Viewer.tsx:296-307` + Ruler root `Ruler.tsx:233-245`; the `data-transport` marker itself landed R9 `2646d80`, `Viewer.tsx:317`; the row's own preventDefault stays for a11y); acceptance pinned `AppShell.test.tsx:505-517`. S-APP `78e7283` — the `[data-transport]` member in `SURFACE_OWNED_KEYS` (`use-keybindings.ts:139-150`, no shiftKey carve-out per 9-G1 P1-1); acceptance pinned `GluedShell.test.tsx:1876`. One ArrowLeft at the scrub row = exactly one frame step — verified in both repos."*
- **:15 (b) clause:** replace the "SUPERSEDED-IN-FLIGHT… at `c885ece` the port still ships tap-accel + K = togglePlay" tail with *"SUPERSEDED (landed — the R5 row below)"* and re-pin the row head to `c020b2a` **252/252** (8 suites; GluedShell 104 + audioService 42 + sceneBridge 41 + deliver 20 + persistence 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9); "the 25-row port map" → "the **27-row** port map (OT's 28 minus the 3 undo rows **plus the r/⇧R pair**)".
- **:17:** strike "The yield-set's THIRD row is PENDING…" → *"The yield-set's third row LANDED (`2646d80`): 'r' sits in `engineOwnedPlain` + `shiftedYields` (`useShortcuts.ts:69/:85`); the ripple tool keeps r in the mock world only."* Re-pin `32abd58` **690/690**.
- **:23:** "in flight: the D30 W-C/W-D absorption waves" → *"landed (W-C `f681d34`, W-D `78e7283`)"*.

**F-2 (P1, mechanical) — the pin/count family.** :4 (header, append the R27 round note), :19 (OT → `55c81c0`/`970948a` 632/632; the 28-row map pin re-anchored to `use-keybindings.ts:23-81`), :34 (ACCEPTANCE → app 252 / nle-ui 690 / OT 632), :2141 ("24-row map" → 27-row), :2148 (the K3 gate evidence re-pinned), :2398 (the R23 status note's suite census: GluedShell S1/S2 pins at 252; nle-ui 690; variants 1,762 in flight), :2423 (§17's "R24 re-verified at `5036387`" gains the R27 note + the two drifted line pins, F-9).

**F-3 (P1) — §3.4A:263's dormant-shells clause.** Replace *"The rows are dormant in every shipped shell by construction (no source viewer exists — 18 §8.5 defers dual viewers to v2; zero live-behavior risk)."* with: *"Live in the variants since R25-W1 (`a83d4d2`) — the source-MODE viewer (`viewerMode: 'program'|'source'`, a single-viewer swap per 18 §8.5's dual-viewer deferral) carries the C46 gate exactly as specced (`useShortcuts.ts:460-475`: source mode → `insertMediaAt` insert/overwrite; the SourceEditBar contract, 18 §4.3/D35.4); dormant in nle-ui, the app port, and OT (no source mode). The R25 ratification is vindicated by the live implementation."* In the same edit, add the mock-verdicts V1 rider to :261's gate text: *"a source with duration is loaded — a still's 5s pseudo-duration satisfies the gate (mock-verdicts V1/OQ2; 09's stills caveat gains the pinned default)."*

**F-4 (P2) — §3.2:194's dangling FX-tool owner.** Replace "Spec 18 §4.5's tool union is the owner of the seventh member" with: *"the seventh member's owner row in 18 §4.5 is still PENDING — 18 §4.5's nine-tool cluster carries no fx member (the R23-WA registration landed here only); the 18-side amendment adds it when the FX-page cross-cut lands."* (Cross-spec note X-1.)

**F-5 (P2) — §3.5's mute family absorbs F4-7 + the set-all verbs.** (a) Rewrite the `Cmd+Shift+M` row's command cell: *"Mute/unmute all **muteable** tracks — the kind-aware set (`isMuteableTrack`: main | audio | non-text-only overlays; text-only/empty overlays never flip — the F4-7 law, nle-ui `5801079`), as ONE set-all convergence (target = `!every(muteable muted)`)"*; note the mock's single-chord ⌘⇧M implements the convergence (the spec's separate ⌘⇧⌥M unmute row stays as the explicit-set alternate). (b) Add the wire note to §3.5's conflict-notes paragraph: *"the set-all wire verbs exist — `track.setAllMuted`/`track.setAllLocked` (OT `api.ts:199/:201`; ONE history entry, the A9 `{changed, muted}` echo; superseding the N×toggleTrackMute batch composite for the rows that have them)"*. (c) ⌘M's row gains the shipped extension: "any-kind focused track; master fallback when nothing focused (the mock's form)".

**F-6 (P2) — the `retimeKeyframe` singular rides 15's D-ARCH-6 fold.** §3.12 (:439-440), §8.3 (:914), §12 (:2120), Appendix B (:2379): add one rider note at §3.12's spec-15 alignment note (:446): *"D-ARCH-6 (OT s17): the wire's singular `retimeKeyframe`/`removeKeyframe` RETIRED — the batch `retimeKeyframes` (one history entry) is the wire form; 15's union fold re-keys this spec's mirrors (`retimeKeyframe` → the batch form) when it lands. NO keyboard row needs a wire-origin re-key: 16 speaks the spec-15 union, not the OT wire; the D-ARCH-6 gesture-seam switches (`core.removeElementKeyframes` `use-timeline-actions.ts:452` / `core.retimeElementKeyframes` `use-keyframe-drag.ts:96` — still the direct-core singulars, the filed consumer duty) are pointer-gesture seams, not keymap rows."*

**F-7 (P2) — §3.1 gains the source-viewer JKL context (mock-verdicts V4).** One sentence after :170's census paragraph: *"Source-mode context (mock-verdicts V4, the variants' W1-B law): while the source viewer is open, J/L/K drive the SOURCE transport (fixed ±1× — the program shuttle's ladder stays program-only; K stops the source transport; the program playhead never moves under the open source monitor — `useShortcuts.ts:60-94`); a §6.2 step-3 panel context, the same gate family as §3.4A's C46."*

**F-8 (P2) — §0:24's K3/OT row re-truth.** Replace "the JKL divergence MOVED to the app side (the port still ships tap-accel + K = togglePlay until the D30 W-D adoption — the R5 row below)" with "the app-side K divergence CLOSED with the W-D adoption (R5, LANDED)"; re-pin the grep sites to `55c81c0`/`c020b2a`/`32abd58` (the C16 gate's absence re-verified at all three); add "OT's B law is now a recorded intentional divergence (DECISIONS U-14) — the R10(k) ask is satisfied OT-side for B; the C16 gate remains the row's live hazard."

**F-9 (P2) — §17's engine line pins.** player.ts `:994-995` → `:999-1000`; the ratechange cite `:606-607` → `:196` (the event map) + `:603-605` (the clock-forwarding comments); append the R27 re-verify note at `f9ac806` (748/748; the other four citations exact).

**F-10 (P2) — §0:16's mini row.** Add the V/X rows + the in-flight census: *"R24-2 W2 landed the tool radio: V/X = select/transition (miniPlus-gated, `useKeys.ts:55-70`; `trimTool: 'select'|'roll'|'slip'|'slide'|'transition'`, `useMini.ts:320`) — X is a NEW corpus key (unbound in Appendix A); in-flight count 428 @ the `fb05540` re-pin (census 147 units / 134 GAP / 395 authored; the mini's own S9 register re-keys at its round-WRAP)."*

**F-11 (P3) — the conflict-count summaries.** :44, :55, :2061: "18 conflicts… (12 original + 6 audit-flagged… #13–#18)" → "20 conflicts (12 original + 6 audit-flagged #13–#18 + the R25/D34 rows #19–#20)".

**F-12 (P3, registered, no edit needed) — §8.3's `execute()` bodies** stay flagged for the seal-round migration (:67); re-confirmed unchanged.

---

## §4 Cross-spec notes (for the cross-cut wave — NOT this spec's edits)

1. **18 §4.5 lacks the FX-tool owner row** (F-4): the ninth-... tenth member never landed in 18; 18 §5's normative gesture table also lacks the ripple/slip/slide TOOL-gesture rows (mock-trim F-6/N-3 — trim + roll only at 18:264-265). Both belong to 18's amendment; 16's §3.2 census is otherwise current (the mini's `transition` tool is a mini surface, not an 18 §4.5 census change).
2. **15's D-ARCH-6 fold re-keys 16's keyframe mirrors** (F-6): `retimeKeyframe` singular → the batch form, union count 78 → 78+N. 15's own "24 routed + 6 exceptions" census family is the fleet P1 already filed (scout-app/scout-ot).
3. **05 §8.1's stale ToolMode enum** (mock-trim #22/N-7a): the four-tool listing vs 16 §3.2/15 §4.3.45/18 §4.5's nine — the fix re-points to 16 §3.2 as the key home.
4. **09's stills 5s pseudo-duration default** (mock-verdicts V1): 16's C46 rider (F-3) is its keyboard-side companion; 09:25 is the ruling's home.
5. **18 §4.3's SourceRangeBar/A1 amendments** (mock-verdicts V1/V4) are the surface-side twins of F-3/F-7.
6. **The C16 e.repeat gate's app-side re-expression (K3)**: the gate is live in the mini only; the app's S split + OT's s remain machine-gun-able on key-hold — the 16:24 hazard is now one W-C-era merge away from being fixable in the canonical keymap (the port file is a documented carrier).

---

## §5 What 16 uniquely sees (the keyboard-lens findings)

1. **The D30 W-D trio is this spec's private register — and it all landed.** No other spec carries R5/R6/Z4; 16's §0 GAP register is the only place they live, and all three flipped DONE in one app+package lockstep sequence (`2646d80` + `f681d34` + `78e7283` + `0328bc0` + nle-ui `755235c`). The audit's headline: the spec's last three "in flight" rows are finished work needing only the register flip (F-1).
2. **The keymap-lockstep law is now a proven cross-repo pattern** (the third execution of D28-A5): the r-key flip needed one package commit + one app re-pin, exactly as §0:30 specified — the spec's own acceptance criteria ("engine world — one r-press = one loop-set (no tool change); mock world — the ripple tool fires") are pinned verbatim in `GluedShell.test.tsx:1850-1851`. The r1 keymap wave (the source-edit rows + the variants' map) should cite this as its template.
3. **The five-surface census's honest ledger:** nle-ui 53 / variants 56 / OT 28 / port 27 / mini ~15 rows — of the 181-row Appendix A universe, the implemented-set claim (53/56) re-verified exactly; what changed is the SURFACE COUNTS below the cheat-sheet rows (the port +2, the mini +2 gate-ON).
4. **The C16 auto-repeat gate is the last live hazard in the canonical keymap family** — re-verified absent at all three R27 pins (OT/app/nle-ui; live only in the mini). A held S in the app machine-guns splits today.
5. **The `,`/`.` four-meaning ladder is now HALF-live**: source-mode (the variants, C46-gated ✓ as specced) and the un-gated slip fallthrough everywhere (N-4: both shells fire slipNudge on any tool, hardcoded 24fps in nle-ui) — the spec law is ahead of every implementation except the source gate.
6. **No keyboard row needs the D-ARCH-6 wire-origin re-key** (F-6) — the gesture-seam switches are pointer-only; the singular retimeKeyframe is a 15-mirror that rides 15's fold.

— end of report —
