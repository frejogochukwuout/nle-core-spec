# R27-W3 — spec-08 (color grading) per-section audit

**Task ID:** R27-W3-08 | **Agent:** spec-08 | **Round:** R27 final-tightness, Wave 3 (per-spec fleet)
**Spec:** `nle-core-spec/08-color-grading.md` (2,520 lines, read in full, section by section).
**Ground-truth applied:** the R27 pin world (BRIEFING.md) + `audits/fleet-r27/mock-color.md` (the key input) + `mock-variants.md` + `mock-verdicts.md` + the mock's color code (`ui-mock/shell-variants/src/lib/color/` + `src/components/pages/color/`, live-read) + the engine @ `f9ac806` (live-verified). Every claim carries file:line. Research-only.

---

## §1 The per-section table

| § | Content | Verdict | Evidence (the deciding facts) |
|---|---|---|---|
| §0 | Forward inventory | **P1** | The W-B row (08:16) states THREE clauses that are false since R24-W2: (a) "ScopesDock … in the timeline-area console row next to the MixerDock" (it is the ~160px pane UNDER the viewer, ColorPage.tsx:3-19/AppShell.tsx:449); (b) "ColorNodeGraph (the nodes-in-viewer surface, D-B2 — the AppShell swaps the viewer for the graph… ruling 14)" (region [2] is ALWAYS viewer-led since W2 `0196c22`; the graph is console-row slot [6]); (c) "the `timelineCompact` auto\|on\|off density toggles (D-B3)" (dead since W1's ViewOptionsPopover). The four mock-color P1 gaps live HERE as missing rows: the venue table, the node-graph execution model, the C50 ruling (only a pointer, 08:25), the wheels gesture law. Plus stale pins/counts: engine `5036387` 458/458 (live: `f9ac806`, 748/748); nle-ui `fc4cc35` 674 (live: `32abd58`, 690); app `c885ece` 174 (live: `c020b2a`, 252); "1,521+ tests live" (live: 1,733 it-blocks/64 files, wrap `3db130f`); lib/color "167 dedicated tests… colorSpace 31 / gradedImage 26" (live: **170** — colorSpace 33, gradedImage 27; gradeMath 56 / qualifierMath 28 / scopesMath 20 / index 6 hold); "ColorPage 33 / ScopesDock 20 / StillsPanel 14 / mockGrades 18" (live: **43 / 22 / 19 / 21**; ColorInspector 10, GradedViewerCanvas 15 hold). Also 08:16's "gap C59" dangles: REFERENCE-REGISTER's C-ledger ends at **C58** (register :85) — C59 lives in the MOCK's own deviation ledger; say which ledger. |
| §1–§3 | Purpose / goals / inventory | **SOUND** | 08:59 carries the 33ms law (goal 4) — the law the mock's commit-on-release violated and A3 restores. The 19-effect inventory + FreeCut refs are reference-fidelity claims (verified by SCOUT-08; engine 44-id registry = 43 pipeline + 1 lut re-counted live at `f9ac806`: `rg -c "id: 'gpu-"` → 43+1 ✓). |
| §4.1 | The 4-wheel model | **SOUND** | Matches the mock's WHEELS meta (WheelsPanel.tsx:69-74) and GradeParams (gradeMath.ts:42-70). |
| §4.2 | The wheels shader (our port) | **SOUND** — byte-coherent with the mock | Full re-derivation done: the mock's `gradeInto` (gradeMath.ts:301-447) reproduces the §4.2/§17.A-port shader step-for-step — BT.709 masks at 0.0/0.18/1.0 (:309-312 ≡ 08:190-195), tints sh→mid→hl→off with the same `amount < 0.001` guard (:316-339 ≡ 08:179-200), temp/tint coefficients 0.1/0.05 (:342-348 ≡ 08:202-208), exposure/contrast-pivot/midDetail/CDL-levels/points/zone-adds/sat/colorBoost/hue-rotate/lumMix (:350-438 ≡ 08:210-240), NO output clamp (:440-441 ≡ 08:242). `wheelTintChannel(c,t,a,m) = c + (c·lerp(1,t,a) − c)·m` (gradeMath.ts:141-144) is algebraically identical to `mix(color, color * mix(vec3f(1.0), tintColor, amount), mask)` (08:182). The parity oracle is safe to bind at r3. |
| §4.3 | Wheels UI | **P1** | No gesture law exists; the text describes FreeCut's absolute puck as the model — "puck (drag to set hue+amount), master ring gauge (dock only), R/G/B value chips (dock only), thumb wheel for the scalar" (08:262-264), `getHueAmountFromClient` "verified" (08:277), and Q9: "**We adopt FreeCut's UI as our reference**" (08:956). The mock still implements exactly that absolute model (WheelsPanel.tsx:147-174 — `applyPuck` computes absolute angle/len, `commitPuck` commits once on pointer-up; header :12-14 "commit on pointer-up — ONE undoable setGrade per drag, the D3 law") — which R25-A3 rules WRONG (r25-reaction-round.md:202-213) and which already violated 08:970/08:59's 33ms live law (mock-verdicts V12's CONFIRMED half). → Amendment A (below). |
| §5.1–§5.2 | Curve model + LUT bake | **P1 — the composition-order conflict (ruling-blocker)** | 08:299-300: "out = channel(master(x)), so the master curve is applied first, then per-channel" + the bake code `evaluateMonotoneCurve(redPoints, master)` (08:330-332) = **ch∘master**. The mock — named "r3's parity reference" by 08:15 — ships and tests the OPPOSITE: `lut.r[i] = srgbDecode(y(r(i/255)))`, "the channel curve first, then the luma curve on top, Resolve's Custom Curves order" (gradedFrame.ts:83-108), pinned by gradedFrame.test.ts:84 ("THE COMPOSITION ORDER: the Y curve applies to all three rgb channels AFTER each channel's own curve (y∘r, not r∘y)") and :187. §19 pins NO order at all (no dual-curve test exists — see §19 row). → the D37+ ruling proposal (below). Secondary (P2): the LUT-domain mismatch — the GPU port samples an encoded→encoded LUT with shader-side conversions (17.B-port :1407-1419); the mock bakes encoded→**linear** LUTs (srgbDecode baked in, 256-entry, 8-bit output quantization, gradedFrame.ts:100-117) — same law, coarser bake; the r3 parity gate must budget/widen it. |
| §5.3 | Curve shader | **SOUND** | The width-literal note (08:395) verified verbatim in the quoted source (17.B :1371). |
| §5.4 | Curves UI | **P2** | Describes FreeCut's panel only (08:399-401); no YRGB model, no histogram-behind-grid law, no gesture grammar (mock-variants §2 row 10 — the R24-W2 A2-R4 rebuild: CurveSet {y,r,g,b}, [Y\|R\|G\|B] radiogroup, ~64-bin histogram at the 10fps family throttle, 8% near-band insert, lost-pointercapture commit; curveMath.ts:5-37/:96-117, CurvesPanel.tsx:62-99/:176-188). |
| §6 | Levels | **SOUND** | §17.C-port's encode-domain pow is the convention; no conflicts found. |
| §7 | 3D LUT | **P3** | The 16-bit port + the sRGB-bug fix are correct and complete (08:468-534, §17.D-port). Registration note only: the mock's LUT select is honest display-state with fake option labels (WheelsPanel.tsx:454-479) — register that the options are labels, not LUTs (mock-color row 11). |
| §8 | Qualifier | **P2** | The §17.E-port is verified faithful (mask on sRGB-encoded, correction in linear — the mock's qualifierMath.ts:6-14 implements the same domain law; the 709-on-encoded vs 601 deviation documented). Missing: (a) the **matte-finesse out-ruling** — the reference's 14-field block (qualifier_ui.html :570-727) was dropped mock-side as spec-less (QualifierPanel.tsx:14-16); 08 §8 must carry the explicit v1 OUT-ruling (the §16.E polygon precedent) or the r1 port will guess; (b) the **eyedropper seed law** — 3×3 average, hueCenter=h·360, sat ±0.08, luma ±0.06 — is mock-only law, now test-pinned (gradedFrame.test.ts:243-252) and needs the spec home + the tiny-readback venue (copyTextureToBuffer 3×3); (c) the BT.709-luma-on-encoded choice (mock) vs §17.E-port's `luminance(encoded)` — consistent ✓, but state it. |
| §9 | Power window | **P2** | Missing the three rulings mock-color filed (row 8): the venue statement as law (fragment inline v1; polygon+JFA v2 — implied by §9.2/:602 + §16.E but never stated as THE venue law), the missing UI-reference decision (no mock surface exists; FreeCut's `gpu-power-window-panel.tsx` is the only reference), and the **tracking OUT-ruling** (no tracker anywhere in the corpus — must be ruled out of v1 explicitly). P3 rider: §9.1's default table lists FreeCut's `exposure=0.3` quirk (08:596) — a fresh window would brighten by 2^0.3≈1.23×; the engine port defaults 0 (pipeline.ts:1991); state OUR defaults (0), not the reference's. |
| §10 | Other effects | **SOUND** | No conflicts; the vibrance/contrast/temperature notes check out against the ports. |
| §11 | Scopes | **P2** (three riders, two SPEC-LAW-NEEDED) | The math + §18 bit-depth port are SOUND (verified against the mock's scopesMath/scopeDraw incl. the 10fps throttle — ScopesDock.tsx:80-81 `SCOPE_THROTTLE_MS=100`). Missing law: (a) **the playhead-frame trigger** — R25-A2: "scopes react to the PLAYHEAD FRAME (always-on analysis, not selection)" (r25:192-193); §11 (08:748-812) specifies math + throttle but never the analyzed frame (mock-verdicts V9); (b) **the measured signal** — pre/post-grade is unruled: the mock measures the post-grade 8-bit display buffer (gradedFrameBus + scopesMath.ts:1-2 — Resolve behavior); 04 §16.5 says only "reads from working linear-light texture" (04:2106) — neither says post-GRADE; 08:17 fixes only the data source at r3; (c) **the readback-vs-compute venue** — §11.2's compute+render architecture implies zero readback but never states it as law (the mock's canvas2d painters + ≤10k stride cap are the CPU fallback, not the production venue). |
| §12 | Real-time strategy | **SOUND** | Realized 1:1 by the mock (gradedImage.ts:204-287 cache+coalescer; GradedViewerCanvas.tsx:190-223 rAF-coalesced re-grade); the identity-skip the mock cites as "§12.1 no-op rule" (gradedFrame.ts:151) is implied by §12.1 items 1-2 (08:820-824) — give it explicit wording when Amendment B lands. |
| §13 | Open questions answered | **SOUND** on content | Q1-Q8 hold. Q6's R24 note pins `fc4cc35` (stale, P3). Q9's "We adopt FreeCut's UI as our reference" (08:956) is the §4.3 P1's citation — A3 supersedes it for the INPUT model (FreeCut's panel stays the ANATOMY reference). |
| §14 | Test intent list | **SOUND** | Test 9 (33ms, 08:970) is the law the mock violated; test 5's curve swap is domain-agnostic. |
| §15/§15A | Engine code refs | **P3** (2 of 8 line pins drifted at the R27 pin) | Live @ `f9ac806`: `wheelTint` :1919 ✓, `registerEffects({` :4595 ✓, `luminance601` usage :1930 ✓, the rgba8unorm data-texture site :4850 ✓, `runEffectChain` :5179 ✓, `GPU_EFFECT_REGISTRY` :3039 ✓ — all HELD; player.ts's `enabledEffects` :1656 → **:1673** (player.ts now 3,695 LOC); headless/api.ts wire strings :1509/:1515 → **:1529/:1535**. The substance holds: 46 `rgba8unorm` sites/10 files (re-counted live ✓), `effects/` = pipeline.ts+lut.ts ✓, no scopes module ✓, the wire trap unchanged ✓ ('gpu-power-window' :1529, 'gpu-secondary-qualifier' :1535 in `GPU_EFFECT_TYPES`), 44-id registry ✓. |
| §16 | Corrections (A–K) | **SOUND** | §16.A is the verified (hue,amount)+scalar model — correct, and the reference point for the A3 OPEN decision (below). §16.E's polygon out-ruling is the precedent the §8 matte-finesse ruling should copy. |
| §17 | Full shader quotes + ports | **SOUND** | §17.A-port ≡ the mock's gradeInto (verified above); §17.B/C/D/E/F/G/H ports are internally consistent with their §-sections. |
| §18 | Scope bit-depth port | **SOUND** | The three-part port + the YCbCr-on-encoded note (08:2057) + §18.4's 8-bit-canvas-is-fine law all hold; the mock's scopesMath implements the same BT.601 vectorscope + densityAlpha family (scopesMath.ts:121-230). Cross-spec P3: 04 §8.5's "255.0 → 1023.0 (10-bit)" shorthand (04:667) disagrees on its face with §18's real per-scope law (×65535 weights, 1024 bins, render re-scale) — 04 should point here (mock-color row 10). |
| §19 | Testing (the executable contract) | **P1** + P2s | **P1 — the packing test contradicts §4.2/§16.A:** `color-wheels-uniform-packing-28-f32-112-bytes` (08:2173-2180) lists `liftHue, liftAmount, gammaHue, gammaAmount, gainHue, gainAmount … wheelRotation, hueRotation, satMix, lumMix` — the SEED-SPEC naming §16.A explicitly rules WRONG — and omits `blackPoint, whitePoint, offHue, offAmount, midDetail, colorBoost, shadows, highlights, _pad1, _pad2`. As written the test cannot pass against the `color.ts:591-599` layout it cites; the mock's `packWheelsParams` (gradeMath.ts:217-248) follows §4.2 correctly. **P2 (a):** NO dual-curve composition test exists — the executable contract pins no order (the D37 ruling's test ask). **P2 (b):** `curves-master-channel-affects-luma-only` (08:2256-2261) — the neutral-bar assertion cannot distinguish "master applies to all three channels" (what §5.1 and the mock both implement) from "luma-only master" (a third semantics the prose asserts: "operates on luminance only… not on the RGB channels"). **P2 (c):** the UI test `color-wheel-pointer-drag-issues-update-effect-command` (08:2391-2399) pins the FreeCut absolute puck formula — it must be rewritten with Amendment A (the relative model's Δ→accumulation + live preview), else the A3 amendment lands with a contradicting test. |

**Tally: P1 ×4 sites (§0's stale W-B text + the four missing laws it should carry; §4.3's absent gesture law; §5.1/§5.2's composition-order conflict; §19's packing-test field list) · P2 ×7 (§5.2 domain, §5.4, §8, §9, §11, §19's three riders) · P3 ×6 (§7 labels, §9 defaults, §13.Q6 pin, §15A drift, §18↔04 shorthand, §0's counts/pins).**

---

## §2 The fix-list (exact edits)

### F1 — P1 amendment A: the wheels gesture law (new §4.3a; supersedes 08:262-264/:277/:956's input-model claims)

> **§4.3a Wheel input model (the R25-A3 law; FreeCut's panel remains the ANATOMY reference — layout/descriptors — not the input model):**
>
> Disc drags are **relative and accumulating (trackball-style)**: grab anywhere inside the disc; the value accumulates pointer deltas (`amount += k·Δrim`; hue advances by the tangent direction), clamped at the rim (amount |v| ≤ 1; hue wraps mod 360). **Live preview during the drag** — every accumulated delta re-grades the target within the §2 goal-4 latency law (§14 test 9's 33ms); there is no commit-on-release *deferral of the preview*. **One undo entry per gesture**: the drag buffers transiently and commits a single `updateEffect`/`setGrade` on pointer-up or lostpointercapture (the preview rides the transient buffer at full frame rate). Center = neutral; crossing to the opposite side passes through the complementary hue (continuous; an optional ≤3% dead zone at center).
>
> **Master luma:** a horizontal dial below the wheel (left darker / right lighter; YRGB move together) + **Ctrl/Cmd+drag inside the disc = master adjust** + **Shift+drag = absolute jump** (the reachability escape hatch).
>
> **Resets:** double-click the disc = **color-only reset** (hue+amount → neutral; the master/luma scalar survives — the explicit exception to 18 §5A's blanket double-click reset); the per-wheel corner button = full reset (color + master).
>
> **Readouts:** 4 YRGB numeric fields per wheel, live during the drag. Editability per the OPEN decision (F2). Hue-ring graticule: vectorscope orientation (red upper-left, 60° spacing).

Companion edits: (i) 08:956 (Q9) gains "input model superseded by §4.3a (R25-A3); FreeCut's panel remains the anatomy reference"; (ii) §19's :2391-2399 UI test rewritten to the relative model (drag by Δ produces an accumulated delta + a live preview commit; one undo entry); (iii) 18 §5A:301 gains the one-line exception cross-ref (mock-verdicts V14).

### F2 — the OPEN decision to file BEFORE F1's prose lands (mock-variants rec 3; the A3-vs-§16.A model question)

> **OPEN decision: A3's "4 YRGB numeric fields per wheel, live, EDITABLE" vs §16.A's verified (hue, amount)+scalar uniform.** The 28-f32 layout (§4.2/§16.A) has no per-channel fields to edit. Options: (a) NARROW — the fields are derived read-only displays (the mock's current `yrgbReadout`, WheelsPanel.tsx:20-24); (b) **WIDEN-via-projection (recommended)** — per-wheel YRGB editing is a UI-side solve onto the existing uniform (Y writes the scalar; R/G/B solve (hue, amount) from the RGB delta via the luma-neutral `wheelDelta` derivation, gradeMath.ts:146-155) — honors "editable" with ZERO shader/uniform change; (c) true param widening — a §4.2 breaking change (new uniform, §17.A-port regen, packUniforms rewrite). The ruling must land before the sibling's W3 wheels rewrite and before r3 parity fixtures are cut.

### F3 — P1 amendment B: the node-graph execution model (new §20; C56's closure)

> **§20 Grading node graph — data + execution model.**
> **Data model:** a grade target owns an **ordered node list**. Targets: a clip (its list) and the timeline (one shared list applied after every clip's list — the F4 sequential law). Node: `{ id, kind: 'wheels' | 'curves' | 'qualifier' | 'window' | 'lut' | 'fx', enabled, label, params }`. v1 is **serial only** — no parallel branches, no mixer (the reference's 2-in/1-out mixer and the Water/Tilt/Lens cards are v2 topology; the mock renders them as display state, ColorNodeGraph.tsx:64-121/:271-284, gap C56). Soft UI cap 8 nodes/target (the reference topology's count).
> **Execution model:** the node list maps to **N sequential fragment passes over the scene-linear `rgba16float` working texture** (04 §5.2 ping-pong), inside 04 §4's "all grading math happens here" box (04:145), one fullscreen-quad draw per node: uniform write (112B wheels / 64B qualifier / 64B window) or data-texture bind (curves 1D LUT §5.3 / 3D LUT §7.3). Qualifier ∩ window composition stays the §9.3 v2 chain.
> **No-op law (explicit):** identity nodes are skipped at build time (§12.1 — an untouched list costs nothing; the mock's `buildGradeStack` identity-skip, gradedFrame.ts:146-157, is the reference).
> **Keyframing:** OUT of v1 — per-node keyframes require a param-over-time model no spec owns; register as the follow-up if asked.
> **Stills interop (C59):** once node lists exist, a still captures the whole list (the mock's clip-level still is the v0 reference; StillsPanel.tsx:96-135).

### F4 — P1 amendment C: the C50 ruling (new §21; promotes 08:25 from pointer to law)

> **§21 Grade persistence + the sequential law (C50 — the ruling).**
> **Homes:** clip grades are the EXISTING per-element effect records (15 §4.3.52-56's addEffect/updateEffect/removeEffect/toggleEffect; a `colorWheels`+`curves`+`lut` stack on the element). The **timeline grade** is one scene-level `GradeRecord` (`{ wheels?, curves?, qualifier? }`) in the project model (09-side field — cross-spec note) that applies to **every video element in the timeline, after that element's clip grade** — never "one track" (Resolve has no track grade; R25-A2's copy law).
> **The sequential law (normative):** frame grade = `[clipGrade] → [timelineGrade]`, **each a full record application in sequence — NOT a params merge** (order matters: clip gain 2 + timeline lift 0.05 ≠ one merged record; pinned by gradedFrame.test.ts:140/:156). The curve LUT + qualifier node of each pass compose right after its §4.2 pass (gradedFrame.ts:169-176).
> **Boundary (do not conflate):** this linear per-element sequential pass is NOT D29.5c's consumer-side CSS-filter final pass (the nle-ui `SceneGrade` preview — a different domain, compose-then-filter on the display canvas; 08:24). Both stand.
> **Persistence round-trip:** records serialize with the scene; load REPLACEs, never merges; one undo entry per gesture (§4.3a).

### F5 — P1 amendment D: the venue law (new §0 D-row, cross-linked 04 §8 — the user's "to what extent" answer, codified)

> **Venue law (what runs where):** All per-pixel color math — the wheels' 14-step grade, the curve-LUT hop, the 3D LUT, the qualifier, the power window, tone mapping — runs as **WebGPU fragment passes over the scene-linear `rgba16float` working texture** (04 §4's box), one pass per grade node, ping-ponged (04 §5.2), between composite and display transfer; clip→timeline (and future node-graph) grades are **sequential full passes, never params merges**. Curve and 3D-LUT data are **CPU-baked at edit time** (1024-entry 1D / 33³ 3D, 16-bit) and uploaded as textures — never evaluated per pixel on the CPU. **Scopes are compute-shader reductions over the post-grade signal with `linear_to_srgb` before binning** (§18), rendered **on-GPU** (graticule + trace render pass; zero readback), throttled to 10fps (§11.4); readback of the small u32 accumulators is permitted ONLY as the canvas2d-drawing fallback. The **eyedropper's 3×3 seed** is the one mandatory CPU readback (a 3×3 `copyTextureToBuffer`). UI gesture state (the §4.3a trackball law, transient buffers, one undoable commit per gesture) is CPU-side and reaches the GPU **only through the 112-byte uniform write** per committed grade. The mock's `src/lib/color/` CPU implementation is the **parity oracle** (≤1 LSB-equivalent, shared fixtures, CI), not a production venue; there is no production CPU render path (WebGPU-only, master Decision 4).

### F6 — the D37+ ruling proposal: the curve composition order (P1 blocker; researched this round)

> **The conflict:** 08:299-300 + :330-332 say `channel(master(x))` — master first (FreeCut `evaluateGpuCurvesEffectChannel`, gpu-curves.ts:302-312). The mock — the named r3 parity reference (08:15) — ships and tests `y∘r / y∘g / y∘b` — channel first, luma on top (gradedFrame.ts:83-108; gradedFrame.test.ts:84 "y∘r, **not r∘y**", :187). §19 pins no order at all.
> **Research state (2026-09-14, this audit):** Resolve's internal order is **not publicly documented** — BMD's manual describes the custom curves as independent Y+RGB adjustments with no composition statement (the R24-A2 research base, r24-audit-round.md:106-111); three web probes (BMD forum, tutorials) returned no authoritative source. The mock's "Resolve's Custom Curves order" claim (gradedFrame.ts:85) is **unverifiable as of this audit**. FreeCut's order is documented in code but is a reference implementation, not a Resolve contract — we already deviate from FreeCut on 8-bit LUTs (§16.B/C), the sRGB bug (§7.4), and clamps (§4.2).
> **Proposed ruling — D37: adopt y∘ch (channel first, luma last).** Grounds: (1) the parity corpus pins it — the mock's curve/stack tests are the r3 gate's fixtures; flipping the mock churns the corpus for zero functional gain, keeping it costs a two-line spec edit; (2) it composes with the sequential-pass family ([clip→timeline] — later passes see earlier output; the luma curve is the final tonal authority over the channel-balanced signal); (3) FreeCut's order carries no counterweight beyond being the reference's behavior. **Edits:** 08:299-300 → "out = master(channel(x)) — the channel curve is applied first, then the luma/master curve on top (y∘r / y∘g / y∘b)"; 08:330-332's bake: `evaluateMonotoneCurve(masterPoints, evaluateMonotoneCurve(redPoints, x))` + comment; §17.B-port's LUT-semantics comment. **Spec-text hygiene:** do NOT assert "Resolve's order" in spec text absent a source — cite our own order and grounds. **Open verification (non-blocking):** a live Resolve dual-curve probe may confirm/refute; if it contradicts, re-open D37 before r3 fixtures ship. **Test ask:** §19 gains the dual-curve test: "a y curve mapping 0.5→0.7 + an r curve mapping 0.5→0.4 → the red output at input 0.5 = y(0.4) (≈ the y curve evaluated at 0.4), NOT 0.4 evaluated at y(0.5)". **Parity-fixture rider (P2):** the GPU port samples an encoded→encoded LUT with shader-side linear↔sRGB (17.B-port); the mock bakes encoded→linear (8-bit output quantization, 256 entries); the r3 gate must compare in the encoded domain and either widen the mock's bake (256→1024, `Math.round(out*255)`→float) or budget the 8-bit hops inside the ≤1-LSB-equivalent gate.

### F7 — P1: §19's packing test rewrite (08:2173-2180)

Replace the field list with the §4.2 `WheelsParams` verbatim: `shHue, shAmount, midHue, midAmount, hlHue, hlAmount, temperature, tint, saturation, exposure, contrast, pivot, lift, gamma, gain, offset, blackPoint, whitePoint, offHue, offAmount, midDetail, colorBoost, shadows, highlights, hue, lumMix, _pad1, _pad2` (the mock's `packWheelsParams`, gradeMath.ts:217-248, is the executable reference; the current list re-uses the seed-spec names §16.A rules wrong and omits 10 real fields).

### F8 — P1: §0's W-B row re-truth (08:16, the V10 contradiction) + the counts

Rewrite per mock-verdicts V10 + mock-variants §2 row 2: ScopesDock = the ~160px under-viewer pane (W2), tabs Parade/Waveform/Vectorscope/Histogram, store-gated, one scope at a time, §11.4 10fps — **REGISTERED FORWARD: R25-A2 supersedes — the color console row = thin TAB strip [Timeline | Nodes | Scopes], the under-viewer pane RETIRES, scopes react to the playhead frame**; ColorNodeGraph = console-row slot [6], region [2] ALWAYS viewer-led (the viewer swap + ruling-14 stale-frame text retire); the density toggles = the ViewOptionsPopover's Compact-tracks checkbox (W1). Refresh the counts to the live reading (this audit): ColorPage 43 / ScopesDock 22 / StillsPanel 19 / ColorInspector 10 / GradedViewerCanvas 15 / mockGrades 21; lib/color 170 (colorSpace 33 / gradeMath 56 / qualifierMath 28 / scopesMath 20 / gradedImage 27 / index 6). Fix the C59 pointer: "gap C59 (the MOCK's deviation ledger — .drx export is the render-round boundary)" — the spec register's C-ledger ends at C58.

### F9 — P2: the §11 riders (three, one amendment)

(a) *"Scopes analyze the frame under the playhead — always-on analysis, never selection-driven; the visible scope follows the playhead at the §11.4 throttle."* (R25-A2, mock-verdicts V9.) (b) *"The measured signal is the post-grade, display-encoded signal (the WYSIWYG instrument): computed from the graded working texture, `linear_to_srgb` before binning (§18.2); pre-grade display is a v2 viewer toggle."* — this rules 04 §16.5's ambiguity. (c) *"Scope traces render on-GPU; CPU readback of the u32 accumulators is the canvas2d fallback only; the mock's canvas2d painters + stride cap are the CPU-fallback reference, not the production venue."*

### F10 — P2: §8/§9 riders

§8: the matte-finesse OUT-ruling ("the reference's 14-field Matte Finesse block is OUT of v1 — no spec counterpart; cleanup (blur/erode matte) would be extra passes or a JFA reuse, 04 §8.4 — a v2 ask"), the eyedropper seed law + venue ("3×3 average → hueCenter = h·360, sat ±0.08, luma ±0.06; the one mandatory readback — a 3×3 copyTextureToBuffer"), the BT.709-on-encoded statement. §9: the venue statement as law, the UI-reference decision (build from FreeCut's panel), the tracking OUT-ruling ("no tracker in v1 — a point/cloud tracker would be a separate spec if ever asked"); §9.1's default note ("our defaults: exposure 0 — FreeCut's 0.3 quirk is not inherited").

### F11 — P2: §5.4 refresh + §19's two test fixes

§5.4 gains the R24-W2 reference rows (CurveSet {y,r,g,b} + the frozen `{master: tagged points}` storage seam, [Y|R|G|B] radiogroup w/ roving, the ~64-bin channel histogram BEHIND the grid at the 10fps family throttle, the 8% near-band insert, Delete/right-click removal, lost-pointercapture commit). §19: add the F6 dual-curve test; fix `curves-master-channel-affects-luma-only` (:2256-2261) — either reword the prose to "the master curve applies equally to all three channels (a neutral bar's chroma is preserved by construction)" or, if luma-only masters are actually intended, file that as a THIRD model decision (it matches neither §5.1 nor the mock — flag, don't silently keep).

### F12 — P3 batch

(a) §0/§13.Q6 pin refresh to the R27 pins (engine `f9ac806` 748/748; nle-ui `32abd58` 690; app `c020b2a` 252; variants wrap `3db130f`, 1,733/64) + the §15A drift notes (player.ts `enabledEffects` :1656→:1673; headless/api.ts :1509/:1515→:1529/:1535 — all other §15A pins verified HOLDING at f9ac806). (b) §7: register the mock's LUT-select labels as labels. (c) §18↔04: the §8.5 shorthand pointer (cross-spec). (d) §16.A gains a forward note pointing at F2's OPEN decision.

---

## §3 Cross-spec notes (for the cross-cut wave)

1. **04** — §16.5's scope-source row ("reads from working linear-light texture", 04:2106) needs the **post-grade** rider (F9b — 08's ruling is the answer, 04 gets the pointer); §8.5's "255→1023" shorthand (04:667) should point at 08 §18 as the canonical bit-depth table (mock-color row 10, P3); §8 gains the venue-law cross-link (F5).
2. **09** — the C50 ruling's timeline-grade field + the GradeRecord homes are 09-side rows (F4); 09:25's stills caveat (5s pseudo-duration, V1) is 18/09's item, not 08's — noted for the fleet.
3. **15** — F4 reuses the existing effect verbs (§4.3.52-56 ✓ verified); the timeline-grade target may eventually want a scene-level verb (r3 design; do NOT mint one now).
4. **16** — the A3 modifier grammar (Ctrl/Cmd = master, Shift = absolute jump, dbl-click = color-only reset) should be registered in 16's gesture map when F1 lands.
5. **18** — 18:301's blanket dbl-click reset gains the wheels exception (F1 companion); 18 §4.8:220's single-column Color page vs the W2 composition is mock-variants row 7 (18's W3 agent owns; 08:927's note already says the right thing).
6. **17** — its §0 header still says "24 routed wire verbs + 6 documented exceptions" (stale fleet-wide; the R27 count is 31 = 28+3); 17:2393's 08 facet row is fine.
7. **12** — pixelmatch (§5, 12:68/:307) ✓ verified as cited by 08 §19.
8. **REFERENCE-REGISTER** — row 16's re-truth + the R25-A2/A3 REGISTER-PENDING notes + the A3-vs-§16.A OPEN-decision row (mock-variants §3 items 5/11); the C-ledger's C50 row should gain "spec-side ruling LANDED (08 §21)" once F4 applies.

---

## §4 What 08 uniquely sees (not in any Wave-1/2 report)

1. **The §19 packing-test field-list bug** (F7) — the executable contract contradicts §4.2/§16.A's verified uniform; no prior report read §19's Tier-1 list. Genuinely P1: as written the test cannot pass against the layout it cites.
2. **§19 pins no curve composition order at all** — the dual-curve test's ABSENCE is the decisive D37 argument (the only pinned order anywhere in the corpus is the mock's y∘ch).
3. **§19's puck UI test (:2391-2399) is the A3 amendment's hidden third edit site** — rewrite it with F1 or the amendment lands with a contradicting test (mock-verdicts' V12 text touches §4.3 only).
4. **The §4.2↔mock byte-coherence verification** — the 14-step + wheelTintChannel algebra re-derivation (§1 table): the parity oracle is safe to bind; only test-count drift (167→170).
5. **The §15A engine-pin stability check at the R27 pin** — 6 of 8 line refs hold EXACTLY at f9ac806 (the color stack didn't move through the R25-R27 waves); 2 drifted (+17/+20 lines); the 46-sites/10-files and 44-registry counts hold.
6. **The D29.5c vs C50 boundary** — the consumer CSS-filter final pass and the linear sequential timeline pass are different laws; F4 states the distinction so the C50 ruling doesn't look like it re-litigates D29.5c.
7. **The C59 dangling pointer** (08:16 vs the register's C58 ceiling).
8. **§9.1's exposure=0.3 default quirk** — the engine port defaults 0; the spec should state OUR defaults.
9. **The web-research verdict on "Resolve's order"** — undocumented publicly; the D37 text must cite our own grounds, not an unverifiable Resolve claim.

---

## §5 Verification register (what was re-derived live)

- Spec 08 read in full (2,520 lines); every §-section verdict above carries its line cites.
- Mock live-reads: gradeMath.ts:126-155/:298-447 (the 14 steps + wheelTint algebra), gradedFrame.ts:1-179 (y∘ch bake :83-108; the stack :138-157), gradedFrame.test.ts:50-252 (the order/sequential/eyedropper pins), WheelsPanel.tsx:1-214 (the absolute model :147-174; read-only YRGB :20-24), ColorNodeGraph.tsx:55-121/:262-291 (8 nodes/8 edges; NODE_BINDINGS + C56 toast), QualifierPanel.tsx:1-30 (the finesse drop :14-16), ScopesDock.tsx:75-149 (10fps law :80-81), useUiStore.ts:106-194 (the sidecar + TIMELINE_GRADE_KEY :113). Live test counts: ColorPage 43, ScopesDock 22, StillsPanel 19, ColorInspector 10, GradedViewerCanvas 15, mockGrades 21, WheelsPanel 6, controls 6, lib/color 170 (33/56/28/20/27/6).
- Engine @ f9ac806: §15A's 8 line pins re-grepped (6 hold, 2 drifted — see F12a); 46 rgba8unorm sites/10 files re-counted; 44-id registry re-counted; effects/ census; wire strings located; MASTER.md's P2 backlog still lists scopes.
- Cross-spec spot-checks (7+): 04:139-153/:235-243/:641-654/:2102-2128; 18:172-176/:220/:301; 09:23-27; 15 §4.3.52-56; 16:411 (§3.11); 17:2393 + header; 12:68/:307; REFERENCE-REGISTER rows 16-17 + C-ledger C33-C58.
- Web research (3 probes): no public documentation of Resolve's custom-curve composition order — recorded in F6.

*Report ends. Research-only: no corpus file modified except this report; no commits/pushes; stayed on main.*
