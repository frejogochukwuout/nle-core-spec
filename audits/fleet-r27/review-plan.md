# The W5 Adversarial Review of the Plan-Overhaul Draft (xcut-plan §4)

**Task ID:** R27-W5-PLAN · **Agent:** review-plan · **Round:** R27 final-tightness audit, Wave 5 (adversarial review of the D40/W4 plan-overhaul draft) · **Date:** 2026-09-13
**The artifact:** `audits/fleet-r27/xcut-plan.md` §4 (file lines 118-258, the fenced draft = the complete replacement IMPLEMENTATION-PLAN.md). **Method:** fresh context; every check re-derived live against the trees at `/home/z/my-project/*` (app `c020b2a` verified HEAD; engine `f9ac806`; OT `55c81c0`/code `970948a`; WDC `ec8fd5c`; nle-ui `32abd58`); report-only — no corpus edits, no commits, stayed on main.

---

## §1 THE ESTIMATE SWEEP — **PASS (zero hits)**

Swept the draft (extracted §4, 141 lines) for: `wk\b|week|solo|two-dev|business day|\bday\b|month|1.5-3|estimate|hour|minute|session|calendar`. Results:
- **Zero duration vocabulary survives.** The only "estimate" hits are the law's own self-references (draft :4 "There are no time estimates in this plan (D38.1)… never when N weeks pass"; :39 "it is checked, never estimated"; the lineage :252) — legal.
- The two soft-duration candidates are **declared event bounds**, not estimates: the pin-bump ("the app pin-bumps at its next session boundary — before the next fleet round runs", draft :231) and the C7 migration ("the app migration at the next app session", draft :183) — both re-typed per the §5 D38.1 ruling and both battery-flagged events. No operational row encodes a duration.
- The proposed battery check (`rg -c "wk\b|solo|two-dev" IMPLEMENTATION-PLAN.md` == 0, xcut-plan :276) **passes the draft today** — but it is too narrow to guard the future: it misses `week(s)`, `day(s)`, `month`, `1.5-3`. Note also that widening it to `week` would false-positive on the charter's own "never when N weeks pass" sentence. **P3-F14 (one edit + one reword):** reword the charter line to "…a phase ends when its exit gate is green — never on a calendar" and widen the battery class to `wk\b|week|solo|two-dev|\bday\b|month|business`.

## §2 THE COLD-EXECUTOR TEST (the plan-executability law) — **5/5 startable, one soft gate**

Five leaf rows picked at random; every entry point opened live:

| Row | Owner | Entry point (live-verified) | Exit gate (command) | Deps | Verdict |
|---|---|---|---|---|---|
| S-app (1)(b) the gesture-seam switch | nle-test-app ✓ | `.agents/PLAN.md:104-147` (the D-ARCH-6 filing block, read live — quotes the switch), `src/timeline-port/hooks/use-timeline-actions.ts:452` (`core.removeElementKeyframes` — **verified at :452 exactly**), `use-keyframe-drag.ts:96` (`core.retimeElementKeyframes` — **verified at :96 exactly**) | `npx vitest run src/wire-coverage.test.tsx` (9 its incl. `THE GATE` at :380, consuming the exported registries) — the gate-goes-red-until-the-switch discriminator is machine-checkable ✓ | OT s17 landed (`55c81c0` HEAD ✓; vendored lock `upstreamHead: 6e2b91a` ✓; the batch verbs + 3-entry `WIRE_UI_EXCEPTIONS` at `api.ts:328-335` ✓) | **PASS** |
| S-ot (1) the carrier-reduction filing | opencut-timeline ✓ | file the titled block in `opencut-timeline/.agents/PLAN.md` (exists ✓; grep confirms NO carrier block today ✓ — "STILL UNFILED" is TRUE); content enumerated (6 props — `selectionIds`/`confirmDelete`/`onZoomChange` grep-zero in OT src = real work; `initialSelectedIds`/`onSelectionChange`/`cancelRegistry` partially present) | filing: the block greps present in PLAN.md; landing: the app's `census-check.mjs` (register-equality, CI-enforced) ✓ | none (additive-optional) | **PASS** (nit: "file it THIS window" is round-relative phrasing in a standing doc — P3-F15) |
| S-wdc (1) the S-series docs wrap | web-daw-core ✓ | the repo-ROOT `HANDOFF.md:73-77` (**verified stale**: says "The **4** deferred upstream tests" while 5 landed as G1) + `SKILL.md:25-26` (the coreOwned census — verified site) | **SOFT**: "HANDOFF truthful" is not a command — P3-F10 gives it a checkable form (`rg -c "4 deferred" HANDOFF.md` == 0; the coreOwned count vs `git ls-files src`) | none | **STARTABLE, gate soft** |
| S-package (4) the AW1-2 mixer twin | nle-ui ✓ | `src/components/mixer/ChannelEditor.tsx:91/:99` — **verified live: `max={4}` twice** (the NumField + the range input; the Inspector DID get +20 @ `32abd58`) | `npx vitest run` (mirror the `32abd58` Inspector pin pattern: the +10 dB commit discriminates) ✓ | none | **PASS** |
| S-engine (5) the NS-1..NS-5 queue | nle-engine ✓ | `.agents/PLAN.md:463-485` — **verified: all five NS rows present** with the exact content the draft summarizes (NS-4 "the r1-port actionable core") | "each ruled or landed" — "ruled" = the D-register/arch grep (semi-machine-checkable) + the repo gates (vitest 748/tsc/layer fence/OV-12) ✓ | NS-2 rides the K3 consumption; NS-4 = the r1-entry architecture decision (both stated) | **PASS** |

**Bonus cold-executor facts:** the §0 environment-prerequisites paragraph (PAT + `npm run bootstrap`; OT's `bun run dev` BEFORE `bun run test` at :3001; WDC's `--frozen-lockfile` + the sync clone; the engine submodules; nle-ui `npm ci`) is the strongest operationalization in the draft — every claim checked against the repos holds. Two path nits: `LAW-NET-INVENTORY.md` + `CORE-SEAMS §2` (cited in S-app (2), draft :138) live at `nle-core-spec/ui-mock/shell-mini/docs/` — **not in the app repo**; qualify the paths (P3-F7). The mocks track's Repo cell uses relative paths (`ui-mock/shell-mini`) while every other row is absolute (P3-F9).

## §3 THE SEQUENCING SOUNDNESS — **critical path verified; three edge-set gaps**

- **The critical path is right and live-verified:** re-pin → K3 → K4 → w1 → w2∥w3 → r1-entry gates → r-phases. The D-ARCH-6 consumer re-pin is correctly the HEAD (S-app (1) FIRST; the single global entry point at draft :146 names it; E1 makes the ported coverage gate its consumer). Its own prerequisites are all green (OT s17 landed; nle-ui's AW1-2 Inspector fix landed @ `32abd58` so sub-item (d) is unblocked — verified against the live vendor pin `83ff8a8` + the `32abd58` commit). **Nothing gates K4 that is ungated**: K4 ← E8 (K2+K3); K2 ← the S-spec registration (doc row) + S-engine's seam-shape pairs; K3 ← E1/E2 + the mapping row.
- **P2-F1 — K3's exit gate omits its own sixth half.** Draft :164: K3's OPEN list names six halves (a)-(f) incl. "(f) the VLM visual net", and §0's S-app done-state says "the VLM net live" — but the K3 exit-gate cell ("the coverage gate green + the compensating pin family green; the corpus row-by-row green; the mapping row enumerated; the audio pins green (DONE); the mode-coverage family green") **never mentions the VLM net**. A machine checking K3's gate would pass with the VLM net unlanded. **Fix:** append "+ the VLM net live (REFERENCE-REGISTER rows verified)" to the K3 exit-gate cell.
- **P2-F2 — the register has two owners across two "streams".** S-spec (3) owns "the register maintenance (D35, standing every round)" (draft :143) while the mocks track row says "the fleet's only duties are the register coordination (re-key at WRAP only…)" (draft :144) — and the mocks track edits `ui-mock/**` INSIDE the same nle-core-spec tree, which the carve law's clause (i) does not bound (it covers only "the module streams"). The WRAP-only law mitigates temporally, but the register FILE (`REFERENCE-REGISTER.md`) has two named executors. **Fix (one sentence in the mocks row):** "the register file's executing owner is S-spec; the sibling's WRAP is the re-key TRIGGER (an event), never a second editor; the mocks track never edits `specs/` or `audits/`."
- **P2-F3 — the amendments-before-VLM edge is missing from E1-E10 while D38.3 declares the edge set exhaustive.** The draft itself states the ordering twice (S-app (6), draft :138: "our amendments land before the sibling's VLM pass, else their net verifies dead law"; the mocks row's "amendments-before-VLM ordering") — a real cross-stream edge that E1-E10 (draft :208-223 / xcut-plan §2b) does not contain, under a ruling that says "bounded by exactly ten hard edges… everything else is spurious sequencing" (xcut-plan :270). **Fix:** add **E11 — S-spec's amendment wave → the sibling's VLM pass / the app's VLM-net adoption** to §2b, and re-count D38.3 ("exactly eleven").
- **P2-F4 — the mock-retirement user-gate status is unstated.** The retirement mechanism is visible (protocol #6, D35.4/D26.4 triggers, post-K4/w1, keyed on register flips — draft :143/:144/:235), but it is absent from the user-gate register (draft :190 lists five points, with the D37 color rulings as #5). Whether EXECUTING the retirement (deleting the mini/variants lineages — irreversible, sibling-authored work) is mechanical or needs the user's sign-off is not decidable from the doc. **Fix (one line in the user-gate register):** "the mock retirement executes on the D35.4 register flips (mechanical, not user-gated — the flips are the proof)" — or, if the fleet prefers, gate it; either way SAY it.
- **P2-F5 — the register's standing OPEN design-round decisions have no phase home.** The register's OPEN table carries: marker v2 (C33), captions (C34), the 3-vs-5 page shape (XMOCK-7), the FX-page grammar (XMOCK-6), the A3-wheels row. None appears in any track's worklist or any phase in the draft (only the A3-wheels law is staged, inside the D37 set at r3-entry). At least the page-shape + FX-grammar pair plausibly gates w1's chrome/token wiring (the MiniShell C0 consumes 18's page model). **Fix:** one S-spec line: "the register's OPEN queue phase-tagged: page-shape + FX grammar at w1-entry; marker v2 + captions at r5-entry; A3 wheels at r3-entry (in D37)" — or an explicit "register-carried, unscheduled by design" note.
- **P3-F11 — the ASCII map over-draws vs the edge set:** the S-ot lines merge into the K3→K4 spine (draft :214-215) though no E-edge runs S-ot → K3/K4; annotate the merge as phase co-location or drop the connectors.
- **P3-F12 — an implicit E1 → K2-seam-shapes edge:** S-engine's seam-shape pairs pin "the app's seam shapes", which CHANGE at the re-pin (the gesture-seam switch + insertBatch); an executor following "the ONLY edges are E1-E10" could write them pre-switch. Add the note (the pairs pin the POST-re-pin shapes).
- **P3-F13 — S-wdc's false serial arrow:** "the docs wrap FIRST → the M2 mixer surface" (draft :199) — no gate runs between a doc edit and the design round; mark them ∥ (the "FIRST" is priority, not a dependency).

## §4 THE STATE TRUTH — **100%: zero false DONEs (13 claim families live-verified)**

| Draft claim | Live verification |
|---|---|
| K3 audio pins @ `44e8049` — `waveformPeaks` 9 tests | commit exists ✓; `src/waveformPeaks.test.ts` = **9 its** ✓ (on WDC's exported contract) |
| The FIVE ports @ `3a28b6e` — `upstream-ports.test.ts` 25 | commit exists ("42 laws re-expressed… 670/670") ✓; `tests/vitest/upstream-ports.test.ts` = **25 its** ✓ |
| E1/E2 @ `8188d4e`+`412693d` | both commits exist with the E1/E2 fix bodies ✓; `timeline-linked-source-edit.test.ts` = **26 its** ✓ (13+13 review folds) |
| The coverage gate | `src/wire-coverage.test.tsx` = **9 its** incl. `THE GATE: every WIRE_COMMAND_TYPES entry minus the live exception registry` at :380, consuming the exported registries ✓ |
| The census-CI | `scripts/census-check.mjs` + `census-mutation-gate.mjs` = **8 scenarios** (clean, M1-M6, M6b) ✓; `docs/port-census.md:29-37` reads "42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + the EngineMount host" @ `6e2b91a` ✓ |
| The maintainPitch resolution | `engineService.ts:510-513` = the read-merge (`cmd.patch.preservePitch ?? f.el.retime?.maintainPitch ?? true`) ✓; zero code hardcodes remain (one comment mention at :515) ✓; `94d5a2e`/`2de34ac`/`d8d1b2b` all exist ✓ |
| The pin world (§1) | app HEAD `c020b2a` ✓ 252 = 104+42+41+20+20+9+7+9 (every suite live-counted: GluedShell 104, audioService 42, sceneBridge 41, deliverService 20, persistenceService 20, wire-coverage 9, engineSeam 7, waveformPeaks 9) ✓; engine `f9ac806` ✓ **25 test files** ✓; OT `55c81c0`/`970948a` ✓, `api.ts:328-335` exported `WIRE_UI_EXCEPTIONS` with **3 entries** ✓, `WIRE_COMMAND_TYPES` = **31 verbs** (28+3) ✓; WDC `ec8fd5c` ✓ **34 test files** ✓ (S-series absorbed, fork `f5011b3` ✓); nle-ui `32abd58` ✓ (690 in its commit msg) |
| The stale-row kills (S3/S10) | INDEPENDENTLY CONFIRMED: the old plan's "audio pins hard-gated on S-wdc" is false twice over (WDC contract landed R26 `035afe8`/`387f327`; app pins `44e8049`); the old "zero upstream drift / 759/759" is false (`ec8fd5c` = the S-series sync commit itself) |
| The filing gaps (S12) | nle-ui `PLAN:210/:219` both unchecked `- [ ]` ✓; DECISIONS jumps D28 (:1062) → #30 (:1176), no D29 ✓; no MiniShell in nle-ui src ✓ (grep-empty) |
| The WDC docs staleness (M6) | root HANDOFF `:73-77` says "**4** deferred" (5 landed) ✓; SKILL `:25-26` coreOwned site ✓ |
| The D-ARCH-6 filings (M3) | app `PLAN.md:104-147` block read live: the re-pin + the seam switch (with the exact hook cites) + the pool multi-insert staggered design (at PLAN :136-141) + the nle-ui re-pin ✓; the vendored OT lock `upstreamHead: 6e2b91a` ✓ (the draft's re-pin source pin is CORRECT; the app PLAN's own "a25d199" at :126 is the stale one — not the draft's problem) |
| The NS queue + OT next-horizon | engine `PLAN:463-485` NS-1..5 ✓; OT `PLAN:550-556` = the linkage/constants/wire rows + the P3 sweep, exactly as cited ✓ |

**Count-integrity caveats (not false DONEs — volatility discipline):** (a) the draft's "322 authored + 33 projection pins" (S-app (2)) is the **R23 seal arithmetic (355)**; the register's live acceptance list is "149 units / 445 tests" (LAW-NET-INVENTORY's own header) and the sibling has moved further (live at my read: mini 11 files / 491 its; variants 65 files / 1,806 its) — cite the inventory as the authority, drop the stale split (P3-F8); (b) §1's in-flight figures (mini "448", variants "live 1,773") are already orphaned numbers (register says 445/1,762; live is 491/1,806) — per the register-timing law (xcut-amend R8) cite the register-declared figure + the in-flight flag, never a bare live count (folds into F8); (c) **P2-F6 — one stale line cite:** "trimToPlayhead… half-pinned at GluedShell :361/:368" — live truth is `GluedShell.test.tsx:391/:398` (grep-verified; nothing at :361/:368 matches). The draft's own pin-hygiene law (protocol #10) makes file:line load-bearing — refresh it.

## §5 THE USER-GATED POINTS — **4/5 of the canonical set + 1 wanted addition; 1 status gap**

Visible and early (draft :190 + §3(e) + D38.5): (1) the r1-entry absent-family scope gate ✓ (material = the leverage map §4: 6 ports + 3 composites + 1 greenfield); (2) D6's r4 re-affirmation ✓; (3) the C7 rename timing (r1 END, mechanical) ✓; (4) D28.2 the productization gate + the seal-snapshot option ✓ (protocol #8 keeps the full definition). **Additions the draft correctly surfaces beyond the old plan:** the D37 color rulings at r3-entry (the curve order y∘ch, the A3 wheels gesture law — the register's A3 OPEN row, the node-graph model, the C50 ruling) ✓ — exactly the "new user-gated decisions" wanted. **The gap:** the mock retirement (the task's fifth canonical point) is present only as a MECHANISM — see P2-F4; state its user-vs-mechanical status. No other unsurfaced user gate found: the D39-candidate trio (xcut-matrix §4.3) is ruling-work for the fold, not user gates; the A3-wheels law is inside D37 ✓.

## §6 THE PARALLELISM MAXIMIZATION — **near-maximal; the carve is sound**

- The eight-stream carve (6 repos + spec + mocks) is correct at the repo grain; every module stream's tree is disjoint; the cross-repo filing law + the pin-lockset law handle the mediated couplings; the mocks/S-spec tree-sharing is the one collision plane (F2).
- Within-stream ∥-sets are marked (S-app (2)∥(3)∥(4); S-engine's five-∥; S-package ∥ crawl; the ∥-validity laws at draft :226 are all true — verified w2∥w3 different seams, r1∥r2∥r3 different repos, instruments ∥ crawl with r3-consuming-only, K2 verification-not-construction).
- The two-agent split survives estimate-free: "the highest-leverage pairing… one stream on S-app…, one on S-ot/S-package" (draft :225) — a parallelism fact, no staffing model.
- Serial-but-parallelizable rows found: only S-wdc's docs-wrap → M2 arrow (F13) and — at the round-execution level — §7's W6, which could name the W-A-first law (the mechanical re-pin commit lands first because everything else cites it; the per-file spec edits then run ∥ after) — but excluding round-detail from a standing plan is the draft's own S16 design choice, so this is optional polish (P3-F17), not a defect.
- The mocks-alignment waves (amendments-before-VLM) are correctly ordered but un-edged (F3).

## §7 VERDICT — **SHIP-WITH-FIXES**

No P1s: zero duration vocabulary survives; zero false DONEs (13 claim families live-verified, including the two stale-claim kills independently confirmed); the critical path is live-true with the gate-breaking re-pin correctly at its head; 5/5 random leaf rows are cold-startable with real, verified entry points. The fixes are all local (one cell / one sentence each), applied by W6 when it lands §4 verbatim:

**P2 (fix before the W6 paste):**
1. **K3 exit gate omits the VLM net** (draft :164) — append "+ the VLM net live (the register rows verified)" to the K3 gate cell (the §0 done-state already says it).
2. **Register two-owner** (draft :143-144) — add to the mocks row: "the register file's executing owner is S-spec; the sibling's WRAP is the re-key trigger (an event), never a second editor."
3. **Missing E11** (xcut-plan §2b + D38.3 at :270; the ordering itself is stated at draft :138/:144) — add "E11. S-spec's amendment wave → the sibling's VLM pass / the app's VLM-net adoption" and re-count D38.3 to eleven edges.
4. **Mock-retirement user-gate status** (draft :190 register) — one line: mechanical-on-the-D35.4-flips (or user-gated), stated explicitly.
5. **The register's OPEN decisions need phase homes** (draft :143) — page-shape + FX grammar at w1-entry; marker v2 + captions at r5-entry; A3 wheels at r3-entry (in D37).
6. **Stale line cite** (draft :138) — "GluedShell :361/:368" → "**:391/:398**".

**P3 (fold with W6's edit pass):** 7. qualify `LAW-NET-INVENTORY.md` + `CORE-SEAMS` with `nle-core-spec/ui-mock/shell-mini/docs/` paths (:138); 8. drop the stale "322+33" split — cite the inventory/register as the authority + mark in-flight counts volatile (mini/variants live figures are already past the draft's) (:138/:152); 9. absolute paths in the mocks Repo cell (:144); 10. give the WDC docs wrap a checkable gate (`rg -c "4 deferred" HANDOFF.md` == 0; coreOwned vs `git ls-files src`) (:141); 11. annotate the ASCII map's S-ot merges as co-location, not edges (:214-215); 12. note the seam-shape pairs pin the POST-re-pin shapes (:139/:163); 13. mark S-wdc's docs wrap ∥ M2 (:199); 14. reword "never when N weeks pass" → "never on a calendar" + widen the battery estimates-absence regex to `wk\b|week|solo|two-dev|\bday\b|month|business` (:121/:276); 15. de-round-relative "file it THIS window" → "file it as the queue's head block" (:139); 16. name the battery script venue (`scripts/battery_r26.py` the fork base) in S-spec (1) (:143); 17. optionally name the W-A-first law in §7's W6 (:256).

**Report-only confirmed:** no spec/repo files touched outside this report + the worklog; no commits/branches; stayed on main.
