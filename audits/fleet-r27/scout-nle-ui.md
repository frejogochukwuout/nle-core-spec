# Scout report — nle-ui (R27 W1, final-tightness fleet)

**Range audited:** R26 pin `3026099` (674 tests) → HEAD `32abd58` (**690 tests, re-run LIVE by this scout: vitest 690/690 in 85.3s, `tsc --noEmit` exit 0, `node scripts/check-boundary.mjs` → "OK (package src is engine-free)"**). Repo `/home/z/my-project/nle-ui`, tree clean on main, 8 commits in range.

---

## §1 The commit-range ground truth (verified from code)

### 1.1 The 8 commits (`git log --oneline 3026099..32abd58`)

| # | Commit | Wave | Tests | What landed (code-verified) |
|---|---|---|---|---|
| 1 | `755235c` | F4-5 (H1-E P2 #2) | 674→678 | **Z4 double-apply guard, package half** — `e.stopPropagation()` on all four nudge branches: Viewer scrub-row (`src/components/shell/Viewer.tsx:296-307` — the comment names the probe: "preventDefault alone does NOT stop the native event from reaching useShortcuts' window listener, which nudged AGAIN (2 frames per press in the router-less world)") + Ruler root (`src/components/timeline/Ruler.tsx:233-245`). The scrub row already carried `data-transport=""` (`Viewer.tsx:317`, landed R9 `2646d80`). Pin: `AppShell.test.tsx:505-517` ("Z4 focused-surface arrows — one key, one owner": scrub-row ArrowLeft = exactly 1 frame, ⇧ArrowLeft = exactly 10, ruler same, nothing-focused keeps the global nudge). |
| 2 | `4098108` | F4-6 (H1-E P2 #6) | 678→679 | **setElementField/setTransition identity guards (the law-73 class)** — `patchValueSame` (`useUiStore.ts:271-279`: scalars `===`, plain objects structural with SORTED keys, arrays `===`), the guard before `withHistory` (`useUiStore.ts:1065-1073`), the setTransition partial-merge + absent-clear no-ops (`useUiStore.ts:1142-1162`). Killed the dead-undo-steps from the Inspector Reset buttons / double-click resets / re-add-transition. Pin: `useUiStore.test.ts:1016`. |
| 3 | `5801079` | F4-7 (H1-E P3 #9+#10) | 679→681 | (a) **kind-aware toggleMuteAll mock fallback** — ONE shared law `isMuteableTrack` (`useUiStore.ts:332-333`: `t.kind === 'main' \|\| t.kind === 'audio' \|\| !t.elements.every((e) => e.type === 'text')`) driving BOTH routed + mock halves (text-only/empty overlays never flip). (b) **duplicateElements severs `linkedTo` on the copy** (`useUiStore.ts:937-945`, `delete copy.linkedTo` at :945 — "the splitElement R14 law's twin"). Pins: the mixed-state convergence + the RR1-B-5 mirror twin + the ⌘⇧M shape in `useShortcuts.test.tsx`. |
| 4 | `1967d18` | F4 wrap (docs) | 681 | nle-ui `worklog.md` +71 lines — the F4 session record (test trajectory "vitest 678 → 679 → 681", per-fix vacuity proofs via `git stash`). |
| 5 | `af32e69` | **R9-b** (H2-B wave 1) | 681→682 | **The maintainPitch S-spec ruling — `el.preservePitch` joins the model.** Full detail in §2.1 below. |
| 6 | `4b0fcbe` | H2-B-2 (R9-b review folds) | 682 | (P2-1) the **absent-≡-default identity law** — `DEFAULTED_PATCH_KEYS = { preservePitch: true }` + `patchKeySame` (`useUiStore.ts:281-294`): resetSpeed's `{speed:1, preservePitch:true}` on a fresh element is identity, no history entry, nothing materialized. (P3-5) the **honest tri-state** on the Preserve-pitch toggle (`Inspector.tsx:764-771`: `pitchStates`/`pitchOn`/`pitchMixed`; `aria-pressed={pitchMixed ? 'mixed' : pitchOn}` at :972; clicking from mixed writes true to all). (P3-6) the **identity-law attribution** moved to the field comment (`mockData.ts:66-71`: the dormant-false reload drop is the APP's load-bridge identity law, NOT engine law — "the engine itself stores a committed {rate: 1} retime verbatim"). |
| 7 | `f098374` | **FW-D** (RA round, HW-2 §3 CR) | 682→689 | **`setElementFieldAll(ids, patch)` — the coalescing CR.** Full detail in §2.2. Call sites retargeted: the Inspector's `setFieldAll` (`Inspector.tsx:694-695`) + the Clip clear-effects loop (`Clip.tsx:242` — ONE `setElementFieldAll(targets, {effects: []})`). |
| 8 | `83ff8a8` | FW-D wrap (docs) | 689 | worklog entry: the FW-D session record + the R9-b upstream-landing verification (item-by-item vs HW-2 §4) + the app-side residuals handoff (see §1.3). |
| 9 | `32abd58` | **AW1-2** (cross-seam, from the OT s17 session) | 689→690 | **The Inspector Gain authoring ceiling raised +4 → +20** — the fleet-coherent [−60,+20] domain (`Inspector.tsx:1001-1002`: `min={-60} max={20}`, the +4→+20 one-char diff; pin `Inspector.test.tsx:209-226`: +10 dB must COMMIT as `10^(10/20)=3.1623` — "under the +4 cap it clamped to 4 → 1.5849" — and the −60 floor pins at 0.001). The +4 cap was the nle-engine AW-1 census's documented P3. |

### 1.2 Where `preservePitch` now lives in the model (R9-b's answer to the R26 HANDOFF ask)

- **`ElementJSON.preservePitch?: boolean`** — `src/lib/mockData.ts:72`, immediately after `speed` (:59), with the 12-line ruling as the field's law comment (`mockData.ts:60-71`, quoted in §2.1).
- **The router contract** — the `'patch'` dispatch carries it: `src/state/timelineRouter.ts:116` — `{ type: 'patch'; elementIds: string[]; patch: { name?; hidden?; volume?; speed?; preservePitch?: boolean; transitionOut?: … | null } }`, with the merge law doc'd at `:108-115` ("speed rides the patch as the LINEAR rate — the impl converts to the engine's retime {rate, maintainPitch}… preservePitch rides the patch as the NLE-default boolean — ABSENT in the patch = inherit the engine retime's current flag ?? true; the impl MERGES it into the retime (a preservePitch-only patch keeps the current rate)").
- **The store carve-out** — `splitEnginePatch` (`useUiStore.ts:314-323`) splits `name, volume, transitionOut, speed, preservePitch` as ENGINE-OWNED; both `setElementField` (:1050-1076) and `setElementFieldAll` (:1078-1124) route through it.
- **The Inspector toggle is MODEL-BACKED** — `Inspector.tsx:724-733` (`pitchAll` writes `setFieldAll({ preservePitch: !pitchOn })`; "the mock's component-local pitch state is GONE"), reads `e.preservePitch !== false`. Pin: `Inspector.test.tsx:254-275` (absent=true start, both toggle directions, the group Reset restores 100% + preserve ON).
- **The mock-local list**: `Inspector.tsx:675-676` — "(preserve-pitch LEFT this list with R9-b — it is model-backed on el.preservePitch now.)"

### 1.3 The consumer residual status (verified at nle-test-app @ `c020b2a`)

- The app's vendored submodule pin: `git ls-tree HEAD vendor/nle-ui` → **`83ff8a8`** (R9-b + FW-D consumed; **AW1-2 NOT yet absorbed** — the app's `.agents/PLAN.md:143-146` files "The nle-ui re-pin absorbs the AW1-2 ceiling fix (nle-ui @ 32abd58…). Rides your standing nle-ui freshness re-pin").
- The FW-D worklog's app-side residuals have **LANDED since** (the worklog note is stale in the app's favor): the engineService preservePitch branch is live (`nle-test-app/src/engineService.ts:481-524` — the read-merge-write: `maintainPitch = cmd.patch.preservePitch !== undefined ? cmd.patch.preservePitch : f.el.retime?.maintainPitch ?? true`, plus the default-reteim skip at :523); the sceneBridge load law (`sceneBridge.ts:229-262`, `maintainPitch: el.preservePitch !== false`) and the reverse projection (`sceneBridge.ts:435-447`, `retime.maintainPitch === false → el.preservePitch: false`, absent otherwise). The app's worklog:620 records the UI-leg pin (RED on `1967d18`, GREEN on `83ff8a8`).

---

## §2 The new law state

### 2.1 R9-b's ruling text as landed (the field's law comment, `mockData.ts:60-71`)

> "R9-b (the maintainPitch S-spec ruling, H2-B wave 2026-09-10): preserve-pitch on retime (spec 06 §5.11). ABSENT = true — the NLE default (speed changes keep pitch); explicit false = pitch-affected varispeed (the chipmunk law). The doc field is a PROJECTION of the engine's retime.maintainPitch (the D-T5 pattern): authored through the engine patch surface, carried back by the mirror. A dormant false at rate 1 does not survive reload — that drop is the APP's load-bridge identity law (sceneBridge skips authoring retimes at speed 1), NOT engine law: the engine itself stores a committed {rate: 1} retime verbatim (fromJSON normalizes trims/locks/animations, never retimes). The toggle is the retime's companion, not an independent element property."

Three laws in there: **(a)** the field law (absent ≡ true, explicit false = varispeed), **(b)** the projection law (doc field ⇄ engine `retime.maintainPitch`, authored engine-side, mirrored back), **(c)** the reload-drop attribution (app load-bridge identity law, not engine law). Plus the H2-B-2 fold: **(d)** the absent-≡-default identity law (writing the default onto an absent field is a semantic no-op — `DEFAULTED_PATCH_KEYS`, `useUiStore.ts:288`: "new absent-≡-default fields register HERE").

### 2.2 FW-D's coalescing law (the UNDO-GROUP CONTRACT, `useUiStore.ts:1088-1103`)

> "UNDO-GROUP CONTRACT (one user verb = ONE undo unit in the OWNING domain): engine-owned keys → ONE dispatch carrying the WHOLE id list (the router's patch command takes elementIds[]; the impl folds N ids into ONE updateElements batch → ONE engine history entry → one engine engine ⌘Z reverts the whole group). Sidecar keys → ONE withHistory mutating every resolvable, UNLOCKED hit — locked and missing ids skip PER ELEMENT (the single-element law), and the F4-6 identity guard applies per element: an element already carrying the patch's values is skipped (patchKeySame); if NO element changes, NO history entry lands (the no-pollution contract…). A MIXED patch (engine-owned + sidecar keys in one call — no current control commits both domains at once) lands ONE entry in EACH domain: the ⌘Z facade (engine-first-then-store) then takes TWO presses to fully revert a mixed patch. Keep controls single-domain unless the facade grows a composite undo unit."

Mechanics: ONE shared `splitEnginePatch` (`useUiStore.ts:296-323`) so "the next engine-owned field joins both halves in one edit (preservePitch, R9-b, was the last to join)"; the routerless half is ONE `withHistory` with the N-loop inside one mutate ("past.length +1 not +N"). Pins: `useUiStore.test.ts:751+` (routerless 3-element patch → past+1 & one undo reverts all; all-identical incl. absent-≡-default preservePitch → NO entry; locked/missing per-element skip; empty ids no-op), `timelineRouterDispatch.test.ts:165` (ONE patch command carrying the whole id list; identity re-send adds nothing), `Inspector.test.tsx:140` (the group verb through the real component → ONE undo unit).

### 2.3 AW1-2's domain law (the fleet-coherent volume-dB ceiling)

- The Inspector's Gain authoring domain is **[−60, +20] dB** (`Inspector.tsx:1001-1002`), the display form of `el.volume` (LINEAR, converted by the true dB law `20·log10`/`10^(dB/20)` with the 0.001 linear floor, `Inspector.tsx:1000/:1007` — the F7 law).
- **The one home:** opencut-timeline's exported `src/lib/timeline/core/audio-params.ts` (read at OT HEAD `55c81c0`): "The volume-dB domain law — THE one home (D-ARCH-2, arch round s16)… `VOLUME_DB_MIN = -60; VOLUME_DB_MAX = 20; DEFAULT_VOLUME_DB = 0`… ZERO-IMPORT LEAF… the consumer's bridge deep-imports this file directly… the constants are the shared contract." Per OT DECISIONS #26 r4; the engine's OV-01/OV-02 (R27 grid) collapsed the volume-dB law to this upstream home; nle-ui consumes it "at this repo's next vendor re-pin" (commit `32abd58` message).
- The pin holds the fleet law: +10 dB must COMMIT (3.1623) — `Inspector.test.tsx:209-226`.

---

## §3 The spec-impact register

| spec §site | current text (quote) | new truth (file:line evidence) | severity | proposed amendment |
|---|---|---|---|---|
| **09 §3.1 `ElementJSON`** (`09-project-model.md:177-178`) | "`preservePitch?: boolean;       // (Round 15 amendment, B2) retime keeps pitch (18 §4.4's preserve-pitch toggle)`" — the field exists since R15, but **no default, no projection law, no engine threading, no identity law** | The R26-HANDOFF model decision is RULED and LANDED: field law absent≡true + projection + reload-drop attribution (`nle-ui/src/lib/mockData.ts:60-72`); router threading (`timelineRouter.ts:108-116`); app impl landed (`engineService.ts:481-524`, `sceneBridge.ts:229-262/:435-447`) | **P1** (the R26 HANDOFF's #2 ask: "the model ruling (ElementJSON field vs surface state) — file it as a DECISIONS entry") | Record the ruling as a D37+ DECISIONS entry + amend the §3.1 field comment + §7.2 B2 (`09:337`): absent≡true (the NLE default), the doc field is a projection of engine `retime.maintainPitch` (authored via the patch surface, mirrored back), the dormant-false reload drop is the APP load-bridge identity law (NOT engine law — fromJSON never normalizes retimes), and resetSpeed's `{speed:1, preservePitch:true}` is identity on an absent field (the absent-≡-default class) |
| **06 §5.12 Retime (Speed Change with Pitch Preservation)** (`06-nle-ops.md:1804-1921`) | The section is an OpenCut-classic code census (`renderRetimedBuffer`, `shouldMaintainPitch({rate, maintainPitch})` at `:1730-1734`, presets `maintainPitch = false` default at `:1793`); **no doc-field law, no absent≡true statement** | The ruling names this section's domain: `el.preservePitch` absent≡true ⇄ `retime.maintainPitch`; the bridge normalizes (`sceneBridge.ts:262` `maintainPitch: el.preservePitch !== false`; back-projection :435-447 only writes explicit false) | **P1** | Add the companion-field law to §5.12: the doc model's default is TRUE (unlike OT's `RetimeConfig` where absent = pitch-affected — the bridge converts); a preservePitch-only patch keeps the current rate (merge law); cite the nle-ui/app landing |
| **06 §5.11 vs §5.12 citation split** (`06-nle-ops.md:1694` Rate Stretch / `:1804` Retime) | nle-ui code cites "**spec 06 §5.11**" for preserve-pitch (`mockData.ts:61`, `Inspector.tsx:974` data-tip, `timelineRouter.ts` comment via `useUiStore.ts:309`); spec 18 §4.4 also cites "spec 06 §5.11" (`18:184`) | §5.11 is Rate Stretch (its quotes carry the `shouldMaintainPitch` API); §5.12 is the Retime/pitch-preservation section — the flag's natural home | P3 | Pick ONE home (recommend §5.12, cross-ref from §5.11's `shouldMaintainPitch` quote) and align the code citations + 18 §4.4's pointer in the same amendment |
| **18 §4.4 tab table** (`18-ui-shell.md:184`) | "Speed: rate % + preserve-pitch toggle (→ retime family, spec 06 §5.11)" | The toggle is MODEL-BACKED on `el.preservePitch` with the honest tri-state: `aria-pressed` true/false/**mixed** (WAI-ARIA) for a mixed selection, click-from-mixed writes true to all (`Inspector.tsx:764-771/:972`); absent≡true start; group Reset = {speed:1, preservePitch:true} identity-safe (`Inspector.tsx:747-749`) | **P2** | Amend the Video-tab row: model-backed toggle, the absent≡true read (`e.preservePitch !== false`), the mixed-selection tri-state, the §5.12 cross-ref |
| **18 §0 honest-mock ledger** (`18-ui-shell.md:17`) | "The honest-mock ledger (marked honest in both worlds, per the module cards): … Inspector Pan/Transform/**preserve-pitch (mock-local)**; …" | STALE: "preserve-pitch LEFT this list with R9-b — it is model-backed on el.preservePitch now" (`Inspector.tsx:675-676`); Pan + Transform remain mock-local (`Inspector.tsx:672-679`, `:988-996` — pan's non-persisting note at :1016) | **P2** | Remove preserve-pitch from the mock-local ledger list; note Pan/Transform remain |
| **18 §4.4 Audio tab** (`18-ui-shell.md:185` + `:189`) | "**Volume %**, pan (−100..100), fade-in/fade-out seconds"; "(Round 15 amendment, B2: … 'Gain dB' is withdrawn — no dB field exists in 09 and dB conversion is display-side only…)" | The shipped tab is **Gain (dB)** writing the REAL `el.volume` through the true dB law (F7, pre-R26) with domain **[−60,+20]** (AW1-2: `Inspector.tsx:997-1008`); "display-side only" survives (el.volume stays LINEAR) but "Volume % / Gain dB withdrawn" is false | **P2** (the F7 half is pre-existing staleness; AW1-2 completes the domain and forces the reword) | Reword the Audio-tab row: Gain in dB (display form of 09's linear `volume`, domain [−60,+20] per the one-home law), pan (mock-local, non-persisting — the real pan is the strip layer), fades |
| **09 §3.1A B2 + volume domain** (`09:173-175`, `:337`, `:1942/:2233`) | "volume's persisted unit is LINEAR 0..1 — the shell displays dB via conversion at the UI boundary"; the [−60,+20] clamp appears only in OpenCut migration reference tables (`:1942`, `:2233`) | The AUTHORING domain is now fleet-coherent [−60,+20] at every Gain surface (AW1-2 `Inspector.tsx:1001-1002`; OT one-home `core/audio-params.ts` `VOLUME_DB_MIN=-60/VOLUME_DB_MAX=20/DEFAULT_VOLUME_DB=0`, D-ARCH-2 + DECISIONS #26 r4; engine OV-01/OV-02 collapsed to it; 20 §N2b's fold clamp `[-60, +20]` at `20:125`) | **P2** | State the dB authoring domain + the one-home law once (09 §3.1A B2 note): the persisted form stays linear 0..1, the display/authoring form is dB in [−60, +20], `VOLUME_DB_MAX=20` lives in OT's `core/audio-params.ts` (the zero-import leaf every consumer deep-imports); cross-ref 20 §N2b |
| **16 §0 Z4 row** (`16-keyboard-shortcuts.md:31`) | "**D30 W-D (in flight) — Z4, the scrub-row double-apply guard.** The package's Viewer scrub row gains `data-transport`; the port keymap ignores events whose target is inside `[data-transport]`… Owner: S-package + S-app. Acceptance: one ArrowLeft at the scrub row = exactly one frame step." | The S-PACKAGE half LANDED (`755235c`): stopPropagation on the scrub row + ruler-root arrow/Home/End branches (`Viewer.tsx:296-307`, `Ruler.tsx:233-245`), the `data-transport` marker already at `Viewer.tsx:317`; acceptance pinned (`AppShell.test.tsx:505-517`). The S-app/port-keymap half is the app scout's call | **P2** | Flip the row's S-package half to DONE (commit `755235c`, the pin site); keep the S-app half's status as reported by the app scout |
| **06 §5.0 D32 fan-out table + base-verb list** (`06:413-428`) + **§5.10 Duplicate** (`06:1681-1692`) | The ten-mode table has no Duplicate row; the base-verb inheritance line (`:428`) lists "split… trim… delete/range-removal… move… rate-stretch" — **duplicate absent**; §5.10 is the OpenCut quote (clone animations, `alwaysNew` placement) with **no link law** | **Duplicate severs `linkedTo` on the copy** — mock: `useUiStore.ts:938-945` ("the splitElement R14 law's twin — a verbatim copy made a ONE-WAY pair… Severing on the copy keeps the original pair truthful and the dup free to form its own link"); routed: severing is BY CONSTRUCTION (new ids carry no sidecar meta — `nle-test-app/sceneBridge.ts:505-511`, "linkedTo is a pure doc sidecar"); OT's core has no link field (`types/index.ts:180-229`, no link member) | **P2** | Add the duplicate sever law: one sentence in §5.10 + a base-verb entry in the §5.0 list ("duplicate — the copy severs `linkedTo`; the original keeps its pair") |
| **06 §5.15 Mute** (`06:2051-2072`) | The OpenCut quote carries `canTrackHaveAudio(track)` — the kind-aware routed law | The mock twin now matches: `isMuteableTrack` shared by BOTH toggleMuteAll halves (`useUiStore.ts:325-333`) — the same ⌘⇧M converges to the same target per world; text-only/empty overlays never flip | P3 | Optional: a one-line note that the shell's mute-all convergence target = the muteable set (kind-aware), citing the shared law |
| **15 §4.3.15 `UpdateElementsCommand`** (`15-wire-protocol.md:951-977`) + **06 §7** (`06:2300`) | `updates: Array<{trackId, elementId, patch: Partial<TimelineElement>}>`; "Each user action is wrapped in a single `Command` … so undo undoes the whole action" | FW-D's `setElementFieldAll` is a PACKAGE store action, **not a wire verb** — its wire form is ONE `'patch'` dispatch carrying `elementIds[]` (`timelineRouter.ts:116`) which the app folds into ONE `timeline.updateElements` batch → one engine history entry; the routerless half is ONE `withHistory`. 18 §4.4's "one `updateElements` per settle" (`18:194`) is now literally true (was N-per-settle under the fan-out) | P3 | Register the UNDO-GROUP CONTRACT in 18 §4.4's commit-semantics paragraph (one user verb = ONE undo unit in the OWNING domain; a mixed patch = one entry in EACH domain, the ⌘Z facade takes two presses; keep controls single-domain) — no 15 change needed (the updates[] array already carries the law); optionally a 06 §7 note that a multi-selection Inspector patch = one `updateElements` |
| **Test-count citations 674→690 (the re-pin workload)** — 38 lines across 14 files | The corpus cites nle-ui 674 at BOTH the R24 pin (`fc4cc35`) and the R26 pin (`3026099`) | nle-ui @ `32abd58` = **690/690** (live-run verified this scout; trajectory 674→678→679→681→682→689→690 per commit messages + `worklog.md`). Sites: **00** `:3/:19/:400` · **03** `:4` · **04** `:4/:18` · **08** `:4/:19` · **09** `:4` · **10** `:12` · **11** `:14` · **12** `:4/:14/:18/:36` · **13** `:14` · **16** `:4/:17/:34/:2398` · **17** `:4/:18/:20/:37/:2359/:2730` · **18** `:4/:14/:15/:513` · **19** `:5/:13/:32/:212/:352/:422` · **IMPLEMENTATION-PLAN** `:22/:42/:52` (line numbers drift with edits — re-grep at fold time) | **P1 mechanical** (ARCH-R27:34: "battery_r26 136/141 — the 5 fails are pure pin-lag") | The W6 re-pin wave: every §0 fleet row + the 00 §2A fleet table move to `32abd58` 690 + the consumer pin (app vendored nle-ui @ `83ff8a8` with the AW1-2 absorb queued — `nle-test-app/.agents/PLAN.md:143-146`); R24-era lineage rows may keep their historical counts per the lineage convention (00:400's D21 re-type) |

---

## §4 Open questions

1. **The §5.11/§5.12 home split (P3 above):** the nle-ui code + spec 18 cite §5.11 for preserve-pitch while §5.12 is the Retime section. Is §5.11 acceptable as the citation home (its `shouldMaintainPitch` quote carries the API), or does the amendment consolidate on §5.12 and fix the code citations? Recommend the latter; one home, cross-ref'd.
2. **The engine-side duplicate link law:** the engine's own TimelineData stack (the render/export venue model per D12) carries `linkedGroupId` — does IT need an explicit sever-on-duplicate statement, or is the doc-side law (09's pairwise `linkedTo` + the app sidecar's id-keyed meta) sufficient? The nle-ui/app evidence says severing is by-construction in the OT-core world (no link field) and explicit in the mock; the spec should state which world's mechanism is normative.
3. **The consumer pin for the pin world:** the app pins nle-ui @ `83ff8a8` (pre-AW1-2). D21 (00:400) re-typed the pin world to "repo-HEAD pins AND consumer pins, both classes separately" — the R27 fleet table should carry the nle-ui consumer pin + the queued AW1-2 absorb explicitly (currently only in the app's own PLAN).
4. **The composite undo unit door:** FW-D's contract leaves it open ("Keep controls single-domain unless the facade grows a composite undo unit"). Should the spec schedule that facade growth (a mixed engine+sidecar patch reverting in one ⌘Z), or ratify the two-press law as final? No current control commits both domains, so this is a latent question only.
5. **The remaining F4-era P3s (out of this range but adjacent):** the domain-drift pairs #7/#8 (the Inspector Rate authoring max 400% vs the engine retime clamp [0.01,5] vs fit-to-fill's [0.1,5] acceptance — `Inspector.tsx:964` `max={400}`), the rename pointer #12, the Cmd+S latch #13, the bracket upper clamp #14 — the nle-ui F4 worklog registers these as still open; the spec 06 §5.9F/§11.7 rate-domain split (FF-2) is the related filed row. Confirm their disposition in the W3/W4 passes.
6. **Storybook build:** this scout ran vitest/tsc/boundary live but not the Storybook build (74 stories, CI-gated per the R25 card); no change in range touches stories, so no re-run was warranted — flagging for completeness.
