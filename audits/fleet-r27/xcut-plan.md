# The Implementation-Plan Overhaul — R27 W4 (xcut-plan)

**Task ID:** R27-W4-PLAN · **Agent:** xcut-plan · **Round:** R27 final-tightness audit, Wave 4 (the user's call-out #6) · **Date:** 2026-09-13
**The user's directive (verbatim):** "the overall implementation plan - forget about time estimates remove those all, but i need extremely clear sequencing and phasing, and well carved parallel workstream for what we can paralleze to maximize velocity. all these need to be extremely carefully analyzed to ensure they are fully operationalized and executable."
**Method:** IMPLEMENTATION-PLAN.md read complete (156 lines) + live re-derivation of every claimed-stale row against the R27 pin world (engine `f9ac806` 748/748 · OT HEAD `55c81c0`/code `970948a` 632/632 · WDC `ec8fd5c` 777/777 · nle-ui `32abd58` 690/690 · app `c020b2a` 252/252 · mini 448 in-flight · variants 1,773 live/1,762 registered) — the five W1 scouts, the six W2 mock reports (leverage/trim/insert/color/verdicts), spec-00/06/12+13/17/19's W3 reports, the engine's PLAN `:463-556` + `gaps/audit/ra-round/audit-AW1.md` §3, OT's PLAN `:440-560`, the app's PLAN `:45-147`, WDC's root HANDOFF, nle-ui's PLAN/DECISIONS — every claim below carries file:line or commit evidence. Report-only: no corpus edits, no commits, stayed on main.

---

## §1 THE AUDIT of the current plan (the removal list + the stale/missing rows)

### 1a. The removal list — EVERY time estimate in IMPLEMENTATION-PLAN.md

| # | Site (line) | The text | Disposition |
|---|---|---|---|
| R1 | :3 (the doc-scope sentence) | "THIS doc owns HOW/**WHEN**/WHO/WHERE — workstreams, sequencing, parallelism, entry points, gates, **estimates**, protocol" | REMOVE "estimates" from the charter; WHEN becomes gate-ordering, not calendar |
| R2 | :20 (S-engine row, item 3) | "the color instruments (…; **the 4-6 wk estimate is the ARCH-R22 reviewed register**)" | REMOVE the clause; the instruments' acceptance = spec 08 §0's enumerated milestones, green-as-queued |
| R3 | :23 (S-spec row, item 5) | "the r1-entry decision material: the mode fleet's 10 verdict cards + the ~70-76 gap rows + **the +1.5-3 wk rider**" | REMOVE the rider; the material is superseded by the leverage map (§1c-M4) |
| R4 | :73 (r1 row) | "**the one-business-day app migration** + keymap-sync sub-gate" | RE-TYPE event-based: "the app migration lands at the next app session after the rename (the pin-bump protocol's bound)" |
| R5 | :111 (critical path) | "**Solo:** serial along the path, the ∥ streams ride whenever idle. **Two-dev:** one on S-app (K3/K4), one on S-ot/S-package (…) — **the crawl's 5-11/3-6 estimate assumes this split**" | REMOVE the staffing model + the estimate; keep the pairing hint as "the highest-leverage pairing" (a parallelism fact, not a schedule) |
| R6 | :117 (protocol item 2, pin-lockset law) | "the app pin-bumps **within one business day**" | RE-TYPE event-based: "the app pin-bumps at its next session boundary — before the next fleet round runs" (a staleness bound, not a duration estimate; battery flags violations) |
| R7 | :155 (the entire Estimates paragraph) | crawl ≈ **5-11 wk solo / 3-6 two-dev** · walk ≈ **10-15 / 7-10** · run ≈ **20-28 / 13-17** (18-25.5 without r4) · the mode-family rider **+1.5-3 wk / +1-2 two-dev** | REMOVE the paragraph wholesale — replaced by gate-defined phases (§4's §2: a phase ends when its exit gate is green, never when N weeks pass) |

**Count: 7 removal/re-type sites; zero duration figures survive.** Everything else in the plan that smells temporal is already event- or gate-based (the [H:] human gates, the D28.2 productization trigger, the D6 re-affirmation) — those stay.

### 1b. The stale rows (the plan drives work — every stale "do X" invites a duplicate landing)

| # | Plan site | Says | Live truth (evidence) | Fix |
|---|---|---|---|---|
| S1 | :19 S-app (1c) | "**land the maintainPitch residual HERE** (`engineService.ts:477/:486` hardcodes `maintainPitch: true`…)" | **RESOLVED in code** — `94d5a2e`+`2de34ac`/`d8d1b2b`: `engineService.ts:510-513` read-merge-write, `sceneBridge.ts:262/:447` both projections, the GluedShell R9-b pins `:700/:761` (scout-app §1.6); the app's own PLAN row `:47-52` is stale — tick it, don't re-file | Strike the row; add the app-hygiene tick (PLAN/HANDOFF refresh) |
| S2 | :19 S-app (1b) + :42 + :52 | the census-CI "EXECUTED R26" but "the register (41 = 35 zero-action (7 byte-exact + 28 mechanical-exact)…)" | register is **42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ `6e2b91a`** (`docs/port-census.md:29-37`, census-check live-run; mutation gate **8** scenarios, was 6) | Re-base the arithmetic everywhere |
| S3 | :19 S-app (5) | "K3's audio pins + the VLM net (the audio pins STILL hard-gated on S-wdc's unlanded waveform promotion — the only K3 sub-half with an external prerequisite…)" | **FALSE twice over**: the WDC contract landed R26 (`035afe8`/`387f327`) AND the app's K3 audio pins LANDED @ `44e8049` — `src/waveformPeaks.test.ts` (9 tests) consuming the EXPORTED contract names. Only the VLM net remains | Flip the sub-half to DONE; K3 now has ZERO external prerequisites |
| S4 | :19 S-app (2) | "K3's store/policy halves ∥ NOW (unstarted, **unblocked**)" | Unstarted (correct) but **no longer unblocked-first**: the app's own PLAN ordering + scout-app OQ6 put the D-ARCH-6 re-pin first (the ported coverage gate goes red at the re-pin until `use-timeline-actions.ts:452` + `use-keyframe-drag.ts:96` switch to the batch wire verbs) | Re-order: the re-pin is S-app's head (see M3) |
| S5 | :19/:42/:52/:54 (the app's counts) | "the app's vitest (**206→**)", "322 authored + 33 projection pins", the "24 routed + 6 exceptions"/"24/24" family | **252/252, the 8-suite roof** (GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + **wire-coverage 9** + engineSeam 7 + **waveformPeaks 9**); the wire census **31 = 28 routed + 3 exceptions** (D-ARCH-6) | Re-base all app counts |
| S6 | :20 S-engine | "the engine's vitest (**458→**)", "471/471, the 13-test pin file" | **748/748** (25 test files; bridge-seams 151); the E1/E2 pin file is **26 tests** (H/F review folds); timeline.ts **8,997 LOC** — every pre-R26 line pin moved ~1,500 lines | Re-base; §1c-M1 carries the pin-hygiene rule |
| S7 | :18 S-ot (1b) | "WIRE_UI_EXCEPTIONS as a first-class shared export (OT R9-d — **the 6-verb exception registry is test-local today, zero src matches**)" | **LANDED @ AR-1** (`40f22af`, D-ARCH-3): `api.ts:328-335` exported tsc-asserted + the derived Set; the runner copy deleted; consumed LIVE by both gates | Strike the row (done-by-landing) |
| S8 | :18 S-ot (1c) | "R9-b's keyframe-authoring ruling ASK (…the ruling gates any R9-b work)" | **LANDED @ AR-2** (`8a3eb35` upstream + app): the dblclick authoring both venues + the registry reconciliation 6→5 (later 5→3 at D-ARCH-6) | Strike the row |
| S9 | :18 S-ot (4) + :84 (the r1 verb list) | "K2's runner registration (…green **24/24**…)" / r1's OT family "`timeline.roll`/slide/`trim{ripple}`/insertEdit/overwrite/replace/append/**insertBatch**" | M49C is **28 routed + 3 exceptions**; **insertBatch LANDED** (`970948a` api.ts:1262-1370) — it is not an r1 verb; `removeKeyframes`/`retimeKeyframes` also landed | Re-key the gate; move insertBatch out of r1 into DONE; add the r1 param-alignment decisions (singular-absolute vs plural-delta; per-element vs flat lists) the landing forced |
| S10 | :21 S-wdc | "the S4 seal round is CLOSED — **759/759**, P3-only, **zero upstream drift**" | **FALSE**: the S-series upstream wave absorbed (19 locked files + the 18-file closure, fork `f5011b3`) — **777/777 @ `ec8fd5c`**; WDC's own HANDOFF is materially stale (scout-wdc §2) | Re-write the row head + add M6 (the docs wrap) |
| S11 | :21 S-wdc (3) | "the FIVE deferred upstream test ports (…5 bodies…)" | **LANDED engine-side** — `upstream-ports.test.ts` (G1 @ `3a28b6e`, 25 tests: all five bodies re-expressed against the relocated bridge, the no-bridge-surface dispositions documented in-file) | Strike from S-wdc; register as done (the port venue moved to the engine per D12) |
| S12 | :22 S-package | the C0 MiniShell + the filing-gap residuals (D36.4: "the still-open filings STAY") | Verified STILL OPEN: **no MiniShell in nle-ui src**; **no C0 row in nle-ui's PLAN** (grep-empty); DECISIONS jumps D28 → #30 (no D29 entry); P-widen `:210`/P-lock-route `:219` unchecked | Row stands — the oldest open filing set in the fleet (3 rounds) |
| S13 | :23 S-spec | battery_r25 + the R25 fold + "the two mocks' suite-count pins — variants 1521+ / mini 355" | battery_r27 this round; the register reads **variants 1,762/64 (live 1,773)** + **mini 448 in-flight** (re-keys at the sibling's WRAP only — the coordination law, mock-verdicts §3) | Re-key (M5) |
| S14 | :42 (§1) | the R25 pin world ("engine `3989506` (458…)/OT `fdb771c`…/app `64fb0ab` (206…)/mini 445/variants 1521+") | the R27 pin world (header above); consumer pins: app→engine `f9ac806` + OT mirror `6e2b91a` + nle-ui `83ff8a8` + WDC `ec8fd5c`; engine→OT `6e2b91a` + WDC `ec8fd5c` | Re-base (battery_r27 enforces) |
| S15 | :93 (§4) | "Runs in parallel RIGHT NOW… S-app's `.agents/` + census-CI filings…" | The `.agents/` bootstrap (`7fd3eb6`) and the census-CI execution both LANDED R26; the parallel set needs the re-carve (§2 below) | Re-draw |
| S16 | :125-133 (§6) | "This round's steps (the S-spec track's work order — the R25 record)" | History; the R26 + R27 records exist in the worklog/ARCH docs | Fold to the lineage table (§6) — a standing plan should not carry a round-specific step list |

### 1c. The missing rows (verified absent from the plan — grep-confirmed)

| # | The missing row | The evidence it must carry |
|---|---|---|
| M1 | **The NS-1..NS-5 design queue** (engine PLAN `:463-485`, filed from OT's SA-1) | NS-1 transport/render-clock coupling (→ spec 03; app-owned glue today, undocumented) · NS-2 real-media peaks for the timeline UI (→ 20/02; rides the K3-consumption of the waveform contract) · NS-3 clip-frame thumbnails (→ 02; `(mediaId, sourceSec) → drawable`) · NS-4 **SceneTracks→GPU render path (the r1-entry architecture decision — 04/07)** · NS-5 edit→A/V reschedule classification (→ 03/20). The plan carries NOTHING (scout-engine §3: "grep: no NS-1…NS-5") |
| M2 | **The r1-port collision map** (engine `gaps/audit/ra-round/audit-AW1.md` §3 `:193-204`; registered engine-side PLAN `:12` + `:433-437`) | 7 rows: (1) E1/E2 linked-companion + relink — **NONE in OT — "the biggest single hole"** (the linkage-model decision); (2) E3 error contract → the port's envelope; (3) F5 mode clamps → port with the ops; (4) N3 locked-track guards (36 ops) → op-by-op; (5) the constants/domain lattice → ONE constants module (extend OT's `core/audio-params.ts`, the OV-01/02 one-home precedent); (6) sync-lock groups → op family or bridge-side; (7) the two headless wires → **spec-15 wins**. OT's own next-horizon queue (`:550-556`) files the same three (linkage/constants/wire) — bilateral agreement, zero plan presence |
| M3 | **The D-ARCH-6 filings' execution** (filed app-side @ `c020b2a`, PLAN `:104-147`) | (a) the OT re-pin `6e2b91a` → the s17 tip (`55c81c0` mirror) + **the port's gesture-seam switch** — `src/timeline-port/hooks/use-timeline-actions.ts:452` (`core.removeElementKeyframes`) + `use-keyframe-drag.ts:96` (`core.retimeElementKeyframes`) → the batch wire verbs origin 'ui' (the ported gate goes RED at the re-pin until they switch); (b) the pool multi-insert rides `insertBatch` (one per kind, staggered construction — the filed design `docs/design-r9-d30.md:210/269`); (c) the nle-ui re-pin absorbing AW1-2. **This is S-app's new head** |
| M4 | **The A3/color verdicts' consumption** (mock-color.md + mock-verdicts.md) | The r3 row is a stub today. The phase-entry artifacts now exist: the **venue table** (fragment passes on the rgba16float working texture; CPU-baked LUT data; compute scopes on-GPU; the CPU lib = oracle, not venue), the **A3 wheels gesture law** (relative/accumulating + master dial + dbl-click color-only), the **node-graph execution model** (unspecced — C56), the **C50 grade-sidecar/sequential ruling**, and the **curve composition-order conflict** (spec 08:299 "channel(master(x))" vs the mock's y∘ch `gradedFrame.ts:83-108` — the D37 ruling proposal, spec-08 F6). All four P1s are r3's phase-entry decisions, exactly like the r1 scope gate |
| M5 | **The register maintenance, re-keyed** (D35, standing) | The WRAP-only coordination law (6 rules, mock-verdicts §3): register 1,762/64 vs live 1,773; mini 448 vs the briefing's 415 vs spec-19's 428 — **per-wave movements are recorded, not chased**; the register re-keys at the sibling's round-WRAP only. The plan's S-spec row still says "variants 1521+ / mini 355" |
| M6 | **The S-series docs wrap** (WDC-side; scout-wdc §4.1) | The sync commit was code-only; the root HANDOFF is materially false in three claims (the 4→5 off-by-one `:73-77`; SKILL's coreOwned 5-vs-14 `:25-26`; the missing S-series wrap). In NO queue — the plan's S-wdc row must file it |
| M7 | **The OT P3 register sweep** (standing; OT PLAN `:554-556`) | The HA-1/2/3 P3 batches, the 305 fixed sleeps, a11y depth, dead CSS, V-S16 NEW-2/3 — a standing crawl-tail stream the plan never carried |
| M8 | **The D37/D38 namespace in the ruling-basis header** | The header cites ARCH-R24/R25 only; ARCH-R27 is the live ruling venue (D37+ open: the curve order, the preservePitch/volume-dB registrations) and this overhaul itself is D38 |
| M9 | **The app's DeliverPage blob contract** (scout-app §2, "still open") + the stale PLAN/HANDOFF hygiene rows | The one remaining CR from the R9 filer; plus the tick-list rows S1/S13 |

---

## §2 THE DEPENDENCY RE-DERIVATION (the K-ladder's real position at the R27 pins)

### 2a. Where each rung actually is

| Rung | The real position (evidence) | What genuinely remains |
|---|---|---|
| **K1** | **GREEN everywhere**: engine 748/748 · OT 632/632 (M49C 28/3, live-registry-read) · WDC 777/777 · nle-ui 690/690 · app 252/252 · the mocks in flight (mini 448; variants 1,773) — every §0 GAP row's owner-repo carries its pin | Nothing (the standing green-keeping discipline) |
| **K2** | The INSTRUMENT is green (M49C + the W-F port both consume the exported registry); the engine-side pair corpus is LARGER than the plan knows: `timeline-edit-ops` (25) + `timeline-linked-source-edit` (26) + `timeline-mode-clamps` (20) + `timeline-locked-track` (14) + `timeline-relink-fixes` + `upstream-ports` (25) — the H/F/G waves landed the mode-matrix wired-subset pairs engine-side (the parity twin per D12) | (a) the **12/17 registration** (the combined-family census + the 10-mode matrix twin row + the zero-orphan-rows law — spec-12/13's U5: "the registration home itself is missing the row it will need"); (b) the **OT-ops→engine-decode round-trips at the app's seam shapes** (extends `timeline-edit-ops`/`video-sync`) |
| **K3** | LANDED: the routed-verb completeness machine-check (W-F port, LIVE-registry law, the AR-2 gesture pin) + the Z6 dead-zone sweep + **the audio pins** (`44e8049` — S3 kills the plan's last external-prerequisite claim) + M2 Wave 1 app-side. OPEN: the D-ARCH-6 re-pin + gesture-seam switch (**the gating duty — the ported gate goes red at the re-pin until the two hooks switch**), the store/policy corpus (322 authored + 33 projection pins — unstarted), the mode-matrix K3 rows, the compensating shell-path pins, the data-test mapping row → the DOM-structural half, the VLM net | Six sub-halves, all app-owned + one S-spec registration; **zero external prerequisites** |
| **K4** | Unstarted (no e2e suite at the app) | The whole rung: import(virtual) → cut → play → export, the same routed verbs, the shell-path exception |

### 2b. The hard edges (what gates what — re-derived; every edge verified live)

```
E1. S-app's D-ARCH-6 re-pin + gesture-seam switch  →  the ported coverage gate's greenness (NOT other repos' work)
E2. The data-test mapping row (S-spec/S-app)       →  K3's DOM-structural half ONLY (W-C landed; the swap-layer premise retired)
E3. S-package's C0 MiniShell                        →  w1's chrome wiring (NOT the crawl's path — crawl-tail as ever)
E4. S-wdc's M2 Waves 2-3 + the async pre-retime     →  r2's depth (not crawl; Wave 1 consumed already)
E5. The linkage-model ruling (Stage 0)              →  the r1 ports of insert-edit/overwrite/replace/ripple-overwrite + the companion fan-out
E6. The constants-module decision (Stage 0)        →  every r1 op that touches retime/speed domains (D11's lattice: one module, OT's core/audio-params pattern)
E7. The E3 error contract + N3 guard survey (Stage 0) → every r1 verb's envelope + lockPreCheck extension
E8. K2+K3 gates both green                          →  K4 (the crawl exit needs both rungs' gates, not their labels)
E9. K4                                              →  w1 (the crawl's completion proof precedes humans)
E10. The spec-track battery/fold                    →  the round cadence (gates rounds, never code)
```

**The genuinely-serial spine is SHORT:** the re-pin → K3's halves → K4 → w1 → w2∥w3 → r1-entry (the scope gate + D28.2) → the r-phases. Everything else rides parallel forever (E1-E4 are local to their streams).

### 2c. The r1 dependency re-derivation (the leverage map's answer — `mock-leverage.md` §4/§5)

- **The algorithms exist** (engine, complete, E1/E2-fixed, 20+26-test carried corpus, deliberately UNWIRED per D12 — venue-internal, the parity twin that pins r1); **the interaction plumbing exists** (OT: gesture controllers, the wire registry + tsc-lockstep, `withUndo`, `validateInsertElement`, placement, the ripple diff, 632 tests + the self-re-declaring census); **the UX grammar + composite planners exist** (the variants mock: `insertPlan.ts` preview==commit, 37 fit-to-fill pins, `trimLaws.ts`). Nothing is greenfield except **replace** (both sides) and the **linkage model** (the biggest hole — OT has NO linkage field at all).
- **Stage 0 — the three mechanism decisions BEFORE any port** (the trials-and-errors the user wants to avoid): (1) the linkage-model ruling (opencut field/registry vs dispatch-level + bridge-side relink; spec-side owner 06 §5.0 + 09 §B1); (2) the constants module (D11's lattice + volume-dB via OT's `core/audio-params.ts`); (3) E3's error contract + the N3 guard survey (36 ops; OT's `lockPreCheck` is the seam).
- **Stage 1 — the trim family** (roll → ripple-trim → slip → slide): shared infra first (the binary-search clamp engine `clampDeltaToPreserveEditState` + the junction-drag session in `resize-controller.ts`), then the greenfield slip/slide ops with G-SLIP-4's five-convergence delta-space pinned ONCE.
- **Stage 2 — the source-edit family** (insert-edit → overwrite): the E1/E2-tested algorithms + the 26-test pin file carried; `SourceEditBar`/`insertPlan` the UX reference (the preview==commit planner pattern ports into OT's preview/commit seam).
- **Stage 3 — the composites** (append → fit-to-fill → ripple-overwrite): all ride LANDED verbs (`insertBatch`, `updateElements{retime}`, the delete+move+insert trajectory); append + fit-to-fill are the cheapest of the ten.
- **Stage 4 — replace (the one greenfield) + the wave-2 families** (rate-stretch command, freeze-frame, range-removal, close-gap, join) + sync-lock (its op-family decision resolved by then).
- **Throughout:** every port lands with (a) the carried engine tests green in OT, (b) the census self-re-declaring (D29.2 — proven three times: 24+6 → 25+5 → 28+3), (c) the mode's 05 §8A grammar row + D32 fan-out row, (d) the battery's mode-matrix check re-derived (D30.1).

---

## §3 THE DESIGN RATIONALE (sequencing + parallelism + operationalization)

**(a) Gate-defined phases.** Every phase's exit is a machine-checkable predicate ([P]) or a registered human gate ([H:reason]). A phase ENDS when its gate is green — never when N weeks pass. This is not just the removal of estimates; it is what makes the plan executable by agents: a gate is a command you run (a suite, a script, a grep-count, a battery class). The one place time appeared as a bound (the pin-bump freshness) is re-typed as an event (the next session boundary, battery-flagged).

**(b) The sequencing** follows §2's spine: the re-pin first (it is the only work that can turn a currently-green gate red — the gate-breaking duty), then K3's six halves (parallelizable among themselves once the re-pin lands), K2's registration rides S-spec's standing round (it is a doc row, not code), K4 last of crawl, w1's humans, then the r-ladder with Stage 0 mechanism decisions as r1's phase-entry gate (exactly like the absent-family scope gate — they are USER-visible decision points with the material already filed).

**(c) The parallel carve** (the maximum-velocity set): **eight streams** — the six repo tracks + the spec track + the mocks track. The carve law: (i) the module streams never touch each other's trees — cross-repo work is FILED as titled queue blocks (the cross-project queueing protocol, OT SKILL law 71); (ii) a stream's internal order follows its own gates; (iii) the ONLY cross-stream edges are §2b's E1-E10 — everything else is spurious sequencing to be deleted on sight; (iv) the mocks track coordinates through the register (WRAP-only re-key) so its waves never block ours and vice versa; (v) K2 is verification, not construction — it tightens as the pairs land, it never gates a code stream.

**(d) The operationalization law**: every workstream row carries OWNER-REPO + ENTRY POINT (a file path or a filed queue block) + EXIT GATE (machine-checkable) + its DEPENDENCY EDGES. A leaf row is startable by a cold executor with zero questions. The §4 draft below is written to that bar (the §0 table's cells are self-contained work orders; the cold-executor re-simulation pattern from R25 carries forward as the fleet's verification instrument).

**(e) The user-gated decision points stay VISIBLE and early**: (1) the **r1-entry absent-family scope gate** (now materialized as the leverage map's §4 table — 6 ports + 3 cheap composites + 1 greenfield; the user passes/defers the four absent families); (2) **D28.2 the productization gate** (r1-entry OR the first outside consumer — whichever precedes; opens the seal-snapshot option); (3) **D6's r4 re-affirmation** (real-media decode — deprioritized, stays user-gated); (4) the **C7 rename** (r1 END, mechanical, one session); (5) the **D37 rulings** at the r3-entry (the curve order + the A3 gesture law + the node-graph model + the C50 ruling — the venue table).

---

## §4 THE DRAFT REPLACEMENT TEXT (the complete new IMPLEMENTATION-PLAN.md)

````markdown
# IMPLEMENTATION-PLAN — the executable plan (separate from the spec set)

**What this is:** THE plan — the execution runbook for the NLE project. It is deliberately NOT a numbered spec: the spec set (`00`-`20`) owns WHAT and the acceptance criteria (each spec's §0 FORWARD INVENTORY — BASE pinned / GAP with owner+acceptance / TEST PLAN, the R22 posture law); THIS doc owns HOW/WHO/WHERE — workstreams, sequencing, parallelism, entry points, gates, protocol. **There are no time estimates in this plan (D38.1): a phase ends when its exit gate is green, never when N weeks pass.** A gate is a command you run; a row without a machine-checkable gate is a spec bug.
**Ruling basis:** `audits/ARCH-R24-timeline-strategy-and-topology.md` (v2 — D26 the adopt-wholesale convergence / D27 the repo topology / D28 evolve-in-place + the productization gate / D29 the seam-finalization instruments; D23 plan separation + D24 the ladder stand, D25's mechanism amended per D26) + `audits/ARCH-R25-edit-mode-completeness.md` (v2 — D30 the mode-matrix law / D31 the source-edit family / D32 the linked-companion law / D33 the visual-grammar register / D34 the keyboard family / D35 the reference register / D36 the execution wiring) + **`audits/ARCH-R27-final-tightness-audit.md` (D37+ the live namespace; the R27 fleet's evidence packs: the five scouts + the six mock reports — above all `audits/fleet-r27/mock-leverage.md` the leverage map, whose Stage 0-4 order IS r1's internal sequencing) + D38 THIS overhaul (the estimates removal + the gate-defined phases + the parallel-carve law — §5 of `audits/fleet-r27/xcut-plan.md`)**. Supersedes spec 14's plan content (retired to a redirect stub; the lineage in §6).

**Stage vocabulary (D24):** **CRAWL** = the programmatic-verification layer (no human eyes; K1 module nets → K2 combined nets → K3 app-behavior nets → K4 the automated e2e exit). **WALK** = the real app wiring with humans (w1 grammar+fidelity+human rounds, w2 media, w3 project). **RUN** = the full NLE at DaVinci/web-DAW depth (r1-r6). The stages label VERIFICATION DEPTH — code lands continuously in every stream; a stage's exit gate is what sequences.
**Crawl's purpose (the user's law):** eliminate as many issues as possible before the walk — "ideally everything can continue to be tested programmatically"; human-required gates are registered below WITH reasons.
**The convergence spine (D12, ratified both sides `1064f91`):** opencut-timeline is THE editing engine (SceneTracks SSOT + the spec-15 wire); the engine's stack is the render/export venue model; the junction is one-directional (`SceneTracks → bridge → the venue stack`); engine-native edit verbs are FROZEN venue-internal — the offline reference + the parity twin that pins r1. The ten-mode ports land in OT's ops layer (D12.3); the engine methods are the algorithm sources, never the wire.

---

## 0. HOW TO EXECUTE — the entry points (start here)

**If you are starting work:** identify your track in the table, open its repo, orient per its row's ORIENTATION cell (the fleet convention is the target repo's `.agents/HANDOFF.md` + `PLAN.md` — exceptions noted per row; WDC keeps its handoff/plan at the repo ROOT), read this doc's §3 track section, execute the FIRST ACTION, and let the GATES tell you when the step is done. **Environment prerequisites (all repo rows — the R24 cold-executor audit, re-verified by every scout wave since):** the app's private vendor submodules need `NLE_GH_PAT` + `npm run bootstrap` (README: PAT → non-recursive submodule init → install → annotakit build) before any gate runs; **OT** runs `bun install` AND starts its dev server FIRST (`bun run dev` → :3001 — `bun run test` drives Playwright against `http://localhost:3001/` and does NOT self-start it; `bunx playwright install chromium`; plain `npx playwright install` rewrites package.json pins + creates package-lock.json, revert both); **WDC** runs `bun install --frozen-lockfile` (its drift gate additionally needs an upstream web-daw clone, `sync --check --source <clone>` — currently clean at lock `f5011b3`); **nle-engine** runs `npm install` + `git submodule update --init` with PAT remotes (else the live-run venue is GHA CI — the OV-12 vendored-timeline gate runs the vendored tree's own suite at every gitlink move); **nle-ui** runs `npm ci` from the committed lockfile (the Storybook build rides CI). Every step ends with: gates green → commit → push → (if module repo) notify the app for the pin bump (the file-queue protocol: a titled block in the target repo's queue doc). **A step with no testable gate is not a step — write the gate first.**

**Taxonomy (one sentence):** a TRACK is a repo-owning stream (S-ot/S-app/S-engine/S-wdc/S-package/S-spec + the mocks track); a PHASE is a ladder rung (K1-K4 crawl / w1-w3 walk / r1-r6 run — each ends at its gate); a WORKSTREAM is a track's worklist slice through the phases (§3).

| Track | Repo (entry dir) | First action (the current work order — each item: entry point → exit gate) | Gates (per step) | Done-state (this window) |
|---|---|---|---|---|
| **S-app** | `/home/z/my-project/nle-test-app` (orient: `.agents/` — landed `7fd3eb6`) | **(1) FIRST — the D-ARCH-6 filings' execution (the gate-breaking duty; filed @ `c020b2a` PLAN `:104-147`):** (a) the OT vendor re-pin `6e2b91a` → the s17 tip (`55c81c0` mirror; UPSTREAM.lock + the census reference together); (b) **the port's gesture-seam switch** — `src/timeline-port/hooks/use-timeline-actions.ts:452` (`core.removeElementKeyframes`) + `src/timeline-port/hooks/use-keyframe-drag.ts:96` (`core.retimeElementKeyframes`) → the batch wire verbs (`timeline.removeKeyframes`/`timeline.retimeKeyframes`, origin 'ui'; upstream's `use-keyframe-drag.ts` takes an OPTIONAL `dispatch` prop — "pass it and the switch is one line"); (c) the pool multi-insert rides `timeline.insertBatch` (ONE per KIND — the 15 §13.15 kind-split; staggered construction `startTime[i] = base + Σ durations[<i]`; firstAvailable; one history entry per kind-group — the filed design `docs/design-r9-d30.md:210/269`); (d) the nle-ui re-pin absorbing AW1-2 (Gain ceiling [−60,+20]). GATE: the ported wire-coverage gate green against the LIVE 31/28+3 registry (it goes RED at the re-pin until (b) lands — that red IS the acceptance discriminator); the singular verbs absent from the vendored `WIRE_COMMAND_TYPES`. **(2) K3's store/policy halves ∥ (the LAW-NET corpus re-expression — 322 authored + 33 projection pins, unstarted):** over the REAL vendored OT; the re-expression target = the app's real surfaces (the GluedShell store family + `engineService.ts` + `sceneBridge.ts` — CORE-SEAMS §2 is the field-by-field map; LAW-NET-INVENTORY.md the acceptance list); each family's tests land in the app's vitest beside the surface they pin. **(3) the mode-matrix K3 rows:** the slip pin (slip-G-SLIP-7: a wireLog 'slip' assertion + the D28 dispatch pin — `store.slipNudge` → the `slip` case in engineService exists), the composed ripple family (ripple-trim-G4: 4 units/16 tests over `timeline.trim`/`rippleDelete` — `trimToPlayhead(edge, ripple)` half-pinned at GluedShell :361/:368), the selectTool controller (ROLL-7: the tool radios stop being inert view-state), the companion clamp (slip-G-SLIP-6: mirror `_slipImpl`'s tightest-δ). **(4) the compensating shell-path pin family** (the trusted-integrator verbs ⌘B/⌘M/⌘⇧M/,/. driven directly through `engineService`, owner-tagged — the D30 R2/R8 owned consequence; ⌥[/⌥]/⇧M already pinned). **(5) K3's DOM-structural half** (gated ONLY on the data-test mapping row): D26.5's three-way vocabulary — OT `data-test=` for timeline units (73 unique names; the counting convention pinned first — spec-19's finding) / nle-ui `shell-*` for chrome units / the structural-gap worklist for no-counterpart units; the mini's census gets the mapping row. **(6) the VLM net** (adopt `ui-mock/shell-variants/scripts/vlm-*` onto the app's `.storybook` + OT's runner; REFERENCE-REGISTER.md's rows are the verification worklist; the coordination law: our amendments land before the sibling's VLM pass, else their net verifies dead law). **(7) K4** (the e2e: import(virtual) → cut (drag/trim/split/ripple) → play (engine clock) → export — the same routed wire verbs through the real UI; the shell-path exception applies identically). **(8) w1 → w2 → w3** (§2's walk rows; unchanged in law). **(9) the app's hygiene rows:** tick the stale maintainPitch PLAN row (the code landed — `94d5a2e`; do NOT re-file), the HANDOFF refresh (H2-B-stale), the DeliverPage blob contract (the one open CR). **DONE, struck from the work order:** the `.agents/` bootstrap (`7fd3eb6`), the census-CI execution (`9d310f8`+`3e4f0cd`; the register 42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + host @ `6e2b91a`, mutation gate 8/8), the D30 waves (`64fb0ab`), the K3 audio pins (`44e8049` — `waveformPeaks` 9 tests on the exported contract), the maintainPitch residual, M2 Wave 1 app-side (`1aebbd5`). | the app's vitest (**252→**), `tsc`, `npm run boundary`, `vite build`, the census script (CI-enforced), the wire-coverage gate | the D-ARCH-6 re-pin + seam switch green against 31/28+3; the K3 corpus row-by-row green (322 + 33 + the mode rows + the shell-path pins); the mapping row enumerated; the VLM net live; **K4: the automated e2e green, zero mock paths** |
| **S-ot** | `/home/z/my-project/opencut-timeline` (orient: `.agents/`) | **(1) the carrier-reduction work order — STILL UNFILED (ordered R24; verified absent at `55c81c0` — file it THIS window** as the titled block in `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol): the 6 optional injectable props (the selection-bridge pair `initialSelectedIds`/`onSelectionChange`/`selectionIds`, the zoom bridge `zoom`/`onZoomChange`, `confirmDelete`, the `timeline-scroll` anchor, `cancelRegistry` injection — additive-optional, classic defaults M-pinned) + `EMPTY_CONTEXT_ITEMS` + the functional `setPos(null)` + the W8 `!shiftKey` playhead-guard carve-out (the hole-documentation landed `fdb771c`; the OT-side code fix stays live) + the jsdom no-DragEvent guard/docs note; each landing flips an app carrier toward exact (census-tracked). **(2) the data-test stability contract (D26.5):** freeze the convention AS the stability contract + close the census gaps — NOT a new `data-testid` layer. **(3) the view-config surface** (zoom-ladder config + ripple-toggle exposure — the C1 OT-side halves; `rippleMode` is a toolbar prop today, not the exposure). **(4) K2's in-page runner registration** (the runner + M49C exist and are green 28 routed + 3 exceptions via the live registry read — the registration is S-spec's 12/17 row, not OT work). **(5) the structural grammar half** (crawl-tail: the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas — additive components, classic defaults M-pinned). **(6) the token half at w1-entry** (the mini theme mode on `globals.css` `:root` + `theme.ts` — D25.3a). **(7) the r1-port design round, OT-side rows** (the next-horizon queue `:550-556`, the collision map's OT half): the linkage-model row + the constants-module lattice + the wire convergence (spec-15 wins, OT's dialect) — the design round that precedes the family ports. **(8) the r1 op-family ports per the leverage map's stages** (§2's r1 row; the shared clamp infra + junction-drag session FIRST; every port carries the engine's tests + the census self-re-declares). **(9) the P3 register sweep** (standing: the HA-1/2/3 P3 batches, the 305 fixed sleeps, a11y depth, dead CSS). **DONE, struck:** `WIRE_UI_EXCEPTIONS` export (AR-1, `api.ts:328-335`), the R9-b keyframe-authoring ruling (AR-2 — the dblclick authoring landed + the registry reconciled), the batch verbs `insertBatch`/`removeKeyframes`/`retimeKeyframes` + the singular retirement (D-ARCH-6, `970948a`; 632/632; M49C 28/3). | `bun run dev` (:3001) THEN `bun run test` (632→, the M-report json, 72 entries, M49C 28/3 via the live read of BOTH exported registries), `tsc --noEmit`, the real-mouse suites; every prop/contract lands with new M-pins for every classic default | the carrier-reduction work order FILED then landed (carriers flip, census-tracked); the data-test contract frozen; the view-config surface + the structural components exist additively; the mini theme mode exists at w1; the r1 design-round rows filed |
| **S-engine** | `/home/z/my-project/nle-engine` (orient: `.agents/`) | **(1) the honest-preview widening (R9-d, unchanged-OPEN):** Vignette/Glow/LUT-preview v1 remain; only the Gaussian-Blur subset landed (`8a0b7fe` + `4b2dfd1`). **(2) the projector parity corpus** (the N1 composition-frame family's formal S4 suite — incl. the W2.5 effects sidecar + `buildElementFilterString`; the projector duty itself is LANDED-AS-BRIDGE per D12 — `src/lib/nle/bridge/`). **(3) the color instruments** (scopes/qualifier/power-window — spec 08 §0's S-engine row; r3's consumption; the venue table is the r3-entry artifact). **(4) K2's engine-side pairs:** the OT-ops→engine-decode round-trips at the app's seam shapes (extends `tests/vitest/engine/timeline-edit-ops`/`video-sync`) — the mode-matrix wired-subset pairs LANDED via the H/F/G waves (`timeline-edit-ops` 25 + `timeline-linked-source-edit` 26 + `timeline-mode-clamps` 20 + `timeline-locked-track` 14 + `timeline-relink-fixes` + `upstream-ports` 25); what remains is the seam-shape family. **(5) the NS-1..NS-5 design queue** (PLAN `:463-485`): NS-1 transport/render-clock coupling · NS-2 real-media peaks (rides the K3 consumption) · NS-3 clip-frame thumbnails · **NS-4 the SceneTracks→GPU render path (the r1-entry architecture decision — with the collision map, this is the design round that shapes r1)** · NS-5 edit→A/V reschedule classification. **(6) the r1-port parity-twin role (D12):** the engine methods stay FROZEN venue-internal — the E1-a..d/E2-a residue clarifications resolve AT the port (the OT session's rows); the carried test corpus (`timeline-linked-source-edit` 26 + `timeline-edit-ops` 25 + `timeline-mode-clamps` 20 + `timeline-locked-track` 14 + `timeline-relink-fixes`) is the port's acceptance. **(7) the async pre-retime** (design-w2 §4; the WDC-parallel stream). **DONE, struck:** the R9-queue reconciliation (executed R26), E1/E2 (FIXED `8188d4e`+`412693d`), the mode-defect rows ROLL-2/ROLL-3/slide-G3/ripple-trim-G3 (landed via F5 — `timeline-mode-clamps.test.ts`), the FIVE upstream test ports (G1 `3a28b6e` — `upstream-ports.test.ts` 25), M2 Wave 1 (`50b91f5` — the bus-law + sidechain-parity, consumed), the OV convergence (OV-01/02/05/12), D12 ratified. | the engine's vitest (**748→**, 25 test files), `tsc`, the layer fence, the OV-12 vendored-timeline CI gate | the widening + the parity corpus + the instruments' enumerated milestones each green as queued; the NS queue's five seams each ruled or landed; the K2 seam-shape pairs green |
| **S-wdc** | `/home/z/my-project/web-daw-core` (orient: the repo-ROOT `HANDOFF.md` + `PLAN.md` — NOT `.agents/`) | **(1) FIRST — the S-series docs wrap (in NO queue today; scout-wdc §4.1):** the root HANDOFF's Where-things-stand rewrite (the S-series absorbed: 19 locked files + the 18-file closure, lock `f5011b3`), the 4→5 test-port off-by-one fix (`:73-77` + PLAN `:145`), the SKILL coreOwned census 5→14 (`:25-26`), the worklog entry. **(2) the M2 mixer surface, Waves 2-3** (the reverb/authoring surfaces + PDC — design round FIRST, `design-m2-mixer-surface.md` v1.1 the wave map; the F6 el.effects-sidecar decision included). **(3) the async pre-retime** (with the engine's design round). **(4) the P3 ledger** (upstream-class notes, F12, F9, F10/F11, R4, F4). **DONE, struck:** the waveform-contract promotion (R26 `035afe8`/`387f327` — survived the S-series byte-verbatim, 14 pins inside the 777), the FIVE deferred upstream test ports (LANDED engine-side as G1 — the venue moved per D12), M2 Wave 1 (engine-side; consumed app-side `1aebbd5`). | WDC's vitest (**777→**, 34 test files), `tsc`, the drift-gate (fails-on-drift; needs an upstream web-daw clone) | the docs wrap landed (HANDOFF truthful); the M2 Waves 2-3 green as queued; the pre-retime ruled + landed |
| **S-package** | `/home/z/my-project/nle-ui` (orient: `.agents/`) | **(1) C0 MiniShell** (the w1-prep chrome family: Topbar/Inspector frame/MediaPool frame/Splitter+R18j laws/Toast role=alert/Viewer frame + tokens/qc- CSS; slots-compatible; package-owned minimal placeholder; zero engine imports — the boundary script) — **still NOT STARTED (verified: no MiniShell in src — 3 rounds the oldest open row)**. **(2) the filing-gap actions (D27.1/D36.4 — still open, verified):** file the C0 MiniShell row in nle-ui's OWN PLAN (grep-empty today) + file the D29 DECISIONS entry (the ledger jumps D28 → #30) + tick P-widen + P-lock-route (`PLAN:210/:219`, unchecked). **(3) the mode-matrix package rows (r1-adjacent):** the ripple-tool inert-for-trim posture (ripple-trim-G7: route the gesture — the variants' `Clip.tsx:586-595` the reference — or document mode-inert until OT's G2 lands) + the slip keymap law (slip-G-SLIP-5: the runtime-fps resolver or the registered deviation). **(4) the AW1-2 mixer-panel twin (NEW — the [−60,+20] hole only spec-19 saw):** `src/components/mixer/ChannelEditor.tsx:91/:99` still caps at `max={4}` — the Inspector is +20, the Mixer panel is not. **(5) the F4-era P3s:** the Inspector Rate authoring max 400% vs the fit-to-fill [0.1,5] acceptance domain (`Inspector.tsx:964`), the rename pointer, the Cmd+S latch, the bracket upper clamp. **(6) W-ops→r1 keymap surfaces later; r5 polish rows (C12/C11/C14/C15).** | the package's vitest (**690→**), `tsc`, the boundary script, the Storybook build (rides CI) | MiniShell renders in the package Storybook with the chrome laws green; the C0 row + the D29 entry filed + the P-ticks ticked; the ripple-tool + slip-keymap postures registered; the ChannelEditor ceiling +20 |
| **S-spec** | `/home/z/my-project/nle-core-spec` (this repo) | **(1) battery_r27 FIRST** (the R27 pin world + the census-coherence re-key 28+3 with the lineage tail 24→28→30→24+6→25+5→31=28+3 + the AR-2 live-registry consumption law + the register checks + the plan-gate re-points to THIS doc; the new check classes: the line-pin re-base map as ONE consolidated input (spec-19 §2), the app census arithmetic 42/36+5, the origin-attribution clause, the mode-matrix presence checks). **(2) the K2 registration at K2 entry:** the combined-family census + the 10-mode matrix twin row in 17 + the D30.3 zero-orphan-rows law (every matrix row LANDED or carrying a live r1-scheduled row with owner + acceptance — the completeness is over the REGISTER, not over landed-ness). **(3) the C-ledger rolling disposition + the register maintenance (D35, standing every round):** REFERENCE-REGISTER.md's counts re-derive live (a drift without amendment = a battery failure); **the WRAP-only coordination law** (the register re-keys at the sibling's round-WRAP only; per-wave movements recorded, not chased — register 1,762/64, live 1,773; mini 448 in-flight); zero un-tagged C-rows older than two rounds; the D26.4 mock-retirement triggers key on the LANDED flips. **(4) the D37+ rulings this round:** the curve composition order (spec-08 F6's proposal — y∘ch), the A3 wheels gesture law, the node-graph execution model, the C50 sidecar/sequential ruling, the color venue table (the r3 phase-entry artifacts), the preservePitch + volume-dB D-registrations. **(5) the user-gate preparation:** the r1-entry absent-family scope decision material = the leverage map §4/§5 (the ten modes: 6 engine-complete ports + 3 cheap composites + 1 greenfield) + the D28.2 productization timing. | the battery (r27, then every round); the sibling-race merge-first protocol | battery_r27 green; the K2 census registered at K2 entry; the register current (fresh + intake-complete); the D37 rulings folded |
| **Mocks track** | `ui-mock/shell-mini` + `ui-mock/shell-variants` (in-repo; orient: each mock's `docs/` + the R25 §6 verdict ledger) | The sibling streams run their own waves (mini R24-2 in flight through W3+; variants R25 insert-rescue landed, more waves coming); **the fleet's only duties are the register coordination** (re-key at WRAP only; the 6 rules — mock-verdicts §3) + the amendments-before-VLM ordering (their net verifies our amended rows, never dead law). The register rows flip LANDED as the owning specs' rows land (the D26.4 retirement triggers). | the register's battery classes; each mock's own suite (mini 448→; variants 1,773→) | the register re-keyed at each WRAP; zero register/battery drift |

**The single global entry point:** this table. When in doubt: S-app row → step (1) (the D-ARCH-6 re-pin + seam switch — the gate-breaking duty; everything else in the crawl rides ∥ behind it), then step (2) (the K3 corpus — the crawl's bulk).

---

## 1. The fleet (cited, not re-declared)

The pin world lives in `00-master-spec.md`'s fleet table (D21 — HEAD pins AND consumer pins, both classes; the battery enforces coherence; 00-master is the canon). Current reference set (2026-09-13, the R27 pins; **battery_r27 re-baselines the pin world once, centrally, after the fleet**): engine `f9ac806` (**748/748**, 25 test files; code anchor `50b91f5`) / OT HEAD `55c81c0` (**code pin `970948a`**, src-diff empty; 632/632 — 481 in-page + 151 real-mouse across 72 entries; **the wire census 31 = 28 routed + 3 exceptions**, M49C machine-checked via the live registry read) / WDC `ec8fd5c` (**777/777**, 34 test files; lock `f5011b3`; the S-series absorbed) / nle-ui `32abd58` (**690/690**) / app `c020b2a` (**252/252**, the 8-suite roof; census 42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ OT mirror `6e2b91a`) / mini **in flight** (448 it-blocks live; register re-keys at WRAP) / variants **in flight** (register 1,762/64; live 1,773). **Consumer pins (live):** engine→OT `6e2b91a` + WDC `ec8fd5c`; app→engine `f9ac806` + OT mirror `6e2b91a` + nle-ui `83ff8a8` (AW1-2 absorb queued) + WDC `ec8fd5c`. **This plan never re-declares pins — it cites them.**

## 2. The stage ladder (D24) — exit gates + gate classes

Every gate is tagged **[P]** (programmatic — the default; nets, pins, scrapes) or **[H:reason]** (human-required — the registered exception, only at walk for the classes the user named). **A rung's exit gate is a predicate over the repos at their pins — it is checked, never estimated.**

### CRAWL — the programmatic-verification layer

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **K1 module nets** | each repo's own suite, extended per its spec §0 GAP acceptance rows (the R27 state, live-verified by the scouts: engine 748/748 @ `f9ac806`, OT 632/632 @ `55c81c0` incl. the M49C gate 28 routed + 3 exceptions, WDC 777/777 @ `ec8fd5c`, nle-ui 690/690 @ `32abd58`, app 252/252 @ `c020b2a` — live-run) | every repo's suite green at its own gates; every §0 GAP row's owner-repo carries the row's pin | [P] |
| **K2 combined nets** | the named cross-module family: engine-in-the-middle (OT-ops × engine composition), WDC's integration family, OT's in-page UI-over-core runner (pixel reads + real-mouse) — censused + registered in spec 12/17 (the registration is S-spec's pending row; the mode-matrix twin row in 17 rides it) + **THE 10-MODE EDIT-MATRIX registered in spec 06/17** (D30.1: mode \| engine math \| OT wire verb \| UI affordance \| pins \| verdict; baseline at the R27 pins: 6/10 engine-complete-and-pinned (roll/ripple-trim/slip/slide/insert-edit/overwrite — the E1/E2 fixes + the 20+26-test carried corpus), 0/10 wired, 4/10 spec'd composites (replace/append/ripple-overwrite/fit-to-fill)) | the family census green; the missing pairs closed (the OT-ops→engine-decode round-trips at the app's seam shapes — the mode wired-subset pairs LANDED engine-side via the H/F/G waves; the seam-shape family remains) **+ the mode matrix registered + ZERO ORPHAN ROWS (the D30.3 K2 form: every matrix row is either LANDED or carries a live r1-scheduled row with owner + acceptance — the completeness is over the REGISTER, not over landed-ness; the ports land at r1 per D30.2, and K2 checks the register)** | [P] |
| **K3 app-behavior nets** | the app wiring driven headlessly. **LANDED:** the routed-verb completeness machine-check (the W-F port — the module-scope accumulator + the gesture sims + `accumulated ⊇ WIRE_COMMAND_TYPES − WIRE_UI_EXCEPTION_VERBS` through the real mounted UI; **consuming the LIVE exported registry** — the AR-2 consumption law: a consumer gate consumes the exported registry, never a hand-mirrored copy; + the volume-line dblclick → `timeline.upsertKeyframe` origin 'ui' gesture pin) + the Z6 dead-zone sweep + **the offline audio pins** (`44e8049` — the waveformPeaks suite on WDC's exported contract; the R26 prerequisite MET and consumed) + M2 Wave 1 app-side. **OPEN (six halves, all app-owned, zero external prerequisites):** (a) the D-ARCH-6 re-pin + the port's gesture-seam switch (the gate-breaking duty — see S-app (1)); (b) the LAW-NET corpus re-expression (322 authored + 33 projection pins — incl. the composed ripple family; r1 graduates the OT interval-diff family; LAW-NET-INVENTORY is the acceptance list); (c) the mode-matrix K3 rows (the app's mode-coverage pin family — the slip pin, the composed ripple family, the selectTool controller, the companion clamp; the app column matches the LANDED set with pins + the honest-absent verdicts registered); (d) the compensating shell-path pin family (the trusted-integrator verbs driven through `engineService`, owner-tagged); (e) the DOM-structural gate = D26.5's three-way data-test mapping (gated ONLY on the mapping row); (f) the VLM visual net | the coverage gate green (against the live registry) + the compensating pin family green; the corpus row-by-row green; the mapping row enumerated present/absent/structural-gap; the audio pins green (DONE); the mode-coverage family green for the wired subset | [P] |
| **K4 the crawl exit** | the single automated e2e suite: import(virtual) → cut (drag/trim/split/ripple) → play (engine clock) → export — driving the same routed wire verbs through the real UI (D29.1c; the shell-path exception applies identically) | **green end-to-end, zero mock paths, zero human input** — the crawl's completion proof | [P] |

**Ordering law (hard edge):** K3's DOM-structural half gates on the data-test mapping row ALONE. K3's store/policy halves run ∥ behind the D-ARCH-6 re-pin (the gate-breaking duty lands first or the ported gate goes red). The crawl's structural grammar work lands IN the crawl window as OT additive components — **crawl = structure + behavior; w1 = tokens + fidelity**.

### WALK — the real app wiring, humans enter

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **w1 grammar + human rounds** | the token half of the grammar (the qc- theme on OT's CSS surface), the MiniShell chrome wiring (C0 consumed), the viewer-frame fidelity, the app-vs-OT-runner side-by-side, the annotakit review loop for the app | the side-by-side VLM pass; **the human test rounds on the complex timeline operations** (the class the user named — drag/trim feel, interaction latency, edit-session flow); every finding lands as a new programmatic pin wherever possible or is registered with owner | [P] + [H:interaction-feel] |
| **w2 media layer** | the MediaRecord registry + probe + lookup, the full event staircase (spec 15 §9.5), telemetry (the cloudcut hybrid-storage RFC a design input only, NO dependency — D27.3) | media round-trip pins; every staircase row published + consumed + pinned; the full-scope demo on virtual media re-gates (K4's suite extended) | [P] |
| **w3 project layer** | ProjectJSON persistence (spec 09), multi-scene + scene wire ops, cross-scene-undo + history budget | project round-trip vs per-scene OT toJSON; the scene suite green | [P] |

**Walk exit:** a real edit session — import real media → edit → save → reload → export — human-tested with the findings register closed or owner-tagged. **[H:real-session]**

### RUN — the full NLE (DaVinci Resolve + web-DAW class, the shell-variants scale)

| Phase | Deliverable | Exit gate | Class |
|---|---|---|---|
| **r1 op depth** (was W-ops) | **The ten-mode family per the leverage map's stages** — the trials-and-errors avoidance plan (`audits/fleet-r27/mock-leverage.md` §4/§5, the r1-port contract): **Stage 0 FIRST (the mechanism decisions):** the linkage-model ruling (the collision map's biggest hole — OT has NO linkage field; 06 §5.0 + 09 §B1 the spec owners) + the constants module (D11's lattice + volume-dB; OT's `core/audio-params.ts` the one-home pattern) + E3's error contract + the N3 guard survey (36 ops; `lockPreCheck` the seam) + the error-envelope refinement (spec 15 §6.3 first). **Stage 1 the trim family** (roll → ripple-trim → slip → slide; the binary-search clamp engine + the junction-drag session first; G-SLIP-4's delta-space conventions pinned once). **Stage 2 the source-edit family** (insert-edit → overwrite; the E1/E2-tested algorithms + the 26-test pin file carried; the insertPlan preview==commit pattern). **Stage 3 the composites** (append → fit-to-fill → ripple-overwrite — all over LANDED verbs). **Stage 4 replace (the one greenfield) + the wave-2 families** (retime/freezeFrame/rangeRemoval/closeGap/join) **+ sync-lock** (the op-family decision resolved by then). **Every port:** the carried engine tests green in OT + the census self-re-declaring (D29.2, proven thrice) + the 05 §8A grammar row + the D32 fan-out row + the battery's mode-matrix re-derive. **C7 rename at END** + the app migration at the next app session + the keymap-sync sub-gate + the r1 param-alignment decisions (singular-absolute vs plural-delta; per-element vs flat lists; insertBatch's bare-verb vs superset) | S1 dispatch-complete + typed NOT_IMPLEMENTED honest; the carried engine tests green in OT per port; OT suite + carried tests green; spec 15 §13.15 rows flip ALIGNED | [P] |
| **r2 audio depth** (was W-audio) | mixer G-wiring full depth, sidechain/PDC/automation shapes, the M2 Waves 2-3 consumption (Wave 1 — the bus-law + sidechain-parity — LANDED + consumed; the SoundTouch offline pitch + varispeed W2 LANDED `de09c93`, consumed; N2b keyframed volume LANDED `37cdd28`, consumed) | offline parity ≤ −60 dBFS any channel + realtime behavioral pins; the null rig passes | [P] |
| **r3 color** (was W-color) | the grade-math binding + parity; the S-engine instruments consumed (scopes/qualifier/power-window). **Phase-entry artifacts (the D37 rulings — mock-color/mock-verdicts):** the venue table (fragment passes on the rgba16float working texture; CPU-baked LUT data; compute scopes on-GPU; the eyedropper the one mandatory readback; the CPU lib = oracle, not venue), the A3 wheels gesture law (relative/accumulating + master dial + dbl-click color-only), the node-graph execution model, the C50 grade-sidecar/sequential ruling, **the curve composition order (D37 proposal: y∘ch)** — all ruled BEFORE the parity pins are written | grade-math parity pins (max delta ≤ 1 LSB-equivalent); scopes plot engine data + pinned | [P] |
| **r4 real media decode** (was W-n5) | N5: the media registry + real decode → VirtualMediaAsset | **PENDING the user's D6 re-affirmation (deprioritized)** | [P] |
| **r5 interchange + polish** (was R-fcpxml/R-polish) | FCPXML (greenfield; the parser+fixture-corpus choice is the phase-entry artifact), keymap long tail (C22 ledger), i18n, a11y residuals | validates vs the chosen reference parser on the corpus; spec 18 §11 audit; battery | [P] |
| **r6 the non-blocking tail** (was R-engine-p2/R-cloud) | CPU transition renderers, Shape/Lottie/Subtitle/Controller surfacing, other engine P2; cloud render UNCHARTERED unless the user re-scopes | per-item engine milestones green as queued; slips independently by design | [P] |

**The user-gated decision points (registered, visible, early):** (1) **the r1-entry absent-family scope gate** — the decision material is the leverage map §4: 6 engine-complete ports + 3 cheap composites (append/fit-to-fill/ripple-overwrite) + 1 greenfield (replace); the user passes or defers the four absent families' inclusion at r1-entry; (2) **D28.2 the productization gate** — r1-entry OR the first consumer outside the six-stream fleet, whichever PRECEDES; at that gate (and only then) the seal-snapshot option opens (tag the sealed shas, cut clean public packages via export-ignore/docs-reorganization — a future user-visible ARCH decision); (3) **D6's r4 re-affirmation**; (4) **the C7 rename timing** (r1 END, mechanical); (5) **the D37 color rulings** (r3-entry).

## 3. The workstreams (the tracks' worklists, keyed to spec §0s)

Each track's worklist rows cite the OWNING spec's §0 GAP register (the posture law — acceptance lives there, not here). Phase tags use the D24 vocabulary; the old→new mapping is §6.

**S-ot (opencut-timeline):** the carrier-reduction program (filed THIS window) + the data-test contract + the view-config surface → the structural grammar half (crawl-tail) + the token half (w1-entry) → **the r1-port design round OT-side rows (the linkage model + the constants module + the wire convergence) → the r1 op-family ports per the leverage stages (Stage 1 trim → Stage 2 source-edit → Stage 3 composites → Stage 4 replace + wave-2 + sync-lock) + the carried-test families + the gesture routing** → C7 at END. **The P3 register sweep rides ∥ throughout.**
**S-app (nle-test-app):** the D-ARCH-6 re-pin + seam switch (FIRST — the gate-breaking duty) + the app hygiene ticks → the K3 corpus (store/policy ∥ mode rows ∥ shell-path pins) + the mapping row → DOM-structural → the VLM net → K4 → w1 (token wiring + side-by-side + human rounds) → w2 media (spec 15 §9.5 + spec 09's media half) → w3 project (spec 09) → r5 fcpxml.
**S-engine (nle-engine):** the widening + the parity corpus + the instruments + the K2 seam-shape pairs ∥ **the NS-1..NS-5 design queue (NS-4 = the r1-entry architecture decision)** + the r1 parity-twin role (frozen venue-internal; the residue clarifications resolve at the port) + the async pre-retime → the r2/r3/r6 consumption points as they land.
**S-wdc (web-daw-core):** the S-series docs wrap FIRST → the M2 mixer surface Waves 2-3 (design first) + the async pre-retime → the P3 ledger → r2 depth.
**S-package (nle-ui):** C0 MiniShell + the filing-gap residuals (the C0 row, the D29 DECISIONS entry, the P-ticks — 3 rounds open) + the ChannelEditor AW1-2 twin + the ripple-tool posture + the slip keymap law (r1-adjacent) ∥ crawl → w1 consumption → r1 keymap surfaces → r5 polish rows.
**S-spec (this repo):** battery_r27 + the R27 fold → **the K2 registration at K2 entry (the 12/17 twin rows + the 10-mode matrix + the zero-orphan-rows law)** + the C-ledger rolling disposition + **the register maintenance (D35: the WRAP-only re-key law)** + **the D37+ rulings (the color phase-entry artifacts + the preservePitch/volume-dB registrations)** + the user-gate preparation (the leverage map as the r1-entry decision material) → the mock-retirement triggers (post-K4/w1, keyed on the register flips).
**Mocks track:** the sibling waves + the register coordination (re-key at WRAP only; the amendments-before-VLM ordering).

## 4. The parallelization map + critical path

**Runs in parallel RIGHT NOW (the eight-stream carve — the module streams never touch each other's trees; cross-repo work is FILED, never unilateral):** S-app's re-pin + K3 corpus ∥ S-ot's carrier-reduction filing + program + the r1 design-round rows ∥ S-engine's widening + parity corpus + instruments + the NS queue ∥ S-wdc's docs wrap + M2 Waves 2-3 ∥ S-package's C0 + the filing gaps + the ceiling twin ∥ S-spec's battery_r27 + the D37 rulings ∥ the mocks' own waves ∥ the standing P3 sweeps (OT's register; WDC's ledger; nle-ui's F4-era P3s).

**The dependency graph (the hard edges — §2b's E1-E10, drawn):**
```
S-spec: battery_r27 + the R27 fold ─────────────────── (the round cadence; gates ROUNDS, never code)
                                                         
S-app:  the D-ARCH-6 re-pin + gesture-seam switch ─┬─→ K3 store/policy ∥ mode rows ∥ shell-path pins ─┐
        (the gate-breaking duty — FIRST)           │                                              ├─→ K4 ─→ w1 ─→ w2 ∥ w3 ─┐
S-ot:   the carrier-reduction program ∥ ────────────┤   the data-test mapping row → K3 DOM-structural ┘                        │
        the structural grammar half (crawl-tail) ──┘                                                                        ↓
S-package: C0 MiniShell ─────────────────────────→ w1's chrome wiring (NOT the crawl's path)                r1 .. r6
S-engine: widening ∥ parity corpus ∥ instruments ∥ K2 seam-shape pairs ∥ the NS queue (NS-4) ─→ r3's consumption            ↑
S-wdc:   the S-series docs wrap → M2 Waves 2-3 ─────────────────────────────────────→ r2's depth                             │
Mocks:   mini R24-2 ∥ variants R25 waves (the register re-keys at WRAP only) ──────────→ the VLM net's worklist              │
                                                          r1-ENTRY ────────────────────────────────────────────────────┘
                                                          ↑ the user gates: the absent-family scope gate + D28.2 productization
K2: the 12/17 registration + the zero-orphan-rows law (S-spec; rides ∥ — the REGISTER, not landed-ness)
```

**Critical path:** S-app's re-pin → the K3 corpus → K4 → w1 → (w2∥w3) → the r1-entry user gates → the r-phases. **The r1-internal critical path is Stage 0** (the linkage model + the constants module + the E3/N3 contracts — the mechanism decisions that unblock 5/6/7/9 and every envelope). **The highest-leverage pairing** (a parallelism fact, not a schedule): one stream on S-app (the re-pin + the K3 corpus + K4), one on S-ot/S-package (the carrier-reduction + the structural half + the chrome + the r1 design rows) — the crawl's two ends.
**∥-validity laws:** w2 media ∥ w3 project holds (different seams, both app-owned after w1's shell stabilizes); r1 ∥ r2 ∥ r3 hold at run (different repos); the color instruments (S-engine) run ∥ crawl but r3's gate only requires them at consumption; K2 ∥ everything (it is verification, not construction — it tightens as the pairs land).

## 5. The execution protocol (standing practice)

1. **Push at every micro-milestone** (GIT IS THE DISK); `git fetch` before every push; **never force push** (merge-first on sibling races — documented and recurring; the OT/app fetch-first coordination is the live precedent).
2. **Pin-lockset law:** module lands first under ITS gates; the app pin-bumps at its next session boundary — before the next fleet round runs (the battery flags violations; the OV-12 engine gate runs the vendored tree's own suite at every gitlink move); HEAD-follow opens bump PRs, never pushes main.
3. **The battery runs after every spec round** (battery_r27, the successor — see §6/§7); recalibrate stale checks; exempt windows look BEFORE and AFTER each hit.
4. **Decisions go through adversarial review rounds** (fresh-context sub-agents) before implementation — the ARCH-R22/R23/R24/R25/R27 trail is the pattern.
5. **A gap row with no acceptance is a spec bug; a facet with no coverage-matrix row is a spec bug** (the R22 laws, in 00-master's standing-laws section).
6. **The census discipline (D26, ENFORCED since R26):** the app's port tree stays a converging mirror governed by the census (register-equality, CI-gated recomputation via `census-check.mjs` + the 8-scenario mutation gate; a carrier without a divergence rationale is a census violation); upstream waves re-pin via 3-way merge — no-conflict files adopt-wholesale, carriers merge against the documented zone maps; the mini mock stays alive until the app matches it (the retirement triggers post-K4/w1, keyed on REFERENCE-REGISTER.md's rows flipping LANDED, D35.4). **The AR-2 consumption law (a consumer gate consumes the exported registry, never a hand-mirrored copy) is the fleet-wide generalization — proposed as a 00-master §2A standing law.**
7. **The drag-law freeze (D22)** and the count-discipline law (declared == actual at the pins) are standing — see 00-master.
8. **The productization gate (D28.2):** r1-entry, OR a packaging need — any consumer OUTSIDE the six-stream fleet pinning/consuming a module, or a distribution request — whichever PRECEDES r1-entry. At that gate (and only then) the seal-snapshot option opens (§2's r-phases note). Nothing before that gate.
9. **The cross-repo landing-vs-filing line (OT SKILL law 71):** land direct fixes whose gates you can run; file titled queue blocks for work whose acceptance lives in the other repo's machinery. **A filing older than two rounds is a finding** (the S-package C0-row/D29-entry pair is the live example).
10. **The pin-hygiene law (this round's lesson, spec-19 §5.9):** cite CONSTANTS and leaf helpers by file:line; convert method-family citations to symbol+file form (timeline.ts moved ~1,500 lines in three rounds while every constants pin held).

## 6. Lineage (the traceability appendix)

| Era | Plan | Fate |
|---|---|---|
| R9 | P0-P6 phases | retired to git history |
| R15 | A0-A7b assembly | superseded by R22 (absorbed into the W-rows) |
| R22 | spec 14 v2.1: C0-C4 / W-* / R-* | superseded by the R24 doc (D24's mapping re-homed every row) |
| R23 | the plan doc + ARCH-R23 | superseded by the R24 state (D23/D24 carried; D25 amended) |
| R24 | the R24 state + ARCH-R24 | superseded by the R25 state — D26 the convergence + census discipline; D27 the topology; D28 evolve-in-place + the productization gate; D29 the instruments |
| R25 | the R25-5 state + ARCH-R25 | superseded by the R27 state — D30 the mode-matrix; D31 the source-edit family; D32 the linked-companion law; D33 the visual-grammar register; D34 the keyboard family; D35 the reference register; D36 the execution wiring |
| R26 | the execution round (no plan revision — the amendments executed: E1/E2, the census-CI, the waveform promotion, the `.agents/` bootstrap, the W-G reconciliation filings) | folded through the R26 integration review; the record in git + the worklog |
| R27 | **THIS doc (the D38 overhaul) + ARCH-R27** | **current** — the estimates removed; gate-defined phases; the eight-stream parallel carve; the leverage map adopted as r1's internal sequencing (Stage 0-4); the NS queue + the collision map + the D-ARCH-6 duties registered; the D37 color rulings staged at r3-entry |

## 7. The audit fleet (the user's standing directive — per-module, per-file, per-track)

**The R27 wave (the pattern's current form):** (a) **W1 the module scouts** — five agents, one per core repo, each live-verifying its repo at the round's pins (HEAD, test counts, tsc, the seam surface, the queue, the spec-facing staleness) — the evidence packs every other wave cites; (b) **W2 the mock analysts** — six agents (the trim/insert EXTREME-depth reads, the variants delta harvest, the color WebGPU feasibility, the leverage research, the design-verdict absorption); (c) **W2b the briefing fold** — one orchestrator pass folding W1+W2 into the per-spec fleet's shared contract; (d) **W3 the per-spec fleet** — 18 agents, one per spec file, sub-spec depth, seeded by the briefing; (e) **W4 the cross-cuts + the plan** — five agents (the mode-matrix re-derive, the keyboard+wire-census coherence, the register maintenance, the signoff+seal coherence, THIS plan overhaul); (f) **W5 the adversarial review** on the amendment set + this overhaul; (g) **W6 the amendments + verify** (the spec edits + battery_r27 + the verification agents + the integration review). Agents edit ONLY their file, run NO git commands (the orchestrator commits per batch), and return a findings report. battery_r27 re-baselines the pin world once, centrally, after the fleet.
**The standing shape (every round):** the scouts → the mock/regional analysts as the round's questions demand → the per-spec fleet at sub-spec depth → the cross-cuts + the plan → the adversarial review BEFORE the amendments → the battery + the fold. **The R24/R25 waves are the precedent records** (`audits/fleet-r24/`, `audits/fleet-r25/`; the R23 per-file fleet the original directive's shape).
````

---

## §5 THE D38 RULING PROPOSAL (the plan-overhaul decision — for the ARCH-R27 fold + the W5 adversarial review)

**D38 — the implementation-plan overhaul (this document's mandate):**

**D38.1 — The estimates removal + gate-defined phases.** No duration figure, staffing-model split, or calendar bound appears anywhere in the plan. A phase ends when its exit gate is green — never when N weeks pass. The two latent SLAs are re-typed as event bounds (the pin-bump at the next session boundary, battery-flagged; the C7 app migration at the next app session). *Grounds:* the user's verbatim directive; the R27 evidence that the estimates had already rotted (the R25 re-based "5-11 wk HOLDS" paragraph itemizes four deltas that landed within two rounds — the estimate was a maintenance liability with zero decision content; every one of its inputs is already a gate or a user decision).

**D38.2 — The sequencing law.** The critical path is re-derived at every plan revision from the LIVE gate states (never inherited): today it is S-app's D-ARCH-6 re-pin → the K3 corpus → K4 → w1 → w2∥w3 → the r1-entry user gates → the r-phases. **r1's internal order is the leverage map's Stage 0-4** (the mechanism decisions → the trim family → the source-edit family → the composites → replace + wave-2 + sync-lock), and **Stage 0 is r1's phase-entry gate** exactly as the absent-family scope gate is: the three mechanism decisions (the linkage model, the constants module, the E3/N3 contracts) are the trials-and-errors the user named, and both repo-sides have already filed them (the engine's collision map; OT's next-horizon queue).

**D38.3 — The parallel-carve law.** The maximum-velocity set is the eight-stream carve (the six repo tracks + the spec track + the mocks track), bounded by exactly ten hard edges (E1-E10, §2b) — everything else is spurious sequencing and is deleted on sight. The module streams never touch each other's trees; cross-repo work is FILED as titled queue blocks; a filing older than two rounds is a finding; the mocks coordinate through the register (WRAP-only re-key) so neither stream ever blocks the other.

**D38.4 — The operationalization law.** Every workstream row carries owner-repo + entry point (a file path or a filed queue block) + exit gate (machine-checkable) + its dependency edges — a cold executor starts ANY leaf row with zero questions. The cold-executor re-simulation (the R24/R25 pattern) is the fleet's verification instrument for this bar, re-run at every plan revision.

**D38.5 — The user-gated register.** The decision points that belong to the user stay visible and EARLY in the plan (never discovered mid-phase): the r1-entry absent-family scope gate (material: the leverage map §4), D28.2 the productization gate + the seal-snapshot option, D6's r4 re-affirmation, the C7 rename timing, and the D37 color rulings at r3-entry.

**Adoption path:** the W5 adversarial review passes on this report → the W6 amendment wave replaces IMPLEMENTATION-PLAN.md with §4's draft verbatim (style-preserved: dense, cited, gate-first) → battery_r27 gains the plan-check classes (the estimates-absence check: `rg -c "wk\b|solo|two-dev" IMPLEMENTATION-PLAN.md` == 0; the D38 ruling-basis presence; the eight-stream carve presence; the leverage-map citation in r1's row) → 00-master's D-register records D38 at the fold.

---

**Report-only confirmed:** no spec/repo files touched outside this report + the worklog; no commits/branches; stayed on main.
