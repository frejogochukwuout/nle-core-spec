# R27-W3-02 — spec-02 (Workers & Threading) — the per-section audit

**Agent:** spec-02 · **Round:** R27 final-tightness, Wave 3 (per-spec fleet)
**Spec:** `02-workers-threading.md` (2,649 lines, read in full, section by section)
**Ground-truth pins:** engine `f9ac806` (code anchor `50b91f5`, 748/748, tsc 0 — gates lines at `c01a187`/`480a216`), WDC `ec8fd5c` (777/777, re-run live by scout-wdc), app `c020b2a` (252/252), OT HEAD `55c81c0`/code `970948a` (engine vendors OT `6e2b91a`). Engine vendored pins re-verified live: `git submodule status` → OT `6e2b91a`, WDC `ec8fd5c`; `grep -rn "new Worker(" src/` → **0 sites** (re-verified at `f9ac806`).
**Method:** every claim below re-derived against the live trees (reads/greps cited); scout reports (scout-engine/scout-wdc/scout-app) used as pin-count authority where they re-ran gates live.

---

## §1 The per-section verdict table

| § (lines) | Content | Verdict | Evidence |
|---|---|---|---|
| Status :4 + §0 heading :12 | "v-next R24… LOW-DELTA round"; "the R24 posture" | **P2** | The posture label is two rounds old; the R25–R27 movement (S-series, M2 Wave 1, RC) is NOT low-delta for this domain: the diagnostics/heartbeat mechanism, the worklet-bundle rebuild, two serving venues, and the flush-args law all landed |
| §0 :15 (engine BASE row) | `5036387` 458/458; "Vendors OT @ `c15a629`, WDC @ `494f6ff` (one docs-only commit behind WDC HEAD `85b81b0`…)"; "(R24: … this row's surfaces re-read unchanged at `5036387`)" | **P2** | Pins/count 3 rounds stale → **`f9ac806` 748/748**; vendors → **OT `6e2b91a` + WDC `ec8fd5c`** (submodule status, live). The row's named audio-thread surfaces all re-verified INTACT at `f9ac806` (soundtouch-processor.worklet.ts exists; segment-strip-adapter.ts:87 imports `retimeChannels`/`RETIME_RATE_MIN/MAX`, :469-470 `RETIME_CACHE_CAP_BYTES = 96 MB`, :218 three-site schedule law; realtime-engine.ts:52/:55 `onBusMeter`/`reattachBusMeters` + :21/:53 wireBuses sever-all restore; player.ts:3363/realtime-bridge.ts:478 `getScheduledClipSourceTime`; N2b `gainAutomation` threaded; W2a/W2k pins at tests/vitest/nle-bridge.test.ts:758/:899). But "zero audio-thread/worker surface movement" no longer holds: M2 Wave 1 (`50b91f5`) landed the bus-law + sidechain parity in `bridge/scene-mixer.ts` + **the engine's `public/worklets` git SYMLINK** (new venue, missing from this row) |
| §0 :16 (WDC BASE row) | "`85b81b0` — 759/759"; "(R24: … the 3 `public/worklets/` files + the worklet law files re-verified unchanged.)" | **P2** | Pin/count stale → **`ec8fd5c` 777/777** (34 files). The R24 parenthetical was true then; the standing row is now superseded by the S-series: `dsp-effects-worklet.js` REBUILT (+~75 lines, the `DspDiagnosedProcessor` mixin, `:315-380`), `diagnostics.ts` (376 L) + 17 more files in the 18-file closure — none mentioned. The row's surviving claims verified intact: `DSP_WORKLET_URL_CANDIDATES` root-base-first (effects.ts:157-161), the bounded-retry + unity-passthrough bootstrap contract (effects.ts:148, :332) |
| §0 :17 (app BASE row) | "`c885ece` — 174/174… the W2.1 sidechain ducking over WDC's `collectSidechainWires`, and the W2.4 deliver export… **whose offline path SKIPS worklet-backed inserts entirely** (`deliverService.ts`: "no worklet-backed insert effects in the offline path"), the K3 row's offline-side cost surface" | **P1** | The premise is DEAD (scout-app's 02:17 row, re-verified live): `deliverService.ts:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0` (the M2-Wave-1 §1a flush-args law; `WORKLET_BACKED_INSERT_TYPES` :170-172, `sceneHasWorkletBackedInserts()` :177-181; pins deliverService.test.ts:429-471) — offline export flushes worklet module loads before `startRendering`. The sidechain clause is also superseded: the app's `rewireSidechains` is DELETED (`audioService.ts:319-322` comment); the BRIDGE (`SceneMixer.materializeStrips`) owns the duck wires on both paths (single-owner law). Pin/count 4 waves stale → **`c020b2a` 252/252** |
| §0 :20 (worker-pool surfacing r6) | GAP row | SOUND | Zero workers at `f9ac806` (grep 0 sites) — row still open, correctly |
| §0 :21 (§8.1 decode worker r4, D6 pending) | GAP row | SOUND | `metadata.ts:83` still comment-only (re-verified); the export-path mediabunny imports are unchanged in kind |
| §0 :22 (worklet asset serving, K3) | "the app serves WDC's 3 worklet bundles… from its own `public/`… **today the app maps inserts but serves no worklets, so they silently passthrough**; re-verified at `c885ece`: the app's `public/` directory does not exist at all" | **P1** | The "serves no worklets / silently passthrough" clause is FALSE now. The M2 Wave 1 VENUE fix landed: the app's vite `serve-vendored-worklets` plugin (vite.config.ts:28-69 — dev middleware + `closeBundle` dist copy of `vendor/web-daw-core/public/worklets`, zero byte duplication) serves `/worklets/dsp-effects-worklet.js` at the first `DSP_WORKLET_URL_CANDIDATES` candidate. The literal `public/`-absence fact still holds (live: no `public/` dir) — but the app DOES serve the worklets now, browser AND export. The row misses the ENGINE's venue too (public/worklets symlink, M2 Wave 1). What legitimately remains K3: the real-browser venue pin (see fix-list) |
| §0 :23 (export-render worker offload r6) | GAP row | SOUND | `export/orchestrator.ts:10` "main-thread (no worker offload — Stage 3)" — still true |
| §0 :24 (async pre-retime r2) | GAP row | SOUND | Still open in BOTH owning queues (engine PLAN "G2: the async pre-retime, design-first" at :388/:396; WDC HANDOFF item 3); the adapter is still the synchronous O(span) WSOLA on the play edge (re-verified — no async path in segment-strip-adapter.ts) |
| §0 :26 (acceptance row) | "458/458 @ `5036387`, 759/759 @ `85b81b0`, 174/174 @ `c885ece`"; "WDC W2/W2l/… + W1k (759), app W2 wiring + W3 JKL (174)" | **P2** | The mechanical re-pin: 748/748 @ `f9ac806`; 777/777 @ `ec8fd5c`; 252/252 @ `c020b2a` |
| §0A :30-40 | Sections map | SOUND | Structural, still accurate |
| §1 :44-47 | Purpose | SOUND | Target-state; no live-repo claims |
| §2 :50-67 | Why workers | SOUND | Target-state rationale (FreeCut/OpenCut-classic citations are historical reference) |
| §3 :70-108 | Worker inventory + the zero-workers note :107 | **P2** | The note's fact HELDS at `f9ac806` (grep: 0 `new Worker(` sites; comment-mentions only, re-verified at mixer.ts:619, export/index.ts:13, orchestrator.ts:10, video-sync.ts:1186) — but the re-verify stamp is R24 (`5036387`); needs the R27 stamp |
| §4 :111-157 | ManagedWorker abstraction | SOUND | FreeCut quote-fidelity, target-state |
| §5 :159-240 | Memory discipline | SOUND | Target-state + FreeCut measurements (historical) |
| §6 :242-255 | COOP/CEOP | SOUND (P3 note) | Target-state. Observed: NO fleet repo sets the headers (app vite.config.ts has no `server.headers`) and no SAB exists anywhere — consistent (no consumer), but §6's "we set them anyway" is unlanded target; record only |
| §7 :257-310 | AudioWorklet special case | **P1** | The section's existing text (hard-RT deadlines, per-quantum, one audio thread per ctx) is sound, and §7.1's R23/R24 fleet-reality notes re-verify at R27 (soundtouch.ts/varispeed.ts were NOT among the 19 refreshed files — the W2 law intact; adapter consumption intact). But §7 carries **NONE of the landed audio-thread discipline**: the EngineDiagnostic bus + heartbeat/liveness monitor (`diagnostics.ts:1-376`), the in-`process()` protocol (`dsp-effects-worklet.js:315-380`: 256-quantum heartbeats, NaN scans, silence scans, preallocated messages), the stall **bypass-to-passthrough** recovery ladder (`effects.ts:441-507` — stall >3 s while running → bypassed, NaN → permanent bypass, graceful recovery without charging the bounded state-restore budget), and the **rt-safety-gate transitive-heritage fix** (`scripts/rt-safety-gate.ts:140-187` — the direct-extends match found 0 seeds after the mixin landed, the gate's PASS was vacuous; now walks local base classes cycle-safely). Corpus-wide fact #7: missing from ALL 21 specs; 02 is its natural home |
| §7.2 :302 | The R23/R24 fleet-census note ("exactly these two Blob-URL processors plus WDC's three served assets; no repo added worker/worklet code at the R24 pins") | **P2** | Census re-verified at R27 (grep `registerProcessor` fleet-wide): OT **0** sites, nle-ui **0**, engine `soundtouch-processor.worklet.ts` + the demo page (same processor), WDC `varispeed.ts` + the 3 bundles — the processor FAMILIES are unchanged, so the census shape holds. But the R27 delta is real: the dsp bundle was rebuilt (the mixin diagnoses all 3 of its processors), and TWO new serving venues landed (engine `public/worklets` symlink; the app's vite plugin) — the note needs the R27 re-verification sentence |
| §7.3 :304-309 | OfflineAudioContext cloud render | SOUND | Target-state; the WDC offline-render readiness gate (offline-render.ts:736-768 — await every effect's `ready`, bounded 2 s) is the landed analog, and its flush discipline is exactly what the app's flush-args law consumes |
| §8 :312-944 | Per-worker specs (8.1-8.9) | SOUND | FreeCut/OpenCut-classic quote-fidelity, target-state. The §8.1 note's mediabunny claims re-verified: export-path imports real (`orchestrator.ts:232` exact, `:836` drifted from :822; `settings.ts:302`/`:332` exact; mediabunny@1.50.8 the only runtime dep) |
| §9 :946-987 | Worker creation pattern | SOUND | Target-state |
| §10 :989-1027 | Error handling | SOUND | Target-state. §10.4's degradation table now has a LANDED AudioWorklet-side analog (the WDC bypass ladder) — the §7.4 amendment should cross-note it |
| §11 :1029-2169 | Q1-Q13 answered | SOUND | Reference-repo fidelity (static history; FreeCut/OpenCut-classic are pinned references) |
| §12 :2171-2181 | Test plan (6 classes) | SOUND | Test class 5 (AudioWorklet) should absorb the diagnostics-protocol tests when §7.4 lands — fold into FIX-1 |
| §13 :2184-2334 | Code references | SOUND | FreeCut/OpenCut-classic inventory (historical) |
| §13B :2336-2351 | Engine references table | **P2** | Header pins stale (458/`c15a629`/`494f6ff`). Line cites re-verified at `f9ac806`: worklet :13/:47 ✓, virtual-audio.ts:4 ✓, registry.ts:24 ✓, metadata.ts:83 ✓, orchestrator.ts:232 ✓, settings.ts:302/:332 ✓ — but `player.ts:1629` → **:1646** (the `renderVideoFrame` line) and orchestrator `:822` → **:836**. The row's substantive claims (zero workers; mediabunny export-only; the W2 half-supercession notes) all still true |
| §14 :2354-2537 | Corrections #1-#18 | SOUND | Historical record |
| Testing :2539-2645 | Tier 1/2/3 + property + assets + commands | **P2** | (a) The asset list cites `10s-test-pattern-1080p.mp4` and `single-clip-red.json` — **neither exists in 17 §5's canonical fixture list** (17 has `10s-red-1080p.mp4`, `10s-440hz-sine.wav`, and `single-clip.json` at 17:964), violating 02:2618's own "do not invent new names" rule. (b) :2645 cites "17-test-plan.md §6.4 for the canonical capture harness" — **dangling**: 17 §6.4 is "When WYSIWYG tests run" (scheduling, 17:1214-1226); no `MediaStreamAudioDestinationNode` harness exists anywhere in 17. (c) :2578 "cross-references spec 01's 'OPFS persistence' matrix row" — mis-pointed: 17's facet map gives OPFS persistence to **02** (17:2387) and 09 (17:2394), not 01 (17:2386: Undo/redo, Project save/load, Worker lifecycle) — P3 |

**Totals: P1 ×3 · P2 ×9 · P3 ×3** (the P3s: the 01-matrix mis-point; the §6 COOP/CEOP unlanded-target note; the rAF law absence — see §3 below).

---

## §2 The fix-list (exact edits, for the amendment wave)

### FIX-1 (P1) — insert §7.4 after §7.3 (before `## 8.`, i.e. after line 309): the audio-thread observability + realtime-safety law

New subsection, `### 7.4 Audio-thread observability & realtime-safety discipline (the WDC S-series law — LANDED at `ec8fd5c`)`, carrying five laws with their file:line anchors:

1. **The EngineDiagnostic bus** (`web-daw-core/src/lib/daw/diagnostics.ts:1-376`): structured, bounded error events from audio producers (worklet effects, WAM adapters) to subscribers; the 7 kinds (`bootstrap-failed`, `node-create-failed`, `processor-error`, `nan-output`, `silence-suspected`, `heartbeat-stalled`, `recovered`); the bounded ring (`DIAGNOSTICS_RING_MAX = 64`); CONSOLE-SILENT (producers keep their own warn lines); WeakMap context binding (`bindDiagnosticContext` — binding is not a reference leak); the node key STABLE ACROSS REBUILDS (track+slot identity, `diagnosticNodeKey`).
2. **The off-thread liveness monitor**: `registerHeartbeat`/`beat`/`unregisterHeartbeat` + `registerPoll`; `HEARTBEAT_STALL_MS = 3000` (≈4 missed beats at the 256-quantum cadence), `MONITOR_TICK_MS = 1000`; **armed by the first beat** (a never-beating node is "heartbeat not supported", never falsely stalled — the jsdom/mock-runtime guard); a stall fires ONLY while the registration's `isRunning()` reports true (a suspended context is not a stall); the graceful-recovery clause (`HEARTBEAT_GRACE_MS = 2 ticks` — a ≥3 s main-thread block coalesces queued beats; recovery then must NOT charge the bounded state-restore budget, `M3-REV-CODE`); self-starts on first registration, self-stops on last unregister.
3. **The in-`process()` protocol** (`public/worklets/dsp-effects-worklet.js:315-380`, the `DspDiagnosedProcessor` mixin all three DSP processors extend): heartbeat every 256 render quanta (≈0.68 s @ 48 kHz); NaN scan (first index + count, throttled `NAN_REPORT_GAP_QUANTA = 375`); silence scan (hot-in/quiet-out power thresholds; onset `SILENCE_ONSET_QUANTA = 750`, repeat 3750 — ADVISORY, never auto-bypass); the ONE preallocated message object mutated per post (zero JS-level allocation; structured clone is the platform cost); every deviation carries a `// rt-safe: <reason>` annotation.
4. **The stall bypass-to-passthrough ladder** (`effects.ts:441-507`, mirrored in the dry/wet variant `:1993-2105`): heartbeat stall > 3 s while running → **bypass to passthrough** (audio keeps flowing) + a `heartbeat-stalled` event with `bypassed: true`; NaN on output → **permanent** bypass (`nan-output`); silence → advisory only; recovery rewires the worklet path, graceful (queued-during-block) recovery exempt from the bounded state-restore budget; budget-exhausted → stays bypassed (reported). This is §10.4's degradation table's AudioWorklet-side landing.
5. **The rt-safety-gate** (`scripts/rt-safety-gate.ts`): the static analyzer over the worklet HOT PATH — every function transitively reachable from a processor's `process()` — flagging allocation (new/literals/spread/allocating builtins), locks & syscalls (`postMessage`, `console.*`, `Date.now`, `performance.now`, timers), and control hazards (throw/await/yield). **Seeds = `process()` methods of classes whose heritage reaches `AudioWorkletProcessor` DIRECTLY OR TRANSITIVELY through LOCAL base classes** (the Chunk-12 M3 fix: the `DspDiagnosedProcessor` mixin made the direct-extends match find 0 seeds — the whole diagnosed-DSP call graph walked NOTHING and the gate's PASS was VACUOUS; the cycle-safe `isWorkletSubclass` now walks the chain, `rt-safety-gate.ts:140-187`). BOTH the TS sources AND the built bundles are analyzed (the bundle is what the browser executes); the gate is LETHAL (exit 1), with a poisoned-fixture test proving violations are actually caught.

Plus a status sentence: LANDED WDC-side at `ec8fd5c` (inside the 777/777); the engine consumes it via the vendored tree (re-pinned `480a216`); the app's K3 browser-venue row (FIX-3) is where a consumer pin lands. Cross-refs to add: spec 20's file-map (the `diagnostics.ts` row — scout-wdc's P1), spec 17 §13A's "AudioWorklet message protocol" facet (17:546) — the protocol now includes the diagnostics messages; §12's test class 5 gains the protocol rows (heartbeat-stall bypass pin; NaN-bypass pin; gate-lethality pin).

### FIX-2 (P1) — rewrite 02:17's app row (the deliver + sidechain clauses)

Replace "…the W2.1 sidechain ducking over WDC's `collectSidechainWires`, and the W2.4 deliver export running the engine's main-thread offline mixdown — whose offline path SKIPS worklet-backed inserts entirely (`deliverService.ts`: "no worklet-backed insert effects in the offline path"), the K3 row's offline-side cost surface." with:

> "…the W2.1 sidechain ducking — now under the SINGLE-OWNER law (M2 Wave 1, `1aebbd5`: the app's `rewireSidechains` DELETED; the bridge's `SceneMixer.materializeStrips` collects + connects the duck wires through the shared `collectSidechainWires` contract on BOTH the realtime and offline paths — `audioService.ts:319-322` carries the replacement note), and the W2.4 deliver export running the engine's offline mixdown — which SINCE M2 Wave 1 (§1a, `deliverService.ts:252`) flushes worklet module loads before `startRendering` (`workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0`; the app-reachable worklet-backed insert family = compressor/gate/deesser/limiter, `:170-172`; a worklet-backed insert whose module load races `startRendering` renders PASSTHROUGH — the offline-duck inertness class this law exists to close)."

And re-pin the row head: "nle-test-app @ `c020b2a` — 252/252, tsc 0" (the census/register facts move with the fleet re-pin; see scout-app §1 for the 42-file census).

### FIX-3 (P1) — rewrite 02:22's K3 worklet-serving row

Replace the row's acceptance + premise with:

> "**Worklet asset serving** (the retired spec-14 §4.3 row): **RESOLVED at R27 in a different shape than specced** — the app serves the vendored worklets at their expected URL via the vite `serve-vendored-worklets` plugin (vite.config.ts:28-69: dev middleware + `closeBundle` dist copy of `vendor/web-daw-core/public/worklets`; ZERO byte duplication — the files live only in the WDC submodule; the app's own `public/` directory still does not exist, and does not need to). The ENGINE's venue is the `public/worklets` git SYMLINK to `../vendor/web-daw-core/public/worklets` (M2 Wave 1, `50b91f5`) — both serve the first `DSP_WORKLET_URL_CANDIDATES` root-base candidate (`/worklets/dsp-effects-worklet.js`, effects.ts:157-161). What remains owner nle-test-app, **K3**: the real-BROWSER venue pin — a Playwright/headless-Chrome test proving the worklet-backed inserts (compressor/limiter/stereo-widener) run the REAL DSP, not the unity-passthrough fallback (the app's suite is vitest/jsdom-only today; the flush-args pins at deliverService.test.ts:438-471 cover the args, not the venue)."

### FIX-4 (P2) — the mechanical re-pin batch (6 edits)

- :15 engine row: `5036387`/458 → `f9ac806`/748; "Vendors OT @ `c15a629`, WDC @ `494f6ff` (one docs-only commit behind WDC HEAD `85b81b0` — zero code delta)" → "Vendors OT @ `6e2b91a`, WDC @ `ec8fd5c`"; append the R27 movement note (the RC round + M2 Wave 1: bridge bus-law/sidechain parity; the public/worklets symlink — audio-adjacent movement exists now).
- :16 WDC row: `85b81b0`/759 → `ec8fd5c`/777; replace the R24 parenthetical with the R27 one: "the S-series upstream wave (19 refreshed + 18-file closure: diagnostics.ts, the rebuilt dsp-effects-worklet bundle, the rt-safety-gate fix, wam-instrument+state, the MIDI family, the catalog split); `dsp-effects-worklet.js` was REBUILT (the `DspDiagnosedProcessor` mixin) — §7.4."
- :17 app row: re-pin `c885ece`/174 → `c020b2a`/252 (fold with FIX-2).
- :26 acceptance row: "458/458 @ `5036387`, 759/759 @ `85b81b0`, 174/174 @ `c885ece`" → "748/748 @ `f9ac806`, 777/777 @ `ec8fd5c`, 252/252 @ `c020b2a`"; "(759)" → "(777)"; "(174)" → "(252)".
- :107 the zero-workers note: append "and again at `f9ac806` (R27, 2026-09-13 — still zero sites)".
- :302 the census note: append the R27 sentence: "**R27 re-verification:** the census HOLDS (no new processor families — OT/nle-ui zero `registerProcessor` sites; engine's `nle-pitch-shifter` + WDC's `daw-varispeed` + the 3 served bundles), but the DSP bundle was rebuilt with the diagnostics mixin and TWO new serving venues landed (engine `public/worklets` symlink; the app's vite plugin — §0 row). The r2 async pre-retime row is still open in both queues."
- :2338 §13B header: "458/458 vitest @ `5036387`, vendors OT @ `c15a629` + WDC @ `494f6ff`" → "748/748 vitest @ `f9ac806`, vendors OT @ `6e2b91a` + WDC @ `ec8fd5c`".
- §0 heading :12 + Status :4: "R24" → "R27" posture label; drop the "LOW-DELTA round" claim (see §1 row 1).

### FIX-5 (P2) — §13B line re-anchors (2 edits)

- `player.ts:1629` → `player.ts:1646` (the `renderVideoFrame` quote, `src/lib/nle/playback/player.ts:1646` at `f9ac806`).
- The §8.1-row note's `export/orchestrator.ts:232,822` → `:232,836`.

### FIX-6 (P2) — the Testing section's pointers (3 edits)

- :2621 `tests/fixtures/videos/10s-test-pattern-1080p.mp4` → either register it in 17 §5 first (the file's own rule) or re-point to the existing `10s-red-1080p.mp4` + a differing-frames note (17:862's table already keys red for blend tests; the filmstrip test wants frame-varying content — the honest fix is registering the fixture in 17 §5, cross-spec note below).
- :2623 `tests/fixtures/projects/single-clip-red.json` → `single-clip.json` (17:964) or register the red variant in 17 §5.
- :2645 "see `17-test-plan.md` §6.4 for the canonical capture harness" → the harness does not exist in 17; re-word to "the capture harness (`MediaStreamAudioDestinationNode`) is to be registered in spec 17 (§2.2 audio-WYSIWYG or §6) when the Tier 2 suite lands — 17 §6.4 currently carries only the scheduling law".

### FIX-7 (P3) — :2578

"cross-references spec 01's 'OPFS persistence' matrix row" → "cross-references the 'OPFS persistence' facet row (spec 17 §13A, 02's own row)". (01's facets are Undo/redo + Project save/load + Worker lifecycle per 17:2386; 01:331 already reverse-references 02 §8.6 correctly.)

---

## §3 What 02 uniquely sees (the findings only this spec's lens produces)

1. **02 is the natural home of the corpus's biggest missing law** (briefing fact #7): the audio-thread observability + realtime-safety mechanism has zero trace in all 21 specs — 20 owns the WDC file-map, but only 02 owns the AUDIO-THREAD discipline itself (what may run inside `process()`, what the host does when a processor dies). The §7.4 amendment (FIX-1) is this round's 02-specific deliverable; nothing in the W1/W2 waves covers it.
2. **The offline-flush + venue cluster is 02-owned**: scout-app filed the dead 02:17 premise to this spec; the flush-args law (workletFlushMs), the two serving venues (engine symlink + app plugin), and the DSP_WORKLET_URL_CANDIDATES root-base-first law together form a coherent "worklet venue-selection law" that currently lives nowhere — FIX-2/FIX-3 assemble it here.
3. **The zero-worker honesty note (§3:107) re-verified at R27** — the fleet's WEB-WORKER half remains at exactly ZERO `new Worker(` sites in the engine (by design, D5/procedural media), while the AUDIO-THREAD half keeps growing (W2/W1/H16/N2b + now the S-series diagnostics). The split is the spec's own thesis, and it held through three rounds.
4. **The rAF observation (the third thread-class)**: the fleet's actual thread inventory is workers (zero) + the audio thread (the worklets) + **one rAF loop per app** — the app's playhead ticker (`use-playback-ticker.ts:26-66`, rAF + `TICKS_PER_SECOND` from the vendored barrel, the port-census's 5th carrier). 02 has no rAF/main-thread-budget law (only FreeCut's `preview-work-budget.ts` reference in the §4.1 caveat). No violation observed (one ticker, one app); candidate P3 law if the fleet grows a second rAF consumer. Recording here so the cross-cut wave knows it was seen, not missed.
5. **§6's COOP/CEOP is the fleet's longest unlanded target**: no repo sets the headers (app vite.config has no `server.headers` block), no SAB exists anywhere — consistent because nothing needs it yet. The spec should not change, but the posture row could say "unlanded, no consumer" rather than implying it's set.
6. **17's back-pointer to 02 drifted**: 17:2387 cites "`02-workers-threading.md` `## Testing` (≈2493)" — the section is now :2539 (+46). A 17-owned fix (cross-spec note), but discovered from 02's side.

---

## §4 Cross-spec notes (for the cross-cut wave — not my edits to make)

1. **spec 20** (audio-core): the WDC file-map needs the S-series rows (scout-wdc's P1: `diagnostics.ts` + the 18-file closure) — 02's §7.4 and 20's file-map row must cite the same files/lines (diagnostics.ts:1-376, dsp-effects-worklet.js:315-380, effects.ts:441-507, rt-safety-gate.ts:140-187). **02 and 20 currently have ZERO cross-references to each other** — the two audio specs are unlinked; the amendment should add both directions (02 §7.4 → 20's file-map; 20's worklet rows → 02 §7).
2. **spec 17**: (a) the "AudioWorklet message protocol" facet row (17:546) should absorb the diagnostics-protocol sub-rows when FIX-1 lands; (b) 17:2387's "≈2493" pointer → :2539; (c) 17 §5 needs the `10s-test-pattern-1080p.mp4` fixture registered (or 02 re-pointed — FIX-6); (d) the MediaStreamAudioDestinationNode capture harness needs a home (17 §2.2 or §6) — currently exists only in 02's prose.
3. **spec 19** (code-references): the §13B re-pin (FIX-4/5) should be mirrored in 19's engine rows where the same line numbers appear; scout-engine's register carries the master list.
4. **spec 09/20 (volume-dB, corpus fact #4)**: 02 is NOT in the volume-domain cluster (no volume text) — no action here; noted so the cross-cut wave doesn't look for one.
5. **The K3 browser-venue pin (FIX-3)** is app-owned work that 02 registers — the acceptance-gate linkage (02 §0 GAP row ↔ 17 §13A facet ↔ IMPLEMENTATION-PLAN S-app) should stay coherent when the row is re-scoped.
6. **D12's ratification note (corpus fact #13)**: 02 has no D12 text; the engine's venue-model ruling touches 02 only via the export-render worker row (§8.5's offload stays r6 under the venue model) — no edit needed, coherence holds.

---

## §5 Verification ledger (the checks behind the table)

- WDC `ec8fd5c`: diagnostics.ts read in full (376 L; all constants verified); dsp-effects-worklet.js read (523 L; mixin at :322, HB_EVERY_QUANTA :315, NaN/silence scans :327-372, rt-safe annotation :378); rt-safety-gate.ts read in full (targets :57-69, blocked callees :72-86, the transitive-heritage fix :140-187 with its own bug comment :143-148); effects.ts bypass ladder (:148, :205-222, :332, :351-537, :1993-2105); offline-render.ts readiness gate (:736-768); DSP_WORKLET_URL_CANDIDATES (effects.ts:157-161).
- App `c020b2a`: deliverService.ts (:150-265 read; flush law :162-172/:177-181/:241-252); deliverService.test.ts (:429-471); vite.config.ts (:28-69 the plugin; :103 publicDir = nle-ui's; no `server.headers`); audioService.ts (:4, :188, :245, :257-322); `public/` absent (live `ls`); use-playback-ticker.ts (rAF :60/:66).
- Engine `f9ac806`: 0 `new Worker(` sites (grep); submodule status (OT `6e2b91a`, WDC `ec8fd5c`); the §0-row surfaces (adapter :87/:218/:388-470; realtime-engine :21/:52-55; player :1646/:3293-3363; metadata :83; registry :24; virtual-audio :4; worklet :13/:47; export orchestrator :10/:232/:836; settings :302/:332; mixer :619); HANDOFF/PLAN (G2 open); public/worklets symlink (live ls).
- OT `55c81c0`/`970948a`: `registerProcessor` grep → 0 sites (no worklets in OT).
- Spec corpus: 03 §5.2 (:300+, the 23-format recount matches 19 YUV + 4 RGB), 03 §14.D, 04 §14.C (:1246), 17 §5/§6.4/§13A (:535/:544/:546/:964/:1214/:2386-2394), 01 (:331 reverse-ref), the heartbeat/diagnostics/rt-safety corpus grep (only the unrelated cloudcut-nle row at 11:15).

**Bottom line:** spec 02's FreeCut-side reference body (§4/§5/§8/§11 — ~85% of the file) is stable and sound; the entire P1 load sits in the four R24-era posture rows (§0) + §7's missing law — exactly the surfaces the R25-R27 fleet movement (S-series + M2 Wave 1 + RC) landed on. Three fixes (FIX-1/2/3) are substantive; FIX-4/5 are mechanical; FIX-6/7 are pointer hygiene.
