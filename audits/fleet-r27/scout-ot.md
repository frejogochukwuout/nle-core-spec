# Scout-OT — opencut-timeline fdb771c..55c81c0 (R27 W1)

**Agent:** scout-ot · **Round:** R27 final-tightness, Wave 1 · **Date:** 2026-09-13
**Scope:** the OT repo's movement from the R26 HEAD `fdb771c` (code pin `c15a629`, 536/536) to HEAD `55c81c0` — the absorbed F4 wave, session-16 (HB hardening + AR-1/AR-2 + DECISIONS #26), session-17 (D-ARCH-6 the wire widenings + DECISIONS #27).
**Method:** every claim below was read from code at HEAD `55c81c0` (or the named commit), the git range itself, or the spec corpus at `cdc4e94`. No file outside this report + the worklog was touched.

---

## §1 The commit-range ground truth

### 1.1 The range

`git log --oneline fdb771c..55c81c0` = **52 commits** (author dates 09-10 03:24 → 09-13 11:29), composed of:

| Slice | Commits | Content |
|---|---|---|
| The absorbed F4 wave | `9ee3537` `10fa4c6` `d8b7084` `476cfc1` `3be2dea` (5) | retime-derive overlap, W8 playhead guard, negative trims, keyframe-flick fall-through, the wrap |
| Session-16 | `32e27a4`…`6e2b91a` (39) | the s16 audit fleet (`7e552d7`); HB-1/1b/1c/2/3/4 fix waves (M51-M53, M55-M57 + R twins); AR-1 (D-ARCH-2/3), AR-2 (D-ARCH-5); V-S16 intra-batch overlap (`2492a24`); DECISIONS #26 (`d3ffb65`), SEAMS honesty (`e40431a`), SKILL 61-66, PLAN/HANDOFF/worklog wrap |
| Session-17 (D-ARCH-6) | `75b72b7` `ab7727c` `ec86ad3` `970948a` `5f8b6d9` `fb1e272` `46c83ae` `55c81c0` (8) | design v1 → R-ARCH-17 review (16 findings folded) → v2 → **feat `ec86ad3`** → **test `970948a`** → DECISIONS #27 + SEAMS (`5f8b6d9`) → worklog/PLAN/SKILL wrap |

### 1.2 The wire census, READ FROM CODE (the ground truth)

`WIRE_COMMAND_TYPES` at `src/lib/timeline/headless/api.ts:243-275` — **31 verbs** (counted from the array; the tsc-lockstep asserts at `:277-297` hold it == the `TimelineCommand` union at `:56-212`, both directions; the w11 count pin `milestones-w11.ts:252-254` asserts `length === 31`):

1. `timeline.advancePlayhead` 2. `timeline.delete` 3. `timeline.duplicate` 4. `timeline.insert` 5. **`timeline.insertBatch`** ⭐new 6. `timeline.move` 7. `timeline.moveBookmark` 8. `timeline.pause` 9. `timeline.play` 10. `timeline.redo` 11. `timeline.removeBookmark` 12. **`timeline.removeKeyframes`** ⭐new 13. **`timeline.retimeKeyframes`** ⭐new 14. `timeline.rippleDelete` 15. `timeline.seek` 16. `timeline.selectElements` 17. `timeline.setLoopRegion` 18. `timeline.setPlaybackRate` 19. `timeline.split` 20. `timeline.toggleBookmark` 21. `timeline.trim` 22. `timeline.undo` 23. `timeline.updateElements` 24. `timeline.upsertKeyframe` 25. `track.add` 26. `track.remove` 27. `track.setAllLocked` 28. `track.setAllMuted` 29. `track.toggleLock` 30. `track.toggleMute` 31. `track.toggleVisibility`

`WIRE_UI_EXCEPTIONS` at `api.ts:328-335` — **3 exceptions** (a tsc-asserted `Record<verb → law>` exported since AR-1 D-ARCH-3, `40f22af`):
- `timeline.selectElements` (selection is VIEW state; remote-consumer verb)
- `timeline.advancePlayhead` (the ticker is rAF-local)
- `timeline.trim` (uniform-delta engine shape; the UI's resize commits through `updateElements` patches)

**Census: 31 verbs = 28 routed + 3 exceptions.** Verified three ways: (a) the AR1-5 registry pin (`milestones-ar1.ts:180-191` — exactly the three verbs, sorted); (b) the M49C gate artifact — `download/timeline-test-report.json` (refreshed in `970948a`) records `"fired 28 distinct command types through the REAL UI (origin:\"ui\" dispatches only …) across the phases; 3 documented exceptions; no gaps"`, PASS; (c) the M55R AR2R-6 arithmetic pin (`scripts/m55-ar2-real-mouse.mjs:525-567` — 31 = 28+3, page-local). The gate reads BOTH registries LIVE from the page (`scripts/run-timeline-tests.mjs:390-399` → `__VIEW_TEST__.wireUiExceptions` / `wireCommandTypes`, exposed at `src/app/view/page.tsx:486/:494`).

### 1.3 The census lineage and the delta vs the spec's reading

The spec corpus's operative reading (15 §0/§13.15, 06, 05, 09, 12, 17, 18, 19 — all R24/R25-pinned) is **"30 names = 24 routed + 6 exceptions @ code tip `c15a629`"** (spec 15:14, 06:32, 05:14, 09:14, 18:514, 19:48/309…). The real lineage:

| Pin | Census | Evidence |
|---|---|---|
| R15 charter (`0412e41`) | 24 (no split) | spec 15:4905 lineage row |
| R22 (`05584d8`) | 28 actual | spec 15:4905 |
| R23 (`222532c`) | 30 | spec 15:4905 |
| R24 code tip (`c15a629`) | 30 = **24 routed + 6 exceptions** | the spec's current reading (the 6: selectElements, **upsertKeyframe, removeKeyframe, retimeKeyframe**, advancePlayhead, trim) |
| AR-2, session-16 (`30bfe2a`) | 30 = **25 routed + 5 exceptions** (upsertKeyframe ROUTED by the dblclick authoring; left the registry same-commit) | `api.ts:315-326` (the applied-law comment); AR2R-6 |
| **D-ARCH-6, session-17 (`ec86ad3`/`970948a`)** | **31 = 28 routed + 3 exceptions** | this report §1.2 |

Net delta vs the spec's reading: **+3 new verbs** (`insertBatch`, `removeKeyframes`, `retimeKeyframes` — all routed origin "ui"), **−2 retired verbs** (`removeKeyframe`, `retimeKeyframe` — REMOVED from the `TimelineCommand` union at `api.ts:56-212`; a stale singular dispatch now falls to the never-guard's typed `INVALID_PARAMS` "unhandled command" at `api.ts:2537-2545`, and the AR1-5 pins assert their absence from both registries at `milestones-ar1.ts:202-208`), **1 re-classified** (`upsertKeyframe`: exception → routed).

### 1.4 The batch verbs' semantics (code-verified)

- **`timeline.insertBatch`** (type `api.ts:66-88`; arm `:1262-1370`): ONE history entry (wraps ONE `core.insertElements`, `ops/timeline-core.ts:887-994+, withUndo("Insert elements", …)`); first element at `startTimeTicks`, later members keep RELATIVE offsets (callers wanting contiguity stagger the construction); per-member validation through the **shared `validateInsertElement` helper** (`api.ts:634-809`, ~175 lines — the singular insert's inline block extracted, one writer for the element-field law incl. SEAL SE-1 type discriminant, W9 E9 graphic/effect fields, SC-8.1 startTime, duration/trim defaults + the HB2-3 cross-field laws); **F1A-3 mixed-kind batch → INVALID_PARAMS naming the kind-split duty** (`api.ts:1326-1331`); **intra-batch self-overlap or placement failure → CONFLICT, whole-batch atomic** (`api.ts:1365-1369`; the core's layer-(a) guard `timeline-core.ts:933-944`); **empty batch → INVALID_PARAMS** — the deliberate divergence from delete's NOT_FOUND-for-empty, pinned as M58-1 (`api.ts:1269-1273`); explicit-onto-locked → `TRACK_LOCKED` (`:1290-1296`); success echoes the minted refs `data.elements` (`:1354-1364`). Placement is REJECTED-NOT-SHIFTED (the group lands as-requested or one new track is minted — the core placement law).
- **`timeline.removeKeyframes`** (type `api.ts:156-176`; arm `:2188-2285`): cross-element by design (marquee spans elements); ONE history entry — classic's BatchCommand-of-RemoveKeyframeCommands law (W9 E5); **the WYSIWYG bake rides the core op through the wire** (when a channel empties, the value-at-playhead is baked into the base param — `timeline-core.ts:1715-1724` + the pre-sample `:1746-1789`, PAIR-scoped per RA-3); wire-side **dedup** by the 4-tuple (`:2229-2235`); **batch-atomic TRACK_LOCKED** lockPreCheck (`:2239-2246`); **F16 pre-resolution against committed state** so a stale ref is a NAMED `NOT_FOUND` (`:2252-2263`); post-state echo = one `{elementId, keyframes}` per distinct element (`:2271-2278`). The singular per-key form is RETIRED — classic routes every removal through the batch manager ("a single removal is a batch of one", timeline-manager.ts:523-590; zero fleet callers per the R-ARCH-17 census).
- **`timeline.retimeKeyframes`** (type `api.ts:177-197`; arm `:2286-2390`): **delta-shaped** — the keyframe-drag gesture's native commit (shift every selected key by `deltaTicks`, each clamped to its element's `[0, duration]` — `timeline-core.ts:2037-2044`, the clamp observable through the wire); cross-element; ONE history entry (`withUndo("Retime keyframes")`, `timeline-core.ts:1987`); the **F4 benign zero-delta** — `delta 0 → {ok:true, data:{changed:false}}`, NEVER the NOOP failure code (`api.ts:2358-2366`); non-integer delta → INVALID_PARAMS (`:2320-2327`); dedup + batch-atomic lock + F16 pre-resolution as removeKeyframes. The OT code itself names the union divergence: "The plural form is an OT EXTENSION pending the r1 §4.1A union decision — the D29-F8 endorsement's SPIRIT … not its letter (the endorsed rows are §4.3.64-65 + §4.3.9)" (`api.ts:183-186`), and "the spec's per-element {elementId, keyframeIds[]} shape is the r1 param-alignment decision, not ours to pre-empt" (`api.ts:163-166`).
- **The three gesture-commit seam switches** (origin "ui"):
  - keyframe-drag → `retimeKeyframes` — `use-keyframe-drag.ts:105-123` (dispatch threaded per the AR-2 precedent; the direct core call stays as the wireless-consumer fallback; the controller's own session-start lock gate STAYS — gesture UX, R-ARCH-17 F13).
  - deleteSelected keyframe branch → `removeKeyframes` — `use-timeline-actions.ts:401-431` (the S2 locked-subset pre-filter stays as ratified UX policy, DECISIONS #25 r6; the wire's TRACK_LOCKED is defense-in-depth).
  - library multi-insert → `insertBatch` per KIND — `src/app/view/page.tsx:535-599`: **staggered construction** (F1 — `startTime[i] = playhead + Σ durations[<i]`, all-at-playhead would self-overlap-reject) + **kind-split at the library layer** (F2 — `byKind` map, one homogeneous batch per kind, N-kinds entries); the M49R N-entries divergence pin was INVERTED to the one-entry law (`scripts/m49-residuals.mjs:162-260` — homogeneous pair: ONE insertBatch, ONE entry, ONE undo-both; mixed: per-kind dispatches).
- **Harness migration** (the live demo of the retirement): `milestones-animations.ts`/`milestones-review6b.ts` singular dispatches rewritten to batch forms — "absolute composed as delta" (`ec86ad3` diff: `retimeKeyframe {timeTicks:S(2)}` → `retimeKeyframes {keyframes:[…], deltaTicks:S(1)}`).

### 1.5 Tests + the code-pin proposal

- **632/632** at HEAD — `download/timeline-test-report.json` (committed in `970948a`): total 632 / passed 632 / failed 0, **72 report entries**; split per the artifact: **452 in-page + 30 REAL-DragEvent/keyboard integration + 150 REAL-mouse** (the R24 convention "386+150" lumped the DragEvent/keyboard families into the in-page figure — the re-pin should cite 632 total with the report json as authority, as before).
- **The code pin: `970948a`** (proposal). Verified: `git diff 970948a..55c81c0 -- src/` is EMPTY and `-- scripts/` is EMPTY; the four later commits touch only `.agents/{DECISIONS,SEAMS,PLAN,HANDOFF,SKILL}.md` + `worklog.md` (`5f8b6d9` docs, `fb1e272` worklog, `46c83ae` PLAN, `55c81c0` SKILL/HANDOFF). This refines the R27 skeleton §2's guess ("the code pin moves to `55c81c0`"): the R24 pin convention (HEAD named separately when docs-only past the code tip — `fdb771c`/`c15a629` was exactly this shape) gives **HEAD = `55c81c0`, code pin = `970948a`**.
- In-repo polish note (P3, OT-side): the `WIRE_COMMAND_TYPES` doc header still says "the 30 wire command types" (`api.ts:235`) while the array lists 31 — cosmetic only (the census is machine-checked three ways); same class: `SEAMS.md:184-185` retains the session-14 section's "24/24 routed, 6 exceptions" historical line (superseded in-file by the s16/s17 sections at `:224-233`).

---

## §2 The new law state (as stated OT-side)

### 2.1 DECISIONS #26 (`d3ffb65`, session 16) — six rulings (`.agents/DECISIONS.md:1127-1198`)

1. **D-HB2 — fromJSON is the scene-load boundary**: wire-equivalent discipline at the SECOND JSON boundary; structural invariants (duplicate ids, non-positive/non-integer durations, negative starts, per-track overlaps, out-of-bounds keyframes, hostile retime/transitionOut shapes) **rejected WHOLE** with typed `[Timeline] fromJSON:` errors; normalization preserved; the round-trip law (toJSON output always passes) constrains the validator; the raw constructor is the documented escape hatch. **Code:** `TimelineCore.fromJSON` `ops/timeline-core.ts:2443-2537` — `validateSceneForLoad(scene)` fires FIRST (`:2468-2471`), `sceneLoadError` throws the typed form (`:2545-2547`), the validator + its per-element keyframe-id scope notes at `:2582-2599+`; the normalization set (id-counter seeding incl. keyframe ids `:2472-2500`, strict-boolean lock `:2511-2516`, trim defaults, animations canonicalization) preserved.
2. **The registry reconciliation (supersedes #25 r2's upsert law)**: `upsertKeyframe` NOW UI-routed (AR-2); WIRE_UI_EXCEPTIONS 6→5 same-commit as the coverage landed; the registry is a tsc-asserted exported Record consumed LIVE by the gate; M49C 25/5.
3. **D-ARCH-1 — the assembly ruling**: OT = THE editing engine of the assembled NLE (SceneTracks SSOT + ops + the spec-15 wire); nle-engine's stack = the render/export venue; one-directional junction. (Ratified engine-side at their R27 pin — the engine scout's scope.)
4. **The shared numeric laws have ONE home**: the volume-dB law lives in `core/audio-params.ts` (**verified: `src/lib/timeline/core/audio-params.ts:28-48`** — `VOLUME_DB_MIN=-60`, `VOLUME_DB_MAX=20`, `DEFAULT_VOLUME_DB=0`, `clampVolumeDb` with the non-finite→DEFAULT fold, `volumeDbToLinear`; zero-import leaf; audio-state.ts a re-export shim); retime constants barrel-exported. Cross-repo adoption verified: the engine's `src/lib/nle/bridge/opencut-laws.ts` (at engine HEAD `f9ac806`) deep-imports exactly these leaves (OV-01/OV-02).
5. **D-ARCH-5 — the keyframe-authoring gesture is OURS (greenfield)**: double-click on the volume line AND the expanded lanes authors a keyframe at the frame-snapped click time; the value law (on-curve interpolation, seed = params.volume/DEFAULT, CLAMPED via clampVolumeDb, y-independence pinned); exact-tick = replace (an edit, not a mint). **Code:** `use-keyframe-authoring.ts:1-80` (the full law header) + the two venues (`AudioVolumeLine.tsx:228-232` `venue:"volume-line"`; the expanded-lane twin threaded via `TimelineElementView.tsx:139-150`), dispatched `timeline.upsertKeyframe` origin "ui" — the FIRST wire command routed from BELOW TimelineView.
6. **V-S16 NEW-1 — the intra-batch overlap law**: a batch's OWN spans must not overlap each other — the insert path's layer-(a) law lifted to the shared `timeSpansSelfOverlap` and wired into the move/updateElements/trim validators (a committed intra-batch overlap previously poisoned the scene AND bricked the save file at the HB-2 load boundary — the save-brick class; M56-pinned).

### 2.2 DECISIONS #27 (`5f8b6d9`, session 17) — four rulings (`.agents/DECISIONS.md:1202-1275`)

1. **The three batch verbs** (one history entry each) — the laws as §1.4 above, incl. the shared `validateInsertElement` extraction ("one writer for the element-field law").
2. **The singular verbs RETIRE (the registry 5→3)** — classic's batch-manager law + the zero-caller census held across the fleet (R-ARCH-17); the absolute-retime capability composes as `deltaTicks = target − key.time` (the migration note, demonstrated live in the harness rewrites); the census re-declared MECHANICALLY per D29-F8 (tsc-lockstep + M49C; no spec amendment owed for the census itself). `retimeKeyframes` is an OT EXTENSION pending the r1 §4.1A union decision.
3. **The gesture-commit seams close (W11-f's keyframe half)** — the three switches of §1.4; M49R's N-entries pin INVERTED to the one-entry law (the M28R in-wave-rewrite precedent).
4. **The consumer's queue duty** — the app's port carries the same direct-core gesture seams; their re-pin MUST include their own switch or their ported coverage gate fails against the 31/3 registry; their pool multi-insert rides insertBatch + the kind-split. **Filed:** the app's `c020b2a` ("docs(queue): the opencut-timeline s17 filings …"). Verified: the app's `vendor/nle-timeline/UPSTREAM.lock.json` mirrors **`6e2b91a`** (pre-D-ARCH-6 code) — the re-pin is genuinely pending; the app's port still carries direct-core keyframe seams (`src/timeline-port/hooks/use-keyframe-drag.ts` etc.).

### 2.3 SEAMS truth-up (`5f8b6d9`, `.agents/SEAMS.md:224-241`) + SKILL laws 67-71 (`55c81c0`)

The SEAMS wire-dispatch-origin section now states the 3-verb registry + the 31 = 28+3 census + the batch-verb seam rows + the consumer re-pin duty. SKILL 67-71: the **coverage-carrier law** (per-test freshPage resets `wireUiCoverage`; every real-mouse suite must END with a carrier test whose page fires all the suite's verbs — the M58R carrier is one page, three gestures, three verbs); replace-before-insert splice order; the ESM export shape law; the **retire-don't-route ruling** (compiler-enforced reconciliation + the zero-caller census duty — generalizable); the **cross-repo landing-vs-filing line** (land direct fixes whose gates you can run; file titled queue blocks for work whose acceptance lives in the other repo's machinery).

---

## §3 The spec-impact register

Severity: **P1** = spec states something FALSE now / a landed law missing from spec entirely · **P2** = stale citation/count/pointer · **P3** = polish. Spec corpus at `cdc4e94` (00 v11.0/R26-era re-pins; all §0s re-pinned R24/R25 to `fdb771c`/`c15a629`/536).

| # | spec §site | current text (quote) | new truth (file:line evidence) | Sev | proposed amendment |
|---|---|---|---|---|---|
| 1 | **15 §0** status (:4) | "the wire is the W11 complete-UI shape @ OT `ded43c4` (code tip **`c15a629`** — the **24 routed + 6 exceptions** census, machine-checked by the M49C coverage gate)" | 31 verbs = **28 routed + 3 exceptions** @ code pin `970948a` (api.ts:243-275/:328-335; M49C artifact) | P2 | Re-pin (HEAD `55c81c0`, code tip `970948a`) + re-census; add the census lineage tail "→ 25/5 at AR-2 → **31 = 28+3 at D-ARCH-6 `970948a`**" |
| 2 | **15 §0** OT row (:14) | "NOTE (battery-relevant): the exception registry is TEST-LOCAL upstream — OT exports NO exception constant; spec-side the 6-name list is a documented OT fact cited at the runner's location, not an engine symbol" | **FALSE since `40f22af` (AR-1 D-ARCH-3, session 16)**: `WIRE_UI_EXCEPTIONS` is a first-class tsc-asserted `Record<verb→law>` export (api.ts:328-358) + the derived `WIRE_UI_EXCEPTION_VERBS` Set, barrel-re-exported, page-exposed (`view/page.tsx:494`), read LIVE by the M49C gate (run-timeline-tests.mjs:390-399); the runner's test-local copy was DELETED | **P1** | Rewrite the NOTE: the registry is an exported engine symbol, tsc-lockstep-asserted (TS2344 on drift), gate-consumed live; the spec's 6-name list → the 3-name registry |
| 3 | 15 §0 (:14) | "the census RE-BASED to the W11 truth (D29.2): 24 UI-routed verbs + 6 documented exceptions (the 30-name census split at `c15a629`)" + the R25 re-read (:4) "536/536 + the M49C gate 24/24 + 6 exceptions re-run live" | 632/632; M49C 28 routed + 3 exceptions (report json @ `970948a`) | P2 | Counts + lineage + pin re-base |
| 4 | **15 §4.1A** routing-disposition preamble (:296) | "R24 live reading (@ OT code tip `c15a629`): the prefixed census is 30, split **24 UI-routed + 6 documented exceptions** … 27 of the 78 union members have live OT wire counterparts" | census 31 = 28+3; the counterpart set moved: `upsertKeyframes`←`timeline.upsertKeyframe` (AR-2), `removeKeyframes`←`timeline.removeKeyframes`, and `insertBatch` (the §4.3.9-batch/superset slot) now have live OT counterparts → the natural reading is **29 of 78**, with `retimeKeyframes` an OT EXTENSION past the union's singular `retimeKeyframe` (api.ts:177-197 names this itself) | P2 | Re-read at the new pin; rule the counterpart arithmetic (open Q2 below) |
| 5 | **15 §13.15** discriminator row (:4905) | "**30 types** … the W11 census split: `WIRE_COMMAND_TYPES` (api.ts:182-213 …) — 24 UI-routed verbs + 6 exceptions (the runner's registry, `scripts/run-timeline-tests.mjs:283-300` — test-local upstream, no exported constant; M49C asserts 24/24 …)" | 31 types; the union at api.ts:56-212; WIRE_COMMAND_TYPES at :243-275; the registry at :328-335 (exported, NOT runner-local); M49C 28/3 via the live read (runner :390-399) | P2 | Re-write the row at the new pin; fix the line citations + the registry-location claim (overlaps finding #2) |
| 6 | **15 §13.15** §4.3.64-65 row (:4915) | "NEW ROW (R24): the batch-keyframe wire verbs — … **the wire exposes only the singular verbs (`timeline.upsertKeyframe`/`removeKeyframe`/`retimeKeyframe`)** … \| QUEUED (r1-adjacent)" | **LANDED @ `970948a`**: `timeline.removeKeyframes` exists (api.ts:156-176, :2188-2285) and `timeline.upsertKeyframe` is UI-routed (AR-2); the singular `removeKeyframe`/`retimeKeyframe` are RETIRED from the union (never-guard :2537-2545; absence pinned at milestones-ar1.ts:202-208). The census re-declared mechanically as D29-F8 promised — "no spec amendment" applies to the CENSUS, but this row's OT-state cell is now false | **P1** | Flip the row: LANDED (BASE @ `970948a`); record the shape deltas for the r1 param-alignment (cross-element flat member lists vs the union's per-element `{elementId, keyframeIds[]}`; `retimeKeyframes` delta-shaped plural vs the union's singular absolute §4.3.66) |
| 7 | **15 §13.15** §4.3.9-batch row (:4916) | "the library multi-insert currently runs `applyBatch([insert × N])` — N history entries (one undo per entry, a CONSCIOUS divergence …); the batch verb restores the one-entry law \| QUEUED (r1-adjacent)" | **LANDED**: `timeline.insertBatch` (api.ts:66-88, :1262-1370) + the library multi-insert switched to ONE insertBatch per kind-group, staggered construction (view/page.tsx:535-599); M49R inverted to the one-entry law (m49-residuals.mjs:162-260) | **P1** | Flip to LANDED; keep the r1 decision "bare `insertBatch` vs the `insert {elements[]}` superset" OPEN (it is the param-alignment wave's charge) |
| 8 | 15 §13.15 command-coverage row (:4914) | "27 of 78 … 3 OT-side verbs beyond the union (`advancePlayhead`/`setAllLocked`/`setAllMuted`); 51 absent" | the 3 beyond-union verbs are unchanged (the batch verbs map to existing union slots/§4.3.9); the counterpart count needs the +2/+3 re-derivation (see #4) | P2 | Re-derive with the ruled convention |
| 9 | 15 §13.15 §6.3 row (:4906) | "TRACK_LOCKED … extended by W11 to the singular keyframe verbs (R-A P3-9: `removeKeyframe`/`retimeKeyframe` report TRACK_LOCKED …; api.ts:1462/:1517/:1549; 12 commands total)" | the singulars retired; the law is now **batch-atomic TRACK_LOCKED** on `removeKeyframes`/`retimeKeyframes` (api.ts:2239-2246/:2337-2344; the w11 P3-9 pin rewritten batch-atomic, milestones-w11.ts:257+) | P2 | Update the row's OT-side citation to the batch-atomic law |
| 10 | 15 §7.1A (intra-batch overlap, Round 8) | the §7.1A law covers applyBatch's own inserts ("the batch's own inserts are validated against each OTHER") | **V-S16 NEW-1** extended the law to the move/updateElements/trim VALIDATORS (a single command's own N-span param list; shared `timeSpansSelfOverlap`; `2492a24`, M56-pinned) — the save-brick class (a committed intra-batch overlap made the scene UNLOADABLE at the HB-2 boundary) | P2 | Extend §7.1A's statement one sentence: the guard is per-command (any wire param carrying N spans), not only applyBatch |
| 11 | **06 §0** status (:4) + the ten-mode preamble (:12, :29) + the OT row (:32) + the C7 lineage (:42) + §10.4 note (:2596) + §10.5 W11 row (:2616) | "**24 routed command verbs + 6 documented exceptions**, M49C machine-checked" (×6 sites, incl. :2616 "the 30-name census split 24 UI-routed + 6 documented exceptions") | 31 = 28+3 @ `970948a`; the registry exported | P2 | Re-base all six sites (one edit family) |
| 12 | **06 §5.9D AP-1 GAP row** (:1638) | "OT's pool batch is playhead-anchored `applyBatch([insert × N])` stacking with N undo entries (`view/page.tsx:515-555`)" | **FALSE**: the pool multi-insert now fires ONE `timeline.insertBatch` per kind-group with staggered construction (view/page.tsx:535-599 @ `970948a`); the N-entries divergence narrowed to N-kinds; the M49R pin inverted | **P1** | Rewrite the row's OT-fact cell (insertBatch LANDED; kind-split + staggered construction = the landed law); the r1 append-strategy half of the row stands unchanged |
| 13 | **09 §0** OT row (:14) | "536/536 … `WIRE_COMMAND_TYPES`, 24 routed verbs + 6 documented exceptions … `static fromJSON(scene, fps)` **with the load-time normalization set** (strict-boolean locked …; missing trims → 0 …; the id counter seeded …)" | 632/632, 28+3; and fromJSON is now the **VALIDATING scene-load boundary** (D-HB2): the normalization-set sentence is half the law — structural invariants are rejected WHOLE with typed `[Timeline] fromJSON:` errors (timeline-core.ts:2443-2537, :2582+; M53 + M49R pins) | P2 (counts) / see #14 for the law | Re-pin + point the fromJSON sentence at the load-boundary row (#14) |
| 14 | **09 §3.3 / new §3.3A** (the model law) | the schema-validation law covers the PROJECT layer (Zod at the ProjectManager, §3.3 :358+); **no row states the per-scene load boundary's rejection contract** (grep: zero "hostile" mentions in 09/17/15/12) | **D-HB2 landed + pinned** (M53 hostile-class table, never-partial-load, round-trip acceptance, normalization survival; M49R Load-surface pins) — a landed law missing from the spec corpus | **P1** (landed law missing) | Add a §3.3A row (or a §3.3 paragraph): fromJSON is the scene-load boundary — the rejected-whole class list (duplicate ids, non-positive/non-integer durations, negative starts, per-track overlaps, out-of-bounds keyframes, hostile retime/transitionOut), the typed error form, the round-trip law (toJSON always passes), the raw-constructor escape hatch; mirror one line in spec 17's round-trip matrix row |
| 15 | **05 §0** OT row (:14) + §16.5 headless row (:1260) | "exceptions: `timeline.selectElements` … `timeline.{upsertKeyframe, removeKeyframe, retimeKeyframe}` (batch-gesture one-history-entry law), `timeline.advancePlayhead` …, `timeline.trim`" | `upsertKeyframe` ROUTED (AR-2); `removeKeyframe`/`retimeKeyframe` RETIRED; +3 batch verbs — the enumerated exception list is wrong in both directions; :1260 "30-type prefixed union — 24 UI-routed + 6 documented exceptions" | **P1** (the list names verbs that no longer exist) | Rewrite the exception enumeration (3) + the census; re-pin |
| 16 | **05 §8 / 16 §3.12 / 18 §4.x** (gesture-carrier question) | 05 §8 has NO keyframe-interaction section (8.1-8.9 + 8A cover tool modes/click/drag/trim/razor/playhead/marquee/library-DnD/keys); 16 §3.12's keyframe rows are panel-scoped (`K`/`Shift+K`); 18 §4.2's pool card carries only the single `insertElement` row (:234); no spec site describes the keyframe-authoring affordance or the drag/delete wire commits | **Landed OT-side**: the AR-2 dblclick authoring (both venues, on-curve value law, clamp, exact-tick replace — use-keyframe-authoring.ts:1-80); the drag→retimeKeyframes / delete→removeKeyframes / multi-insert→insertBatch seam switches (§1.4); the multi-select "Insert N at playhead" button (view/page.tsx:309-321) | P2 (missing surface law — the specs' union-vocabulary rows are not false, the gestures are simply uncarried) | Minimal: one 05 §8 row (the keyframe gesture family: author-dblclick / drag-retime / delete, one history entry each, the S2 inert family) + a 16 §3.12 note (the timeline-surface twins of the panel rows) + an 18 §4.2 pool-card multi-insert row; alternatively record them as OT-facts in 05 §16.5A's op-port table |
| 17 | **16 §3.12** keyframe-nudge rows (:439-440) | "`Left`/`Right` (in keyframe panel) … `{ type: 'retimeKeyframe', params: { elementId, keyframeId, time: <currentTime ± 1frame> } }`" | the union's singular absolute-time form has NO OT counterpart anymore — OT landed the plural DELTA-shaped `retimeKeyframes` and retired the singular (api.ts:177-197: "an OT EXTENSION pending the r1 §4.1A union decision"); OT's migration idiom: delta = target − key.time | P2 (union-side unchanged; the divergence is the new r1 input) | Annotate §3.12 (or the §13.15 row when flipped): the OT wire now exposes only the plural delta form; the r1 alignment must rule singular-absolute vs plural-delta + the param grouping |
| 18 | 16 §0 OT rows (:19 etc.) | "OT @ HEAD `ded43c4` (code tip `c15a629`) — 536/536 … the canonical keymap now lives in the tree" | 632/632 @ HEAD `55c81c0` / code tip `970948a`; (the keymap itself did not move in this range — the W11-f keyframe seams did) | P2 | Re-pin |
| 19 | **18 §0** OT row (:514) | "hosts the W11 wire-dispatch layer — **24 routed + 6 exception verbs**, `WIRE_COMMAND_TYPES` + the M49C coverage gate … 386 in-page + 150 real-mouse suites — **536/536** @ `ded43c4` [code pin `c15a629`]" | 28 routed + 3 exceptions; 632/632 @ `55c81c0`/`970948a` | P2 | Re-pin + re-census |
| 20 | **00 §0** fleet row (:17) | "536/536 … @ `fdb771c` (**code pin `c15a629` UNCHANGED — src-diff `c15a629..HEAD` EMPTY …**; 536/536 + the M49C gate 24/24 + 6 exceptions re-run LIVE this round; R25 — … consumers pin `c15a629`)" | HEAD `55c81c0`, code pin `970948a` (src-diff `970948a..55c81c0` EMPTY, verified); 632/632; M49C 28/3; D-ARCH-6 + session-16 landings to fold into the row's highlight list | P2 (the pin canon row — drives battery_r26's 5 pin-lag fails) | Re-pin; note the consumer lock-copy moved past `c15a629` (see #24) |
| 21 | **12 §0** status (:4) | "the wire census **24 routed + 6 exceptions is the census both key on**" (the K2/K3 instrument registration: OT's M49C + the app's W-F port) | the instruments re-key on **28 routed + 3 exceptions**, now via the exported-registry live consumption (the app's AR-2 port already consumes `WIRE_UI_EXCEPTION_VERBS` from the vendored barrel — their `c020b2a` filing) | P2 | Re-key the instrument registration + add the live-registry consumption law (one line) |
| 22 | 17 (:15 counts, :29/:40 "drives the same **24 routed wire verbs** through the real UI", :2359 re-tier row "OT 536") | " | 28 routed; 632 | P2 | Re-pin + re-census (three sites) |
| 23 | **19-code-references** (:5, :13, :26, :48, :72-73, :137, :309, :352, :365, :413 — 22 `c15a629` hits) | "536/536 … R24 @ `c15a629`" / "all **30** headless types at `c15a629` (24 at W5; 18 at the R8 sweep) … are `timeline.*`/`track.*`-prefixed" (C7, :309) / "**30 at `c15a629`** (24 routed + 6 exceptions …)" (:73) | 31 types @ `970948a`; 632/632; the registry exported; the C7 rename worklist sizes change | P2 | Re-pin the reference map + C7 row (the C7 disposition rows themselves — advancePlayhead/setAllLocked/setAllMuted — are UNCHANGED by this range) |
| 24 | 09 §0 / 15 §0 / 18 §0 consumer rows + IMPLEMENTATION-PLAN (:32) | "the app's vendored OT lock-copy @ `c15a629` byte-exact (live-verified R24/R25)" | the app mirrors **`6e2b91a`** (UPSTREAM.lock.json, mirrored 2026-09-13 — session-16 code, PRE-D-ARCH-6); the re-pin queue is filed (`c020b2a`) | P2 (consumer-pin class; the app scout owns the detail) | The R27 re-pin wave updates the consumer-pin rows to the queue's target (OT `55c81c0` mirror) |
| 25 | 13-subagent-scout-plan (:14) + IMPLEMENTATION-PLAN (:42 pin world) | the R24/R25 pin preambles ("OT `c15a629` 536") | 632 @ `970948a`/`55c81c0` | P3 | Fold at the re-pin |
| 26 | 15 §0 (:18 spec-internal registers pointer) + §13.15 (:4899 header) | "§13.15 … R24-refreshed to the 24 routed + 6 exceptions" | 28+3 | P2 | Same edit family as #1/#5 |
| 27 | OT-side citation nuance | OT's code cites "the spec 15 §13.15 kind-split rule" (api.ts:76, :1323, view/page.tsx:536) | the spec's normative kind-split sentence lives at **06 §5.9D's D31A composite row** (06:1632: "F1A-3's heterogeneous-batch rejection splits a mixed pool selection into per-kind batches"); 15 §13.15 carries no such rule text today | P3 | When flipping the §13.15 batch rows to LANDED, either cite 06 §5.9D or add the one-line kind-split law to 15 §13.15 (making OT's citation true) |

**Not impacted (verified):** the C7 disposition rows for `advancePlayhead`/`setAllLocked`/`setAllMuted` (15 §13.15 :4905 — unchanged); 06 §4.3's classic BatchCommand documentation (:182-211 — classic-side, correct); 06 §5.9's two-insert-semantics preamble (the placement-insert law is exactly what insertBatch implements — strengthen, don't change); 15 §7 applyBatch laws; 09's single-scene ruling (:26 — "toJSON()/fromJSON() remain the whole serialization surface" still true); 18 §4.2's pool-dblclick reveal (a different surface than the AR-2 timeline dblclick — no "one gesture, one meaning" conflict, worth a cross-note only); spec 16's element-level editing rows.

---

## §4 Open questions for the orchestrator

1. **The code pin.** I propose **code pin `970948a`, HEAD `55c81c0`** (src+scripts diff empty over the last 4 docs commits — the exact R24 `fdb771c`/`c15a629` shape). The R27 skeleton §2 guessed "the code pin moves to `55c81c0`"; under the established convention the code tip is `970948a`. Ruling needed for the 00-master pin row + battery_r27.
2. **The union-counterpart arithmetic** (15 §4.1A :296 + §13.15 :4914): "27 of 78" at `c15a629` → at `970948a` the natural mapping adds `upsertKeyframes` (via the routed `upsertKeyframe`) + `removeKeyframes` = **29 of 78**, with `insertBatch` either counting toward §4.3.9's slot or as the batch-form of an already-counted member (the row itself says "or the `insert {elements[]}` superset — the r1 param-alignment decision"), and `retimeKeyframes` an extension pending the r1 union decision. This is a counting-convention ruling (W4's keyboard+wire-census cross-cut owns the coherence check).
3. **The r1 param-alignment decisions the landing forces** — recommend pre-filing them in 15 §13.15 when flipping rows #6/#7 to LANDED: (a) retime: the union's singular absolute `retimeKeyframe` (§4.3.66, also 16 §3.12's nudge rows) vs OT's plural delta-shaped `retimeKeyframes`; (b) keyframe param grouping: the union's per-element `{elementId, keyframeIds[]}`/`{elementId, keyframes[]}` vs OT's cross-element flat member lists; (c) insertBatch's bare-verb vs `insert {elements[]}` superset (already registered). OT deliberately did not pre-empt these (api.ts:163-166, :183-186).
4. **Does D-HB2 get 09 law?** I propose YES (finding #14, P1): a §3.3A row stating the scene-load boundary's rejected-whole contract + the round-trip law + the escape hatch, and one mirrored line in 17's round-trip matrix (the M53/M49R pins are the executable acceptance). Does the orchestrator also want the W9 E8/E13/M47 R3A-3 "raw-constructor escape hatch" re-key noted (the hostile states those pins defended moved to load-rejected)?
5. **Does the keyframe-authoring story need 09/06 law?** The AR-2 gesture (greenfield dblclick) + the registry reconciliation are OT/UI law; the model-side consequence is nil (keyframes persist in `animations` unchanged). Recommend: carry the GESTURE in 05 §8 (+16 §3.12 note) per finding #16, and one sentence in 09 §0's OT row (the upsert verb's UI-routed status — the exception-list fix #15 covers it). No 06 §5.9 row needed (no new op family).
6. **The gesture-seam carrier question** (the task's direct ask): spec 16 (keyboard), 18 (shell), 05 (timeline) do NOT carry the three seam switches today — the specs speak the union vocabulary at the command level, and the seam switches are OT-internal dispatch facts. The one spec site that DID carry the old seam law (05 §0's exception enumeration with the "batch-gesture one-history-entry law" gloss) is now false (#15). Minimal-carrier recommendation: fix #15 + finding #16's one-row-per-surface additions; do NOT invent union-vocabulary rows for wire-dispatch origins (that's 15 §13.15's job — the flipped rows #6/#7 + the registry-export note #2 carry it).
7. **The consumer re-pin chain**: the app mirrors `6e2b91a` (pre-D-ARCH-6) and the engine vendors `6e2b91a` (per the R27 skeleton's engine row); the app's queue is filed (`c020b2a`). The spec's consumer-pin rows say `c15a629` — doubly stale. Confirm the W4/W6 re-pin uses the queue's target (`55c81c0` mirror) rather than re-verifying intermediates.
8. **battery_r27 check classes**: (a) the census-coherence checks re-key 24+6 → 31/28+3 (the lineage tail: 24→28→30→24+6→25+5→31=28+3); (b) a new class worth considering: "the exception-registry export exists" (the §0 NOTE #2 flip — the app's live-registry consumption law is the fleet-side twin); (c) the 632 count + the 452+30+150 split (the report json is the authority — the in-page/real-mouse split convention changed shape at R26/R27; recommend citing total only, as 15 §0:14 already does).
9. **The M49C instrument's origin-attribution law** (HA-4-2, session 16): the gate counts ONLY origin:"ui" dispatches (api.ts:366-380; run-timeline-tests.mjs gate phase). The specs' "machine-checked" claims predate it. Fold one clause into the #1/#5 re-base ("origin-attributed — test-handle dispatches never count") — it is also the law the app's ported gate keys on (their AR-2 filing).
10. **P3 in-repo notes (file upstream, not spec)**: api.ts:235's "the 30 wire command types" header (array is 31); SEAMS.md:184-185's historical "24/24 + 6 exceptions" line in the session-14 section (superseded in-file at :224-233 but a stale read is possible). These are OT-side polish, not spec amendments.
