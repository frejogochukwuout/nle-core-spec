# R27-W5 — review-rulings: the adversarial review of the round's rulings (D37-D41 + the ruling-rows)

**Task ID:** R27-W5-RULINGS · **Agent:** review-rulings (fresh-context adversarial reviewer) · **Date:** 2026-09-14
**Under review:** the orchestrator's disposition over `xcut-amend.md` §4 (R1-R13), `xcut-matrix.md` §4 (the D39-candidate trio), `xcut-plan.md` §5 (the D38 proposal), `spec-08.md` F2/F6, `mock-color.md` (the venue law + curve-order analysis), `mock-verdicts.md` §3.2/§4.
**Method:** every load-bearing claim re-derived FIRST-HAND at the live pins — the engine (`f9ac806`/code anchor `50b91f5`), OT (`55c81c0`/code pin `970948a`, src-diff empty ⭐), WDC (`ec8fd5c`), nle-ui (`32abd58`), app (`c020b2a`), the mock trees, and the spec corpus — plus the D-lineage check against ARCH-R22/23/24/25 + 00-master's ledger. Report-only: no corpus edits, no commits; stayed on main.

**The headline:** **zero REJECTs; D37-D41 all RATIFY-WITH-AMENDMENT; 7-of-8 ruling-rows RATIFY clean (R4 amended).** Every ruling is correct on its evidence — I verified the load-bearing facts end-to-end (the curve orders in BOTH the engine and the mock; the preservePitch/volume-dB laws across all three layers; the pin conventions against the live git logs). But the disposition has **three dropped rulings** (xcut-amend's R6 + xcut-matrix's D39-B/C — none appears in the D-list OR the ruling-rows list), **one mis-framed clause** (D41's linkage parenthetical re-opens the D32.5-settled question instead of ruling the actually-open Stage-0 question), and a set of **internal-consistency defects the W6 wave would otherwise bake in** (the draft plan's stale D38 self-reference, its 481+151 split vs R4(ii)'s total-only law, its 73-vs-59 data-test figure, and the eight-stream arithmetic that doesn't add up).

---

## §1 The D-numbering lineage check (the precondition)

- **D1-D36 are all consumed; D37 is the first free number.** Verified: D18-D22 (R22), D23-D25 (R23), D26-D29 (R24), D30-D36 (R25, ARCH-R25:4's namespace note), **R26 assigned none** (00-master v11.0: the execution round — no new decisions). Nothing below D37 is skipped; the disposition's D37-D41 are sequential. ✓
- **The lineage hazard is renumbering propagation, not assignment.** The source reports carry THREE different provisional numberings: xcut-plan §5 calls the plan overhaul **D38** (its draft plan's ruling-basis line, §6 lineage table, adoption path, and battery hook "the D38 ruling-basis presence" all say D38); xcut-matrix §4.3 labels its trio **D39-A/B/C** and says "the D37/D38 slots are claimed by the preservePitch + wire-census candidates per spec-00/spec-15"; xcut-amend R1/R2 assign D37 to the curve order. The disposition's resolution (D37 curve / D38 pair / D39 A3 / D40 plan / D41 Stage-0) is coherent and follows the consolidator — **but W6 MUST carry an explicit renumbering map**, or the corpus inherits: the draft plan asserting "D38 = THIS overhaul" against a D-register where D38 is the model-coherence pair; 06 §0's new Stage-0 GAP rows citing the dead "D39-A" label; and battery_r27 checking for a "D38 ruling-basis" that no longer exists. (AMENDMENT-0, applies to all of §2-§4.)

## §2 The five D-level rulings

### D37 — the curve composition order (y∘ch): **RATIFY-WITH-AMENDMENT**

**Correct on the evidence — verified first-hand at both decisive sites:**
- The mock pins y∘ch: `gradedFrame.ts:91-106` (`composedLut`: `evaluateCurve(yPts, evaluateCurve(ptsOf(pts), i / 255))` — channel first, luma on top) + `gradedFrame.test.ts:84` ("THE COMPOSITION ORDER … y∘r, **not r∘y**") + the :187 pixel-loop test. Flipping 08:299-300/:330-332 **aligns** the spec with the mock's pinned tests — it resolves the contradiction; nothing in the mock breaks.
- No engine test pins any order (grep: zero curve-order assertions across `tests/` — only the frozen-API name "curves" at `api-surface.frozen.ts:196` and an unrelated planner comment). **Flipping 08 does not break the engine's suite.**
- The "don't cite Resolve" hygiene is right: F6's research record (BMD manual silent; 3 negative web probes; the R24-A2 base) is the correct posture, and the mock's own "Resolve's Custom Curves order" comment (`gradedFrame.ts:85`) is unverifiable — the spec text citing our own grounds is the defensible form.

**The stress-test finding the ruling under-weights (AMENDMENT):** the ruling's grounds say "FreeCut's order carries no counterweight" — but the counterweight is not just FreeCut-the-ancestor: **the engine's OWN current bake implements master-first**: `nle-engine/src/lib/nle/effects/pipeline.ts:1483-1486` (`buildGpuCurvesLutData`: `const master = evaluateMonotoneCurve(masterPoints, x); data[i*4] = Math.round(clampNum(evaluateMonotoneCurve(redPoints, master), 0, 1) * 255)` — channel(master(x)), plus the same 256-entry 8-bit LUT shape 08 already overrides to 1024/Uint16). The counterweight is real but weak (the engine is the in-between reference, not canon; that bake is already slated for r3 corrective work). The amendment: **spec 08/19 must register `pipeline.ts:1483-1486` as the named r3 flip site** (the same corrective class as the 8-bit LUTs / sRGB bug / clamps rows), so the r3 binding consciously flips it rather than silently shipping master-first against the new law.

**Two riders (P3, fold into the F6 text):** (i) the §19 dual-curve test's negative arm should read "NOT `r(y(0.5)) = r(0.7)`" — F6's current "NOT 0.4 evaluated at y(0.5)" is garbled; (ii) the **LUT-domain sub-question** (mock-color §4 row 4's second half: the encoded-domain double-hop vs a linear→linear LUT sampled directly — one GPU fetch) is registered as a P2 parity rider but never RULED. Either rule it now (linear→linear one-fetch as the GPU law; the mock's encoded bake stays the parity oracle inside the ≤1-LSB-equivalent gate) or explicitly defer it to the r3 gate — the current half-state invites a mid-binding relitigation.

### D38 — the model-coherence pair (preservePitch + volume-dB): **RATIFY-WITH-AMENDMENT**

**Both laws verified end-to-end, first-hand, all three layers:**
- preservePitch: engine `scene-to-segments.ts:596/:623` (`if (el.retime?.maintainPitch === true)` — runtime absent≡false) ⭐; app `sceneBridge.ts:262` (`maintainPitch: el.preservePitch !== false` — the absent≡true conversion) ⭐ + `:447` (back-projection writes only explicit false) ⭐ + the preservePitch-only patch keeping the rate (`:499-:518`) ⭐. The dual-representation precedent (D28-A2) exists as cited (09:13-14).
- volume-dB: OT `src/lib/timeline/core/audio-params.ts:28-47` is the one home ⭐ (VOLUME_DB_MIN −60 / MAX +20 / DEFAULT 0 / `clampVolumeDb` folding NaN+±∞→DEFAULT / `volumeDbToLinear`); the engine value-imports it ⭐ (`bridge/opencut-laws.ts:54-79`); the [−60,+20] span's linear equivalent [0.001, 10] is arithmetically exact.

**Amendments:** (i) register as **D38.1/D38.2** — the corpus's citation pattern is sub-numbered halves (D28-A2, D32.2, D33.4); a merged blob forces "D38(i)" citations that don't match the register's grammar. (ii) **State the two-field structure explicitly** — cluster-a's compressed text ("persisted LINEAR gain (1 = unity) … absent≡0 dB≡unity") conflates the two absent-defaults: spec-09's `el.volume` is the persisted LINEAR form (absent ≡ **1**), OT's `ElementParams.volume` is dB (absent ≡ **0 dB**, per the D-S5 header at audio-params.ts:4) — both ≡ unity gain, numerically different defaults. The one-home owns the dB domain + conversions, NOT the persistence shape. Say so, or the next round reads "absent≡0" against a linear field. (iii) Keep the ChannelEditor residual routed to the plan as R12 states — verified live: `nle-ui/src/components/mixer/ChannelEditor.tsx:91/:99` still `max={4}` (min −60) while the Inspector is +20 ⭐.

### D39 — the A3 wheels, widen-via-projection: **RATIFY-WITH-AMENDMENT**

**The Y-field stress-test PASSES:** the Y numeric field writes the same scalar the master dial drives (per-wheel: lift −0.2..0.2 / gamma 0.25..4 / gain 0.25..4 / offset in ×1023 code values — `gradeMath.ts` `yrgbReadout` + `DEFAULT_GRADE` metas) — same field, same range, zero representational loss. The 28-f32 uniform genuinely stands unchanged (verified layout: `packWheelsParams`, 28 slots, byte-0 `shHue` → byte-27 `_pad2`).

**The real semantic gap the ruling never states (the amendment's core):** the (hue, amount) model spans a **1-D curve inside the 2-D luma-neutral plane** — `wheelDelta(h,a) = (t−1)·a − luma709((t−1)·a)` sweeps one direction per hue (`gradeMath.ts:146-155`), so an arbitrary typed R/G/B triple generally has **no exact representation**: editing R/G/B is a nearest-point projection with a snap, not a bijective solve. Resolve's YRGB fields are 3 independent channel values (a 3-D per-wheel model) — ours cannot fully honor that, which is the honest price of zero-shader-change. State it IN the ruling, or r3's parity fixtures will rediscover it as an "unexpected" snap. Required rider text: (i) the projection-snap law (off-curve edits → nearest on-curve (h,a); the residual is silently dropped); (ii) the **round-trip law + a §19 test** — the displayed R/G/B are luma-INCLUSIVE (`t_c·amount`) while the solve is luma-NEUTRAL; typing displayed values back must return the same (h,a) (it does algebraically — the luma component strips exactly — but only if the solve neutralizes first; pin it); (iii) the **2-to-1 ambiguity convention** — `wheelDelta(h,−a) = −wheelDelta(h,a)` means (h, −a) and (h+180°, a) are the same point; pick one (recommend: amount sign is canonical, hue confined to [0,360)); (iv) the offset wheel's ×1023 dual-unit readout law (one sentence, D28-A2-class).

**The sequencing constraint is STALE (amendment):** the ruling must land "before the sibling's W3 wheels rewrite" — but **W3 already landed** (`b08b83a`, mid-W4) and implemented the drag grammar with the readouts **still read-only** ("read-only with the spec 08 §16.A aria hint, not editable" — `WheelsPanel.tsx:46-47` ⭐, deferring to the spec's open decision). The ruling is timely and unpre-empted; re-state the constraint as "before the mock's next wheels touch + before r3 fixtures."

### D40 — the plan overhaul: **RATIFY-WITH-AMENDMENT**

**Correct on the evidence:** all 7 estimate sites verified live in the current plan (:3, :20 "the 4-6 wk estimate", :23, :73 "one-business-day", :111 the solo/two-dev staffing model, :117, :155 the whole Estimates paragraph); the draft replacement removes them all and carries the gate-defined phases, the cold-executor bar (owner + entry point + gate + edges on every row — the §0 table delivers it), and the 5 user-gated points (D38.5's list matches: the r1-entry scope gate, D28.2, D6's r4 re-affirmation, the C7 timing, the color rulings at r3-entry).

**Amendments (all W6-mandatory):**
1. **The eight-stream arithmetic doesn't add up.** The draft's own taxonomy names **5 repo tracks** (S-app/S-ot/S-engine/S-wdc/S-package) + S-spec + the mocks track = **7**; the parallelization map's 8th stream is "the standing P3 sweeps" (a cross-repo lane that overlaps S-ot/S-wdc/S-package); D38.3's formula ("six repo tracks + the spec track + the mocks track") matches NEITHER enumeration. Fix the law text: "seven tracks + the standing P3 lane" (or name the eighth) — a ratified law must not contain an arithmetic that fails its own table.
2. **Renumbering propagation** (AMENDMENT-0): the draft's ruling-basis ("D38 THIS overhaul"), §6 lineage ("the D38 overhaul"), adoption path ("00-master's D-register records D38 at the fold"), and battery hook ("the D38 ruling-basis presence") all re-key to **D40**; D38.5's "the D37 color rulings at r3-entry" re-keys to "the color rulings (D37/D39) — r3-entry gates their CONSUMPTION (the venue table + parity fixtures), not re-decision."
3. **The draft's §1 contradicts R4(ii) as ratified:** it carries "632/632 — **481 in-page + 151 real-mouse across 72 entries**" while R4(ii) rules total-only (spec-19's artifact decomposition is 452+30+150 — the two splits disagree on the 30 DragEvent/keyboard tests' bucket). Pick one: either the plan cites the total only, or R4(ii) is restated as "the total is canonical; any split must be the report-json's own decomposition."
4. **The draft's S-app row carries "73 unique names"** for the data-test census vs spec-19's 59 (component tree) / spec-17's 73 (all src) — see R4(iii) below; the plan row must match the pinned scope.
5. The draft's S-spec row lists "the D37+ rulings this round" as six items (curve order, A3, **node-graph, C50, venue table**, preservePitch+volume-dB) — under the disposition, node-graph/C50/venue-table are ruling-FREE W-B amendment families (08 F3/F4/F5), not D-entries. Reconcile the row with the D37-D41 register (this is wording, not substance).

### D41 — the r1-entry mechanism set (Stage 0): **RATIFY-WITH-AMENDMENT — the review's biggest correction**

**Not redundant with D32.5 — but the disposition's framing re-opens the settled question instead of ruling the open one.** Verified: **D32.5 already settled the MODEL SHAPE** (09 §3.1A B1:335 — "the persistence form is PAIRWISE (linkedTo…); the runtime expansion is GROUPS — the engine's linkedGroupId, expanded from the pairwise links at load… never persisted here"; 06 §5.0:432 — "pairwise at rest, groups at runtime; the r1 port maps groups→pairs explicitly"). The disposition's parenthetical — "the linkage model decision (**pairwise linkedTo vs the engine's linkedGroupId** — the biggest hole)" — states the D32.5 question, not the open one. Ruling it as framed would (a) violate the round's own charter ("do not re-litigate settled law D1-D36", BRIEFING protocol) and (b) leave the real hole unruled.

**The actually-open Stage-0 question** (mock-leverage §5.2:139, the collision map, scout-engine's E1/E2 row): **an opencut-side linkage field/registry in OT's own model, OR dispatch-level pairwise (the app's sidecar) + the relink policy ported into the app's bridge.** "Everything in modes 5/6/7/9 + the companion fan-out hangs on it" — the engine's D32.1 fan-out LAW is spec'd; what's absent is WHERE THE LINK LIVES during OT ops (OT has NO linkage field at all — av-link.ts:39-43 is the engine's, the app dispatches pairwise). The amendment: re-frame D41's linkage clause to that question, citing D32.5 as settled context it does not reopen; the constants lattice + E3/N3 envelopes are correctly framed as-is. Also scope D41 as the r1-entry GATE REGISTRATION (decision material + owners + acceptance per xcut-matrix D39-A: 06 §0 GAP rows, owner S-spec-06+S-ot, acceptance = the decision text + the constants module landed in OT) — the decisions themselves are made at r1-entry, per D40's own D38.2. And **fold NS-4 in** (see §3 item 1) — it is the same class (the r1-entry architecture precondition set), which also restores the dropped R6.

---

## §3 The ruling-rows

| Row | Verdict | Notes |
|---|---|---|
| **R4 — counting conventions** | **RATIFY-WITH-AMENDMENT** | (i) 29-of-78 verified coherent: 27 @ `c15a629` + upsertKeyframes (routed singular) + removeKeyframes (landed plural); insertBatch → insert's existing slot; retimeKeyframe's singular-absolute has no counterpart (the plural delta form is the r1-pending extension). The conservative no-counterpart call for retime is defensible AND the inline-convention statement covers the flip — but note the **coupling to R11's r1 decision (a)**: if r1 aligns the retime shapes, the count becomes 30; the convention sentence must survive that flip. (iii) the data-test basis needs its **SCOPE stated, not just the number**: three figures already diverge — 59 unique (component tree, spec-05/spec-19), 73 unique (all src, spec-17), ~103 (repo-wide, spec-19) — and the disposition's own draft plan cites 73. Pin: "unique names, scope = the component tree (59 at `55c81c0`); all-src and repo-wide figures are separately-scoped rows." (ii) total-only ✓ — but the draft plan's §1 carries the 481+151 split (the contradiction to fix at W6, D40 amendment 3). (iv) dual-figure variants census ✓ (matches xcut-register's ONE-method law). |
| **R7 — mock-verdicts open set** | **RATIFY** | Stronger than the proposal knew: the sibling's W1 (a83d4d2) **already implements the strip-only dim** ("a ~50% dark overlay ON THE STRIP (never on the poster image; the image is never dimmed)" — `SourceRangeBar.tsx:13-14` + the `shell-source-dim-left/right` elements ⭐) — the §3(c)-vs-§6 conflict self-resolved in the implementation; the "ask the sibling to reconcile" softens to a register note. C46 satisfied ✓ (16 §3.4A:261 "a source with non-zero duration loaded"; `SOURCE_STILL_PSEUDO_DUR` landed ⭐ — 5s ≠ 0 passes). 1280×800 as registered divergence ✓ (the floor is 18's law; the mock's sub-floor behavior is the deviation — the page-shape round may revisit). Target-model note now + 09 rows at the color round ✓ (OQ-5's own recommendation). Custom-JSON DECLINE ✓ (V18's classification, no spec'd JSON export target). One nit: disambiguate "C46" in the amendment text — the §3.4A gate-context C46 vs the mini LAW-NET C46 ("clip-is-a-button", 16:16) are different ledgers. |
| **R8 — register-timing law** | **RATIFY** | The six coordination rules + WRAP-only re-key match xcut-register's own demand (the battery failing on pin-lag BY DESIGN was the W4 finding); rule 4 (amendments-before-VLM) is correctly carried as the sequencing rider. |
| **R9 — pin conventions** | **RATIFY** | Verified live: engine HEAD `f9ac806` is docs-only over code anchor `50b91f5` (0 src-touching commits after it ⭐); OT HEAD `55c81c0` over code pin `970948a` (0 src-touching commits after ⭐); 748 is the current engine count (multi-report live re-derivation). The QUEUES-not-violations consumer posture matches the two-truth form (consumer pin + owner pin + the queued switch). |
| **R10 — projector re-type** | **RATIFY** | The bridge family exists and is the discharged duty (12 files under `src/lib/nle/bridge/` ⭐, twin-pinned); 19 F-2's text + 00 D16's illustrative-path note are the right edits; S4 parity corpus correctly remains open. |
| **R11 — r1 param-alignment pre-filings** | **RATIFY** | Register-now/rule-at-r1 matches the C7-rename-at-r1-END law; the three decisions (singular-absolute vs plural-delta retime; per-element vs cross-element grouping; bare insertBatch vs the superset) are the right set — spec-15's flipped rows name exactly these. |
| **R12 — diagnostics disposition** | **RATIFY** | 02 §7.4 is the right MASTER home: the five laws are audio-THREAD discipline (what may run in `process()`, what the host does when a processor dies) — 02's scope; 20 already carries the WDC file-map + the two-way 02↔20 link per cluster-d, so the "both" structure is built, not pending. The ChannelEditor residual correctly routes to the plan (verified live ⭐). |
| **R13 — 22-effect count** | **RATIFY** | 15 CPU + 3 DspDiagnosed + 4 worklet-family = 22 ✓; the W3-20 live re-derivation at all three pins supersedes the stale "17 DSP" figure. |

---

## §4 THE DROPPED RULINGS (the disposition must restore — nothing else in the round covers them)

1. **xcut-amend R6 — NS-4's D-level home: DROPPED.** The disposition's D-list and ruling-rows list both omit it. NS-4 (the SceneTracks→render-plan adapter vs a new compositor input contract) is "the r1-entry architecture decision" (scout-engine; the draft plan's own S-engine row item 5). BRIEFING fact #14 says the NS queue must land in the corpus. **Fix: fold into D41** (the same r1-entry precondition class — D41 becomes "the Stage-0 mechanism trio + NS-4's render-path decision"), with 04/07 carrying only the contract rows per R6's recommendation. If not folded, it needs its own D-number.
2. **xcut-matrix D39-B — rateStretch stays wave 1: DROPPED.** Three corpus sites (06 §0 GAP row 1, the plan's r1 row, FF-1's "riding 15 §13.15's wave-1 rateStretch row") vs the leverage map's Stage-4 loose phrase. Fold into **D40's plan content** (the r1 row's wave assignment) — but it must be stated somewhere ratifiable or the W6 plan application inherits the map's phrase.
3. **xcut-matrix D39-C — sync-lock's r1-scheduled GAP row: DROPPED.** Without it, the D32.2 precedence law (lock > sync-lock > link) has **no scheduled enforcement surface** — sync-lock is named in D12.3's port list and §10.5's OT-GAP row but appears in NO r1 wave list ("after the trim family — it wraps them"). This is a concrete W-B edit (06 §0 GAP register row; owner S-ot; acceptance = the carried sync-lock tests green in OT + the track.syncLock field + the two propagations + D32.2 verified per landed mode). **If the disposition doesn't restore it, it is lost with no other carrier.**
4. *(P3)* F11's flagged micro-decision — the `curves-master-channel-affects-luma-only` test (08:2256-2261) matches neither §5.1 nor the mock; recommend the REWORD ("the master curve applies equally to all three channels; a neutral bar's chroma is preserved by construction") so no third model decision is opened; state it so W6 doesn't silently keep the ambiguous text.

## §5 The verdict tally + the fold checklist

| Ruling | Verdict |
|---|---|
| D37 curve order | RATIFY-WITH-AMENDMENT (engine-site corrective row `pipeline.ts:1483-1486`; the LUT-domain sub-question ruled-or-deferred; test wording) |
| D38 model-coherence pair | RATIFY-WITH-AMENDMENT (D38.1/D38.2 sub-numbering; the two absent-defaults + two-field structure stated) |
| D39 A3 widen-via-projection | RATIFY-WITH-AMENDMENT (the projection-snap law + round-trip test + 2-to-1 convention + offset dual-unit; the stale before-W3 constraint re-dated) |
| D40 plan overhaul | RATIFY-WITH-AMENDMENT (the eight-stream arithmetic fixed; D38→D40 renumbering propagated; the 481+151 and 73-unique contradictions reconciled; the S-spec row's D-list reconciled) |
| D41 r1-entry mechanism set | RATIFY-WITH-AMENDMENT (the linkage clause RE-FRAMED to the open OT-mechanism question, D32.5 cited as settled-not-reopened; NS-4 folded in; scoped as the gate registration) |
| R4 counting conventions | RATIFY-WITH-AMENDMENT (the data-test SCOPE stated; the r1-decision coupling noted; the draft-plan split contradiction flagged) |
| R7 mock-verdicts open set | RATIFY (the landed W1 confirms strip-only; the C46 ledger disambiguation nit) |
| R8 register-timing law | RATIFY |
| R9 pin conventions | RATIFY (live-verified) |
| R10 projector re-type | RATIFY |
| R11 r1 pre-filings | RATIFY |
| R12 diagnostics law | RATIFY |
| R13 22-effect count | RATIFY |
| **REJECTs** | **0** |

**The W6 fold checklist (the amendments, as one list):** (1) the D-renumbering map (xcut-plan's D38→D40 everywhere incl. the battery hook; xcut-matrix's D39-A/B/C labels → D41/D40-rider/W-B-row in every pasted GAP text; spec-00's "the D37 option" → D38.1); (2) D37's engine-site corrective row; (3) D38's sub-numbering + absent-defaults; (4) D39's projection-law riders + re-dated constraint; (5) D40's stream arithmetic + the two count contradictions; (6) D41's linkage re-framing + the NS-4 fold; (7) the three restored rulings (R6/D39-B/D39-C) with their homes; (8) R4's scope statement; (9) the F11 reword decision; (10) the C46 ledger disambiguation.

**No ruling the FLEET missed beyond the above** — I checked the obvious candidates: the A2 console-row TAB (V10) and the stills law (V1) are W-B amendment families with no conflict (correctly not D-level); the venue law (F5), node-graph model (F3), and C50 (F4) are uncontested new-law drafts (correctly W-B); the AR-2 consumption law rides cluster-e + the 2A.10 route (the disposition should name 2A.10 once, but cluster-e already routes it). The round's own "nothing re-litigates settled D-law" posture holds everywhere EXCEPT D41's as-written parenthetical — which is exactly the one place a fresh-context reviewer existed to catch it.

*Report-only confirmed: no spec/repo file modified outside this report + the worklog; no commits, no branches; stayed on main. Every ⭐ claim re-derived live this session at the R27 pins.*
