# R27 FLEET BRIEFING — the shared context for the per-spec audit wave (Wave 3)

**Read this FIRST, then your assigned spec, then the scout reports you need.** You audit ONE spec file at sub-spec depth. Report-only: your only writes are your report file + the worklog. No commits, no branches (watchdog), no edits to the spec corpus.

## The R27 pin world (the audit's ground-truth pins — proposed by Wave 1, battery_r27 will enforce)

- **nle-engine `f9ac806`** (the docs-only head over code anchor `50b91f5`; timeline.ts is 8,997 LOC now — **every pre-R26 line number in the corpus is suspect**) — tests **748/748**, tsc 0. The RC round landed: D12 the assembly ruling RATIFIED engine-side (opencut is THE editing engine; the engine's stack is the render/export venue model; one-directional junction); OV-01/OV-02 the volume-dB law + retime clamp one-home collapse (bridge/opencut-laws.ts deep-imports opencut's pure leaves); OV-05 the visual seam reads el.transitionOut; OV-12 the vendored-tree re-pin gate in engine CI; M2 Wave 1 the bus-law + sidechain-parity (materializeStrips); NS-1..NS-5 design queue; the r1-port collision map.
- **opencut-timeline HEAD `55c81c0`, code pin `970948a`** — **632/632**, M49C 28/3. **D-ARCH-6 LANDED: `WIRE_COMMAND_TYPES` = 31 verbs (28 routed + 3 exceptions: selectElements/advancePlayhead/trim) — the singular removeKeyframe/retimeKeyframe RETIRED; `timeline.insertBatch`/`removeKeyframes`/`retimeKeyframes` live (one history entry each; the shared validateInsertElement helper; F1A-3; intra-batch overlap CONFLICT whole-batch; empty = INVALID_PARAMS; the WYSIWYG bake; the kind-split + staggered construction at the library layer).** AR-1: core/audio-params is the numeric laws' ONE HOME (VOLUME_DB_MIN/MAX). AR-2: keyframe authoring → timeline.upsertKeyframe, then REMOVED upstream when its UI coverage landed. D-HB2: fromJSON is the scene-load boundary.
- **web-daw-core `ec8fd5c`** — **777/777**. The S-series upstream wave (19 locked files + 18-file closure): the diagnostics EngineDiagnostic bus + heartbeat monitor; the rebuilt dsp-effects-worklet bundle (DspDiagnosedProcessor: 256-quantum heartbeats + NaN/silence scans in process()); **the rt-safety-gate transitive-heritage fix**; wam-instrument+state; the instrument factory/registry/param-path; the pure-logic MIDI family; the sample-catalog split. The waveform contract survived byte-verbatim (777 includes the 14 pins). **WDC's own docs never absorbed the S-series wrap (HANDOFF stale — their next session's problem).**
- **nle-ui `32abd58`** — **690/690**. **R9-b: `el.preservePitch` joins ElementJSON (absent≡true; the engine's retime.maintainPitch projection) — the R26 model-decision ask is RESOLVED in code.** FW-D setElementFieldAll (one user verb = one undo unit per owning domain). F4: identity guards (law-73 class), kind-aware toggleMuteAll, duplicateElements SEVERS linkedTo. AW1-2: Inspector Gain ceiling +4→+20 (the [−60,+20] fleet domain).
- **nle-test-app `c020b2a`** — **252/252**, census register **42 = 36 zero-action + 5 carriers + 1 host** (carriers: TimelineView, use-timeline-actions, use-keybindings, TimelineContextMenu, use-playback-ticker @ OT mirror 6e2b91a). AR-2: the wire-coverage gate consumes the LIVE WIRE_UI_EXCEPTION_VERBS from the vendored barrel + the real volume-line dblclick → timeline.upsertKeyframe origin 'ui'. M2 Wave 1 app-side: the bus fold REPLACEMENT, single-owner sidechain, the worklet VENUE fix. **maintainPitch RESOLVED in code** (engineService.ts:510-513, sceneBridge.ts:262/:447) — the app's PLAN row is stale.
- **shell-mini: R24-2 IN FLIGHT (W0 foundation + W1 property editing + W2 transitions/fades landed mid-audit; count 357→415; census 145 units/132 GAP/382 authored).** **shell-variants: R25-W1 (the insert rescue wave) landed; census re-read 1,762 it-blocks; more waves coming.** Coordination law: the register re-keys at the sibling's round-WRAP only; per-wave movements are recorded, not chased (the mock-verdicts report's 6 rules — `audits/fleet-r27/mock-verdicts.md` §3).

## The corpus-wide facts your audit must apply (the W1/W2 P1 set — verify in YOUR spec, cite line numbers)

1. **The wire census moved: 31 verbs = 28 routed + 3 exceptions.** Any "24 routed + 6 exceptions" (or 30/5, or "upsertKeyframe is an exception") reading is STALE (spec 15 §4.1A/§13.15, 05 §16.5, 06 §0, 12, 17, 19).
2. **The D-ARCH-6 batch verbs LANDED** (insertBatch/removeKeyframes/retimeKeyframes + the singular retirement). Spec 15's QUEUED rows saying "wire exposes only the singular verbs" are FALSE now.
3. **`el.preservePitch` is ElementJSON law** (absent≡true; the engine's retime.maintainPitch is the runtime projection; the spec-09 model + spec-06 §5.12 companion law need the ruling).
4. **The volume-dB domain is [−60,+20] fleet-coherent; opencut's core/audio-params.ts is the ONE HOME** (the D7 amendment + AW1-2). Spec 09's linear "0..1" volume statements need the dual-representation law; spec 18's "Gain dB is withdrawn" is FALSE (the [−60,+20] dB rail is live).
5. **The engine's sidechain wiring EXISTS now** (M2 Wave 1) — spec 20's "the engine's sidechain wiring does not exist yet" is FALSE; the bus-law/AuxBusSettings.muted rows missing.
6. **The OV-05 one-input-source transition law** (el.transitionOut is the field the visual seam reads; params override) — spec 07 needs it.
7. **The WDC diagnostics/heartbeat law is missing from ALL 21 specs** (the EngineDiagnostic bus, the stall bypass-to-passthrough, the in-process() protocol, the transitive-heritage gate fix).
8. **The E1/E2 fixes LANDED R26** (engine 8188d4e; the 13-test pin file; 06 §0's E1 row is flipped DONE but line numbers like :4719/:4702 are stale — timeline.ts moved to 8,997 LOC; the class methods re-anchored: rollingTrimItems :3324, rippleTrimItem :~3300s, slip :4956, slideItem :5097, performInsertEdit :5630, performOverwriteEdit :6007, rateStretchItem :3840).
9. **The mode matrix holds** (the trim/insert HTML audits found ZERO P1 spec gaps — R25's D30-D32 law is verified at sub-spec depth; see mock-trim.md/mock-insert.md for the P2/P3 residuals: 18 §5's tool rows, 05 §8.1's stale ToolMode enum, 06's stale engine line pins).
10. **The color cluster (mock-color.md)**: everything Resolve-copied is WebGPU-reproducible; the P1s are the wheels gesture law (A3: relative/accumulating + master dial + dbl-click color-only — vs 08 §4.3's absolute puck), the node-graph execution model (08 unspecced), the C50 sidecar/sequential ruling, the venue table, and **the curve composition-order conflict (spec 08:299 "channel(master(x))" vs the mock's y∘ch "Resolve's order" gradedFrame.ts:83-108)**.
11. **The variants-verdict contradictions (mock-verdicts.md)**: 18:176 "stills keep the honest static band" vs A1 "stills get FULL transport + strip dimming never poster dimming"; 08:16's stale W-B color-layout text vs the confirmed console-row TAB; 08 §4.3's absolute wheels vs A3's relative/accumulating (also 08:970's 33ms law).
12. **The AR-2 consumption law** (a consumer gate consumes the exported registry, never a hand-mirrored copy) — proposed as a 00-master §2A standing law.
13. **D12's ratification** — engine-side DECISIONS #12 ratifies the spec's Decision 12/16 junction; only a dated ratification note needed.
14. **The NS-1..NS-5 queue + the r1-port collision map appear nowhere in the spec corpus** (engine-side filings; the plan should carry them).
15. **maintainPitch's app queue row is stale** (the code landed; the plan's S-app row + the app's PLAN row need the flip).

## The audit protocol (every agent)

- **Depth bar:** section by section; every claim you make carries file:line (or commit) evidence; every spec claim you CHECK re-derives against the live tree (grep/read — the repos are at /home/z/my-project/*).
- **Verdicts:** per section: SOUND (evidence cited) / P1 (spec states something FALSE now, or a landed law is missing entirely) / P2 (stale citation/count/pointer/underspecified) / P3 (polish). NO P4-and-lower noise. Do not re-litigate settled law (D1-D36) unless the corpus text now contradicts the live truth.
- **Output:** your report at `audits/fleet-r27/spec-<NN>.md` with the per-section table + a fix-list (the exact edit proposals, so the amendment wave can apply them mechanically).
- **Worklog:** append `---\nTask ID: R27-W3-<NN>\nAgent: spec-<NN>\n...` to /home/z/my-project/worklog.md.
- **Do not** propose edits to files other than your spec; cross-spec coherence is the cross-cut wave's job (but DO record cross-spec notes in your report's §4).
