# Scout Report — nle-engine (R27 Wave 1, module scout)

**Task ID:** R27-W1-ENGINE · **Agent:** scout-engine · **Range audited:** `0a49286..f9ac806` (63 commits, 2026-09-10 → 2026-09-13)
**Method:** full commit-log read + code reads of every headline change (opencut-laws.ts, composition-frame.ts, scene-mixer.ts, mixer-track-model.ts, scene-to-segments.ts, api-surface.frozen.ts, .github/workflows/ci.yml) + the engine's own law docs (`.agents/DECISIONS.md`, `.agents/PLAN.md`, `.agents/HANDOFF.md`, `gaps/audit/ra-round/audit-AW1.md` §3, `gaps/audit/rc-round/audit-RC-V1.md`) + the full spec-corpus grep for pins/counts/line-numbers. The vitest suite was NOT run live (the clone has no `node_modules` and the vendor submodules are uninitialized — the sandbox would need a full install); counts below are the repo's own gates lines, cross-checked against static test counts (see §1.4).

---

## §1 The commit-range ground truth (verified by code reads)

### 1.1 What landed, in order

| Wave (commits) | What | Code evidence |
|---|---|---|
| **H-round fix fleets** (F1a `a1448ad`, F1c `fa27a69`+`a9313a1`, F2 `b7a5545`..`1c04887`, F5 `c1e6ad9`/`9608973`/`089ed2a`, F6 `ccd2f0b`/`553f203`/`ecdf6b5`) | the E1 linked mid-clip insert fix, N3 locked-track op-gate fleet (spec 00:838's law), N4 NaN guard fleet, non-finite volume/pitch folds, exception-safe painter brackets, pcm-s16 in the audio-only ladder, mode-clamp family (ROLL-2/ROLL-3/slide-G3/ripple-trim-G3), relink per-group + slip-G3 + N5-composite | `git log --oneline 0a49286..f9ac806`; per-commit messages carry per-fix file lists |
| **G1** (`3a28b6e`) | 42 deferred upstream test laws re-expressed against the relocated bridge (670/670) | commit message |
| **FW-A** (`446f92d`..`e9f2d91`, 9 commits) | the HW-1 opus findings: transform drop-verbs (TRANSFORM_INVALID_DROPPED), N4 transform + add-family gates, sourceFps repair → OWNING timeline fps, registry epoch + collision-free auto-ids, getMaskView transient, composite() clears before degraded early-returns, findSourceOverrun fps triple-fallback, groupBounds loop (688/688) | commit range + messages |
| **Re-pin `f056f02`, `a07c222`, `2501183`, `55e5365`** | vendor/opencut-timeline: `c15a629`→`3be2dea`→`b98e847`→`a25d199`→**`6e2b91a`** (the s16 upstream wave absorbed) | `git submodule status` at HEAD: `-6e2b91a… vendor/opencut-timeline` |
| **FW-C** (`2b2de33`, `6e34887`, `4c1975f`, `a18e07b`) | **P1 `bridge/opencut-laws.ts` created** (7 hand-synced replicas → ONE leaf + drift fences); AW1-3 fade-curve twin-equality pin; **D7 amendment (volume dB [−60, +20])**; **D11 the retime-domain lattice** (722/722) | `.agents/DECISIONS.md:139-153` (D7 amendment), `:474-519` (D11) |
| **FW-E1/2/3** (`d0906a8`, `57f9871`, + app-side) | anchorX/anchorY transform hardening; RA-V1 P3s; the dormant-seam wiring; 729/729 | worklog `:5658-5673` |
| **RA verification** (audit-RA-V1/V2/V3 in `gaps/audit/ra-round/`) | all waves SOUND; RA-V2-1 (P1, census register miss) → FW-E2; RA-V2-2/3 dormant seams → FW-E2; RA-V3 CONVERGED (0 open P0/P1/P2) | `gaps/audit/ra-round/audit-RA-V3.md` |
| **OV-01/OV-02** (`b5f39cb`) | **the value-law collapse**: `opencut-laws.ts` now VALUE-IMPORTS the upstream pure leaves (`opencut-timeline/core/audio-params` clampVolumeDb / volumeDbToLinear / VOLUME_DB_MIN/MAX/DEFAULT; `opencut-timeline/ops/retime` clampRetimeRate behind flat-number adapters); scene-to-segments deletes its clamp replicas; fences upgraded to adapter≡upstream grids + semantic literals; **AR-2 seam pin** (REAL `upsertKeyframe` channels fold to exact N2b breakpoints) | `src/lib/nle/bridge/opencut-laws.ts:54-79` (the imports + thin adapters), `:88-97` (ticks law stays a replica + fence), `bridge-seams.test.ts:2283-2500` (upsertKeyframe import + OV-01/OV-02/AR-2 describes) |
| **OV-05** (`9850231`) | **one input source for the transition family**: `composition-frame` reads the ENGINE field `el.transitionOut` (type discriminant `"crossfade"` + ticks→sec + finite/positive) as BASE, the consumer sidecar as OVERRIDE | `composition-frame.ts:301-317` (`elementTransitionOut`), `:334` (`params[el.id]?.transitionOut ?? elementTransitionOut(el)`); pins at `bridge-seams.test.ts:869-914` |
| **OV-12** (`2fd68c6`) | **the vendored-timeline re-pin gate**: engine CI's `vendored-timeline` job runs the vendored OT tree's OWN suite whenever the gitlink moves (fail-safe detection: non-push/missing before-sha → verify anyway) | `.github/workflows/ci.yml:216` (job), `:226-253` (detection), `:253` (skip notice) |
| **M2 Wave 1** (`50b91f5`) | **F-1 the bus return-gain law**: `wireBuses` writes through upstream `applyAuxBusGain` (`muted ? 0 : clamp(volume, 0, 1)` — one law, three consumers: DAW engine syncAuxBuses / offline renderer / the NLE bridge); `AuxBusSettings.muted` additive-optional. **F-2 THE PARITY LAW**: sidechain wires collected from the strips' OWN declarations (`collectSidechainWires`) and connected INSIDE `materializeStrips` after `applySettings` — the offline export path previously NEVER wired sidechains (a ducked compressor was silently inert on export); sever law = per-wire IDENTITY DIFF. **The worklet venue fix**: `public/worklets` is now a git SYMLINK to `../vendor/web-daw-core/public/worklets` | `scene-mixer.ts:42-59` (law header), `:65` (the import), `:277-280` (applyAuxBusGain call), `:222-225` + `:314-339` (syncSidechainWires after applySettings; identity diff); `mixer-track-model.ts:89-102` (AuxBusSettings.muted); 9 pins in `tests/vitest/m2-wave1-mixer-surface.test.ts` |
| **D12 ratification** (`1064f91`) | **DECISIONS D12**: opencut is THE editing engine; the engine's TimelineData stack is the render/export venue model (venue-internal state like an IR, NOT a second user timeline); the junction is ONE-DIRECTIONAL (`SceneTracks → bridge {scene-to-segments, transition-inputs, composition-frame} → the venue stack`); edits NEVER flow back; engine-native edit verbs FROZEN venue-internal; the engine Timeline NOT deleted (offline reference + the parity twin that pins r1) | `.agents/DECISIONS.md:521-584` |
| **RC-V1 verification** (`c01a187`, `gaps/audit/rc-round/audit-RC-V1.md`) | 0 P0/P1; 1 P2 (AR-2 seam unfenced app-side — closed in-session app `8a3eb35`); 3 P3 registered (the `conversions.ts` `dbToLinear` conscious-decline; the bezier mid-segment audio divergence; + app-side) | `audit-RC-V1.md:142-143` |
| **WDC re-pin** (`480a216`) | vendor/web-daw-core `387f327` → **`ec8fd5c`** (the S-series upstream wave: 19 locked files + the 18-file closure, all VERBATIM; the twin fences held — bridge-seams 151/151) + the tsconfig `files` entry for the vendored ambient declaration | `git submodule status`: `-ec8fd5c… vendor/web-daw-core`; commit message |
| **SKILL wrap** (`f9ac806`, HEAD) | laws 88-91 (live-registry consumption, mutation-gate robustness, three-config subpath alias, gitlab namespace probe) — `.agents/SKILL.md` only | `git show --stat f9ac806` |

### 1.2 The exact code-pin proposal

**HEAD `f9ac806` is DOCS-ONLY over the last code commit** (`git show --stat f9ac806`: `.agents/SKILL.md` +49 only). The last commit touching `src/` or `tests/` is **`50b91f5`** (M2 Wave 1: scene-mixer, mixer-track-model, the M2 test file, the public/worklets symlink). After it: `2fd68c6` (`.github/workflows/ci.yml` — the OV-12 gate), `1064f91`/`c01a187`/`c49ddb2`/`9ed8c77` (docs/worklog/HANDOFF), `480a216` (tsconfig `files` entry + the WDC gitlink), `f9ac806` (SKILL).

**Proposal: pin `f9ac806`** — the app ALREADY vendors it (nle-test-app `9448667` "engine 1064f91→f9ac806"), the docs-only delta is inert, and one pin keeps the fleet lockset coherent. For line-number-sensitive citations the **code-content anchor is `50b91f5`** (src/tests tree; tsconfig+vendor = `480a216`).

### 1.3 Gates at HEAD (the repo's own gates lines)

**vitest 748/748, tsc 0, layer fence 52, probes 9/9** — attested twice: worklog @ `c01a187` ("Engine @ c01a187: 748/748, tsc 0, fence 52, probes 9/9", `worklog.md:5806`) and the `480a216` commit message ("Gates: vitest 748/748 (738 + 10…), tsc 0, fence 52, probes 9/9"). Count progression across the range: 471 (R26 pin) → 670 (G1) → 688 (FW-A) → 722 (FW-C) → 729 (FW-E1) → 736 (OV-01/02) → 739 (OV-05) → **748** (M2 Wave 1 rebased in + the WDC-sync parametrized growth). ⚠ **ARCH-R27's §1/§2 tables say "(739)" — that is the mid-round OV-05 reading, not the final; the correct figure is 748.**

Static cross-check (this sandbox, no install): 665 `it(`/`test(` sites + loop-generated parametrized families (14 `for..of` loops in bridge-seams alone; `m2-wave1` generates its grids) — 748 is consistent with the static shape. Key suites now: **bridge-seams 151** (was 113 at R24), **nle-bridge 99** (spec 20 still says 96), **m2-wave1-mixer-surface 9**, upstream-ports 25. The D9 API freeze is **455 names, still unmoved** (`api-surface.frozen.ts` last touched `c6dc8f5`, pre-range; python count = 455 — matches spec 19:136).

### 1.4 Consumer pins at HEAD (the lockset)

- engine→OT: **`6e2b91a`** (was `c15a629` at the R26 spec row) — `git submodule status`
- engine→WDC: **`ec8fd5c`** (was `494f6ff`) — idem
- OT imports in engine src: **5 files** — 4 type-only (av-link, transition-inputs, scene-to-segments, composition-frame) + **2 VALUE deep-imports in `opencut-laws.ts`** (`core/audio-params`, `ops/retime` — leaves only, never the barrel; the OV-01/02 sanctioned convergence)

---

## §2 The current law state (as the engine's own docs state it)

### D12 — the assembly ruling, RATIFIED engine-side (`.agents/DECISIONS.md:521-584`, landed `1064f91`)
opencut-timeline is **THE editing engine** (SceneTracks SSOT + spec-15 wire dispatch). The engine's freecut-lineage stack (`timeline/timeline.ts` + `core/types.ts` TimelineData + JSON-RPC wire + Player/scene-assembly/gpu-compositor/export over TimelineData) is **the RENDER/EXPORT venue model** — venue-internal state like an IR, NOT a second user timeline. **The junction is ONE-DIRECTIONAL**: `SceneTracks → (bridge: scene-to-segments / transition-inputs / composition-frame) → the venue stack`; edits never flow back. Engine-native edit verbs (overlap clamps, split bookkeeping, sync-lock ripple, E1/E2/F6 relink) are **FROZEN as venue-internal**. The engine Timeline is NOT deleted: offline reference + the parity twin that pins r1.

### D7 — volume in dB, amended (`.agents/DECISIONS.md:139-153`)
The volume dB domain is **[−60, +20] at every implementation twin** (opencut `core/audio-params` clampDb family; the engine bridge's flattener `clampKeyDb`/`clampVolumeDb`; the native fold twin `keyGainLinearNative`) — non-finite → unity; authored +40 renders at +20. The old [−60, +12] text was the freecut-era transcription; no twin implements it. The nle-ui Inspector ceiling was a **deliberate UX SUBSET** (authoring ⊂ model, the same lattice as Speed [0.1,4] ⊂ [0.01,5]) — **note: nle-ui's AW1-2 (`32abd58`) has since raised the ceiling +4 → +20, so the subset clause is now historical; authoring == model domain** (nle-ui scout's row).

### D11 — the retime-domain lattice (`.agents/DECISIONS.md:474-519`)
Four domains, four venues: **[0.01, 5]** opencut `ops/retime` (the WIRED SSOT; bridge replicates via `opencut-laws` — now VALUE-IMPORTED per OV-01) · **[0.1, 4]** nle-ui Inspector (AUTHORING SUBSET) · **[0.1, 16]** engine-native `core/timeline-math.ts` (the OTHER timeline model — test-runner/headless/persistence venues only, NOT on the wired path) · **[1/32, 32]** WDC `varispeed.ts` (the audio DSP domain). Plus the documented **dead zone [0.01, 1/32)** (opencut-legal, WSOLA-illegal → maintainPitch takes the legacy pitch-affected fallback with the loud warn). Authoring path: Inspector [0.1,4] → opencut DERIVE clamp [0.01,5] → flattener/composition-frame at the SAME clamped rate → adapter WSOLA domain.

### The NS-1..NS-5 design queue (`.agents/PLAN.md:463-485`, filed from opencut's SA-1)
- **NS-1** transport/render-clock coupling (who is master during playback; the segmentSignature/reschedule glue is app-owned and undocumented)
- **NS-2** real-media peaks for the timeline UI (decode → per-clip peaks + cache + invalidation; the wdc waveform contract is the named shape)
- **NS-3** clip-frame thumbnail queries ((mediaId, sourceSec) → drawable; today only composition-frame's Canvas2D resolveMedia)
- **NS-4** SceneTracks→GPU render path (the r1-port actionable core: a SceneTracks→render-plan adapter or a new compositor input contract)
- **NS-5** edit→A/V reschedule classification (which edits need strip re-materialization vs reschedule vs nothing)
- plus **D-ARCH-6 absorption** (opencut's insertBatch + batch keyframe verbs + registry shrink 5→3 + the app's pool switch)

### The r1-port collision map (`gaps/audit/ra-round/audit-AW1.md` §3, `:193-204`)
| Engine-native law | opencut analog today | Port action |
|---|---|---|
| E1/E2 linked-companion + ≥2-splits relink | **NONE** (no linkage field; app dispatch pairwise) | add a linkage model OR keep dispatch-level + port relink policy — **the biggest hole (R7)** |
| E3 INVALID_PARAMS contract | wire validates; ops return booleans | re-surface as the port's error contract |
| F5 mode-clamp family | no mode-clamp twins | port the clamps with the ops |
| N3 locked-track guards (36 ops) | `track.locked` exists; op guards partial | port op-by-op |
| clampSpeed [0.1,16] / DEFAULT_TIMELINE_FPS 30 / frames vs clampRetimeRate [0.01,5] / MediaTime / FrameRate | — | **domain lattice decision — one constants module** |
| sync-lock groups | NONE | op family or bridge-side expansion |
| Headless JSON-RPC | spec-15 wire | **ONE wire: spec-15 wins** |

End-state: opencut stays THE model; the engine shrinks to effects + media + export + the bridge (the G-layer); WDC stays a pure library; the app+ui remain the consumer pair.

---

## §3 The spec-impact register

| spec §site | Current text (quote) | New truth (evidence) | Sev | Proposed amendment |
|---|---|---|---|---|
| **20 §0 GAP (line 20)** + **20 §7 (:145)** | "`collectSidechainWires` exists WDC-side (`channel-strip.ts:63`; R24 attribution fix: … **nle-engine wires NOTHING today** …; **the engine's sidechain wiring does not exist yet**)" | **FALSE.** M2 Wave 1 (`50b91f5`) landed the engine-side wiring: `collectSidechainWires` is consumed INSIDE `materializeStrips` (`scene-mixer.ts:222-225`), after `applySettings`, with the per-wire IDENTITY-DIFF sever law (`:314-339`); both consumption paths (realtime monitor + `renderOffline`) build through it — "the duck the user hears is the duck the user exports" (previously the offline path NEVER wired sidechains — a ducked compressor was silently inert on export) | **P1** | Rewrite the §7 M2 row + the §0 GAP row: the engine-side sidechain surface LANDED (Wave 1); the remainder is Waves 2+ (the app's ducking UI sidecar, the EQ mapping decision — the wdc HANDOFF queue). Fold the F-2 parity law into §5's bridge laws |
| **20 §4.2 (:102) + §7 (:147) + §0 (:22)** | per-track G fields (fader/pan/mute/solo/inserts/sends/outputBus); "today: `bridgeSceneSettings` materializes unity faders + solo only, §7's honest gap"; "no insert chains, no aux sends, no reverb are materialized from the NLE data model" | **Partially FALSE + a landed law missing.** F-1: bus return gains now write through upstream `applyAuxBusGain` (`muted ? 0 : clamp(volume, 0, 1)` — one law, three consumers; `scene-mixer.ts:277-280`); `AuxBusSettings` gains the additive-optional `muted` field (`mixer-track-model.ts:89-102`); the raw `gain.value = volume` write is retired. The G-surface statement is now stale for BUS settings (per-track inserts/sends remain Waves 2+) | **P1** | Add the bus-settings shape (index/volume/effects/muted?) + the F-1 return-gain law to §4.2/§7; re-scope the "unity faders + solo only" honesty note to per-track inserts/sends only |
| **07 §6.1A R23 note (:367)** + **07's N1 law** | "both engine seams (N3 audio + N1 visual) derive it from the element-anchored sidecar" — no precedence statement, no hygiene law | **Landed law missing.** OV-05 (`9850231`): the ENGINE field `el.transitionOut` is the BASE input (type discriminant `"crossfade"`; duration ticks→seconds via the ticks law; finite/positive — `transitionInputsFromScene`'s exact hygiene, `composition-frame.ts:301-317`), the consumer sidecar is the OVERRIDE (`:334`). Mirror lag can no longer split preview/audio. App-side residual registered (the app still supplies the legacy bag; dropping it removes the last projection surface — engine PLAN P3 register `:488-491`) | **P1** | Amend §6.1A's note: state the one-input-source law (BASE = engine field, OVERRIDE = consumer sidecar) + the hygiene discriminant + the shared-window/A-V-floor cross-ref; register the app-side sidecar-drop residual |
| **18 §4.4 (:185, :189)** | "Volume %, pan (−100..100)…"; "'Gain dB' is withdrawn — no dB field exists in 09 and dB conversion is display-side only" | **FALSE.** The live Inspector's audio tab is a **Gain dB rail [−60, +20]** (nle-ui `Inspector.tsx:998-1008`: min −60, max +20, unit " dB", the D28-A2 true-dB law 20·log10 / 10^(dB/20) over the store's linear `volume`); nle-ui `32abd58` (AW1-2) raised the ceiling +4 → +20 "the fleet-coherent [−60,+20] domain" | **P1** | Restore the Gain dB row with the domain [−60, +20] and the D28-A2 conversion law (store linear ⇄ dB display); align :185's "Volume %" to the dB presentation |
| **09 §3.1 ElementJSON (:173-178)** + the volume-domain coherence row (09/20/02) | "`volume: number` // unit is LINEAR 0..1 as persisted; the shell displays dB"; `preservePitch?: boolean` with no default law. **No [−60,+20] domain stated anywhere in 00-20** (the only hits are freecut-citation rows 09:1942/:2233) | The live model is a **two-representation law**: the UI store holds LINEAR volume (floor 0.001 ≈ −60 dB, ceiling 10 = +20 dB), the OT element holds **`params.volume` in dB** (the D28-A2 convention — `scene-to-segments.ts:164-170`), the engine flattener clamps [−60,+20] with NaN→unity via the upstream ONE home (`opencut-laws.ts:73-79`; `scene-to-segments.ts:506-511`). "0..1" is false as stated | **P1** | Add the volume-domain law to 09 (model domain [−60,+20]; the linear⇄dB representation map; NaN→unity; absent=0 dB), a coherence row in 20 §4.1's `volumeDb` entry, and 18's authoring rail; name opencut `core/audio-params` as the ONE home (SSOT), engine bridge deep-imports |
| **06 §11.7 (:2695-2700)** | "Rate bounds — DIFFERENT between FreeCut and OpenCut… Adopt OpenCut's bounds (0.01–5.0) for the retime module. FreeCut's wider bounds (0.1–16) are for rate-stretch only." | The law matured into **D11's four-domain lattice** (opencut [0.01,5] wired SSOT · nle-ui [0.1,4] authoring subset · engine-native [0.1,16] venue-internal · WDC [1/32,32] DSP) + the dead zone [0.01, 1/32) + the authoring path (DECISIONS:474-519). §5.9F (:1668) carries a three-domain-intersection framing; the nle-ui subset venue is stated NOWHERE | **P2** | Upgrade §11.7 to the D11 lattice table (four domains, four venues + the dead zone + the authoring path); cross-ref §5.9F (FF-2's registered cross-note); add the nle-ui authoring-subset venue |
| **06 §0 engine row (:34) + §10.4's algorithm sources** | "nle-engine @ `5036387` — 458/458 vitest… timeline.ts 7,502 LOC: `slip` :4143 / `slideItem` :4246 / `rollingTrimItems` :2984 / `rateStretchItem` :3155 / `freezeFrameAtPosition` :7185 / `removeRangesFromClip` :6910" | timeline.ts is now **8,997 LOC** (+1,387/−110 over the range): slip **:4956** / slideItem **:5097** / rollingTrimItems **:3324** / rateStretchItem **:3840** / freezeFrameAtPosition **:8652** / removeRangesFromClip **:8235** (rg over the file at HEAD). ~20 further `timeline.ts:NNNN` citations across 00-20 (e.g. 06:1619 `timeline.ts:4513-4515`; 06:2579 `:4702`; 05:1280 `rateStretchItem :3404-3408`) all moved | **P2** | The round's mechanical re-pin batch: re-verify/re-point every engine line-number citation at `f9ac806` (code anchor `50b91f5`); §10.4's six algorithm-source rows above are the highest-traffic |
| **00 §0 engine row (:16) + the fleet pin rows (:3)** | "@ `0a49286` (R26…) **471/471** (458 + 13)…; consumes OT @ `c15a629` (UNMOVED since the R24 re-pin) + WDC @ `494f6ff`" | Pin → **`f9ac806`**, tests → **748/748**, consumer pins → OT **`6e2b91a`** + WDC **`ec8fd5c`**. "458/458" appears ~48× across 18 spec files; "471/471" ×5; "440/440" ×12 — all stale | **P2** | The fleet re-pin (already the orchestrator's §1 mechanical work; battery_r26's 5 pin-lag fails) |
| **19 §3.1 vendored-submodules row (:137)** | "OT is TYPE-ONLY in src/ (4 sites, all `import type`)" | **Stale.** 5 files import OT: 4 type-only + **2 VALUE deep-imports** in `opencut-laws.ts` (`core/audio-params`, `ops/retime` — leaves only, never the barrel; OV-01/OV-02's sanctioned convergence, the zero-runtime-dependency law amended) | **P2** | Amend the row: type-only ×4 + the opencut-laws leaf VALUE-imports (the one-home law); note the vitest regex subpath alias + the OV-12 CI gate |
| **19 §3.1 projector row (:139) + 00 D16 law 1 (:374)** | "Projector (D16) | **NOT LANDED** — `src/lib/nle/projector/` does not exist"; D16: "The projector is ENGINE-home (`nle-engine/src/lib/nle/projector/`…)" | **Misleading now.** The projector DUTY is discharged by the bridge family (`src/lib/nle/bridge/`: scene-to-segments + composition-frame + transition-inputs — the SceneTracks→ingestion translation, twin-pinned), under a different path than D16's illustrative `projector/`; D12 ratifies exactly this junction | **P2** | Amend D16 law 1's path to `src/lib/nle/bridge/` (or mark the path illustrative) + flip 19's row to LANDED-as-bridge (the parity corpus already lives in spec 04's N1 rows) |
| **00 D12 clauses / 06 §10.5 (:2623)** | Decision 12.3: "one algorithm home… port-scheduled into it… the EDITING wire never routes through it"; D16 law 1: "editing never reads back from engine state" | **Consistent with engine D12** (one-directional junction; venue-internal freeze). What's new engine-side: the freeze is now bilateral law ("nobody fixes one side expecting the other to follow"), the TimelineData stack is typed as the render/export venue model, and the cross-repo ruling has BOTH signatures | **P3** | Add a dated ratification note to D12/D16 recording the engine-side D12 (both signatures) + the venue-internal freeze clause; no semantic change needed |
| **00/03/05/06/18 (no site)** | The NS-1..NS-5 seams and the r1-port collision map appear NOWHERE in the spec corpus (grep: no `NS-1`…`NS-5`, no "collision map", no linkage-model statement outside 06's r1 rows) | Five seams + the collision map are the design queue BOTH repo-sides filed (engine PLAN:463-485; AW-1 §3). The plan's r1 row (IMPLEMENTATION-PLAN:73) carries wave 1/2 families + C7 but NOT the linkage-model hole, the constants-module decision, the spec-15-wins wire convergence, or NS-4's SceneTracks→GPU input contract | **P2** | Register the design queue: NS-1→03 (transport/clock coupling), NS-2→20/02 (peaks; rides K3), NS-3→02 (thumbnails), NS-4→04/07 (the SceneTracks→GPU input contract — the r1-entry architecture decision), NS-5→03/20 (reschedule classification); fold the collision map into IMPLEMENTATION-PLAN's r1 entry (or a new 06 §10.6) |
| **20 §0 engine row (:16)** | "nle-bridge.test.ts still 96" | nle-bridge.test.ts is now **99** tests (static count) | **P3** | Count refresh with the re-pin |
| **20 §10 N2b facet (:183) + §4.1** | "the dB-domain law exact (linear-in-dB ≡ exponential-in-linear, maxDiff < 0.002)" | RC-V1 registered: the flattener ramps LINEARLY between bezier keys (boundary-eval parity with upstream's "bezier treated as linear") while the UI envelope renders the true curve — mid-segment audio can deviate from the drawn envelope (not reachable from the AR-2 default set; rides the channels-round). Plus the `conversions.ts` `dbToLinear` replica survives as a conscious decline (venue-internal per D12) | **P3** | Add the two RC-V1 residual notes to the N2b row (registered, not blocking) |
| **12/17/19 (no site)** | No mention of the OV-12 re-pin gate | Engine CI now runs the vendored tree's OWN suite when the gitlink moves (ci.yml:216-253) — a pin can never land a red vendored tree | **P3** | Register the mechanism in 19's vendored-submodules row (or 17's verification ladder) as the engine's pin-integrity gate |
| **09 §3.1 (:177) preservePitch field** | "`preservePitch?: boolean` // retime keeps pitch (18 §4.4's preserve-pitch toggle)" — no default, no mapping | R9-b landed: **ABSENT = true** (the NLE default), explicit false = pitch-affected; the app bridges `el.preservePitch` ⇄ `retime.maintainPitch` both directions (app `sceneBridge.ts:229/262/436-447`); the engine threads `el.retime?.maintainPitch === true` → `source.maintainPitch` (`scene-to-segments.ts:596/623`) | **P2** | State the absent-≡-true default + the ⇄ mapping in 09 (the R26-filed model decision — resolved) |

**Counts: P1 = 5 · P2 = 8 · P3 = 4.** (P1s are one coherent cluster: the M2 Wave 1 audio laws + the volume-domain package + the OV-05 input-source law.)

---

## §4 Open questions for the orchestrator

1. **The 748 figure.** ARCH-R27 §1/§2 record engine tests as "(739)" — that is the mid-round OV-05 reading. The gates lines at both `c01a187` (worklog:5806) and `480a216` say **748/748**. The orchestrator's §2 pin-world row and battery_r27 should use 748 (and the W3 fleet's per-spec re-pins should quote it, not re-derive).
2. **Pin choice.** I propose **`f9ac806`** (fleet-HEAD coherence; the app already vendors it; docs-only delta over the code). If the spec prefers a "code pin" discipline (as OT's `c15a629` convention), the code anchor is **`50b91f5`** (src/tests) / **`480a216`** (tsconfig+vendor). One pin should be named NOW to avoid the fleet quoting three different anchors.
3. **D11's nle-ui authoring subset row ([0.1, 4])** — the engine's D11/D7 lattice docs still describe the Inspector ceiling as +4 dB (the subset), but nle-ui `32abd58` raised it to +20 (authoring == model). Spec-side this SIMPLIFIES the lattice (one less venue to document), but the engine's own DECISIONS text needs the same refresh — cross-ref the nle-ui scout before amending 06/09/18.
4. **Where does the NS queue get its spec home?** My §3 proposal assigns NS-4 (SceneTracks→GPU input contract) to 04+07 — but it is genuinely an ARCH-level decision (it IS the r1 port's shape). Does the orchestrator want it as a D37+ ruling in 00 instead, with 04/07 carrying only the contract rows?
5. **The volume-domain package spans four specs (09/18/20/02).** One amendment set or per-spec W3 dispatches? The one-home SSOT statement (opencut `core/audio-params`, engine bridge deep-imports) belongs in 20; 09 gets the model law; 18 the authoring rail. Recommend one cross-spec author to keep the three statements byte-coherent.
6. **Spec 06's §10.4 line numbers will rot again at every engine commit.** Worth considering a pin-relative citation convention ("at `<pin>`: slip@timeline.ts:4956") so the W6 verify pass can grep-and-check mechanically (battery-able) — the current prose buries pins mid-sentence.
7. **The OT-side wire census (30→31 names, 28 routed + 3 exceptions) and D-ARCH-6** are the OT/app scouts' rows; the engine-side consequence is only the OV-12 gate + the live-registry consumption law (app `8a3eb35`). I did not file spec-15 rows for them — confirm the OT scout owns those.
