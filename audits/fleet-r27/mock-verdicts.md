# R27-W2 — DESIGN-R25 §6 verdicts absorption (mock-verdicts)

**Agent:** mock-verdicts (research/report-only; R27 final-tightness audit, Wave 2).
**Date:** 2026-09-13. **Source doc:** `nle-core-spec/.agents/design/r25-reaction-round.md` (v2-FINAL, "BINDING for the R25 waves", r25:3) — the sibling stream's reaction-round binding doc. **Method:** the doc read in full; every verdict cross-referenced against the spec corpus (18/08/05/16/09 cited by line) and `REFERENCE-REGISTER.md`; the R24 audit nets (`r24-audit-round.md` §2) consulted for lineage. **Classification scheme (per the task):** SPEC-LAW-NEEDED (new § + wording proposed) · CONTRADICTION (P1-class, both sides cited) · REGISTER-ONLY (a REFERENCE-REGISTER row update) · NO-ACTION (mock-internal).

**The headline:** the verdicts mostly land on ground the R25 register already owns — W1's insert-mode verifications and all of W2's trim conformance are the mock conforming to OUR law (05 §8A + 18 §4.3, landed R25). The NEW law the corpus must absorb clusters in exactly three places the register never reached: **the source-viewer transport (A1)**, **the color layout's frame/trigger law (A2)**, and **the wheels' input model (A3)**. Three P1-class contradictions with current spec prose exist: stills' source behavior (18 §4.3), the wheels' absolute-puck model (08 §4.3), and the node-graph viewer-swap row (08 §0's W-B text, stale since R24).

---

## §1 The verdicts extracted (the doc's own words)

### 1.0 The live-probe record (r25 §0, :15-46) — the three root causes

- **Insert previews work; the BAR was the defect (flex starvation).** "Insert previews WORK mechanically — all 7 mode hovers arm the ghost + mode badge + displaced ghosts + down-arrow + right-arrows (the full `timeline_edit_modes (2).html` grammar is present)" (r25:18-20). "**THE DEFECT** … flex starvation of the edit bar. `shell-viewer-transport` (source mode) = [edit-bar wrapper (`flex-1 min-w-0`)] + [trim-controls 72px `shrink-0`] + [duration readout 178px `shrink-0`]. Measured bar widths by canvas: **≤900px → 0px (buttons INVISIBLE)**; 1100 → 174px (≈1.5 buttons)… The reviewer's review canvas (annotakit drawer open) sits in the 700–1100 band → they SAW a 0–2-button bar" (r25:23-31). "The W5b `overflow-x-auto` one-row fix was INSUFFICIENT… **The row needs PRIORITY-based responsive degradation, not overflow.**" (r25:31-33). Lineage: F1's P1 "SourceEditBar wraps to 2 rows (46px) inside the FIXED 32px transport row at the 1280×800 floor" (r24:205-208).
- **The I/O crop works mechanically** ("drag the IN handle → range band + readout update; the planner carries `ctx.sourceRange`" — r25:34-36) "but there is NO play control in source mode and NO crop feedback ON THE POSTER (the range exists only in a 16px bar)" (r25:36-38). Lineage: F1's P2 "source trim-in/out buttons are no-ops that render enabled with a tip… violates the honest-control law" (r24:210-212).
- **Wheels today:** "the 2D puck = balance (hue by direction, amount by distance), ONE setGrade per release. What the reference grammar ADDS (Resolve): the outer RING = the wheel's master luma, fine-drag modifier, dbl-click reset — research A3 verifies before the W3 ruling" (r25:39-42).

### 1.1 A1 — the source viewer (r25 §6, :180-191, binding; feeds W1)

> "Resolve treats STILL sources as normal clips (default 5s) with the FULL transport — play 'plays' the frozen frame; the honest mock is a moving playhead + running TC over the poster. I/O marks = bracket flags ON THE SCRUB STRIP + out-of-range dimming ON THE STRIP (never on the image); duration readout switches to the range. Transport row: left = TC/jog · center = go-to-start / step / play / step / go-to-end / loop · right = Mark In / Mark Out. W1-B adopts: 5s-pseudo-duration playhead scrub + play, I/O flags + strip dimming, the range-driven readout; the SourceRangeBar merges into a real scrub strip (playhead + flags + dimming). The 7 edit-mode buttons stay in the row (Premiere's Source-Monitor placement) behind a clear divider, as the PRIORITY cluster." (r25:180-191)

### 1.2 A2 — the color layout (r25 §6, :192-200, binding; feeds W3)

> "scopes react to the PLAYHEAD FRAME (always-on analysis, not selection) → the console-row TAB ruling is CONFIRMED (beside the node graph; the under-viewer pane RETIRES). Target grammar: a 3-chip breadcrumb header on the inspector — `[clip name · track] ▸ [Clip grade | Timeline grade] ▸ [Node n · label]` — with the node chip clickable to focus the graph; the level segmented control mirrors the node-graph header. Timeline-grade copy: 'applies to every clip in this timeline, after clip grades' (never 'one track' — Resolve has no track grade). Orange-dot affordance on inspector tabs holding adjustments in the current node." (r25:192-200)

Placement lineage: R23 W-B put scopes "in the timeline-area console row next to the MixerDock" and the node graph in "the nodes-in-viewer surface… the AppShell swaps the viewer for the graph" (08:16); R24's A2 re-homed nodes to "the F6 console row, slot [6]… NEVER the viewer swap (kills the R23-WB stale-frame deviation)" (r24:116-117) and put scopes "an ~160px pane UNDER the viewer" (r24:122); R25 A2 retires the under-viewer pane and confirms the console-row TAB (r25:136-137: "the color console row = [Timeline | Nodes | Scopes] thin tabs; the under-viewer scopes pane RETIRES — the R24-#68 placement superseded by the reviewer's latest").

### 1.3 A3 — the wheels (r25 §6, :202-213, binding; feeds W3)

> "disc drags are RELATIVE/ACCUMULATING (trackball-style: grab anywhere, `v += Δpointer`, clamp at rim) with LIVE preview + live YRGB readouts — our absolute-vector + commit-on-release model is WRONG and gets rewritten. Center = neutral; crossing = complementary hue (continuous, optional ≤3% dead zone). Master luma = a HORIZONTAL DIAL below the wheel (left darker / right lighter, YRGB moves together) + Ctrl/Cmd+drag inside the disc = master adjust (Resolve's documented behavior) + Shift+drag = absolute jump. Dbl-click disc = color-only reset; per-wheel corner button = color+master reset. 4 YRGB numeric fields per wheel, live, editable (dbl-click-to-type, ↑/↓ nudge, drag-to-scrub). Hue ring: vectorscope orientation (red upper-left, 60° spacing). One undo entry per gesture (the live preview commits once)." (r25:202-213)

The current mock model it kills: "commit on pointer-up — ONE undoable setGrade per drag, the D3 law" (`WheelsPanel.tsx:12-14`) and "setGrade({hueField, amountField}) on pointer-up (the D3 gesture law)" (`WheelsPanel.tsx:148`).

### 1.4 The wave contracts (r25 §3, :112-145)

- **W1 (insert/edit rescue)**: "(a) restructure the source-mode transport row: the edit bar is the PRIORITY consumer (flex basis + shrink rules), the duration readout + trim controls degrade first (readout drops below ~360px free width; trim icons keep), mode buttons go icon-only below ~140px-per-button availability — **7 buttons ALWAYS visible at every width ≥ the shell's min** (labels in data-tips/aria-labels)" (r25:114-119); "(b) the source viewer transport: play/pause + step ±1 + go-to-start + go-to-end + a source PLAYHEAD that scrubs the poster within the I/O range (rAF play = honest moving playhead over the still; **the loop law: play stops at out; J/K/L join the program-mode grammar**)" (r25:119-124); "(c) poster crop feedback: the region outside [in,out] dims on the poster itself + the handles' time readouts" (r25:124-125 — see OQ-1); "(d) … NEW pins: bar ≥7 visible buttons at 820px canvas; play button works; range affects the poster dim; the insert still commits" (r25:125-127). Insert-mode verification targets (r25 §2.2, :96-110): replace's same-length law "verify + pin the same-length law live"; fit-to-fill "verify the badge math".
- **W2 (trim conformance)**: "per §2.1 — probe each mode, implement missing affordances (the slip full-source outline + bright in-preview; the slide neighbor shrink boxes; roll/ripple arrows + green edges as cursor-follow affordances during the gesture), pin each" (r25:128-131). The §2.1 cards (:76-94) name the four reference grammars (roll's both-seam green edges; ripple's dimmed downstream + push arrow; slip's white full-source outline + bright in-preview + red border/glow; slide's shrink boxes + direction arrows).
- **W3 (color, after A2+A3)**: "the wheels grammar (ring = luma, fine-drag, dbl-click reset — per A3's Resolve findings); the target-model clarity (R7–R10); the scopes → console-row TAB…; the Toolbar2 Scopes/Nodes buttons activate their tabs" (r25:132-137).
- **W4 (mixer/console)**: "the element-visibility toggles; the RsmRow hover jump fix (live repro first); the icon corrections; the timeline-toolbar mixer toggle REMOVAL (deletion-pinned); the responsive degradation ladder" (r25:138-140).
- **W5 (deliver)**: "the export summary → console-row panel; the custom JSON preset" (r25:141-142, from R18 "export summary → separate panel, not inspection" + R19 "add custom JSON format").
- **W6 (view-state + refresh)**: "per-page timeline view-state memory; the inspector refresh law; the per-kind compact modes" (r25:143-144, from R3/R4/R5 — compact-video / compact-audio / compact-all).

### 1.5 The audit-fleet grid (r25 §4, :147-169)

"After W1–W6 land + the runtime sync, dispatch the audit waves — each agent fresh-context, one AREA, read-everything + live-probe mandate, findings as P1/P2/P3 with file:line + repro" (r25:150-152). The grid: **Views (7)** Edit·Color·FX·Audio·Deliver·Media-pool·Viewer/source — **UI groups (12)** timeline core/gestures/toolbar+popovers/inspector family/mixer family/console-row+ docks/Toolbar2+AppDock/SourceEditBar+insert modes/SourceRangeBar+transport/color wheels-curves-qualifier/scopes+nodes/deliver family — **Aspects (8)** a11y/CSS layer hygiene/state-store invariants/undo-history seams/shortcut map coverage/test-quality/story truth/console-error sweep — **Visual net:** VLM sweep of all 124 stories (r25:154-166). "Triage law: every P1/P2 → a fix wave item with a pin; P3s → the residue register (PLAN)… The round ends when a full re-audit pass returns ≤P3 everywhere" (r25:167-169). Gates: "full vitest suite green (1740 baseline)… commit → push → bundle… after EVERY wave" (r25:171-176).

---

## §2 The absorption register

| # | verdict (source) | classification | the spec §site it touches (quote) | the proposed action | severity |
|---|---|---|---|---|---|
| V1 | **A1-a stills get FULL transport, default 5s pseudo-duration** (r25:181-182, 186) | **CONTRADICTION** (+ SPEC-LAW-NEEDED) | `18-ui-shell.md:176` (§4.3 SourceRangeBar row): "**stills keep the honest static band (no duration → no range)**" vs r25:181: "Resolve treats STILL sources as normal clips (default 5s) with the FULL transport". The mock's current code agrees with 18 (`SourceRangeBar.tsx:45`: "a still image (no duration) keeps the honest static band — no range to trim"). | Amend 18 §4.3's SourceRangeBar row: *"Stills are sources too: a still loads with a 5s pseudo-duration (Resolve's default), full transport, and a trimmable [0, 5s] range; the honest play is a moving playhead + running TC over the poster."* Companion: 09's stills caveat ("an image media's duration is a SYNTHETIC extent… an edit decision, not decoded truth" — 09:25) gains the pinned default: **5s, user-overridable**; 16 §3.4A's C46 gate ("a source with duration is loaded" — 16:261) gains the note that the pseudo-duration satisfies the gate. | **P1** |
| V2 | **A1-b I/O flags + strip dimming, never on the image** (r25:183-185) | **SPEC-LAW-NEEDED** | 18 §4.3's scrub-row richness (program viewer): "the scrubber renders the in/out + loop range as a shaded band" (18:172) — the source-side has no equivalent; 18:174's fallback source-preview says nothing about marks. | Add to 18 §4.3 (source-mode rows): *"Source I/O marks render as bracket flags on the scrub strip with out-of-range dimming ON THE STRIP — never on the image; the duration readout switches to the [in,out] range."* (Adopt A1's form; see OQ-1 for the R25-internal poster-dim conflict.) | P2 |
| V3 | **A1-c source transport row layout; the 7 edit buttons behind a divider as the PRIORITY cluster** (r25:185-191) | **SPEC-LAW-NEEDED** | 18:175 (SourceEditBar row): "mounted in the source preview's transport row" — no layout law; 18:173 (program transport-row): "center cluster = step-back, play/pause, step-fwd, jump-start, jump-end; right cluster = loop, mark-in, mark-out, add-marker" — the program row, not the source row. | Add to 18 §4.3: *"Source transport row: left = TC/jog; center = go-to-start / step / play / step / go-to-end / loop; right = Mark In / Mark Out; the 7 edit-mode buttons stay in the row behind a clear divider, as the PRIORITY cluster (Premiere's Source-Monitor placement)."* | P2 |
| V4 | **A1-d source playhead scrub + play-stops-at-out + JKL join the program grammar** (r25:119-124, 186-187) | **SPEC-LAW-NEEDED** | 18:174 (fallback source-preview): "it plays the raw asset via a plain `<video>` element swapped over the canvas" — silent on stills, on scrub, on stop-at-out; 16 §3.1's JKL rows are context "Always" (16:134-140) but never name the source viewer. | Extend 18 §4.3's source-preview rows: *"A source playhead scrubs the poster within the I/O range; play is the honest rAF moving playhead (a still 'plays' its frozen frame); playback stops at out; J/K/L drive the source playhead in source mode."* Optional one-line note in 16 §3.1. | P2 |
| V5 | **A1-e the SourceRangeBar merges into a real scrub strip** (r25:188) | **REGISTER-ONLY** | Register row 3 (`REFERENCE-REGISTER.md:25`): "Source in/out marks — SourceRangeBar, per-media `sourceRanges`, slider semantics, stills' honest static band". | Row 3's census description updates when their W1-B lands: the surface = scrub strip (playhead + I/O flags + dimming), the stills clause dies (per V1), mock home unchanged. The 18 §4.3 wording rides V1/V2. | P2 |
| V6 | **W1-a the priority-based responsive degradation law** (r25:114-119, root cause :23-33) | **SPEC-LAW-NEEDED** | 18:175 carries no responsive law; the nearest floor law is 18:119: "Minimum window 1280 × 800 — Below this, show a 'window too small' overlay rather than degrade" (see OQ-4). | Add to 18 §4.3's SourceEditBar row: *"Responsive priority, not overflow: the edit bar is the transport row's PRIORITY consumer; the duration readout and trim controls degrade first, mode buttons drop labels to icon-only before anything hides — all 7 buttons remain visible at every width ≥ the shell's min (labels live in data-tips/aria-labels). A horizontal-scroll bar is a defect, not a degradation."* | P2 |
| V7 | **W1-b insert-mode verifications** (replace same-length, fit-fill badge math, ghost in-view — r25:101-110) | **NO-ACTION** (law already carried) | 18:175: "**replace's ghost width == the target's width exactly** (the same-length swap tell…)"; 05 §8A speed-badge row: "**Precision law: ONE decimal** ('1.7x') — the shell-variants' two-decimal `toFixed(2)` is the registered divergence this row corrects" (05:626); ghost in-view = the R23-WD scroll-into-view (r25:108-110). | None — their W1 verifies conformance to existing R25 law. Register note only (row 1 pins re-key). | P3 |
| V8 | **W2 trim conformance** (r25:128-131 + §2.1 cards :76-94) | **NO-ACTION** (their W2 implements OUR register) | 05 §8A already carries every named affordance: `trim-edge-crisp` roll = "**BOTH facing edges of the junction pair**" (05:616); `white-box-family` slip outline + slide shrink boxes incl. D33.4's composite ruling (05:620); `in-point-preview-frame` "**BRIGHT**" (05:621); `cursor-glyph-ladder` "renders ONLY inside an active-gesture preview, never on hover" (05:622); `arrow-family` two-way/push/diverging/outward/down-arrow state machine (05:623); the green ruling (05:603). | None on content. Register note: row 14 + row 1 pin counts re-key after their W2; 05 §8A's interim-carrier citations (shell-variants `Timeline.tsx:1654-1670`) may gain new line pins. | P3 |
| V9 | **A2-a scopes react to the playhead frame (always-on, not selection)** (r25:192-193) | **SPEC-LAW-NEEDED** | 08 §11 (08:748-812) specifies scope math + the 10fps throttle but never the analyzed frame; 08:17 fixes only the data source ("the scopes plot THIS MOCK buffer; at r3 they must plot ENGINE data"). | Add to 08 §11 (a rider at §11.1 or §11.4): *"Scopes analyze the frame under the playhead — always-on analysis, never selection-driven; the visible scope follows the playhead in real time at the §11.4 throttle."* This is the r3 engine-binding's trigger law. | P2 |
| V10 | **A2-b the console-row TAB confirmed; the under-viewer pane retires; nodes never swap the viewer** (r25:136-137, 194-195) | **CONTRADICTION** (stale W-B text) | `08-color-grading.md:16` (§0 W-B row): "ColorNodeGraph (the nodes-in-viewer surface, D-B2 — **the AppShell swaps the viewer for the graph**; while it owns the region the dock honestly draws the LAST published frame, ruling 14)" and ScopesDock "in the timeline-area console row **next to the MixerDock**" vs r25:136-137: "the color console row = [Timeline \| Nodes \| Scopes] thin tabs; the under-viewer scopes pane RETIRES — the R24-#68 placement superseded" (+ r24:117: "NEVER the viewer swap (kills the R23-WB stale-frame deviation)"). | Amend 08 §0's W-B row to the final form: the color console row is a thin TAB strip [Timeline | Nodes | Scopes] — one space, the Toolbar2 Scopes/Nodes buttons activate their tabs; the viewer is never swapped for the graph (the R23 D-B2 viewer-swap text and ruling-14 stale-frame note retire); the R24 under-viewer scopes pane never lands (r24:122 superseded). 18:220's simplified single-column package page is untouched (the mock stays the run-era reference, per 08:927's scope note). | **P1** |
| V11 | **A2-c the 3-chip target breadcrumb + the timeline-grade copy** (r25:195-199) | **REGISTER-ONLY** (reference-class) | The color inspector's target model is 08 §0's W-B reference class (row 16; 08:16). The copy law aligns with the spec's existing model: the per-scene grade renders as "ProgramCanvas's FINAL PASS (compose-then-filter…)" (08:19) = "applies to every clip in this timeline, after clip grades". | Fold into register row 16's description when their W3 lands (the breadcrumb + the "never one track — Resolve has no track grade" clarification); a one-line 08 §0 W-B addition at the same edit if the color round wants it in-tree. | P3 |
| V12 | **A3-a relative/accumulating drags + live preview + live YRGB readouts** (r25:202-205) | **CONTRADICTION** (+ CONFIRMED half) | `08-color-grading.md:262-264` (§4.3 UI): "puck (drag to set hue+amount), master ring gauge (dock only), R/G/B value chips (dock only), thumb wheel for the scalar" with FreeCut's "`getHueAmountFromClient` pointer→hue/amount conversion" as the verified reference (08:273-274) — an ABSOLUTE pointer→value puck; and 08:956: "**We adopt FreeCut's UI as our reference**". vs r25:202-204: "disc drags are RELATIVE/ACCUMULATING (trackball-style: grab anywhere, `v += Δpointer`, clamp at rim)… our absolute-vector + commit-on-release model is WRONG". CONFIRMED half: the live-preview requirement is already spec law — 08:59 "Adjustments appear within 1 frame (~33ms)" + 08:970 "Drag a color wheel pointer. Assert grade updates within 33ms" — the mock's commit-on-release (`WheelsPanel.tsx:148`) violated 08:970; A3 restores conformance. | Amend 08 §4.3 with an input-model row: *"Wheel drags are relative and accumulating (trackball-style): grab anywhere, the value accumulates pointer deltas, clamped at the rim; live preview + live YRGB readouts during the drag (the §13 test-9 33ms law); center = neutral; crossing to the opposite side passes through complementary hue (continuous; an optional ≤3% dead zone). One undo entry per gesture (the live preview commits once)."* | **P1** |
| V13 | **A3-b master luma = horizontal dial + Ctrl/Cmd+drag + Shift+drag absolute jump** (r25:206-209) | **CONTRADICTION** (control form) | 08:262-264: "master ring gauge (dock only)… thumb wheel for the scalar (lift/gamma/gain/offset)" (FreeCut-derived forms) vs r25:206-209: "Master luma = a HORIZONTAL DIAL below the wheel (left darker / right lighter, YRGB moves together) + Ctrl/Cmd+drag inside the disc = master adjust (Resolve's documented behavior) + Shift+drag = absolute jump". | Amend 08 §4.3: the master-luma control form = a horizontal dial below the wheel; Ctrl/Cmd+drag inside the disc adjusts master (YRGB moves together); Shift+drag = the absolute jump escape hatch. The FreeCut ring-gauge/thumb-wheel forms retire from the reference. | P2 |
| V14 | **A3-c dbl-click disc = color-only reset; corner button = color+master** (r25:209-210) | **CONTRADICTION** (cross-spec tension) | `18-ui-shell.md:301` (§5A): "**Double-click resets**: sliders, knobs, and NumberFields reset to their spec 09 default on double-click — implemented as the same reset command as the inspector's per-group reset" (a blanket reset-to-default law) vs r25:209-210: "Dbl-click disc = color-only reset; per-wheel corner button = color+master reset". | Add the wheels exception: in 18 §5A ("double-click on a color wheel's disc resets the COLOR component only — the master/luma value survives; the per-wheel corner button is the full reset") or carry it in 08 §4.3 with a cross-ref from 18 §5A. | P2 |
| V15 | **A3-d 4 live editable YRGB fields; hue-ring vectorscope orientation** (r25:210-212) | **SPEC-LAW-NEEDED** | 08:262-264: "R/G/B value chips (dock only)" — read-only chips, no Y; no hue-ring orientation statement. | Amend 08 §4.3: *"Per wheel: 4 YRGB numeric fields, live and editable (dbl-click-to-type, ↑/↓ nudge, drag-to-scrub); the hue field's ring graticule uses vectorscope orientation (red upper-left, 60° spacing)."* The (hue, amount)+scalar data model (08 §16.A, :1075-1089) is COMPATIBLE — see OQ-3. | P3 |
| V16 | **A3-e one undo entry per gesture** (r25:212-213) | **NO-ACTION** (CONFIRMED) | Matches register row 13 (REFERENCE-REGISTER.md:35: "Undo history surface… 15 §6.2 strategy 2 + 06 §7") and the mock's own D3 law (`WheelsPanel.tsx:12-14`). | None. | P3 |
| V17 | **W4 mixer/console** (r25:138-140) | **REGISTER-ONLY** + NO-ACTION | R13's strip element toggles (FX sends/pan/meter) fold into C40's queue ("Strip chrome beyond the G-surface (record-arm/inserts/input rows)… QUEUED — 20's M2 mixer design round (r2)" — REFERENCE-REGISTER.md:67); R16's timeline-toolbar mixer toggle removal is mock-internal (18:387 already removed the spec's mixer panels; the 3-vs-5 page OPEN decision, register row 8 + :51, is untouched); R14/R15/R17 are mock-internal fixes. | Register: extend C40's queue note with the element-visibility toggles; no spec edit. | P3 |
| V18 | **W5 deliver** (r25:141-142) | **REGISTER-ONLY** (+ a decline note) | Register row 17 (REFERENCE-REGISTER.md:39): "the mock-only halves ride XMOCK-3's r5 deliver polish". The custom JSON preset has NO spec'd export target — 18:220's deliver = "FCPXML via `exportFCPXML`… optional cloud master via `exportMaster`, render settings, progress list" (specs 10/11). | Register: note both W5 halves on row 17 (the summary panel = mock placement detail; the custom JSON preset = mock-preview-only — a JSON export format would be a NEW 10/11 row if ever wanted; DECLINE-class absent a user ask). | P3 |
| V19 | **W6 view-state + refresh + per-kind compact** (r25:143-144) | **SPEC-LAW-NEEDED** (R4) + **REGISTER-ONLY** (R3/R5) | R4's refresh law: 18 §4.4 (18:178-198) has no refresh law (adjacent: C44 "Inspector empty-state → active-track fallback… QUEUED — owner S-spec 18 §4.4", REFERENCE-REGISTER.md:71). R3's per-page view-state memory extends C57 ("Per-track height overrides… QUEUED — owner S-spec 05 §12.2/18 §4.9… the mock's view-state answer — never snapshotted — is the reference", REFERENCE-REGISTER.md:84); R5's per-kind compact refines 05:751 "Collapsed tracks: 24px". | Add to 18 §4.4: *"Refresh law: on any view/editor-mode transition the inspector re-resolves its target entity; an unresolvable target shows the empty state, never stale data."* Register: fold R3 into C57's queue, R5 as a C57-adjacent note (both await the B3 height-home design round). | P2 (R4) / P3 (R3+R5) |

**Tally: 19 rows — 3 CONTRADICTION-class (V1, V10, V12) · 7 SPEC-LAW-NEEDED (V2-V4, V6, V9, V14 rider, V15, V19-R4) · 5 REGISTER-ONLY (V5, V11, V17, V18, V19-R3/R5) · 4 NO-ACTION (V7, V8, V16 + V17's mock-internal halves).** Note V12 carries a CONFIRMED half (the 33ms live-update law was already 08's; the mock was the violator).

---

## §3 The register worklist + the coordination note

### 3.1 The REFERENCE-REGISTER worklist (rows the verdicts touch)

| Row (REFERENCE-REGISTER.md line) | The touching verdicts | The update |
|---|---|---|
| **Row 3 — Source in/out marks** (:25) | V1, V2, V5 (A1) | The census description's "stills' honest static band" clause dies; the surface description becomes the scrub strip (playhead + I/O flags + dimming). Re-key after their W1-B lands. |
| **Row 2 — Source-preview chrome** (:24) | V3, V4, V6 (A1/W1) | The row's content extends: the source transport + the priority ladder land as 18 §4.3 rows (this round's amendments); the SourceEditBar mock home + pins unchanged until their W1 re-pins. |
| **Row 16 — Color surfaces** (:38) | V9, V10, V11, V12-V15 (A2/A3) | Update at their W3: the console-row TAB grammar, the playhead-frame trigger, the wheels rewrite. The row's owning-spec citation (08 §0 W-B) gains the amended rows. |
| **Row 15 — Transport** (:37) | V4 (A1) | Add the source-mode half (JKL + play-stops-at-out) once 18 §4.3's rows land. |
| **Rows 1 + 14 — edit modes / trim-edge grammar** (:23, :36) | V7, V8 (W1/W2) | Pin counts re-key after their W1/W2 waves (the census). |
| **Row 17 — Deliver** (:39) | V18 (W5) | Note the two mock-only halves (summary panel, custom JSON preset) on the XMOCK-3/r5 queue. |
| **Row 13 — Undo history** (:35) | V16 (A3-e) | No change (CONFIRMED). |
| **Row 8 — Page system** (:30, OPEN table :51) | V17 (W4) | No change (mock-internal; the 3-vs-5 OPEN decision stands). |
| **C-ledger** (:60-87) | V17, V19 | C40 gains the element-toggle note; C44 rides V19's 18 §4.4 law; C57 gains R3/R5. **New C-rows from their fleet sweep at their wrap (35.3 intake — zero untracked).** |
| **Suite-count pins** (:13) | all waves | The census re-key: today "1,733 it-blocks / 64 test files (their wrap declares 1,734)" (also ARCH-R27:32/38: 1,740 green, 124 stories). Their W1-W6 + fleet will move it again — **re-key only at their round-wrap declaration, never mid-flight** (35.2). |

### 3.2 The coordination note — their 50+ grid vs our R27 fleet

Their W7+ grid (r25:147-169: 7 views × 12 UI groups × 8 aspects + a VLM net over 124 stories) and our R27 fleet (ARCH-R27:42-50: W1 module scouts → W2 mock analysts → W3 per-spec 00-20 → W4 cross-cuts → W5 adversarial → W6 amendments + battery) **must not collide on the same surfaces**. Six rules keep them disjoint:

1. **Different trees, different HEADs.** Their fleet mutates `ui-mock/shell-variants` (the sibling stream's runtime, pinned at their HEAD `3db130f` → their wave commits); ours mutates only the spec corpus. Zero file overlap by construction — our W2 mock analysts (this report included) are READ-ONLY on their tree.
2. **Finding routing.** Their fleet's P1/P2/P3s are MOCK defects → their fix waves + their PLAN residue register (r25:167-169), never our spec defect register. Our fleet's spec findings cite mock surfaces only as evidence; law questions route to the REFERENCE-REGISTER + the C-ledger — the ONE intake point (35.3) prevents double-tracking.
3. **Register timing.** Their W1-W6 waves move it-block counts continuously; our 35.2 battery pins drift with every wave. The re-key belongs to our W4 xcut-register agent, keyed on their round-wrap declaration (register:13: "the sibling's next round-wrap declares the final figure"). Rows touched by in-flight waves carry a REGISTER-PENDING marker — **no premature LANDED flips** (the flip is the D26.4 retirement trigger's key; register:11).
4. **The K3/VLM tie runs in our favor.** "The register rows are the verification worklist: each DESIGN-REFERENCE/LAW-REGISTER row names what the crawl's VLM pass must see" (register:11) — their VLM net (r25:164-165) consumes OUR row descriptions as expectations. Our amendments must land (or at least be register-noted) before their fleet's VLM pass, else their net verifies against dead law (the stills row is the immediate example: the VLM should expect the 5s transport, not the static band).
5. **Convergence direction.** Where their verdicts are Resolve-researched (A1/A2/A3 — exactly the class the task names), the spec absorbs FIRST (this round), citing r25 §6, so their W1-B/W3 implement the amended spec rows: the mock stays the reference implementation of spec law, not a parallel authority. The reverse already happened once: their W2's trim targets ARE our 05 §8A (V8) — the healthy pattern.
6. **Sequencing hazard.** Their fleet dispatches only "After W1–W6 land + the runtime sync" (r25:149-150); our W3/W4/W6 run now. Our amendments must cite r25 §6 as the incoming reference (REGISTER-PENDING), not surfaces that don't exist yet.

---

## §4 Open questions

1. **The R25-internal poster-dim conflict (must be resolved before our V2 wording finalizes).** W1's contract (c): "poster crop feedback: the region outside [in,out] **dims on the poster itself**" + the gate pin "range affects the poster dim" (r25:124-125, :126-127) vs A1's verdict: "out-of-range dimming **ON THE STRIP (never on the image)**" (r25:183-184, the §6 binding text that itself says "W1-B adopts: … I/O flags + strip dimming" r25:186-187). Both are dated 2026-09-13. Proposed for the spec: A1's strip-only form (it is the Resolve-researched ruling and protects the colorist's view of the image); ask the sibling's W1 implementer to reconcile §3(c) with §6 before the wave gates pin "range affects the poster dim".
2. **Does the 5s pseudo-duration satisfy 16 §3.4A's C46 gate?** The gate reads "a source with duration is loaded" (16:261). Proposed: yes — the pseudo-duration IS the loaded source's duration for gate purposes (and it is what makes a still insertable at 5s, Resolve parity); needs the one-line note (V1) or an explicit 16 §3.4A rider.
3. **The wheels data model.** A3's "live YRGB readouts" over 08 §16.A's corrected FreeCut params — "(hue, amount) per wheel + scalar lift/gamma/gain/offset" (08:1075-1089). Read: compatible (the tint+master model maps onto (hue, amount)+scalar; the YRGB fields are derived displays), but the mapping should be STATED before r3 so the engine binding doesn't fork a second color model. Recommend the W2 mock-color (WebGPU feasibility) sibling report rules it; the V12/V15 amendments then cite the ruling.
4. **The 1280×800 floor.** 18:119: "Below this, show a 'window too small' overlay rather than degrade" — but the reviewer's live canvas sits at 700-1100px (r25:29-30) and the mock degrades responsively there. Does the spec's floor stand (making V6's ladder a within-floor guarantee, and the mock's sub-1280 behavior a registered deviation), or does the shell adopt responsive sub-floor behavior? Registered as an open decision for the W3 spec-18 analyst + the page-shape design round.
5. **The Timeline-grade target.** A2's breadcrumb exposes "Timeline grade" as a first-class inspector target with the copy "applies to every clip in this timeline, after clip grades" (r25:195-198) — semantically the spec's per-scene SceneGrade final pass (08:19), but the mock's `'timeline'` post-clip second pass is still "a README-registered deviation" (register:77, C50). Formalizing the target model (clip grade → timeline grade ordering, per-node state) needs 09-side rows — ride the A2 absorption's 08 §0 W-B edit or defer to the color round? Recommend: register row 16 note now, 09 rows at the color round.
6. **The custom JSON deliver preset.** Confirm DECLINE-class (no spec'd JSON export target exists — 18:220, specs 10/11) unless the user asks; the mock's preset stays row-17 mock-only.
7. **The census method divergence.** Their wrap declares 1,734 it-blocks "by their method"; our battery's line-start method reads 1,733 (register:13). After their R25 waves + fleet the figures move again — the W4 xcut-register agent should record BOTH the declared and the scraped figures at the re-key (the 35.2 amendment), as the register already does.

---

*Research-only agent: no files modified outside this report + the worklog; no commits, no pushes; stayed on main.*
