# R27-W3-10 — spec 10 (FCPXML export) — the per-section fleet audit

**Agent:** spec-10 · **Round:** R27 final-tightness, Wave 3 (per-spec fleet) · **Mode:** report-only
**Spec:** `10-fcpxml-export.md` (2,345 lines; last corpus edit = R24 `05b6755`/`faea627` — zero R25/R26/R27 edits, so every pin is R24-era)
**Ground truth re-derived live:** engine `f9ac806` (748/748, tsc 0), OT `55c81c0` (code pin `970948a`, 632/632), WDC `ec8fd5c`, nle-ui `32abd58` (690/690), app `c020b2a` (252/252); `IMPLEMENTATION-PLAN.md`, specs 00/01/04/09/15/16/17; the fleet reports (spec-09/spec-04/spec-15/spec-20 audits + mock-variants).

---

## §1 Per-section verdict table

| § | Section | Verdict | Evidence |
|---|---|---|---|
— header | Status (:4) | **P2** | "Round 24" is the last round note; the R24 pins (:12) are three rounds stale (engine `5036387` 458 → `f9ac806` 748; OT `ded43c4`/`c15a629` 536 → `55c81c0`/`970948a` 632; WDC `85b81b0` 759 → `ec8fd5c` 777; nle-ui `fc4cc35` 674 → `32abd58` 690; app `c885ece` 174 → `c020b2a` 252). The standard re-pin batch (FIX-3). |
| 0 | FORWARD INVENTORY | **P1 ×1 + P2 ×5** | **The P1 — the Z2 GAP row (:21) is FALSE now**: "the MEDIA half's export is currently UNGRADED — deliverService has ZERO grade/color terms at `c885ece`" — the app's Z2 LANDED: `deliverService.ts:300-324` @ `c020b2a` (verified in full: `buildGradeFilterString(grade)` from the store's per-scene color, scratch canvas → `ctx.filter` final pass, "the same final-pass law ProgramCanvas uses (what you grade is what you get)", Safari <18 no-op documented). The row's D29.5c decline note also carries spec-04's two stale clauses (Z2 landed; the engine's R9-c re-filed the seam-side proposal — spec-04 audit Row 4). P2s: (a) the app-half BASE sentence (:14) is missing the **M2 Wave 1 workletFlushMs venue law** (`deliverService.ts:252` `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0` + `WORKLET_BACKED_INSERT_TYPES` :170-172 — offline export flushes worklets now; the r5 dependency); (b) the OT row (:15) "the W11 wire-dispatch round changed no `SceneTracks`/serialization surface" is stale — D-HB2 landed since `c15a629` (`fromJSON` = the scene-load boundary: validateSceneForLoad first, typed rejected-whole errors, OT `timeline-core.ts:2433-2537` — spec-09 audit §4-B); (c) the variants' DeliverPage clause (:14/:24, "the R22 W5 grammar") is two waves stale (R24-A3 W4 `6b0bd91`: the unconditional ruler + RangeBand + `deliverViewStore` — mock-variants row 17); (d) the K4 row's "grep-verified at `c885ece`" → `c020b2a` (re-grepped: still ZERO e2e/playwright in the app); (e) the app row (:16) App.tsx:56 ✓ and `package.json:23` mediabunny 1.50.8 EXACT ✓ hold — only the pin hashes are stale. The seam-split STORY itself re-verifies: media half landed BOTH layers (engine `src/lib/nle/export/` 5 files/1,806 LOC; app deep-import `renderCompositionCore` @ deliverService.ts:35/:338), format half **zero-anywhere** (rg -ri fcpxml → engine 0, OT 0, WDC 0; app = the defensive throw :409-412 + its 2 seam tests `deliverService.test.ts:388-390`, `GluedShell.test.tsx:1571/:1601`; nle-ui = the mock rails, `DeliverPage.tsx:31-32/:127/:136/:369/:380`). |
| 0A | Refined-spec notes | SOUND | The structural claim (1–9 verbatim + 11–15 new) matches the file as it stands. |
| 1 | Purpose | SOUND | Export-only v1; the three-NLE handoff framing. (17's import-direction rows are the cross-spec tension — §4 below.) |
| 2 | Goals | SOUND + P3 | Goal 6 "Speed changes preserved … via `<timeMap>`" — the retime model now carries `preservePitch` (09:177; R9-b) which no goal/row mentions (FIX-8a). |
| 3 | What FCPXML supports | SOUND + P2 (rows-unwired) | The DTD shape (spine/lanes/asset-clip/transition/title) is internally consistent with §11/§13. P2: §3.3's mapping rows `srcEnable="video"` (video-only) and `<conform-rate>` (frame-rate conform) are documented but `buildAssetClip` (§4.4) never emits either — and `srcEnable` gains salience from 09's new `isSourceAudioEnabled` (the A/V-detach toggle — spec-09 audit field-inventory row; a video with source audio disabled exports exactly as `srcEnable="video"`). FIX-8d. |
| 4 | Mapping ProjectJSON→FCPXML | **P2 ×5** (model coherence) | (a) §4.3 `scene.tracks.main.elements.map(id => this.getElementById(ctx, id))` — the **N1-retired string[]-IDs reading** (09:139-140: "INLINE element records in time order — the string[]-IDs reading is retired"); (b) §4.4's volume sketch violates the volume-domain law (below, focus #2); (c) §4.4 never maps `el.preservePitch` (below, focus #4); (d) §4.5's `switch (transition.type)` cases 'wipe'/'slide'/'iris'/'glitch' read the **pre-S3 shape** — 09:237 `type: 'crossfade'` (structural only, spec 07 §6.1A) + `presentation` is the registry key (09:238; OT `types/index.ts:145-150` `ElementTransition {type:"crossfade"; duration; presentation?; alignment?}`); (e) §4.5's offset math `a.startTime + a.duration - transitionDuration` ignores `alignment` (09:240: 0..1, default 0.5 cut-centered — the formula assumes alignment=1); plus §4.7 reads `ctx.project.markers` — **contradicting its own Correction #10's A2 acknowledgment** (markers are PER SCENE, 09:115-116/:278-283; the 09 audit's Edit D-4 kills the project-level Zod line). |
| 4.8 | Time formatting | **P2** (the NTSC/1001 family) | Under 09/OT's FrameRate convention (`{numerator, denominator}` = the RATE; 29.97 = `{30000, 1001}` — 09:2600-2602, OT `frame-rate.ts:14` `FPS_29_97 = {30000, 1001}`): the discriminator `fps.numerator === 1001` (:605/:636) matches NO convention that also makes `ticksPerFrame` (:621) correct — for {30000,1001} the NTSC branches never fire and `formatOneFrame` emits `1/30000s` (should be `1001/30000s` per 10's OWN §11.4 table), `formatFrameDuration` emits `100/3000000s`, and `formatTime`'s `reduceFraction(frames, fps.numerator)` drops the 1001 factor (frame N at 29.97 = N·1001/30000 s, not N/30000 s). One frame = `denominator/numerator s`. 10's unique seam — see §5. |
| 5 | Source file paths | SOUND | Relative-path approach + the `.fcpxmld` bundle (§11.4 ✓); `storage.loadMedia`/`project.metadata.name` consistent with 09's ProjectJSON/MediaStorageRef. |
| 6 | FCPXML validation | SOUND | DTD-not-XSD strategy; the home is §14. |
| 7 | Resolve & Premiere | SOUND | Q3/Q4's DEFERRED posture consistent with §15's manual M2/M3. |
| 8 | Limitations | SOUND + P3 | The lossy list is coherent (grade/EQ/multicam/captions/keyframeAnimation-v2; §0's N2b `gainAutomation` KNOWN_LOSSY row ✓). P3: reverse (`speed: -1.0`, 09:170) is unmapped and unnamed in the lossy list (FIX-8c). |
| 9 | EDL bonus | SOUND | Deferred v2. |
| 10 | Open questions (Q1–Q11) | SOUND | The DTD-backed answers stand; Q5's "write our own Zod schema" adoption decision is the reference-parser state (focus #3). |
| 11 | Schema reference | SOUND | The DTD distillation is internally consistent with §3/§4/§13 (frameDuration table :1263-1272 ✓; §11.6 documents `preservesPitch=(0|1) "1"` — the DTD default that the §4.4 mapping should consume). |
| 12 | Code references | **P2 ×2** | §12.1 stale LOC counts: 00-master 466 → **863**; 09-project-model 2,379 → **3,102** (the spec-09-flagged staleness; 10's own seed 727 is historical-by-design). §12.8 has R23/R24 re-verification notes but **no R27 note**: engine `5036387` 458/458 → `f9ac806` **748/748**; zero-FCPXML STILL HOLDS (rg over src/+tests/ → 0); export subsystem 1,785 → **1,806 LOC** (orchestrator +35/−14: HW1-8 the pcm-s16 audio-only ladder `84c909d` + the F2-7 hostile folds `1c04887`); the `MediaMetadata` colorSpace ENGINE-GAP re-verified OPEN (metadata.ts:48-102 has no colorSpace/colorInfo/primaries anywhere); the R7-era line pins drifted but substance holds (re-anchored live: `speed?` types.ts:644, `volume?` :656, `markers?` :1373, `TrackKind` :1066, `presentation` :422, `CompositionItem` :1008). FIX-6. |
| 13 | Corrections to seed | SOUND | All 11 corrections DTD-cited; #10's A2 prose vs §4.7's code inconsistency booked under §4. |
| 14 | Browser validation | SOUND | Zod-browser + optional server `xmllint --dtdvalid`; the §6/§14/§15 strategy is one law. |
| 15 | Test plan (seed intent) | SOUND | 13 items; M1–M4 manual/gating consistent with 17 §12 (:2072-2075). |
| 16 | Testing (executable) | **P2 ×2 + P3** | (a) `with-retiming.json` (:2286-2287) + the Tier-1 retime test (:1979-1984) say "a clip with `retime: { speed: 2.0 }`" — **09's model has the DIRECT `speed` field** (09:170; OT projects it as `retime.rate` — the "speed no-longer-sidecar" item); (b) the Test-assets block's eight `tests/fixtures/projects/10/*.json` are **unregistered in 17 §5.3** (17:946-971 has no 10/* rows — the R7 finding, still open) + `simple-cut.json`/`multi-track.json` shadow 17's own fixtures with a DIFFERENT multi-track recipe (10: "3 tracks, 2 clips each" vs 17:957 "5 tracks, 10 clips"); the canonical media names DO exist (17 §5.1: `10s-red-1080p.mp4` :864, `10s-white-1080p-hdr-pq.mp4` :876 ✓). P3: the text-element test's "with `<text>` and `<text-style-def>` resources in `<resources>`" (:1986-1988) contradicts §4.6's own nesting (text-style-def is a child of `<title>`, DTD :522). The Tier-3 note (:2128-2136) verified ✓ — 15 §4.3.74 @ :2001-2050 with the canonical `format?`/`bundleMedia?` params + the determinism note for T3.2; the 78-member union carries the export trio (spec-15 audit §1 row 4.1: "78 members counted … incl. the export trio"). |

**Net: P1 ×1 (the Z2 row's false premise) · P2 ×13 (clustered into 7 fixes) · P3 ×4.** The DTD spine (§3/§10Q/§11/§13/§14), the validation strategy, and the r5 phase posture are SOUND.

---

## §2 The five focus items — evidence

**#1 The export seam split — still the truth, with two movements.** Re-grepped at the R27 pins: engine/OT/WDC = **0** FCPXML matches; app = the defensive throw (`deliverService.ts:409-412` "FCPXML export is not implemented (spec-10 future)") + 2 seam tests; nle-ui = mock rails (DeliverPage.tsx :127/:136/:250-253/:369/:380 — fcpxml default preset, "Bundle media with FCPXML", `shell-deliver-btn-export-fcpxml`, the future toast; useShortcuts.ts ⌘E toast). Media half landed at both layers ✓ (engine export 5 files/1,806 LOC; app deep-import @ deliverService.ts:35). **Landed since R24 that 10 doesn't carry:** (a) **Z2** @ `deliverService.ts:300-324` — the export-grade final pass (the P1); (b) **M2 Wave 1's workletFlushMs** @ `:241-253` — "the flush is > 0 whenever any worklet-backed insert rides a track … a worklet-backed compressor whose module load races startRendering falls back to PASSTHROUGH: the duck silently vanishes from the export" — 250-when-present, the app-reachable set compressor/gate/deesser/limiter (:170-172); the engine's own export paths pass 0 (`orchestrator.ts:670`, `audio-mixdown.ts:362` — the fast path; `scene-mixer.ts:444` `?? 50` default). (c) the engine's export hardening (+21 LOC since R24: HW1-8 pcm-s16 + F2-7 hostile folds). (d) F3 the blob-URL revoke (:100-120) — P3, noted in FIX-3's refresh.

**#2 The field inventory coherence (10's export shape vs 09's model).** Verified against 09 §3.1 (live schema) + OT types + the spec-09 audit's 20-row inventory:
- **transitionIn**: 10 never reads it ✓ (only `el.transitionOut` :328 — the surviving field; 09's audit retires `transitionIn` — no 10-side edit needed).
- **TransitionJSON pair-anchored**: 10's §4.5 keys the effect on `transition.type` with wipe/slide/iris/glitch cases — DEAD against the live shape (`type: 'crossfade'` structural-only + `presentation` registry key: 09:237-238, OT `ElementTransition` :145-150). The offset math also ignores `alignment` (default 0.5 cut-centered).
- **preservePitch (the new field)**: 09:177 `preservePitch?: boolean` (absent≡true, the R9-b ruling per the 09 audit's Edit 1) + the DTD's `<timeMap preservesPitch=(0|1) "1">` (:1147/:1310) — **10 documents the attribute but never maps the field**. The mapping is a default-on-default fit: emit `preservesPitch="0"` only on explicit `false`.
- **speed no-longer-sidecar**: 09:170 `speed: number` is a DIRECT ElementJSON field (OT's `retime.rate` is the engine projection) — 10's §4.4 reads `el.speed` ✓ BUT its own fixture/test say `retime: { speed: 2.0 }` (the sidecar shape) — internal incoherence.
- **volume**: 09:173 "LINEAR 0..1" (the R15 reading the 09 audit corrects to linear-gain/1=unity + [−60,+20] dB authoring + absent≡0dB + NaN→unity, one-home audio-params). 10's `gainToDb(el.volume)` = 20·log10 with `gain <= 0 → -96`: undefined (absent) hits the −96 floor — **violates absent≡unity**; no NaN→unity; no domain statement.
- **N1 inline elements** (09:139): 10's §4.3 still maps string[] ids.
- **A2 per-scene markers** (09:115): 10's §4.7 reads `ctx.project.markers` (its own Correction #10 says per-scene).
- **`el.transform` scaleX/scaleY/rotation/centerX/centerY** ✓ (09:201-208 — but flipX/flipY unmapped, P3); **`el.opacity`** ✓ (09:184); **`el.muted`/`audioFadeIn/Out`** ✓ (09:179-181); **settings.fps/canvasSize/displayMode/audioChannels/audioSampleRate + currentSceneId** ✓ (09:94-100/:74); **MediaRecord fields** ✓ (09:246-263); **MediaTime = 120,000 ticks/s** ✓ re-verified at OT (`media-time.ts` `TICKS_n = 120_000`).

**#3 The r5 phasing — correctly greenfield, artifact intact.** The plan's r5 row (`IMPLEMENTATION-PLAN.md:77`) still carries "FCPXML (greenfield; the parser+fixture-corpus choice is the phase-entry artifact)" verbatim ✓; spec 14 is the redirect tombstone (34 lines, D23) ✓; nothing landed that moves the phase (format half zero-anywhere, focus #1). Reference-parser decision state: **our-own-Zod-schema is DECIDED** (§10 Q5 + §14.1); server-side `xmllint --dtdvalid` decided (§14.4); what remains parked at entry = the third-party reference-parser question + the fixture-corpus shape (17 §5.3 registration rides it — FIX-7b).

**#4 The preservePitch/retime export — speed yes, pitch no.** Speed maps (§4.4 `buildTimeMap` :400-402, §11.6 patterns, §13 Correction #1 the timeScale debunk, Tier-1 retime test, §15 test 9). preservePitch: **no mapping anywhere** — only the DTD attribute inventory (:1147, :1310). Reverse (`speed: -1.0`, 09:170) is also unaddressed (not in §8.1's lossy list).

**#5 Pre-R26 pins/citations + cross-refs — 8 spot-verified:** (1) 15 §4.2:324 export row ✓ ("FCPXML is r5 greenfield" — the same split); (2) 15 §4.3.74:2001-2050 ✓ (format/bundleMedia canonical params; determinism note names T3.2); (3) 01 §14.11/ExportManager :373-376 ✓ ("ProjectManager copy removed Round 7"); (4) 16:391 ⌘E → `{type:'exportFCPXML', params:{format:'fcpxml-1.10', bundleMedia:false}}` ✓ + 16:397's attribution ✓ (the R7 1.10/1.11 reconciliation landed — the old version mismatch is closed); (5) 17 §13A :536-537 ✓ + §12 M1-M4 :2072-2075 ✓ + §5.1 canonical media :864/:876 ✓; (6) 04:24 the D29.5c decline row — present but with the two stale clauses (spec-04 audit Row 4; 10's Z2 row is the twin); (7) 09's §8.13/SCOUT-09 pointer (10:1419) — resolves ✓ (09:1734-1863 carries the formatColorSpaceTriplet pair; the LOC count 2379→3,102 is stale); (8) IMPLEMENTATION-PLAN r5 row ✓ + spec 14 tombstone ✓.

---

## §3 Fix-list — exact edits (mechanically applicable)

**FIX-1 (P1) — 10:21, the Z2 GAP row. Replace the premise clause** (from "the MEDIA half's export is currently UNGRADED" through "assert the painter ran the grade).") with:
> **CLOSED (the app's Z2 landing, re-verified R27 @ `c020b2a`):** the scene grade reaches the export as the CONSUMER-SIDE FINAL PASS — the same law ProgramCanvas's monitor uses: paint to a scratch canvas, then draw scratch→render with `ctx.filter = buildGradeFilterString(grade)` (`deliverService.ts:300-324`); ungraded scenes take the direct path (no scratch, no extra pass); Safari <18 no-ops the filter and the ungraded frame still exports. What you grade is what you get. The D29.5c engine-seam DECLINE stands (the compose-then-filter math law, engine `8a0b7fe`) — with the R27 note that the engine's R9-c re-filed the seam-side final-pass proposal (`paintCompositionFrame(…, { grade })`) as its possible evolution (04 §0's row is the twin). Orthogonal to the FORMAT half: FCPXML 1.10 doesn't carry grade state anyway (§3.4/§8 — v1 exports neutral + the LUT workaround).

**FIX-2 (P2) — 10:14, the app-half BASE sentence.** After "…segments — never the live mixer, the one-engine law)" insert: "— and, since the app's M2 Wave 1, under the WORKLET-FLUSH law: `workletFlushMs: sceneHasWorkletBackedInserts() ? 250 : 0` (deliverService.ts:252 + the WORKLET_BACKED_INSERT_TYPES set :170-172 — compressor/gate/deesser/limiter; offline ducking rides the SAME Comp inserts the monitor uses and a worklet module load racing `startRendering` falls back to PASSTHROUGH — the silent-duck-loss class the wave closed; the r5 dependency for all export-side audio work; the engine's own export paths pass 0 — orchestrator.ts:670 — the fast path)".

**FIX-3 (P2) — the R24→R27 re-pin batch.** :4 gains "Round 27 — the Z2 flip + the worklet-flush law + the model-coherence fold (see `audits/fleet-r27/spec-10.md`)"; :12 pins → engine `f9ac806` (748/748), OT HEAD `55c81c0` (632/632; code pin `970948a`), WDC `ec8fd5c` (777/777), nle-ui `32abd58` (690/690), app `c020b2a` (252/252); :15's serialization clause → "…`toJSON`/`fromJSON` — fromJSON is now the D-HB2 scene-load boundary (validateSceneForLoad first; typed rejected-whole errors; OT `timeline-core.ts:2433-2537` @ `970948a`)"; :14/:24 the variants' DeliverPage clause → "…(the R22 W5 grammar, since extended by R24-A3 W4 `6b0bd91`: the unconditional ruler + the 32px RangeBand + `deliverViewStore`'s single-writer job store)"; :20's "grep-verified at `c885ece`" → `c020b2a`; :14 optionally notes F3 (the blob-URL revoke, deliverService.ts:100-120).

**FIX-4 (P2) — §4's model-coherence edits (the 09 fold):**
- **(a) 10:319-321:** replace `scene.tracks.main.elements.map(id => this.getElementById(ctx, id))` with the N1 law: elements are INLINE `ElementJSON[]` in time order (09:139) — sort in place, delete the getElementById indirection (same for overlay/audio loops :339-356).
- **(b) 10:407-410:** replace the volume sketch with the domain law: `if (el.volume == null) → omit <adjust-volume> (FCP default 0 dB — absent≡unity, 09 B2/R27); else amount = clamp(20·log10(el.volume), −60, +20) dB (the fleet [−60,+20] domain; NaN→unity at the one-home clamp — opencut core/audio-params)`. Delete the `gain <= 0 → -96` floor (unity-floor 0.001 ≡ −60 dB replaces it).
- **(c) 10:400-402:** `buildTimeMap(el.speed, el.duration, el.sourceDuration, fps)` → `buildTimeMap(…, el.preservePitch)`; §11.6's pattern gains one line: `emit preservesPitch="0" only when el.preservePitch === false (absent≡true ⇔ the DTD default "1")`.
- **(d) 10:490-503:** the switch keys on `transition.presentation` (09:238; OT ElementTransition — `type` is always 'crossfade'); keep the Cross-Dissolve fallback for unmapped presentations (the v1 law); **10:475-476** the offset formula gains the alignment term: `offset = cut − transitionDuration · (1 − (transition.alignment ?? 0.5))` (09:240; the engine planner's cut-centered law).
- **(e) 10:586-592:** `ctx.project.markers` → `scene.markers` (A2 per-scene, 09:115; 10's own Correction #10 already states it).

**FIX-5 (P2) — 10:603-646, §4.8's NTSC family.** State the FrameRate convention once ("`{numerator, denominator}` is the RATE; 29.97 = {30000, 1001} — 09 §3.2/OT frame-rate.ts") and fix the three functions: one frame = `fps.denominator/fps.numerator s` (`formatOneFrame` AND `formatFrameDuration` — the `fps.numerator === 1001` discriminator is wrong under this convention and is deleted); `formatTime` reduces `frames · fps.denominator / fps.numerator` (not `frames/fps.numerator` — the 1001 factor was dropped). Cross-check: §11.4's own table (29.97 → `1001/30000s`) is the acceptance oracle.

**FIX-6 (P2) — §12's refresh.** :1417 `466` → `863`; :1419 `2379` → `3,102` (+ "(R27: 09's own audit re-based the schema blocks — 10's §4 must be re-derived against them; see FIX-4)"). Append an R27 note to §12.8 after the R24 note (:1519-1531): "R27 re-verification (engine @ `f9ac806`, 748/748): (1) zero-FCPXML STILL HOLDS (src/+tests/ → 0). (2) The export subsystem grew 1,785 → 1,806 LOC — HW1-8 the pcm-s16 audio-only ladder (`84c909d`) + the F2-7 hostile-fold batch (`1c04887`); the M2 Wave 1 flush law landed APP-side (deliverService.ts:252) while the engine's own paths keep workletFlushMs: 0 (orchestrator.ts:670). (3) The MediaMetadata colorSpace ENGINE-GAP re-verified OPEN at metadata.ts:48-102. (4) The R7-era line pins drifted (types.ts now 1,516 LOC); substance re-anchored: speed :644, volume :656, markers :1373, TrackKind :1066, presentation :422, CompositionItem :1008."

**FIX-7 (P2) — §16's fixture coherence.** (a) :1979-1984 + :2286-2287: "`retime: { speed: 2.0 }`" → "`speed: 2.0`" (09's direct ElementJSON field — the sidecar reading retired; OT's `retime.rate` is the engine projection). (b) The Test-assets intro (:2265-2272) gains: "the eight `10/*` fixtures register in spec 17 §5.3 at the r5 phase-entry corpus creation; `simple-cut.json`/`multi-track.json` share names with 17's own registry fixtures (17:946/:957) — the 10/ namespace disambiguates paths, and 10's multi-track recipe (3 tracks × 2 clips) deliberately differs from 17's (5 tracks/10 clips); both facts recorded to keep the corpus honest."

**FIX-8 (P3) — polish:** (a) §2 Goal 6 append "(and pitch — `preservePitch` rides the same `<timeMap>` via `preservesPitch`, §11.6)"; (b) :1986-1988 the text-element test: "resources in `<resources>`" → "children of the `<title>`" (per §4.6/the DTD); (c) §8.1 add "❌ Reverse (`speed: -1.0`) — v1 exports the clip at |speed| with the reverse flag in KNOWN_LOSSY_FIELDS (a DTD-verified reverse timeMap is a v2 decision)"; (d) §3.3 annotate the `srcEnable`/`conform-rate` rows "documented but not emitted by the v1 `buildAssetClip` sketch — v2 wires them (srcEnable becomes load-bearing if 09's `isSourceAudioEnabled` lands in the doc model)".

---

## §4 Cross-spec notes (for the cross-cut wave — NOT my edits)

1. **17 §7.7/:1475 + 17:2075 (M4)** reference `importFCPXML` and an IMPORT-direction round-trip ("export from FCP → import to our editor") — **no spec defines importFCPXML** (the R7 finding, still open) and 10's own M4 is the RE-EXPORT direction (export → FCP → FCP re-export → compare). One of the two M4s must move; 10 stays export-only v1.
2. **09's B2 amendment set** (the spec-09 audit's Edits 1-3) — 10's FIX-4b must land in the same wave (byte-coherent volume law: 09 model / 18 authoring rail / 20 fold / **10 export**).
3. **04:24's D29.5c row + 10's Z2 row** — the twin flip (spec-04's Row-4 edit is drafted; FIX-1 here is the same movement, both citing deliverService.ts:300-324 + R9-c).
4. **17 §5.3 registration** — 10's eight fixtures + 16's three-clips.json ride the same r5/r1 corpus-creation registration rule.
5. **00's D37+ candidates** (the preservePitch ruling + the volume-dB one-home law) — 10's export mapping is a fourth consumer of both; the cross-cut wave should name 10 §4.4 in the D37 text.
6. **20/02's workletFlushMs venue law** — the law's home is 20 §4 (the spec-20 audit drafted six coordinated edits); 10's §0 carries only the export-seam dependency view (FIX-2).
7. **15:2050** cites "spec 10 §13 requires stable UUIDs" — the requirement actually lives in §4.2's `generateUID` sketch (P3 precision note for 15's next pass).

---

## §5 What 10 uniquely sees

1. **The NTSC/1001 time-formatting family (FIX-5)** — the MediaTime-ticks ⇄ FCPXML-rational-seconds seam is 10's alone; the `fps.numerator === 1001` discriminator bug is invisible to every other spec (nobody else converts to the 30000ths basis). The spec contradicts its own §11.4 oracle table.
2. **The Z2 flip's export-side twin** — 04 owns the renderer-side twin row; 10's §0 GAP row is the only other place that states the export-was-ungraded premise, now false.
3. **The workletFlushMs r5 dependency** — 20/02 own the law; 10 owns the dependency view (any r5 export-side audio work inherits the flush law; the app's 250-when-present vs the engine's 0 fast-path split).
4. **The transition-shape export keying** — the S3 two-tier law (structural `type` + `presentation` registry key, 07 §6.1A) has its export consequence only here: §4.5's switch reads the pre-S3 shape.
5. **The format-half's total-greenfield re-verification** — the four-repo zero-grep + the app's defensive row + the mock rails + the intact r5 phase-entry artifact: the seam-split story survives R27 exactly, which is what licenses the phase posture.
6. **The M4 direction contradiction with 17** (cross-note #1) — only visible from 10's side of the handoff.
