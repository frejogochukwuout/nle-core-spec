# R27-W3-19 — spec 19 (Code References & Engine Reconciliation) — the pin/line register audit

**Task ID:** R27-W3-19 · **Agent:** spec-19 · **Round:** R27 final-tightness, Wave 3 (per-spec fleet)
**Spec:** `19-code-references.md` v3.2 (the R24 re-baseline — 421 lines) — **the corpus's pin/line register. Report-only; no corpus/repo edits.**
**Ground truth:** the live trees at the R27 pins — engine `f9ac806` (code anchor `50b91f5`; 748/748; timeline.ts **8,997 LOC**), OT HEAD `55c81c0` / code pin `970948a` (632/632; **31 = 28 routed + 3 exceptions**), WDC `ec8fd5c` (777/777), nle-ui `32abd58` (690/690), app `c020b2a` (252/252), the mocks in-repo. **Method:** ~45 of 19's OWN pins re-derived by direct repo read/grep this session (every method anchor in §2's map marked ⭐ was re-grepped live); the five Wave-1 scouts + the six Wave-2/W3 sibling reports consumed for the module narratives (their claims spot-verified, not re-derived).

**The headline:** 19 is structurally SOUND — the hierarchy, the link-vs-inline policy, the ledger's LAW content, the C-series framing, and the §7/§8 history all re-verify — but its register layer is **three rounds behind** (R24 pins, R24 counts, R24 census arithmetic). Because 19 is the corpus's reference authority, every stale number it carries is a stale number the battery and the sibling specs inherit. The single coherent fix is the v3.3 re-base drafted in §3 (F-1..F-6) + the consolidated line-pin map (§2) that the whole amendment wave keys on. One NEW cross-seam fact surfaced only here: **the AW1-2 ceiling fix covered the Inspector only — the Mixer panel's clip-gain field still caps at +4 dB** (§5-U1).

---

## §1 Per-section verdict table

| § (lines) | Verdict | The finding (evidence at the R27 pins) |
|---|---|---|
| Status header :1-7 | **P1** | The whole pin world is R24: engine `5036387` 458 → **`f9ac806` 748/748**; OT `ded43c4`/`c15a629` 536 → **`55c81c0`/`970948a` 632/632**; WDC `85b81b0` 759 → **`ec8fd5c` 777**; nle-ui `fc4cc35` 674 → **`32abd58` 690**; app `c885ece` 174 → **`c020b2a` 252**; mini 355 "sealed" → **in flight (register 428)**; variants 1,521+ → **register 1,762/64, live 1,773 mid-R25**. Also: the D26 census figures (39 mirrors/7 carriers/`use-wire-dispatch` pending) superseded (F-4). |
| §0 FORWARD INVENTORY :11-26 | **P1** | (a) C7 row :16 "the union is 30 prefixed types at `c15a629` … the D29 re-base splits the census 24 routed + 6 exceptions" → **31 = 28+3 @ `970948a`** (api.ts:243-275/:328-335; the singular `removeKeyframe`/`retimeKeyframe` RETIRED; `upsertKeyframe` ROUTED at AR-2). (b) The consumer re-pin residual :17 (engine WDC `494f6ff`) → **OT `6e2b91a` + WDC `ec8fd5c` both moved**. (c) The D26 census row :18 (39 mirrors + host, 7 byte + 25 mechanical, 7 carriers) → **42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host @ `6e2b91a`** (census-check run live this session). (d) The projector row :21 — see §3 F-2. (e) Acceptance :26 "engine 458 and OT 536 appear identically in specs 00/17/19" → **748 / 632** (the battery_r27 re-key). |
| §0A TL;DR :30-32 | **P1** | Same pin/count family + the missing R25-R27 landings the paragraph should now name: the RC round (D12 ratification, OV-01/02/05/12, M2 Wave 1), D-ARCH-6, AR-1/AR-2, R9-b preservePitch, FW-D, AW1-2, the S-series. The mini figure inside :32 is already 428 (the R24-2 sibling amended it in flight) but reads "sealed" — it is in flight. |
| §1 Purpose :36-38 | **SOUND** | Prose; no pins. |
| §2.1 tier table :44-52 | **P1** | Every asset row carries R24 pins/counts (tier 2/3/3A/4/5A rows + the consumer pins). Tier-5A row :52 says mini "355 SEALED" + variants "1,521+ at the R23 state" — both superseded (the mocks are active again; §3.5/§3.6 of this report). |
| §2.2-2.3 history/:62-66 | **SOUND** | Prose; the "engine converges toward the spec" law unchanged (D12's ratification strengthens it — cross-note only). |
| §2.4 seam resolution :68-79 | **P2** | (a) §2.4.1: `SceneTracks types/index.ts:116-119` **HOLDS EXACT** ⭐; `TimelineData core/types.ts:1354-1406` **HOLDS** ⭐ (the interface starts :1354); the Clip union `core/types.ts:1030` **HOLDS** ⭐. (b) The "FOUR type-only sites" + `composition-frame.ts:100` → **4 type-only (scene-to-segments :67 ⭐, av-link :49 ⭐, transition-inputs :70 ⭐, composition-frame :100→:113 ⭐) + 2 VALUE leaf deep-imports** in `bridge/opencut-laws.ts:60-61` (`core/audio-params`, `ops/retime`) — the zero-runtime-dependency convention was AMENDED by OV-01/OV-02 (leaves, never the barrel; opencut-laws.ts:32-37 states the amended law). (c) §2.4.2 :73 "30 at `c15a629` (24 routed + 6 exceptions)" → 31 = 28+3. (d) §2.4.4: `placeholder-compositor.ts:116` **HOLDS EXACT** ⭐. (e) §2.4.3's "port-scheduled… not started" — still true, but the engine's D12 ratification + the r1-port collision map + NS-1..NS-5 are the new context 19 should register (F-9). |
| §3.1 nle-engine :83-139 | **P1** | The R15 table is history-marked (fine). The R24 register is the live one and is stale on every row: tests "458/458 (13 files…)" → **748/748 (25 files: 4 top-level — nle-bridge 99 ⭐, nle-bridge-realtime-midi 8, upstream-ports 25, m2-wave1-mixer-surface 9 — + 21 engine-side, incl. bridge-seams 151 (was 113) and the E1/E2 pin file timeline-linked-source-edit 13)**; the bridge family "11 files / 4,334 LOC" → **12 / 4,645** (+ opencut-laws.ts; composition-frame 459→**552**); the audio-graph pins :3287→**:3304** (player), :796→**:810** (orchestrator), audio-scene :803→**:865**; the W2-consumption pins (:246/:305/:611/:638) → **:207-214/:273 + :596/:623**; LOC census "54,791/62" → **57,064/63** ⭐; the vendored-submodules row "TYPE-ONLY in src/ (4 sites)" → **+2 VALUE leaf imports + the OV-12 re-pin gate (ci.yml:216-253)**; the projector row :139 — F-2. The frozen-API row (455 names) **HOLDS** ⭐; layer fence 52 holds; FCPXML-zero holds. |
| §3.2 opencut-timeline :141-198 | **P1** | The R24 register: 536→**632** (report json `total:632, passed:632` ⭐; the split convention now 452 in-page + 30 DragEvent/keyboard + 150 real-mouse per the artifact); code pin `c15a629`→**`970948a`** (src+scripts diff empty to HEAD ⭐); the canonical tree "40 files / 8,604 LOC" → **41 / 9,176** ⭐ (+`use-keyframe-authoring.ts` 262L ⭐; TimelineView 1,516→**1,569** ⭐; TimelineElementView 883→**950** ⭐; hooks 20→**21** ⭐; use-wire-dispatch 90→**95** ⭐); the headless-union row :153 — **the file's only P1-class falsehood**: "24 UI-routed verbs + 6 documented exceptions (`upsertKeyframe`/`removeKeyframe`/`retimeKeyframe`…)" names verbs that no longer exist → 31 = 28+3 (union :56-212; `WIRE_COMMAND_TYPES` :243-275 ⭐; `WIRE_UI_EXCEPTIONS` :328-335 ⭐, **exported since AR-1 — the "runner-local" premise dead**; insertBatch arm :1262-1370 ⭐; applyBatch :1739→**:2559** ⭐; in-batch undo/redo rejection :1745→**:2565** ⭐; NOOP :921→**:1590** ⭐); the module-area LOC census all moved (core 445→493, placement 854→875, ops 4,562→**5,080** (timeline-core 2,890→**3,360** ⭐), controllers 3,419→3,453, headless 1,806→**2,643**, testing 16,731/28→**21,365/37**); `setTracksMuted :2145`→**:2185** ⭐; the M49C cell "24/24 PASS" → **28 routed + 3 exceptions via the live read of BOTH exported registries** (run-timeline-tests.mjs:390-399 ⭐) + the HA-4-2 origin-attribution law (only origin:"ui" dispatches count). The W8 UI-surface list :181-194 (R15-era pins) and the host-seam block :196 are P2 (the tree moved: AudioVolumeLine now carries the AR-2 dblclick at :228-232 ⭐; the host seam at :178 ⭐; `/view` page 610→**659** ⭐; data-test 68→69-70 components / 100→~103 repo-wide — see U-3). :150's "the contract the app's `/dev/view-fixture` replicates" — the page does NOT exist at `c020b2a` (planned 17 §17A S3); "must replicate" (:196) is the correct tense (P3). |
| §3.3A web-daw-core :200-208 | **P1** | 759→**777/777 @ `ec8fd5c`** (34 files); the three-class census :204 "copy 75 src files + 4 root; shims 11; coreOwned 5" → **copy 93 + 4 root; shims 11; coreOwned 14** ⭐ (extraction-manifest.json: `copy`93/`copyRepoRoot`4/`shims`11/`coreOwned`14; the 14: index, meter-tap, soundtouch, varispeed, waveform-contract, 7 test files + setup, `types/spessasynth-core.d.ts`); :202 "upstream unchanged since the sync, ls-remote verified 2026-09-04" — **superseded**: the fork advanced to `f5011b3` and the S-series (19 locked files + the 18-file closure) was absorbed 2026-09-13 (lock sha `f5011b3` ⭐, 97 entries, zero disk↔lock drift); **the S-series surfaces are missing from the card entirely** — the diagnostics family (`EngineDiagnostic` bus + heartbeat monitor, diagnostics.ts:1-57; the `DspDiagnosedProcessor` in-`process()` mixin, dsp-effects-worklet.js:315-372; the rt-safety-gate transitive-heritage fix, rt-safety-gate.ts:141-182; the bypass-to-passthrough-on-stall law), the wam-instrument/state path, the pure-logic MIDI family, the sample-catalog split, the instrument factory/registry/param-path (3 VERBATIM copies, not shims — the file-map label that spec 20:209 carries as "(shims)" is false), `library-audio-proxy`, `stall-race`, the ambient-declaration consumption law (consumers must add the vendored `.d.ts` to tsconfig `files`). |
| §3.3B nle-ui :210-212 | **P1** | 674→**690 @ `32abd58`**; the card's queue/state is pre-F4: **F4-6 identity guards** (`patchValueSame` useUiStore.ts:271-279 ⭐; `splitEnginePatch` :314 ⭐; the setTransition no-ops), **F4-7 kind-aware toggleMuteAll** (`isMuteableTrack` :332) + **duplicateElements severs `linkedTo`** (:937-945), **R9-b `el.preservePitch`** (mockData.ts:60-72 ⭐ — absent≡true, the projection law, the reload-drop attribution), **FW-D `setElementFieldAll`** (:1078-1124; the UNDO-GROUP contract), **AW1-2 the Gain ceiling [−60,+20]** (Inspector.tsx:1001-1002 ⭐) — none named. The consumer row "consumed by the app @ `fc4cc35` (no lag)" → **`83ff8a8`** with the AW1-2 absorb queued (app PLAN:143-146). The C0 MiniShell still NOT STARTED ⭐ (verified: no MiniShell in src) — that clause survives. |
| §3.3C nle-test-app :214-216 | **P1** | 174→**252 @ `c020b2a`** (8 suites: GluedShell, audioService, sceneBridge, deliverService, persistenceService, wire-coverage, engineSeam, waveformPeaks ⭐); "the app has NO `.agents/` yet" → **landed `7fd3eb6`**; the vendoring record 4 pins stale (engine **f9ac806**, OT mirror **6e2b91a**, nle-ui **83ff8a8**, WDC **ec8fd5c** ⭐); the census "39 mirrors … 7 carriers + `use-wire-dispatch` pending W-C" → **42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host; use-wire-dispatch a zero-action mirror since W-C** ⭐; the missing landings: AR-2 (wire-coverage.test.tsx:29/:49-59/:328-377 ⭐ — the LIVE-registry consumption + the volume-line dblclick gesture pin), M2 Wave 1 (deliverService.ts:252 `workletFlushMs` ⭐; the bus-fold replacement; the sidechain single-owner law), R9-b resolution (engineService.ts:510-513 ⭐, sceneBridge.ts:229-262/:435-449). |
| §3.3 cloudcut :218-220 | **SOUND** | Dormant per D27.3; no pins. |
| §3.4 teachers :222-224 | **SOUND** | Prose. |
| §3.5 shell-variants :226-232 | **P1** | "R23 state (UNCHANGED at R24 — no new mock rounds)" is **FALSE**: the sibling stream ran R24-2 + R25; the register reading is **1,762 it-blocks / 64 files** (re-derived at the R24-2 W2 re-pin) and the live count is **1,773** ⭐ (R25-W1/W2 in flight — `TrimAffordances.tsx` in the tree ⭐). Per the coordination law the register re-keys at the sibling's round-WRAP only; 19 must carry the declared figure + the in-flight flag (F-7). |
| §3.6 shell-mini :234-244 | **P2** | Internally mixed: :236 still says "355/355 … SEALED" while :241 was amended in-flight to 428/147-census. Live truth: **428 tests / 10 files** ⭐ (R24-2 W0+W1+W2+W2-fix landed; W3+ pending — in flight; the briefing's "415" is the pre-W2-fix reading). The census 147 units / 134 GAP / 395 authored is current. Reconcile the two sentences + flag in-flight (F-7). |
| §4 link-vs-inline :246-255 | **SOUND** | Policy prose; the four inline cases stand. |
| §5 insight ledger :257-295 | **P2** | The LAWS all re-verify standing (this is the ledger's core value). Pin status (all re-grepped ⭐): **HOLD exact** — #8 pipeline.ts:5135/:5206 (file still 5,320 LOC); #11 scene-assembly.ts:729/:1249-1255; #13 api.ts:1057-1060 (batch-abort text :1056-1061); #14 api.ts:543-556 (`resolveOperationRefs`); #2 video-sync :181-225 + :907-913 (:951 code); #7 lut.ts:314; #28 scale.ts:6-8 + element-interaction-controller.ts:53 + box-select-controller.ts:34 + pixel-utils.ts:13-15/:63 + ruler-utils.ts:17-38 (the 18px floor at :43); #26 placement/index.ts:175-197 (function now :196 — range still brackets). **DRIFTED** — #12 `timeline.ts:1521`→**:1642**; #30 api.ts:921→**:1590** (hasAnyRef?NOOP:NOT_FOUND), :986→:1643 (split-edge); #31 api.ts:1739→**:2559**, :1745→**:2565**; #27 api.ts:558-718/:712-718→ the insert arm **:1189** + the landed-time comment **:1255**; #18 worklet ":47"→:64 (the 1-in/1-out class); #1 clock.ts :137→:135, :568/:591→:570-573 (minor); #6's "29 rgba8unorm sites"→**46 sites / 10 files** ⭐. |
| §6 corrective C1-C9 :297-315 | **P2** | C1's "29 sites"→**46/10** ⭐ (the C1 correction itself stands — spec 04 verified the frozen-exact 8-bit baseline); C2's pins hold except `EDIT_OPERATION_NAMES :2025-2044`→**:2045+** ⭐ (`editProject :1065` and `resolveOperationRefs :543` HOLD ⭐); C7 row :309 — the 30/24+6 worklist → 31/28+3 + the retired-singular + insertBatch notes; C8 — the projector re-type (F-2); C9 holds. |
| §7 D1-D5 :317-330 | **SOUND** | Historical answers; D1's escalation note is superseded by the C8/D12 re-type (cross-note only). |
| §8 ROI table :332-351 | **P2** | Historical verdicts stand; the "Timeline UI" row's parenthetical "(32/40 zero-diff at `c15a629`; 7 documented carriers; `use-wire-dispatch` pending D30 W-C…)" → 36/41 at `6e2b91a`, 5 carriers, use-wire-dispatch zero-action. |
| §9 watch list :353-375 | **P1** | The R24 fleet paragraph :352 (all pins/counts — keep as lineage, add the R27 paragraph); item 6 :365 "the union is 30 types (24 routed + 6 exceptions per the W11 census)" → 31=28+3; item 9 (op-family port) — the D24 mapping stands but the engine's r1-port collision map + NS-1..NS-5 design queue are the missing context (F-9); **new watch items missing**: the D-ARCH-6 consumer re-pin queue (app mirror `6e2b91a` is a session behind `970948a` — the gate-breaking duty), the S-series docs wrap (WDC HANDOFF stale), the nle-ui AW1-2 absorb, the app's stale maintainPitch PLAN row. |
| §10 usage rules :377-381 | **SOUND** | The anti-fabrication rule (§10.5) is what this audit executed. |
| §11 relationship map :383-399 | **SOUND** | Structure intact; the R24 note (:397) about the audit fleet re-basing the sibling anchors is this round's charge — delivered as §2's map. |
| §12 seal checklist :401-421 | **P2** | Item 6/7 (:413) carry the 30/24+6 figures; item 8's projector clause needs F-2; item 12b (layout.tsx "Z.ai Code Scaffold") — **still standing at `f9ac806`** (layout.tsx:17/:25 ⭐), the residue is now three rounds old; item 12a's consumer re-pin "EXECUTED at R24" needs the R27 successor note (the app's OT/nle-ui re-pins + the D-ARCH-6 queue). |

**Counts: P1 = 9 sections (one coherent cluster: the R24 register layer) · P2 = 7 · P3 = 2 · SOUND = 8.**

---

## §2 THE CONSOLIDATED LINE-PIN RE-BASE MAP (the amendment wave's single input)

19 is the corpus's line-register authority; this map covers 19's own pins AND the sibling-spec cite families 19 governs (their agents' maps folded in — every ⭐ entry re-grepped live this session at `f9ac806`/`970948a`).

### 2A. nle-engine @ `f9ac806` (docs-only head; code anchor `50b91f5`; timeline.ts 8,997 LOC)

| Symbol | Old pin | New pin | Carried by |
|---|---|---|---|
| `rollingTrimItems` | :2984 | **:3324** ⭐ | 06 §0/§10.4, 05 |
| `rippleTrimItem` | :2839 | **:3132** ⭐ | 06, 05 |
| `slip` | :4143 | **:4956** ⭐ | 06, 05 |
| `slideItem` | :4246 | **:5097** ⭐ | 06, 05 |
| `performInsertEdit` | :4702 | **:5630** ⭐ | 06, 05, 00 D31.1 |
| `performOverwriteEdit` | :4860 | **:6007** ⭐ | 06, 05 |
| `rateStretchItem` | :3155 | **:3840** ⭐ | 06, 05 |
| `rateStretchWithRipple` | :6351 | **:7645** | 06 §10.4 |
| `splitClip` / `splitAll` | :2367 / :2576 | **:2620 / :2840** | 06 §10.4 |
| trimHead/tail | :2646/:2742 | **:2920/:3026** | 06 §10.4 |
| `rippleDelete` ×2 | :3574/:3606 | **:4286/:4320** | 06 §10.4 |
| `removeItems` | :3757 | **:4507** | 06 §5.9C |
| `duplicate` | :3812 | **:4575** | 06 §10.4 |
| move ×2 | :3945/:4035 | **:4715/:4823** | 06 §10.4 |
| range family ×4 | :6910/:7057/:7098/:7124 | **:8235/:8511/:8557/:8587** | 06 §10.4 (`removeRangesFromClip` :8235 ⭐, `freezeFrameAtPosition` :8652 ⭐) |
| `joinItems` | :7341/:7411 | **:8817** ⭐ | 06, 05 |
| `closeGap` ×2 | :6180/:6284 | **:7463/:7574** | 06 §10.4 |
| sync-lock | timeline.ts:3511 + sync-lock.ts:503 | **:4218** + **:579** | 06 §10.4 |
| undo/snapshot | :6070/:6115 | **:7350/:7395** | 06 §10.4 |
| `_commit` / `snapshotsEqual` | :1844 / :1521 | **:1969 / :1642** ⭐ | 06, 19 #12 |
| `calculateSpeed` | core/timeline-math.ts:133 | **HOLDS** ⭐ | 06, 05 |
| `scheduleClipWithFades` | player.ts:3287 | **:3304** ⭐ | 19 §3.1 |
| `renderAudioMixdown` | orchestrator.ts:796 | **:810** ⭐ | 19 §3.1 |
| `clampAuthorFadeToSpan` | audio-scene.ts:803 | **:865** ⭐ | 19 §3.1/§9 |
| W2 retime thread | scene-to-segments.ts :246/:305/:611/:638 | **:207-214/:273 + :596/:623** ⭐ | 19 §3.1 |
| compositor rgba8unorm | :32/:981 | **:1130/:1174/:1205** ⭐ | 19 §3.1 |
| `EDIT_OPERATION_NAMES` | api.ts:2025-2044 | **:2045+** ⭐ | 19 C2 |
| clock throttle/monotonic | :137 / :568/:591 | **:135 / :570-573** ⭐ | 19 #1 |
| worklet 1-in/1-out | :47 | **:64** ⭐ | 19 #18 |
| OT type-only sites | comp-frame :100 (others hold) | **:113** ⭐; :67/:49/:70 hold ⭐ | 19 §2.4.1 |
| Counts | 458 tests/13 files; 54,791 LOC/62 files; bridge 11/4,334 | **748/25; 57,064/63; 12/4,645** ⭐ | 00/17/19 |
| Holds verified (no edit) | — | pipeline.ts:5135/:5206 (5,320 LOC); scene-assembly :729/:1249; api :543-556/:1057-1060/:1065/:378-406/:110-130; video-sync :181/:188/:907/:951; lut :314; placeholder-compositor :116; api-surface 455; Clip union :1030; TimelineData :1354 | 19/04/06 |

### 2B. opencut-timeline @ `970948a` (HEAD `55c81c0`; api.ts headless)

| Symbol | Old pin | New pin | Carried by |
|---|---|---|---|
| `WIRE_COMMAND_TYPES` | api.ts:182-213 (30) | **:243-275 (31)** ⭐; tsc-lockstep :277-297 | 15/05/06/09/12/17/18/19 |
| `WIRE_UI_EXCEPTIONS` | runner-local, 6 names | **api.ts:328-335, EXPORTED, 3** ⭐ (AR-1; the runner copy deleted) | 15 §0/§13.15, 19 |
| insertBatch arm | — (QUEUED) | **:1262-1370** ⭐ | 15, 06 |
| removeKeyframes / retimeKeyframes arms | — | **:2188-2285 / :2286-2390** | 15, 16 |
| `applyBatch` + in-batch undo/redo | :1739/:1745 | **:2559/:2565** ⭐ | 19 #31, 15 |
| NOOP/NOT_FOUND (trim) | :921 | **:1590** ⭐ | 19 #30, 15 §6.3 |
| split-edge NOOP | :986 | **:1643** ⭐ | 19 #30 |
| `setTracksMuted` | timeline-core.ts:2145 | **:2185** ⭐ (setTracksLocked :2141) | 19 §3.2 |
| insert data block | :558-718/:712-718 | **:1189 + :1255** ⭐ | 19 #27 |
| `enforceMainTrackStart` | placement :175 | **:196** ⭐ (the :175-197 range still brackets) | 19 #26, 05 |
| fromJSON boundary | — | **timeline-core.ts:2443-2537** (D-HB2) | 09 §3.3A (new) |
| `SceneTracks` | types/index.ts:116-119 | **HOLDS** ⭐ | 19 §2.4.1 |
| Constants family | scale.ts:6-8; pixel-utils :13-15/:63; element-interaction :53; box-select :34; ruler-utils :17-38 | **ALL HOLD** ⭐ | 19 #28 |
| Tree census | 40 files/8,604 LOC; TimelineView 1,516; ElView 883; hooks 20; use-wire-dispatch 90; /view 610; data-test 68/100 | **41/9,176** ⭐; **1,569** ⭐; **950** ⭐; **21** ⭐ (+use-keyframe-authoring 262 ⭐); **95** ⭐; **659** ⭐; **69-70 components (59 unique)/~103 repo-wide** | 05 §0/§16.5, 18, 19 |
| Module areas | core 445/placement 854/ops 4,562 (core 2,890)/controllers 3,419/headless 1,806/testing 16,731(28) | **493/875/5,080 (3,360 ⭐)/3,453/2,643/21,365(37)** ⭐ | 19 §3.2 |

### 2C. web-daw-core @ `ec8fd5c` — nle-ui @ `32abd58` — app @ `c020b2a`

- **WDC:** 759→**777** (34 files); lock sha `eeb3b24`→**`f5011b3`** ⭐; manifest copy 75→**93**+4 / shims 11 / coreOwned 5→**14** ⭐; `instruments/*` = 7 shims + **3 verbatim copies** (factory/registry/param-path — the "(shims)" label at 20:209 and any 19 echo is false); `audio-registry.ts:24-74` **survived byte-verbatim** (hash-verified by scout-wdc — spec 20's pins hold).
- **nle-ui:** 674→**690**; the four new law sites (mockData.ts:60-72 ⭐ / useUiStore.ts:271-279 ⭐, :314, :1078-1124 / Inspector.tsx:1001-1002 ⭐); the app's consumer pin `fc4cc35`→**`83ff8a8`**.
- **app:** 174→**252**; census **42=36+5+1** ⭐; pins engine `f9ac806`/OT mirror `6e2b91a`/nle-ui `83ff8a8`/WDC `ec8fd5c` ⭐; AR-2 sites wire-coverage.test.tsx**:29/:49-59/:328-377** ⭐; M2 site deliverService.ts**:252** ⭐; R9-b sites engineService.ts**:510-513** ⭐ + sceneBridge.ts:229-262/:435-449.
- **Mocks:** mini register **428/10** ⭐ (live 428; W0-W2+W2-fix; W3+ pending — in flight); variants register **1,762/64**, live **1,773** ⭐ (R25-W1/W2 in flight; re-key at WRAP only).

### 2D. The sibling cite families 19 governs (folded from their agents' verified maps — re-derive at the fold, do not re-derive now)

- **06 §10.4 (the ~25-pin table):** spec-06's F-1 map = §2A rows above + the "102 public methods"→**108** + the header's "UNCHANGED at `5036387`" note is FALSE at `f9ac806` (timeline.ts grew 7,502→8,997 through the H/F/FW/RC waves).
- **05 §16.4/§16.5:** §2A engine rows + :2224→**:2569**, :5546→**:7708**, :5642→**:7850**, api :804→**:830**, scene-assembly :210/:158→**:215/:160**, transform-resolver :462→**:303**; the §8A interim-carrier pins drifted twice (Timeline.tsx:1535→:1654, badge :1573→:1692, arrows :1654-1670→:1776/:1790, hit zones Clip.tsx:1359-1408→:1493-1510) — the durable fix is testid-keyed citations.
- **02:** :107 the zero-workers note (fact HELD; re-stamp `f9ac806`); :2338 §13B header pins 458/`c15a629`/`494f6ff` → **748/`f9ac806`, `6e2b91a`, `ec8fd5c`**.
- **04's 9/13 §13D cites:** 9 exact (wheelTint :1919, registerEffects :4595, luminance601 :1930, rgba8unorm :4850, runEffectChain :5179, GPU_EFFECT_REGISTRY :3039 + 3); 4 stale clusters refreshed by spec-04 (player enabledEffects :1656→:1673; api :1509/:1515→:1529/:1535; :1797→:1825; :724→:542-557; :1575→:1580; bridge-seams :223→:257, :161→:179).

---

## §3 The fix-list (mechanically appliable; the v3.3 re-base)

**F-1 (P1, the header + §0 + §0A + §2.1 + §9 R27 paragraph) — the fleet re-pin.** Replace the five pin/count clusters everywhere in 19 (22 `c15a629` hits + the R24 figures): engine `5036387` 458→**`f9ac806` 748/748** (docs-only head over code anchor `50b91f5`; 25 test files; LOC 57,064/63); OT HEAD `ded43c4`/code pin `c15a629` 536→**`55c81c0`/`970948a` 632/632** (the split 452 in-page + 30 DragEvent/keyboard + 150 real-mouse per the report json); WDC `85b81b0` 759→**`ec8fd5c` 777/777**; nle-ui `fc4cc35` 674→**`32abd58` 690/690**; app `c885ece` 174→**`c020b2a` 252/252**; consumer pins: engine→OT **`6e2b91a`** + WDC **`ec8fd5c`**; app→engine **`f9ac806`** + OT mirror **`6e2b91a`** + nle-ui **`83ff8a8`** (AW1-2 absorb queued) + WDC **`ec8fd5c`**. Update :26's count-consistency line to "engine 748 and OT 632 appear identically in specs 00/17/19" (battery_r27 keys on it). Add one R27 fleet paragraph to §9 above the R24 lineage paragraph, naming the round's landings: the RC round (D12 ratification, OV-01/02/05/12, M2 Wave 1), D-ARCH-6, AR-1/AR-2, R9-b/F4/FW-D/AW1-2, the S-series.

**F-2 (P1, :21 + :139 + §12 item 8 + C8) — the projector row re-type.** "NOT LANDED — `src/lib/nle/projector/` does not exist" is literally still true ⭐ but misleading as a GAP: **the projector DUTY is discharged by the bridge family** (`src/lib/nle/bridge/`: scene-to-segments + composition-frame + transition-inputs — the SceneTracks→venue translation, twin-pinned by bridge-seams 151/151), under a different path than D16's illustrative `projector/`; engine D12 (DECISIONS.md:521-584) ratifies exactly this junction. Rewrite the row: "**LANDED-AS-BRIDGE** (R27): the SceneTracks→venue translation lives in the bridge family per engine D12; the `projector/` path was never minted. The open half is the **parity corpus** (the N1 family's formal S4 suite — still unlanded; NS-4's SceneTracks→GPU render path is the registered design queue) + 00 D16 law 1's path citation becomes illustrative."

**F-3 (P1, the 24+6 census family — 11 sites: :5, :16, :26, :32, :73, :150, :153, :155, :309, :352, :365, :413) — the 31=28+3 re-declaration.** Every "30 types / 24 routed + 6 exceptions / 24/24 PASS" → "**31 types = 28 routed + 3 exceptions** @ `970948a` (selectElements/advancePlayhead/trim; `WIRE_COMMAND_TYPES` api.ts:243-275, `WIRE_UI_EXCEPTIONS` :328-335 EXPORTED since AR-1 — the runner-local registry premise is dead; M49C 28/3 via the live read of both registries + the HA-4-2 origin-attribution law)". The :153 exception enumeration names retired verbs — rewrite it (upsertKeyframe ROUTED at AR-2; the singular removeKeyframe/retimeKeyframe RETIRED, absence-pinned; insertBatch/removeKeyframes/retimeKeyframes LANDED, one history entry each). The lineage tail for :16/:309: 24 (R15) → 28 (R22) → 30 (R23) → 24+6 (R24) → **25+5 (AR-2)** → **31=28+3 (D-ARCH-6)**. The D29-F8 clause held: the census re-declared mechanically, no semantic amendment owed — but 19's OWN enumeration of the stale list as fact is the P1.

**F-4 (P1, :18 + :216 + §8's Timeline-UI row) — the app census re-declare.** "39 mirrors + host; 7 byte-exact + 25 mechanical + 7 carriers; `use-wire-dispatch` pending W-C" → "**42 port files = 41 mirrors + host @ `6e2b91a`: 36 zero-action (6 byte-exact + 30 mechanical-exact) + 5 carriers (TimelineView, use-timeline-actions, use-keybindings, TimelineContextMenu, use-playback-ticker) + the host; `use-wire-dispatch` a zero-action mirror since W-C; register-equality CI-enforced (census-check + mutation gate 8/8 green)**". Add: the `.agents/` landed (`7fd3eb6`); the 8-suite roof; the D-ARCH-6 queue (the OT re-pin + the port's gesture-seam switch — `use-timeline-actions.ts:452` + `use-keyframe-drag.ts:96` still direct-core; the gate goes red at the re-pin until they switch).

**F-5 (P1, §3.1's R24 register) — the engine-card re-base (the full drafted table).**
- Tests row: "**748/748 vitest** (25 files: 4 top-level — nle-bridge 99, nle-bridge-realtime-midi 8, upstream-ports 25 (the G1 re-expression), m2-wave1-mixer-surface 9 — + 21 under `tests/vitest/engine/`, incl. bridge-seams **151** (was 113) and the E1/E2 pin file timeline-linked-source-edit 13), tsc 0, fence 52, probes 9/9; lineage 458 (R24) → 471 (R26) → 670 (G1) → 688 (FW-A) → 722 (FW-C) → 729 (FW-E1) → 736 (OV-01/02) → 739 (OV-05) → **748 (M2 Wave 1)**; HEAD `f9ac806` is docs-only over code anchor `50b91f5`."
- Bridge-family row: "**12 files / 4,645 LOC** (+ opencut-laws.ts — the OV-01/OV-02 one-home collapse: VALUE deep-imports of opencut's pure leaves `core/audio-params` + `ops/retime`, the drift fences, the AR-2 seam pin); composition-frame.ts **552** (OV-05: `el.transitionOut` the BASE :301-317, sidecar the OVERRIDE :334); scene-mixer.ts (M2 Wave 1: the bus-law `applyAuxBusGain` :277-280 + the sidechain-parity — `collectSidechainWires` consumed INSIDE `materializeStrips` :222-225/:314-339, per-wire identity-diff sever); mixer-track-model.ts (AuxBusSettings.muted :89-102); N2b/MIDI/W2-consumption rows: re-pin to :596/:623 (retime thread), :207-214/:273 (option fields); D12 RATIFIED (DECISIONS :521-584 — opencut is THE editing engine; the engine stack is the render/export venue model; one-directional junction; venue-internal freeze)."
- Audio-graph row: `player.ts:3304` / `orchestrator.ts:810` / `audio-scene.ts:865`.
- Vendored-submodules row: "OT **TYPE-ONLY ×4** (scene-to-segments :67, av-link :49, transition-inputs :70, composition-frame :113) **+ 2 VALUE leaf deep-imports** in `opencut-laws.ts:60-61` (leaves only, never the barrel — the zero-runtime-dependency law AMENDED by OV-01/OV-02); **OV-12**: engine CI's `vendored-timeline` job runs the vendored OT tree's own suite whenever the gitlink moves (ci.yml:216-253); OT @ `6e2b91a` (one session behind the code pin — the D-ARCH-6 absorption queued) + WDC @ `ec8fd5c`."
- LOC/file census row: **57,064 LOC / 63 files**; timeline.ts **8,997** (the R15-era table's 7,480 is history).

**F-6 (P1, §3.2/§3.3A/§3.3B/§3.3C) — the four module cards' register rows**, per §1's findings (OT: tree/census/headless rows per §2B; WDC: the three-class census + the S-series surfaces block — ADD the diagnostics/heartbeat law, the wam-instrument+state path, the MIDI family, the catalog split, the 3 instruments copies, the ambient-declaration consumption law, `library-audio-proxy`, `stall-race`, the rt-gate fix; nle-ui: the F4/R9-b/FW-D/AW1-2 landings + the consumer pin; app: per F-4 + the AR-2/M2/R9-b evidence rows). Keep the R15/R24 rows as lineage per the file's own convention.

**F-7 (P1/P2, §3.5/§3.6 + §0's mock rows) — the mock cards.** mini: reconcile :236's "355 SEALED" with :241's 428 amendment — "**in flight: R24-2 W0-W2+W2-fix landed (428 tests / 10 files; census 147 units / 134 GAP / 395 authored), W3+ pending**"; variants: "**the register reading is 1,762 it-blocks / 64 files (re-derived at the R24-2 W2 re-pin); the R25 waves are in flight (live count 1,773 at this audit; TrimAffordances.tsx landed); the register re-keys at the sibling's round-WRAP only**". Strike §3.5's "UNCHANGED at R24 — no new mock rounds".

**F-8 (P2, §5/§6 pins) — the ledger re-anchor.** Apply §2A/§2B's DRIFTED rows to #12 (:1642), #30 (:1590/:1643), #31 (:2559/:2565), #27 (:1189/:1255), #18 (:64), #1 (:135/:570-573), #6 (46/10), C1 (46/10), C2 (:2045+), §3.1's four audio pins, the W2-consumption pins, composition-frame :113. The HOLDS need no edit (optionally add one dated "re-verified R27 @ f9ac806/970948a" line to the ledger's preamble).

**F-9 (P2, new content 19 should carry) — the engine-side design queue + the cross-repo rulings.** Register (one row each, §0 GAP or §9): the **NS-1..NS-5 design queue** (transport/render-clock coupling; real-media peaks; clip-frame thumbnails; SceneTracks→GPU render path; edit→A/V reschedule classification — engine PLAN:463-485); the **r1-port collision map** (E1/E2 linkage the biggest hole; the constants-module lattice decision; spec-15-wins wire convergence — audit-AW1 §3); the **D12 ratification note** (both signatures now exist); the **AR-2 consumption law** as a pointer to 00 §2A.10 (the standing-law draft); the **OV-12 gate**; the **live-registry class** (the mirror-stale incident is 19's own cautionary tale — worth a ledger row #34).

**F-10 (P3):150** — "the contract the app's `/dev/view-fixture` replicates" → "the contract the app's **planned** `/dev/view-fixture` (17 §17A S3) must replicate" (the page does not exist at `c020b2a`; :196 already uses the correct "must" form).

---

## §4 Cross-spec notes (for the cross-cut wave; recorded, not actioned)

- **00:** the fleet rows + §2A fleet table consume F-1 verbatim; 2A.10 (AR-2 standing law) is drafted in spec-00's report — 19 §3.3C should point at it; D16 law 1's path citation rides F-2; D12's ratification note (both signatures).
- **15:** the census re-declaration (F-3) is 19's mirror of spec-15's §0/§4.1A/§13.15 rows — one edit family; the C7 worklist sizing (31) is shared.
- **05/06:** their §16.x/§10.x re-anchor maps are §2A/§2B of this report — the wave applies ONE map (their agents' F-1s already cite the same new pins; I re-verified the 9 highest-traffic methods independently).
- **20:** the S-series card content (F-6's WDC block) is spec-20's file-map amendment seed (scout-wdc's §3); the "instruments/* (shims)" label fix; the nle-bridge 96→99 count.
- **02:** :2338 §13B header rides F-1; the §7.4 diagnostics law draft (spec-02's report) is the corpus home for the S-series law 19's card should cross-ref.
- **04:** §13D's 4 stale clusters per §2D (spec-04's report).
- **09/16/17/12/18:** the preservePitch ruling (09 §3.1/§7.2), the D-HB2 scene-load boundary (09 §3.3A), the 8-suite roof (12/17 §17A), the data-test mapping row + the honest-mock ledger updates (18) — all ride their own agents' reports; 19's job was the register (done).
- **Battery hooks:** (a) the count-consistency class re-keys 458/536→748/632 (00/17/19); (b) the census class re-keys 24+6→28+3; (c) a new check worth adding: "19's §5 ledger pins re-verify at the round pins" (the ledger is machine-checkable — 12 of its rows carry exact line pins; this round 9 of 12 held, 3 drifted).

---

## §5 What 19 uniquely sees

1. **THE consolidated line-pin re-base map (§2)** — the only corpus-wide, register-authority view of the line-number rot; the amendment wave's single input for the engine/OT pin families.
2. **The ChannelEditor residual (NEW, P2-class cross-seam):** AW1-2 (`32abd58`) raised the Inspector's Gain ceiling to +20 (`Inspector.tsx:1001-1002` ⭐) but **`src/components/mixer/ChannelEditor.tsx:91/:99` — the Mixer panel's clip-gain authoring field — still caps at `max={4}`** (min −60). The fleet [−60,+20] coherence claim has a hole no scout or sibling spec reported; the engine's D7 lattice text still describes the +4 subset as the nle-ui ceiling (also stale). Route to spec 18/20's AW1-2 rows + the nle-ui queue.
3. **The census re-declaration history as evidence for the AR-2 law:** the census moved twice mechanically (24+6 → 25+5 at AR-2 → 28+3 at D-ARCH-6) exactly as D29-F8 promised, and the one place the stale enumeration survived as *fact* is a spec enumerating the exception list — 19's own :153. The live-registry consumption law (00 §2A.10 draft) is the generalization; 19 should carry it as ledger row #34's cautionary tale (the hand-mirrored copy went stale at exactly the re-pin the design said it wouldn't survive).
4. **The type-only convention's evolution:** the bridge's zero-runtime-dependency law is now "type-only ×4 + VALUE leaves ×2 (never the barrel)" — `opencut-laws.ts:60-61` — with `composition-frame.ts:100→:113` the only site pin that drifted. No other spec carries this convention statement.
5. **The projector duty's true landing shape (F-2)** — "LANDED-AS-BRIDGE" is a resolution only the register's authority could give (00 D16 + 04's NS-4 + 09's C8 all point here).
6. **The mock-register coordination law applied:** register 1,762 vs live 1,773 (variants) and 428 vs the briefing's 415 (mini) — 19 is where the declared-vs-live distinction must be stated, because the sibling's in-flight waves will move both again before WRAP.
7. **The engine's test-file census 13→25** (nobody else carries it) + the suite-level detail (bridge-seams 113→151; the E1/E2 pin file; upstream-ports as the G1 re-expression venue).
8. **The layout.tsx "Z.ai Code Scaffold" residue is three rounds old** (layout.tsx:17/:25 ⭐) — 19 §12's item 12b is the only corpus site tracking it; it survived the entire RC round untouched.
9. **The ledger's stability asymmetry (a positive finding):** every OT-side constants pin in §5 #28 (scale/pixel-utils/interaction/box-select/ruler-utils) and the deep engine algorithm pins (pipeline :5135/:5206, scene-assembly :729/:1249, api :543-556/:1057-1060) HELD through three rounds — while the engine's timeline.ts method family moved ~1,500 lines. **The recommendation embedded in the data: cite CONSTANTS and leaf helpers directly, but convert method-family citations to symbol+file form** (the testid-keyed-citation lesson spec-05 drew for its own register, generalized) — worth a one-line addition to §10's usage rules at the fold.
10. **The data-test census convention gap:** my raw counts (70 components / 103 repo-wide mentions) vs spec-05's 69 (excluding a selector reference) vs 19's R24 census (68/100) differ because **the counting convention is nowhere stated** — 19, as the register, should pin it (raw `data-test` mentions? emission sites? unique values?) before the next census lands in three different numbers again.

**Report-only confirmed:** no spec/repo files touched outside this report + the worklog; no commits/branches; stayed on main.
