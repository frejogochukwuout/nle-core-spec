# R27-W3 — spec-07 audit (07-composition.md, the composition runtime: FrameDescriptor, layers, transitions, effects, masks)

**Task ID:** R27-W3-07 | **Agent:** spec-07 | **Round:** R27 final-tightness audit, Wave 3 (per-spec fleet)
**Spec audited:** `07-composition.md` (2,427 lines) — section by section at sub-spec depth.
**Ground truth:** nle-engine @ `f9ac806` (748/748, tsc 0; `src/lib/nle` **63 files / 57,064 LOC**; timeline.ts 8,997) — live-read in full: `bridge/composition-frame.ts` (552 L), `bridge/transition-inputs.ts` (314 L), `tests/vitest/engine/bridge-seams.test.ts` (2,584 L / **151 `it(` / 33 describes** — counted), `transitions/{registry,planner,handle-utils,pipeline}.ts`, `effects/pipeline.ts`, `gpu/{compositor,mask-manager,texture-pool}.ts`, `playback/{scene-assembly,player}.ts`, `core/{types,transform-parent}.ts`, `timeline/timeline.ts`, `persistence/index.ts`, `headless/api.ts`, `.agents/{PLAN,HANDOFF}.md`; opencut-timeline @ `55c81c0` (code pin `970948a`, 632/632 — `types/index.ts`, `ops/{timeline-core,split}.ts`, `render/placeholder-compositor.ts` live-read); nle-test-app @ `c020b2a` (ProgramCanvas 242 L, deliverService **418 L**, audioService — live-read); nle-ui @ `32abd58` (useUiStore setGrade); ui-mock/shell-variants @ HEAD `57c7bea` (the 27-name `TRANSITION_PRESENTATIONS` list re-counted). FreeCut/OpenCut-classic reference repos are NOT in the sandbox — §11/§12.A/§13-§16's reference-repo cites are SCOUT-07-era verifications, checked for internal consistency only (same limitation 04's report registered). The sibling reports read: spec-04, spec-09, spec-19, mock-color §5, scout-engine.

---

## §1 The per-section verdict table

| § (lines) | Content | Verdict | Evidence |
|---|---|---|---|
| Status header (:4) | The R24 pin story | **P2** | Every pin one round behind: engine `5036387` 458 → **`f9ac806` 748**; OT `ded43c4`/`c15a629` 536 → **`55c81c0`/`970948a` 632**; app `c885ece` 174 → **`c020b2a` 252**; "bridge pins 97→113" → **151**. No D12 ratification note (briefing #13) |
| §0 BASE — engine row (:13) | The N1/N3 surface + W2.5 + Wave 4A | **P1** (one FALSE law) + P2 pack | **The Gaussian-Blur default is FALSE**: "default 4, non-finite→4" — live `GAUSSIAN_BLUR_DEFAULT_RADIUS = 10` (composition-frame.ts:210, F2-5 `c8a4d00`), pinned at bridge-seams:985/:990/:1010 (the cross-module coherence pin reads the registry's declared `gaussianBlur.params.radius.default` live). Twin of 04's Row 1. P2s: census "113 pins… the only commits to touch the file since the R23 pin" — 5 more commits touched it (F2-5/F2-6/F2-7/P1/OV-05; census → **151**); `clampFadeToElementSpan` — **wrong name at its own R24 pin** (live: `clampFade(fade, ownDuration)` transition-inputs.ts:186; `git log -S` across --all finds the spec's name nowhere); W2.5 line cites drifted (:119→**:133**, :124→**:138**, :161→**:179**, :223→**:257**, :229→**:263**; describes :921/:970/:998 → **:980/:1045/:1073**). Verified SOUND: the cut-centered window formula `[cut − D·alignment, cut + D·(1−alignment))` (transitionWindow :352-360, half-open at :400/:415), z-order main→overlays-reversed (:338-347), head/tail handles (:403-407), Wave 4A P0.1/P0.2 still fixed (pipeline.ts:5148/:5204, mask-manager :682/:734), 44-effect registry (43 ids in pipeline.ts + `gpu-lut` in lut.ts — counted), the grade DECLINE (D29.5(c)) — but see R9-c re-filing (Row 13) |
| §0 BASE — OT row (:14) | S3 transitionOut laws + placeholder compositor | **P2** | `types/index.ts:145/:190/:166-168/:231-238` ALL EXACT ⭐ (ElementTransition, the field, the params-overwrite-only law, the 7-way union); `ops/timeline-core.ts:2638` → **:3115/:3123** (the S3 null-clear law); `ops/split.ts:161` → **:181** (the S3-law destructure; first drop site :148-149); placeholder-compositor 175 LOC ✓ names spec 04 ✓ (:13) |
| §0 BASE — app row (:15) | ProgramCanvas + deliverService + the audio legs | **P2** | ProgramCanvas 242 LOC ✓, compositionParamsFromDoc ✓ (:61), the compose-then-filter grade law ✓; deliverService "344 LOC" → **418** (Z2 landed the grade at :300-324); `scene-to-segments.ts:369` → **:347**; `audioService.ts:469/:515` → **:468/:514**; **`rewireSidechains after every materializeStrips` is GONE** — M2 Wave 1 (`audioService.ts:319-331`): the app-side rewire is DELETED, the bridge (SceneMixer.materializeStrips) owns the sidechain wires on BOTH paths; mediabunny@1.50.8 ✓ |
| §0 BASE — mock row (:16) | The FX/transition mock reference | **P2** | Pin `e89cd65`/"1,521 unchanged @ R24" — HEAD now `57c7bea`, census 1,762+ it-blocks mid-flight (R25-W1 + W2b; record-not-chase per mock-verdicts §3). The 27-name `TRANSITION_PRESENTATIONS` re-counted EXACT ⭐ (mockData.ts:28-37 — 27 entries, mirrors the engine's 27 `gpuTransitionId` presentations, re-counted) |
| §0 GAP rows (:19-23) | Parity corpus, v1 exclusions, K3, K4, r6 | **P2** | Row 1 verified accurately OPEN (no S4 suite/corpus/parity row in the engine tree or PLAN @ `f9ac806` — grep-verified; N1 CLOSED as a seam, PLAN.md:122) BUT "the HANDOFF next-steps list is G1/G2/N5/P3" is STALE — the RC-round HANDOFF's list is now r1-port design round / D-ARCH-6 absorption / NS queue / M2 mixer / P3 cherry-pick (HANDOFF.md "Suggested Next Steps"); the NS-4/r1-port adjacency is unregistered (04's Row 5 twin). Row 2 SOUND (only Gaussian Blur maps, :263 honest skip — live). Rows 3-4: "174-test glue suite…@ `c885ece`" → **252 @ `c020b2a`** (8 suites — waveformPeaks + wire-coverage new); "no e2e…@ `c885ece`" → still true, re-key the pin. Row 5 SOUND (r6 non-blocking) |
| §1 Purpose / §2 Goals / §3 Architecture (:29-85) | The bridge contract statement | **SOUND** | "Renderer (see 04-renderer-color.md)" ✓; the box-structure matches the live bridge family (pure math + thin painter) |
| §4 FrameDescriptor Shape (:89-172) | The wire contract + 17 blend modes | **SOUND + P3** | Historical OpenCut-classic port (SCOUT-07-verified, repos absent); internally consistent. P3: `LayerTransform` carries no anchor/corner-pin — the engine's LIVE resolved transform carries `anchorX/anchorY` (player.ts:1773/:2203/:2457 `ax:`) + corner-pin (already noted at §12.A.1) — see RA-V1-1 (Row 11) |
| §5.1-§5.5 Composition Algorithm (:175-321) | Tree-walk, ordering, active resolution, transform, occlusion | **SOUND + P2** | §5.5's engine cites ALL EXACT ⭐ (scene-assembly.ts:1358/:1379/:1334/:1340/:1165-1169/:1180-1188 — live-read, six-for-six); §12.A.1's §5.2/§12.2 rows too (:1379, :1580 — see Row 9 for the drift). §5.4's transform section carries NO anchor field — the RA-V1-1 finding (Row 11) |
| §6 / §6.1 / §6.1A / §6.2 / §6.3 Transitions (:324-453) | The transition model + registry + pipeline | **P1** (the missing OV-05 law) + P2s | The two-tier model verified: registry.ts:30-35 (the split-as-first-design-decision block) EXACT ⭐; planner.ts:21 ⭐; handle-utils.ts:318 ⭐; 27 presentations re-counted ⭐; pipeline.ts:90/:96 ⭐; headless/api.ts:620-621/:851-860 ⭐. **Missing: the OV-05 one-input-source law** (briefing #6, this round's 07 P1 — drafted at Row 1). 07:367's three stale bits: "both engine seams…derive it from the element-anchored **sidecar**" (FALSE — the audio seam reads the ENGINE FIELD only, transition-inputs.ts:117/:123; the visual reads field BASE + sidecar OVERRIDE); "left = the last source **starting** at/before the cut" (the pre-F1 pairing — live pairs by END, transition-inputs.ts:235-246, the F1 audit fix inside the N1 landing); the OT cites :2638/:161 (Row for OT above). **The P8 bracket STANDS CORRECTLY** ⭐: "params keys are overwrite-only and the null-clear law does NOT extend to them (`types/index.ts:166-168`)" — live-verified verbatim; the fleet-r25 F12 fold (hazard-note, NOT the placement:'overwrite' phantom) is honored |
| §7 Effect Resolution (:456-568) | EffectPass model + chain ordering | **SOUND + P2 cites** | §7.2's W2.5 row cites drifted (the §0 twin); the 44-registry + honest-skip statements ACCURATE ⭐ (the wire-advertising-unimplemented-effects trap — GPU_EFFECT_TYPES api.ts:1492 with `gpu-power-window` :1529 / `gpu-secondary-qualifier` :1535 unimplemented — is **04's GAP row, correctly not duplicated here**; 07 makes no contradicting statement). §7.3's ping-pong cites EXACT ⭐ (effects/pipeline.ts:5113/:5148/:5204) |
| §8 Mask Resolution (:572-670) | Mask model + JFA | **SOUND** | Historical + spec-design; the §12.A.1 rows are where the engine cites live (Row 9) |
| §9 Audio Composition (:674-686) | The split + the UI shell refs | **SOUND** | Spec 18's inspector Effects/Transition tabs ✓ (18:228/:250); spec 15 §4.3.52/53/57/58 numbering holds ⭐ |
| §10 Caching (:689-756) | Layer texture cache + FrameDescriptor cache | **SOUND** | Spec-only design (FreeCut/OpenCut-classic historical cites — SCOUT-era) |
| §11 Open Questions answered (:760-1037) | The SCOUT-07 dossier | **SOUND (by history)** | Reference repos absent from the sandbox — internal consistency only (04's registered limitation). No corpus-internal contradiction found |
| §12 Test Plan (:1040-1062) | The architect's 9-bullet intent | **SOUND** | Unchanged seed text, no live-truth coupling |
| §12.A / §12.A.1 Code References (:1065-1176) | The nle-engine reference table | **P2** | Preamble: "458/458 @ `5036387`…54,791 LOC (62 files)…every row's file:line re-verified UNCHANGED" → **748/748 @ `f9ac806`, 57,064 LOC (63 files), 4 rows drifted** (player.ts:1321→**:1326** `_buildLayers`, :1575→**:1580** the type-dispatch line, mask-manager.ts:723→**:734** the combine call, :671→**:682** getMaskResult). 12 rows EXACT ⭐ (scene-assembly ×2, types.ts:311/:704, registry:2249, planner:21, handle-utils:318, pipeline:90/:96, effects:5113/:5148/:5204, compositor:33, transform-parent:878, texture-pool:5). Also needs the D12 ratification note (the "NOT canon / the spec wins" framing predates D12 — the engine's stack is now THE ratified render/export venue model, DECISIONS.md:521) |
| §13 Corrections (:1179-1310) | Seed-spec corrections 13.1-13.11 | **SOUND** | Historical; §13.11's 187-blend law internally consistent with §17's Tier 2 rows |
| §14-§16 Quotes + inventories (:1314-1754) | frame.rs + frame-descriptor.ts + node tree | **SOUND (by history)** | SCOUT-07 verbatim quotes; reference repos absent; no corpus-internal contradiction |
| §17 Testing (:1758-2423) | Tier 1/2/3 + property + assets | **SOUND** | The executable contract is spec-owned; cross-refs verified: 17 §13A.5 ⭐ (17:2299), 15 §4.3.61-63 ⭐ (15:1768/:1800), 16 §3.11 ✓, 00 Decision 6 ⭐ (00:277), 01 §3.6 ⭐ (01:1494/:2138), 03 §0's viewer half ✓ (03:25) |

---

## §2 The fix-list (exact edits, amendment-wave ready)

**Row 1 — [P1] §6.1A: add the OV-05 one-input-source law (the missing landed law).**
Insert after the R23 note (07:367), as a new block:

> **R27 amendment — the one-input-source law (OV-05, engine `9850231` 2026-09-13; D-T6's ruling extended to the visual seam; D12's venue framing).** The transition family has ONE authoring home: the ENGINE field `el.transitionOut` (the S3 element field — spec 09's persisted model). The visual seam's input law:
> - **BASE = the engine field.** `collectVisuals` reads every element's own `el.transitionOut` (`composition-frame.ts:294-317` `elementTransitionOut`; the collect-site override at **:331-334**) — a host that supplies NO sidecar bag still gets transitions.
> - **OVERRIDE = the consumer sidecar.** `params[el.id].transitionOut` (the D2e bag, `{durationSec, alignment?}` — already in seconds) overrides the field when present: the legacy-host compat path (a host that still mirrors the field into its bag keeps working). The sidecar is NOT a second authoring home: the app-side drop (ProgramCanvas's `compositionParamsFromDoc` still supplies it) is the registered residual — engine HANDOFF "Suggested Next Steps" #5 / PLAN's P3 register (:488).
> - **Field hygiene — the audio seam's `transitionInputsFromScene` EXACTLY** (one input source, one law; `transition-inputs.ts:117-135`): type discriminant `type === "crossfade"` (a hostile/persisted `{type: "dip"}` is not this seam's transition — skip, never render a foreign type as a crossfade; the wire validates but fromJSON does not), duration **ticks → seconds** via the ticks law (120 000/s), **finite and positive**, `alignment` carried verbatim (clamped [0,1], NaN/null → 0.5 downstream — `effectiveAlignment` :212-218, the planner's clampTransitionAlignment parity).
> - **The second authoring home is CLOSED:** mirror lag can no longer make preview and audio disagree — the N3 audio seam reads the ENGINE FIELD ONLY (no sidecar at all), the N1 visual seam reads the same field as its base. Both seams derive their pair-forms from the one field, each pairing on its own substrate (audio: left = the last source whose END is at/before the cut, right = its immediate follower, gapped followers > `CUT_PAIR_TOLERANCE_SEC` (2.0s) no-op — transition-inputs.ts:235-253; visual: the nearest earlier visual on the same track — composition-frame.ts:371-379, the same 2.0s gapped-pair guard at :397).
> - **Pins (engine, `bridge-seams.test.ts`):** :869 the field-driven window (the CROSSFADE pin's exact geometry authored on the field), :886 sidecar-override-wins (legacy compat), :901 foreign-type/non-finite hygiene skip. Engine PLAN registers OV-05 CLOSED (739/739, PLAN.md:457).

**Row 2 — [P1] §0 + §12.A.1: the Gaussian-Blur default flip (the twin of 04's Row 1).**
- 07:13: replace "`buildElementFilterString` (:223 — the honest CSS-filter subset: 'Gaussian Blur' → `blur(radius px)`, default 4, non-finite→4, negative clamps ≥ 0 (R8-REV #5 — invalid CSS would no-op the WHOLE `ctx.filter`)" with "(**:257** — the honest CSS-filter subset: 'Gaussian Blur' → `blur(radius px)`, **default 10 — the GPU registry's declared default per the F2-5 cross-module coherence pin** (the old fallback 4 previewed a blur the render never shipped; `GAUSSIAN_BLUR_DEFAULT_RADIUS` :210), non-finite→10, negative clamps ≥ 0 (R8-REV #5 — invalid CSS would no-op the WHOLE `ctx.filter`)".
- Same-frame refreshes in 07:13: "`CompositionElementParams.effects` (:119…" → "**:133**"; "`CompositionElementEffect` = `{name, params}` at :124" → "at **:138**"; "the `CompositionDrawOp.filter` field (:161)" → "(**:179**)".
- 07:1169 (the §12.A.1 W2.5 row): the same five fixes (":223"→":257"; ":119"→":133"; ":124"→":138"; ":161"→":179"; "default 4, non-finite→4"→"default 10 (F2-5), non-finite→10"; "(:229)"→"(:263)").

**Row 3 — [P2] §0 engine row: the census + the "only commits" claim + the RR1-A name.**
- "the bridge file's census: **64 → 97 → 113 pins** (18 → 21 describes…; +16 to 113 @ R24 from the W2.5/R8-REV rounds — the only commits to touch the file since the R23 pin; the three new `W2.5` describes (:921 the filter-string law, :970 the op threading, :998 the painter call-order)…" → "the bridge file's census: **64 → 97 → 113 → 151 pins** (18 → 21 → **33 describes**; +38 to 151 @ R27 from the F2-5/F2-6/F2-7 hardening + the P1 opencut-laws consolidation + OV-05 — five commits (`c8a4d00`/`02b0976`/`1c04887`/`2b2de33`/`9850231`) touched the file since the R24 pin; the W2.5 describes now at **:980 (filter-string) / :1045 (op threading) / :1073 (painter call-order)**, the OV-05 pins at :869/:886/:901, the F2-6 exception-bracket describe at :1136, the F2-7 probe describe at :2208…".
- "`transition-inputs.ts` `clampFadeToElementSpan`" → "`transition-inputs.ts` `clampFade` (:186 — fades clamp to each element's own span; the name `clampFadeToElementSpan` appears nowhere in the live history)"; optionally append the one-law note: "the AUTHOR-fade clamp is the shared `clampAuthorFadeToSpan` (audio/audio-scene.ts, imported at scene-to-segments.ts:68, applied :475-476 — one law at both native twins)".

**Row 4 — [P2] §0 app row: the M2 Wave 1 sidechain story + the Z2/LOC refreshes.**
- "344 LOC" → "**418 LOC** (Z2 landed: the scene grade reaches the EXPORT, deliverService.ts:300-324 — the same final-pass law)".
- "`buildAudioSegments` (`scene-to-segments.ts:369`) consumed with mid-entry math (`audioService.ts:469/:515`)" → "(**:347**)…(**:468/:514**)".
- "the ducking-sidecar fold (`insertEffects` → the external-sidechain compressor mapping; `rewireSidechains` after every materializeStrips, over WDC's exported `collectSidechainWires` @ `85b81b0`); `foldSidechains` as named in the charge does NOT exist in any live repo — the verified surface is the fold/rewire pair above" → "the ducking-sidecar fold (`insertEffects` audioService.ts:109/:161) + **M2 Wave 1's single-owner law: the app-side `rewireSidechains` is DELETED — the bridge (SceneMixer.materializeStrips) collects the sidechain wires from the strips' own `sidechainSource` declarations (the shared `collectSidechainWires` contract) on BOTH the realtime and offline paths** (audioService.ts:319-331); `foldSidechains` as named in the charge does NOT exist in any live repo — the verified surface is the insertEffects fold + materializeStrips's internal wire collection".
- "the app's composition consumers re-verified @ `c885ece`" (Status header :4) → "@ **`c020b2a` (252/252, 8 suites)**".

**Row 5 — [P2] §0 GAP row 1: the parity-corpus re-key + the NS-4/r1-port adjacency (04's Row 5 twin).**
"STILL OPEN @ `5036387` (re-checked @ R24 per the engine card: the HANDOFF next-steps list is G1/G2/N5/P3 — no parity row)" → "STILL OPEN @ **`f9ac806`** (re-checked @ R27: the RC-round HANDOFF's next-steps list is now the r1-port design round / D-ARCH-6 absorption / the NS queue / the M2 mixer / the P3 cherry-pick — STILL no parity row; N1 remains CLOSED as a seam, PLAN.md:122). *(R27 register: the engine's r1-port design round (HANDOFF #1) carries SA-1's NS-4 — the SceneTracks→GPU render path, "either a SceneTracks→render-plan adapter or a new compositor input contract over SceneTracks" (PLAN.md:475) — as a design input; the corpus's target contract may be reshaped by that ruling.)*"

**Row 6 — [P2] §0 OT row: the pin + the two drifted law cites.**
- "`opencut-timeline @ ded43c4` — 536/536 (code pin `c15a629`…)" → "**@ `55c81c0`** — **632/632** (code pin **`970948a`**; the D-ARCH-6 verbs landed — 31 = 28 routed + 3 exceptions; the composition consumers re-verified live against the OT repo itself this round)".
- "(`ops/timeline-core.ts:2638` — `transitionOut: null` DELETES the key…; ref refreshed from `:2613` @ `222532c`)" → "(**`ops/timeline-core.ts:3115-3123`** — the S3 null-clear law…; ref refreshed from :2638 @ the R24 pin)".
- "(`ops/split.ts:161` — the out-boundary belongs to the right half)" → "(**`ops/split.ts:181-183`** — the S3-law destructure (the first drop site :148-149); the out-boundary belongs to the right half)".

**Row 7 — [P2] §6.1A's R23 note (07:367): the three stale bits (beyond Row 1's insert).**
- "the RIGHT member is derived by pairing (left = the last source **starting** at/before the cut, right = its immediate follower" → "left = the last source whose **END** is at/before the cut (the F1 audit fix — pairing by start shifted the pair one boundary right on any track with ≥3 elements), right = its immediate follower".
- "The pair-form above is the composition runtime's RESOLVED form — both engine seams (N3 audio + N1 visual) derive it from the element-anchored sidecar" → "…derive it from the **ENGINE FIELD** (N3 audio: the field only — no sidecar; N1 visual: the field as BASE, the sidecar as OVERRIDE — the OV-05 law above)".
- Optionally append the model-home cross-note: "(The persisted/wire shape is 09's element-anchored field — `ElementTransition {type, duration, presentation?, alignment?}`; THIS interface is the derived pair-form: its `id`/`timing` are runtime conveniences, not persisted fields. See 09's R27 field-inventory ruling: `transitionIn` retires, `TransitionJSON` re-shapes to the element-anchored field.)"

**Row 8 — [P2] §6.1A (or §6.2): register the A/V divergent-floors law (F2, audited).**
Append to §6.1A: "**Divergent floors, by design (F2 audit):** the VISUAL seam always renders the full descriptor window (video "handles" are decode pre-roll, not clamped media spans — the consumer's resolver owns media bounds); the AUDIO seam degrades to a hard cut without handles; a descriptor on a GAPPED boundary (the follower more than `CUT_PAIR_TOLERANCE_SEC` = 2.0s past the left's end) is a no-op on BOTH sides (composition-frame.ts:58-69/:196/:397; transition-inputs.ts:158/:251). The sidecar `speed` is VISUAL-ONLY — consumers mirror it into the opencut `retime` or A/V drifts." (The first two sentences' facts are new to the spec; the last is §0's row 2 — keep one home, cross-ref.)

**Row 9 — [P2] §12.A.1: the preamble + the four drifted rows.**
- Preamble (:1150-1154): "458/458 vitest, tsc 0 @ `5036387`" → "**748/748, tsc 0 @ `f9ac806`** (docs-only head over code anchor `50b91f5`)"; "54,791 LOC under `src/lib/nle` (62 files)" → "**57,064 LOC (63 files)**"; append to the R24 note: "*(R27 re-verify: the E1/E2 + RC/OV waves moved the GPU files — 4 rows below drifted (player :1326/:1580, mask-manager :682/:734), 12 re-verified exact; the bridge family itself gained the F2-5/F2-6/F2-7/P1/OV-05 waves — see §0's census row)*".
- Row §4 FrameDescriptor purity: "`player.ts:1321`" → "**:1326**".
- Row §12.2: "`player.ts:1575`" → "**:1580**" (the dispatch line; the Wave 4D-B text-layer cite :1557 still reads as the adjacent comment — re-anchor if quoted).
- Row §8.3 chaining: "`mask-manager.ts:723`" → "**:734**".
- Row §8.3 ownership: "`mask-manager.ts:671`" → "**:682**".
- Add the D12 ratification note to the preamble: "*(R27: engine DECISIONS.md D12 (`:521`) RATIFIES the spec's Decision 12/16 junction — the engine's stack is the render/export VENUE model, opencut the editing engine; the 'in-between reference' framing below predates the ratification and reads now as 'the venue-side reference implementation + the r1 port's parity twin')*".

**Row 10 — [P2] §0 mock row: the record-not-chase re-key.**
"`ui-mock/shell-variants` @ `e89cd65` — **1,521 tests** (unchanged @ R24 — no variants pin this round…)" → "@ `e89cd65` — **1,521 tests at the R24 pin** (R27 record-not-chase: HEAD now `57c7bea`, census re-read 1,762+ it-blocks mid-flight — the R25-W1 insert rescue + W2b landed; per the mock-verdicts §3 coordination law the register re-keys at the sibling's round-WRAP; the 27-name list + the drag-to-clip payload contract re-verified standing this round)".

**Row 11 — [P2] §5.4 + §12.A.1's transform row: register RA-V1-1 (the anchor fields) — the spec corpus's ONLY transform-resolution hook.**
- §12.A.1's §5.4 row (transform-parent.ts:878 — cite EXACT ⭐): append to the note: "*(R27: the authoring transform carries `anchorX`/`anchorY` (local item px, resolver default `?? width/2, height/2` — transform-parent.ts:139-147; the GPU consumes `resolved.anchorX` at player.ts:1773/:2203/:2457). RA-V1-1 (`d0906a8`, FW-E1): the anchor fields JOIN the four transform-field lists — the patch gate (timeline.ts:255-265 NUMERIC_TRANSFORM_PATCH_FIELDS), the hydration normalizer (persistence/index.ts:662-672), the keyframe lanes (keyframe-store.ts:561; core/keyframes.ts:181), the wire zod (headless/api.ts:1561/:1623) — closing the garbage-override hole (`??` passes 'abc'/NaN through → NaN anchor binding at the GPU, silently). §5.4's resolved LayerTransform derives center from the anchor×scale binding; anchor/corner-pin are engine-resolved-transform fields beyond 07's OpenCut-port shape.)*".
- Optional §5.4 one-liner: "the engine's authoring-side transform additionally carries anchorX/anchorY + cornerRadius/corner-pin (see §12.A.1's §5.4 row)".

**Row 12 — [P2] §0 engine row: the R9-c re-filing register (04's Row 4 twin).**
Append to the grade-DECLINE sentence: "*(R27 note: the engine's PLAN re-filed a seam-side final-pass proposal (R9-c, PLAN.md:156-168: `paintCompositionFrame(ctx, ops, media, { grade })`) — the compose-then-filter ORDER is preserved, the venue moves consumer→seam; the D29.5(c) decline stands until that proposal is ratified as its evolution or rejected. The app's Z2 export-side fix LANDED — deliverService.ts:300-324.)*"

**Row 13 — [P3] §0 acceptance row (:25): "battery_r24's posture checks" → "battery_r27's" (the fleet's battery re-keys each round); the Status header's "Round 24" story gains the one-line R27 re-verify note (Row 4's app pin + engine/OT pins).

---

## §3 Focus-item verdicts (the six assigned questions)

1. **The OV-05 one-input-source law (this round's 07 P1).** CONFIRMED P1 — the law is entirely absent from 07 while both mock-color's §5 placement ruling and spec-04's §3.2 determination name 07 §6.1A as its home (and 04's own text carries only the pointer). Engine ground truth, live-read in full: composition-frame.ts:39-48 (the header law, "the second authoring home is closed"), :294-317 (`elementTransitionOut` — type discriminant `"crossfade"` at :310, ticks→sec via `mediaTimeSec` :311, finite/positive :312, alignment verbatim :315), **:331-334** (inside `collectVisuals` :319-349: `transitionOut: params[el.id]?.transitionOut ?? elementTransitionOut(el)`), the pairing :371-379, the window :352-360; pins ×3 at bridge-seams.test.ts:869/:886/:901; the audio seam's field-only read at transition-inputs.ts:117-135 (same hygiene, verbatim parity); engine PLAN.md:457 (CLOSED, 739/739) + :488 (the app-side sidecar-drop residual — ProgramCanvas.tsx:66-68 still supplies the legacy bag). **Drafted at Row 1.**
2. **The transition family.** (a) The P8 bracket at 07:367 **stands correctly** — "params keys are overwrite-only and the null-clear law does NOT extend to them (`types/index.ts:166-168`)" is live-verbatim; the fleet-r25 F12 fold (hazard-note, NOT the placement:'overwrite' phantom) is honored by the current text. (b) The two-tier model verified at every cite (registry.ts:30-35/:2249 + 27 presentations counted via `gpuTransitionId`; planner.ts:21; handle-utils.ts:318; api.ts:620-621/:851-860). (c) The field's MODEL home: 09's R27 audit retires `transitionIn` + re-shapes `TransitionJSON` to the element-anchored `ElementTransition`; 07's pair-form interface is safely bracketed as the RESOLVED form by the R23 note but needs the explicit model-home cross-note (Row 7) so the amendment waves don't read 07's `leftElementId/rightElementId/id/timing` as persisted fields.
3. **The visual layer inventory.** `collectVisuals` (:319-349): main-first then overlays in REVERSE array order (overlay[0] topmost — consistent with 07 §5.2's "overlay-2 first, then overlay-1" via the OT `.reverse()`); video/image/text only (`isVisual` :276-278); hidden-element skip :323; trackHidden deferred to the op loop :383; visibility half-open with EPS :422; alpha = opacity × in-ramp × out-ramp :426-430 (0-alpha omitted). The 44-effect registry: **count EXACT** (43 ids + gpu-lut); 07's statements (":13 the 44-effect GPU registry owns their fidelity", :1169 same) ACCURATE; the wire-trap (GPU_EFFECT_TYPES api.ts:1492 advertising `gpu-power-window` :1529 + `gpu-secondary-qualifier` :1535, unimplemented) is 04's GAP row — 07 correctly does not duplicate it and makes no contradicting statement.
4. **The element-filter/effects sidecar + the parity corpus.** `buildElementFilterString` (:257-274) live-read: Gaussian-Blur-only mapping, default 10 (F2-5), negative ≥0 (R8-REV #5), join-with-spaces, honest skip, identity '' — all as 07 describes EXCEPT the default (Row 2). The painter's F2-6 try/finally brackets (:497-510/:521-529) + F2-7 videoWidth/videoHeight probes (:541-552) are landed hardening 07 doesn't yet cite (fold into Row 3's census sentence). The parity corpus: verified accurately OPEN (no S4 suite/corpus/parity row anywhere in the engine tree or PLAN; HANDOFF's list changed shape but still carries no parity row) — Row 5's re-key + the NS-4 adjacency.
5. **The anchor-field hardening (RA-V1-1).** 07 does NOT carry it — and **neither does any other spec** (grep across the corpus: zero `anchorX` mentions in all 21 specs + the fleet-r27 audits). The hardening is real and five-site: timeline.ts:255-265, persistence/index.ts:662-672, keyframe-store.ts:561, core/keyframes.ts:181, headless/api.ts:1561/:1623 (landed FW-E1 `d0906a8`). 07's only hook is §5.4 + the §12.A.1 §5.4 row (transform-parent.ts:878 — cite EXACT) — Row 11 registers it there; the field-inventory home belongs to 09/06/15 (cross-spec note).
6. **The pre-R26 pins + cross-refs (spot-verified 14).** VALID+EXACT ⭐: 15 §4.3.61-62 (15:1768/:1800), 17 §13A.5 (17:2299), 00 Decision 6 (00:277), 01 §3.6 (01:1494), 18's inspector tabs (18:228/:250), 03 §0's viewer half (03:25), 04 §0's parity twin, OT types/index.ts:145/:190/:166-168/:231-238, scene-assembly ×6 (:1358/:1379/:1334/:1340/:1165/:1180-1188), registry/planner/handle-utils/pipeline/api ×7, effects/pipeline ×3, compositor:33, transform-parent:878, texture-pool:5, the mock's 27-name list. STALE (fixed): the comp-frame line family (:119/:124/:161/:223/:229/:921/:970/:998 — the corpus-wide ":70-94→:100+" drift: the transform slots now :79-80/:105-107, the type-only import :113 — also hits IMPLEMENTATION-PLAN.md:20/:33 and spec-19's :100, cross-spec), the OT :2638/:161 pair, the four §12.A.1 rows, the app :369/:469/:515 family, the pin world itself.

---

## §4 Cross-spec notes (for the cross-cut wave — not my edits)

- **04**: (a) Row 2/Row 3 are the twins of 04's Row 1 (same F2-5 flip, same :223→:257 family) — apply together; (b) 04's N1-row pointer ("law text at 07 §6.1A") depends on Row 1 landing; (c) the R9-c re-filing register (my Row 12) mirrors 04's Row 4(b) — one story, two venues' rows.
- **09**: the model-home rulings (transitionIn retire; TransitionJSON → the element-anchored `ElementTransition`; the field inventory's anchor gap) — 07's Row 7 cross-note keeps the two specs telling one story: 09 owns the persisted shape, 07 owns the resolved pair-form + the seam laws. 09 should cite the OV-05 law's home (07 §6.1A) in its transitionOut row.
- **15/06/05**: the RA-V1-1 anchor fields joined the wire zod (15), the patch gate (06), and plausibly the keyframe-gesture surface (05) — no spec carries them; the field-inventory amendment should pick one home (09 B2 per its audit's framing) and the others cite it.
- **19**: its "type-only sites" convention row (composition-frame :100→:113) is the same drift family as my Row 3; the opencut-laws VALUE-import amendment (:60-61) makes the bridge convention "type-only ×4 + VALUE leaves ×2" — 07's §0 N1 description says "type-only imports" (via 07:13's implicit convention) and could take the one-phrase amendment.
- **IMPLEMENTATION-PLAN.md**: :20/:33 cite "composition-frame.ts:70-94" (the transform-sidecar slots) — now :79-80/:105-107; the R9-a future-extension row's evidence line needs the same re-anchor when the plan is next touched.
- **18**: the inspector Effects/Transition tabs + the mock `data-tab` refs verified standing — no action.
- **Briefing facts check for 07**: #1/#2 (wire census) — no exposure (grep-verified: 07 carries zero verb-count text); #3 (preservePitch) — no exposure; #6 (OV-05) — the P1 (Row 1); #7 (WDC diagnostics) — not 07's surface; #10/#11 (mock-color/mock-verdicts) — no 07-side exposure (the curve-order conflict + venue table are 08's; the GRADE decline consistent); #13 (D12) — Row 9's note; #14 (NS queue/r1-port) — Row 5; #15 (maintainPitch) — no exposure.

## §5 What 07 uniquely sees (the fleet-only findings from this audit)

1. **The OV-05 law's full text is 07's to carry** — the fleet's two placement rulings (mock-color §5, spec-04 §3.2) both point here, and the R23 note's "sidecar" framing is the exact sentence the convergence inverted: 07 is the only spec whose text now says something FALSE about the transition input source (the audio seam reads the field ONLY; the visual reads field-BASE + sidecar-OVERRIDE).
2. **The `clampFadeToElementSpan` phantom-name** — cited at 07:13 (and in the R23 report), the name exists NOWHERE in the engine's live history (`git log -S --all` empty); the real law is `clampFade` (transition-inputs.ts:186) + the shared `clampAuthorFadeToSpan` for author fades. A citation that was wrong at its own pin — found only from 07's angle.
3. **The F2-5 default-radius flip's second exposure** — 04's Row 1 filed the renderer-side; 07:13/:1169 carry the identical "default 4" text, and 07 is the spec the F2-5 pin's coherence argument (preview must ship the render's default) is law for.
4. **The M2 Wave 1 sidechain story's app-side half** — 07:15's "rewireSidechains after every materializeStrips" is now the only spec text describing a DELETED function as live (spec-19/20 carry the engine-side bus law; the deletion's app-side fact is 07's consumer row's to carry).
5. **RA-V1-1's absence is total** — zero `anchorX` mentions across the whole 21-spec corpus; 07's §5.4/§12.A.1 is the only transform-resolution hook that can register it without a new home being chosen first (Row 11 buys the amendment wave the time).
6. **The A/V divergent-floors law (F2) has no spec home at all** — the engine's header states it (visual full-window vs audio hard-cut degradation + the 2.0s gapped-pair no-op); 07 §6.1A is its natural home (Row 8) and no other fleet report surfaced it.

*Report ends. Research-only: no repo files modified outside this report + the worklog; no commits/branches (watchdog respected).*
