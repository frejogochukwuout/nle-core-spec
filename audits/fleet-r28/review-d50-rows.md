# The W3 Adversarial Review of D50 (the page shape + FX grammar) + the ruling-rows R16-R21

**Task ID:** R28-W3-6 · **Agent:** review-d50-rows (adversarial, seal round) · **Date:** 2026-09-15
**The artifacts:** `audits/ARCH-R28-seal-round.md` §4 — **D50** (FIVE PAGES, two pinned as modes) + the ruling-rows **R16** (register re-key) / **R17** (pin re-base) / **R18** (CR-F1/F4 law absorb) / **R19** (E1-a/E1-b) / **R20** (A3-wheels) / **R21** (plan lane seal) — drafted on `audits/fleet-r28/research-pagefx.md` + `scout-register.md` + `scout-engine.md` (+ `scout-app.md` for the R17 split).
**Method:** fresh context; every check re-derived live against the read-only trees at `/home/z/my-project/*` (engine HEAD `074a2f6` ✓, code anchor `74bef08` ✓; WDC `83b8850` ✓; app `85cff80` ✓; nle-ui `32abd58` ✓; OT `55c81c0` ✓; the mocks + the spec corpus at the working tree). Report-only: no corpus edits, no commits. One output file (this one) + the worklog append.

---

## §1 THE VERIFICATION SWEEP — 14 spot-checks (all re-derived live)

| # | The claim under test | The live evidence | Verdict |
|---|---|---|---|
| 1 | **18 §4.8's current three-page text** (what exactly it says) | `18-ui-shell.md:221`: "page dock — **three pages, not seven**" — `Edit` / `Color` / `Deliver`, with the divergence documented **as three-way** (this spec's 3 vs "the BASE nle-ui's 4 (+Audio — `Page = 'edit'\|'color'\|'audio'\|'deliver'`…)" vs "the variants mock's 5 (+Audio +FX) — registered OPEN, D35.4; a future design round's call"), and "Dropped pages: Media, Cut, Fusion, Fairlight (§8.2-8.4)". Supporting rows verified: `:33` (§0's OPEN registration), `:41` (§2.2 "the 7-page dock collapses to 3 pages"), `:111` (§3.1 diagram "page dock (Edit/Color/Deliver)"), `:4` (the v1.7 status header carries the OPEN registration too), `:388-394` (§8.2-8.4/§8.8 the removal ledger + the R27 REGISTERED DIVERGENCE). | **VERIFIED** — the research pack's §1.1 cites are exact |
| 2 | **16's ⌘3 row today — the collision** | `16-keyboard-shortcuts.md:367`: "\| `Cmd+3` \| Switch to **Effects workspace** \| (UI state) \| Always" (and `:368` `Cmd+4` "Audio workspace"); the PENDING reconciliation note at `:378` ("this table's `⌘3` = 'Effects workspace' **collides with the live `⌘3` = Deliver**… resolution rides the seal round"); §0's K3 row at `:28` carries the same PENDING; App A's twin at `:2276` (`kbd-workspace-effects \| Cmd+3`). | **VERIFIED** — the collision is exactly as the ruling states |
| 3 | **The mock's one-flag law** (fxMode — the implementation) | `shell-variants/src/state/useUiStore.ts:563-567`: `fxMode: boolean` — "written ONLY by setTool (fx tool ⇔ fxMode off the FX page) and setPage"; the writers: setPage `:1273` (`fxMode: p === 'fx'`) + the exit laws `:1288` (fx-selection domain dies on page-exit, scope `page === 'fx' \|\| fxMode`) and setTool `:1436-1437` (`fxMode: s.page === 'fx' ? true : t === 'fx'`). Two doors, one engine — exactly the ruling's law. | **VERIFIED** |
| 4 | **The app's real current pages + the 4-page union claim** | `nle-test-app/src/App.tsx:15,52` renders nle-ui's `<AppShell />`; nle-ui `src/state/useUiStore.ts:17`: `Page = 'edit' \| 'color' \| 'audio' \| 'deliver'` — **the 4-page union is nle-ui's** (the accepted BASE); nle-ui's `AppDock.tsx` renders exactly 4 tabs; no `FXPage`/`AudioPage` components anywhere (grep-verified — the audio page is AppShell branches). The app's live dock TODAY = the 4-page BASE grammar; the FX page exists **only in the variants mock**. | **VERIFIED** — the research pack's §2.2 is honest about this |
| 5 | **The register rows 7/8/20's current text** | `REFERENCE-REGISTER.md:30` (row 7, FX page): PROPOSAL (XMOCK-6 — QUEUED to the page-shape design round), MOCK-IS-REFERENCE; `:31` (row 8, page system): "**MOCK-YIELDS-REGISTERED** (…20's M2-remainder owns the real mixer surface at r2; the page-inventory amendment rides the OPEN ruling)"; `:43` (row 20, mixer family): MOCK-YIELDS-REGISTERED "rides row 8's 3-vs-5 OPEN decision; 20's M2-remainder owns the real mixer surface at r2". The OPEN table `:52-56`: 5 rows (marker v2 / captions / 3-vs-5 / FX grammar / A3-wheels-RULED). | **VERIFIED** |
| 6 | **The battery's register checks** (the line-start method + which checks the re-key affects) | `scripts/battery_r27.py:299-309` `_mock_it_census` — the line-start method (stripped line starts `it(`/`it (`); `:317-318` the pin-line regex (the register is the authority, parsed live); `:335` **the markers check requires BOTH "REGISTER-PENDING" AND "WRAP" as substrings of the register**; `:336-337` zero-shrinkage (live ≥ declared); `:596-615` the 8-family pin spot-checks (drift-tolerant — "recorded, WRAP-gated"); `:588-591` the OPEN-table check: **5 rows + `reg.count("QUEUED-design-round") >= 4`** + "RULED (R27". Live re-derivation (battery's own method, this session): variants **1,939 / 68** ✓, mini **495 / 12** ✓; the 11-test gap arithmetic re-derived exactly (REPLAY_CASES 7 entries at insertPlan.test.ts:447-455 + `it.each(` at :458 — 0 line-start, 7 runner; AA5_SITES 5 entries at appLayers.test.ts:122 + 1 line-start `it(` at :132 — 5 runner; 1,939+7+4=**1,950** exactly). | **VERIFIED** — R16's headline figures are exact |
| 7 | **spec-20 §5 law 10 + the "5 pins" cite** (R18's target) | `20-audio-core.md:129` (law 10): "…with the **per-wire identity diff as the sever law**; realtime and offline share the ONE wiring… Pinned by m2-wave1 F-2 (**5 pins**)"; the matrix row `:189`: "m2-wave1 (5 pins: connect/re-point/prune + the REAL offline duck + control)". Live at `nle-engine` HEAD: `m2-wave1-mixer-surface.test.ts` = **10 its** (describe census re-derived: F-1 bus law 4 + F-2 wiring 4 + F-2 parity 2 → **F-2 = 6**); the unconditional-connect fix + the CR-F1 regression pin are in the file (`:246`, `:258`, `:362`); the "9 pins" cites at `20:20`/`:149` are now 10. | **VERIFIED** — the amendment form fits; two count families are stale (5→6 AND 9→10) |
| 8 | **R17's engine split** (HEAD vs code anchor vs the app's vendor pin) | Live git: engine HEAD `074a2f6` (SKILL laws 92-95, docs) over `74bef08` (the CR-F1/F4 code commit) with `e7cdede` (CI fix — **deletes `package-lock.json`, a tracked-tree change: infra/hygiene, NOT docs-only** per scout-engine §4) + 3 docs + 1 net-zero merge between; **25 test files** (4 top-level + 21 engine); the 749 arithmetic re-derived exactly: **665** line-start `it(`/`test(` declarations + 24+14+9+37 loop expansion (nan-guard `nonFinite` 12 its×3 values=36, `patchRows` 15, `transformRows` 10, locked-track `rows` 38 — all four loop bodies read and counted) = **749**. The app's `git submodule status`: **engine `74bef08`** / nle-ui `83ff8a8` / WDC `ec8fd5c` (+ OT mirror lock `6e2b91a` in UPSTREAM.lock.json) — the app vendors the CODE ANCHOR, one step behind the engine HEAD, exactly as R17 records. | **VERIFIED** — the split is correctly represented |
| 9 | **R17's WDC + app figures** | WDC HEAD `83b8850`; `git diff --stat ec8fd5c..HEAD` touches **only** HANDOFF.md + worklog.md (+32/−3) — docs-only ✓; UPSTREAM.lock `f5011b3` present ✓. App HEAD `85cff80` ✓; the 252/252 roof re-derived (GluedShell 104 + audioService 42 + sceneBridge 41 + deliverService 20 + persistenceService 20 + wire-coverage 9 + engineSeam 7 + waveformPeaks 9 = 252) ✓; census register `docs/port-census.md:29-37` = 42 = 36 zero-action (6 byte + 30 mechanical) + 5 carriers + 1 host ✓ (scout-app's live `census-check.mjs` run: register-equality OK). | **VERIFIED** |
| 10 | **The per-page view-state memory — per-PAGE or per-MODE?** | `useUiStore.ts:748` `pageTimelineView: Record<Page, PageTimelineView>` — keyed **per page, five entries** (seeded `:1249-1252`); `resolveTimelineCompactScope` `:259-261`: `if (s.page === 'fx') return 'off'` — **page-scoped, NOT fxMode-scoped**; the ViewOptionsPopover's Compact control is "DOM-ABSENT on the FX page" (`ViewOptionsPopover.tsx:28-30`: "the resolver ignores the entry there, so no control may claim it"). | **VERIFIED** — with the scope wrinkle filed as §2-F3 |
| 11 | **The mock's dock + keymap** (5 tabs or 4+mode?) | `AppDock.tsx:11-27`: PAGES = edit/color/**audio**/**fx**/deliver — **5 tabs**, dock order Edit/Color/Audio/FX/Deliver (⌘1/⌘2/⌘4/⌘5/⌘3); the Audio tab is a **TOGGLE** (`:57-60`: re-clicking the active audio tab calls `exitAudioFocus()`, all others `setPage`). `shortcutMap.ts:92-97`: "⌘1 / ⌘2 / ⌘3 — Switch page: Edit / Color / Deliver", "⌘4 — Toggle Audio focus", "⌘5 — FX page" (+ Esc exits). nle-ui's twin (`shortcutMap.ts:98-99`) matches minus FX. | **VERIFIED** |
| 12 | **The riders' mock evidence** (FxBrowser grammar, ruling 22, the mixer law) | `FxBrowser.tsx:41-55`: SECTIONS = Effects / Video Transitions / Fades; FADE_ROWS = 6 presets (Fade In/Out 0.5/1/2s); ruling 22 at `useShortcuts.ts:207-231` verbatim (the selected FX object deletes FIRST, scope `page === 'fx' \|\| fxMode`); the mixer-toggle audio-only law at `useUiStore.ts:1294-1296` ("the exit law keeps every console closable on the page that owns it (audio)"). | **VERIFIED** |
| 13 | **R21's targets** (the plan's user-gated mentions) | `IMPLEMENTATION-PLAN.md` carries exactly **3** user-gated mentions: `:25` (the S-spec row item (5) "the user-gate preparation"), `:72` (the enumerated user-gated list — five points, each now ruled: (1)→D46, (2)→R15c, (3)→R15a, (4)→R15b, (5) already D37/D39), `:83` (the §2 flow row). The battery's plan checks (:83-94) are structural (entry table, taxonomy, ladder, gate classes) — the R21 fold breaks none. | **VERIFIED** |
| 14 | **R19/R20's targets** (06 §5.0's residue row; the register's A3 row) | `06-nle-ops.md:439` (E1's GAP row) lists the E1-a..d residues verbatim ("the companion-track downstream non-ripple; the locked-companion closure gap; the N-group orphaning; the legacy-path (originId) fan-out absence") — D42's riders 1-2 amend exactly these; E2-a's residue at `:1591`. `REFERENCE-REGISTER.md:56` (the A3 row) already reads "**RULED (R27 — D39: WIDEN-VIA-PROJECTION)** — … The model question is CLOSED; r3-entry gates the parity fixtures' consumption" — R20's "the register's row text already reflects this" is accurate. | **VERIFIED** |

**Sweep result: 14/14 verified, zero fabrication in the research pack or the scouts.** The attacks below are therefore about what the ruling and rows **leave unsaid**, not about what they say.

---

## §2 THE ATTACK ON D50

**F1 — STRONGEST FINDING: the ⌘1-⌘9 space has TWO MORE claimants 16's amendment map misses — D50 fixes the ⌘3 collision but leaves a three-way collision standing.**
Grep the full Cmd+number space in 16 (not just §3.8):
- `16:417` (§3.11 Effects/Color): "`Cmd+1`–`Cmd+9` | **Switch color grading panel** (Lift/Gamma/Gain/Saturation/etc.) | (UI only — panel focus) | **Always**" — a hard **Always**-context collision with ⌘1-⌘5 page switching.
- `16:621` (§5.4): "`Cmd+1`–`Cmd+9` **(timeline focused)** | Focus track N directly" — a context-scoped collision.
- `16:644` (§6.1 conflict table row 11): resolves only §3.8 vs §3.11's *presets* ("Workspace = `Cmd+1`–`Cmd+9`. Effect presets = `1`–`9` (no modifier)") — it does **not** name the color-panel row (:417) or the track-focus row (:621), i.e. the conflict table was already incomplete pre-D50.

D50's map touches 4 sites in 16 (§3.8 table :365-368, §3.8's note :378, §0's K3 :28, App A :2274-2277) — none of the three. Post-D50, 16 reads ⌘3=Deliver (Always) **and** ⌘1-⌘9=color panels (Always) **and** ⌘1-⌘9=track-focus (timeline focused): a self-contradiction the corpus's own §6 preamble forbids ("Every conflict is resolved explicitly"). The live trees already answer (the mock's shortcutMap has no color-panel or track-focus ⌘ chords — page keys win unconditionally), so the fix direction is known and cheap: re-home both rows (the color-panel set to a color-page-scoped or ⌥-form grammar; the track-focus set likewise or registered) and widen §6.1 row 11 to name all three claimants. **Fix (amendment 1):** add 3 sites to the 16 list — map 20→23 (17 spec-corpus), one W4 commit unchanged.

**F2 — the Audio page-vs-mode duality: COHERENT (attack fails — but the rewrite must carry the anchor sentence).**
Is "Audio = a focus mode" compatible with "five pages"? Yes — verified as the mock's own exact law: `'audio'` is a **Page-union member** (nle-ui `useUiStore.ts:17`; the variants' five-entry `pageTimelineView` — the per-page view-state memory *needs* the page key: the audio page's compact default is `'video'`) while its **activation grammar** is a focus mode (the dock tab toggles — AppDock.tsx:57-60; Esc exits; `enterAudioFocus(trigger, trackId?)`/`exitAudioFocus`; the R13 re-entry law never resets user state). Every page-scoped law keys on `page === 'audio'`; every activation law keys on the toggle grammar — no consumer needs to choose. The dock shows **5 tabs** (not 4+mode). D50's "FIVE PAGES, two pinned as modes" is this law, and 18 §4.8's rewrite should carry `docs/DESIGN-audio-mode.md:11` verbatim as the coherence anchor ("No Fairlight-style hard page split… ONE timeline, ONE clock, ONE command stream") — the research pack's recommendation already does. No amendment beyond confirming the sentence lands in the W4 text.

**F3 — the FULL-Timeline density exemption: per-PAGE, not per-fxMode — one precision clause needed.**
Does the FX page show a DIFFERENT timeline? No — the same FULL Timeline mounts; only the compact strip never does (the resolver forces `'off'`, ViewOptionsPopover's Compact control is DOM-absent). The per-page memory covers it: the exemption is expressed AS a per-page fact. **But** the scope is `page === 'fx'`, NOT `fxMode` (useUiStore.ts:259-261): the **Edit-page FX-tool door does not get the exemption** — with the fx tool active on Edit and compact `'all'`, the frozen strip mounts while the fx engine is live (the "two doors, one engine" law is about the fx-selection/transition engine, not density; the seam-hit-zone rationale is page-scoped in the mock). D50's own text is faithful ("the FX-page timeline"), but rider (4)'s "fx forces 'off'" can be misread as mode-scoped at the port. **Fix (amendment 2):** one clause in rider (4): "the exemption keys on `page === 'fx'` — not fxMode; the Edit-page FX tool keeps the Edit page's density entry (the mock's resolver, useUiStore.ts:259-261)".

**F4 — the 20-site amendment map vs the 07/18 grep: one status-header sweep + one §4.11 phrase.**
Verified against the corpus: the map's 9×18 sites cover :33/:41/:111/:219-221/:245-253/:199/:217/:388-394/:6.4 — but `18:4` (the v1.7 status header's "the 3-vs-5-page divergence registered OPEN (D35.4…)" mention) rides only if the version bump sweeps it (natural, but unstated), and `18:248` (§4.11's "the keyboard route is `Option+2` (…⌘1–⌘4 stay page switches…)" — the "⌘1–⌘4" phrase must become "⌘1–⌘5") belongs in site 5's edit family. 07's page mentions beyond §6.3:348 are the BASE mock pin row `07:16` (see F-R16-2 below — R16's territory). **Fix (amendment 3):** name both in the W4 sweep.

**F5 — the DeliverPage/mixer row-20 coupling: RESOLVES (attack fails).**
Row 20's law is "rides row 8's 3-vs-5 OPEN decision; 20's M2-remainder owns the real mixer surface at r2". D50 gives the mixer a **home** (the Audio focus-mode page: ChannelEditor rail + Sound Library dock + MixerDock — audio-page-only per the verified R23-WB law at useUiStore.ts:1294-1296) while 20 §7 M2 keeps the **surface** ownership (the real parametric EQ/reverb/PDC capabilities) at r2. Home vs surface is a clean split; the audio-page-only mixer-toggle law (verified live) is exactly what's being specced; row 20's re-key (map site 16) says exactly this. The C40 strip-chrome queue rides unchanged. No amendment.

**F6 — "matching the accepted BASE (nle-ui's 4-page union)": loose wording, registered impl posture saves it.**
The BASE is a **4**-page union; the ruling's five pages = BASE's 4 + the mock's FX. Until XMOCK-6's r5 absorption, spec law (5) > BASE (4): a deliberate SPEC-WINS-UNADOPTED window. The ruling registers it ("the nle-ui FX-page absorption + the app's page-key K3 tests ride r5/K3"), and the register flip (site 15: "ADOPTED / LANDED-SPEC-R28 (XMOCK-6/7 closed)") means the DECISION closed, not the impl — the W4 editor must keep those two halves distinguishable in the row text (the research pack's map does; the ruling's one-liner blurs them). **Fix (amendment 4):** one clause in the register row-7 flip: "the spec row is LANDED; the nle-ui FX-page absorption + the app's page-key tests ride XMOCK-6's r5/K3 impl half".

**F7 — the battery site (19) is computed against a single-ruling delta: stale on arrival.**
Site 19 says "the OPEN-table check re-keys (5→3 rows; QUEUED count 4→2)". But D48 (row 5) and D49 (row 6) strike their OPEN rows **in the same W4** — the round-final state is **1 row (the A3-wheels row) / `QUEUED-design-round` → 0**. battery_r28's B-5/O check written to D50's 4→2 fails the moment the combined W4 lands. **Fix (amendment 5):** re-key site 19 to the round-final state (rows 5-8 all close; the OPEN table = the A3 row; the check asserts the four RULED/CLOSED dispositions + the A3 RULED row).

**F8 — the option-(b) rejection and the K3/D26.4 arguments: UPHELD.**
Verified: 16 §0's K3 acceptance needs the page inventory ruled to even write the app-side page-key tests (:28); the mock never yields (row 8's MOCK-YIELDS-REGISTERED can never flip LANDED under (b) — the D26.4 retirement triggers would deadlock on unflippable rows 7/8); the corpus would keep "ruling 3 pages and validating a 5-page mock" (the R25 xcut's own words). The three-way-divergence note at 18:221 confirms (b)'s permanence cost. The rejection stands.

**F9 — minor: ruling 22's stated home (18 §6.4) is the Error & Notification UX section.**
Defensible (§6.4:365 owns destructive-action routing — "Everything else commits directly — undo is the safety net"), but the Delete-PRIORITY law's natural companion is the fx-selection domain landing at §4.4 (site 6). One sentence in site 9 stating the §6.4 anchor (the destructive-action row) avoids a W6 "why does the delete law live in the toast section" re-audit.

---

## §3 THE ATTACK ON THE RULING-ROWS

### R16 (the register re-key) — the form is RIGHT; two battery-coherence gaps

**The form:** the headline **must** be the scraped pair (1,939/68) — verified: the battery's pin-line regex (battery_r27:317-318) parses the register's headline as the DECLARED figures and enforces live ≥ declared; a "1,950 it-blocks" headline FAILS (1,939 < 1,950); a "1,939/68" headline passes with the runner pair in the parenthetical (both recorded satisfies 35.2-rider's "recording both the declared and the scraped figure"). The 11-test gap reconciles exactly (re-derived: §1-check-6). **battery_r28 will pass with both recorded.** ✓

**F-R16-1 — "REGISTER-PENDING retires" is ambiguous, and the literal reading breaks B-5/I.** battery_r27:335 requires `("REGISTER-PENDING" in reg) and ("WRAP" in reg)` — a substring check over the whole file. The scout's paste-ready rider edit retires the *current-markers sentence* but the 35.2-rider's **mechanism law text** (register:9's first half: "rows touched by in-flight waves carry a REGISTER-PENDING marker…") retains the term — the scout says so explicitly (§4: "the term REGISTER-PENDING remains in the rider law text (`:9`)"). R16's ruling text does NOT. A W4 editor applying "retires" literally (or a W6 sweep striking stale markers) fails the battery. **Fix (amendment 6):** R16's text gains one clause: "the variants' current-marker retires; the 35.2-rider's mechanism sentence (the term's law-home) STAYS — B-5/I keys on the term (battery_r27:335)".

**F-R16-2 — the citation-site list misses two live-law stale count sites.** `04-renderer-color.md:19` and `07-composition.md:16` both carry live-law design-reference rows: "`ui-mock/shell-variants` @ `e89cd65` — **1,521 tests**" — four rounds stale, off the scout's 20-site list (§6a) and its adjacent-staleness note. No battery check reads them, but 35.2's own law ("a count drift without a register amendment is a battery failure") and the round's zero-staleness posture say the re-key sweep should fold them (re-key to the WRAP figures or re-point at the register). **Fix (amendment 7).**

**F-R16-3 — the dead battery constants.** battery_r27:310-311 (`VAR_DECLARED, VAR_FILES = 1796, 65` — unused except the MINI fallback at :364, and contradicting the register's own 1,762/64) — the scout's next-action 4 flags them; R16's row doesn't. Fold into the W5 fork (amendment 8).

### R17 (the pin re-base) — the split is correctly represented; three add-ons

Verified live end-to-end (§1-checks 8-9): engine HEAD `074a2f6` / code anchor `74bef08` / 749/749 / 25 files (arithmetic re-derived exactly); WDC `83b8850` docs-only + lock unchanged; app `85cff80` / 252/252 / census 42 = 36+5+1; the vendor set engine `74bef08` / nle-ui `83ff8a8` / WDC `ec8fd5c` / OT mirror `6e2b91a`. **The battery's check at :656 (the app-submodules check — scout-app §3 names it "the battery's 4th failing check") will assert the app's ACTUAL gitlinks**: engine `74bef08` — NOT the engine HEAD — because the app vendors the CR-F1/F4 code state, one engine step behind (the OV-12 CI fix + docs + the `2640a81` mirror-rescue are engine-repo-only). R17 records exactly this. ✓

**F-R17-1 — "docs-only" must not be copied into the engine-HEAD check's parenthetical.** The R27 battery's check text says "docs-only over code anchor"; the R28 range contains `e7cdede` (CI fix + **the package-lock.json deletion — a tracked-tree change**, scout-engine §4's caveat). battery_r28's check text should say "code anchor `74bef08` (the last commit touching src/+tests/; after it: one infra/hygiene + docs + a net-zero merge)". **Fix (amendment 9).**
**F-R17-2 — the one-line recorded divergence.** scout-app's open item 2: the vendor-vs-HEAD lag (74bef08 ≠ 074a2f6) deserves one battery/register comment "so a future round doesn't mistake it for drift". **Fix (amendment 10).**
**F-R17-3 — the LOC re-key** (57,064/63 → **57,085/63** under `src/lib/nle`, scout-engine §6) is un-named in R17's row. Fold into the re-base edit family (amendment 11).

### R18 (the CR-F1/F4 absorb) — the form fits; the edit family is bigger than the row names

Verified: law 10's sever half stays true; the amendment (the CONNECT is unconditional over the wanted set — `AudioNode.connect` is idempotent; the `rebuildEffects` arg-less sever door closed) inserts cleanly as the clause the law now lacks; the "5 pins" cite → **6** confirmed by describe census (F-2 = 4 wiring + 2 parity). **F-R18-1:** the same edit family must also re-key the **"9 pins"** cites (`20:20`, `20:149` — the m2-wave1 file-count rows) to **10 its** (the file moved 9→10; the scout's staleness table lists :149/:228/:184 as the P2 same-fold). **Fix (amendment 12).** Optional note: the F-2 describe title at `m2-wave1:205` ("(identity diff)") now describes only the sever half — cosmetic, repo-side, note-only.

### R19 (E1-a/E1-b) — coherent pointer row. RATIFY as-is.
The edits ride D42's riders 1-2; 06:439's E1 row lists exactly the residues the riders amend (verified). No independent amendment is owed; nothing the scouts surfaced is uncovered.

### R20 (A3-wheels) — accurate no-op. RATIFY as-is.
Verified: the register's A3 row (line 56) already carries "RULED (R27 — D39… The model question is CLOSED; r3-entry gates the parity fixtures' consumption" — the row text needs nothing beyond the OPEN-table retirement noted at D50/R16 (and see §2-F7: the retirement is round-combined).

### R21 (the plan lane seal) — convertible in full; name the phase-tag re-key
Verified: 3 user-gated sites (plan :25/:72/:83); every enumerated point has a ruling (D46/R15a/R15b/R15c; D37/D39 already ruled); the battery's plan checks are structural — no break. **F-R21-1:** the fold must also re-key the S-spec row's phase tags (plan:25 item 3: "the register's OPEN decisions phase-tagged: **page-shape + FX grammar at w1-entry; marker v2 + captions at r5-entry**" → RULED-R28 pointers) and plan:25 item (5) ("the user-gate preparation" → the D46 ruled-material pointer) — R21's "the design-round set marked DONE with pointers" implies it; name the sites so W4 doesn't miss them. **Fix (amendment 13).**

---

## §4 THE VERDICTS

| Target | Verdict | The one-line basis |
|---|---|---|
| **D50** | **RATIFY-WITH-AMENDMENT** | The five-page ruling survives every substantive attack (F2, F5, F8 all fail to break it; the Audio page/mode duality is the mock's own coherent law; the ⌘3/⌘5 reconciliation and the option-(b) rejection stand) — but the amendment map misses 3 sites in 16's own ⌘1-⌘9 space (F1), the battery re-key is computed against a single-ruling delta (F7), and the density-exemption scope needs one precision clause (F3). |
| **R16** | **RATIFY-WITH-AMENDMENT** | The 1,939/68 headline + declared-pair recording is the right form per 35.2's rider and the battery's own parsing (verified live, arithmetic exact); amendments: the REGISTER-PENDING term-survival clause, the 04:19/07:16 stale sites, the dead battery constants. |
| **R17** | **RATIFY-WITH-AMENDMENT** | The vendor-vs-HEAD split is correctly represented (verified live at every gitlink); amendments: the not-docs-only parenthetical, the recorded-divergence note, the LOC re-key. |
| **R18** | **RATIFY-WITH-AMENDMENT** | Law 10's amendment form fits and the 5→6 cite is exact (describe-census verified); amendment: the 9→10 pin-count cites in the same edit family. |
| **R19** | **RATIFY** | Coherent pointer row; the edits ride D42's riders; nothing uncovered. |
| **R20** | **RATIFY** | The register row already reflects the closure (verified verbatim); the no-amendment claim is accurate. |
| **R21** | **RATIFY-WITH-AMENDMENT** | All 3 user-gated sites verified convertible to ruled-with-reversal; amendment: name the phase-tag re-key sites in the S-spec row. |

### The amendment list (concrete — all W4/W5 folds inside the rulings' own logic; zero re-opens)

1. **D50/16:** add 3 sites — `16:417` (§3.11's `Cmd+1`–`Cmd+9` color-panel row), `16:621` (§5.4's track-focus row), `16:644` (§6.1 row 11 widened to name all three claimants); re-home or register both rows; map 20→23 sites.
2. **D50 rider (4):** the density exemption keys on `page === 'fx'` — not fxMode (the Edit-page FX tool keeps the Edit page's compact entry; useUiStore.ts:259-261).
3. **D50 W4 sweep:** `18:4` (the status-header OPEN registration rides the version bump) + `18:248` (§4.11's "⌘1–⌘4 stay page switches" → "⌘1–⌘5").
4. **D50 register site 15:** the row-7 flip keeps DECISION-closed vs IMPL-half distinct ("LANDED-SPEC-R28; the nle-ui FX-page absorption + the app's page-key tests ride XMOCK-6's r5/K3").
5. **D50 battery site 19:** re-key to the round-final OPEN-table state (rows 5-8 close via D48/D49/D50 → 1 row / `QUEUED-design-round` → 0; assert the four closed dispositions + the A3 RULED row).
6. **D50 site 9:** one sentence anchoring ruling 22's law at §6.4's destructive-action row (or land it at §4.4 beside the fx-selection domain).
7. **R16:** the REGISTER-PENDING **term** survives in the 35.2-rider mechanism text (register:9) — B-5/I's substring check (battery_r27:335) keys on it; only the variants' current-marker retires.
8. **R16 sweep:** add `04:19` + `07:16` (the "`ui-mock/shell-variants` @ `e89cd65` — 1,521 tests" design-reference rows) to the re-key.
9. **R17/battery_r28:** the engine-HEAD check's parenthetical = "code anchor `74bef08`" (NOT "docs-only" — `e7cdede` deletes package-lock.json); + the one-line recorded divergence (app vendor `74bef08` ≠ engine HEAD `074a2f6` by design, not drift); + the LOC re-key 57,064→57,085.
10. **R18:** add the "9 pins" cites (`20:20`, `20:149`) → 10 its to the same edit family.
11. **R21:** name the phase-tag re-key (plan:25 item 3's "page-shape + FX grammar at w1-entry; marker v2 + captions at r5-entry" → RULED-R28) + plan:25 item (5)'s user-gate-preparation → D46 pointer.
12. **R16/W5:** drop/re-key the dead constants at battery_r27:310-311 (`1796/65`).

**Strongest finding: F1** — D50 fixes 16's ⌘3 collision but leaves `16:417` ("Cmd+1–Cmd+9 switch color grading panel — **Always**") and `16:621` ("Cmd+1–Cmd+9 focus track N — timeline focused") standing against the new ⌘1-⌘5 page law, with the §6.1 conflict table (row 11, `16:644`) still pretending only two claimants exist. The corpus's own law ("Every conflict is resolved explicitly — there are no implicit 'whichever handler is on top wins' behaviors") is violated by the post-D50 residual state until the three sites join the amendment map.

**Zero rejections. Every verdict is RATIFY or RATIFY-WITH-AMENDMENT; every amendment is a W4/W5 fold inside the ruling's own logic.**
