# fleet-r28 — adversarial review: D45 (the NS-4 render path + the R9-c fold) + D46 (the r1 scope gate)

**Task ID:** R28-W3-3 · **Round:** R28 seal round, W3 (adversarial review) · **Reviewer targets:** ARCH-R28-seal-round.md §4, D45 (`:135-139`) + D46 (`:141-147`) · **Evidence base:** `audits/fleet-r28/research-ns4.md`, `research-scope.md`, `audits/fleet-r27/mock-leverage.md` §4/§5 (the stage claims), read live against the corpus tree — engine @ `074a2f6`, OT @ HEAD `55c81c0` (code pin `970948a`, src-diff empty — all OT line cites stand), app @ `85cff80`, nle-ui `32abd58`. Read-only; no state-modifying git; sole writes = this file + the worklog append.

---

## 1. The verification table (claim × spot-check, cited)

| # | Claim (ruling → research → corpus) | Independent spot-check | Verdict |
|---|---|---|---|
| 1 | **NS-4's filing** — "effects/masks/text at fidelity need either a SceneTracks→render-plan adapter or a new compositor input contract over SceneTracks" | `nle-engine/.agents/PLAN.md:475-477` — verbatim, incl. the either/or the ruling decides; siblings NS-1 `:464-467` (segmentSignature/reschedule glue app-owned, "double-clocks or drifts"), NS-3 `:472-474` ("no GPU-path equivalent"), NS-5 `:478-480` — all as research-ns4 §1.1 quotes them | ✅ VERIFIED |
| 2 | The bridge today = audio-complete + **Canvas2D-only visual**; no SceneTracks reaches the GPU venue | `src/lib/nle/bridge/` — 12 files: `scene-to-segments` / `segment-strip-adapter` / `scene-mixer` / `realtime-engine` / `mixer-track-model` / `scored-notes` / `fade-curve` / `conversions` / `opencut-laws` / `av-link` (audio + laws) + `composition-frame` + `transition-inputs` (visual, Canvas2D). No projector/GPU file; `composition-frame.ts:88-91` honest-skips everything but Gaussian Blur to "the GPU effects registry … renders them at fidelity" | ✅ VERIFIED |
| 3 | **Preview and export are ONE path** — `_buildLayers` shared, `player.ts:1297-1322` | `_renderFrame` `:1253-1295` calls `_buildLayers(frame, false)`; the `_buildLayers` doc `:1297-1299` — "Shared by onscreen `_renderFrame` and offscreen `renderFrameOffscreen`"; the export orchestrator consumes it as its frame source (`orchestrator.ts:16` "the Player's `renderFrameOffscreen`", `:102`, `:735` `renderFrame: (frame) => player.renderFrameOffscreen(...)`, `:912`) | ✅ VERIFIED |
| 4 | **The 46 rgba8unorm sites / 10 files** — engine-internal, zero junction surface | `rg -o rgba8unorm src/` → **exactly 46** across **exactly 10 files**: effects/pipeline 8, gpu/mask-manager 8, gpu/compositor 7, playback/player 7, text/text-texture-cache 4, gpu/texture-pool 4, gpu/glyph-atlas-text-pipeline 3, transitions/pipeline 3, gpu/row-alignment 1, effects/lut 1 — matching `08-color-grading.md:18` and research-ns4 §1.4 site-for-site. **Zero occurrences in `bridge/`** (the junction literally never names a texture format) | ✅ VERIFIED EXACT |
| 5 | The venue's existing input surface = `setTimeline` (D12 clause 4's consumer) | `player.ts:645` `setTimeline(tl: Timeline)`; scene-assembly's two-layer cache memoized by (tracks, transitions) **array identity** (`scene-assembly.ts:16-24`) — the invalidation seam NS-5 plugs into | ✅ VERIFIED |
| 6 | OT's render contract — `setTracks()/renderFrame(time)`, "only the painter changes" | `opencut-timeline/src/lib/timeline/render/placeholder-compositor.ts:12-14` verbatim; `setTracks(tracks: SceneTracks)` `:116-118` + `renderFrame(time)` `:120` | ✅ VERIFIED |
| 7 | D12 both signatures: clause 1 (`project(scene) → TimelineData`, deterministic + idempotent, "never an editing input") + clause 4 (the projector feeds `setTracks`) | `00-master-spec.md:328` + `:331` verbatim; engine ratification `.agents/DECISIONS.md:521-542` ("venue-internal state like an IR … The engine Timeline model is NOT deleted: it stays the offline reference implementation + the parity twin that pins the eventual r1 port") | ✅ VERIFIED |
| 8 | Spec 07's FrameDescriptor law — "the renderer doesn't know about `SceneState`" | `07-composition.md:31` (purpose), `:37` ("pure function … no side effects"), `:91` verbatim; `04:462-464` the renderer iterates `descriptor.items`; 04 §9's WYSIWYG law `:671-694` — "Same code, same shaders, same color pipeline. Pixels must be bit-identical" | ✅ VERIFIED |
| 9 | 04 §0's rows the ruling flips: the D29.5c DECLINE `:24` ("not re-filed engine-side"), the projector-parity row `:26`, the r3 binding row `:23` | Read all three `:23-26`; `:24`'s "not re-filed engine-side" is the stale clause the R27 fleet flagged (`fleet-r27/spec-04.md:48`) — D45's flip text is consistent with that finding | ✅ VERIFIED |
| 10 | **The R9-c filing** — `paintCompositionFrame(ctx, ops, media, { grade })`, "own the law once for every consumer (monitor, export, any future renderer)" | `nle-engine/.agents/PLAN.md:156-162` — the proposal text verbatim incl. the signature. **BUT the disposition block `:186-189` still reads `[~] DECLINED-BY-LAW` ("ARCH-R24 F1 … do NOT file the seam promotion … retained as history")** — see attack A2 | ✅ VERIFIED (with the disposition staleness) |
| 11 | **WHO owns `paintCompositionFrame`** — engine bridge; the app's Z2 law is a duplicated app-side wrapper | Painter = `src/lib/nle/bridge/composition-frame.ts:486` `paintCompositionFrame(ctx, ops, resolveMedia)` — **no grade param today**; BOTH app surfaces deep-import it (`deliverService.ts:40`, `ProgramCanvas.tsx:44-47`) and EACH re-implements the final-pass wrapper: `ProgramCanvas.tsx:201-215` (scratch + `ctx.filter`, monitor) and `deliverService.ts:308-338` (scratch + `buildGradeFilterString` + filtered `drawImage`, export; `:40` import; the Z2 comment `:308-312`). `buildGradeFilterString` is APP-side (`ProgramCanvas.tsx:91-94`) | ✅ VERIFIED — the "one law home" question answered (attack A3) |
| 12 | The Z2 landing — `deliverService.ts:300-324` | Read `:299-338`: `renderCanvas` `:300`, the grade read `:313`, `gradeFilter` `:314`, the scratch pair `:315-318`, the compose-then-filter branch `:328-334`, direct path `:336`. *Cite-range nit: the block runs to `:338` (the `getImageData` return), past the ruling's `:324`* | ✅ VERIFIED (cite extends to :338) |
| 13 | **08 §17.B's fragment-pass-on-rgba16float** | 08's §0 R27 **grading-venue-law row** — "WebGPU fragment passes over the scene-linear `rgba16float` working texture … between composite and display transfer"; §17.B itself carries the FreeCut `curvesFragment` quotes `:1390-1409` (Uint8 rgba8 + `rgba8unorm`) + §17.B-port `:1412-1424` ("rgba16float linear-light", LUT 256→1024) — the research's charter-label reconciliation (§17.B = the quotes; §0 = the law) is correct | ✅ VERIFIED |
| 14 | The projector's current disposition — LANDED-AS-BRIDGE, NS-4 = the registered design queue | `19-code-references.md:21` verbatim ("a path, not a duty … NS-4's SceneTracks→GPU render path is the registered design queue") | ✅ VERIFIED |
| 15 | **D46's stage containment** — Stage 3 composites (append → fit-to-fill → ripple-overwrite), Stage 4 replace | `mock-leverage.md` §5.2 `:147-149` verbatim; the plan's r1 row `IMPLEMENTATION-PLAN.md:65` — "Stage 3 the composites … Stage 4 replace (the one greenfield) + the wave-2 families + sync-lock" — exact match | ✅ VERIFIED |
| 16 | The scale anchor — D36.6's "+1.5-3 wk solo ONLY IF the user passes the absent-family scope gate" | `00-master-spec.md:467` verbatim; D40.1's gate-framing at `plan:3` ("a phase ends when its exit gate is green — never on a calendar") | ✅ VERIFIED |
| 17 | **The "nothing depends on them" claim** — E5 runs INTO the four; sync-lock wraps the TRIM family; C7 renames only LANDED verbs | Plan edge list `:114`: "E5. The linkage-model ruling (Stage 0, D41) → the r1 ports of insert-edit/overwrite/**replace/ripple-overwrite** + the companion fan-out" — the only named edge, INTO them; mock-leverage `:119` sync-lock "after the trim family (it wraps them)"; 15:4918's own row: "the census re-declares MECHANICALLY per the tsc-lockstep/M49C law … on landing" — a cut verb never lands, never renames | ✅ VERIFIED (with the D30.3 wrinkle — attack B3) |
| 18 | The K2 zero-orphan form + the battery's r1-SCHEDULED count | `06:29` D30.3 — "at K2 every matrix row is LANDED or carries **a live r1-scheduled row** with owner + acceptance"; `plan:25` (the S-spec K2 registration) + `plan:104` (the diagram line) repeat the same wording; `battery_r27.py:630` — `specs[15].count("r1-SCHEDULED") >= 4` over the four verb rows | ✅ VERIFIED — and the literal "r1-scheduled" wording is the wrinkle (attack B3) |
| 19 | The corpus phase-tag surface per family | 06 §0 matrix rows (`:24-27`, the wire column's "r1:" tags) + §5.9C-F GAP tables (Phase cells: RE-1 "r1", RE-2 "r1-adjacent"); 15:4916-4921 (four rows, replace "78 → 79, union-version bump" at `:4918`); 16:270 (`E`, r1-scheduled marker) + `:281` (F12) + `:285` (button-first note) + `:653` (row 20, the ⇧⌥. four-meaning resolution — exists BECAUSE of ripple-overwrite, per `:32`); 18:175 (the 7-mode bar); **05:21 (the op-family GAP row carries "(**r1**; tests carried…)") + 05:1277-1280 (the §16.5A port table's wave column "1 (R25 — D31.x)")** — the two sites D46's ~7-count omits (attack B4) | ✅ VERIFIED (with the undercount) |
| 20 | The placement carrier — append rides the 6th `PlacementStrategy` on a landed 5-union; the wire exposes 2-of-5 | `opencut-timeline/src/lib/timeline/placement/index.ts:44-54` — exactly five (`explicit`/`firstAvailable`/`preferIndex`/`aboveSource`/`alwaysNew`); `headless/api.ts:62`/`:85` — `strategy?: "firstAvailable" \| "explicit"` (2-of-5). *Cite nit: research-scope says "api.ts:42-50"; the strategies sit at :62/:85 (line drift, claim true)* | ✅ VERIFIED (cite drift noted) |

**Base result: 20/20 load-bearing claims verified — zero fabrication, zero pin drift beyond two cite-range nits (rows 12, 20). The 46/10 count and the one-path property reproduce exactly.**

---

## 2. Attack on D45 (the NS-4 ruling + the R9-c fold)

### A1 — Does the adapter ruling actually answer NS-4's question? YES on the fork; the BOUNDARY riders were dropped between pack and ruling (severity: MEDIUM-HIGH)

NS-4's filing (PLAN:475-477) asks only the architecture question — adapter vs new compositor input contract — and D45 answers it decisively:

- **Venue-native (b) is correctly REJECTED on the corpus's own laws:** it would make the venue SceneTracks-aware, violating 07:91 ("the renderer doesn't know about `SceneState`") and re-implementing the bridge's twin-pinned translation laws inside the venue — the visual twin of the dual-home D12 clause 3 killed (00:330). Verified the venue consumes only `TimelineData` today (`setTimeline` `:645`; scene-assembly's cache `:16-24`).
- **The WYSIWYG argument is code-true:** preview and export share `_buildLayers` (row 3) — a pure adapter over a one-path venue preserves 04 §9's bit-identical law BY CONSTRUCTION; the hybrid (c) would fork it. The audio venue's precedent (scene-to-segments → strips, one topology realtime+offline) is the working existence proof of the pattern the ruling extends.
- **The 46-site upgrade's zero junction surface is stronger than claimed:** not only do all 46 sites/10 files sit engine-internal — **zero `rgba8unorm` occurrences exist anywhere in `bridge/`** (row 4). The format upgrade cannot touch the junction even by accident.

**The real question includes realtime preview, and here the ruling under-delivers.** A pure `project(scene)` projector plus a clock is not yet a render path: during playback/scrub, SOMETHING decides when re-projection happens, and the venue's plan cache is keyed on **array identity** (`scene-assembly.ts:16-24` — "Only recomputes when tracks or transitions array reference changes"). A naive adapter that re-projects per frame allocates fresh arrays → the static plan recomputes every frame → a performance cliff the ruling never names. research-ns4's riders 4-6 assign the boundary precisely — **rider 4: NS-1 inherits** (the adapter is a pure (scene, t) function, clock-agnostic; NS-1's surface shrinks to the transport/schedule glue, the D16 Clock-truth law at 00:376); **rider 5: NS-5 inherits** (the change-classification seam = the projector's invalidation boundary — re-project vs incremental vs nothing — with the array-identity cache as the named consumption point, serving visual IR + audio segment set at one seam); **rider 6: NS-3 inherits** (the GPU thumbnail query rides the adapter's media-resolution seam). **D45's ruling text carries only a single NS-5 clause** ("NS-5's edit-classification seam serves both venues at one point") — the NS-1 confinement and the NS-3 home appear NOWHERE in §4. Under the seal-round's own charter (§0.3: "NOTHING design-side may remain that the executors would have to return to the meta lane for"), the three-way NS-1/NS-4/NS-5 boundary IS design-side: an executor implementing the adapter cold, from D45 alone, cannot tell where clock coupling or invalidation cadence is ruled, and the per-frame-reprojection trap is unnamed. The pack supplied the answers; the ruling dropped two of the three.

**Fix (amendment):** fold research-ns4's riders 4-6 into the W4 amendment set (00's D-entry + the 04 §0 row): (a) the adapter is clock-agnostic — NS-1's ruling surface is the transport/schedule glue only; (b) NS-5's seam = the projector's invalidation boundary, consumed at the venue's array-identity plan cache (`scene-assembly.ts:17-24`), one seam serving both venues; (c) NS-3's GPU thumbnail query rides the adapter's media-resolution seam. Add the one-sentence trap warning: the projector must NOT re-project per frame during playback (the array-identity cache thrashes); cadence is NS-5's seam.

### A2 — "The engine's re-filed proposal": the characterization is contestable and the ratification leaves the engine's OWN queue stale (severity: MEDIUM)

The engine's PLAN carries the R9-c proposal text with an open checkbox (`:156-162`) and, directly below it, the R25-era disposition block (`:186-189`): "**DECLINED-BY-LAW** (ARCH-R24 F1: the decline is the law … **do NOT file the seam promotion** … the proposal text above is retained as history)". "Re-filed" is the R27 fleet's reading of the retained open checkbox (`fleet-r27/spec-04.md:48` — "the engine DID re-file it"); the engine itself never re-filed anything — its standing disposition says the opposite. D45 inherits the imprecise framing (as does the charter's E4 row). This is not cosmetic: **the ratification makes the engine's own queue row FALSE the moment the ruling lands**, and D45's disposition names only "Spec 04's twin row" as the flip site — it does not file the cross-repo amendment into the engine's PLAN (the D36.2 queue-filing protocol: "the spec round FILES it, the engine session executes"), and it does not name **08's twin row** either (04:24's own text: "Twin row: spec 08 carries the grade-side twin of this decision" — 08's §0 D29.5c DECLINE row needs the same evolution note, or 04 and 08 drift apart at W4).

**Fix (amendment):** the R9-c fold's W4 edit set = (a) 04:24 (the decline row flips to "ratified-as-evolution, r3-consumable" — as drafted); (b) 08's §0 twin row gains the same evolution note (the venue fragment-pass law is UNTOUCHED — see A3); (c) a titled cross-repo filing into `nle-engine/.agents/PLAN.md` flipping the R9-c disposition block from DECLINED-BY-LAW to "RATIFIED as D29.5c's registered evolution (R28 D45); lands engine-side at the r3-consumption point"; (d) the disposition's history note corrected — the "re-filed" phrasing becomes "the retained open proposal, ratified at R28" (the engine never re-filed; the R27 fleet's reading is the origin).

### A3 — The R9-c fold: "ONE law home" is right in substance, imprecise in wording; the SceneTracks-blindness attack dissolves (severity: LOW-MEDIUM, wording)

**The blindness attack fails, on the corpus's own seam law:** `paintCompositionFrame` is bridge-side (the junction itself), and the bridge's law is "STRUCTURE comes from the opencut TScene … PARAMS come from the consumer's sidecar bag" (`composition-frame.ts:7-12`). The grade is NOT SceneTracks data — it is consumer state (the app's `useUi.color[sceneId]` store, `deliverService.ts:313`), arriving as a param exactly as `elementParams`/`effects` do (the D2e field law). Venue-blindness is untouched: the venue stack never calls the painter; the app does. No conflict with the adapter ruling — the two touch different tiers.

**The ownership claim is TRUE and verified:** today the compose-then-filter law has TWO app-side homes over one engine painter — `ProgramCanvas.tsx:201-215` (monitor) and `deliverService.ts:315-338` (export), each with its own scratch canvas + `ctx.filter` pass, plus a THIRD partial home for the string mapping (`buildGradeFilterString`, `ProgramCanvas.tsx:91-94`, app-side). R9-c's `{grade}` param collapses these to one seam home — "own the law once for every consumer" is the proposal's own text (PLAN:160-161). **But the ruling's phrase "gives the app's Z2 landing … and the venue ONE law home" is loose in one direction:** the VENUE's grade home is and stays 08's r3 fragment passes over the rgba16float working texture — `paintCompositionFrame` can never be the venue's grade home (it is Canvas2D, CSS-filter domain, the honest-preview tier). If W4 reads "the venue ONE law home" as claiming the painter as the venue's grade authority, the amendment would contradict 08's venue-law row. The correct statement: R9-c gives the **consumer-preview tier** (monitor + export) ONE law home at the seam; the venue keeps its own (the r3 fragment passes); the shared thing is the ORDER law (compose-then-filter), which both tiers implement in their own domain — the same one-law/two-tier pattern the effects family already runs (D29.5d's honest CSS subset vs the 44-effect GPU registry).

**Fix (amendment):** (a) reword the ruling's clause to "gives the app's Z2 landing and the consumer-preview tier ONE law home (the seam's final pass); the venue's grade home stays 08's r3 fragment passes; the compose-then-filter ORDER is the shared law"; (b) register the app-side migration as part of the landing — `ProgramCanvas` + `deliverService` switch to the `{grade}` param and retire their wrapper pairs at the seam's landing (r3), with `buildGradeFilterString`'s fate named (it moves engine-side as the pinnable string law — the `buildElementFilterString` precedent — or the seam takes the pre-built string; pick one at W4); (c) the cite `deliverService.ts:300-324` extends to `:338` (the closure's return).

### A4 — The adapter's payload typing was left to W2 and W2 didn't take it (severity: LOW, but the seal bar wants it REGISTERED)

research-ns4's open micro-decisions: "the exact IR payload typing at the seam (TimelineData vs a FrameDescriptor-shaped plan slice) — recommend deferring to the parity corpus's shape as written; the sidecar bag's GPU-path law (extend OV-05's base/override convergence to the projector); the sticker/graphic honest-skip coverage list." D45 mints the IR as `project(scene) → TimelineData` (D12 clause 1's words) while simultaneously citing 07's `buildFrameDescriptor` law as the adapter's charter — two different granularities (per-timeline state projection vs per-frame render plan). The deferral is fine (the parity corpus is the natural shaper); an UNREGISTERED deferral in a seal round is not — it is exactly the class of thing §0.3 bars.

**Fix (amendment):** one sentence in the W4 04 amendment: the seam payload typing resolves at the parity-corpus authoring (S-engine), with the sidecar-bag GPU-path law (OV-05's extension) and the honest-skip coverage list riding the same work order.

**D45 decision-survival summary:** the adapter-over-venue-native choice is correct, code-grounded, and law-grounded; the R9-c ratification is the right disposition of E4 (the math law preserved, the dual consumer home collapsed, the decline's own reasoning — per-element ≠ composited — untouched). The defects are completeness defects (the dropped riders, the landing-site omissions, the loose "venue" phrasing), not architecture defects.

---

## 3. Attack on D46 (the r1-entry scope gate)

### B1 — Include-all-four vs trim-first: the inclusion is justified ON THE REGISTERED AMBITION; the deferral lever answers the trials-and-errors concern (no finding)

The user's standing directive pair — DaVinci/web-DAW depth AND "reduce trials and errors" — is exactly the tension D46 must resolve, and it resolves it the right way round: the completeness census (the ten-mode matrix, D30's own definition of the ambition, re-derived every fleet pass) makes any cut a trade against the ambition's OWN metric, while the trials-and-errors risk is quarantined by structure, not by scope: the three composites ride LANDED verbs under spec'd §5.9X laws with named pin sets (verified: `insertBatch` BASE @ 15:4916; the delete/move/insert trio routed; `calculateSpeed` exists both sides), and the one greenfield (replace) is sequenced LAST (Stage 4, plan:65 = mock-leverage §5.2 `:149` verbatim) behind three named cut gates with a registered zero-code reversal. The cut order (replace → ripple-overwrite → fit-to-fill → append) is the stage order reversed — the cheapest-to-cut is the last cut, the most expensive is the first — which is the correct shape. The +1.5-3 wk anchor (00:467) is the only corpus cost figure and is now gate-framed (D40.1). I find no overreach: replace's inclusion is the one judgment call, and it is (a) the best-specified gap (06 §5.9C's contract is complete: exact-length postcondition set, out-auto-adjust law, joinItems remap vs cascade-drop, sever-not-delete, unfillable-source refusal — verified `:1603-1621`), (b) isolated, (c) reversibly deferred. **No amendment to the inclusion itself.**

### B2 — The cut conditions are operational at three of four gates; the transition-remap probe has no failure protocol (severity: MEDIUM)

The named gates check out as evaluable: (i) "Stage 0's D42/D44 rulings not folded by the r1-entry fleet round +1" — a verifiable round event; (ii) the Stage-2 exit gate ("the carried insert/overwrite pins green in OT") — a test gate the plan already defines; (iii) fit-to-fill's two conditions are ALREADY decidable — D43 registered `FIT_TO_FILL_RATE` [0.1,5] (so condition one is known-green) and the wave-1 rateStretch gate is a plan gate; (iv) append's total-r1-overrun form inherits D40.1's exit-gate law. **The soft spot is replace's condition (c): "the transition-remap probe fails in OT's element-owned `transitionOut` model (a redesign, not a port)."** "Fails" has no protocol — the leverage map names it only as an open question ("whether OT's `transitionOut` model can host the 'transition blocked' state the clamps need", `:121`). As written, an executor cannot fire this cut without inventing the failure criterion mid-flight — precisely the kind of return-to-the-meta-lane event the seal round bars. Same class, smaller: ripple-overwrite's condition (a) ("the D42 E1-a ruling forces a re-derivation of the delta law's downstream set") needs its check stated as a diff (does the E1-a track-family reading change the downstream set the delta law computes? — a comparison against 06:1652's law, not a judgment).

**Fix (amendment):** name the probe — a time-boxed OT-side spike, two exits: (a) a pinned `transitionOut` endpoint-remap test green (the joinItems-pattern law over an OT fixture) → replace proceeds; (b) the redesign verdict filed as a new D-entry → the cut fires. Ripple-overwrite's E1-a condition restated as the diff check above.

### B3 — "Nothing in the r1-r6 plan depends on any of them" is TRUE on every code/plan edge — and FALSE on one LITERAL (severity: MEDIUM, the strongest D46 finding)

Independently re-derived: the E5 edge runs INTO the four (plan:114); sync-lock wraps the trim family (leverage `:119`); the C7 rename is inert for un-landed verbs (the census re-declares ON LANDING only — 15:4918's own row text, D29 F8; a cut verb contributes zero names to the rename pass); K3's app column matches the LANDED set (a cut family is simply absent); the mock-retirement triggers key on LANDED flips (never fire — registered); 16's #19/#20 rows are the one real collateral and D46 registers the re-key as rider (c) — honest. **But the K2 zero-orphan law as LITERALLY WRITTEN says "a live r1-scheduled row"** — 06:29 (D30.3), repeated verbatim in `plan:25` (the S-spec K2 registration) and `plan:104` (the diagram). A family deferred to r2/r5 carries a live r2/r5-scheduled row with owner + acceptance — which does NOT satisfy the literal text. D46's claim "the deferral keeps owner + acceptance → zero orphans preserved (the D30.3 zero-orphan form)" cites 06:29 as if the form already accommodates the re-point; it does not, as written. The K2 gate at the next registration would read the law as r1-only and see an orphan. The ruling's re-tag list does touch "the K2 registration note" — but amending the NOTE (plan:25) without amending the LAW (06:29) leaves the two in contradiction.

**Fix (amendment):** the D46 W4 edit (or the cut-firing edit, whichever comes first) generalizes D30.3's wording in 06:29 + plan:25 + plan:104 to "…LANDED or carries a live scheduled row at its registered phase, with owner + acceptance — zero orphan rows" (one clause, three sites, mechanical). Until then the reversal protocol's "zero orphans preserved" claim is only true in spirit.

### B4 — The "~7 mechanical re-tags" undercounts the phase-tag surface (severity: LOW-MEDIUM)

D46 enumerates: 06 §0 + §5.9X, 15 §13.15, 16 §3.4A/F-block, the plan's Stage line + user-gate text, the battery's r1-SCHEDULED count, the K2 note — 8 items called "~7". Two verified phase-tag sites are missing: **05:21** (the op-family GAP row carries "(**r1**; tests carried from nle-engine timeline.ts…)") and **05 §16.5A's port-table rows** (the wave column reads "1 (R25 — D31.x)" for all four — verified at 05:1277-1280). Both are phase cells that go stale on a re-point to r2/r5; neither is in the enumeration. The plan's E5 line (which names replace/ripple-overwrite as consumers) also wants a parenthetical on a cut. The corrected figure is **~10-11 mechanical re-tags across 7 files** — still zero code, still mechanical, but the seal round's cold-executor bar (and battery_r28's check classes) want the COMPLETE set, because an incompletely re-tagged corpus is a battery failure at the next round — the exact failure mode the reversal protocol exists to avoid.

**Fix (amendment):** complete the enumeration in D46's reversal protocol: + 05:21, + 05 §16.5A (the four rows' wave cells), + the plan's E5 line note; restate the count as ~10-11.

### B5 — The "79th union member" collides with D47.3's "78→79→80" (severity: P3, cross-ruling)

D46 (quoting 15:4918, written at R25) calls `timeline.replace` "the 79th union member." D47.3 — ruled in the SAME §4 — has the union gain the bare `insertBatch` first ("78→79→80"), making replace the **80th**. review-d47-r15 verified D47.3's arithmetic against the census machinery but the two rulings' ordinals remain unreconciled; the W4 editor for 15 §13.15 inherits two different numberings for the same landing sequence. **Fix:** one cross-cut note at W4 — the replace row's "78 → 79, union-version bump" (15:4918) updates to the post-D47.3 sequence (insertBatch 79th on its fold, replace the next new member; final count 80), and D46's "79th" reads as "the next new member after D47.3's insertBatch." (Also note research-scope's "api.ts:42-50" for the 2-of-5 strategies actually sits at headless/api.ts:62/:85 — line drift only, claim true.)

### B6 — Checks that PASSED (no findings)

- The battery dependency is correctly registered (battery_r27.py:630's `>= 4` count; D46 names the re-calibration).
- The zero-code claim at r1-entry is true — nothing is landed; the mid-r1 "sunk pin-authoring only" and post-r1 "K2 row re-point + the mock stays the standing reference" stages are correctly costed (register:12's LANDED-flip trigger).
- The reversal conditions (r5-entry / D28.2 outside-consumer / user override) match the plan's registered text (plan:72) and §0.4's override law.
- The D30.2 "landed census unaffected" claim is the verb rows' own text (15:4918-4921, each row carries it).
- Rider (b)'s `RATE_OUT_OF_DOMAIN` matches the errors pack + D44's ruling (cross-checked vs review-d43-d44 — no conflict).

---

## 4. Verdicts

### D45 — RATIFY-WITH-AMENDMENT

The architecture is correct and the strongest claims verify to the digit (46/10 exact, zero `rgba8unorm` in the junction, one preview/export path code-true, the R9-c proposal text faithful). The amendments are completeness fixes, not re-opens:

1. **(A1, load-bearing)** Fold research-ns4's riders 4-6 into the W4 set: the NS-1 clock-agnostic confinement, NS-5's invalidation boundary AT the array-identity plan cache (with the per-frame-reprojection trap warning), NS-3's media-resolution-seam home.
2. **(A2)** The R9-c landing set gains: 08's §0 twin row, the cross-repo engine-PLAN disposition filing (D36.2 protocol), and the "re-filed → retained-open-proposal, ratified R28" history correction.
3. **(A3)** Reword "the venue ONE law home" → the consumer-preview tier's one home; the venue's grade home stays 08's r3 fragment passes; register the app-side migration (ProgramCanvas + deliverService → `{grade}`, the wrappers retire) and `buildGradeFilterString`'s landing shape.
4. **(A4)** Register the seam-payload-typing deferral (resolves at the parity-corpus authoring) + the sidecar-bag GPU-path law + the honest-skip coverage list as one S-engine work-order sentence.
5. *(nit)* extend the Z2 cite to deliverService.ts:299-338.

### D46 — RATIFY-WITH-AMENDMENT

Include-all-four stands (the ambition's own census + the composites' landed carriers + replace's isolation); the deferral lever's structure is right (cut order = reversed stage order). The amendments:

1. **(B3, the strongest finding)** Generalize D30.3's "a live r1-scheduled row" to "a live scheduled row at its registered phase" in 06:29 + plan:25 + plan:104 — otherwise a fired cut violates the K2 zero-orphan law AS WRITTEN, and D46's own "zero orphans preserved" claim is only true in spirit.
2. **(B2)** Name the transition-remap probe protocol (time-boxed OT spike; two exits — pinned remap test green, or the redesign verdict filed as a new D-entry); restate ripple-overwrite's E1-a condition as the downstream-set diff check.
3. **(B4)** Complete the re-tag enumeration (+ 05:21, + 05 §16.5A's four wave cells, + the plan's E5 line) and correct the count to ~10-11.
4. **(B5, P3 cross-cut)** Reconcile the replace ordinal with D47.3's 78→79→80 at the 15 §13.15 W4 edit; fix research-scope's api.ts:42-50 cite to :62/:85.

Both rulings' amendments are W4-fold edits inside the rulings' own logic; zero re-opens; no evidence of fabrication or pin drift in either research pack beyond the two cite nits.

**Strongest finding overall:** B3 — the reversal protocol's foundation stone ("a cut is a registered deferral, zero orphans") rests on a law text (D30.3 at 06:29/plan:25/plan:104) that literally requires "r1-scheduled"; a fired cut would breach the K2 gate as written until the three-site wording is generalized. (D45's strongest: A1 — the NS-1/NS-5 boundary riders dropped between pack and ruling leave the realtime-preview question — the half of NS-4 the filing actually cares about — without a ruling-of-record home, and the per-frame-reprojection trap unnamed.)
