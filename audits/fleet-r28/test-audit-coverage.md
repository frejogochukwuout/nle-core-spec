# R28-T1-d — the test-spec coverage audit (12 / 17 / SIGNOFF × the D42-D50 law surface)

**Task ID:** R28-T1-d · **Agent:** coverage-gap auditor (sub-agent) · **Date:** 2026-09-16
**Tree (read-only on specs):** `nle-core-spec` @ **main `980920b`** (verified: `git rev-parse HEAD` = 980920b; working tree clean on `*.md`).
**Mission:** the user's law — *"for any new functional areas we propose or upgrades to existing we should fully cover the test aspects in the spec and plan."* Audited: the test-side specs (`12-testing-strategy.md`, `17-test-plan.md`, `TESTABILITY-SIGNOFF.md`) + the per-spec `## Testing` sections (spot-sample 05/06/08/09/15/16/18/20) against the R28 ruling surface (ARCH-R28 §4 + §4.1: D42-D50 + R15-R21).
**Method:** every claim below was verified live at `980920b` with file:line cites (Read/Grep, no `find`/`grep` shells); the W4-commit diff (`git diff 2dc4af1..980920b`) was hunk-mapped file-by-file to prove which sections the R28 corpus fold touched and which it did not. Sibling inputs: `test-scout-ot-wdc.md` (T1-b) + `test-scout-app-ui-mocks.md` (T1-c) — the demonstrated-practice census this audit's fixes key on.

---

## §1 The current test-law inventory (what 12/17 carry — verified live)

### 1.1 The headline finding: the R28 corpus fold never touched the test specs

`git show --stat 980920b`: the W4 fold amended **14 files** — 00/04/05/06/07/08/09/10/15/16/18/20 + PLAN + REGISTER. **`12-testing-strategy.md`, `17-test-plan.md`, and `TESTABILITY-SIGNOFF.md` are ABSENT from the R28 fold** (last touched at R27: `git log -1 -- 12-testing-strategy.md` = `f0e45e5`/`750df90` (R27 W6-A + merge fix); `17-test-plan.md` same; `TESTABILITY-SIGNOFF.md` = `60e9e39` (R27 W-C)). Cross-check by content: `rg -c "R28" 12-testing-strategy.md 17-test-plan.md TESTABILITY-SIGNOFF.md` → **0 / 0 / 0**; `rg -c "D4[2-9]|D50"` over the same three files → **0 / 0 / 0**; `battery_r28` appears in exactly one spec-tree file (`IMPLEMENTATION-PLAN.md:36`) — never in 12/17. The hunk map confirms the same per-spec: no W4 hunk touches any `## Testing` section (05's Testing @ :1595 vs hunks at :18/:719/:1274; 06's Testing @ :3197 vs hunks ≤ :2706; 09's Testing @ :2580 vs hunks at :155/:338; 16's §9 @ :1762 vs hunks ≤ :1308; 18's §12 @ :482 vs hunks at :1/:244; 15's §15.4 @ :5248 vs hunks ≤ :5015). **The design law moved at W4; the test law did not move at all.**

### 1.2 What spec 12 carries (the infrastructure/methodology home)

| Element | Site (live @ 980920b) | State |
|---|---|---|
| §0 forward inventory — the K1-K4 tier map + GAP register | :9-:36 | **R27-pinned, R28-stale**: BASE row :14 keys engine 748/748 @ `f9ac806` / OT 632 / WDC 777 @ `ec8fd5c` / nle-ui 690 / app 252 @ `c020b2a` / mini 495-in-flight / **variants 1,796/64** — vs the plan's R28 reference set (:36: engine **749/749 @ `074a2f6`** code anchor `74bef08`, WDC `83b8850`, app `85cff80`, variants **1,939/68** + the declared pair 1,950/126, mini 495/12). Status header :4 is staler still (mini 441 / variants 1,773 / "battery_r27 this round"). |
| K1 row | :18 | R27 counts (748/632/777/690/252). |
| K2 row — M49C the OT-side completeness instrument, census **31 = 28 routed + 3 exceptions** | :19 | Law itself still true (the census re-declares mechanically per D29-F8); no R28 projection note (the D47/D46 verb growth re-keys it at the r1 landings). |
| K3 row — the W-F coverage-gate port + the compensating trusted-integrator pin family (⌘B, [ ]/⌥[ ], ,/., M/⇧M, ⌘M/⌘⇧M) | :20 | Current law; carries the M/⌧M family but nothing D48-shaped (range markers). |
| K4 row — the 28-routed-verb e2e | :21 | Current. |
| GAP rows (LAW-NET / K2 census PENDING / K3 corpus / K3 audio / VLM / K4 / r2-r5 parity+corpus tails) | :24-:34 | Current; **no row for any D42-D50 area**. |
| ACCEPTANCE & TEST PLAN + battery posture | :36 | Names **battery_r27** + the R27 pin keys — R28-stale (battery_r28 is W5/W6's registered re-base, but 12's own posture row must re-key WITH it). |
| §5 pixel verification / §6 audio verification (the mechanics) | :300-:531 | Timeless infra; consumed by D45's parity oracles. |
| §8 property-based testing for NLE ops | :585-:690 | Six invariant families (split-preserves-duration :604; trim-never-negative :617; no-overlap :630; source-bounds :648; **locked-tracks-not-modified :663**; undo-restores-exact-state :679). **Nothing D42-shaped** (no linkage-closure invariant, no dead-pointer invariant) and **nothing D46-shaped** (the four families are absent from the invariant set). |
| §9 test matrix (smoke/full/nightly) / §11+§17 CI / §15-§16 assets+references | :694-:2403 | Generic infra; no R28 dependency. |
| §18 meta plan + `## Testing` (the meta-tests: canary/negative/flaky/manifest-sha) | :2404-:2831 | Current. |

### 1.3 What spec 17 carries (the methodology + matrices home)

| Element | Site (live) | State |
|---|---|---|
| Status header | :4 | v1.5 R27: engine 748 @ `f9ac806` etc.; **mini 441 / variants 1,733/64** (doubly stale vs 12's own §0 and vs the register's 1,939/68); "battery_r24 → battery_r27". Carries the pre-D47 OT-side sentence "the M58/M58R batch-verb pin family … the singular verbs RETIRED" — **now wrong by overreach**: D47.3 rules `insert` stays singular, neither deprecated nor aliased (only the singular remove/retime retire; 15:5022). |
| §0A the acceptance-executability law (BASE rows state regression role; GAP rows state acceptance gate + facet row; count discipline battery-enforced) | :11-:41 | The LAW is the audit's own instrument — and it is the one that convicts: rule 2 (:16) "a gap row with neither [gate nor facet row] is a spec bug", and §14.4 step 0 (:2423) "every facet your spec introduces … has a row in §3.1 or §13A.7 (or you add one in the same PR). **A facet with no row anywhere is a spec bug.**" The D42-D50 facets have no rows anywhere in 17. |
| §0A BASE / GAP | :20-:34 | R27 pins (748/632/777/690/252); rule 4 (:18) still says "battery-enforced (**battery_r24** …)"; GAP row :34 same; the K2 census row (:24) PENDING (correct — K2 entry hasn't happened) but pre-D46/D47-shaped. |
| §2.5 "When to add a new test" — rule 8 the error-path census | :450-:454 | **D44's own named audit mechanism** (ARCH-R28 §4 D44: "The enforcement: the wire dispatch arm (runtime) + the never-guard/tsc-lockstep (compile) + **17 §2.5's rule-8 error-path census (audit)**") — and the text is pre-R28: "every `CommandError` code the feature can emit — including `NOOP` (spec 15 §6.3)". No class layer, no 25-code count, no benign-echo family, no `'domain'` constraint type. |
| §3.1 the feature matrix | :497-:546 | Has rows for the LANDED families (split :504, trim :505, ripple :506, roll/slip/slide :507-509, delete/insert :510-511, rate-stretch/retime/freeze/range-removal :512-:515, undo/redo :532, save/load :533-:534, FCPXML :536-:537, WYSIWYG trio :538-:540). **NO rows for: the four D46 families (replace/append/fit-to-fill/ripple-overwrite), linked-companion ops (D42), edit-domains (D43), the error envelope's two-tier shape (D44), markers point+range (D48), caption tracks (D49), the page system (D50).** |
| §4 per-module template + the 06 worked example | :590-:843 | Template current; the worked example pins the LANDED families only. |
| §13A the facet additions | :2246-:2362 | §13A.1 NFR recipes (error-path discipline row :2263 keys "spec 15 §6.3's registry" — pre-R28); §13A.3 absorbed fixtures (trim NOOP-vs-rejection :2288 — the seed of D44's benign family, not yet the law); §13A.4 wire conformance — **schema sweep :2292 "for each of the 78 union members"** (STALE: D47.3/D46-B5 rule 78→79→80; 15:5023/:5025 carry the R28 arithmetic) and **error-path census :2293** (same staleness); §13A.5 projector property tests :2299-:2308 (deterministic/idempotent/never-loss/no-readback — the pattern D45's render-plan projector reuses; not extended to it); §13A.6 UI-shell facet tests :2310-:2318 (no page-key rows); **§13A.7 facet matrix :2320-:2360 — zero D42-D50 rows**; the enforcement rule :2362 ("a facet introduced by any future round must land in this matrix … in the same PR") — the R28 round violated its own rule. |
| §14.2 spec-by-spec mapping | :2378-:2399 | Line anchors drifted (05 "≈1428" vs live :1595; 06 "≈2908" vs :3197; 08 "≈2056" vs :2132); **15/16/20 absent from the table** (their testing homes — 15 §15.4 @ :5248, 16 §9+§16 @ :1762/:2400, 20 §10 @ :176 — unmapped). |
| §17A the app-tier roof | :2728-:2751 | R24-era counts inside the principle paragraph (:2730 "R24 counts … 174 — the 6-suite inventory"); S5 names "**battery_r24.py** today" (:2740) — two battery generations stale; §13A.7's re-tier row :2359 still R24-baselined (engine 458 …). |
| §18 meta | :2755+ | Current. |

### 1.4 What the per-spec `## Testing` sections carry (spot-sample, the heavy eight)

- **05** `## Testing` @ :1595-:1900: keyboard-driven Tier-3 law (the state-WYSIWYG umbrella `keyboard-state-wysiwyg-for-every-shortcut` @ :1805), `keyboard-m-adds-marker-at-playhead` @ :1798 (**the point-marker-only law — nothing D48**); property rows :1813+ (virtualization/snap). The D48 scene-shrink law landed in LAW text (:724) with no test row. The D46 re-tag landed at :21 + §16.5A's wave cells (:1286-:1290) — "tests carried from nle-engine timeline.ts — acceptance: carried engine tests green."
- **06** `## Testing` @ :3197-:3680: the strongest per-spec section — per-op contracts (5×14 @ :3218), split/trim/move/ripple algorithmics, **`ripple-overwrite-composite-makes-room` @ :3305** (the one D46 family with a named test row — re-keyed R25/D31.6), the property family incl. `multi-select-op-consistency` @ :3605 ("plus cascade-required companions: linked items, sync-locked neighbors" — a pre-D42 gesture at the linkage law, no pairwise-field pins). **No rows for replace/append/fit-to-fill; no linkage pin set.** The LAW side is current: rider 12's uniform closure predicate @ :411, E1-c @ :440 (the pairwise-twin pin — the mixed-split fixture), the E2-a prune/CASCADE law @ :1592 + the OW-4 pin @ :1603 (the split-then-overwrite fixture, zero dead pointers), RE-1's Tier-1 pins @ :1622 (exact-length/no-move/undo-atomicity/transition-survival/companion-untouched), AP-2's M16-class pin set @ :1642, §5.9F's three-way intersection @ :1671 + FF-1/FF-2 rows @ :1683-:1687 (37 per-mode pins + the A/V pair sync pin + the refusal law).
- **08** `## 19. Testing` @ :2132: the spec-04 boundary table carries **`keyboard-cmd-1-through-9-switches-color-grading-panel` @ :2168 — a RULED-OUT claimant** (D50/16 §6.1 row 11 @ 16:645: the ⌘-number namespace belongs to the page law unconditionally; the color-panel reading is retired). STALE.
- **09** `## Testing` @ :2580: schema/persistence/fixture-battery rows. The **D48 model fold is INCOMPLETE here**: the `Marker` interface @ :286-:292 is still `{id, time, label?, color?}` (no `duration?`/`notes?`) and A2's ruling text @ :335 predates D48; the D49 fold is absent (`SceneTracksJSON` @ :119-:124 has no `captions` family).
- **15** §15.4 "Testing the protocol itself" @ :5248: the round-trip sweep + census — pre-R28 (no class-layer/echo-family assertions). The LAW side is R28-rich (§6.3's two-tier taxonomy + class-tag table + never-silent law @ :2965-:3014; the flat keyframe forms @ :4335-:4371; the insertBatch-ripple ruling @ :5023; the replace 80th-member row @ :5025; addTrack 'caption'+language @ :1092-:1115; the linkedTo patch key @ :962-:971). **§4.3.49-51's marker command params were NOT D48-widened** (still `time/label/color/type` @ :1566-:1600 — the `type?: 'note'|'chapter'…` field D48 never ruled).
- **16**: the D50 fold is thorough (the five-page law @ :365-:379; the three-claimant rulings @ :418/:622/:645; Appendix A re-keyed to **182 rows** @ :2404-:2426) and :28 registers the remaining TEST work verbatim: "Remaining at K3: **the app-side page-key tests** + the nle-ui FX-page absorption — XMOCK-6's r5/K3 impl half." Appendix C's marker row :2414 = **5 tests** (the point-era count; no range-marker rows). The ⇧M start-match + ⌥M inspector field set landed @ :342/:351.
- **18** `## 12. Testing` @ :482: the shell suite (state rows, menus, save chip, toasts, cursor grammar) + a Deliver-page export test — **no page-key (⌘1-⌘5) rows, no caption rows, no marker-inspector rows.** The v1.8 status header (:4) announces the five-page ruling + §4.11's ⌘1-⌘5 widening (:246), but **§4.8's body still carries the pre-D50 three-page OPEN registration** ("three pages, not seven … registered OPEN, D35.4" @ :244-area) and §1 (:41) still says "the 7-page dock collapses to 3 pages."
- **20** §10 @ :176: the facet table IS R28-current on its audio rows (the CR-F1 re-key 9→10 pins, :21/:126/:186-area) — the one `## Testing`-equivalent section the W4 fold actually touched.

### 1.5 The SIGNOFF (see §4 for the disposition)

`TESTABILITY-SIGNOFF.md` @ :3-:5: the R7-era certification rider'd R27 — the four re-stamp conditions + the two stale-number riders ("73 EngineCommand types" → "31 verbs = 28 routed + 3 exceptions"; "~180 keyboard bindings" → 16 §0's census). Both riders are now themselves stale (the union is 78→**80** at the r1 bump per 15:5023/:5025; the keymap registry is **182 rows** per 16:2404; the error registry is **25 codes, two-tier** per 15:3002). Same state for `FINAL-SIGNOFF.md`.

---

## §2 The per-area coverage verdict table

Verdicts: **COVERED** (a current test law exists at the named home) / **STALE** (a law exists but pre-ruling wording, wrong counts, or dead anchors) / **MISSING** (no test law anywhere in 12/17 or the owning `## Testing` section). "Law side" = the ruling's own text landed in the owning spec at W4 (verified); "Test side" = fixtures + oracle pattern + acceptance + phase in 12/17/`## Testing`.

| Area | Law side (owner, live cite) | Test side verdict | The minimal complete test-law statement (fixtures · oracle · acceptance · phase) |
|---|---|---|---|
| **D42 linkage** — `linkedTo` pairwise field + runtime closure + rider 12 lock/sync-lock predicate + prune/CASCADE + E1-c | LANDED: 06:411 (rider 12), 06:440 (E1-c + its pin), 06:1592/:1603 (prune/CASCADE + OW-4 pin), 09:345 (B1 D42 note: the carried E1/E2/F6 pins assert link values through undo/redo/serialization), 15:962-:971 (patch key, string/null-clears) | **MISSING in 12/17** (no matrix row, no facet row, no property invariant); the owning 06 GAP rows already name the pins — 17 never mirrors them | 17 §3.1 row "Linked-companion ops (`linkedTo` pairwise + closure)" T1 ✅/T3 ✅/Property ✅ + §13A.7 facet row citing 06 §5.0/§5.9B. Pin set: locked-partner-skip (a locked companion does not move while the anchor edits); sync-lock-skip when §6 owns the track; E1-c mixed-split fixture (untouched third member's original link survives; halves re-pair by side); prune/CASCADE split-then-overwrite fixture (companion cascades WHOLE; zero dead `linkedTo` on survivors); patch-key null-clears + TRACK_LOCKED on a locked-track patch; relink-both-halves; duplicate-severs (F4). Properties (12 §8): closure is symmetric/cycle-free/order-stable; **no dead `linkedTo` after any op**; undo/redo/serialization preserve `linkedTo` exactly (the E1/E2/F6 carried-pins law). Phase: r1 Stage 0 (the field+closure+four-verb-laws one-additive-OT-change is S-ot's filed first step). |
| **D43 edit-domains.ts** — one-home constants + NATIVE_VENUE_SPEED fence + FTF three-way intersection | LANDED: 06:1671 (the live intersection, [0.1,5], refusal-never-clamp; AUTHORING_SPEED named NOT an input) | **MISSING** (no row in 12/17; the battery check-2 widening is named only in ARCH-R28 §4.1 D43-A3, unbuilt) | 17 §13A.7 facet row "edit-domains one-home lattice (D43)" T1: the bridge-seams fence assert (the deep-imported module ≡ engine MIN/MAX_SPEED — the WSOLA/ticks pattern); the computed-intersection property (`FIT_TO_FILL_RATE` == [MIN_RETIME_RATE,MAX_RETIME_RATE] ∩ NATIVE_VENUE_SPEED ∩ WSOLA_RATE ⇒ [0.1,5], and **refuses correctly when WDC narrows** — the property that kills static text); `minDurationTicks(fps)` + dead-zone predicate pins; the nle-ui local twin pinned ⊂ (pin failure ⇒ align nle-ui to the one-home, never edit the module); the Stage-0 zero-behavior-change re-export check (volume-dB + retime leaves re-exported VERBATIM). Battery: check-2 widened to the C1-fold core-file→OT-leaf edge (bridge/ + the folded timeline-math.ts site), still leaf-only. Phase: r1 Stage 0 + battery_r28. |
| **D44 two-tier error envelope** — 25 codes class-tagged, 'domain' constraint, never-silent echo family, 9 lockPreCheck riders | LANDED: 15:2965-:3014 (the taxonomy + class-tag table + never-silent law + benign-echo family) | **STALE** — the mechanism EXISTS (17 §2.5 rule-8 :450-:454; §13A.4.2 :2293; §13A.1 :2263 — D44's own named audit mechanism) but all three pre-R28; 05:25's GAP row still says "the ~24-code envelope" | Re-key the three sites to the two-tier law: every one of the **25** fine codes (24 + `RATE_OUT_OF_DOMAIN`) triggered ≥1 test asserting the **code AND its CLASS** (per 15 §6.3's class-tag table) + the constraint type where declared (`'domain'` in the union); the **benign-echo family asserted as `ok:true`** (A9 set-alls, zero-delta `retimeKeyframes`, D-T7 same-position `moveElements`) and never counted as refusals; the never-silent guard (no other `ok:true` non-mutation is spec-legal); the **9 lockPreCheck riders** each with a TRACK_LOCKED trigger (roll/slip/slide/rateStretch/retime/freezeFrame/rangeRemoval/replace + the widened placement-target). 05:25's row re-keys to "LANDED at 15 §6.3 — the 25-code two-tier envelope; the r1 refinement is the OT follow." Phase: the census re-key now; the riders' triggers ride the r1 ports. |
| **D45 NS-4 bridge adapter** — SceneTracks-blind projector; consumer-preview `{grade}` one-home; buildGradeFilterString | LANDED: 08:24 (R9-c ratified as D29.5c's evolution; `buildGradeFilterString` engine-side as the pinnable string law; the app migration registered). NOT LANDED: 04's twin row still reads "declines … BY DESIGN" (04:24 tail — the "ratified-as-evolution, r3-consumable" flip is a W4 residue) | **PARTIAL/STALE** — 17 §13A.5 (:2299-:2308) covers the D12 flat-seam projector's four properties; the render-plan projector (the visual twin) + the R9-c seam have no rows | Extend §13A.5 (or add §13A.8 "the render-plan projector (NS-4)"): the same four properties on `project(scene) → TimelineData` (deterministic, idempotent, never-loss, no-readback) + the **venue-blindness architecture check** (the venue stack's import graph contains no SceneTracks type — the static no-readback pattern applied to the adapter's consumer) + the idempotence-under-identity trap warning (NS-5's array-identity plan cache — reprojection only on array identity, never per frame). R9-c seam pins (r3): `buildGradeFilterString`'s string law (the `buildElementFilterString` precedent pattern — canonical order, neutral `''`, identity passthrough), the compose-then-filter ORDER pin, and the app-side wrapper-retirement migration pin (ProgramCanvas + deliverService switch to `{grade}`). Phase: r1-entry (the adapter is the r1-entry architecture decision) / r3 (the R9-c consumption). |
| **D46 the four absent families** — replace/append/fit-to-fill/ripple-overwrite + the transition-remap probe | LANDED: 06 §5.9C-F GAP rows (RE-1 :1622, AP-2 :1642, RO row :1644-:1653, FF-1/FF-2 :1683-:1687) + 05:21/§16.5A re-tags + 15:5025 (the 80th member) | **MISSING in 12/17** (no §3.1 rows; 06's Testing carries ONE row for ripple-overwrite @ :3305 only); the probe protocol has **no spec home** (ARCH-R28 §4.1 D46-B2 only) | Four §3.1 matrix rows (T1 ✅ / T3 where keyed / Property ✅) + §13A.7 facet rows citing the §5.9X pin sets: replace (exact-length, no-move, undo atomicity, transition survival, companion untouched); append (M16-class: append-at-track-end, playhead-ignored, multi-append order + one-entry, linked-pair sync + atomic refusal, empty-track→0); ripple-overwrite (the delta-law trajectory row 06:3305 already names; the downstream-set diff check = the E1-a condition); fit-to-fill (the mock's 37 per-mode pins re-expressed OT-side + the A/V same-rate one-commit pin + the out-of-domain INVALID_PARAMS refusal). **The probe protocol needs a home**: a PROBE row in 06 §5.9C's GAP set (owner S-ot, phase Stage-4 entry) — a time-boxed OT spike with two exits: (A) a pinned `transitionOut` endpoint-remap test green ⇒ replace proceeds; (B) the redesign verdict filed as a new D-entry ⇒ the deferral lever fires. Phase: r1 Stages 2-4 per the leverage map. |
| **D47 the param trio** — flat self-addressing keyframes; both verbs; insertBatch ripple + 79th/80th arithmetic | LANDED: 15:4335-:4371 (flat schemas), 15:5022 (the D47 rulings folded), 15:5023 (insertBatch `{elements, placement?, anchor?, ripple?, idSeed?}` — push by the block's TOTAL span), 15:265/:320 (the retimeKeyframes/Keyframe family rows) | **STALE + MISSING** — 17 §13A.4.1 :2292 says "78 union members" (→ 80 at the r1 bump); 17's header :4 "the singular verbs RETIRED" contradicts D47.3 ("insert stays singular"); no facet row for the flat-form laws or the TOTAL-span push | Re-key §13A.4.1 to 78 **→ 79 → 80** (two new sweep examples: bare `insertBatch` and `replace`, generated from 15 §5's example table per the single-source law); fix the header sentence (the singular remove/retime retire; insert's singular form stays); add to §13A.4's transaction family: the **insertBatch ripple TOTAL-span pin** (push magnitude == first-element anchor + relative offsets composed == insert's D31.6 full-duration push) + the flat-form conformance rows (flat `KeyframeRef[]`/`{keyframes[], deltaTicks}`; the grouped `{elementId, keyframeIds[]}` form REJECTED post-bump — the old-replay deprecation path per §2-law-5). The 79/80 arithmetic becomes a battery_r28 census class (15 §13.15's counterpart counter). Phase: the re-keys now; the pins ride the r1 union-version bump. |
| **D48 marker v2** — duration+notes; notes-owns-note/label-synthesized; scene-shrink; ⇧M start-match | LANDED: 16:342/:351 (⇧M start-match ±1 frame; the ⌥M inspector v2 field set), 05:724 (scene-shrink keep-and-display-clamp), 10:586/:602/:616 (export mapping). **NOT LANDED: 09:286-:292 + :335 (the interface/ruling still pre-D48), 15:1566-:1600 (§4.3.49-51 params unwidened), 18 §4.4 (no marker-inspector rail swap)** | **MISSING** (no marker row in 17 §3.1/§13A; 05's Testing has the point-marker row only @ :1798; 16 Appendix C :2414 = 5 point-era tests) | Prereq (W6 delta sweep): complete the model fold at 09 §3.1/§3.1A (+zod cleanup), 15 §4.3.49-51 (`duration?`, `notes?`; disposition the stale `type?` field), 18 §4.4. Then: 17 §3.1 row "Markers (point + range v2)" — T1: add/update/delete + range validation (`end = time+duration`, ≥1 frame, ≤ scene duration); the **scene-shrink clamp pin** (a shrink keeps the marker, display clamps — never a delete); the **notes-owns-note round-trip pin** (`Marker.notes` ⇄ OT `Bookmark.note`; `label` doc-side-synthesized — the id pattern; the export mapping pin per 10:586); T3: the ⇧M **START-match** window (±1 frame; strictly-inside-span deletes nothing) + ⌥M inspector field-set rows (Duration range-only). Phase: the wire surface rides the r1 union bump if the model fold lands there; the marker v2 family is r5-entry (the registered phase — clip markers bundle); the keymap rows ride 16's K3 corpus. |
| **D49 captions** — track-kind hybrid; addTrack 'caption'+language; burn-in exclusion; TextElement body | LANDED: 15:1092-:1115 (addTrack row: the 'caption' kind + `language?` BCP-47 + the r1 OT widening), 10:544 (the text body's home = first-class `ElementJSON.text`). **NOT LANDED: 09:119-:124 (no `captions` family in SceneTracksJSON), 10:811 (§8.1 blocker unflipped — "our project model doesn't yet"), 18 §4.4/§4.7, 05 §7.3/§12.1/§12.2** | **MISSING** (no caption row anywhere in 12/17; the mock's 10 live pins are register-side only) | Prereq (W6): complete the model fold at the five sites above. Then: 17 §3.1 row "Caption tracks (kind + language + burn-in exclusion)" — T1: `addTrack {type:'caption', language}` BCP-47 validation; the **DESCRIPTIVE duplicate-language law** (addTrack does NOT reject; export merges deterministically — D49/F1) + the r5 enforcement registration; the **burn-in exclusion pin** (captions excluded from `elementAtTime` — the one-line kind filter); the text-body round-trip (`ElementJSON.text` first-class, retiring the `params?.text` stand-in); T2: the burn-in pass as the topmost visual layer; r5: the file-per-language export granularity (SRT/ASS/VTT). Phase: the OT rows (TrackType + SceneTracks.captions + the addTrack machinery) at **r1** (the register flip's stated cost); the export corpus at r5. |
| **D50 the five pages** — page-key tests; fx density exemption `page==='fx'`; ⌘1-⌘5 | LANDED: 16:365-:379 + :418/:622/:645 (the five-page law + the three-claimant ruling) + 16:28 (the remaining K3 test work registered) + 18 v1.8 status/:246. **NOT LANDED: 18 §4.8's body (the three-page OPEN registration stands) + 18:41 ("collapses to 3 pages")** | **STALE + MISSING** — 08:2168 carries the RULED-OUT `keyboard-cmd-1-through-9-switches-color-grading-panel` row; 18 §12 has no page-key tests; 17 §13A.6 has no page rows | Re-key 08's Testing row (⌘1-⌘5 = page switches per 16 §3.8; effect presets stay `1`-`9` no-modifier) + 04:2293's twin; re-key 18 §4.8/§1 to the five-page ruling. Then 17 §13A.6 (or §13A.7) page-system facet rows: the **⌘1-⌘5 page-key tests** (Edit/Color/Deliver/Audio-focus/FX; the Audio focus-mode toggle — re-click/Esc exits; per-page view-state memory); the **fx density exemption keyed `page === 'fx'`** (NOT `fxMode` — the Edit-page FX tool keeps the Edit page's compact entry; the ViewOptionsPopover DOM-absence law); the ⌘6-⌘9 reserved-rows pin. Phase: K3/r5 (XMOCK-6's registered impl half — 16:28 already owns it; 17 just needs the facet row so the gate can key on it). |
| **R16-R18 (the round maintenance rows)** | LANDED: register + plan re-keys (battery_r28 named at plan:36; the WRAP pair 1,939/68 + 1,950/126) | **STALE** in 12/17 (the pin/posture rows — §1.2/§1.3 above) | The W5/W6 mechanical re-base (already registered as battery_r28's charge — the corpus-wide stale-pin re-base f9ac806→074a2f6 etc.): 12 §0 (:4/:14/:18/:36), 17 (:4/:20/:18/:34/:2730/:2740/:2359). Not new law — but it must land WITH battery_r28 or the count-discipline law (17 §0A rule 4) fails its own battery. |

---

## §3 The cross-cutting gaps

### 3.1 The r1 port acceptance protocol — R28-widened, unconsolidated

The plan's r1 row (IMPLEMENTATION-PLAN.md:67) states the protocol for the LANDED families: "Every port: the carried engine tests green in OT + the census self-re-declaring (D29.2, proven thrice) + the 05 §8A grammar row + the D32 fan-out row + the battery's mode-matrix re-derive." The R28 rulings ADD five protocol elements that exist nowhere as test law:

1. **D42**: the ported pins re-keyed to the pairwise field (09:345 — "the carried E1/E2/F6 pins assert link values through undo/redo/serialization"); rider 9's count reconciliation (the plan's S-engine(4) row now carries 13+13+20+12+table+11, decomposition UNKNOWN — landed at W4).
2. **D43**: the Stage-0 zero-behavior-change fence (the one-home re-export verbatim + the battery's leaf-only import check widened to the C1 fold's edge — ARCH §4.1 D43-A3, unbuilt).
3. **D44**: the 9 lockPreCheck riders + the never-guard/tsc-lockstep compile checks + 17 §2.5's census (the audit mechanism — stale, §2).
4. **D46**: the transition-remap probe at Stage-4 entry (two exits — the only test that gates a family's INCLUSION) + ripple-overwrite's downstream-set diff check (the E1-a condition restated as B2).
5. **D47**: the union-version-bump migration path (§2-law-5's old-replay deprecation; the grouped-form retirements ride the same bump).

**Fix:** ONE consolidated r1-port acceptance clause — either a 17 §13A.7 facet row ("the r1 port acceptance protocol") or a rider on the plan's r1 row enumerating the five additions with their per-stage pin families. Without it the executors' work orders carry the Stage ladder but not the R28-widened gate content; the probe especially (a gate with two exits and a cut consequence) must not live only in an audit doc.

### 3.2 The battery's test-pinning law — R28 classes unbuilt

battery_r27.py keys the R27 world (`FLEET_HEAD` @ :133 = engine `f9ac806`/748 …; the roof arithmetic @ :388-:394; the count-consistency class @ :381-:384). The registered W5 fork (battery_r28) must carry, beyond the pin re-base: **D43-A3** (the check-2 widening — the C1-fold edge, leaf-only); **D44-A6** (a class-tag completeness class: the 25 codes × one class each, scraped from 15 §6.3's table + the census-script re-key); **D47** (the 78→79→80 counterpart arithmetic + the insertBatch-ripple/replace §13.15 rows); **D50 site 19** (the round-final OPEN-table state: rows 5-8 closed via D48/D49/D50 → 0, assert the four closed dispositions + the A3 RULED row); **R16** (the dead constants drop — battery_r27:310-311's 1796/65). None of these are named in 12/17 (the battery's spec-side posture homes) — they live only in ARCH-R28 §4.1's addenda. **Fix:** 12 §0's battery-posture row (:36) gains the R28 class list when battery_r28 lands (one edit, W5/W6).

### 3.3 The plan's test-track carve — distributed, and the R28 test-law fold has no owner

There is NO dedicated test stream — by design: the K-ladder (plan §2, :42-:72) IS the verification spine (K1 module nets / K2 combined + the 10-mode matrix / K3 app-behavior / K4 the e2e exit), every module stream's gates column carries its suite counts, and **S-spec(2)** (:25) owns the spec-side test duties: "the K2 registration at K2 entry: the combined-family census + the 10-mode matrix twin row in 17 + the D30.3 zero-orphan-rows law." Two holes:

1. **The 10-mode matrix's 17-side twin row is still PENDING** (the plan's K2 row says "registered in spec 06/17" — 06 §0 carries it @ :14-:29; 17 carries nothing; `rg "mode matrix|ten-mode" 17-test-plan.md` → 0 hits). This was already a registered S-spec(2) duty at K2 entry; D46's RULED-IN re-tag widens what the twin must say (the four families' rows now carry live scheduled rows at Stage 2-4 with the deferral lever).
2. **No stream owns the R28 test-law fold.** The W4 fleet grid (ARCH-R28 §3, :92) lists the fold's file set — "00/03/04/05/06/07/08/09/15/16/18/19 + the register + the plan + the signoffs" — **12/17 were never in it**; the W6 cross-cuts (:94 — mode-matrix, census, register, E2-consistency, the plan's cold-executor re-test) don't include a 12/17 test-law pass either. The result is §1.1's headline: a fully-folded design corpus whose test law is one round behind — the exact failure mode the user's law names.

**Fix:** add S-spec(6): "the R28 test-law fold — 17 §3.1/§13A/§2.5-rule-8 + 12 §0/§8 amendments for D42-D50 (this audit's §5 P1s), landing with (or before) battery_r28" with the acceptance "17 §14.4 step-0 audit green: every D42-D50 facet has a matrix/facet row; battery_r28 carries the new census classes." The K2 registration (S-spec(2)) absorbs the widened mode-matrix twin.

---

## §4 The SIGNOFF re-stamp question

**Standing.** TESTABILITY-SIGNOFF.md is the R7-era certification of the scout→audit→revise process, rider'd at R27 (:3) with four re-stamp conditions: (1) the R27 amendment wave W6 — LANDED; (2) battery_r27 green; (3) the register's OPEN decisions ruled; (4) K4's crawl exit. FINAL-SIGNOFF.md carries the same rider.

**What R28 changed:** condition (3) is now **MET** (the register's OPEN design table is at 0 rows — D48/D49/D50 closed rows 5-8 at W4; REFERENCE-REGISTER's round-final note records it); the R28 analog of (1) — the W4 corpus fold — LANDED 2026-09-16 @ `980920b`, though with fold residues (§2's D48/D49/D50 prereq rows: 09's marker interface, 15 §4.3.49-51, 18 §4.4/§4.8, 10 §8.1); (2) battery_r28 is W5/W6 pending; (4) K4 pending.

**Recommendation — re-RIDER now, do NOT full-re-stamp.** The rider (one paragraph at :3-:5, same form as the R27 one) should record: the R28 state of the four conditions ((3) MET; (1) MET-with-residues; (2)+(4) pending); the pin-set re-key (K1 at the R28 pins: engine 749/749 @ `074a2f6` (code anchor `74bef08`) / OT 632 @ `55c81c0`/`970948a` / WDC 777 @ `83b8850` / nle-ui 690 / app 252 @ `85cff80`); and **the two stale-number riders re-keyed a second time**: the union count "73 EngineCommand types" → the live census 31 = 28+3 with the **ruled r1 projection 78→79→80** (15:5023/:5025); the keymap "~180 bindings" → **the 182-row canonical registry** (16 Appendix A, re-keyed R28-D50); plus the error registry's **25-code two-tier** shape (15:3002). A full re-stamp should wait for battery_r28 green + K4 — and should additionally certify **test-law completeness** (this audit's P1s landed + a 17 §14.4 step-0 sweep green). Without that clause the seal would certify a corpus whose test law lags its design law by one round — which is precisely what this audit found happening again (the R27 rider was written after an identical lag; the pattern repeats every round the test specs are left out of the fold's file set).

---

## §5 The ranked fix list

**P1 — spec bugs per 17 §14.4 step 0 (a facet with no row anywhere): the D42-D50 test-law rows.** One edit wave, owner S-spec (the new S-spec(6) item), landing with battery_r28:

1. **17 §2.5 rule-8 + §13A.4.2 + §13A.1's discipline row → the D44 two-tier census** (25 codes × class + constraint types; the benign-echo family; the 9 lockPreCheck riders). The cheapest P1 — the mechanism exists; only the shape re-keys. D44's ruling names this exact site as its audit mechanism.
2. **17 §3.1 + §13A.7: the D42 linkage row/facet + 12 §8's three linkage properties** (symmetry/cycle-free; no-dead-pointers after any op; undo/serialize preserves `linkedTo`). The pin set already exists in 06's GAP rows — this is a mirror, not authorship.
3. **17 §3.1's four D46 family rows + §13A.7 facets citing the §5.9X pin sets + the transition-remap probe's spec home** (a PROBE row in 06 §5.9C's GAP set — the two-exit gate currently lives only in ARCH-R28 §4.1).
4. **17 §13A.4.1's 78→80 re-key + the header's singular-retirement fix (D47) + the insertBatch TOTAL-span push pin.**
5. **The D48/D49/D50 model-fold completion (the W6 delta-sweep prereqs):** 09:286-:292/:335 (marker interface + A2 text + zod), 15:1566-:1600 (§4.3.49-51 params + the `type?` disposition), 09:119-:124 (the captions family), 10:811 (§8.1 flip), 18 §4.4 (marker inspector + caption rows), 18 §4.8/:41 (the five-page re-key), 08:2168 + 04:2293 (the ruled-out ⌘1-9 rows). *Filed here because they block the marker/caption/page test law; the W6 sweep owns the landing.*
6. **The marker/caption/page facet rows (D48/D49/D50)** once (5) lands — per §2's minimal statements.

**P2 — the consolidation + registration items:**

7. **The r1-port acceptance protocol's consolidated clause** (§3.1) — a 17 §13A.7 row or a plan-r1 rider enumerating the five R28 additions.
8. **S-spec(6) lands in the plan** (§3.3) + the K2 registration's 17-side mode-matrix twin row absorbs the D46 re-tag.
9. **12 §0 + 17 §0A/header/§17A/§13A.7-re-tier-row's R28 pin re-key** (749/632/777/690/252 @ the R28 pins; variants 1,939/68 + 1,950/126; mini 495/12; battery_r27→r28) — rides W5/W6's registered re-base but MUST land with battery_r28 (else 17 §0A rule 4's own battery fails).
10. **battery_r28's five new classes** (§3.2 — D43-A3, D44-A6, D47 arithmetic, D50 site 19, R16 dead constants).
11. **17 §14.2's mapping-table repair** (drifted anchors; add the 15 §15.4 / 16 §9+§16 / 20 §10 rows).
12. **17 §13A.5's NS-4 extension** (§2's D45 row: the render-plan projector's four properties + the venue-blindness check + the {grade} seam pins at r3).
13. **The SIGNOFF re-rider** (§4 — both files, one paragraph each).

**P3 — hygiene:**

14. 16 Appendix C's marker row 5 → the v2 enumeration set (range add/delete/start-match/inspector rows) — rides the K3 corpus anyway.
15. 05:25's "Error-code coarseness" GAP row re-key (~24-code → the LANDED 25-code two-tier envelope; the row's "amendment first" precondition is MET).
16. 12 §8's property suite gains the D46 family invariants (append/ripple-overwrite join the no-overlap/undo families; fit-to-fill's exact-fill + refusal invariant).

---

**Audit verdict (one line):** the corpus's test LAW is one full round behind its design law — the W4 fold amended 13 law-side files and zero test-side files; by 17's own §14.4-step-0/§13A.7-enforcement rule, every D42-D50 facet is currently a spec bug; the fixes are cheap (six P1 edits, mostly mirrors of pin sets the owning specs already carry) but must be owned (S-spec(6)) or the R29 round will inherit the same lag.
