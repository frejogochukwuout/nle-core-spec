# integration-review-r25 — the R25 round gate (fresh context, read-only except this file)

**Reviewer:** INTEGRATION REVIEWER (fresh context). **Date:** 2026-09-09. **Scope:** the whole R25 round — the ruling (ARCH-R25 v2), the 27 fleet reports (5 scouts + 10 modes + 5 cross-cuts + 2 adversarial reviews + 5 verify), the 8 round commits (`3684166..b5a49e4`, pre-round base `850b973`), the amended corpus, the three cross-repo filings, and the battery. **Method:** the ruling read in full; both adversarial reviews folded-claim-by-claim against the v2 text and the landed corpus; `python3 scripts/battery_r25.py` re-run by this reviewer (**123/123 PASS**); the round's full diff of `15-wire-protocol.md` read hunk-by-hunk (the census invariant); the filings read at their target repos (`82b3ea9` / `275bb9d` / `24e1fff`); the plan's six cold-executor rows re-resolved live after the wave-6 stamps; the key regions of every amended file spot-read (`06 §0/§5.0/§5.9/§5.9B-F`, `05 §8A`, `16 §3.4A/§3.4B/§3.2`, `18 §9/§4.3`, `09 §B1`, `00 D30-36 + §2A.8/§2A.9`, `REFERENCE-REGISTER.md`, `IMPLEMENTATION-PLAN.md`).

---

## The four asks (verdicts + evidence)

**Ask 1 — "One more round of deep review/audit… budget not 5 or even 10 sub-agents but much larger, potentially 50+… multi-pass, drive all areas." → MET.**
The round ran **33 sub-agents across 5 waves + 2 adversarial passes + a verification pass + a filing wave + this integration review**: 5 module scouts → 10 one-per-mode agents → 5 cross-cuts → the ruling → 2 fresh-context adversarial reviewers (design-consistency: 15 findings, every load-bearing claim re-derived; corpus-faithfulness: ~50 claims re-derived live, 44/50 exact) → 6 amendment agents → 5 verification agents (fresh-context per amended file + the cross-file sweep) → 3 cross-repo filings → battery_r25 (123 checks, teeth-tested: the mini corpus is SCRAPED via a live vitest run that fails hard if the JSON output is absent; the variants' 1,588 it-blocks / 61 files re-counted from source). The literal "50+" agent count was not hit (33); the multi-pass structure was — every pass adversarially re-derives its predecessor's output, which is what the ask's intent ("absolutely tight and final") purchases. Every corpus area was carved: 5 repos + 10 modes + 5 cross-cuts + the 9 amended files + the register + the plan.

**Ask 2 — "Check for updates from nle-ui/app… better seam integration… mostly sealed?" → MET.**
Five fresh scout cards at the R25 pins: nle-ui `3026099` (6 shell/keymap/docs commits, the timeline gesture surface byte-identical to R24 — sealed); app `64fb0ab` (**ALL D30 waves W-C..W-G landed**: the wire-dispatch port, the keymap lockstep with an upstream-back fix, real Save/Load, the M49C-coverage-gate port, the CR filings; carriers 7→5 at the declared irreducible set); engine `3989506` (one docs-only commit); OT `fdb771c` (code byte-identical to the `c15a629` pin — the R9 seam queue is docs-only); WDC `85b81b0` (zero movement, seal held). The "mostly sealed" verdict was answered honestly at two levels (the ruling's §2.2/§2.3): modules YES; **the spec's own orders were lagging** — and the four lagging items (the app `.agents/` bootstrap overdue ×2, the census-CI mechanism with no ci.yml step, the WDC waveform filing that had never landed in ANY venue, the engine's 3-of-4 stale R9 dispositions) were all converted to QUEUE FILINGS and **executed this round** (see The gates, item 4).

**Ask 3 — the two new DaVinci mocks (trim_edit_modes.html + timeline_edit_modes (2).html): "Did we have all these specified? If not deeply analyze." → MET.**
The answer was a definitive **NO, 6/4**: four TRIM modes specified-but-unwired (engine class-methods only + variants-mock), insert/overwrite deep-implemented engine-side but stated in no prose, and **REPLACE / APPEND-AT-END / RIPPLE-OVERWRITE / FIT-TO-FILL absent from the spec corpus and all five product repos** (grep-verified by ten independent mode agents, re-verified by both adversarial reviewers; the faithfulness reviewer precision-corrected the headline to "no spec row, no production codebase; the mock design docs + the variants mock are the registered references" — the v2 carries the correction). The deep analysis is the round's spine: ten per-mode reports → D30 the ten-mode matrix (now law in 06 §0, re-derived every fleet pass, the K2 zero-orphan-rows gate) → D31/31A the 06 §5.9 restructure + the four family contracts (§5.9B-F, each with r1-scheduled GAP rows: OW/RE/AP/RO/FF) → D32 the linked-companion law (06 §5.0's ten-mode fan-out + the E1 defect) → 15's four r1-scheduled verb rows (census NOT reopened) → D34 the keyboard family (16 §3.4A/§3.4B). One P1-class defect found and live-probed twice (E1, the linked mid-clip insert overlap — a 00:838 violation), filed into the engine's queue with the 4-postcondition fix contract.

**Ask 4 — the shell mocks as references for what the spec missed/under-defined. → MET.**
A systematic harvest, not a skim: the 19-family census (xcut-shell-mocks; 11 aligned / 3 diverging / 8 with mock-only content — **overlapping classes**, the arithmetic honestly re-worded per the adversarial fold), **REFERENCE-REGISTER.md** (19 rows, cross-spec-owned; the C33-C58 ledger intake with every entry dispositioned — 3 ADOPTED / 8 LANDED / 12 QUEUED / 2 DECLINED / 1 SUPERSEDED, zero untracked), D35.4's four doc-level rulings landed (SourceEditBar + SourceRangeBar rows in 18 §4.3, the deliver RangeBand rows in 18 §4.8, the 3-vs-5 page ruling registered OPEN), the four OPEN decisions visible in the register's table (marker-v2, captions, 3-vs-5 pages, FX-page grammar — deferred to future design rounds, by ruling), and the visual-grammar register (the census's "12 of 13 elements pinned NOWHERE" → 05 §8A structural + 18 §9's 8 tokens VERBATIM from the fleet proposal + the green-over-amber ruling + D33.5's fifth-fork-prevention sequencing law).

---

## The gates

1. **battery_r25 = 123/123 PASS — REAL.** Re-run by this reviewer: `python3 scripts/battery_r25.py` → 123/123. The gate has teeth, re-derived from the round's own classes: the mode-matrix (exactly 10 named rows), the family sections + the OW/RE/AP/RO/FF/E1 GAP markers, the P-list phantoms dead (orphan-sweep greps), the grammar (≥12 §8A rows, the 8 tokens, the era-qualifier), the keyboard family (§3.4A/§3.4B + the four-meaning row), the register (19 rows, C33-C58 complete, "Zero untracked"), the plan (S-app post-D30, K2 zero-orphan-rows, root-HANDOFF venue, estimates + the r1 rider), the pin re-bases (all five R25 pins + the consumer pins), the census-untouched class, and the **live scrape** of the mini corpus (355/355 via a real vitest JSON run; the R24 F1 strict port — fails hard, no vacuous pass) plus the **live it-census of the variants** (1,588 blocks / 61 files). The stale-pin sweeps, the typo class, and the count-consistency classes are all mutation-sensitive constraints.
2. **The ruling's review trail — the v2 claims MATCH the reports.** Design review (4 P1 / 6 P2 / 5 P3, GO-WITH-AMENDMENTS): all four P1s folded and verified in the landed corpus (F1 → D32.1's ripple-overwrite companion row + D31.6's clauses; F2 → the ⇧⌥. re-chord with the taken-keys rationale + 16's rows #19/#20 split-family resolutions; F3 → D31.1's everywhere-clause + P11 landing in 15 §4.3.9/§13.15 + 16 §3.2's precision clause; F4 → D30.3's satisfiable zero-orphan-rows form, verbatim in 00 + the plan's K2 gate). All six P2s folded (F5 at doc level — the 06 §5.1 quote-vs-law note + 09 §B1's R25 note; the deep 1-split pairwise-representability mapping is carried to the r1 groups→pairs port; F6 → D31A + the transition/zero-floor/companion clauses + the F1A-2 trajectory; F7 → the overlapping-classes re-word; F8 → D36.4's kept nle-ui filings + the QUEUE-FILING form; F9 → 18 §5A's layering; F10 → the no-F-alternate ruling + the 15 §4.3.45 enum-closure sentence IS in 16 §3.2's boundary). Of the five P3s ("fold at will"): F12/F13 folded; F11 resolved in practice (no §5.9A exists; the two stray §5.9A-F refs fixed by the verifiers); F14/F15 partially (tokens deferred to the xcut report ✓, census arithmetic ✓, estimates ✓, WDC pin listed ✓ — a handful of cosmetic nits remain, see The open flags). Faithfulness review (5 fold-ins + 6 precision fixes): all five fold-ins landed (D36.3 restated honestly, D31.3's 07:367 carve-out, D33.1's token deferral with `--source-active-border` restored, D36.4's W8-corrected disposition, the census arithmetic); the precision fixes landed (the "NOWHERE" scoping + the parenthetical, the E1 pin-evidence wording — corrected again by verify-06 to the live vitest facts). The v2's "all folded" is true for everything the reviews REQUIRED; the residue is P3-class polish, none semantic.
3. **The verification fleet's flags — dispositioned:** the 06 verifier's four pre-existing ripple-flag teaching sites (§5.4:988 / §12-tail / §13 Insert row / §11.3) were **closed by verify-crossfile** (the D31.1 precision notes appended to all four, confirmed in the working tree); the 06 verifier's 00:804 self-citation flag was closed (crossfile fix #8 — the invariant is at 00:838 post-R25); the 16:283 garbled D34.1 echo was closed (the 05-16-18-09 verifier's fix #1, crossfile-confirmed verbatim vs 00's current text). The genuinely OPEN items are listed below.
4. **The cross-repo filings SHOW in the target repos (verified live by this reviewer):** WDC root `HANDOFF.md` **item 0** — the [FILED] waveform-contract promotion block with the audio-registry.ts:24-74 contract, at `82b3ea9` (the only K3 hard-gated external prerequisite; second-round miss now closed at the correct venue); nle-engine `.agents/PLAN.md` — the R9-disposition amendment block (R9-a [~] future-extension, R9-b [x] LANDED + the app residual re-filed, R9-c [~] declined-by-law, R9-d stands) + the E1/E2/E3 blocks with the 4-postcondition fix contract, at `275bb9d`; nle-test-app `worklog.md` tail — the census-CI mechanism block (scripts/census-check.mjs + the register doc + the ci.yml step + the mutation gate + the standing S-app queue notes), at `24e1fff`.
5. **The census-untouched invariant (check C) — HELD, byte-stable.** The round's full diff of `15-wire-protocol.md` (850b973→HEAD) touches ONLY: the status ledger append, §0's R8-row status note + the new r1-scheduled families row, P7 ×2, P11's §4.3.9 doc-comment, §13.15's header ledger note, the §4.3.9 row's P11 append, and the four NEW QUEUED rows (each carrying "NOT in the landed census; the 24 routed + 6 exceptions stand untouched"). **No hunk intersects the census bytes** — the 24+6 figures appear only as unchanged context; the census table, §4.1's census row, the error-code rows, and the column headers are untouched. The two appended ledger notes are house-law-conformant (2.5.1). battery check-class R enforces it; verify-00-15 hunk-audited it independently.
6. **The census-script name + the filing SHAs (check D) — CONSISTENT.** One name repo-wide: `scripts/census-check.mjs` in the ruling D36.1, 00's D36.1, the plan (S-app / R2 / the §7 R25 row), and the app filing itself (the verify-00-15 agent unified a three-way split). The SHAs `82b3ea9` / `275bb9d` / `24e1fff` are consistent across the plan's S-wdc/S-engine/S-app rows + §7's R25 row + the wave-6 commit message, and each resolves to a real commit in its target repo whose content matches the ruling's filing spec.
7. **The plan's cold-executor rows after the stamps (check E) — all six RESOLVE.** R1 (app README + worklog tail + the nle-ui `.agents/` R9-close content sources) ✓; R2 (`UPSTREAM.lock.json` upstreamHead `c15a629` + no census step in the app's ci.yml — re-verified live) ✓; R3 (the engine queue at :135-163, the 4 original unchecked entries readable, with the amendment block appended below — and R3's GATE is now green: the dispositions textually match D36.2 and the app-side maintainPitch row exists) ✓; R4 (the root HANDOFF venue exists; the row is now PRESENT as [FILED] item 0 — the entry-point cell's "the waveform row absent" description is now stale, see flags) ✓-with-a-nit; R5 (OT `.agents/PLAN.md` R9 block at :422-466, the titled-block shape demonstrated) ✓; R6 (the app vitest + `wire-coverage.test.tsx` + LAW-NET-INVENTORY.md + the named mode rows) ✓.

---

## The open flags (each: the flag + the recommended disposition)

1. **The 16-vs-15 wire-shape tension (verify-05-16-18-09 F1).** 16 §3.4A's `,`/`.` command shapes (`mode:'insert-edit'`, `type:'overwrite'`) do not match 15 §13.15's r1 decision (the whole source-edit family shares the extended-InsertCommand seam). Both rows are (r1-scheduled)-marked; 15's landed union is untouched; 16:267 itself registers "the final wire shape rides that wave's param alignment." → **CARRIED** (the r1 param-alignment wave owns the ruling: adopt 16's explicit source-edit verbs or re-key 16's two cells to the InsertCommand seam — registered in the verify report + 16's own text).
2. **The register's rows 2/3/4 flip-cells (verify-00-15 Flagged #3).** The divergence-rule cells still read "flips LANDED when the v1.2 rows land" while 18 v1.7's rows landed this same round (row 2's condition fully met; rows 3/4 partial — 16/09 halves + the 15 §13.15 exportMaster.range pointer still pending). The citation-form column already records PROPOSAL→ADOPTED. → **FOLD-NOW at the wrap** (3 cells, trivial; keeps the register's own "flip at the next register pass" promise) — carrying is acceptable, the truth is in the citation-form column.
3. **D35.2's per-row pin-count enforcement.** The battery enforces the two SUITE-count pins (the variants' 1,588 it-blocks / 61 files re-derived from source; the mini 355 live-scraped) but not the 19 rows' individual test-pin counts, which D35.2's letter promises ("every row's test-pin counts re-derive live"). → **CARRIED** (the S-spec standing row — "the register maintenance, every round" — owns it; battery_r26's natural extension).
4. **The R24→R25 census arithmetic (verify-crossfile Flagged #1).** The R25 state "35 zero-action + 5 carriers + host" does not decompose cleanly against the R24 frame "40 port files = 39 mirrors (32+7) + host." Routed to the battery + the next register amendment; the battery currently checks presence, not coherence. → **CARRIED** (the app's census-CI EXECUTION forces the reconciliation — the filing's register doc must declare the per-pin values the script checks against; the mechanism is filed, the arithmetic settles when the script is written).
5. **The variants' suite-count is a static source-count.** The battery counts line-start `it(` blocks (documented in its docstring: "the variants' deps are not installed here, so the census is the register's own method") — an authored-test census, not a passing-test run, unlike the mini's live scrape. → **CARRIED** (honest, documented; a live run requires deps the environment doesn't carry).
6. **The plan's R4 cold-executor entry-point staleness.** R4's entry-point cell still describes the pre-filing state ("items 1-4 enumerated, the waveform row absent"); the row is now present as [FILED] item 0 and the insert-half of R4's action is done by the filing (the pin + the 759→ test remain the WDC session's work). The S-wdc TRACK row carries the accurate FILED stamp. → **FOLD-NOW at the wrap** (one-cell refresh of R4's entry-point/action; optionally a one-line note on R3's already-executed doc-half) — or carry; the S-rows are the primary orientation.
7. **Un-folded P3 polish nits (all cosmetic, all carried):** 06 §5.9's boundary sentence says "rejecting or **displacing**" where the reject-not-shift law's letter is "falling through" (design F14); the re-declare law is cited as "the D29.2 pattern" at 06:12 and 00's D30.2 but "D29 F8" at 15's §13.15 and 06:1632+ (one law, two names); the matrix's "15 wire" column header could carry F15's "spec union; routed-truth in the OT column" qualifier; 17's §13A facet rows for the four new families were not authored (they ride the r1 port wave, per the mode reports' acceptance rows); 09 §B1's 1-split pairwise-representability subtlety (both halves carry `linkedTo` → A; A names at most one — F5's deep half) rides the r1 groups→pairs mapping. → **CARRIED** (each at its next natural edit / its owning wave).
8. **Pre-existing D26 wording (verify-00-15 Flagged #4).** 00:423's "D30's declared irreducible carrier set" means the APP's D30; the namespace note in 00's status line is the only disambiguator. Byte-frozen by law. → **CARRIED** (a future D26 natural edit writes "the app's D30").

---

## The honest limitations (the round did NOT do — all registered, none papered over)

1. **The four absent families are SPEC-FIRST, not implemented.** D30.2 schedules them r1; the implementation is user-gated scope at r1-entry (the +1.5-3 wk rider, stated in 00, the ruling, and the plan). The landed C7 census is untouched; every new verb row carries "NOT in the landed census."
2. **The visual grammar is REGISTERED, not implemented.** 05 §8A:632 says it outright: "No production repo implements this register today." The tokens' landing surface is OT's CSS mechanism at r1 (D33.5's sequencing law).
3. **The register's freshness is partial:** the variants' count is a source-count (not a live run) and the per-row pin counts are not yet battery-enforced (flags 3/5 above).
4. **The 8 mock-only families are registered with 4 doc-level rulings and the OPEN decisions deferred:** marker-v2 vs 09-A2, captions, the 3-vs-5 page shape, and the FX-page grammar are QUEUED-design-round rows — visible in the register, resolved by future rounds, not this one.
5. **The K2/K3 mode-matrix gates are registered, not passed.** D30.3's satisfiable form (zero orphan rows at K2; the app column matches the LANDED set at K3) is law in 00 + the plan's exit gates; K2's combined-family census registration is still PENDING (the S-spec row says so).
6. **The three cross-repo mechanisms are FILED, not executed:** the census-CI script (the app session authors it), the E1/E2/E3 fixes (the engine session, pre-r1), and the WDC waveform promotion's pin (the WDC session). The plan's enforcement-status note states the census case plainly: "ORDERED but NOT YET WRITTEN… battery-verified only."

---

## The verdict

**PASS.**

The round delivered all four asks with real gates: the battery is 123/123 under this reviewer's own run with live scrapes and mutation-sensitive classes; the adversarial trail is genuine (15 + ~50-claim re-derivations, the required folds all landed and re-verified in the corpus); the verification fleet's flags were either closed in-round or carry named owners/waves; the census is byte-stable; the filings show at their target repos at the stated SHAs; the plan resolves for a cold executor. The residues are P3-class polish and honestly-registered deferrals — nothing semantic is open, nothing blocks the wrap.

**The fold list (all OPTIONAL, none gate-blocking — the wrap may fold or carry):**
1. REFERENCE-REGISTER rows 2/3/4 flip-cells (3 cells) — fold-now recommended.
2. Plan R4's cold-executor entry-point cell (the waveform row is now present as [FILED] item 0; the pin remains open work) + a one-line R3 executed-half note — fold-now recommended.
3. (Carry, at next natural edit): 06's "displacing" → "falling through"; the D29.2-vs-D29-F8 citation unification; the "15 wire" column qualifier; 17's facet rows; the 09 B1 split-case mapping note.

**Wrap-readiness (check F): nothing is missing that MUST land before the wrap commits.** The plan already carries the R25 ledger row, the §7 wave record, the FILED stamps, and the enforcement-status honesty; the register and the ruling carry the limitations; the carried flags are recorded in the verify reports (the wrap's HANDOFF/SKILL rewrites should simply not drop them — the r1 absent-family scope gate belongs in the wrap's user-gated set, as 00 and the plan already state). `.agents/HANDOFF.md` and `SKILL.md` are at their R24 state — that is the wrap's own job, not a gap.

*Read-only respected: no repo file other than this report was created or modified; the battery was executed (read-only checks).*
