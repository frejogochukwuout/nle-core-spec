# verify-06 — the 06-nle-ops.md R25 verification (fresh context, ARCH-R25 v2)

**Agent:** 06 VERIFICATION (R25 round). **Input:** `audits/ARCH-R25-edit-mode-completeness.md` (D30/D31/31A/D32 + the P-list) → `06-nle-ops.md` @ the R25 amendment (`8eba197`, 3475→3670 lines) read in full; every code citation re-derived LIVE against the repos (engine `3989506`, OT `fdb771c`/code `c15a629`, WDC `85b81b0`, the in-repo `ui-mock/shell-variants`+`shell-mini`); the pre-R25 file (`f2b39b8`) diffed line-by-line to build the old→new line map. **Method note:** the diff hunks are `4c4, 10c10-29, 387a407-439, 475a528-529, 1433a1488-1493, 1509c1569-1679, 2408c2578-2580, 3110c3282-3286, 3120-3121c3296-3302, 3160-3163c3341-3353, 3175-3176c3365-3371` — the shift is +19 after §0, +52 after §5.0, +54 after §5.1's note, +60 after :1488, +170 after P1, +172 after P9, +176/181/190/195 down the §13 test rows — this map drove the line-citation sweep (item 5).

## The verification table (item | verdict | evidence)

| # | Item | Verdict | Evidence (quotes re-derived live) |
|---|---|---|---|
| 1a | §0 matrix vs D30's ten rows | **PASS (faithful, post-amendment state)** | All ten rows present at 06:16-27 with the ruling's columns (spec home / 15 wire / engine / OT / app / reference). Cell deltas vs the ruling's table are all post-amendment updates, not drift: Insert-edit's spec home "§5.9 (the two-semantics preamble + the splice law, D31.1/31.2 — R25)" (the ruling's "HALF — splice law in NO prose" was the pre-amendment census; the matrix header says "the spec column is post-R25-amendment"); Overwrite's OT cell "the phantom strategy rows retired R25, P2/P6"; engine cells cite live-verified timeline.ts lines (`rollingTrimItems` :2984, `rippleTrimItem` :2839, `slip` :4143, `slideItem` :4246, `performInsertEdit` :4702, `performOverwriteEdit` :4860 — every one re-read in the engine tree @ `3989506`, all byte-correct). |
| 1b | R25 pin block | **PASS (after fix)** | 06:14 now carries "engine `3989506` (ONE docs-only commit past the R24 BASE pin `5036387`…timeline.ts untouched…) / OT code pin `c15a629` (unchanged from the R24 BASE…HEAD moved docs-only to `fdb771c`) / app `64fb0ab`" — matches the ruling's wave-1 pins verbatim; verified live (engine `git log`: 3989506 → 5036387 is the only intervening commit, submodule-only; OT HEAD = fdb771c, code pin c15a629). Before the fix the engine movement was NOT noted → two different engine pins coexisted unexplained. |
| 1c | R24 BASE coexistence | **PASS** | The R24 BASE row (06:31-37, "accepted, pinned 2026-09-07") is untouched and honest — OT/WDC pins identical to R25's, and the (now-fixed) R25 block explains the one moved pin. D30.2/D30.3 note present at 06:29 ("the landed C7 census (24 routed + 6 exceptions) is NOT reopened…at K2 every matrix row is LANDED or carries a live r1-scheduled row — zero orphan rows"). |
| 2a | §5.0 fan-out table, TEN rows | **PASS** | 06:415-426: ten mode rows × the three situation columns (linked pair / transitions / sync-locked) exactly per D32.1. Spot-verifications: slip's "window-mismatched companion is EXCLUDED" ↔ engine `getSynchronizedLinkedItems` :751-767 (verified: the function filters by window); ripple-trim's "engine :2921-2950" (verified: the synced-shift + transition-neighbor deltas); replace's "`joinItems` pattern, timeline.ts:7411-7427" (verified: the `replacementByRemovedId` endpoint-remap + self-transition drop — NOT removeItems' cascade at :3775-3777, verified); fit-to-fill's "`rateStretchItem` :3202-3211" (verified: "Phase 2: stretch synced companions (same actualDuration + actualSpeed)"); ripple-overwrite's row is verbatim-faithful to D32.1's clause (cascade/move-both-tracks/incoming-unlinked). The insert row carries the ≥2-splits relink law + "the left half NEVER moves (Decision 31.2)". |
| 2b | Precedence sentence | **PASS** | 06:411: "track LOCK > sync-lock (track-level, §6 — interval/gap propagation over whole tracks) > link (clip-level, this section)" — D32.2 verbatim, stated once, with the §5.14 anti-conflation precedent (citation re-pinned, see Fixes). §5.0's body also carries the canonical `syncLinked` law (D32.3: the 15 wire name, §4.3.2/4.3.5/4.3.6/4.3.7 — verified: `syncLinked?: boolean` at 15:497/:630/:661/:698; the engine's `options.linked ?? true` on exactly 16 public methods, counted), the split-link ruling (D32.4 — verified live: FreeCut's `relinkSplitSegments` severs on a single linked split; the engine's timeline.ts:671-693 pushes NO group patch for a single linked split → both halves keep the original `linkedGroupId` — "relink-both-halves is the law" is code-true), and the model-shape reconciliation (D32.5 ↔ 09 §B1's R25 note, verified at 09:335). |
| 2c | E1 GAP row | **PASS (after fix)** | 06:438: owner **S-engine**, phase **pre-r1**, acceptance = D32.6's fix (exclude `from < insertFrame`, split straddling companions) + the 4-postcondition named vitest — complete. The mechanism description matches the live code (Phase 2 :4776 seeds `from >= insertFrame` then whole-group `findLinkedClips` pulls the left half; both halves share the original group id — code-verified). The pin-evidence parenthetical was corrected (see Fixes #6): the live vitest calls pass NO options (`tl.performInsertEdit(trackId, 'vid', 85, 0, 30)` — timeline-edit-ops.test.ts:66/:103) → default `linked:true`, and NO fixture contains a linked pair (buildEditOpsFixture constructs unlinked c1/c2) — matching the ruling's "no existing fixture contains a linked pair; the vitest calls use the default `linked:true`". |
| 3a | §5.9 preamble (D31.1/31.2/31A) | **PASS** | 06:1488 states both semantics + the boundary sentence verbatim ("the placement surface places (rejecting or displacing per strategy); the source-edit surface splices (splitting and pushing)") + the everywhere-clause ("the §11.3 ripple flag governs delete and the placement surface's displacement, never the source-edit family"); 06:1490 the mid-clip split law with "the left half NEVER moves" + live-verified engine cites (`_splitClipPure` :4742-4774, Phase 2 shift :4776-4802, `execute()` = one undo); 06:1492 the D31A dedicated-vs-composite criterion with the four family derivations. |
| 3b | §5.9B overwrite (D31.3) | **PASS** | 06:1571-1600: the covered-clip law's four cases + the multi-clip re-scan row + zero downstream movement + transitions drop + R1-B4 abort + ONE atomic undo + the 3-point shape (`max(1, …)` — code-verified in `_performInsertEditImpl`). Evidence: `_performOverwriteEditImpl` timeline.ts:4876-5051 (verified) + the R20-W2 mock `insertPlan.ts:192-255` (`planOverwriteSpans`, verified). GAP rows OW-1/OW-2(E2)/OW-3(E3) each carry owner/phase/acceptance. E2's asymmetry description matches live code (the companion handling comment at :4998-5017); E3 as the family-shared abort is live-verified (timeline-edit-ops.test.ts pins BOTH insert and overwrite empty-returns — a faithful expansion of the ruling's insert-titled E3, see Flags #4). |
| 3c | §5.9C replace (D31.4) | **PASS** | 06:1602-1620: exact-length swap postconditions, OUT auto-derived (`timelineToSourceFrames`, timeline-math.ts:151 — verified), downstream never moves, transitions REMAP (joinItems :7411-7427 — verified), companion SEVERED (`syncLinked:false`; the naive-composition trap `removeItems` :3757 — verified default-true), unfillable source REFUSES `INVALID_PARAMS` (`getAvailableSourceFrames` timeline-math.ts:190 — verified). DEDICATED op per D31A. GAP rows RE-1 (S-engine+S-ot, r1) / RE-2 (S-app, r1-adjacent) complete; the mock's wrong-law evidence verified (`insertPlan.ts:311-340` remove+overwrite; `SourceEditBar.tsx:66` "exact same length" tooltip; insert-modes.md:135 "exact-length swap"). |
| 3d | §5.9D append (D31.5) | **PASS** | 06:1622-1639: per-track last-element-end (mocks verified: `insertPlan.ts:413-414`, mini `useMini.ts:1055` + `geometry.ts:50` contentEnd), multi-append sequential accumulation, playhead IGNORED ("the mode's defining law"), never a push, the linked-append law (A/V pair at the video track's append point; audio conflict refuses atomically), composite via `insertElements`' relative-offset batch + the 6th `PlacementStrategy` + `insertBatch`. GAP rows AP-1/AP-2 complete (the OT evidence verified: `api.ts:42-50` 2-of-5 strategies; the pool batch at `view/page.tsx:515-555` — path fixed, see Fixes #8). |
| 3e | §5.9E ripple-overwrite (D31.6) | **PASS** | 06:1641-1660: the delta law (`newDur − oldDur`, push if positive, PULL if negative, pull leaves no gap, upstream untouched, ONE atomic undo), source's own duration stands (NO out-adjustment — "that is §5.9C's law"), transitions drop, zero-floor law, the composite = `delete + move + insert` (three routed verbs, one applyBatch) **NOT "overwrite + the §11.3 ripple flag"** with the §12-diff asymmetry stated exactly per the ruling ("produces the PULL exactly…cannot produce the PUSH (freed = ∅…)"). GAP rows RO-1/2/3 complete (engine evidence verified: `rippleTrimItem` :2925-2950 + the private sync-lock propagations :3475/:3534; the mock's push-only span form `insertPlan.ts:472-484` with `delta = dur − covered ≥ 0`; the stale test title at useUiStore.test.ts:2048). The r1 verb decision's law-split (06:1652) is cross-consistent with 15's own §13.15 row (15:4920: same wording, "one boolean cannot mean both"). |
| 3f | §5.9F fit-to-fill (D31.7) | **PASS** | 06:1662-1679: speed law with direction ("a LONGER marked range into a SHORTER span speeds UP"), result duration = targetDuration EXACTLY, full marked range window, overwrite-style placement, **domain [0.1, 5]** as the three-domain intersection, out-of-domain REFUSES `INVALID_PARAMS` — **never clamps**; badge one decimal; compositional first (insert + `updateElements{retime}`, r2 verb optional); WDC's W2 consumed (`varispeed.ts:40` — verified: "rate > 1 = faster/shorter, pitch preserved"). GAP rows FF-1/FF-2 complete; FF-2 registers the §11.7 cross-reference as r1 work (honest, not silent). Evidence live: engine `calculateSpeed` (timeline-math.ts:133), OT `retime.ts:29` (`sourceTime = clipTime × rate`) + `:51`. |
| 4 | The P-list fixes (P1/P2/P3/P4/P5/P6/P9) | **PASS — all phantoms GONE** | P1 (old 1509 → 06:1569): "**Ripple insert (re-keyed R25, Decision 31.6 — the P1 fix):** the diff-based claim is retired — the classic §12 diff produces the PULL exactly but NEVER the push (a pure insert vacates nothing → freed = ∅ → no adjustment fires…)" — teaches the right law. P2 (old 3110 → 06:3282-3286): the "unless overwrite placement" phantom gone, re-keyed to "§5.9B's covered-clip law…never rides the move verb's placement path". P3 (old 3120-3121 → 06:3296-3302): `ripple-insert-makes-room` replaced by `ripple-overwrite-composite-makes-room` (the composite's trajectory + delta). P4 (old 3160 → 06:3339-3345): the slide row now states the continuity shift ("`sourceStart` shifts by the continuity delta when the split chain holds (§5.7 steps 5/8…`preserveContinuity` default true, 15 §4.3.7)"). P5 (old 3161 → 06:3346-3353): renamed `slide-continuity-zero-for-non-chain` + "the chain gates only the source delta, not the slide" (+ live cite timeline.ts:4336). P6 (old 3175-3176 → 06:3365-3371): `insert-overwrite-vs-ripple` re-keyed to §5.9B vs §5.9E. P9 (old 2408 → 06:2578-2580): the single "§5.9 Insert / Overwrite | ALIGNED" row split into three honest rows (placement N/A — OT-OWNED; insert-edit PORT-SCHEDULED with the P9 rationale; overwrite PORT-SCHEDULED with E2/E3). **Orphan sweep:** grep of every old phrasing ("unless overwrite placement", "detect the new element as a joined interval", "shift everything to its right by", the old row names) — zero live occurrences; the only remaining mentions are the intentional retirement notes (06:12, :1598, :3301, :3344, :3351, :3370). |
| 5 | In-file line-citation shift sweep (+195 lines) | **PASS (after fixes — 8 citations were stale, all in the amender's NEW text)** | The pre-R25 file contained ZERO `06:NNN` self-citations; the amender's §5.0 introduced 8, written against PRE-R25 line numbers. All re-derived via the old→new map and re-pinned: `06:1271`→`06:1325` (slip's "anchor + all linked companions with the same clamped delta" — verified at new 1325); `06:1289/:1299/:1357`→`06:1343/:1353/:1411` (slide's counterpart resolution / same-neighbor-trims / parallel-slide-group sentences — all verified); `06:1291-1292`→`06:1345-1346` (the two preservation clamps); `06:1014-1022`→`06:1068-1076` (roll's `keepTightestDelta` block); `06:1145`→`06:1199` (roll's binary-search constraint line); `:1825-1837`→`:1995-2007` (§5.14's sync-lock-excluded-from-link-expansion comment block — verified). No other in-file self-citations exist (the §5.9B-F sections cite engine/OT/mock/15 targets only — every one re-verified live, see 3b-3f). |
| 6 | Cross-file anchors | **PASS** | Grepped each target: 05 §8A ("### 8A. Timeline Affordance Grammar", 05:599), 05 §14.5/§14.5A/§12.3 (05:846/:894/:753), 16 §3.4A (16:259) + §3.4B (16:287) + §3.4's `toggleAVLink` (16:251), 18 §4.3 (18:167) / §9 (18:396) / §5A (18:289), 15 §13.15's four r1 rows (15:31 + the rows at :4909/:4920) + §7/§7.1A + §4.3.9, 00's Decisions 30-36 (00:437+ v10.0), 09 §B1's R25 note (09:335, "pairwise at rest → groups at runtime…a split relinks BOTH halves…06 §5.0's split-link law — cross-cite"), OT's SD-5 (drag-drop-controller.ts:354-359 "SEAL SD-5"), 00-master:804→**fixed to :838** (see Fixes #6/#7 — the no-overlap invariant "No overlaps on any track after ripple/move/insert" lives at 00:838 post-R25 / :787 pre-R25; :804 is "All three are CI-blocking" in BOTH versions). |
| 7 | Standing laws | **PASS with flags** | §11.3 (06:2662-2669) intact — its "Any command gets ripple for free" is the OpenCut-classic REFERENCE description; the new law scopes it BY NAME at 06:1488 ("the §11.3 ripple flag governs delete and the placement surface's displacement, never the source-edit family") and 06:1651 ("NOT 'overwrite + the §11.3 ripple flag'") — reconciled by reference, not contradicted; residual un-narrowed sites flagged (Flags #1/#2). §12's classic quote is intact and untouched (no diff hunks in §12's range) and the asymmetry note is present where the ruling put it (§5.9E's composite bullet + P1's re-key at :1569). §6 is referenced correctly by §5.0's precedence and the fan-out's sync-lock column ("per §6", "inserted-gap propagation (§6)", "removed-interval propagation (§6)"). §11.7 referenced by §5.9F:1668 (the [0.01,5] vs [0.1,5] split — registered as FF-2, not silently contradicted). |
| 8 | House hygiene | **PASS** | Zero WIP/TBD/TODO/FIXME markers (grep-clean). No renumbering: §5.0 inserted before §5.1 (06:407/440); §5.9B-F lettered after §5.9 (06:1571/1602/1622/1641/1662); §5.10 Duplicate unchanged at 06:1681; §0's additions sit inside the existing FORWARD INVENTORY block. Table formatting valid: the matrix 7 columns/row, the fan-out table 4 columns (5 pipes × 13 rows), every GAP table 5 columns (6 pipes — E1/OW/RE/AP/RO/FF rows all consistent), the covered-clip table 2 columns — awk-verified uniform. |

## Fixed directly (each: line + what)

1. **06:14 (§0 pin block):** the R25 pin row now notes the movement — "engine `3989506` (ONE docs-only commit past the R24 BASE pin `5036387` below — timeline.ts untouched, the §10.4 line numbers stand) / OT code pin `c15a629` (unchanged from the R24 BASE — the code pin is the census's contract; HEAD moved docs-only to `fdb771c`)". Before: two different engine pins (3989506 in the matrix header, 5036387 in the BASE row) coexisted with no reconciliation — the "must note the movement" requirement of item 1 was unmet.
2. **06:411 (§5.0 precedence):** `:1825-1837` → `:1995-2007` — the §5.14 sync-lock-exclusion precedent (the "Expand shift to linked companions (unless they're on sync-locked tracks…)" block), shifted +170 by the §0/§5.0/P1 insertions above it.
3. **06:417 (fan-out, slip):** `06:1271` → `06:1325` — "Multi-select: Slip applies to the anchor + all linked companions with the same clamped delta."
4. **06:418 (fan-out, slide):** `06:1289/:1299/:1357` → `06:1343/:1353/:1411` and `06:1291-1292` → `06:1345-1346` (all four targets re-derived to the current line content).
5. **06:419 (fan-out, roll):** `06:1014-1022` → `06:1068-1076` (the `keepTightestDelta` counterpart block) and `06:1145` → `06:1199` (the binary-search constraint).
6. **06:438 (E1 row):** (a) `00-master:804` → `00-master:838` — 00's no-overlap invariant ("No overlaps on any track after ripple/move/insert", §13.5) is at :838 in the current 00 (it was at :787 pre-R25; :804 is "All three are CI-blocking…" in both — the fleet's "00:804" was a stale line carried through the xcut report into the ruling); (b) the pin-evidence parenthetical corrected from "all insert fixtures use `linked:false` or unlinked pairs — the default path is the untested path" to "no existing fixture contains a linked pair, and the vitest calls pass no options — the default `linked:true` runs with zero companions; the linked path is the untested path" — live-verified (timeline-edit-ops.test.ts:66/:103 pass 5 positional args, no options → `options.linked ?? true` default; the fixtures build unlinked clips) and matching the ruling's own evidence wording.
7. **06:1631 (§5.9D linked-append):** `00-master:804` → `00-master:838` (same invariant, same stale source).
8. **06:1638 (AP-1 row):** `page.tsx:515-555` → `view/page.tsx:515-555` (OT has two page.tsx files; the pool-batch `handleInsertSelected` lives at `src/app/view/page.tsx:518-554` — the root `src/app/page.tsx` is 306 lines and contains nothing cited) and `15 §13.15's :4901 row` → `15 §13.15's InsertCommand row` (15:4901 is §13.15's intro prose in both pre- and post-R25 15; the param-alignment row is the §4.3.9 InsertCommand row at 15:4909 — section anchor chosen over a line number that has already drifted twice).
9. **06:1652 (§5.9E r1 verb decision):** `15 :836/:865` → `15 §4.3.11/§4.3.12` — the rateStretch/retime `ripple: boolean` params live in 15's §4.3.11/§4.3.12 (at :845/:874 currently, :833/:862 pre-R25 — the cited :836/:865 are adjacent param-doc lines in neither version; the section anchor matches 15's own §13.15 row wording "the §4.3.11/§4.3.12 rateStretch/retime wording is the documentation template").

*(One suspected typo — "`iddle.startTime`" at 06:3306 — proved to be a display artifact of the review tooling, not a file defect: byte-level inspection shows the line has always read "`[middle.startTime, middle.endTime]`" in the pre-R25, R25, and working-tree versions. No edit was needed.)*

## Flagged (each: the issue + why not fixed)

1. **§5.4:988 + §12:3135 + §13:3155 — the residual "flag-routed insert ripple" teaching (semantic, pre-existing, not in the P-list).** §5.4's rationale ("Any command (split, trim, move, delete, **insert**, rate-stretch) automatically gets ripple behavior when `CommandManager.isRippleEnabled === true`"), §12's tail ("**Universal**: any command (… insert …) automatically gets ripple behavior…"), and §13's summary row ("| **Insert** | … | Yes (flag) |") all still teach that the flag gives insert a push — exactly the phantom P1 retired at 06:1569 ("a pure insert vacates nothing → freed = ∅ → no adjustment fires"). These are descriptions of the OpenCut-classic reference mechanism, and the ruling's P-list named only 06:1509/:3110/:3120/:3175/:2408 — but D31.1's everywhere-clause ("the same teaching lands at every ripple-flag site") reads these three sites as un-narrowed. NOT FIXED because re-wording pre-existing reference-description prose beyond the ruling's enumerated sites is a semantic edit (the classic CommandManager hook genuinely applies the diff to any command; what it cannot do is produce a push) — the design owner should decide whether these three get the one-line precision note (as 16:192 and 15:4909 did) or an era/reference qualifier.
2. **§11.3:2669 — same family, lower stakes.** "No separate `RippleCommand`. Any command gets ripple for free." — a seed-correction section describing the classic repo. Reconciled BY REFERENCE from §5.9:1488 (which names §11.3 and scopes the flag), so not a live contradiction; flagged for the same everywhere-clause reason as #1.
3. **00-master's own D32.6 self-citation "00:804" (00:451) — cross-file, not mine to fix.** The ruling (§2) and 00's landed D32.6 both say "an exact-overlap violation of 00:804's no-overlap invariant"; the invariant is at 00:838 (post-R25) / :787 (pre-R25). I fixed 06's two occurrences; 00's occurrence belongs to the 00 verifier (and the ruling/xcut occurrences are historical audit records — leave them). Root cause: xcut-linked-companions.md:108 cited "00-master-spec.md:804 (§13.5 …)" — §13.5 right, line wrong at its own reading.
4. **The §0 matrix's Overwrite row carries "E2/E3 open" while the ruling's E3 is titled "Insert's silent transition-blocked abort" (observation, live-verified — no action needed).** 06's §5.9B (06:1592) legitimately broadens E3 to "the 3-point family's shared R1-B4 abort law", and the engine's own test file pins BOTH the insert and the overwrite silent aborts (timeline-edit-ops.test.ts: "R1-B4: insert/overwrite edits abort when the split is transition-blocked", two `returns EMPTY` tests) — so the matrix cell is a faithful expansion of the ruling's narrower title, not a mis-attribution. The Insert-edit matrix row (06:22) mentions only E1; its E3 ownership is taught at §5.9's preamble (:1490 → "§5.9B's shared 3-point family laws"). Noted so the integration reviewer sees the E-numbering was checked, not missed.

## Verdict

**06-nle-ops.md is a FAITHFUL, WELL-LANDED R25 amendment — GO, with the 9 mechanical fixes applied and 3 semantic residuals flagged for the wrap.** The D30 matrix, the D31/31A §5.9 restructure, the D32 §5.0 law, and all seven P-list fixes are present, internally consistent, and evidence-correct at the byte level (every engine/OT/mock/WDC citation I re-derived resolves to the named content at the named line; the ruling's contracts — covered-clip law, exact-length swap, per-track append, delta law, [0.1,5] domain with refuse-don't-clamp — are all quoted faithfully). The amender's one systematic defect was the line-shift hazard: all 8 in-file self-citations in the new §5.0 were written against pre-R25 line numbers (the +195-line growth) — all re-pinned with re-derived targets; plus two stale `00-master:804` citations (the invariant is at :838), one ambiguous OT path (`view/page.tsx`), two drifted 15 line-cites (converted to stable section anchors), and the un-noted engine pin movement in the §0 pin block. The flagged residuals (the three un-narrowed "any command … insert" ripple-flag teachings, §11.3, and 00's own :804 self-cite) are pre-existing reference prose outside the P-list — corpus-wide decisions for the integration review, not 06-local defects. No commit made (per instructions); the working tree carries only the 9 one-line fixes above (diff vs `8eba197`: 9 insertions, 9 deletions, zero line-count change).
