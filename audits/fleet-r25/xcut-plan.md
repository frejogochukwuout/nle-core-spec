# X-CUT — the IMPLEMENTATION-PLAN execution-gate audit (Fleet R25)

**Auditor:** the SEAM/EXECUTION-GATE cross-cutting agent (read-only; sole artifact = this file; no repo modified, no commits).
**Read:** `IMPLEMENTATION-PLAN.md` (141 lines, the R24-4 state) in full; the five scout cards' verdict/queue sections; the ten mode reports' gap-row tables (shape + spot-verified row IDs: insert-G2, ROLL-2/ROLL-3, slip-G-SLIP-6/7, ripple-trim-G3/G4, append-G5, ripple-overwrite G-RO set).
**Live verification performed by THIS agent (not inherited):** app `git log c885ece..64fb0ab` (W-C `f681d34` / W-D `78e7283` / W-E `b40216e` / W-F `8f22236`+`91f6c45` / 9-G1 REV `0328bc0` / close `c7cc1ae`·`82e38d8`·`64fb0ab`); app `.agents/` absence (`ls` → exit 2) + `scripts/` = bootstrap/check-boundary/dev3000 only; app `ci.yml` read in full (NO census step — mismatch 3 self-verified); WDC root `HANDOFF.md` read in full (next-session scope :32–58 = mixer / "4" ports / async pre-retime / P3 — NO waveform row — mismatch 4 self-verified) + `.agents/` = SKILL.md only + `PLAN.md:150-152` parking lot + `audio-registry.ts:24/:29/:48/:58/:74` de-facto contract; engine `.agents/PLAN.md:135-163` R9 queue read (mismatch 5 self-verified) + `scene-to-segments.ts:239/:246/:305/:353/:518/:611` maintainPitch threading landed; app `engineService.ts:477/:486` `maintainPitch: true` hardcode; OT `.agents/PLAN.md:432-466` R9-a..d read + OT HEAD `fdb771c`/`c37844f`/`ded43c4` log; nle-ui HEAD `3026099`/`2646d80`/`d22e4e9` log + `PLAN.md:210/:219` P-widen/P-lock-route unchecked + DECISIONS tail (D28 `:1062` → `#30` `:1176`, no D29) + no C0/MiniShell row; spec `20-audio-core.md` §0:25/§0:151 (promotion registered spec-side only); spec `15-wire-protocol.md` §13.15 (the D29-F8 insertBatch/batch-keyframe endorsement registered spec-side).

---

## 1. The verified mismatch table (9 rows: plan claim | live truth | evidence)

| # | The plan's claim (R24-4) | The live truth (R25) | Evidence |
|---|---|---|---|
| **1** | S-app row: "**complete the app's OWN D30 waves W-C..W-G** (… in flight — W-A/W-B landed)"; §4 critical-path head; §8: "the app's D30 waves 1-2 (in flight …; W-C..W-G is the item)" | **ALL LANDED.** W-C `f681d34` (use-wire-dispatch NEW + the interlocked five at exact `c15a629` + EngineMount page-owned wire + attachWire registry), W-D `78e7283` (keymap lockstep, nle-ui re-pin `2646d80`), W-E `b40216e` (loop two-way bridge + REAL Save/Load round-trip + Z2 grade→export), W-F `8f22236` (THE COVERAGE GATE: 24 routed verbs through the real mounted UI, ACCUMULATED ⊇ WIRE_COMMAND_TYPES − 6 exceptions, the jsdom DragEvent trap solved) + `91f6c45` (Z6 dead-zone sweep), W-G CRs fired (OT `c37844f` + engine `3989506`), 9-G1 REV `0328bc0` (PROCEED-WITH-FIXES, both P1s fixed), close `c7cc1ae`/`82e38d8`/`64fb0ab`. **206/206 live-run** (scout-app), tsc + boundary green, CI recovered. Port census at the **declared irreducible set: 35 zero-action + 5 carriers + host** (was 32+7) — the fork-retirement gate's carrier-count condition is MET. | app `git log c885ece..HEAD` (read this agent); scout-app's live vitest table; census re-verification (lock-copy byte-exact `c15a629`) |
| **2** | S-app step (0): "create the app's `.agents/` … **OVERDUE per D27.2, BEFORE W-C**" | **STILL ABSENT — overdue ×2 consecutive rounds.** W-C..W-G all ran without it. The content exists in the wrong home: app `worklog.md` (current) + the **nle-ui repo's** `.agents/` R9-close artifacts (DECISIONS #30, SKILL R9 laws, the DragEvent trap). The app's own `README.md` is stale front-door ("30 glue-law tests" :46, engine/WDC "not imported yet" :34, "596 mock-law tests" :7 — all false at 206). | `ls /home/z/my-project/nle-test-app/.agents` → exit 2 (this agent); scout-app README greps |
| **3** | D26.2/protocol §6 item 6: the census is "recomputed app-side by a **CI-gated script … register-inequality fails the build**" | **NOT IMPLEMENTED.** `ci.yml` runs exactly: checkout(submodules) → `npm ci` → `typecheck` → `test` → `build` → `boundary` (test job) + the storybook CSS gate (storybook job). `scripts/` = `bootstrap.sh`, `check-boundary.mjs`, `dev3000.py`. **No census script, no census CI step, no register doc.** The census is battery/scout-verified at round cadence only — that does not fail the app build. The law cites an enforcement mechanism that does not exist. | `.github/workflows/ci.yml` read in full, 2 jobs (this agent); `scripts/` listing (this agent) |
| **4** | S-wdc step (1) "FIRST (D29.4, REQUIRED — K3's audio half HARD prerequisite): **promote the waveform-data seam contract … to the HANDOFF next-session scope** + STATE the contract … + pin it" | **NOT LANDED (2nd round). The two scouts were each half-right; the venue truth:** WDC's queue venue is the **repo-ROOT `HANDOFF.md`** (WDC's own convention — the plan's own S-wdc ORIENTATION cell says "the repo-ROOT `HANDOFF.md` + `PLAN.md` — NOT `.agents/`"; `.agents/` holds SKILL.md only, so "no HANDOFF to promote into" is wrong about the venue but right about the outcome). The root HANDOFF's "Next-session scope (priority order)" reads exactly the four pre-promotion items — 1. "The mixer surface", 2. "The 4 deferred upstream tests", 3. "The async pre-retime queue item", 4. "P3 ledger" — **no waveform row exists anywhere in WDC's queue**. The parking lot (`PLAN.md:150-152`: "Waveform peaks from `audio-registry` feed opencut-timeline's `AudioElement` view … the S-layer data contract") remains unchecked. The de-facto contract IS landed and stable in code (`audio-registry.ts`: `getAudioBuffer` :24, `setAudioBuffer` :29 — recomputes peaks, `getPeaks` :48, `setAudioPeaks` :58, `computePeaks(buffer, buckets)` :74 → `{min, max: Float32Array, buckets}`, `PEAK_BUCKETS = 2000`), but the contract STATEMENT + pin + queue presence never materialized WDC-side. Spec-side the promotion IS registered (`20-audio-core.md` §0:25 + §0:151) — the row remains **spec-carried only**; K3's audio-half hard prerequisite still has no WDC-side queue presence, exactly as R24 left it. | WDC root `HANDOFF.md` read in full (this agent); `web-daw-core/.agents/` = SKILL.md only (this agent); `PLAN.md:150-152` (this agent); `audio-registry.ts` grep (this agent); spec-20 §0:25 |
| **5** | S-engine step (1): "the D29.5 dispositions (the app's W-G CR list, **amended BEFORE it files**)" — ARCH-R24 F1: (a) transform sidecars → **future-extension**; (b) maintainPitch **closed-LANDED**; (c) scene-grade **declined, not filed**; (d) CSS-filter **re-scoped to the open widening** | **The filings predate the amendment — 3 of 4 engine R9 entries carry STALE dispositions.** Per-entry: **R9-a (transform sidecars) STALE** — filed as an active proposal ("Proposal: `CompositionElementParams` gains `{x, y, scale, rotation, flipH, flipV}` …"); the amendment re-files it as future-extension (the slots stand at composition-frame.ts:70-94; not a near CR). **R9-b (maintainPitch) STALE** — filed as engine work ("the engine hardcodes `maintainPitch: true`"); the engine-side threading IS landed (scene-to-segments.ts:246 `maintainPitch?: boolean`, threaded :518/:611/:638 into `SegmentPlayOptions` → the adapter's eligibility branch) — the residual is **app-owned** (the app's engineService.ts:477/:486 hardcodes `maintainPitch: true`; the Inspector toggle is dead because the APP never passes it). **R9-c (scene-grade promotion) STALE/CONTRADICTED** — filed as a proposal; the amendment records the DECLINE as the law (consumer-side final pass; Z2's app fix stands; recorded spec-side, not an engine row). **R9-d (honest-preview widening: Vignette/Glow/LUT-preview v1) ALIGNED** — the one as-filed entry matching the amendment (disposition OPEN; only the Gaussian-Blur subset landed @ `8a0b7fe` + the `4b2dfd1` radius clamp). | engine `.agents/PLAN.md:135-163` read (this agent); engine `scene-to-segments.ts` grep (this agent); app `engineService.ts:477/:486` (this agent); engine log `4b2dfd1`/`8a0b7fe` context |
| **6** | K3 row: deliverable = the coverage-gate port (future tense) + the compensating pin family + the corpus + DOM-structural + audio pins + VLM net; store/policy halves "∥ NOW" | **SPLIT reality.** LANDED: the routed-verb completeness machine-check (`src/wire-coverage.test.tsx`, W-F @ `8f22236` — 24/24 routed − 6 exceptions through the real mounted UI, the LIFO law, the jsdom DragEvent solution) + the Z6 dead-zone sweep pins. **NOT STARTED: K3's store/policy halves** (zero `useMini`/`law-net` re-expression in app src — unstarted, unblocked, ∥-valid as written), K4 (no e2e), the DOM-structural mapping row (now gated on the S-spec mapping row ALONE — W-C landed), the audio pins (still hard-gated on mismatch 4), the VLM net. **PARTIAL: the compensating shell-path family** — ⌥[/⌥] ripple-trim pinned (GluedShell :361/:368), ⇧M pinned (Z6 P12); slip `,`/`.` UNPINNED (mode-slip G-SLIP-7). **NEW INPUT:** the ten mode reports file ~70–76 gap rows proposing the 10-mode matrix as a K2/K3 completeness gate (registration drafted in §3 below). | app `src/wire-coverage.test.tsx` + worklog; scout-app; GluedShell :361/:368; mode-slip G-SLIP-7 (verified this agent) |
| **7** | Estimates: crawl ≈ 5-11 wk solo / 3-6 two-dev (itemized: D30 1-2, K2 1-2, K3 2-4, K4 1-2, structural 1-2) | **Crawl HOLDS (5-11/3-6).** Reasoning: the D30 item (1-2 wk) is now DONE (−), offset by small open items (+): census-CI script + register (≈0.5), `.agents/` + README (≤0.5), engine-R9 reconciliation + filing residuals (≤0.5), mode-matrix registration + wired-subset K3 pins (+0.5-1) — the mode gaps are mostly **law-authoring (S-spec, cheap doc rows) + r1-port additions**, and the engine already holds 6/10 modes algorithmically (class math, engine-pinned browser-side), so no new engine research enters the crawl. **Run r1 does NOT currently contain the mode family the fleet just mapped:** r1's wave list = slip/slide/roll/rateStretch + retime/freezeFrame/rangeRemoval; the 4 absent modes (replace, append-at-end, ripple-overwrite, fit-to-fill) + the insert-edit/overwrite 3-point ports appear NOWHERE in the plan. If ruled in-scope at a r1-entry scope gate: r1 grows ≈ **+1.5-3 wk solo / +1-2 two-dev** (engine ops + OT verbs/UI + carried tests × families). Walk unchanged (10-15/7-10). | plan §8 itemization; the mode fleet's gap rows; scout-engine's 6/10 census |
| **8** | The five cross-repo filings ordered R24: (i) S-ot carrier-reduction work order + data-test contract; (ii) S-wdc waveform promotion; (iii) S-package filing-gap actions; (iv) S-app `.agents/`; (v) the W-G CR list reaching OT/engine in amended form | **2 of 5 landed, refined.** (i) **OT + engine received the app's W-G CRs** ✓ (`c37844f` 4 entries + `3989506` 4 entries) — but the engine set is pre-amendment (mismatch 5); **the ordered S-ot carrier-reduction work order + data-test stability contract were NEVER FILED** (OT's PLAN carries only the consumer R9 block — no carrier-reduction/data-test titled block). (ii) **nle-ui: DECISIONS #30 FILED** ✓ (`d22e4e9`) + the D30 W-D package edits **landed as code** ✓ (`2646d80`) — remaining: the **C0 MiniShell row in nle-ui's PLAN is NOT filed** (grep = 0 rows), **D29 was never filed package-side** (ledger jumps D28 → #30), P-widen/P-lock-route still **unchecked** at `.agents/PLAN.md:210/:219` (ordered ticked). (iii) WDC promotion NOT landed (mismatch 4). (iv) app `.agents/` NOT landed (mismatch 2). | OT PLAN grep (this agent: zero carrier/data-test rows); nle-ui PLAN :210/:219 + DECISIONS tail + no C0 row (this agent); nle-ui log `d22e4e9`/`2646d80` (this agent) |
| **9** | S-ot row: the queue = the carrier-reduction work order + data-test contract + view-config + K2 registration + structural half + token half (written BEFORE OT's R9 seam queue existed) | **Adjudication: YES, absorb as the live queue — with a mapping.** OT's R9 queue (`c37844f` + the `fdb771c` code fix): **R9-a** (endorse `timeline.insertBatch` + batch keyframe wire verbs to nle-core-spec) = **already registered spec-side** (spec 15 §13.15, the D29-F8 endorsement) — no S-ot action until r1. **R9-b** (keyframe authoring has no UI surface at any layer — the volume lanes only EDIT existing keyframes) = needs an **S-spec territory ruling** (upstream lane affordance vs consumer-territory), not an S-ot code item yet. **R9-c** upstream-back hunks **fold INTO the carrier-reduction program**: `EMPTY_CONTEXT_ITEMS` is already D26.3 item 1; functional `setPos(null)` **flips the app's TimelineContextMenu carrier**; the W8 `!shiftKey` playhead-guard hole = **landed upstream** `fdb771c` (tick it); the jsdom no-DragEvent guard/docs = a small upstream note. **R9-d** (`WIRE_UI_EXCEPTIONS` as a first-class shared export — the registry is test-local today, zero src matches) pairs with D29's coverage-gate instrument; a small upstream export that keeps both gates' registries from drifting (tsc-lockstep shape). | OT `.agents/PLAN.md:432-466` read (this agent); OT log `fdb771c`; scout-ot ("not exported as a constant"); spec 15 §13.15 |

---

## 2. The amendment list (per plan section — exact rows to re-write, drafted in the plan's own format)

### §0 preamble + the track table

**S-app row — FULL REWRITE** (the most-stale row in the plan; the critical-path head is done):

> | **S-app** | `/home/z/my-project/nle-test-app` (orient: `README.md` + `worklog.md` tail — the README's gate-count/import rows are stale (30/596/"not imported yet", false at 206); refresh it; the `.agents/` below becomes the orientation home) | (0) **create the app's `.agents/`** (HANDOFF + PLAN minimum, the fleet convention — **OVERDUE ×2 (D27.2): ordered before W-C; W-C..W-G all ran without it**; promote the existing content, don't rewrite it: the worklog's R9 entries + the nle-ui repo's `.agents/` R9-close: the DragEvent trap, the LIFO law, the per-core wire mint, the r-key lockstep) + **the README refresh**; (1) **the D30 waves W-C..W-G: DONE @ `64fb0ab`** (206/206 live; the coverage gate green 24/24 routed − 6 exceptions; the census at its declared irreducible set 35 zero-action + 5 carriers + host; 9-G1 PROCEED-WITH-FIXES, both P1s fixed; the W-G CRs fired) — the fork-retirement gate's carrier-count condition is MET; what remains of the D26 census law is **(1b) the CI enforcement: author the census script + register doc** (the vendored pin vs the port tree, import-specifier-normalized, register-inequality fails the build — `ci.yml` gains the step; per-pin declared values 40 files = 35 zero-action + 5 carriers + the host, never hardcoded) + **(1c) the engine-R9 queue reconciliation filing** (the D29.5-amended dispositions at `nle-engine/.agents/PLAN.md`: R9-a → future-extension; R9-b → closed-LANDED engine-side with the residual re-filed HERE; R9-c → withdrawn, the decline is the law; R9-d unchanged-open) + **the maintainPitch toggle threading as an app row** (`engineService.ts:477/:486` hardcodes `maintainPitch: true` — the Inspector Preserve-pitch toggle is dead app-side); (2) **K3's store/policy halves ∥ NOW** (unstarted, unblocked — unchanged: the useMini-family re-expression over the real surfaces, CORE-SEAMS §2 the field map); (3) **K3's DOM-structural halves** (now gated ONLY on the data-test mapping row — W-C landed; the three-way vocabulary per D26.5); (4) **K4** (the e2e — same routed verbs, the shell-path exception applies); (5) **K3's audio pins + the VLM net port** (the audio pins STILL hard-gated on S-wdc's unlanded waveform promotion — the only K3 sub-half with an external prerequisite); (6) **the mode-matrix K3 rows** (§3: the slip pin, the composed ripple family, the selectTool controller) | this doc + ARCH-R24 (D26/D27/D29) + `docs/design-r9-d30.md` + `docs/audit-port-convergence-r9.md` + `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` + `CORE-SEAMS.md` + `NLE_GH_PAT` + `npm run bootstrap` | the app's vitest (**206→**), `tsc`, the boundary script, `vite build`, **the census script (CI-gated once written — the D26.2 law's missing half)**, the wire-coverage gate | `.agents/` exists + README truthful; the census register CI-enforced; the engine R9 queue reconciled + the maintainPitch residual landed; the K3 corpus row-by-row green (322 authored + 33 projection pins **+ the mode-matrix wired-subset rows**); the audio pins + the VLM net live; **K4: the automated e2e green, zero mock paths** |

**S-ot row — AMEND the First-action cell** (keep the six-item structure; merge the live R9 queue):

> (1) the carrier-reduction work order — **STILL UNFILED (ordered R24; file it THIS window** as the titled block in `opencut-timeline/.agents/PLAN.md` per the cross-project queueing protocol — the R9 block at :422-466 is the format demo) — **now MERGED with OT's own R9-c upstream-back hunks** (the consumer-proven set): the 6 optional injectable props (selection-bridge pair, zoom bridge, `confirmDelete`, the `timeline-scroll` anchor, `cancelRegistry` injection) **+ `EMPTY_CONTEXT_ITEMS`** (already D26.3 item 1) **+ the functional `setPos(null)`** (flips the app's TimelineContextMenu carrier) **+ the W8 `!shiftKey` playhead-guard fix — LANDED upstream `fdb771c`, tick it** **+ the jsdom no-DragEvent guard/docs note**; each landing flips an app carrier toward exact (census-tracked); (1b) **`WIRE_UI_EXCEPTIONS` as a first-class shared export** (OT R9-d — the 6-verb exception registry is test-local; the export keeps the consumer's ported gate from registry drift; pairs with D29's instrument set); (1c) **R9-b's keyframe-authoring ruling ASK** (file with the S-spec mode-matrix registration: upstream lane affordance vs consumer-territory — the ruling gates any R9-b work); (2) the data-test stability contract (unchanged, unfiled); (3) the view-config surface (unchanged); (4) K2's in-page runner registration (S-spec's row, unchanged); (5) the structural grammar half (crawl-tail, unchanged); (6) the token half at w1-entry (unchanged). **R9-a (insertBatch/batch-keyframe endorsement) needs NO action — already registered spec-side (15 §13.15, D29-F8).**

**S-engine row — AMEND the First-action cell:**

> (0) the consumer re-pin wave: DONE at `5036387` — engine HEAD `3989506` is docs-only over it (the R9 queue filing); the app's re-pin is optional freshness, not required; (1) **the R9-queue reconciliation (REWRITTEN — the W-G filings landed PRE-amendment):** execute the D29.5 dispositions at the engine's own R9 queue — R9-a re-labeled **FUTURE-EXTENSION** (the composition-frame.ts:70-94 slots stand; not a near CR); R9-b **CLOSED as LANDED engine-side** (scene-to-segments.ts:246 threads `el.retime.maintainPitch`) with the residual **re-filed app-side** (the app's engineService hardcode); R9-c **WITHDRAWN** (the scene-grade DECLINE is the law — consumer-side final pass per `8a0b7fe`; Z2 stands; spec 04/08 carry the decision); R9-d **unchanged-open** (the honest-preview widening: Vignette/Glow/LUT-preview v1); (1b) **the mode-matrix defect rows (pre-r1 — must land clean before the op ports):** the **linked mid-clip insert corruption** (mode-insert G2 — live-probed, unqueued anywhere: left half overlaps the inserted clip, the audio companion shifts unsplit); the **roll counterpart clamp** (ROLL-2 — no central `keepTightestDelta`, A/V junction desync); the roll/slide/ripple-trim **transition+keyframe preservation clamps** (ROLL-3, slide-G3, ripple-trim-G3 — the binary-search clamp + `applyTransitionRepairs` are spec'd (06:1145/15:642) but unported); **rippleTrimItem exposure + first tests** (ripple-trim-G3); (2) the projector parity corpus; (3) the color instruments; (4) K2's engine-side pairs — **grown by the mode-matrix wired-subset pairs** (the 6/10 class methods ↔ the K2 mode rows).

**S-wdc row — RE-FLAG step (1) (content unchanged; the venue made explicit; the counter made live):**

> (1) **FIRST (D29.4, REQUIRED — K3's audio half HARD prerequisite — NOW 2 ROUNDS UNEXECUTED): promote the waveform-data seam contract into the ROOT `HANDOFF.md`'s next-session scope** (WDC's convention — the repo-root HANDOFF, NOT `.agents/`, which holds SKILL.md only) **+ STATE the contract** (the de-facto `getAudioBuffer`/`setAudioBuffer`/`getPeaks`/`setAudioPeaks`/`computePeaks(buffer,buckets) → {min,max,buckets}` shape at `audio-registry.ts:24-74` + the offline-render/call-spy pin shapes the app authors against) **+ pin it** (a vitest contract test in WDC's own suite — 759→). Spec-20 §0:25/§0:151 already carries the spec-side half; the WDC-side landing closes the loop and unblocks K3's audio half. [(2)-(5) unchanged: the mixer surface; the FIVE deferred test ports (WDC's own docs still say "4" — the off-by-one stands); the async pre-retime; the JKL R4 residue.]

**S-package row — AMEND step (2) (the filing-gap actions — partially closed):**

> (2) the filing-gap actions (D27.1) — **state: DECISIONS #30 FILED ✓ (`d22e4e9`); the D30 W-D package edits LANDED AS CODE ✓ (`2646d80` — the package's own W-D wave; the filing is moot, mark done-by-landing); REMAINING: file the C0 MiniShell row in nle-ui's OWN PLAN** (grep-verified: no row exists — the spec's "filed in nle-ui's PLAN queue" expectation STILL unmet 2 rounds later); **file D29 package-side** (the DECISIONS ledger jumps D28 → #30 — D29's instrument adoptions were never recorded); **tick P-widen + P-lock-route** (both verified still unchecked at `.agents/PLAN.md:210/:219`). [(1) C0 MiniShell and (3) unchanged.]

**S-spec row — AMEND (the R25 round's work order supersedes §6's R24 record):**

> (1) **the battery re-baseline to the R25 pin world** (engine `3989506`/458 · OT HEAD `fdb771c` code-pin `c15a629`/536 · WDC `85b81b0`/759 · nle-ui `3026099`/674 · app `64fb0ab`/206 — battery_r24's checks re-pointed: the census-coherence register 40 = 35 + 5 + host, the `.agents`-presence check, the filings-state check); (2) **the K2 combined-family census + spec 12/17 registration at K2 entry — STILL PENDING, now grown by the mode-matrix registration** (§3); (3) the C-ledger rolling disposition + **the R25 fold: the 5 scout cards + the 10 mode reports + this xcut audit**; (4) **the mode-matrix S-spec rows THIS ROUND** (the 06/15/16/17/18 authoring rows — §3's table); (5) the mode-family scope-gate preparation (the r1-entry decision material).

### §1 (the fleet)

> Re-base the reference set (2026-09-09, the R25 re-baseline; the battery re-baselines centrally): engine `3989506` (458 — docs-only over `5036387`) / OT HEAD `fdb771c` (536; **code pin `c15a629` UNCHANGED — src-diff empty; consumers current without action**) / WDC `85b81b0` (759) / nle-ui `3026099` (674 — shell/keymap/docs only, zero `src/components/timeline/` commits) / **app `64fb0ab` (206, LIVE-run)** / mini 355 / variants 1521+. **Consumer pins (live):** engine→OT `c15a629` + WDC `494f6ff`; app→engine `5036387` + OT lock-copy `c15a629` (byte-exact minus `testing/`) + **nle-ui `3026099`** (bumped by the W-D lockstep) + WDC `85b81b0`.

### §2 (the ladder)

> **K1 row:** re-base the counts — engine 458/458, OT 536/536, WDC 759/759, nle-ui 674/674, **app 206/206 (live-run this round; the R24 static census 174 superseded)**.
> **K2 row:** the deliverable GAINS: "**+ the 10-mode edit-matrix census registered in spec 06/17** (mode | engine math | OT wire verb | UI affordance | pins | verdict — at the R25 pins: 6/10 engine-class-only, 0/10 wired, 4/10 absent; the wired-subset pairs are machine-checkable: the engine class methods ↔ the OT runner families)". The exit gate GAINS: "the mode matrix registered + the wired-subset pairs green (the OT-ops→engine-decode round-trips extended by the 6 existing modes' math)".
> **K3 row:** SPLIT the deliverable into LANDED and OPEN: "**LANDED @ `64fb0ab`: the routed-verb completeness machine-check** (the W-F coverage-gate port — 24/24 routed − 6 exceptions through the real mounted UI, the LIFO accumulator law, the jsdom DragEvent solution) **+ the Z6 dead-zone sweep pins**; **OPEN: the compensating shell-path pin family (PARTIAL — ⌥[/⌥] + ⇧M pinned; slip `,`/`.` + the ⌘B/⌘M/⌘⇧M family unpinned — the mode-matrix K3 rows enter)**; the LAW-NET corpus re-expression (322 + 33, unstarted — incl. the composed ripple family); the DOM-structural gate (now gated ONLY on the mapping row — W-C landed); the offline audio pins (**still hard-gated on S-wdc's waveform contract**); the VLM visual net". The exit gate GAINS: "the app's mode-coverage pin family green for the end-to-end wired subset (split-insert, DnD-insert, slip, ripple-trim-keyboard) + the honest-absent verdicts registered (no silent mode absences)".
> **K4 row:** unchanged (unstarted; the same-verbs law + the shell-path exception stand).
> **Ordering law:** re-state — "**K3's DOM-structural half gates on the data-test mapping row ALONE (D30's W-C LANDED)**; K3's store/policy halves run ∥ (unstarted, unblocked)".

### §3 (the workstreams)

> **S-app:** `.agents/` (overdue ×2) + the README → **the census-CI enforcement + the engine-R9 reconciliation + the maintainPitch residual** → K3 store/policy halves ∥ → K3 DOM-structural (mapping row only) + K4 + the mode-matrix K3 rows → w1 → w2 → w3 → r5. *(the D30 line drops to DONE.)*
> **S-ot:** the carrier-reduction program (filed THIS window, merged with the R9-c hunks) + WIRE_UI_EXCEPTIONS + the data-test contract + the view-config surface → K2's runner registration → the structural half → **r1's op families + C7 (grown by the mode-matrix r1 rows)** → the insertBatch/batch-keyframe landings (already endorsed, 15 §13.15).
> **S-engine:** the R9-queue reconciliation + the mode-matrix defect rows (pre-r1) + the widening + the parity corpus + the instruments + K2's pairs → r2/r3/r6 consumption.
> **S-wdc:** the waveform-contract promotion (STILL first — 2 rounds unexecuted) + the M2 mixer + the FIVE ports + the async pre-retime → r2.
> **S-package:** C0 + the filing-gap residuals (the C0 row, D29, the P-ticks) ∥ crawl → w1 consumption → r1 keymap surfaces → r5 polish.
> **S-spec:** the R25 fold + the battery re-baseline + the mode-matrix registration (this round) + the C-ledger + the mock retirement triggers.

### §4 (the dependency graph + critical path)

> The S-app node's "D30 W-C..W-G" collapses to DONE; the head becomes "**`.agents/` + census-CI → K3 (store/policy ∥; DOM after the mapping row) → K4**". The WDC waveform edge is UNCHANGED (still the only K3 external prerequisite — still unlanded). ADD one node: "**S-spec's mode-matrix registration (this round) → the r1-entry mode-family scope decision → r1's shape**" (the decision material: the mode fleet's 10 verdict cards + ~70-76 gap rows).

### §5 (the protocol)

> Item 6 (the census discipline) gains: "**Enforcement status: the CI-gated script is ORDERED but NOT YET WRITTEN (R25 finding) — until it lands, the census is battery-verified only; the S-app census-script row is the law's missing half.**"

### §6 + §8

> §6: mark the R24 steps closed; supersede with the R25 record (the 5 scouts + the 10 mode reports + this xcut audit + the battery re-baseline + the integration review).
> §8 estimates: crawl **5-11 wk solo / 3-6 two-dev UNCHANGED** (the D30 completion is offset by the census-CI + filings + mode-matrix-registration items); walk 10-15/7-10 unchanged; run 20-28/13-17 unchanged **WITH the mode-family rider: if the 10-mode DaVinci family is ruled in-scope at r1-entry (the 4 absent modes + the insert/overwrite port families), r1 grows ≈ +1.5-3 wk solo / +1-2 two-dev — a user-visible scope gate, paired with the D28.2 productization gate's timing.**

---

## 3. The mode-matrix registration (the plan-side registration — the 10-mode matrix as a K2/K3 gate)

**The registration row (§2 K2 + K3, one instrument, two halves):**

> **THE 10-MODE EDIT-MATRIX** (the R25 mode fleet's instrument — the completeness census the way the 24+6 wire census is): a named register in spec 06 (mirrored in 17's battery rows): mode | engine math | OT wire verb | UI affordance | pins | verdict. **Baseline at the R25 pins:** roll/slip/slide/ripple-trim/insert-edit/overwrite = engine-class-only (6/10, unwired, thin pins); replace/append/ripple-overwrite/fit-to-fill = absent (4/10); **wired end-to-end app-side: split-insert, DnD-insert, slip (keyboard), ripple-trim (keyboard)** — the app's honest-subset state, BY CONSTRUCTION of the classic-parity mirror. **K2's half:** the matrix registered + the wired-subset pairs green (engine math ↔ OT verbs where they exist — the pairs close as r1 lands). **K3's half:** the app's mode-coverage pin family green for the wired subset + the honest-absent verdicts registered (the OP-COVERAGE-style census — with a replace row this time; no silent scope cuts).

**Track routing of the mode fleet's ~70-76 gap rows (the plan-side registration):**

| Track | The rows that enter it | Phase |
|---|---|---|
| **S-spec (THIS round — the authoring/ruling rows)** | 06: the §5.9A append family (append-G1: resolve/multi-append/playhead-ignored/no-push/linked laws); the §5.9B insert-edit law (insert-G1: the 3-point model, the mid-clip split, the transition-window abort, the linked ruling — BEFORE the r1 port needs it) + the §5.9B overwrite partial-coverage law (overwrite-G1) + the §5.9B replace family (replace-G-R1/G-R8: contract + the unfillable-source refusal law); the §5.9 composite-subsection + phantom-row fixes (ripple-overwrite G-RO-1: the :1509/:3120/:3175 rows); the §12 clamp-vs-reject re-wording (slip-G3) + the §13 slide contradiction fix (slide-G4); 15: the §4.3.6 element-type fix (slip-G2) + the verb-decision rows (insert-G3/G4, append-G2, overwrite-G3, ripple-overwrite G-RO-2, replace G-R3's verb, fit-to-fill G5's composable stance); 16: the F9-F12 family rows (replace-G-R4, append-G3, ripple-overwrite G-RO-3) + the R-row two-world note (ripple-trim-G6) + the slide rows (slide-G2); 18: the trim-mode overlay/grammar family (roll-ROLL-1, slip-G-SLIP-1, slide-G5/G6, ripple-trim-G5, overwrite-G5, append-G4, replace-G-R5, ripple-overwrite G-RO-4); 17: the battery rows (insert-G7, append-G6, overwrite-G1's row set). **The ruling asks:** the r1-entry mode-family scope gate; the keyframe-authoring territory ruling (OT R9-b); the fit-to-fill rate-domain unification (fit-G13: [0.1,16]/[0.01,5]/[1/32,32] → recommend [0.1,5] + INVALID_PARAMS refusal). **The mock-owner corrections:** replace-G-R6 and ripple-overwrite G-RO-5's shell-variant fixes are SPEC-REPO mock work (ui-mock/shell-variants), not S-app — register under S-spec's mock pass. | this round (r1-prep) |
| **S-ot (r1 rows)** | The verb/port family: `timeline.roll`/slide/`trim{ripple}`/insertEdit(3-point)/overwrite/replace/append/insertBatch + the M49C census re-declare law (tsc-lockstep — mechanical, no amendment); the carried-test families (ROLL-5's three roll laws, slip-G-SLIP-9's vitest family, slide-G3's tests); the gesture routing (ripple-trim-G2: ripple-mode routes the trim gesture; ROLL-7's OT half; replace-G-R7: the drop-on-clip no-op retired); the R9-c hardening hunks + WIRE_UI_EXCEPTIONS (per §2's S-ot amendment). | r1 |
| **S-engine (the defect-fix rows, pre-r1)** | The linked mid-clip insert corruption (insert-G2 — live-probed, unqueued anywhere: file it in the engine's queue per the cross-repo protocol); the roll counterpart clamp (ROLL-2); the roll/slide/ripple-trim transition+keyframe preservation clamps (ROLL-3, slide-G3, ripple-trim-G3); rippleTrimItem exposure + first tests (ripple-trim-G3); the R9-queue reconciliation (§2's S-engine amendment). **The mode fleet's fit-to-fill posture is cited as evidence, not work:** G1/G2/G6/G7/G9 = LANDED (the speed-to-fit math, the rate-stretch primitive, WDC's varispeed, the composition law, the loop-region I/O). | pre-r1 |
| **S-app (K3 rows)** | The slip pin (slip-G-SLIP-7: wireLog gains a 'slip' assertion + a D28 dispatch pin for the trimStart/trimEnd patch shape) + the group-tightest companion clamp fix (slip-G-SLIP-6 — the engineService slip skips out-of-bounds companions; mirror `_slipImpl`'s tightest-δ); the composed ripple family (ripple-trim-G4: the 4 units/16 tests over the app's composition); the K3 selectTool controller layer (roll-ROLL-7: the tool radios stop being inert view-state); the maintainPitch toggle residual (the engineService retime hardcode). | K3 (crawl window) |
| **S-package (rows)** | The ripple-tool inert-for-trim posture (ripple-trim-G7: route the gesture — the variants' Clip.tsx:586-595 is the reference — or document mode-inert until OT's G2 lands); the slip keymap law (slip-G-SLIP-5: the runtime-fps resolver or the registered deviation; the tool-gated disambiguation). | r1-adjacent |

---

## 4. Cold-executor re-simulation (the 6 most-changed rows, walked as a cold executor would)

**R1 — S-app step (0), the `.agents/` bootstrap.** *Entry point:* `/home/z/my-project/nle-test-app` — the ORIENTATION cell (README + worklog tail) resolves; both files exist and are current. *Action:* create `.agents/HANDOFF.md` + `.agents/PLAN.md`; the content sources are named and reachable (the worklog's R9 entries + the nle-ui repo's `.agents/` R9-close, both cited in the row). *Gate:* the two files exist and are git-tracked — testable (`ls .agents/ && git status --short`). *Blocked?* No — zero cross-repo dependencies. **PASS.**

**R2 — S-app step (1b), the census-CI enforcement.** *Entry point:* the app's port tree + `vendor/nle-timeline` (the byte-exact lock-copy @ `c15a629` — present, `UPSTREAM.lock.json` pins it); the recomputation method is fully specified (import-specifier-normalized per-file diff — the scout cards document the exact procedure). *Action:* author `scripts/census.mjs` + the register doc + the `ci.yml` step (the current ci.yml read by this agent: the insertion point after `npm run boundary` is obvious). *Gate:* the script exits 0 at the current tree (40 files = 35 zero-action + 5 carriers + host) and register-inequality fails the build — testable by running it and by mutating a mirror. *Blocked?* No. **PASS.**

**R3 — S-engine step (1), the R9-queue reconciliation.** *Entry point:* `nle-engine/.agents/PLAN.md` R9 queue (:135-163 — the 4 unchecked entries are readable and unambiguous; read by this agent). *Action:* apply the four D29.5 dispositions (each a one-paragraph doc edit with the evidence named: composition-frame.ts:70-94 for R9-a; scene-to-segments.ts:246 + the app's engineService.ts:477/:486 for R9-b; the `8a0b7fe` design for R9-c) + re-file the maintainPitch residual as an app row. *Gate:* the queue's dispositions textually match ARCH-R24 D29.5 + the app-side row exists — testable (grep both queues). *Blocked?* No — the amendment (ARCH-R24 v2) already exists; this is execution. **PASS.**

**R4 — S-wdc step (1), the waveform-contract promotion.** *Entry point:* `/home/z/my-project/web-daw-core` — the ORIENTATION cell (the repo-ROOT `HANDOFF.md` + `PLAN.md`, explicitly NOT `.agents/`) resolves; the target section ("Next-session scope", :32-58) exists and is the correct venue (read by this agent: items 1-4 enumerated, the waveform row absent). *Action:* insert the waveform row (the contract statement is pre-drafted in both the plan's S-wdc cell and spec-20 §0:25 — the audio-registry.ts:24-74 shape) + author the pin (a contract test in WDC's suite). *Gate:* the row present in the root HANDOFF + `bun run test` green with the new pin (759→). *Blocked?* No. **PASS.**

**R5 — S-ot step (1), the carrier-reduction work-order filing (merged with R9-c).** *Entry point:* `opencut-timeline/.agents/PLAN.md` (exists; the cross-project queueing protocol's format is demonstrated by the R9 block at :422-466 — read by this agent — the titled-block shape to append after). *Action:* append the work-order block (the 6 optional props + the 4 R9-c hardening hunks — every item enumerated with acceptance in the plan's own S-ot cell; the R9-c hunks' text is already written in OT's own PLAN). *Gate:* the block exists with per-item acceptance; future landings are census-tracked (the app's census script — R2 — is the tracker). *Blocked?* No (a docs commit; the `bun run dev` + test gates apply to the code landings, not the filing). **PASS.**

**R6 — K3's amended exit gate, the mode-coverage pin family.** *Entry point:* the app's vitest suite + `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` (the acceptance list exists and is cited by the row) + the named mode rows (slip-G-SLIP-7, ripple-trim-G4 — each with its exact test spec drafted in §3). *Action:* author the slip pin (the wireLog 'slip' assertion + the dispatch pin — the verb is routed app-side today: `store.slipNudge` → the `slip` case in engineService, per scout-app's live trace) + the composed ripple family (the 4 units/16 tests — the `trimToPlayhead(edge, ripple)` path exists and is already half-pinned at GluedShell :361/:368). *Gate:* `npx vitest run` green with the new rows (206→). *Blocked?* No — the verbs and surfaces exist; only the pins are missing. **PASS.**

**6/6 PASS.** Every amended row resolves at its entry point, names a machine-checkable gate, and carries no unresolved dependency — the plan remains cold-executor executable after the amendments.

---

## 5. Verdict

**The plan's architecture survives R25 intact; its S-app row is the most-stale artifact in the fleet.** The six-track/ladder/gates spine is still correct, but the R24-4 text mis-describes the critical path's head as future work when it is closed (W-C..W-G all landed, 206/206 live, the census at its declared irreducible set, the fork-retirement carrier condition MET). The material findings a cold executor would trip on, in priority order: (1) **the census law is half-enforced** — D26.2/§6 claim a CI-gated register-equality build-failure that does not exist app-side (no script, no CI step, no register doc — self-verified in ci.yml); (2) **the app's `.agents/` is overdue ×2** — the fleet's own notification surface was ordered before W-C and every wave since ran without it; (3) **the WDC waveform promotion is 2 rounds unexecuted** and remains K3's only external hard prerequisite, spec-carried only — the venue confusion now settled: the target (root HANDOFF) exists, the row simply never landed in it; (4) **the engine's R9 queue carries 3-of-4 stale dispositions** (the filings fired before the ARCH-R24 v2 amendments; R9-d is the only aligned entry); (5) **the five ordered cross-repo filings are 2/5** with named residuals (the C0 row, D29 package-side, the P-ticks, the carrier-reduction work order itself — never filed). The ten mode reports add a genuine new instrument — the 10-mode edit-matrix (≈2.5-4 wired end-to-end; 6/10 engine-class-unwired; 4/10 absent) — which the plan should register NOW (cheap, S-spec-side, this round) as the K2/K3 completeness gate, with the absent-mode authoring held behind a user-visible r1-entry scope gate (+1.5-3 wk solo if ruled in). The estimates hold: crawl 5-11/3-6 (the D30 completion offsets the new enforcement/filing items; the mode gaps are law-authoring + r1-ports, and the engine already holds 6/10 algorithmically), walk unchanged, run unchanged pending the mode-family gate. **Amend, don't re-architect: the 9 mismatches are row-level, and all 6 re-simulated rows pass cold.**

---

*R25 xcut plan audit — read-only; single artifact: this file. Live evidence: every row of §1's table re-verified against the live repos by this agent (git logs, full file reads of ci.yml / WDC HANDOFF / engine+OT PLAN R9 queues, greps as enumerated in the header); no repo modified, nothing committed.*
