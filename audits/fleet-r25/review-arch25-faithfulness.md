# R25 adversarial review #2 — CORPUS FAITHFULNESS of ARCH-R25

**Reviewer:** fresh-context, read-only (sole write = this file). **Date:** 2026-09-09.
**Method:** the ruling re-read in full, then every load-bearing citation re-derived against the live files: engine `timeline.ts` @ `3989506`, OT @ `fdb771c` (src identical to `c15a629` — `git diff --stat c15a629..HEAD -- src/` empty), WDC @ `85b81b0`, nle-ui @ `3026099`, app @ `64fb0ab`, the numbered specs, and the 20 fleet reports. 50+ discrete claims checked; the load-bearing subset tabulated below. `battery_r24.py` was executed (85/85 PASS — the ruling's "85 checks" re-derived live).

---

## The verification table

| # | Claim (ruling) | Verified? | Evidence (re-derived live) | Correction if wrong |
|---|---|---|---|---|
| 1 | `rollingTrimItems` timeline.ts:2984 | ✅ | engine timeline.ts:2984 (class method, unwired from the 19-op headless surface — scout-engine's census) | — |
| 2 | `rippleTrimItem` :2839 | ✅ | timeline.ts:2839 | — |
| 3 | `slip` :4143 | ✅ | timeline.ts:4143 (`slip(clipId, deltaFrames, {linked})`) | — |
| 4 | `slideItem` :4246 | ✅ | timeline.ts:4246 | — |
| 5 | `performInsertEdit` :4702 / `_performInsertEditImpl` :4719 / `performOverwriteEdit` :4860 | ✅ | all three at the exact lines | — |
| 6 | `joinItems` transition-remap pattern :7411-7427 | ✅ | `joinItems` (:7341); :7411-7427 = the `replacementByRemovedId` endpoint remap + self-transition drop (remap, not cascade-drop) — exactly as D31.4 describes | — |
| 7 | The D32.6/E1 defect: Phase 2's linked expansion pulls the split's LEFT half into the shift set; both halves share the linkage group; companion shifts whole; exact-overlap vs 00:804 | ✅ | Code read directly: `_splitClipPure` (:4591-4603) spreads `linkedGroupId` onto BOTH halves; Phase 2 (:4776-4792) seeds `from >= insertFrame` (right half in), then `findLinkedClips` (:1460-1483, whole-`linkedGroupId` filter) adds the left half + companion; shift `+clipDuration` puts the left half's span inside the inserted clip's span. Matches mode-insert §3.1's live probe verbatim | — |
| 8 | "Unpinned by every suite (all fixtures opt out of linking)" | ✅ (substantively) / ⚠ phrasing | `timeline-edit-ops.test.ts`: the two `performInsertEdit` pins pass NO options (default `linked:true`); the fixture (:24-39) contains NO linked pairs; milestone 7.6 uses `{linked:false}` (mode-insert) | "Opt out" is loose — the fixtures simply contain no linked clips (and the vitest calls don't pass `linked:false`). The linked path is unexercised either way |
| 9 | OT `insertElements` ~:872 placement-reject, main-track zero-anchor, F1A-3 homogeneous batches (D31.1) | ✅ | timeline-core.ts:872; doc-comment "or null when placement failed" (:863); zero-anchor (:865-868); F1A-3 mixed-batch reject (:874-885) | — |
| 10 | P1 — 06:1509 "ripple-insert via the diff" is a phantom (the diff can't push) | ✅ | 06:1509 quotes the joined-interval push; OT `ripple/index.ts:175-180`: `freed = vacated − joined`; a pure insert has vacated = ∅ ⇒ freed = ∅ ⇒ **no shift at all** | — |
| 11 | P2 — 06:3110 "unless overwrite placement" (no overwrite placement exists) | ✅ | OT `PlacementStrategy` (placement/index.ts:43-54) = 5 members (explicit/firstAvailable/preferIndex/aboveSource/alwaysNew) — no `overwrite`; 06's own strategy list (:1497-1502) same | — |
| 12 | P3 — 06:3120 `ripple-insert-makes-room` unreachable | ✅ | same diff math as #10 (freed = ∅ on insert); OT wire has no insert-side ripple routing | — |
| 13 | P4 — 06:3160 "slide: sourceStart/duration unchanged" contradicts the continuity law | ✅ | 06:1293/1298 (source window shifts by the continuity delta) + 15:689-695 (`preserveContinuity` default true) vs 06:3160's test row; mode-slide G4 files exactly this | — |
| 14 | P5 — 06:3161 `slide-no-chain-is-noop` mis-name (chain gates only the source delta; the slide proceeds) | ✅ | 06:1293 ("if … form a split-contiguous chain … the source window shifts"), 15:691 ("only applies if…"); engine `_slideItemImpl` proceeds (mode-slide: timeline.ts:4336); mode-slide G4(b) | — |
| 15 | P6 — 06:3175 references non-existent placement values | ✅ | `placement: 'overwrite'` / `'ripple'` — neither is in any PlacementStrategy union (see #11) | — |
| 16 | P7 — 15 §4.3.6 element list: image yes, composition no; 06+engine the opposite | ✅ | 15:650 "must be `video`/`audio`/`image`" + 15:672 constraint; 06:1267 "video`/`audio`/`composition`"; engine `isMediaItem` (timeline-math.ts:280-282) = video/audio/composition, comment "Image clips stay excluded" | — |
| 17 | P8 — 07:367 is the ElementTransition params-keys "overwrite-only" row (patch semantics, not a placement strategy) | ✅ | 07:367 read: "params keys are overwrite-only and the null-clear law does NOT extend to them (`types/index.ts:166-168`)"; mode-overwrite.md:48 + :137 class it a naming collision/hazard | — |
| 18 | P9 — 06 10.4:2408 marks `performInsertEdit` "ALIGNED" against §5.9 which teaches the OTHER insert semantics | ✅ | 06:2408 row verbatim; 06 §5.9 (:1430-1483) = the OpenCut `InsertElementCommand` placement insert | — |
| 19 | P10 — 05:949's amber ROLL_COLORS claim; FreeCut files in no checkout; 20:8 precedent | ✅ | 05:949 lists `ROLL_COLORS (amber, different glow)`; no freecut tree anywhere under /home/z/my-project (find); 20:8 carries the R15-era point-in-time qualifier verbatim | — |
| 20 | 00-master:804 = the no-overlap invariant | ✅ | 00:804 "No overlaps on any track after ripple/move/insert" | — |
| 21 | 15 §4.3.5 RollCommand exists with the constraint set mode-roll claims | ✅ | 15:609-642: adjacency (`right.startTime === left.startTime + left.duration`), source bounds, min duration 1 frame, transitions & keyframes preserved (binary-search clamp) — mode-roll.md:88 quotes it verbatim | — |
| 22 | 16 §3.2 has V/B/H/Z/T/Y/U/R (+ FX per R23-WA) | ✅ | 16:175-193 (8 plain tool keys + the FX radio note) | — |
| 23 | 16 §6.2: source-mode (panel, step 3) outranks the slip tool (tool, step 4) | ✅ | 16 §6.2 (:609-619): step 3 "Panel-specific context" precedes step 4 "Tool-specific context" (xcut-keyboard §"comma-period" resolves on exactly this) | — |
| 24 | 18 §4.9 owns Shift+F10 (D34.1: NOT available) | ✅ | 18:220 "right-click and on **Shift+F10** … (keyboard route is normative, §11)" + 18:442/:463/:464; found by xcut-keyboard XKB-2 as the fleet's missed claimant | — |
| 25 | The four families (REPLACE / APPEND-AT-END / RIPPLE-OVERWRITE / FIT-TO-FILL) absent corpus-wide | ✅ (specs + 5 codebases) / ⚠ phrasing | My own greps: zero matches in specs 00-20; zero in engine/OT/nle-ui/app/WDC src trees. But they ARE described in `ui-mock/shell-variants/docs/r20/insert-modes.md` and implemented in the variants mock — which the ruling's own matrix credits in the reference column | "NOWHERE in ANY repo" overstates — say "no spec row, no production codebase; mock-repo design docs + the variants mock are the only homes (the matrix's reference column)" |
| 26 | Matrix OT cells: insert "placement-insert only (reject-not-shift)"; ripple-trim "ripple-delete only; trim verb has NO ripple param; W11 exception" | ✅ | OT `timeline.trim` = group shape `{elements, side, deltaTicks}` no ripple flag; one of the W11 6 exceptions (mode-ripple-trim S78; scout-ot's gate line "24 distinct … 6 documented exceptions") | — |
| 27 | Matrix 06 homes: §5.5/§5.6/§5.7/§5.2A/§5.9 exist; §5.9B does not | ✅ | 06:982/:1151/:1275/:777/:1430 headers present; no §5.9B (mode-overwrite G1: "§5.9 has no overwrite prose") | — |
| 28 | D31.7 rate domains: engine [0.1,16] ∩ OT [0.01,5] ⊂ WDC [1/32,32] ⇒ [0.1,5]; direction marked/target; one-decimal badge | ✅ | engine timeline-math.ts:51-53 `MIN_SPEED=0.1, MAX_SPEED=16`; OT retime.ts:12 `MIN_RETIME_RATE=0.01` + clamp [0.01,5]; WDC `RETIME_RATE_MIN=1/32/MAX=32` (scout-wdc); mode-fit-to-fill :27 pins the direction (and flags scout-wdc's inversion); xcut-plan fit-G13 = [0.1,5]+INVALID_PARAMS; badge precision = r20/G14 | — |
| 29 | WDC venue: root `HANDOFF.md` is real, `.agents/` holds only SKILL.md, no waveform row in WDC's queue (2nd round) | ✅ | ls web-daw-core: root HANDOFF.md+PLAN.md; `.agents/` = SKILL.md only; grep of root HANDOFF = no waveform row; scout-wdc: "the D29.4 promotion lives spec-side only"; PLAN.md:150-152 = the parking-lot bullet | — |
| 30 | D36.3 "the R24 promotion was **filed against `.agents/HANDOFF.md`**" | ❌ | **No filing against `.agents/HANDOFF.md` exists anywhere in the record.** The R24 promotion was recorded spec-side only (20-audio-core §0/§7/§151; ARCH-R24 D29.4; the plan's S-wdc row) and never landed in WDC at all; ARCH-R24/plan-and-tracks **correctly** name the root HANDOFF as the venue ("orient: the repo-ROOT HANDOFF.md + PLAN.md — NOT `.agents/`") | Re-state: "the promotion was never executed in WDC — it lives spec-side only (second round); the venue is the root HANDOFF (its `.agents/` holds only SKILL.md)". The re-file + filing-verification-gate fix stands |
| 31 | The engine's R9 queue (3989506) carries 3-of-4 stale dispositions predating ARCH-R24 F1; re-file map R9-a→FUTURE-EXTENSION, R9-b→LANDED (residual app-side), R9-c→DECLINED, R9-d stands | ✅ | engine `.agents/PLAN.md`:135-163 read: R9-a open proposal (vs F1(a) future-extension), R9-b open proposal (vs F1(b) closed-LANDED), R9-c open promotion (vs F1(c) NOT-filed decline), R9-d = the open widening (matches F1(d)); scout-app.md:32 corroborates the mismatch | — |
| 32 | App ci.yml runs no census check; `scripts/census-check.ts` absent; D26.2 claims CI-enforced register-equality | ✅ | ci.yml read: checkout/typecheck/test/build/boundary/storybook only; app package.json scripts have no census; app scripts/ = bootstrap.sh, check-boundary.mjs, dev3000.py; 00-master:445 (D26.2) demands "a CI-gated script … register-inequality FAILS the build" | — |
| 33 | App `.agents/` absent, OVERDUE ×2 | ✅ | ls nle-test-app/.agents → absent; scout-app: "2nd consecutive round overdue" | — |
| 34 | §1 scout cards: engine 3989506 one-docs-commit-past-5036387 458/458; OT fdb771c code-identical to c15a629, 536/536+M49C 24/24, docs-only (R9 queue + R9-c W8 guard); WDC 85b81b0 zero-movement 759/759; nle-ui 3026099 6 shell/keymap/docs commits, gesture surface byte-identical, 674/674+mini 355/355; app 64fb0ab all D30 waves, 206/206 live, 35 zero-action + 5 carriers + host (down from 7) | ✅ | All re-derived: `git log`/`git show --stat` per repo (engine 3989506 touches only `.agents/PLAN.md` +31; OT src diff vs c15a629 EMPTY, commits c37844f+fdb771c = PLAN.md only; WDC HEAD=85b81b0; nle-ui 6 commits fc4cc35→3026099; app 9 commits c885ece→64fb0ab); scout cards match §1 verbatim | — |
| 35 | mode-replace recommends the joinItems remap + `linked:false` sever + a dedicated op (D31.4) | ✅ | mode-replace §(b) Composition-B break #2 (remap vs cascade-drop, `:7411-7427` pattern), §(c) sever law, §"Recommendation — a dedicated engine op `performReplaceEdit`" | — |
| 36 | xcut-keyboard: source-mode-gated primaries `,`/`.`/`Shift+.`/`E`; F9-F12 documented alternates; preventDefault + hostage law; four-meaning §6.1 row; slide trap (00:804/06 §5.7); three-sentence tool boundary | ✅ | xcut-keyboard §2.3 two-tier ruling, §"The slide trap" (:81), §"tool-vs-source-edit" (:109 "at the cost of three sentences"), proposed-rows table (:127 four meanings) | — |
| 37 | xcut-shell-mocks: 19 censused families; the 8 MOCK-ONLY list; C33-C58 ledger; 5-round aging | ✅ (facts) / ⚠ arithmetic | 19-row table read; the 8-list matches the verdict paragraph; the ledger (nle-core-spec/`.agents/SPEC-REVISION-CANDIDATES.md`) carries C10-C58; "five rounds" (R19→R25) | "19 = 11 aligned / 3 diverging / 8 MOCK-ONLY" **sums to 22 ≠ 19** — the verdict classes overlap (compound rows). Re-word: "11 rows with an aligned primary verdict, 3 diverging, 8 carrying mock-only findings (classes overlap)" |
| 38 | D32: seven un-cross-referenced fragments; best statement = a code comment (av-link.ts:10-24); precedence lock > sync-lock > link; `syncLinked` canonical name; relink-both-halves vs the R14 sever; 09 `linkedTo` pairwise vs engine `linkedGroupId` | ✅ | xcut-linked-companions XC-1 ("Seven scattered fragments… the single best statement… is a doc-comment in the engine's bridge"), §5.0 draft (precedence), XC-5/XC-6/XC-7 | — |
| 39 | E2 (overwrite companion asymmetry) / E3 (silent R1-B4 abort) | ✅ | xcut-linked-companions OVERWRITE row (:4998-5017 removed cascade; trimmed survivors NOT trimmed); mode-overwrite G4(a)+(b); engine test :80-97 (empty return, no error code) | — |
| 40 | D36.4 "the W8 guard landed — tick" | ⚠ misleading | The W8 guard is OT's **pre-existing** guard and still **has the `!shiftKey` hole**; what landed this round is (a) the app-side twin fix (app 0328bc0: "the Z4/W8 guard drops the `!shiftKey` carve-out") and (b) OT fdb771c = the **R9-c amendment documenting the hole** (PLAN.md only). The OT-side hole fix remains OPEN (scout-ot table row fdb771c) | Re-state: "the app-side Z4/W8 twin landed — tick; OT's own W8 guard still carries the `!shiftKey` hole — the R9-c(0) upstream-back row stays open (S-ot)" |
| 41 | D33: "12 of the 13 grammar elements pinned NOWHERE"; green-over-amber; 05 §8A + 18 §9 split | ✅ | xcut-visual-grammar :64 census + VG-1; :118 "pin the mock's green"; the 05/18 split per D25.3 (:97-101) | — |
| 42 | D33.1's 8 token names ("trim-edge-green, glow-green, dim-overlay, active-border-red, ghost-fill, arrow-white, slot-dash, badge-chip") | ⚠ mismatch | xcut-visual-grammar's proposed set (:107-116): `--trim-edge-active`, `--trim-glow-soft`, `--clip-dim-overlay`, `--clip-active-edit`, `--ghost-outline`, `--badge-chip-{…}`, `--overlay-arrow`, **`--source-active-border`** | The ruling renames 4, splits `--ghost-outline` into ghost-fill+slot-dash, and **drops `--source-active-border`**. Restate the 8 tokens as the fleet's names (or an explicit re-name map) or the amendment fleet forks the proposal |
| 43 | D36.6 estimates: crawl 5-11/3-6 HOLDS; r1 +1.5-3 wk solo only if the scope gate passes | ✅ | xcut-plan row 7 verbatim | — |
| 44 | §7: "everything battery_r24 carried (85 checks)" | ✅ | `python3 scripts/battery_r24.py` → "battery_r24: 85/85 PASS" | — |
| 45 | §2.1 "(06 §5.9 references them)" for insert/overwrite engine forms | ⚠ loose | §5.9's own prose does not mention the 3-point family (mode-overwrite G1: "§5.9 has no overwrite prose"); the reference is 06 §10.4:2408's row mapping them to §5.9 | Say "06 §10.4's coverage row maps them to §5.9 (the P9 mis-alignment)" |
| 46 | "~76 posture-law gap rows" (§1) | ⚠ unverifiable | Counted 60-130 rows across the ten mode reports depending on which table classes count (S-rows/Δ-rows/G-rows); no stable count of exactly ~76 derivable | Keep the "~" or recompute with a stated rule before battery_r25 pins a number |
| 47 | Internal: D31.2 "left half NEVER moves" vs D32.6's fix; E1's mechanism vs D32.6; P4's fix vs mode-slide's finding | ✅ | D31.2/D32.6/E1 mutually consistent and consistent with the code (the fix = exclude left half from the expansion + split straddling companions); P4's fix text = mode-slide G4's re-wording verbatim | — |
| 48 | Internal: D31.3 "the phantom `placement:'overwrite'` rows (06:3110, 06:3175, **07:367**) re-key to this family" vs P8 | ❌ (inconsistent) | P8 (correctly, per mode-overwrite :48/:137) treats 07:367 as a patch-semantics naming collision → hazard note, NOT a re-key to §5.9B | D31.3 should list only 06:3110 + 06:3175 as re-keying; 07:367 gets P8's hazard-note treatment |
| 49 | D35.3 "the mocks' `.agents/SPEC-REVISION-CANDIDATES.md`" | ⚠ venue | The ledger lives in the SPEC repo's `.agents/` (nle-core-spec/.agents/…); the mocks themselves (shell-variants) have no `.agents/` | Say "the spec repo's `.agents/SPEC-REVISION-CANDIDATES.md` (the mock-proposal ledger)" |
| 50 | §2.1 headline: 4 trim modes specified-but-unwired; 6/10 engine algorithmic; app 2.5/10 wired | ✅ | scout-engine's census (6 EXISTS class-only / 4 ABSENT); scout-app's tally ("2.5/10 … split-insert, DnD-insert, slip; ripple-trim keyboard-only") | — |

---

## Misquotes found

1. **D36.3 (the one true misquote):** "the R24 promotion was filed against `.agents/HANDOFF.md`" — no such filing exists in any record. The R24 promotion (ARCH-R24 D29.4 → 20-audio-core §0/§7 → the plan's S-wdc row) was **spec-side only** and never landed in WDC at all; the R24 documents themselves correctly name the **repo-root HANDOFF** as the venue (plan S-wdc row: "orient: the repo-ROOT `HANDOFF.md` + `PLAN.md` — NOT `.agents/`"; fleet-r24 plan-and-tracks: "✓ root HANDOFF"). The substantive finding (the row is absent from WDC's queue for a second round — scout-wdc: "the promotion lives spec-side only") and the fix (re-file at the root HANDOFF with the audio-registry.ts:24-74 citation + a filing-verification gate) are correct; only the "was filed against" attribution is invented.
2. **D31.3 vs P8 (internal inconsistency):** D31.3 lumps 07:367 into "the phantom `placement:'overwrite'` rows … re-key to this family [§5.9B]"; the ruling's own P8 (and mode-overwrite.md:48, :137) correctly holds that 07:367 is the ElementTransition **params-keys overwrite-only** patch-semantics row — a naming-collision hazard note, not a placement row and not re-keyable to §5.9B.
3. **D33.1's token list** does not match the fleet's proposed tokens (renames; `--ghost-outline` split into ghost-fill+slot-dash; `--source-active-border` dropped) — a fork risk for the amendment fleet (see table #42).
4. **D36.4's "the W8 guard landed — tick"** mis-dispositions R9-c(0): OT's W8 guard still carries the `!shiftKey` hole; only the app-side twin (Z4) and the hole-documentation (OT fdb771c, PLAN-only) landed.
5. **§2.4/D35.1's "19 = 11/3/8"** is arithmetically incoherent as a partition (sums to 22; compound verdict rows counted twice). Each number individually traces to xcut-shell-mocks, but the presentation misstates the census.
6. **Minor phrasing:** "specified NOWHERE in ANY repo" (#25), "all fixtures opt out of linking" (#8), "(06 §5.9 references them)" (#45), "the mocks' .agents/SPEC-REVISION-CANDIDATES.md" (#49), "F10's family neighbor" for the ripple-overwrite alternate (xcut-keyboard actually re-chords to `⇧⌥.` / reliance on Tier-1 `Shift+.`, not an F-family neighbor) — all corrected in the table.

**What survived fully:** every engine/OT line number (all 11), the entire P-list P1-P10 phantom-row set, the D32.6 defect mechanism (matches the code exactly, including the `linkedGroupId`-on-both-halves premise and the whole-companion shift), the corpus-wide four-family absence (re-grepped), the pin world (all five repos re-derived from git), the five scout-card summaries, the mode-replace/xcut-keyboard/xcut-shell-mocks/xcut-linked-companions attributions, the R9 stale-disposition map (matches ARCH-R24 F1 exactly), the ci.yml/census and app-`.agents/` claims, D31.5/D31.7's laws and domains, the battery's 85-check figure, and the D31.2↔D32.6↔E1 internal consistency.

---

## The verdict

**GO-WITH-AMENDMENTS.**

The ruling's factual spine is sound: of ~50 load-bearing claims re-derived live, 44 verify exactly (including every line number, every phantom row, the P1-defect mechanism, the pin world, and the fleet-card summaries). The failures are five amendment-level precision items, not architecture:

1. **D36.3** — re-state the waveform-filing story: the promotion was never filed in WDC (spec-side only, second round); the venue is the root HANDOFF. Drop the invented ".agents/HANDOFF.md filing" attribution. (The fix and the gate stand.)
2. **D31.3** — remove 07:367 from the re-key list (06:3110 + 06:3175 only); 07:367 keeps P8's hazard-note treatment.
3. **D33.1** — restate the 8 tokens as xcut-visual-grammar's proposed names (or an explicit re-name map); restore `--source-active-border`.
4. **D36.4** — correct the W8 disposition (app-side twin landed; OT's hole fix remains open under R9-c(0)/S-ot).
5. **§2.4/D35.1** — re-word the census arithmetic ("classes overlap"; 11 aligned-primary / 3 diverging / 8 with mock-only findings over 19 rows). Optionally tighten the four minor phrasings noted in Misquotes #6.

None of these requires re-opening a decision; all fold into the v2 edit before the amendment fleet runs (§6's step 1→2 ordering already provides the slot).

*Read-only respected: no repo file other than this report was created or modified; `battery_r24.py` was executed (read-only checks) to re-derive the 85-check figure.*
