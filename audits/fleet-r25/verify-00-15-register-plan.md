# Fleet R25 verification — 00-master / 15-wire / REFERENCE-REGISTER / IMPLEMENTATION-PLAN

**Agent:** the 00/15/REFERENCE-REGISTER/IMPLEMENTATION-PLAN verification agent (fresh context). **Method:** read ARCH-R25 (whole) → per-target `git diff HEAD~1` + full-file context; every pin, count, and entry point re-derived live (rg / git / file reads); the ruling's D30-D36 clauses checked against the landed text clause-by-clause. **No commit made** (working-tree fixes only, per the fleet order).

---

## The verification table

| # | Check | Verdict | Evidence |
|---|---|---|---|
| 1 | 00's D30/31/31A/32/33/34/35/36 blocks match the ruling (key clauses) + D26-D29 format | **PASS (2 sub-clauses fixed)** | 30.2 census-not-reopened ✓ ("the landed C7 census (24 routed + 6 exceptions, Decision 29.2) is NOT reopened"); 31.6 composite ✓ (delete+move+insert, applyBatch, one undo, F1A-2 trajectory); 32.4 relink-both ✓; 33.2 green ✓; 35.3 intake ✓; 36.3 root-HANDOFF ✓. Format = the D26-D29 house form (### Decision N + **Decision:** + "Full evidence + rationale: ARCH-R25 (DNN, adversarially reviewed)") ✓. **34.1's Shift+F10 sub-clause was garbled** ("the ripple-overwrite alternate is F10" — contradicts the F-block F10=overwrite) — fixed to the ruling's form (no F-alternate, primary-chord-only). **36.1 said `census-check.ts`** — fixed to `.mjs` (the ruling's name). |
| 2 | §2A.8/§2A.9 present + house form; D26-D29 byte-untouched | **PASS** | Items 8/9 added after item 7 in the numbered-law form (bold lead + battery language), diff hunk 2 adds exactly 2 lines; `git show HEAD~1` vs worktree lines 421-436: **D26-D29 byte-identical**; §2A items 1-7 byte-identical. |
| 3 | The R25 pin block | **PASS** | Status line: engine `3989506` 458 / OT `fdb771c` 536 (code pin `c15a629` unchanged, src-diff empty) / WDC `85b81b0` 759 / nle-ui `3026099` 674 / app `64fb0ab` 206 / mini 355 / variants 1521+ — every figure cross-checked against the five scout cards' own headers (scout-engine :4, scout-ot :5/:7, scout-wdc :4, scout-nle-ui :3, scout-app :4) + the local repos' `git log` (engine HEAD = 3989506, OT = fdb771c). |
| 4 | v10.0 + the namespace note | **PASS** | Date line gains "v10 Round 25 2026-09-09"; Status = v10.0; the namespace note is present verbatim in the Status line ("Decisions 30-36 are SPEC-LAW decisions — the app repo's own 'D30' … is a DIFFERENT namespace and keeps its name"). See Flagged #4 for the residual pre-existing D26 ambiguity. |
| 5 | 15's landed census byte-untouched (hunk by hunk) | **PASS (letter-spirit note)** | 8 hunks: Status (ledger append), §0 R8-row status note + the new r1-scheduled row, P7 doc-comment, P7 constraint, P11 §4.3.9 doc-comment, §13.15 preamble (ledger append), §4.3.9 row (P11 2nd half), 4 new rows. **§0's census line (15:21), §13.15's census figures, §4.1's census row (15:4905), the error-code rows, and the table column headers: byte-identical** (not touched by any hunk). The Status line and the §13.15 preamble each gained an *appended round-ledger note* (both notes assert "the landed census UNTOUCHED/UNCHANGED @ c15a629") — house-style per Document Governance 2.5.1 (the status header carries the revision ledger); the census bytes themselves are untouched. |
| 6 | P7 element list matches 06+engine | **PASS** | 15 §4.3.6 now gates `video`/`audio`/`composition` — matches 06 §5.6's quoted FreeCut guard (06:1227) and the engine's `isMediaItem` (timeline-math.ts:280-282, verified live in the nle-engine checkout @ 3989506). Direction right: composition in, image out (stills carry no source window). |
| 7 | P11's two halves | **PASS** | §4.3.9's doc comment re-keyed to D31.1/D31.6 (the flag never produces an insert-edit push; the composite is delete+move+insert); §13.15's InsertCommand row carries the R25/P11 re-key with the delta-vs-full-duration law-split. |
| 8 | The four r1-scheduled rows | **PASS** | All four present in §13.15, each in the QUEUED column marked "NOT in the landed census; the 24 routed + 6 exceptions stand untouched": replace (**78→79 union-version bump** noted, dedicated op per D31A), append (**6th PlacementStrategy** `{type:'append'}` + insertBatch + the rippleDelete-pattern wrapper), ripple-overwrite (**InsertCommand replace-placement carrying the DELTA-law ripple**; the law-split "insert{ripple} pushes full duration vs replace-placement shifts by delta — one boolean cannot mean both"), fit-to-fill (insert+`updateElements{retime}` composite; domain **[0.1,5]** three-domain intersection; never clamps; first-class verb optional r2). |
| 9 | The register's 19 rows vs the census table | **PASS (3 pin-count fixes)** | Row-by-row against xcut-shell-mocks.md's table: mock homes (:705-714, :652-653, :658-662, :1209/:1224, :1233-1239, :489-493/:210, :84-91, :1130, :904, …), owning specs, verdicts, and divergence rules all faithful; 15 of 17 cited pin counts re-derived live and exact (SourceRangeBar 8, SourceEditBar 15, useShortcuts 56, Marker/Caption 9+9, AppDock 4, ContextMenu 8, TimelineCompact 24, DeliverPage 20, FxBrowser 5 + FxInspector 12). **Two counts wrong → fixed**: insertPlan 30→**31**, CheatSheet 17→**11** (rg-verified live). Row 1's "06 §5.9A-F" → "§5.9/§5.9B-F" (no §5.9A exists). |
| 10 | The C-ledger intake (C33-C58) + suite-count pins | **PASS (1 disposition fixed)** | All 26 ledger entries C33-C58 present vs the real ledger (`nle-core-spec/.agents/SPEC-REVISION-CANDIDATES.md` §H/§I), each dispositioned; arithmetic was 4 ADOPTED / 8 LANDED / 11 QUEUED / 2 DECLINED / 1 SUPERSEDED — **C39's ADOPTED was an overclaim** (18's new chrome contract landed WITHOUT the trigger list; 18's rows still name only clip-menu + source-card triggers) → re-dispositioned QUEUED, arithmetic now 3/8/12/2/1, still zero untracked. Suite pins at top: variants **1521+** (1,588 it-blocks / 61 files) / mini **355** (8 files) — match the xcut census and the live trees. |
| 11 | The register's law header (35.1-35.4) | **PASS (fixed)** | 35.1-35.3 matched the ruling; **the register's "35.4" was the xcut proposal's K3/VLM+retirement tie, NOT the ruling's 35.4** (the four doc-level rulings) — fixed: the law's 35.4 inserted (SourceEditBar/SourceRangeBar/RangeBand adopted this round + the 3-vs-5 page ruling and marker/caption OPEN registrations), the K3/VLM tie kept as an unnumbered operating-law bullet. |
| 12 | §0's six track rows | **PASS (1 wording fix)** | S-app: `.agents/` overdue-×2 + census-CI queue filing + W-G reconciliation + K3 store/policy + mode-matrix K3 rows ✓. S-engine: E1/E2/E3 as QUEUE FILINGS w/ fix contracts + pins, plus ROLL-2/ROLL-3/slide-G3/ripple-trim-G3 ✓. S-wdc: root-HANDOFF re-venue + "NOT DONE until the root HANDOFF SHOWS it" filing-verification gate ✓. S-ot: R9-c absorption + the W8 hole "STILL OPEN" ✓ — but the "(tick it)" instruction was wrong (what landed @ fdb771c is the R9-c item-0 hole-documentation, a PLAN-only commit, verified by `git show fdb771c --stat`: 1 file, .agents/PLAN.md) → fixed. S-package: the still-open nle-ui filings kept ("STAY, not dropped") ✓. S-spec: battery_r25 core + register maintenance + C-ledger rolling disposition ✓. |
| 13 | The K2 zero-orphan-rows gate + K3 landed-set pins | **PASS** | K2's exit gate carries D30.3's satisfiable form verbatim ("every matrix row is either LANDED … or carries a live r1-scheduled row with owner + acceptance; ZERO ORPHAN ROWS — the completeness is over the REGISTER, not over landed-ness"); K3's exit gate carries "the D30.3 K3 form: the app column matches the LANDED set with pins". |
| 14 | The cold-executor 6/6 table | **PASS (verified live)** | All 6 rows resolve: R1 — `nle-test-app/worklog.md` + `README.md` exist, worklog carries the R9-close entries, `nle-ui/.agents/` exists; R2 — `vendor/nle-timeline/UPSTREAM.lock.json` = upstreamHead `c15a629`, app ci.yml has NO census step (as claimed); R3 — `nle-engine/.agents/PLAN.md` R9 queue at :135-163 with exactly 4 unchecked entries (R9-a..d); R4 — WDC ROOT `HANDOFF.md` "Next-session scope" :32-58, **zero "waveform" matches** (the promotion is genuinely unfiled); R5 — OT `.agents/PLAN.md` R9 block at :422-466; R6 — `ui-mock/shell-mini/docs/LAW-NET-INVENTORY.md` exists. |
| 15 | The estimates row + the census-script name unification | **PASS (2 name fixes)** | Estimates: crawl **5-11 wk solo / 3-6 two-dev HOLDS** (the D30 itemization offsets) + the r1 rider (+1.5-3 wk solo ONLY IF the user passes the absent-family scope gate at r1-entry) ✓. Script name was THREE-way split: 00 D36.1 `census-check.ts`, plan S-app `census-check.mjs`, plan R2 `census.mjs` → **unified to `scripts/census-check.mjs`** (the ruling's own name) in all three sites; repo-wide grep confirms ONE name remains. |

---

## Fixed directly

1. **00 D36.1**: `scripts/census-check.ts` → `scripts/census-check.mjs` (name unification with the ruling + the plan).
2. **00 D34.1**: the Shift+F10 sub-clause reworded from "the ripple-overwrite alternate is F10 within the documented family, no shift-chord" (self-contradictory — F10 is overwrite-edit's alternate) to the ruling's form: "ripple-overwrite has NO F-alternate: it is primary-chord-only (⇧⌥.), the F-block's four members are insert/overwrite/replace/append".
3. **IMPLEMENTATION-PLAN R2 (cold-executor)**: `scripts/census.mjs` → `scripts/census-check.mjs`.
4. **IMPLEMENTATION-PLAN S-ot row**: the W8 item's "the upstream half LANDED @ fdb771c (tick it)" → "the hole-DOCUMENTATION landed @ fdb771c (R9-c item 0, a PLAN-only commit; the filing half is done, do NOT tick the fix); the OT-side code hole is STILL OPEN" — the old text instructed a cold executor to tick a fix that never landed (fdb771c touches only `.agents/PLAN.md`).
5. **REFERENCE-REGISTER law header**: inserted the ruling's real **35.4** (the four doc-level rulings: SourceEditBar / SourceRangeBar / deliver RangeBand rows landed this round + the 3-vs-5-page, marker-v2, captions OPEN registrations); the register's previous "35.4" (the xcut K3/VLM + retirement tie — good content, wrong number) kept as an unnumbered "operating law" bullet.
6. **REFERENCE-REGISTER C39**: ADOPTED → **QUEUED** with owner S-spec 18 §4.3 + the verification note (the v1.2 chrome contract landed WITHOUT the trigger list); intake-state line updated 4→3 ADOPTED, 11→12 QUEUED.
7. **REFERENCE-REGISTER row 1**: "06 §5.9A-F" → "06 §5.9/§5.9B-F" (no §5.9A exists in 06; the sections are §5.9 + §5.9B-F).
8. **REFERENCE-REGISTER pin counts** (live rg-verified, correction notes inline): row 1 insertPlan **30 → 31**; row 9 CheatSheet **17 → 11** (the surface's other 5 cheat-sheet-button pins live in AppShell.test's describe).

## Flagged

1. **16:283's echo of the garbled D34.1 quote** (out of this agent's file scope — for the 16 verifier): "per D34.1 'the ripple-overwrite alternate is F10 within the documented family, no shift-chord'" now quotes a phrase that no longer exists in 00 (fixed there) and contradicts 16's own F-block (F10 = overwrite-edit). The sentence's resolution clause is correct ("the F-block stays a four-member family … ripple-overwrite's primary is the ⇧⌥. chord alone"); only the quoted fragment needs the same rewording.
2. **Census-report count errors propagate**: the xcut-shell-mocks census itself carries the two miscounts fixed above (30/17); anything else citing those figures (the mode-insert report's "30 planner pins") should re-read live. battery_r25's register checks must scrape live, not trust the report.
3. **Register rows 2/3/4 flip-state staleness (minor, next maintenance pass)**: the divergence-rule cells still read "flips LANDED when the v1.2 rows land" / "flips LANDED with the 16/18/09 rows" — 18 v1.7's rows landed this same round (row 2's condition fully met; rows 3/4 partial — 16/09 and the 15 §13.15 exportMaster.range pointer rows are still pending). The citation-form column (PROPOSAL→ADOPTED) already records the adoption; flip the cells at the next register pass.
4. **Pre-existing D26 wording now ambiguous (no fix — byte-untouched by law)**: 00:423's "the fork-retirement gate = D30's declared irreducible carrier set" refers to the APP's D30; with spec-law Decision 30 now existing, the Status-line namespace note is the only disambiguator. Leave as-is (D26-D29 are byte-frozen), but a future D26 natural-edit should write "the app's D30".
5. **The register's C-ledger location phrasing**: "the mocks' `.agents/SPEC-REVISION-CANDIDATES.md`" — the actual file is the SPEC repo's `.agents/SPEC-REVISION-CANDIDATES.md` (the ruling's own verbatim phrasing; the mocks read/write it via the shared repo). Residual ambiguity only; noted, not changed.
6. **15's census-region letter-vs-spirit**: the Status line and the §13.15 preamble each gained an appended ledger note (both explicitly assert the census unchanged) — technically "changes in the region", but house-law-conformant (2.5.1: the status header carries the revision ledger) and the census bytes are identical. Recorded for the record; no action.

## Verdict

**PASS with 8 direct fixes.** The amendment fleet's 00/15/register/plan landing is faithful to ARCH-R25 v2: all seven decision blocks match the ruling's clauses (two sub-clause wordings corrected), the census is byte-untouched in 15, the four r1-scheduled verb rows and P7/P11 are correctly landed, the register's 19 rows are faithful to the census with a complete C33-C58 intake, and the plan's six tracks + K2/K3 gates + cold-executor table are present and live-resolvable. The fixes were mechanical (one script name ×3 sites, one census pin count ×2, one citation form, one disposition overclaim, one wrong tick-instruction, one law-header renumber, one self-contradictory chord clause). Nothing semantic remains open in these four files beyond the flagged 16-side echo and the next-pass register flips. No commit made.
