# verify-05-16-18-09 — R25 amendment-fleet verification (fresh context, retry)

**Round:** R25. **Date:** 2026-09-09. **Scope:** 05-timeline.md, 16-keyboard-shortcuts.md, 18-ui-shell.md, 09-project-model.md vs ARCH-R25 D33/D34/D35.4/D32.5 + P10/P11. **Method:** git diff HEAD~1 per file + full current-state reads + cross-checks against `xcut-visual-grammar.md`, `xcut-shell-mocks.md`, 15-wire-protocol.md §13.15, 06-nle-ops.md, and the live engine tree (`nle-engine` @ working checkout).

## Verification table

| # | File | Check | Result |
|---|---|---|---|
| 1 | 05 §8A | 12 register rows, all 6 columns (`element/states/geometry/token refs/owning spec/reference impl`) | ✅ 12 rows × 6 cols (NF=8 incl. pipes) |
| 1 | 05 §8A | Token refs NAMES ONLY — zero hex/rgba/hsl in the §8A line range (599-636) | ✅ zero matches (grep exit 1); all 8 token families of 18 §9 referenced by name |
| 1 | 05 §8A | 4 standing laws (hit-zone≠band / z-order stack / preview==commit / refusal-no-geometry) | ✅ all 4, numbered |
| 2 | 05 §14.10 | P10: amber `ROLL_COLORS`/trim-handles row era-qualified | ✅ R15-era qualifier + green ruling (D33.2) + "unresolvable against source, never law"; status line + §8A restate it |
| 3 | 05 §16.5A | 4 new op-port rows: replace=dedicated+`joinItems :7411-7427` sever+refuse · append=NO engine op (`insertElements` batch) · ripple-overwrite=REFERENCE ONLY (`rippleTrimItem :2839`) · fit-to-fill=`calculateSpeed` (core/timeline-math.ts:133 — live-verified at the engine checkout) + insert+retime composite, [0.1,5] refuse-never-clamp | ✅ all four, verbatim-consistent with D31.4-.7/D31A; wave-1 note added |
| 4 | 05 | numbering intact — only §8A added | ✅ diff touches §0 GAP, §8A (new), §14.10, §16.5A, status line only; no renumbering |
| 5 | 16 §3.4A | 4 primary chords `,`/`.`/`⇧⌥.`/`E` with (r1-scheduled) markers; F9-F12 block; browser-hostage+preventDefault law; Shift+F10 NOT-available | ✅ all present; F-block 4 rows; Shift+F10 refused (18 §4.9 owns it) |
| 6 | 16 §3.4B | 4 slide rows (`, . ⇧, ⇧.`) + the trap note (XKB-6, 00:804 violation class) | ✅ present; deltas + the runtime-fps note |
| 7 | 16 §6.1 | four-meaning row 2 + rows #19/#20; whole-file sweep for ungated `,`/`.`/`E` claims | ✅ rows present; sweep clean — `E` has zero other claimants; §3.4/§3.6 slip-10/nudge-10 pair ungated on the rows themselves but explicitly resolved by row #19; Option+,/. always-slip noted in row 20; found 1 imprecision (row 20, fixed below) |
| 8 | 16 §3.2 | D34.4 boundary (3 sentences) + P11 R-key precision clause | ✅ both present (R-key paragraph, post-R25 :193) |
| 9 | 16 §3.4A JSONs vs 15 r1 rows | append placement `{type:'append'}` = 15's 6th PlacementStrategy ✅; batch composite (delete+move+insert, `type:'batch'` in 15's union :2971) ✅; insert `mode:'insert-edit'` param — **no such param/row exists in 15** (see flagged F1) |
| 9 | 16 :283 | stale garbled D34.1 quote ("the ripple-overwrite alternate is F10...") | ❌ was stale → **fixed** (see below) |
| 10 | 18 §9 | 8 tokens vs xcut-visual-grammar.md :109-116 VERBATIM (names/values/usage) | ✅ names + values byte-identical (`--badge-chip-{bg,border,text}` expanded to its 3 names, same tokens); usage column lightly reworded, consistent; A1-A8/B1-B7 element ids match |
| 11 | 18 | §4.3 SourceEditBar 7 modes (insert/overwrite/replace/append/ripple-overwrite/place-on-top/fit-to-fill — matches xcut-shell-mocks :21) + SourceRangeBar surface-state-not-model + §4.8 RangeBand (loop-seam/drag/ordering/clamp laws) + §5A cursor layering | ✅ all four landed in the right sections (175/176, 222, 328) |
| 12 | 18 §4.9 | Shift+F10 rows byte-untouched | ✅ no diff hunk intersects §4.9 (old lines 224-240); hunks end at :219 and resume at :325 |
| 13 | 09 §B1 | note only; `ElementJSON.linkedTo?: string` definition untouched | ✅ single-line insert; pairwise-persistence/groups-runtime/split-relink-both-halves matches D32.5 + D32.4; 06 §5.0's reciprocal pointer live-verified (:432) |

## Fixed directly (uncommitted)

1. **16:283** — the stale garbled D34.1 echo `"the ripple-overwrite alternate is F10 within the documented family, no shift-chord"` (contradicts D34.1 and the F-block itself: ripple-overwrite has NO F-alternate) → re-quoted to the ruling's actual text: `"ripple-overwrite has NO F-alternate: it is primary-chord-only (⇧⌥.), the F-block's four members are insert/overwrite/replace/append"`. The sentence's conclusion (four-member family; `⇧⌥.` alone) was already correct and stands.
2. **16:267 (`,` row)** — broken citation "15's r1 row, D31.1" (15 has no insert-edit verb row) → re-pointed to the real r1 home (06's PORT-SCHEDULED row, D31.1/12.3) + named 15 §13.15's InsertCommand-placement seam as the wave that settles the final wire shape.
3. **16:653 (row #20)** — over-broad pair resolution implied `Option+Shift+,` also routes to ripple-overwrite in source mode → precision clause added: the ripple-overwrite claimant is the `⇧⌥.` PERIOD sibling only; `Option+Shift+,` has no source-mode descriptor (§8.6's set is `,`, `.`, `Option+Shift+.`) and keeps the slip-10 alt in every context (mirrors row #19's form).
4. **Kept (verified, not mine):** the prior failed attempt's two uncommitted anchor fixes — 16:2051 (`§19:1496-1497` → `05 §19.1 :1537-1538 post-R25`; verified live: 05:1537-1538 holds exactly those comparison rows) and 18:328 (bare stale `05:972` → `05 §14.11` + the stale-anchor note; verified live: `use-timeline-slip-slide`'s initial-pointer-position law is 05 §14.11 :1009). Both correct; left in the working tree.

## Flagged

- **F1 (semantic, cross-file): 16 §3.4A's `,`/`.` wire shapes do not match 15's r1 verb decision.** 15 §13.15's r1 rows cover only replace/append/ripple-overwrite/fit-to-fill, and the ripple-overwrite row's r1 verb decision is that "the whole source-edit family (overwrite/replace/append/rippleOverwrite/fitToFill) shares this one seam" — an **extended `InsertCommand`** (replace/overwrite placement + the delta-law ripple flag). 16 instead illustrates `,` as `{type:'insert', params:{…, mode:'insert-edit'}}` and `.` as `{type:'overwrite', …}` — a `mode` param and an `overwrite` verb that exist in neither 15's union nor its r1 rows. The citation was fixable (done); the shape tension is a design decision — recommend the r1 param-alignment wave either adopts 16's explicit source-edit verbs or re-keys 16's two cells to the InsertCommand-placement seam. Both rows are (r1-scheduled)-marked and 15's landed union is explicitly untouched, so nothing live is wrong today.
- **F2 (observation, no action): 16 §3.4/§3.6's `Shift+,`/`Shift+.` rows** both carry the bare context "When clip selected" (no tool gate on the row face) — a pre-existing form now made safe by §6.1 row #19 (explicit resolution) and §3.4B's gated slide twins. Only worth a row-face context-gate touch if a future round polishes §3.4/§3.6 row text.

## Verdict

**PASS with 3 small fixes + 1 semantic flag.** 05, 18, and 09 are fully conformant to D33/D35.4/D32.5/P10 (structure, tokens-verbatim, era-qualification, byte-untouched §4.9, untouched `linkedTo`). 16 is conformant to D34/P11 in substance (chords, F-block, boundary, four-meaning law, slide rows, R-key precision) with the stale :283 D34.1 echo and two precision defects fixed directly; F1 (the `,`/`.` wire-shape vs 15's InsertCommand-seam r1 decision) is the one item needing a ruling at the r1 param-alignment wave. No commits made; all edits are uncommitted per protocol.
