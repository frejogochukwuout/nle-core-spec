# Scout — opencut-timeline (OT) — Fleet R25 ground-truth round

Auditor: module ground-truth scout (read-only; tree left clean; test artifacts regenerated by the live re-run were `git checkout`-restored). Date: 2026-09-09.

## OT update card (R25, @ fdb771c)

- **Repo:** /home/z/my-project/opencut-timeline (github.com/bearachprema/opencut-timeline + GitLab mirror). **HEAD:** `fdb771cecc5bfd9f2e407d99c1b0eb37e2a55daa` (`fdb771c`), fast-forwarded to origin/main; working tree CLEAN.
- **Code pin unchanged — machine-verified:** `git diff --stat c15a629..HEAD -- src/` is EMPTY. Every byte of engine/wire/UI code is identical to the R24 code pin `c15a629` (W11 close-out). The two new commits touch `.agents/PLAN.md` only.
- **Role confirmed:** canonical timeline repo — ops engine (`src/lib/timeline/**`), canonical React UI tree (`src/components/timeline/**`, 40 files), wire-dispatch surface (`headless/api.ts` + `hooks/use-wire-dispatch.ts`).
- **tsc:** `--noEmit` clean (re-verified after `bun install`).

## Commits since R24 pin ded43c4

Exactly TWO commits, both **docs-only** (`.agents/PLAN.md`, +46/−1 lines total; `git diff --name-only ded43c4..HEAD` → `.agents/PLAN.md` only):

| sha | date | what it did | src/? | (a) edit-mode | (b) wire seam | (c) UI tree | (d) seams/consumer absorption |
|---|---|---|---|---|---|---|---|
| `c37844f` | 09-07 23:42 | **R9 seam queue** — the consumer's wire-dispatch absorption round, 4 attributed PLAN entries (R9-a…R9-d) | no (PLAN.md +42) | no | no (queues verbs, changes none) | no | **YES — the whole commit**: consumer's D30 round filed back upstream |
| `fdb771c` | 09-08 00:03 | **R9-c amendment** — adds item (0) to R9-c: the W8 focused-playhead arrow guard's `!shiftKey` hole (⇧Arrow double-applies = 5s jump + 1 frame; the consumer's 9-G1 review caught the same hole in its twin guard, made both variants surface-owned) | no (PLAN.md +5/−1) | no | no | no (names a UI-tree defect for upstream-back) | YES (app review → upstream queue) |

Neither commit touches the timeline edit-mode surface, the wire-dispatch seam code, WIRE_COMMAND_TYPES, the recorder, the coverage gate, or the canonical UI tree. The R9 content in detail:

- **R9-a** — consumer endorsement of the spec-queue candidates: the app's own coverage gate runs the SAME 6-verb exception registry as M49C; `timeline.insertBatch` + batch keyframe wire verbs would close two exceptions from the consumer side too (the app's pool multi-insert rides `applyBatch → N undo entries` — the documented divergence the consumer now also lives). Endorse filing both to nle-core-spec.
- **R9-b** — keyframe AUTHORING has no UI surface at ANY layer (upstream or consumer): the volume line / lanes only EDIT existing keyframes; the gate stays green only because the singular verbs are exceptions. Proposal: an upstream add-keyframe affordance on the lanes, or a documented consumer-territory ruling.
- **R9-c** — upstream-back port-locals: (0) the W8 `!shiftKey` guard hole; (1) `EMPTY_CONTEXT_ITEMS` stable empty-items fallback (consumer commit bf9be13, the CI-red nested-update cascade class); (2) functional `setPos(null)` in the closed context-menu branch; (3) the jsdom DragEvent trap (no-DragEvent-constructor environments lose MouseEvent coords on drag events — an engine-side guard or docs note would spare future consumers the NaN-x probe session).
- **R9-d** — export `WIRE_UI_EXCEPTIONS` as a first-class shared constant (mirroring DECISIONS #25 r2): the consumer's gate reads `wireCoverage`/`wireLog` through its app-flavored `__VIEW_TEST__`; a shared constant keeps its test-local registry from drifting (tsc-lockstep shape).

**Verdict on movement:** the consumer's needs ARE flowing upstream — but entirely as a QUEUE. Zero consumer feedback has been absorbed into code yet.

## Test counts

**Re-run LIVE this scout** (dev server `bun run dev` :3001; `node scripts/run-timeline-tests.mjs`; 214.8s):

- **536/536 passed, 0 failed — 59 milestone entries** (matches `download/timeline-test-report.json`, the count authority, recounted programmatically: 536 entries / 59 milestones).
- Split: **386 in-page tests / 41 entries** (incl. M49C gate) + **150 real-mouse tests / 18 entries** (M17 + 14 `*R` suites + M49T/H/R/G). Same split R24 recorded.
- Coverage gate line, verbatim: `[runner] coverage gate: PASS — fired 24 distinct command types across the UI phases; 6 documented exceptions; no gaps`.
- tsc clean. Tree restored clean after the re-run (3 regenerated `download/*` artifacts checked out).

## Edit-mode census (the 10 DaVinci edit modes in OT)

| # | Mode | Status in OT | Evidence (file + export/line) | Wire verb? |
|---|---|---|---|---|
| 1 | Roll trim | **ABSENT** | zero grep matches (`rollTrim|rolling edit`); trim is single-edge (`trimElements` side+deltaTime, ops/timeline-core.ts:1146) or group-resize with neighbor-clamp bounds (ops/group-resize.ts:152 `computeGroupResize`) — never the adjacent-clip joint move | — |
| 2 | Ripple trim | **ABSENT as trim; ripple-DELETE exists; ripple MODE = view-state flag only** | `rippleDeleteElements` ops/timeline-core.ts:1242 + the diff engine (ripple/index.ts:121 `computeRippleAdjustments`, :40 `applyRippleAdjustments`); `rippleMode` useState TimelineView.tsx:237 + toolbar toggle TimelineToolbar.tsx:321 (`data-test="toolbar-ripple-mode"`) routes DELETE (use-timeline-actions.ts:431-435 → `timeline.rippleDelete`) + the split-right seek clause (:375-383) — NOT trim; view-state never routes (DECISIONS #25 r1) | `timeline.rippleDelete` (delete, not trim) |
| 3 | Slip | **ABSENT** | zero grep matches in src | — |
| 4 | Slide | **ABSENT** | zero grep matches in src | — |
| 5 | Insert edit | **PARTIAL — verb exists, splice semantics do NOT** | wire `timeline.insert` (headless/api.ts:558) → `insertElements` (ops/timeline-core.ts:872) / `insertElement` (:1000). Placement: 5 strategies (placement/index.ts:43-54), overlap policy **REJECTED, NOT SHIFTED** (placement/index.ts:10-12) — falls to another track or a new track; **NO split-at-playhead when inserting mid-clip, NO downstream push**; main-track zero-anchor clamp (:175-203). Wire exposes only `firstAvailable|explicit` (api.ts:581-590). Library insert = applyBatch of N inserts at playhead (src/app/view/page.tsx:518-552) → N undo entries (the `insertBatch` divergence, DECISIONS #25 r2) | `timeline.insert` (place-at-time semantics) |
| 6 | Overwrite edit | **ABSENT** | no overwrite placement strategy, no verb, no clobber path (only "overwrite" hits = the params open-map law docs) | — |
| 7 | Replace edit | **ABSENT (deliberate classic-parity no-op)** | controllers/drag-drop-controller.ts:354-359 — a target-ELEMENT drop hit returns ("media replace is not implemented"), mirroring classic's not-implemented return | — |
| 8 | Append-at-end | **ABSENT as a mode** | no append computation/verb anywhere; composable manually via `timeline.insert` at an explicit end-of-track `startTimeTicks` | — (`timeline.insert` composable) |
| 9 | Ripple overwrite | **ABSENT** | zero grep matches | — |
| 10 | Fit-to-fill | **ABSENT** | ops/retime.ts (73 lines): rate clamp [0.01, 5] (:15-20) + linear source↔clip mapping (:22-49) — **no speed-to-fit-duration computation**; retime is a field authored via updateElements (OP-COVERAGE row) | — (retime field via `timeline.updateElements`) |

**Score: ~1.5 / 10 DaVinci modes** (insert has a verb with different semantics; rippleDelete is adjacent to ripple trim). Roll / slip / slide / overwrite / replace / append / ripple-overwrite / fit-to-fill have **no OT surface at any layer**. OT's own parity audit (`.agents/OP-COVERAGE.md`) frames completeness vs **opencut-classic** (11/11 classic verbs ported — insert, move, trim, split×3, delete, ripple-delete, duplicate, retime-math, snapping — each with wire verb + UI + pins) and explicitly documents its scope cuts; it does NOT measure against DaVinci's 10-mode trim/insert family. That scope-frame gap (classic-parity ≠ NLE-edit-mode parity) is a fresh finding for the spec round.

## Wire census (30 verbs = 24 routed + 6 exceptions — verbatim names)

Source: `WIRE_COMMAND_TYPES`, headless/api.ts:182-213 (30 entries, tsc-lockstep type-asserts :215-230; count pinned at 30 by milestones-w11.ts:207-208). Exception registry: scripts/run-timeline-tests.mjs:283-300 (+ DECISIONS #25 r2).

**24 routed (UI-fired, machine-checked by M49C):**
`timeline.delete`, `timeline.duplicate`, `timeline.insert`, `timeline.move`, `timeline.moveBookmark`, `timeline.pause`, `timeline.play`, `timeline.redo`, `timeline.removeBookmark`, `timeline.rippleDelete`, `timeline.seek`, `timeline.setLoopRegion`, `timeline.setPlaybackRate`, `timeline.split`, `timeline.toggleBookmark`, `timeline.undo`, `timeline.updateElements`, `track.add`, `track.remove`, `track.setAllLocked`, `track.setAllMuted`, `track.toggleLock`, `track.toggleMute`, `track.toggleVisibility`.

**6 documented exceptions (each with its law):**
`timeline.selectElements` (selection is VIEW state; remote-consumer verb) · `timeline.upsertKeyframe` / `timeline.removeKeyframe` / `timeline.retimeKeyframe` (singular verbs — the UI authors keyframes through the batch gesture paths, one-history-entry law; **spec-queue candidate: batch keyframe wire verbs**) · `timeline.advancePlayhead` (ticker is rAF-local) · `timeline.trim` (uniform-delta harness verb; the UI's resize commits as updateElements patches).

**Not exported as a constant:** the exception registry lives only in the runner script — `WIRE_UI_EXCEPTIONS` has ZERO matches in src/ (R9-d queues the shared-constant export).

## Seam evidence

- **W11 seam intact and re-proven live:** `useWireDispatch` (src/components/timeline/hooks/use-wire-dispatch.ts:50) wraps the view's core in `HeadlessTimelineApi(fps, {core, onApply})` (attach, one engine); recorder ON the api; `applyBatch` data.results; wire-error chip; the M49C gate re-passed 24/24+6 this round. Nothing in the seam changed since R24 — code identity machine-verified.
- **Consumer feedback flowing upstream = REAL but QUEUE-ONLY:** R9 (PLAN.md, filed 2026-09-08) records the consumer's D30 absorption round — nle-test-app re-pinned its lock-copy to `c15a629`, re-converged its timeline-port to the exact W11 component tree (35/40 files exact), and **ported the M49C coverage gate into its own suite** (`src/wire-coverage.test.tsx`, 24/24 through the real mounted UI, jsdom-flavored, same 6-verb registry). OT's PLAN then queues four attributed entries (R9-a…d) including exact upstream-back hardening hunks (`EMPTY_CONTEXT_ITEMS`, `setPos(null)`, the jsdom DragEvent trap, the W8 `!shiftKey` hole). The absorption direction is now bidirectional — the consumer validates the seam AND reports defects back — but **zero of it is in code yet** (all four items unchecked; no worklog entry; no DECISIONS after #25 — DECISIONS.md still ends at #25).
- **DECISIONS #25** remains the operative ruling set (routing law, exception registry, gate mechanism, JKL divergence, surface contracts, NOOP classification, engine widenings). OP-COVERAGE.md (session 15) is the classic-parity matrix. SEAMS.md carries the W11 wire-dispatch seam map + the consumer note ("the same fault-line validation this repo's UI runs is portable to the consumer dispatch surfaces" — now realized in the app's ported gate).

## Queue state (OT's own, from PLAN/HANDOFF)

Open, unchecked (R9, 2026-09-08):
1. **R9-a** — endorse filing `timeline.insertBatch` + batch keyframe wire verbs to nle-core-spec at the next queue visit.
2. **R9-b** — keyframe AUTHORING surface gap: no layer creates a keyframe from user input (upstream affordance on the lanes, or a documented consumer-territory ruling).
3. **R9-c** — upstream-back: (0) W8 `!shiftKey` playhead-guard hole; (1) `EMPTY_CONTEXT_ITEMS`; (2) closed-branch `setPos(null)`; (3) jsdom no-DragEvent guard/docs.
4. **R9-d** — export `WIRE_UI_EXCEPTIONS` as a first-class seam constant.

Standing (HANDOFF "optional next rounds"): consumer re-landing (session-12's lost nle-ui W-B / nle-engine W-C / nle-test-app W-D waves; the parallel stream is active on those repos — check first), the P3 polish backlog, W8-f graph-editor popover (stretch).

**Consumer pins (drift check):** nle-engine submodule `vendor/opencut-timeline` recorded pin = **c15a629** (re-pinned a4e971d → c15a629 in engine commit 5036387 "Fleet coherence (R9 W-A)", 458/458 green at the new pin; submodule not initialized in this sandbox). nle-test-app `vendor/nle-timeline/UPSTREAM.lock.json` = upstreamHead **c15a629**, mirrored 2026-09-08, sole local change = `testing/` removed, "engine is EXACT upstream" (app HEAD 64fb0ab). nle-ui has no direct OT vendor (consumes engine types; HEAD 3026099). **Verdict: ZERO drift — both pins equal the code pin, and OT's delta since is docs-only, so every consumer is code-current without action.**

## Sealed-vs-in-flight verdict

- **SEALED, re-verified live:** 536/536 (386 in-page + 150 real-mouse), tsc clean, M49C gate 24/24 routed + 6 exceptions + no gaps, tree clean at `fdb771c`; code byte-identical to the R24 code pin `c15a629`. The user's "mostly considered sealed" is **accurate for the W11 surface**: nothing code-moved since R24.
- **IN-FLIGHT (paper only):** the entire R9 seam queue — the consumer-absorption round (spec-queue endorsement, keyframe-authoring gap, 4 upstream-back hardening hunks, the WIRE_UI_EXCEPTIONS shared constant) sits unchecked in PLAN; the spec-queue candidates (`timeline.insertBatch`, batch keyframe verbs) are still un-filed; consumer re-landing waves still un-landed.
- **Fresh exposure for the spec round:** the DaVinci-mode census above — 8 of 10 edit modes absent, insert's verb carries place-not-splice semantics, ripple exists as delete + a view-state mode flag, fit-to-fill has no speed-to-fit math. OT is a classic-faithful distillation by design (documented in OP-COVERAGE); the 10-mode NLE trim/insert family is a scope question for nle-core-spec's 06-nle-ops stream, not an OT regression.
- **Next actions for the fleet round:** (1) file the R9-a spec-queue candidates; (2) rule R9-b (keyframe authoring territory); (3) absorb the R9-c hardening hunks upstream (small, consumer-proven); (4) export WIRE_UI_EXCEPTIONS (R9-d, tsc-lockstep shape); (5) decide whether the 10-mode edit family enters any module's scope.
