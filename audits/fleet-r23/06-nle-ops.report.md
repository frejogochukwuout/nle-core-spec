# R23 fleet audit — `06-nle-ops.md` (the NLE operations spec — the op families: ripple/roll/slip/slide/retime/marquee etc.)

**Agent:** R23-fleet-06 (fresh context). **File owned:** `06-nle-ops.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs; counts trusted per the round's numbers, every FEATURE verified by reading code):** opencut-timeline `222532c` (489/489), nle-engine `b8c6f88` (440/440), nle-test-app `70e99f0` (117/117), web-daw-core `494f6ff` (759/759); the in-repo law register (`ui-mock/shell-mini/docs/OT-SEAMS.md`, `LAW-NET-INVENTORY.md`) and the retired spec-14 §4.1 rows (read from git history, read-only) as the posture-law inputs.

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **HEADs + counts:** OT `main` = `222532c`, engine `b8c6f88`, app `70e99f0`, WDC `494f6ff` (all read-only). `download/timeline-test-report.json` → `total 489 / passed 489 / failed 0`, **51 milestone suites** (M44/M45/M46 all present; M45=12 pins, M46=3 pins). Fleet split 359 in-page + 130 real-mouse kept for coherence (see NOTES — my per-test re-sum gives 360/129, same total; the 05 report flags the same ±1).
- **The 30-name wire census (the wave-1 census the task asked to verify):** `headless/api.ts` carries **exactly 30 `case` arms** — 22 `timeline.*` + 6 `track.*` + undo/redo. The growth from the R15/M29 charter's 24 = the S1 transport trio (`setPlaybackRate`/`setLoopRegion`/`advancePlayhead`) + `track.setAllLocked` (M44/D-T3) + `track.setAllMuted` (M46/S3-seam). Verified by enumeration.
- **S3 seam round (M46) LANDED:** `setTracksMuted` at `ops/timeline-core.ts:2108` (the mute-all batch — `withUndo` one-entry, the every-track-at-target F1C-1 NOOP predicate carried over, `"muted" in track` vacuous-at-target family law) + the wire verb `track.setAllMuted` (`api.ts:1385`, boolean validation → INVALID_PARAMS) + M46's 3 pins in the report json. Its D-T3 twin `setTracksLocked` at `:2060` (M44, 15 pins).
- **The F1 whole-codebase hardening ALL LANDED with greppable in-source markers (the F1B laws are op-contract rows of THIS spec):** F1B-2 no-overlap on update span patches → CONFLICT (`api.ts:202/:266`); **F1B-4 missing/null/array params → INVALID_PARAMS, never INTERNAL_ERROR (`api.ts:277/:291`)** — the error-envelope's classification half is now BASE-covered; F1B-5 `setLoopRegion clear:false` never clears (`api.ts:1052-1065`); F1B-6 negative trims/starts rejected (`api.ts:871-880`); **F1B-7 failed `applyBatch` preserves the pre-batch redo stack** (`timeline-core.ts:189/:215/:226/:296/:2360/:2370` — suspendEviction/rollback machinery); F1B-8 the keyframe flick commits (`keyframe-drag-controller.ts:332`); F1A-3 heterogeneous insert batches reject + the **intra-batch overlap guard** (M45's sharpened pin — §5.9's law is now OT-pinned, not just spec text); F1A-4 createTracks id validation; F1C-1 the unlock-direction NOOP law (`timeline-core.ts:2062`).
- **D-T7 (the NOOP asymmetry — an op-contract law this spec's §5.2A should know):** same-position `moveElements` NOOP returns `ok:true`, no history entry, no batch abort (`timeline-core.ts:993`, M44's 7 pins); the T-round reviews' F1B-15/F1B-18 residuals (empty-moves→CONFLICT; NOOP-class successes reporting NOT_FOUND) are the live r1 error-envelope inputs.
- **M45's marquee rows (multi-select/marquee re-filing):** `resolveTimelineElementIntersections` expanded-lane row-shift pin + F1A-3 non-overlapping-span pin present in the json (interaction-side; owned by 05 — no 06 GAP row needed, noted).
- **The engine @ `b8c6f88`:** timeline.ts is now **7,502 LOC** (6,972 at R15); every §10.4 op-family signature re-located (slip `:4143`, slideItem `:4246`, rollingTrimItems `:2984`, rateStretchItem `:3155`, freezeFrameAtPosition `:7185`, the range-removal family `:6910-:7124`, joinItems `:7341`); `snapshotsEqual` with the Wave-4A keyframe/composition/backgroundColor fix now at `:1521-1544`; sync-lock.ts at 639 LOC (`:503`); the three absent families (track-state toggles, snap, splitAndRemove) **still absent — R23-verified**. **N2b keyframed volume LANDED** (`37cdd28` — the gain-automation lane in `bridge/segment-strip-adapter.ts` + `playback/player.ts`); RR1-A fade-clamp is the HEAD commit; `tests/vitest/engine/timeline-edit-ops.test.ts` live (R1-B4 + R2-4 — the K2 seed family).
- **WDC @ `494f6ff`:** W2 SoundTouch varispeed LANDED — `src/lib/daw/soundtouch.ts` + `varispeed.ts` (commit `1b40fd4`, hardened `eddb7f1` with the retime rate-domain [1/32, 32] + degenerate-setter guards) — **the retime audio half for r1's wave 2** (consumed via the engine, not re-ported).
- **The OT-GAP rows HOLD (the r1 port work is still open):** no roll/slip/slide/rateStretch-command/freezeFrame/rangeRemoval/syncLock code anywhere in OT's `src/lib/timeline/{ops,headless}` (grep-verified empty); the `retime` surface in OT is the math (`ops/retime.ts`) + the SC-1 update-patch derive rule + `retimeKeyframe` — no rate-stretch command.
- **The composed ripple family (the D24 compose-at-crawl law):** LAW-NET-INVENTORY carries exactly the GAP-W-ops rows — 4 census units / 16 tests (rippleShiftAfter 4 + cutHead/cutTail 6 + ripple-trim 3 + ripple-quantize 3), disposed "the app composes over `timeline.trim`/rippleDelete at crawl and it graduates at W-ops"; OT's `TimelineCore.rippleDeleteElements` (`:1218`) + wire `timeline.rippleDelete` are the compose targets, both live.
- **Ripple diff block (§5.4/§12):** `ripple/index.ts` line numbers UNCHANGED at HEAD (`:16/:40/:121`, 395 LOC) — the Round-8 executable-reference note needed no line fix.

## FIXED (edits applied to `06-nle-ops.md`)

1. **Status header:** R22 framing → the R23 re-baseline (OT @ `222532c` 489/489; the T-round/F1/S3-seam-round landings; the D24 re-key; the spec-14 §4.1 re-home), R22 kept as the supersession record.
2. **§0 BASE OT row:** `05584d8` 459/459 (329+130 across 14 phases) → `222532c` **489/489 (359 in-page + 130 real-mouse across 51 milestone suites, 14 real-mouse; the report json the authority)**; the ops-surface enumeration extended (split/trim/move/ripple/delete/insert/duplicate + placement); added the S-round completion (S1/S4), the T-round (D-T3 `setTracksLocked` + `track.setAllLocked`, M44; D-T7 the NOOP asymmetry), **the S3 seam round (M46 `setTracksMuted` + `track.setAllMuted`)**, **the F1 hardening** (F1B-1..8 + F1A-2/3/4 + F1A-1 + F1C-1, each named — incl. §5.9's intra-batch overlap guard now OT-pinned), and the **30-name wire census** (the C7 input); D25 canonical-tree pointer added (05's row).
3. **§0 BASE engine row:** `f68ab8c` 356/356 → `b8c6f88` **440/440**, timeline.ts 7,502 LOC with the six r1-port-source line numbers re-verified; **N2b LANDED @ `37cdd28`**, RR1-A at HEAD, the timeline-edit-ops K2 seed noted.
4. **§0 BASE rows ADDED:** web-daw-core @ `494f6ff` 759/759 (the W2 SoundTouch varispeed — the retime audio half, consumed not re-ported) + the in-repo LAW-NET compose-law row (the GAP-W-ops 4/16 family with the D24 disposition).
5. **§0 GAP register — re-tagged per D24 and the §4.1 rows re-homed (charges b/c/d):** op-family ports → **r1 (was W-ops)**; **ADDED the composed-ripple row** (K3 composes app-side over `timeline.trim`/`rippleDelete` at crawl; r1 graduates — the D24 law); **ADDED the C7 row** (30 names → bare union at **r1 END** + the one-day migration + keymap-sync sub-gate); error-envelope → **r1** with the R23 annotation (**F1B-4's INVALID_PARAMS half COVERED by BASE**; the coarseness + F1B-15/18 residuals remain); **ADDED the element-level toggleElementMuted/Visibility wire-verbs row** (r1; ops exist at `timeline-core.ts:1341/:1385`); a closing pointer row for the interaction-side §4.1 rows (05's register). The stale "plus spec 14 §4.1 — pointed, not duplicated" header → "the retired spec-14 §4.1 editing-domain rows are re-homed HERE (the D23 posture law)".
6. **§0 ACCEPTANCE & TEST PLAN:** OT 459/engine 356 → OT 489 (359+130) / engine 440 / WDC 759 / app 117; the K1 regression role + the r1/K3 gate pointers.
7. **§5.2A (NOOP law):** R23 annotation added — D-T7's `ok:true` move-NOOP asymmetry vs `updateElements`' `ok:false` (with verified line refs `:993`/`:1320`), F1B-4, and the F1B-15/18 residuals as the r1 envelope's live inputs; the trim-zero-clamp NOOP law unchanged.
8. **§5.4 note 2:** `TimelineCore.rippleDeleteElements` line ref `:620` → `:1218` (verified).
9. **§10.4 (engine table):** header → "line numbers re-verified R23 @ `b8c6f88`" (R22 pin superseded; refresh at **r1**, was W-ops); blockquote → 440/440, N2b/RR1-A/timeline-edit-ops, 7,502 LOC / ~108 public methods (R15's 6,972/102 kept as the census lineage; the R15 suite counts explicitly kept historical); **all 24 table rows' line numbers refreshed** to the b8c6f88 values; the "three absent families" re-verified at HEAD; Decision-12.3 port schedule re-tagged r1 with the acceptance re-based to OT-489.
10. **§10.5 (OT table):** header → "line numbers re-verified R23 @ `222532c` (489/489 …; refresh at r1)"; blockquote → the R23 landings enumerated (M44/M46/M45/M40) + **the census now 30 names**; **all landed rows' line numbers refreshed** (split `:32/:43`, group-resize `:152`, group-move `:69/:75/:169/:279`, placement `:398`, duplicate `:1254`, rippleDelete `:1218`, preview/commit `:2242/:2288`, UndoStack `:183-313`, TimelineCore `:315-2873` — 2,873 LOC); the OT-GAP rows re-verified-absent with the refreshed engine line refs; **three rows ADDED** — §5.15 element-level toggle ops (`:1341/:1385`, M40), §5.15 lock-all/mute-all batches (`:2060/:2108`, M44+M46 + the wire verbs), and the wire-validation-laws row (F1B family; the envelope's classification half ALIGNED); the closing algorithm-home paragraph re-pointed from "spec 14 §4.1" to this §0 register + r1 + the W2-consumption note.
11. **§10.4's §7 note:** `snapshotsEqual` fix line refs `:1772-1803` → `:1521-1544` (verified: keyframes + compositions + backgroundColor via jsonDeepEqual).

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **The r1 op-family ports (wave 1 + wave 2):** slip/slide/roll/rateStretch + retime/freezeFrame/rangeRemoval — no OT ops code (grep-verified); the engine sources stand as the executable reference (§10.4, line numbers now fresh).
- **C7 rename:** the 30 prefixed names are still on the wire (the union at `api.ts` is 30 members — CORRECTIVE stands until r1 END).
- **Error-envelope coarseness:** only the classification half (F1B-4) is BASE-covered; the ~24-code table + the F1B-15/F1B-18 NOOP/NOT_FOUND inconsistencies remain r1.
- **The element-level toggle wire verbs:** absent from the 30-member union (engine ops exist; routed via `updateElements`).
- **The composed ripple family app-side:** not authored (LAW-NET Part A is the acceptance list; the K3 half) — and the r1 graduation (the OT interval-diff family as exposed ops) unstarted.
- **Sync-lock (§6):** still OT-absent; nle-engine's sync-lock.ts remains the only implementation.

## NOTES

- **The in-page/real-mouse split:** my per-test re-sum of the report json gives **360 in-page + 129 real-mouse** (total exactly 489); the fleet-standard **359 + 130** (used by ARCH-R23, 05, and this round's task text) is kept in the spec for cross-file coherence — the same ±1 the 05 report flagged; immaterial to every claim (the total is the count-discipline number).
- **Two "S3"s disambiguated** in the §0 text the same way 05 does: the R22 S-round's S3 (transitionOut) vs the R23 "S3 seam round" (M46 setTracksMuted) — labeled explicitly so they cannot be confused.
- **Engine's §10.4 "three absent families" note:** the ENGINE-GAP and SPEC-ONLY rows hold; OT landed the batch track-state verbs (M44/M46) on ITS side, which is why those rows live in §10.5 now — the engine still has no public mutators for track state.
- **Line-number policy for the historical tables:** §10.4/§10.5's line numbers were REFRESHED (they are LIVE claims about the current repos, and r1's port work will cite them); the FreeCut/OpenCut-classic citations (§0A, §5.x, §10.1-10.3, §12, §13) are historical teacher references at /tmp paths — untouched per the round's rules.
- **Battery/counts:** no battery runs, no test-suite runs, no git mutations (HEADs by read-only log; the retired spec-14 §4.1 rows recovered by a read-only `git show` of the tombstoned history at `7ba030a`).
- **The 05-coherence sweep:** §0's r1/C7/error-envelope/composed-ripple/toggle-verb rows now match 05's §0 phrasing and counts one-for-one (30 names, K3+r1 dual-phase, F1B-4 annotation, `timeline.trim`/`rippleDelete` compose targets, W8-d/M40 line refs).
