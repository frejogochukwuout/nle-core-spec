# R23 fleet audit — `20-audio-core.md` (the audio-core spec: WDC + the engine bridge + the app's audio host)

**Agent:** R23-fleet-20 (fresh context). **File owned:** `20-audio-core.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs, no fetch/clone, no git mutations, no test runs):** web-daw-core `494f6ff` (759/759 per the round's live-verified number; features verified by reading code + `git log`/`git show`), nle-engine `b8c6f88` (440/440), nle-test-app `70e99f0` (117/117). Consumer pins read live: engine vendors WDC @ `f446512` + OT @ `a4e971d`; app vendors WDC @ `f446512` + engine @ `4ef0147` + nle-ui @ `85dcf57`. In-repo artifacts: `ui-mock/shell-mini/docs/CORE-SEAMS.md` (S16/S17), `LAW-NET-INVENTORY.md` (the 2 GAP-C3 waveform families), the retired `14-implementation-phases.md` stub + its git-history v2.1 @ `7ba030a` (the §4.3 audio rows), `IMPLEMENTATION-PLAN.md`, `audits/ARCH-R23-plan-and-bridge.md`.

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **The three-layer S/G/E model + all §3/§4 contracts still hold at HEAD:** `docs/track-model.md`, `src/lib/daw/channel-strip.ts` (`scheduleAudioClip(clipId, buffer, atTime, offsetSec, durationSec)` signature re-verified verbatim), the engine-side bridge family at `src/lib/nle/bridge/` (M1.6 relocation stands — WDC remains a PURE core, zero submodules, zero NLE types under `src/lib/daw/**`). §5 laws 1/2/4/5/6/7 unchanged in substance.
- **The R22-era BASE pins were EXACTLY as the old §0 said** (WDC `fe05d85` 740/740, engine `f68ab8c` 356/356, app `e662759` 83/83 — all confirmed via `git log`), i.e. the stale claims were staleness, not wrongness: every one has since been superseded by this round's fleet movement.
- **W2 varispeed LANDED, exactly as the plan's r2 row states:** `src/lib/daw/soundtouch.ts` (typed LGPL WSOLA port — the header preserves the copyright + LGPL notice + "NUMERIC LAWS unchanged (float-op order preserved)"; SimpleFilter driver deliberately NOT ported) + `src/lib/daw/varispeed.ts` (`retimeChannels`, F1 flush law EXACT `round(inFrames/rate)`, `RETIME_RATE_MIN = 1/32` / `RETIME_RATE_MAX = 32`, identity degradation for out-of-domain/non-finite/≤0/>2ch, mono→L=R, the general worklet + `VARI_SPEED_PROCESSOR_NAME` blob-URL loader). W2 pins W2a-W2r present in `src/test/varispeed.test.ts` (16) + `varispeed-worklet.test.ts` (incl. the CR5 bit-exact parity contract: class-level bit-exact, prefix bit-exact to the flush-trim region, tail at the documented ~25 dB class). Consumers landed the same round: engine `df6eadf` (the round's closing re-pin of WDC → `de09c93`; the engine W2 feature commit is `0d61964` — the flattener maintainPitch thread + the adapter's pre-retime branch + the 3-site schedule law + the byte-capped recency-LRU span cache + 10 pins W2a-j, with the CR rounds `3de7ed4`/`8fd498d` and the A2 out-of-domain→legacy-pair fix `b7c7c2e` adding W2k), app `0ac3f15` (the app W2 feature commits are `9e92f1c` (el.speed → retime, the #24 offset law live) + `10bbbe8` (the straddling call-spy pin) + `ce57b14` (the speed EDIT path) — NOTE: the task briefing said "app `0ac3c15`"; the verified hash is `0ac3f15` (WDC's own HANDOFF agrees). The spec cites the verified hash.
- **The degenerate-setter + W3C-order + sampleRate laws** verified in code (`eddb7f1`, `96f88cb`): the guards on Stretch/RateTransposer/SoundTouch, `createBuffer(channels, frames, sr)` order with the W2o real-ctx round-trip pin, CR2 setSampleRate honored (W2p).
- **The meter-tap OFFLINE guard (CR-A3):** `src/lib/daw/meter-tap.ts` guards on `typeof ctx.startRendering === 'function'` — offline handles no-op meters (the old docblock claim was false; the W1k CR pin family covers it).
- **The drift-gate now FAILS on drift:** `scripts/sync-from-upstream.mjs` — `--check` is no-writes + "exit 1 on any" drift/import violation (commit `de09c93` CR-P1: "the repo's only drift gate was exit-0-blind"). The old §10 facet row ("clean; new seams FAIL LOUDLY") was true but under-stated — now stated fully.
- **The barrel's consumption reality (D24e law):** `src/index.ts` header — "BOTH current consumers deep-import the vendored tree through their OWN `@/lib/daw/*` alias … ZERO importers today … the FUTURE web-daw-migration surface (UPSTREAMING Phase C)"; `hasMasterChain` dropped (grep-verified dead export, `494f6ff`). The consumed exports ride deep-import paths (retimeChannels + RETIME_RATE_MIN/MAX in the engine's adapter; createEngineMeterTaps in realtime-engine.ts; the harness in the engine's vitest).
- **package.json ships worklets + the LGPL obligations:** `files: [src, scripts, extraction-manifest.json, UPSTREAM.lock.json, COPYING.LESSER, THIRD-PARTY-NOTICES.md, public]` + peerDeps (vitest, web-audio-api, both optional) + engines node ≥24.
- **N2b keyframed volume LANDED @ `37cdd28`** (the engine's S2 seam round): the flattener folds `animations["volume"]` → `gainAutomation` source-frame breakpoints (dB→linear, [−60,+20] clamp, NaN→unity, lane-replaces-clip-static, merge-blocking both directions, fold hygiene), the adapter's dedicated series gain node (exp-ramp exact for linear-in-dB + geometric entry, rate-aware times, kept ramp targets, snapshot copy, 3 teardown sites, linear fallback), `scheduleSegments` threads the field. **18 bridge pins + 2 REAL offline-render sample-level parity pins** verified in `tests/vitest/nle-bridge.test.ts` (the two parity pins at maxDiff < 0.002: the dB-lerp reference law + the trimmed mid-entry/source-frame law).
- **The engine's W2 consumption + W2k:** out-of-domain retime falls back to the LEGACY pitch-affected pair, identity results never reach the span cache (no aliased views of the live buffer) — pinned W2k @ `b7c7c2e`.
- **The app's W3 JKL audio half @ `3f351bc`** (all six laws pinned in `src/audioService.test.tsx` + `GluedShell.test.tsx`): W3-1 composed varispeedRate = segRate × tr; W3-2 the #24 offset law stays ELEMENT-domain (into × segRate, NOT × tr); W3-3 exactly-ONE reschedule (the rate edge runs BEFORE the jump heuristic; `lastScheduledTr` is the single idempotence key gating both entry paths — the store echo S3-C4 + the core-notify edge); W3-4 silent-reverse J; W3-5 timeline-authored fades ÷ tr clamped to the wall span; + the routed setShuttle(2) round-trip through the mirror. `audioService.ts` (`varispeedRate: segRate * tr`, `maintainPitch` threading, `lastScheduledTr`) + `mediaTone.ts` (the deterministic FNV-hash tone law through the engine's `renderFullVirtualAudio`) both verified.
- **The app's `public/` is EMPTY** — the spec-14 §4.3 worklet-asset-serving row is still open (K3), now trivially so (WDC ships `public/` in files; the varispeed worklet needs no asset at all — blob-URL).
- **CORE-SEAMS S16/S17 + LAW-NET's 2 GAP-C3 families verified** as the K3 audio-pin acceptance surface (waveform bar grammar + envelope laws; the mute one-owner law).
- **WDC's live queue (HANDOFF at HEAD)** matches the plan's S-wdc row exactly: (1) the 4 deferred upstream test ports (listed as engine / engine-metronome-automation / engine-param-channel / dsp-aux-sends / wam-effect bodies — nle-engine-side ports), (2) the mixer surface (design round first), (3) the async pre-retime item. The JKL follow-up it also queued app-side is CLOSED by W3.

## FIXED (edits applied to `20-audio-core.md`)

1. **Status line:** v-next → v-next-2 (Round 23 — the fleet audit framing: the R23 re-pins BASE, W2 + N2b + W3 LANDED, the GAP register re-based per D24 + the spec-14 §4.3 rows re-homed, the drift-gate law); the R22 v-next lineage kept as an explicit supersession record.
2. **The R15 re-baseline line:** the engine pin-supersession chain extended (`f68ab8c` 356/356 → `b8c6f88` 440/440) + the consumers-one-docs-only-commit-behind pattern named at its current value (`f446512`).
3. **§0 BASE, all three rows re-pinned + re-featured:** WDC `fe05d85`(consumed `5570321`) 740/740 → `494f6ff` (consumed `f446512`, BOTH consumers) **759/759** — added: W2 (the files, the bit-exact law, the F1 flush law, the [1/32,32] rate domain, the degenerate-setter guards, the worklet + blob-URL venue, the consumer commits), the CR-A3 meter-tap offline guard, the whole-CR rounds (drift-gate fails-on-drift; package ships worklets + LGPL files + peerDeps; the barrel's D24e consumption reality). Engine `f68ab8c` 356/356 → `b8c6f88` **440/440** — added: the W2 bridge consumption (maintainPitch thread, pre-retime branch, 3-site law, span cache, W2a-k, out-of-domain legacy fallback), **N2b LANDED** (the full seam description + 18+2 pins), RR1-A. App `e662759` 83/83 → `70e99f0` **117/117** — added: the W2 app consumption (el.speed → retime both paths, #24 LIVE) + the W3 six laws + audioService.ts/mediaTone.ts.
4. **§0 GAP register re-based per D24 (the big edit):**
   - **N2b row FLIPPED to BASE** (struck in the register, LANDED @ `37cdd28`).
   - **SoundTouch offline pitch FLIPPED to BASE** (struck, LANDED as W2, consumers same round) — the old M2 row's first half.
   - **M2 split into two r2 rows:** the mixer surface (sidechain/PDC/automation/EQ/reverb/inserts — the M2 REMAINDER, WDC+engine, design round first) + the 4 deferred upstream test ports (WDC+engine, TEST-PORT round).
   - **Mixer G-surface FULL wiring:** re-tagged W-audio → **r2**.
   - **The realtime-vs-offline e2e NULL rig (A4-v2):** ADDED as its own row (was folded into the M2 acceptance parenthetical; the spec-14 §4.3 row) — **r2**, app rig.
   - **The async pre-retime queue item:** ADDED (NEW gap surfaced by W2 — the O(span) sync WSOLA on the play edge; engine; **r2**; design-w2 §4's specced fix).
   - **The waveform seam contract:** ADDED (S-wdc's **K3 HARD prerequisite** row — the offline-render + call-spy pin shapes; S16 + the LAW-NET GAP-C3 families; the Node-venue law; the plan's dependency edge).
   - **K3's audio pins:** ADDED (S-app's app-side offline-render + call-spy pins — waveform + the mute one-owner law; **K3**, was C3).
   - **Worklet asset serving:** ADDED (the 3 files to the app's `public/worklets/`; **K3**, was C3 (verify); the spec-14 §4.3 row; the app's public/ verified empty; the varispeed worklet needs no asset).
   - **Register line:** "spec 14 §4.3 — the worklist" → the posture-law statement (all §4.3 rows re-homed here; D23.3; THE plan is `IMPLEMENTATION-PLAN.md`).
5. **§4.1 `StructuralAudioSource` table grown to the live shape:** +5 rows — `maintainPitch?` (W2, merge-blocking), `volumeDb?` (the 0.0001 dB law), `gainAutomation?` (N2b, merge-blocking both directions), `bufferKey?` (baked-variant key, EQ-boundary-no-merge), `fadeInCurve?/fadeOutCurve?/CurveX?` (H15, curves never block alone) — verified field-by-field against `scene-to-segments.ts` at HEAD.
6. **§5 laws:** the preamble's pin census updated (the bridge suite 37 → **96** tests; + the W2a-k + N2b families + WDC's varispeed suites); **law 3 (H8) rewritten** — the legacy playbackRate path distinguished from the LANDED W2 maintainPitch path (the three-site law, the F1 flush law, pitch-preserved 440 Hz, the [1/32,32] rate domain, the out-of-domain legacy fallback W2k, the maintainPitch merge-block W2b, the worklet's blob-URL venue, the bridge consumes the pure path); **law 8 ADDED** (the N2b gain-automation law, complete with the source-frame fold algebra, the dedicated gain node, the exp-ramp exactness, the snapshot law, and the 2 REAL offline parity pins).
7. **§7 M2 roadmap:** a Round 23 amendment ADDED (W2 LANDED → consumed never re-done; the M2 REMAINDER = the 3 live-queue items, each tagged r2; the waveform-peaks sentence re-pointed from M2 to K3's audio half/S-wdc's seam contract).
8. **§10 facet rows:** Triangle conformance re-pinned (721 @ `374711c` → **759 @ `494f6ff`**, the full supersession chain); Upstream drift extended with the drift-gate-fails law (exit 1, fixed `de09c93`); **two rows ADDED** — Varispeed/maintainPitch (W2) and Gain automation (N2b), each with the full pass criteria.
9. **§11 code references:** **5 rows ADDED** — `soundtouch.ts`, `varispeed.ts`, `meter-tap.ts`, the varispeed test pair (the 16 pins + the worklet parity contract), `src/index.ts` (the barrel's D24e consumption reality); the `nle-bridge.test.ts` row updated (37 → 96 tests, the W2/N2b/R1/RR1-A families); the `public/worklets/*` row extended (the 3 named files, shipped in package files, the K3 app-side copy, the varispeed-worklet no-asset note).
10. **§12 open questions:** question 1 (automation curves) updated — the VOLUME lane LANDED via N2b (a different shape than web-daw's Track-param model; the engine's ScalarChannel fold is the precedent), the question generalizes to the other mixer params (r2's design round).
11. **Cross-ref hygiene:** §1's stale "see §5.4" → §5.3 (the varispeed law, now the real home of the SoundTouch-kernel-consumed claim); §4.3's test path corrected to `src/test/instrument-provider.test.ts`.
12. **§0 heading + ACCEPTANCE & TEST PLAN:** the R23 re-base noted; the facet-row reference extended to the W2/N2b rows.

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **The mixer surface (M2 remainder):** no live parametric EQ / reverb sends / per-track inserts / sidechain helper / PDC coordination beyond the existing `applyPdc` seam / general automation shapes in the NLE data model — `bridgeSceneSettings` still materializes unity faders + solo only (§7's honest gap re-read at HEAD: unchanged).
- **The 4 deferred upstream test ports:** not landed (WDC's HANDOFF queue item #1; explicitly "keeps sliding").
- **The mixer G-surface FULL wiring (A4-v1/r2):** unchanged (the strips are real; the wiring is not).
- **The realtime-vs-offline e2e NULL rig (A4-v2/r2):** net-new, not started.
- **The async pre-retime item (r2):** the adapter's pre-retime compute is still synchronous O(span) (read in the adapter; the design-w2 §4 rationale documents the accepted jank for the validation app).
- **The waveform seam contract (K3's hard prerequisite, S-wdc):** not yet stated/pinned WDC-side — the plan's dependency edge exists on paper only.
- **K3's audio pins + worklet asset serving (S-app):** the app's `public/` is empty; no app-side offline-render/call-spy waveform or mute pins yet.
- **(Downstream consumer lag, noted not owned here):** the app's engine pin `4ef0147` trails engine HEAD `b8c6f88` by one commit (RR1-B consumed it; the RR1-A SPAN law is the pending bump — the consumer re-pin wave, the plan's S-engine first action).

## NOTES

- **Hash correction vs the briefing:** the task said "app `0ac3c15`" for the W2 consumer; the verified commit is `0ac3f15` (re-pin WDC → `de09c93`; WDC's own HANDOFF cites the same). The spec cites `0ac3f15`. Likewise "W2 LANDED @ `de09c93`" is the CLOSED-state pin per WDC's HANDOFF; the feat commit is `1b40fd4` — the spec's §11 row carries both.
- **The engine's W2 feature commit is `0d61964`, not `df6eadf`:** WDC's HANDOFF cites `df6eadf` (the round's closing re-pin) as "the consumer landed"; both hashes appear in the spec (BASE cites the re-pin per the fleet convention; §11's soundtouch row cites the WDC-side feat `1b40fd4`).
- **"The 4 deferred upstream tests" names 5 bodies** in WDC's HANDOFF (engine / engine-metronome-automation / engine-param-channel / dsp-aux-sends / wam-effect) — the count "4" is the WDC/plan's own long-standing phrasing (likely 4 upstream test files + the wam-effect body); the spec's row cites the HANDOFF's file list verbatim without re-counting, matching the plan's wording.
- **§6 (M1.5/M1.6) and the Round-15 amendments kept verbatim** per the rules (historical contexts stay): the worklet-serving law in §6.1 remains the standing law; its app-side non-execution is now the §0 K3 row.
- **D25 check:** this file makes no timeline-UI-tree claims (no timeline-port/components/fork references) — no D25 edits required. The K3↔S-wdc ordering edge is reflected in the waveform-seam-contract GAP row.
- **Count discipline:** every count written is verified or round-trusted: 759 (WDC), 440 (engine), 117 (app), 96 (nle-bridge.test.ts `it(` count at HEAD), 16 (WDC varispeed.test.ts), 18+2 (N2b pins, commit-verified), 10 (engine W2 pins, commit-verified), 3 (worklet files, ls-verified), 2 (GAP-C3 families, LAW-NET-verified).
- **§2's AudioMixer-era comparison text kept:** it is the R9 adoption verdict's reasoning (the "has-vs-has-not" case) and already carries the R15 EXECUTED amendment — not a live stale claim.
