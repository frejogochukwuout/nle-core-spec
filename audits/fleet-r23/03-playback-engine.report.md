# R23 fleet audit — `03-playback-engine.md` (the playback engine spec: clock/transport/playhead)

**Agent:** R23-fleet-03 (fresh context). **File owned:** `03-playback-engine.md`. **Date:** 2026-09-07.
**Repos audited against (read at their synced HEADs; HEADs read from `.git/refs` as plain file reads; NO test runs):** nle-engine `b8c6f88` (440/440, tsc 0 — trusted), opencut-timeline `222532c` (489/489 per the report json — parsed), web-daw-core `494f6ff` (759/759 — trusted), nle-test-app `70e99f0` (117/117 — trusted). Engine vendor submodules are EMPTY in this sandbox checkout (not initialized); vendor pins corroborated via the app's `vendor/nle-timeline/UPSTREAM.lock.json` (OT mirror head `a4e971d`), the engine's `.agents/HANDOFF.md` (WDC `de09c93` era), and the sibling fleet reports' git-level re-counts (OT `a4e971d`, WDC `f446512` at HEAD). FEATURES verified by reading code.

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **OT's S1 transport policy — fully verified in code** (`src/lib/timeline/ops/timeline-core.ts`): `setPlaybackRate` (:586 — finite-nonzero domain, negative=reverse, POSITION-CONTINUOUS S1-F5 "a rebase to the snapped playhead would skip up to one frame per JKL press", notify-ONCE, NOT undoable); `setLoopRegion` (:610 — validated, reconciled against committedDuration on every committed change S1-F4, wrap-on-CROSSING-only S1-F3, both directions, in-region-bounded per review R2-A P2-1); `advancePlayhead(dt)` (:658 — consumer-supplied wall dt, anti-drift math on the UNSNAPPED origin, boundary stops vs the COMMITTED duration S1-F2, the ATOMIC boundary pause "listeners never observe playing=true at the end", the S1-F7 mirror-cadence law); the headless `timeline.setPlaybackRate`/`setLoopRegion`/`getLoopRegion` surface (`headless/api.ts:1030+`). The R22 BASE claim's four-law summary (playbackRate/loopRegion/anti-drift/atomic boundary pause) was already true — now enriched with the law-level detail.
- **OT count/authority:** `download/timeline-test-report.json` parsed → `total: 489, passed: 489, failed: 0`, 51 milestone suites (M1-M46 + R-suffixed real-mouse suites); per-test summation = 489 exactly. The fleet-standard split (359 in-page + 130 real-mouse) adopted from ARCH-R23 F4 + the sibling 05 report. M45 (`milestones-tround-fixes.ts`) + M46 (`milestones-s3seam.ts`, 3 pins) present. `setTracksMuted` at `timeline-core.ts:2108`; `toggleTrackLock` at :2033; the lock-track context-menu entry in `TimelineView.tsx`; S2's actions-layer lock pre-filter in `components/timeline/hooks/use-timeline-actions.ts`.
- **The app's W3 (the JKL AUDIO half) — all six pinned laws verified in code + tests** (`src/audioService.ts`, `src/audioService.test.tsx`, `src/engineService.ts`, `src/GluedShell.test.tsx`): (1) the union of W3 §2 + S3-C4's store-echo edge — "one law, two entry paths, one idempotence key" (`audioService.ts:557-572`); (2) the core-notify rate edge runs BEFORE the jump heuristic with an explicit return (":571 one edge one reschedule — no fall-through into the heuristic"); (3) `varispeedRate: segRate * tr` (:504); (4) the #24 offset law ELEMENT-domain (`offsetSec: seg.offsetSec + into * segRate`, :490, "the rate here is the SEGMENT's own"); (5) silent-reverse J (`if (!(tr > 0)) adapterStop()` :453-456 + the W3-4 test "segments stop, NOTHING schedules"); (6) timeline-authored fades (fadeInTimeline computed in the timeline domain first, ÷ tr once, :471-502 + W3-5); the routed setShuttle round-trip (`engineService.ts:119-129` router `setPlaybackRate`: rate 0 → pause; the mirror carries playing/playRate — `GluedShell.test.tsx:492` "W3: setShuttle(2) ROUTED … (F5)"). The port's S1 JKL keymap tap-accel ladder (500 ms same-direction, ⇧L/⇧J fixed 2×, K resets — `use-timeline-actions.ts:176-212`, `use-keybindings.ts`) and the EngineMount transport laws (loop-region one-way push, the rate seed across scene switches, the playRate rides every notify — `EngineMount.tsx:140-219`).
- **The engine's playback core at `b8c6f88`:** `core/clock.ts` (764 LOC — the audio-clock `_now()` at :576, the monotonic guard at :591, floor/ceil at :638, the 100ms timeupdate throttle at :687, R1-B7 Clock.fps re-anchor at :244-258, R1-B8 scrub-implies-pause + R2-5 statechange in `player.ts:786+`); `playback/player.ts` (3,656 LOC — the FX-2/C2 transport-generation counter :494/:504, ratechange event wiring :606, per-type frame build); `playback/video-sync.ts` (1,234 LOC — all six plan functions; LARGE_DRIFT_SECONDS :181; COALESCED_SEEK_EPSILON :225; registerDomVideoElement :1041); `bridge/av-link.ts` (N4, "pairwise A/V link propagation on the opencut shape"); the audio crossfades (audio-scene transition windows; the R3-3-refined mid-entry crossfade clamp + author-head-fade drop, pinned app-side at `audioService.test.tsx:416+` and engine-side in audio-scene).
- **RR1-A the fade-clamp SPAN law:** `clampAuthorFadeToSpan(fadeSec, spanSec)` at `audio/audio-scene.ts:803` with the exact docstring ("spanSec is the OUTPUT span: the clamped bridge duration on the export path (conversions.ts), the remaining timeline span on the player's mid-entry path… PREVIEW ONLY… while the export rendered the clamped law (a player↔export parity break). Both venues call THIS helper now — parity by construction"); consumed at `bridge/scene-to-segments.ts:68` ("A4: the R1-B1 fade-clamp law, now at the flattener twin"). Sibling 01's git-level verification: d421446 + the review round b8c6f88.
- **N2b LANDED:** engine `.agents/PLAN.md` §N2 closure ("N2b CLOSED too — seam round S2, 2026-09-07, engine 37cdd28 + app c8c875e… 18 bridge pins + 2 REAL offline-render sample-level parity pins") + the worklog's seam-round-s2 entry + the app-side glue (`scheduleAll` threads gainAutomation at `audioService.ts:436-440`; `segmentSignature` carries the automation lane at :398-407; the two glue-pin tests at `audioService.test.tsx:699-777`).
- **WDC's W2 varispeed/retime domain:** `lib/daw/varispeed.ts` header read in full — `retimeChannels` (tempo=rate ONLY, output length EXACTLY round(inFrames/rate) the F1 flush law), the identity laws, **RETIME_RATE_MIN = 1/32 / RETIME_RATE_MAX = 32 (CR-A1)**, `lib/daw/soundtouch.ts` (the WSOLA port), the general worklet (`VARI_SPEED_PROCESSOR_NAME`), `maintainPitch`; the meter-tap offline guard (`meter-tap.ts:292` "meters are a LIVE-push law; offline"); the drift-gate FAILS-on-drift (`scripts/sync-from-upstream.mjs:298` CR-B1); the integration family files all present in `src/test/`.
- **The K2 seeds:** `tests/vitest/engine/timeline-edit-ops.test.ts` (146 lines — imports the engine timeline; the R1-B4 transition-blocked aborts + the R2-4 Clock.fps re-anchor pin at :116-118) + video-sync/bridge-seams/planner; the engine's bridge modules import `opencut-timeline` (the vendored OT) at 5 sites; the vitest config's OT/WDC alias law read.
- **The consumer pins:** the app's `vendor/nle-timeline/UPSTREAM.lock.json` → upstream head `a4e971d`, single-path sync law, testing/ stripped; the app's `4ef0147` engine pin (per the plan + sibling 01's git-level re-count: **one commit behind `b8c6f88`**, not the plan's stale "14+").
- **The R15 playhead punch item is CLOSED:** OT's `components/timeline/hooks/use-timeline-playhead.ts` (round 6 F-B6) — `core.subscribe(() => controller.handlePlaybackUpdate(core.getCurrentTime()))`; the playhead's imperative left updates on every engine notify; `getIsPlaying: () => core.isPlaying()` was the fix for "hardcoded false made handlePlaybackUpdate dead code."
- **Cross-refs live:** spec 15 §9.5 (the event staircase register), spec 17's coverage-matrix rows (Varispeed pitch / Scrubbing latency), spec 18 §4.3, the mini's CORE-SEAMS S11 (playback-clock seam → C2 → now K3) + LAW-NET Part B's playback rows (playhead scrub clamp 1 + playback wrap/empty/scrub-extents 5 + R19 seekToClipHead 5 + R22 tick-mid-gesture 1 = the 12-test C2 family), spec 12/17's K2 census role — all resolve.

## FIXED (edits applied to `03-playback-engine.md`)

1. **Status header:** R22 → R23 re-audit framing (BASE extended with the round's landings; the GAP re-tag summary C2-behavior→K3 / C2-frame→w1 / C3→K3 / W-media→w2 / audio-depth→r2; the SCOUT-03 record kept as the historical verification layer).
2. **§0 BASE engine row:** `f68ab8c` 356/356, vendors OT `3420b5f` / WDC `5570321` → `b8c6f88` 440/440; vendors OT `a4e971d` / WDC `f446512` (the W2+whole-CR family). Enriched: the clock's grown laws (R1-B7 fps re-anchor, R1-B8/R2-5 scrub-pause, R2-4 fake-timer pin), the player's transport-generation counter + per-type frame build + Wave-4A scrub cache, the six sync plans, av-link N4, the crossfade laws. **Added the round's landings:** RR1-A the fade-clamp SPAN law (the one-helper parity-by-construction semantics), consumer-compat toSorted, the vendor re-pin, **N2b LANDED** (the flattener fold + the automation gain node + R1-B2 native path; 18+2 pins), the timeline-edit-ops K2 seed.
3. **§0 BASE OT row:** `05584d8` 459/459 (329+130/14 phases) → `222532c` **489/489 (359 in-page + 130 real-mouse across 51 milestone suites; the report json is the count authority)**; the S1 transport policy re-verified at LAW level (S1-F2/F3/F4/F5/F7 + the atomic boundary pause + the unsnapped-origin anti-drift + the timer-free consumer-dt design); added S2 lock (with the components' pre-filter), S3 setTracksMuted, M45/M46, F1 hardening, and **the D25 canonical React UI tree** (use-playback-ticker + use-timeline-playhead; the playhead punch item closed).
4. **§0 BASE WDC row — NEW** (the domain's normative audio core was absent from the R22 §0): `494f6ff` 759/759; W2 varispeed/soundtouch (retimeChannels, the F1 flush law, the identity laws, the RATE DOMAIN [1/32,32], the worklet, maintainPitch); the whole-CR guards (meter-tap offline guard, drift-gate fails-on-drift); the K2 integration family.
5. **§0 BASE app row:** `e662759` 83/83 → `70e99f0` 117/117; **W3 (the JKL audio half) added with all six pinned laws** (both entry paths/one idempotence key, core-notify-before-jump-heuristic, composed varispeedRate = segRate × tr, the #24 offset law ELEMENT-domain, silent-reverse J, timeline-authored fades, the routed setShuttle round-trip); plus the S1 keymap ladder, the EngineMount transport laws, N2b's 2 glue pins, ProgramCanvas (R7/N1), the WDC audio host.
6. **§0 consumer-pin note — NEW:** the app's engine pin `4ef0147` (one commit behind at the audited pins — the plan's "14+" is the HANDOFF-era figure); the app's OT-mirror lock `a4e971d` (one wave behind OT HEAD).
7. **§0 GAP register — re-tagged + extended (was 2 rows, now 7):** the staircase row W-media→**w2**; the C2 row SPLIT per D24 — the behavior half → **K3** (the S11 seam; the LAW-NET Part B 12-test family; store/policy halves author NOW ∥ the D25 bridge) and the viewer FRAME visuals → **w1** (spec-18 §4.3 rows + the mini viewer state testids + the side-by-side vs OT's `/view` gold sample); **added** the C3 audio-crawl row → **K3** (offline-render + call-spy pins; the Node-venue law; hard prerequisite S-wdc's waveform seam contract); **added** reverse-transport audio depth → **r2** (the silent-reverse J is BASE; FreeCut's §8.4 grain plan the reference); **added** the decode/scrub performance architecture (§5 worker pool + §7.3 3-tier cache) → **r4 (user-gated) / r6 (engine-P2)**; **added** the app's engine consumer re-pin → **K1-continuous** (the pin-lockset law). The header re-pointed from "phase per spec 14" → the D24 ladder (`IMPLEMENTATION-PLAN.md` §2) with the posture-law note that spec 14's retired runtime rows live here.
8. **§0 ACCEPTANCE & TEST PLAN:** "the R22 battery's posture checks" → battery_r23; added the K2-census registration note.
9. **Body stale-claim fixes (§3.4 / §6.5 / §10):** the clock-port note → BASE-pinned at `b8c6f88` with the grown-laws list (the "reference, NOT canon" framing narrowed to the MediaTime boundary law, which remains spec-side); the video-sync port note → BASE at the pin; **§10's "player.ts:1038 drops all non-video clips" — STALE, fixed**: marked the Round-7 failure mode historical and added the R23 update (scene-assembly's per-type visual collection video/composition/image/text + adjustment + stable-DOM; `collectFrameVideoCandidates`'s video-only continue is that function's contract).
10. **§13E (the engine reference register):** the preamble's "37,958 LOC, 124 tests, in-between reference NOT canon" → the R23 re-baseline framing (BASE-accepted at `b8c6f88` 440/440; the scout-era snapshot kept as history; the spec-wins law unchanged). All file:line cites re-verified and corrected (clock 550→576, 565→591, 612→638, 661→687; video-sync 925→926, 180→181, 224→225, 1040→1041; player 234→280). Two rows flipped/updated: the §10 render-loop row CORRECTIVE→**ALIGNED (FIXED — the N1 per-type family)** with the scene-assembly:868-877 quote; the §8.2 SoundTouch row → **CORRECTIVE superseded by W2** (the full WSOLA port landed in WDC; the engine's worklet stays the granular realtime insert path). The §7.2 scrub row narrowed (Wave 4A's cache + prewarm landed; the 3-tier GPU/RAM cache remains unbuilt). StaticClock re-verified still-absent (SPEC-ONLY stands).

## REMAINS-OPEN (gaps verified still-open — no repo landed them)

- **K3 playback-behavior corpus** — the mini's 12 playback-law tests are mini-side only (the view-side mock clock); the app-side re-expression over EngineMount's real engine is not authored (the app's GluedShell tests cover the shell flows, not the LAW-NET Part B family).
- **The viewer state rows (w1)** — spec 18 §4.3's loading/decode-fail+retry/offline states are not implemented app-side (ProgramCanvas is the engine-wired monitor; the state-row surface is the crawl's un-landed visual half); the OT `/view` side-by-side is un-run.
- **The K3 audio pins** — the offline-render + call-spy pins over the engine/WDC paths are not authored; S-wdc's waveform seam contract is not yet stated (the plan's hard prerequisite).
- **Reverse-transport audio (r2)** — the JKL negative rate is silent by design; no grain-plan port exists anywhere in the fleet.
- **The decode/scrub performance architecture (r4/r6)** — zero mediabunny usage in the engine (virtual media only; N5 deprioritized per D6); the scrub cache is single-tier (Wave 4A) — the FreeCut 3-tier GPU/RAM cache is unbuilt; decoder prewarm/VideoSourcePool/rVFC remain the engine's P2 backlog.
- **The consumer re-pin** — the app still pins the engine at `4ef0147` (one commit behind `b8c6f88`); the W2/N2b/RR1-A app-side surface rides the re-pin wave.
- **The event staircase playhead rows (w2)** — spec 15 §9.5's register is spec-side; no staircase suite exists app-side.

## NOTES

- **Spec-14 §4 reconstruction (charge c caveat):** the retired v2.1's per-domain §4 sub-registers live only in git history (`7ba030a`), and this agent ran NO git commands per the round's hard rules. The runtime-domain rows were reconstructed from the authoritative R22/R23 artifacts (ARCH-R22-finality's C2/C3 gate text + REVIEW-R22-PLAN's amendments 3/4 + ARCH-R23's D24 mapping table + IMPLEMENTATION-PLAN's K2/K3/w2/r2 rows) and ALL D24-mapped rows touching the playback domain now exist in §0: C2-behavior→K3, C2-frame→w1, C3→K3, W-media→w2, W-audio(varispeed-transport half)→r2, plus the pre-C1/LAW-NET acceptance list cited by the K3 row. No row was found "only in spec 14" for this domain within the readable evidence. The sibling 01 report (which did read git) confirms the §4.2-engine rows; no playback-only §4 row surfaced there.
- **Count discipline:** every count written is from the verified set — 489 (parsed from the report json; 51 suites; per-test sum = 489 exactly), 440/117/759 (trusted per the round's rules), 18+2 (N2b pins, PLAN-verified), 12 (the LAW-NET Part B playback family, census-verified), 3 (M46 pins), 51 (milestone suites). An un-reconcilable per-file `it(` grep (122 vs the declared 117) was NOT written into the spec — the family names are cited without per-file numbers.
- **The OT in-page/real-mouse split:** my independent per-test bucketing of the report json gives 372 non-R + 117 R-suffixed milestone tests — the R-suffix is not a clean real-mouse marker; the fleet-standard 359+130 (ARCH-R23 F4) is what §0 carries, with the json named as the total's authority.
- **The vendor-pin WDC nuance:** the engine's WDC vendor pin at HEAD is `f446512` (a docs-only wrap after the W2 + CR-B rounds) per the sibling 01/04 git-level reads; `de09c93` is the W2 landing SHA in WDC's own history (the plan's "W2 @ de09c93"). §0 carries f446512 with the chain noted; §13E cites de09c93 as the W2 landing — both correct in their frames.
- **The "14+ commits behind" plan figure:** superseded inside the round by the sibling fleet's git-level re-count (one commit); §0 states one commit with the plan's figure explained as the HANDOFF-era count. The IMPLEMENTATION-PLAN's S-engine row should get the same one-commit correction (out of this file's scope — flagged for the orchestrator/battery).
- **No D25-conflicting claims lived in this file** (it never claimed where the React tree lives; §0's OT row now affirmatively states the D25 canonical-tree law for the playback hooks it cites).
- **Deliberately NOT changed:** §1-§12 + §15 (the seed spec, the SCOUT-03 verification record, and the FreeCut Clock full quote — historical, preserved per the round's rules); §14's corrections (historical, still-accurate); the `## Testing` section's seed intent list (spec 17's facet discipline owns the forward form); the §9 Round-9 domain note (spec-20 handoff, unchanged).
