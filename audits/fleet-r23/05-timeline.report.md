# R23 fleet audit — `05-timeline.md` (the timeline / editing-domain UI spec — the D25-affected file)

**Agent:** R23-fleet-05 (fresh context). **File owned:** `05-timeline.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs, no git commands — HEADs read from `.git/refs` as file reads):** opencut-timeline `222532c`, nle-test-app `70e99f0`; the mini's `OT-SEAMS.md` / `CORE-SEAMS.md` / `LAW-NET-INVENTORY.md` read as the in-repo law register. Test counts trusted per the round's numbers (OT 489/489, app 117/117); every FEATURE verified by reading code.

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **HEADs:** OT `main` = `222532c3059…`, app `main` = `70e99f0bed4…` (file-reads of the refs — no git commands).
- **OT 489/489, report-json authority:** `download/timeline-test-report.json` → `total: 489, passed: 489, failed: 0`, **51 milestone suites** (37 in-page + 14 real-mouse by suite count; M45 + M46 present in the milestone list).
- **The S-round (S1-S4 + D-S5) landed** — M43 (17 pins) + M43R (4 real-mouse); OT `.agents/PLAN.md`'s S-ROUND COMPLETE close-out lists exactly the five items the spec's §0 enumerated (transport policy / track lock / transitionOut null=DELETE + split left-drops / bookmark color+note / typed ElementParams).
- **The T-round (D-T3) landed** — `setTracksLocked` + wire `track.setAllLocked` (lock-all ONE history entry) + D-T7 move-NOOP; M44 (15 pins); PLAN's T-ROUND COMPLETE close-out.
- **The S3 seam round (M46) LANDED — the round's headline for this file:** `setTracksMuted` at `ops/timeline-core.ts:2108` (the mute-all batch op, `withUndo` one-entry, the every-track-at-target NOOP predicate "the F1C-1 law carried over", text/graphic/effect overlays vacuously at-target per the `"muted" in track` family law) + the wire verb `track.setAllMuted` (`headless/api.ts:138` union member, `:1385` case with boolean validation → INVALID_PARAMS) + **M46 = 3 pins** (`milestones-s3seam.ts`: partial-states-apply one-entry / one-undo-restores-exactly / wire ok+changed:true+INVALID_PARAMS-on-non-boolean).
- **The F1 whole-codebase hardening ALL LANDED, with in-source markers** (each fix greppable by its F-id):
  - **F1A-1** — `selection/box-select.ts:168` + the `sumExpandedHeightsBefore` helper `:214` (marquee row math now accumulates expanded-keyframe lane extra heights; the P2-6 signature-only fix is real code now).
  - **F1A-2** — `ops/timeline-core.ts:2179` (`insertElementOnNewTrack` W9-E13 trims defaulting + animations normalization).
  - **F1A-3** — `timeline-core.ts:886` (heterogeneous insert batches reject atomically — track-type validation on every batch member).
  - **F1A-4** — createTracks id validation (engine + `api.ts:598` `validateWireMoves` extension).
  - **F1B-1** — the dead-button law (buttons-bitmask guard) in `element-interaction-controller.ts:495`, `keyframe-drag-controller.ts:313`, `playhead-controller.ts:305`, `box-select-controller.ts:186`.
  - **F1B-2** — the no-overlap invariant enforced on `updateElements` span patches (`api.ts:202/:267/:994`, CONFLICT + the corrected comment).
  - **F1B-3** — the non-left-mousedown tolerant start-gates (`box-select:145`, `keyframe-drag:181`, `playhead:163`, `seek:142`, `element-interaction:289`).
  - **F1B-4** — missing/null/non-object `params` → **INVALID_PARAMS** at the top of `applyInner` (`api.ts:277/:285`) — the error-classification half of the spec's error-code row is now covered by BASE.
  - **F1B-5** — `setLoopRegion` branches on the VALUE (`clear === true`; `api.ts:1052-1065`, a falsy clear never clears).
  - **F1B-6** — negative `trimStart/trimEnd` rejected at the wire (`api.ts:871`).
  - **F1B-7** — failed `applyBatch` preserves the pre-batch redo stack (`timeline-core.ts:190/:215/:226/:296/:2360/:2370` — the transaction snapshot/restore machinery).
  - **F1B-8** — the keyframe single-move flick commits (`keyframe-drag-controller.ts:332` fall-through).
  - **F1C-1** — `setTracksLocked` unlock-direction fixed (`timeline-core.ts:2062`, the `everyAtTarget` predicate with the F1C-1 comment).
  - **F1C-2/3/4** — the runner/pin/echo fixes (the `data-test="suite-complete"` runner gate; `milestones-tround-fixes.ts` = **M45, 12 pins**, whose header enumerates F1C-1/F1A-1/F1B-2/4/5/6/7/F1A-2/3/4/F1B-3/8/F1B-1 exactly).
- **The components tree (the D25 core evidence):** `src/components/timeline/` = **39 files / 7,702 LOC**; per-file: TimelineView **1,221L**, TimelineElementView **883L**, TimelineTrack **184L**, Ruler 147 / Playhead 96 / Toolbar 266 / BookmarksRow 218 / ContextMenu 148 / DragLine 63 / SnapIndicator 41 / Tick 57 / AudioWaveform 204 / AudioVolumeLine 225 / theme 93 + the **19-hook** family (selection-journal, use-bookmark-drag, use-box-select included). **Zero `next/*` imports** in components/ (grep-verified); **32 `@/lib/timeline` import statements across 30 files** (the D25 alias surface, exactly as ARCH-R23 states); **zero `data-testid` emissions** (the structural-half gap, verified).
- **The controllers:** all 10 in `src/lib/timeline/controllers/` (+ `event-types.ts`).
- **The `/view` gold sample:** `src/app/view/page.tsx` (406L) — the miniature editor shell (library panel + compositor preview via `PlaceholderCompositor` + timeline, deterministic fixtures, `window.__VIEW_TEST__` hooks at `:357`); the in-page runner `src/app/page.tsx` (305L, `data-test="suite-complete"`).
- **The drag-law/zoom constants are UNCHANGED** (the D22-adjacent contract text verified still-true, no edits needed): `TIMELINE_DRAG_THRESHOLD_PX = 5` strict `>` (`element-interaction-controller.ts:53/:176`); `BASE_TIMELINE_PIXELS_PER_SECOND = 50` / `TIMELINE_ZOOM_MIN = 0.1` / `TIMELINE_ZOOM_MAX = 100` (`view/scale.ts`); `TIMELINE_INDICATOR_LINE_WIDTH_PX = 2` still (`view/pixel-utils.ts:12` — the spec's known-delta note stands).
- **The app fork (the retirement evidence):** `timeline-port/TimelineView.tsx` = **1,328L** with the PORT-LOCAL props exactly as ARCH enumerates (`confirmDelete:130`, `initialSelectedIds`/`onSelectionChange`/`selectionIds:135-144` (the echo-guarded selection bridge pair), `onZoomChange:149`), 17 `@vendor/timeline` imports, the port-only `EMPTY_CONTEXT_ITEMS:155`, the hardcoded `#timeline-scroll:1059`; OT's lock-track context-menu item (`TimelineView.tsx:531` "Lock track"/"Unlock track", state-dependent) is ABSENT from the fork — the ≥2-waves-behind (pre-F1) claim verified by feature diff (fork has no lock item, no `cancelRegistry`, no `contentTopPadding`/`trackExtraHeight` threading).
- **The law register:** OT-SEAMS = **14 rows**, the D22/R22 drag-law freeze verbatim (R18k law rows 1-3); CORE-SEAMS = S1-S10 (timeline-domain seams, S7 = the selection projection, S2 = the geometry-law map); LAW-NET Part A = **91 families / 249 tests** (33 HOLDS / 322 authored).
- **`toggleElementsMuted`/`toggleElementsVisibility` engine ops exist** (`timeline-core.ts:1341/:1385`, W8-d, M40's 11 pins) — routed via `updateElements` patches; the wire surface = **30 `TimelineCommand` members** (was 24 at the R15 charter; +`timeline.setPlaybackRate`/`setLoopRegion`/`advancePlayhead` + `track.setAllLocked` + `track.setAllMuted`).

## FIXED (edits applied to `05-timeline.md`)

1. **Header Status line:** R22 framing → R23 re-baseline + **the D25 single-tree ruling** (OT @ `222532c` 489/489 BASE **and `src/components/timeline/` is THE canonical React tree; the app fork retires**; the gap = r1/D25-bridge/K3 tails), with the R22 lineage kept as the supersession record.
2. **Reference-repos line (the STALE "components pending its W4" claim):** → the canonical React component tree per D25 (W4/W5, M17-M21, verified M17-M46; the app fork retired).
3. **§0 BASE OT row:** `05584d8` 459/459 (329+130 across 14 phases) → `222532c` **489/489 (359 in-page + 130 real-mouse across 51 milestone suites; the report json is the count authority)**; added the T-round, **the S3 seam round (M46: setTracksMuted + track.setAllMuted)**, and **the F1 hardening** (F1A/F1B/F1C enumerated with the wire-validation laws).
4. **§0 BASE new bullet — the D25 canonical-tree law** (replaces the STALE "the React view tree of this spec lives APP-side; OT `view/` is utils-only"): OT's tree = 39 files/~7,700 LOC with the named files/LOC, pure React (zero next/*), M17-M46 + real-mouse + pixel verified, the 10 controllers core-side, **the gold sample = the `/view` runner**; `src/lib/timeline/view/` correctly re-scoped as the utils-only directory (that part of the old claim was true and survives).
5. **§0 BASE app row:** `e662759` 83/83 → `70e99f0` **117/117**; the fork's retirement per D25 (PORT-LOCAL props enumerated; the vendor extension `vendor/nle-timeline/components/` + the `@/lib/timeline` alias; ≥2 upstream waves behind pre-F1 with the missed-deltas list — lock-track menu, P2-6/P2-8/P2-9, `cancelRegistry` — plus the port-only `EMPTY_CONTEXT_ITEMS` + `#timeline-scroll`; the post-swap checklist).
6. **§0 BASE law-register row:** added CORE-SEAMS S1-S10 + LAW-NET Part A (91/249) + the explicit "D22 freeze is standing law — untouched by R23" note.
7. **§0 GAP register — re-tagged per D24 (all 6 rows, transitional dual-tags kept):** op families `r1 (was W-ops)`; C7 `r1 END (was W-ops END)` **with the count corrected 24 → 30** (the family grew: S1 transport trio + setAllLocked + setAllMuted); error-code coarseness `r1 (was W-ops)` **annotated that the F1B-4 INVALID_PARAMS classification half is now COVERED by the OT BASE**; zoom-ladder/ripple-toggle `the D25 bridge (was the C1 OT-side halves; S-ot worklist item 3)`; selection projection `K3 (was C1; CORE-SEAMS S7)`; `onViewStateChange` `r1 additive (was W-ops additive)`.
8. **§0 GAP rows ADDED (posture law — charge c + the D24 mapping):** (a) **the composed ripple family** (the mini's cutHead/cutTail/ripple-trim/quantize, 4 census units/16 tests) — K3 composes app-side over `timeline.trim`/`rippleDelete` at crawl, r1 graduates the OT interval-diff family (the W-ops compose half — was stranded at r1 in draft v1); (b) **the element-level `toggleElementMuted`/`toggleElementVisibility` wire verbs** (spec 15 §4.1A row — engine ops landed W8-d/M40; the dedicated wire pair is the r1 addition); (c) **the D25 bridge row** (S-ot/S-app joint, crawl): the PORT-LOCAL props upstreaming + EMPTY_CONTEXT_ITEMS bug-fix + the timeline-scroll anchor prop; the testid-emission convention (default-off; zero testids today; the 59-static + 15-templated census gate); the view-config surface; the structural grammar half (crawl-tail) + the token half (w1); the vendor extension + fork retirement + post-swap checklist.
9. **§0 GAP header:** "owner + phase per spec 14; the register is spec 14 §4.1, pointed not duplicated" (stale — spec 14 is retired) → "per the D24 ladder; this register IS the editing-domain register: spec 14 §4.1's rows were re-homed HERE at R23, the posture law".
10. **§0 ACCEPTANCE & TEST PLAN:** OT 459/83 → **489 (359+130, 51 suites) / app 117**; added the K3-DOM-downstream-of-the-bridge ordering-law note.
11. **§1 Purpose:** "cite it (plus OpenCut-classic for the components its W4 has not landed yet)" (stale) → the canonical-tree statement; OpenCut-classic re-scoped as the citation of record for the ORIGINAL constants/patterns (§16.1/§16.2).
12. **§16 preamble:** the R8-era pin `d3b2163` + "has **no React components yet** (W4 pending)" (very stale LIVE claim) → the R23 pin `222532c` (R8 lineage noted) + the canonical tree + the `/view` gold sample.
13. **§16.4 nle-engine preamble + census row:** "the timeline UI is entirely greenfield (… components pending W4)" → "no longer greenfield — it is OT's canonical tree (D25)"; table row note "spec is greenfield" → "the UI home is OT's canonical tree (D25)".
14. **§16.5 preamble:** the pin bracket "[R15-era pin — superseded by the R22 pin `05584d8`]" → "superseded by the R23 pin `222532c`, 489/489 + the S/T/F1/S3-seam rounds + the D25 canonical-tree ruling" (R15-era LOC/test counts explicitly kept for lineage); the known-deltas C7 count 24 → 30; D25 + F1 sentences added to the components claim.
15. **§16.5 table Headless API row:** `24-type prefixed union (since W5; 18 at the R8 sweep)` → `30-type prefixed union (24 at the R15 charter; grown by the S1 transport trio, track.setAllLocked, track.setAllMuted)` + the F1B boundary hardening; status CORRECTIVE kept (C7 still pending).
16. **§16.5A deduplicated:** the byte-identical second copy of the projector-clauses block removed (diff-verified identical; a one-line R23 audit note left in place; content unchanged).

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **The r1 family:** slip/slide/roll/rateStretch + wave-2 retime/freezeFrame/rangeRemoval (no OT ops; the §16.5A op-port table stands); **C7 rename** (30 prefixed names still on the wire — CORRECTIVE row stands); the error-code envelope (spec 15 §6.3 first — only the classification half got F1B-covered); the element-level toggleElementMuted/Visibility **wire** verbs (engine ops exist, wire pair absent from the 30-member union); `onViewStateChange` (absent from OT's TimelineView props — OT's own HANDOFF dispositions it as app-shell policy).
- **The D25 bridge (none of it has landed — it is the plan's S-ot first action):** OT emits ZERO `data-testid` (grep-verified); the PORT-LOCAL props are NOT upstreamed (OT's TimelineView has no `initialSelectedIds`/`onSelectionChange`/`selectionIds`/`onZoomChange`/`confirmDelete` — grep-verified absent); no view-config surface (zoom-ladder config / ripple-toggle semantics); no structural grammar components (track-head column / minimized strip / compact-pill / tools-row deltas); no mini theme mode (token half); the app's `vendor/nle-timeline/UPSTREAM.lock.json` is still the single-path core-only shape (`upstreamPath: src/lib/timeline`, `upstreamHead: a4e971d`) — the two-path components extension is future work.
- **The K3 family:** the selection single-subject projection (app-side; not authored); the composed ripple family app-side composition (not authored — LAW-NET Part A is the acceptance list).
- **The known visual delta** `TIMELINE_INDICATOR_LINE_WIDTH_PX = 2` vs OC's 3px — still unfixed in OT (the §16.5 known-deltas note stands).

## NOTES

- **The in-page/real-mouse split:** my independent re-count of the report json gives **360 in-page + 129 real-mouse** by per-test summation — the total is exactly **489** (the json is the count authority on the total); the corpus-standard **359 + 130** (ARCH-R23 F4, used by the fleet) is kept in the spec for coherence. The ±1 is a bucket-classification edge (one test attributed differently between the round's census and my per-test sum) — immaterial, flagged here for the count-discipline law.
- **Two "S3"s disambiguated in the spec text:** the S-round's S3 (transitionOut, session 11, M43) and the NEW S3 seam round (setTracksMuted, session 13, M46) — the §0 BASE row labels the new one "the S3 seam round (M46: …)" so the two cannot be confused.
- **App worklog staleness:** the app's `worklog.md` ends at session 11 (83/83) while HEAD is `70e99f0` (117/117) — counts trusted per the round's directive (no battery runs); the app has no `.agents/` (the plan's S-app row already registers this as the first-action orientation step).
- **App vendor pin:** `vendor/nle-timeline` @ `a4e971d` (fleet's coherent sha family, matching the engine's submodule pin per the 01-fleet report) — the pre-D25 single-path shape; the lock's sync law ("no other local edits permitted") is exactly what the bridge's two-path extension must preserve.
- **Line-number drift in §16.5's table** (R15-era `file:line` cites for OT): left as-is — point-in-time reference; the *behavioral* claims were re-verified at HEAD (drag threshold, zoom constants, layout constants, placement laws all unchanged); only the two LIVE-stale rows (the pin/counts preamble + the Headless API row) were re-based.
- **Sibling artifacts not edited (per D23's mapping-note rule):** `CORE-SEAMS.md` S1's "24-command wire surface" is now stale (30) and OT-SEAMS' op map cites the same era — mapping notes for their owning track (S-spec/mini), out of this agent's one-file scope.
- **The D22 drag-law freeze: untouched** — no drag-law text was edited beyond pin/count freshness (verified: the §8.3/§9 contract text and the OT-SEAMS citation both stand; the freeze is restated in §0 as standing law).
- **No battery/test-suite runs; no git commands** (HEADs established by reading `.git/refs` files; counts from the report json; features by code reads).
