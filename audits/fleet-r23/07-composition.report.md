# Fleet R23 — spec 07 audit report (composition: compositor / layering / blend / transitions)

**File audited:** `nle-core-spec/07-composition.md`
**Agent:** R23-fleet-07 (SPEC-FILE AUDITOR, fresh context)
**Live repos verified (2026-09-07 HEADs):** nle-engine `b8c6f88` (440/440, tsc 0 — trusted counts, features read) · opencut-timeline `222532c` (489/489) · nle-test-app `70e99f0` (117/117) · `ui-mock/shell-variants` `e89cd65` (1,521) · engine vendor submodules re-read via the wave-1 verification: OT `a4e971d`, WDC `f446512`
**Ruling basis:** ARCH-R23 D23/D24/D25 + `IMPLEMENTATION-PLAN.md` (§2 the ladder + §3 workstreams); coherence-checked against the wave-1 `04-renderer-color.report.md` (the N1 composition-frame family, the placeholder-compositor row, the Wave-4A P0 flips, the r6 CPU-transition row)

---

## VERIFIED-STRONG (claims that held against the live repos)

1. **The N1 composition-frame bridge family** — `src/lib/nle/bridge/composition-frame.ts` verified in full: `buildCompositionFrame` pure math + `paintCompositionFrame` thin Canvas2D painter, jsdom-null-ctx no-op, z-order main→overlays-reversed, the cut-centered transition window `[cut − D·alignment, cut + D·(1−alignment))` with head/tail source handles, other presentations degrading to the crossfade floor, the documented v1 exclusions (sticker/graphic not drawn, text dest = full frame, `speed` visual-only, track `muted` delegated). Pinned in `tests/vitest/engine/bridge-seams.test.ts` (two dedicated describes).
2. **The bridge pin arithmetic** — counted live: `bridge-seams.test.ts` carries **97 `it()` pins in 18 describes**; the R22-era "64 seam pins" claim decomposes EXACTLY (N1 21 + N2 14 + N3 17 + N4 12 = 64), the +33 being the T-round `transitionInputsFromScene` (4), R1-B1/B2/B3 (7: the RR1-A span clamp + the N2b native volume-keyframe lane + the inspector-fields differ), R2-1/R2-2 (2), R3-1/2 + R3-5 (2), and the S3 hardening wave (18: static volume domain, retime rate, av-link × track locks, transitionOut fromJSON bypass).
3. **N3 (transition windows), N4 (av-link), W1 (meters)** — all re-verified: `bridge/transition-inputs.ts` (cut-centered handle-extension equal-power windows, the degradation ladder, the (trackId, atSec) pairing law); `bridge/av-link.ts` (the id-closure link expansion); `bridge/realtime-engine.ts`'s real `onMeter` master + `onBusMeter(idx)` aux 1..4 ("METER PUSH is real since W1, 2026-09-06"). All N1-N4 + W1 marked ✅ CLOSED in the engine's `.agents/PLAN.md`.
4. **N2b keyframed volume LANDED** — engine `37cdd28` (the flattener folds `animations["volume"]` to gainAutomation breakpoints, the dedicated adapter automation node, the R1-B2 native-path parity lane) + consumed app-side `c8c875e` (the app's `audioService.ts` N2b comments + the `audioService.test.tsx` N2b describe). Removed from any GAP framing; now BASE.
5. **RR1-A fade-clamp SPAN** — verified: `scene-to-segments.ts` RR1-A-1 (fades carry RAW, clamp at the push bounded by the final span) + `transition-inputs.ts:184` `clampFadeToElementSpan`; pinned by the R1-B1 "one law, both venues" describe.
6. **The Wave 4A P0 fixes** — both verified in code (and coherent with the wave-1 04 audit): P0.1 the effects singleton `return this._pongTexture;` is GONE (`effects/pipeline.ts:5148` `applyToTexture` + `:5201-5210` the legacy `apply` — "Wave 4A contract change: the returned texture is a NEWLY-CREATED, caller-owned texture"); P0.2 the cross-clip-shared mask combine textures are GONE (`gpu/mask-manager.ts:671-758` `getMaskResult` per-call outputs, invert exactly once, `:739-757` pool acquire/copy-out). This spec's §7.3/§8.3 laws are now the port's own contract.
7. **The 27-presentation registry** — counted live: `transitions/registry.ts` `BUILTIN_PRESENTATIONS` carries exactly **27** entries (fade, wipes, slides, dip-to-color, dissolve, clock-wipe, iris-circle, zoom-blur, flip, the 7 freecut variants, glitch, pixelate, chromatic, radialBlur, liquidDistort, lensWarpZoom, lightLeakBurn, filmGateSlip). The spec's "27 entries in the reference" claim held; the mock's `TRANSITION_PRESENTATIONS` also carries 27.
8. **The §6.1A transition model (two-tier, cut-centered, handle-governed)** — every load-bearing quote re-verified: `transitions/planner.ts:21` (the leftPortion math — ref still exact), `handle-utils.ts:318` (`getMaxTransitionDurationForHandles`), `core/types.ts:311` (`TransitionType = 'crossfade'`), `registry.ts:2249` (BUILTIN_PRESENTATIONS — exact), `transitions/pipeline.ts:90` (`TransitionPipeline` + `cachedBindGroups` at `:96`, single-pass, `rgba8unorm`).
9. **The §5.5 `disableOcclusion` contract is REAL engine-side** — `scene-assembly.ts:1334/1340` (`disableOcclusion?: boolean` on `buildFrameRenderTasks`), used for export/offscreen and when active shape masks could punch through the occluder (`:1165-1169`).
10. **The toSorted consumer-compat** — `transitions/planner.ts:378` ("Consumer-compat (W3 app re-pin): toSorted is ES2023…") + `gpu/texture-pool.ts:309`.
11. **The BlendMode CORRECTIVE stands** — `gpu/compositor.ts:33` `BLEND_MODE_INDEX`: 25 FreeCut modes, no `plus-lighter` (the `plus-lighter ≡ linear-dodge` mapping note remains correct).
12. **The occlusion amendment, refined not broken** — `scene-assembly.ts:1358-1377`: the port's "Wave-seal occlusion fix" (`:1180-1188`, the track-order axis law — OUR higher `order` = visually TOP, FreeCut's axis inverted) refined the scan to top-first/topmost-occluder/maximal cull. The amendment's effect (occluded layers excluded; occluder's own track renders) is unchanged — the spec's old bottom-up quote is now marked historical and the normative text re-based.
13. **The Round-7 "type-agnostic items" corrective is RESOLVED engine-side** — `player.ts:1575` is now `clip.type !== 'video' && clip.type !== 'image'` (Wave 4C P1.3: image renders exactly like video), text dispatched at `:1557`, nested compositions at `:1533`; the remaining video-only continue is `collectFrameVideoCandidates`'s own contract. Same finding as wave-1's 03 report — the row flips CORRECTIVE → ALIGNED.
14. **The app ProgramCanvas** — `src/ProgramCanvas.tsx` (154 LOC): the N1 composition frames host exactly as claimed (live core scene + D2e sidecars + hash-color resolver; jsdom pins the composition math via `data-ops`/`data-playhead`).
15. **The OT placeholder compositor + the S3 laws** — `src/lib/timeline/render/placeholder-compositor.ts` (176 LOC, Canvas2D solid-color composition behind `setTracks()` + `renderFrame(t)`, its header naming spec 04's renderer as the real painter; M12 `readCenterPixel` pins in `testing/milestones-view.ts`); the `ElementTransition` type at `types/index.ts:145-150` (`type: "crossfade"` + optional `presentation`/`alignment` — structurally §6.1A's wire); the null=DELETE-key law at `ops/timeline-core.ts:2613`; split's left-drop at `ops/split.ts:161`; the 7-way element union at `types/index.ts:231-238`.
16. **The variants' R23 W-A FX surface** — `components/fx/FxBrowser.tsx`/`FxInspector.tsx` (the FX page's dock: 5 effect rows + ALL 27 `TRANSITION_PRESENTATIONS` + 6 fade presets; the frozen `application/x-nle-effect` drag payload, spec 15 §5.4) + the timeline's interactive transition boxes and fades grammar (`Clip.tsx`/`Timeline.tsx` R23-WA blocks) — the mock reference, now BASE.
17. **The K2 engine-side seeds exist** — `tests/vitest/engine/` carries `timeline-edit-ops`, `video-sync`, `bridge-seams`, `planner` (the cross-module family; coherent with the plan's S-engine row).

## FIXED (stale claims corrected in the spec)

1. **§0 BASE — the engine pin/count/vendors** — `f68ab8c` 356/356, vendors OT `3420b5f` + WDC `5570321` → **`b8c6f88` 440/440, vendors OT `a4e971d` + WDC `f446512`**. BASE extended with the landed-since-R22 surface (N1 family + N3/N4, W1, N2b `37cdd28`, RR1-A, the toSorted compat, the Wave-4A P0 fixes) and the pin-count re-baseline (64 → 97 with the exact decomposition).
2. **§0 BASE — the fleet rows** — the app row re-pinned (`e662759` 83/83 → `70e99f0` 117/117, ProgramCanvas's mechanics enumerated); the OT row ADDED (the placeholder compositor + the S3 transitionOut laws + the 7-way union — previously the spec's §0 had no OT row at all); the mock row ADDED (`e89cd65`, 1,521 tests, the R23 W-A FX surface — with the 00-master-1470 reconciliation note, per the count-discipline convention).
3. **§5.5 occlusion cutoff** — the normative scan re-based to the top-first/topmost-occluder law (the port's Wave-seal fix; the old bottom-up quote marked historical), line refs refreshed (`:1243`→`:1358-1377`, `:1263`→`:1379`), and the `disableOcclusion` contract annotated as verified real (`:1334/1340`, `:1165-1169`).
4. **§6.1A line refs** — `registry.ts:29-33` → `:30-35`; `handle-utils.ts:302` → `:318`; `headless/api.ts:825` → `:620-621,851-860` (the addTransition op moved). All quotes otherwise byte-verified.
5. **§6.1A + a new R23 note** — the LIVE consumer wire documented: the transition rides the element's own `transitionOut` field (element-anchored), the pair-form is the composition runtime's RESOLVED form (both N1/N3 seams derive it); the S3 null=DELETE-key law, the split left-drop law, and the params overwrite-only law all cited at their live lines — the composition runtime's shared-window obligation stated.
6. **§12.A.1 header** — the "37,958 LOC, 124 tests" SCOUT-07 snapshot superseded in place (440/440, tsc 0, ~54,700 LOC under `src/lib/nle` across 62 files — counted live) with the R23 re-verify note.
7. **§12.A.1 table — all 17 rows refreshed to `b8c6f88`** (scout-era refs preserved as "refreshed from" annotations): scene-assembly `:16` (exact, kept), `:1243`→`:1358`, `:1263`→`:1379`; player `:949`→`:1321`, `:1038`→`:1575`; types `:260`→`:311`; handle-utils `:302`→`:318`; pipeline `:89`→`:90`; effects `:5041`→`:5113`; mask-manager `:477`→`:723`, `:468`→`:671`; compositor `:32`→`:33`; registry `:2249` + planner `:21` + texture-pool `:5` (exact, kept); transform-resolver `:59` → `core/transform-parent.ts:878` (the FX-4 relocation).
8. **§12.A.1 — two rows flipped to PORT-FIXED (Wave 4A)**: the §7.3 texture-ownership row (P0.1) and the §8.3 mask-texture-ownership row (P0.2); the §10.1 texture-pool row's "dead code" note retired (LIVE since Wave 4A — player + mask-manager acquire/release); the §5.4 row's "corner-pin unwired — engine P1.8" note retired (WIRED since Wave P1.8, `core/types.ts:704`); the §12.2 row flipped CORRECTIVE → ALIGNED (the Round-7 failure mode fixed by Waves 4C/4D); the §4 purity row's ENGINE-GAP nuanced (the FrameItem/QuadTransform descriptor stays SPEC-ONLY, but the N1 bridge now supplies the pure serializable DRAW-OP list as the consumer seam).
9. **Header status line** — re-based to R23 (the re-verify's scope + the forward-work enumeration).

## GAP FLIPS / ADDS / RE-TAGS (the §0 register, per D24)

- **RE-TAGGED — the register header**: "owner + phase per spec 14" → the D24 ladder / `IMPLEMENTATION-PLAN.md` §2, with the posture-law note that the retired spec-14 §4 composition rows re-homed HERE.
- **KEPT (re-verified OPEN) — the projector parity corpus**: S-engine (∥ crawl) — no S4 suite / parity corpus in the engine's tree or its PLAN queue at `b8c6f88` (N1 CLOSED as a seam; the formal corpus unauthored). The row now carries the retired-spec-14 §4.2 provenance + the spec-04 §0 twin cross-ref (cross-referenced, not duplicated — the "one row lives in ONE spec" law respected by the explicit twin note).
- **ADDED — the N1 visual seam's v1 exclusions** (S-engine ∥ crawl, additive): sticker/graphic not drawn, text ops dest = full frame (no spatial transforms), `speed` visual-only, track `muted` delegated — the seam's own header registers this floor; acceptance = the excluded types draw + transform sidecars honored.
- **ADDED — the C2 composition-frame consumption behaviors → K3 (was C2-behavior)**: scrub/play/seek over the real engine clock driving `buildCompositionFrame`, the D2e sidecar law, the store/policy halves authored NOW ∥ the D25 bridge — explicitly LINKED to spec 03 §0's viewer/transport split (link, not duplicate).
- **ADDED — the C4 crawl-exit legs → K4 (was C4)**: the e2e's play + export legs render composition frames end-to-end.
- **ADDED — the transition-presentation depth + the P2 surfacing tail → r6 (was R-engine-p2), non-blocking**: the 27-presentation visual surfacing beyond the honest crossfade floor + the ShapeItem/Lottie/Subtitle/Controller surfacing; the CPU transition renderers (~1,450 LOC, export fallback — the engine's P2 backlog, verified) is noted as spec 04 §0's row (the renderer half), THIS row owning the presentation-depth half.
- **Not re-homed here (verified not spec-07's)**: the union façade (spec 05/12's W-ops→r1), N2b (LANDED — BASE), N5 real media decode (r4, user-gated — spec 15's), the color instruments (r3 — spec 04/08's), the grade-math binding (r3 — spec 04/08's).

## REMAINS-OPEN

1. **The projector parity corpus** (S-engine ∥ crawl) — the formal S4 suite / SceneTracks→ingestion translator + corpus; the engine's PLAN.md has no row for it (the plan's S-engine row + this §0 row are its only homes).
2. **The N1 visual seam's v1 exclusions** (S-engine ∥ crawl) — the sticker/graphic + text-transform + resolver extensions.
3. **The K3 composition-frame consumption behaviors** — the app-behavior nets over the N1 seam (store/policy halves may author now; the D25 ordering law governs the DOM-structural half).
4. **The K4 legs** — the automated e2e (play + export over the N1 render path).
5. **The r6 tail** — the 27-presentation visual depth + the P2 surfacing; the engine P2 backlog still lists "CPU transition renderers (~1,450 LOC, export fallback)" + the ShapeItem/Lottie/Subtitle/Controller items.
6. **The 8-bit GPU baseline character** (owned by spec 04 §13D — noted here for coherence): 46 `rgba8unorm` sites / 10 files under `src/lib/nle` at `b8c6f88`; the transitions pipeline is `rgba8unorm` (`pipeline.ts:116`) — this spec's scene-linear blend corrections (§13.11's `rgb(187,187,0)` law) remain forward work at the renderer binding.
7. **The FrameDescriptor (FrameItem/QuadTransform) shape** — still SPEC-ONLY: the engine's interactive path is the imperative `_buildLayers` (LegacyCompositeLayer) and the consumer seam is the DRAW-OP list; the formal serializable descriptor remains the composition-runtime rebuild's contract.

## NOTES

- **Dual phase tags** are used throughout §0 (the D24 vocabulary + the retired spec-14 term in parentheses) per battery_r23's pre-fleet-close convention.
- **Count discipline**: the mock row cites the live variants count (1,521 @ `e89cd65`) with the explicit reconciliation note vs 00-master's 1470 — the battery re-baselines centrally post-fleet; this file does not pre-empt it.
- **Spec-14 §4 reconstruction (charge c caveat)**: the retired v2.1's per-domain §4 sub-registers live only in git history (`7ba030a`); this agent ran NO git commands per the round's hard rules. The composition-domain rows were reconstructed from the authoritative artifacts (ARCH-R23's D24 mapping table + `IMPLEMENTATION-PLAN.md` §2/§3 + the wave-1 01/04 reports' §4.2 dispositions + the engine's own PLAN/P2 backlog): C2-behavior→K3, C4→K4, R-engine-p2→r6 all now exist in §0, and the §4.2 rows that touch composition are either re-homed here (the projector parity corpus — pre-existing at R22, now provenance-tagged) or explicitly cross-referenced to their owning spec (04's CPU-transition-renderers + instruments rows, 05/12's union façade, 15's N5). No composition-only row was found stranded in spec-14 within the readable evidence.
- **Coherence with the wave-1 04 audit** (per the task's read-first instruction): the N1 family, the placeholder-compositor row, the Wave-4A P0 flips, the projector-parity twin, and the r6 CPU-transition row all match 04's §0 wording; the K3/K4 legs match the plan's D24 mapping; the 64→97 bridge-pin arithmetic is this file's own contribution (the decomposition is exact).
- **The §12.7/§13.11 blend-pixel law and §17's 72-test contract** are the intended (future) executable contract — no live claims, preserved untouched. §1-§11's FreeCut/OpenCut-classic SCOUT-07 material is historical by design and preserved.
- **No git commands run, no test suites run** (per the fleet rules — counts trusted, features verified by reading code). Only `07-composition.md` was edited.

## VERDICT

**The spec's composition thesis held — the two-tier cut-centered transition model, the N1 family, and the occlusion/ordering laws all verify byte-level against the live repos (and the engine has since realized several of the spec's own corrective laws: the Wave-4A texture-ownership fixes, the type-agnostic dispatch, the wired corner-pin) — but every LIVE pin/count/line-ref was stale (the R22 f68ab8c/356 world), the §0 GAP register carried a single row against the D24-mapped C2/C4/r6 obligations, and the S3 element-anchored transition wire was undocumented. All fixed; the §0 triad now matches the D24 ladder, the live repos, and the wave-1 coherence set.**
