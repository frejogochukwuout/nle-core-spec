# Fleet R23 — spec 04 audit report (renderer/color pipeline)

**File audited:** `nle-core-spec/04-renderer-color.md`
**Agent:** R23-fleet-04 (SPEC-FILE AUDITOR, fresh context)
**Live repos verified (2026-09-07 HEADs):** nle-engine `b8c6f88` (440/440, tsc 0 — trusted counts, features read) · opencut-timeline `222532c` (489/489) · nle-test-app `70e99f0` (117/117) · `ui-mock/shell-variants` `e89cd65` (1,521) · engine submodules re-read live: OT `a4e971d`, WDC `f446512`
**Ruling basis:** ARCH-R23 D23/D24/D25 + `IMPLEMENTATION-PLAN.md` (§2 r3's row + §3 workstreams)

---

## VERIFIED-STRONG (claims that held against the live repos)

1. **The 44-effect registry** — verified byte-level: 43 `id: 'gpu-*'` effects in `effects/pipeline.ts` + `gpu-lut` in `effects/lut.ts` (the file's own comment: "Wave 3E adds: lut3d — 1 new effect bringing the registry from 43 → 44 entries"). The R22 BASE claim was exactly right.
2. **The color instruments are STILL zero engine code** — grep for scopes/vectorscope/qualifier/power-window across `src/lib/nle` finds only: (a) the FreeCut wire-schema strings `gpu-power-window` / `gpu-secondary-qualifier` in `headless/api.ts`'s `GPU_EFFECT_TYPES` (the byte-identical WIRE CONTRACT — "Wave 1 lists the same set as freecut's contract.mjs" — NOT implementations; a subtle trap now noted in the GAP row), (b) one param-metadata comment in `effects/pipeline.ts:70` ("point — 2D point (spatial editor, e.g. power window center)"), (c) false friends ("equal-power windows" = audio crossfades; "disqualifiers" = occlusion predicates). The engine's P2 backlog still lists "scopes (waveform/vectorscope)". **The S-engine row stands.**
3. **The engine's 8-bit FreeCut-baseline character** — still true @ `b8c6f88`: 46 `rgba8unorm` sites across 10 files under `src/lib/nle` (32 in the 7 gpu/-family files); no colorSpace set on the canvas; 4096 texture cap; `transitions/pipeline.ts` still `this.format = 'rgba8unorm'`. The §13D CORRECTIVE framing survives with refreshed line refs.
4. **The N1 composition-frame bridge family + W1 meter bridge landed (R22-era, per the prompt)** — both verified: `bridge/composition-frame.ts` (`buildCompositionFrame` pure math + `paintCompositionFrame` Canvas2D painter) + `bridge/realtime-engine.ts`'s real `onMeter`/`onBusMeter` push; both pinned in `tests/vitest/engine/bridge-seams.test.ts` (97 tests; N1 has two dedicated describes). The engine PLAN.md's N-queue records N1 ✅ CLOSED with the app's `ProgramCanvas` (314d926) + W1 ✅ CLOSED cross-repo.
5. **OT's placeholder compositor** — `src/lib/timeline/render/placeholder-compositor.ts` exists exactly as advertised: Canvas2D solid-color composition behind `setTracks()` + `renderFrame(t)`, M12-pixel-verified (`readCenterPixel` in `testing/milestones-view.ts` + milestones-hardening), used by the `/view` runner page. Its header explicitly names THIS spec: "The REAL renderer (nle-engine's WebGPU compositor, or the final engine per nle-core-spec 04) plugs in behind the same contract … only the painter changes." Now reflected as a BASE row.
6. **The app's ProgramCanvas** — `src/ProgramCanvas.tsx` hosts the engine's N1 seam (live core scene + D2e sidecars + hash-color resolver; jsdom pins the composition math via data attributes). 117/117. Verified.
7. **The variants' W4 mock math** — `src/lib/color/` all present (colorSpace / gradeMath / qualifierMath / scopesMath + gradedImage + index; 170 dedicated tests in the lib), spec-08-exact per spec 08's §0. The R23 W-B color wave landed at `db58d8d` and verified in code: **ScopesDock** (`components/pages/color/ScopesDock.tsx` — the four TABS: Luma WFM / RGB Parade / Vector / Histogram, ARIA tabs, one scope full-size), **nodes-in-viewer** (ColorNodeGraph owns the viewer region, D-B2), **StillsPanel** (`StillsPanel.tsx` — the DaVinci-style Gallery of clip-level grade records, `colorStills` store home), **density toggles** (`timelineCompact: 'auto' | 'on' | 'off'` in `useUiStore.ts`). Variants at 1521 (ba46cf3's own gate line; 00-master's 1470 is its pre-W-C/D/E pin-era figure — the battery's central re-baseline reconciles).
8. **The mini's CORE-SEAMS audited-absent register** — `shell-mini/docs/CORE-SEAMS.md` §4: "Color/grading instruments — S-engine scopes/qualifier (W-color binding); the mini's viewer is a single frame." The negative-result register exists and is now cited in §0 as the S-engine-scope evidence.

## FIXED (stale claims corrected in the spec)

1. **§0 BASE — the engine pin/count/vendors**: `f68ab8c` 356/356, vendors OT `3420b5f` + WDC `5570321` → **`b8c6f88` 440/440, vendors OT `a4e971d` + WDC `f446512`** (submodule status re-read live). BASE extended with the landed-since-R22 surface (N1 bridge family, W1 meter bridge, RR1-A/toSorted, N2b `37cdd28`, S3 wave, the K2 cross-module seeds: `timeline-edit-ops`/`video-sync`/`bridge-seams`/`planner` — all four files verified present).
2. **§0 BASE — the mock row**: variants "1,334 tests … Its engine binding is W-color" → `e89cd65` **1,521** + the W-B color wave enumerated (ScopesDock/Nodes/StillsPanel/density) as **the mock-side reference half of r3's parity gates**; added the OT placeholder-compositor row + the app ProgramCanvas row (the engine-binding story is now three BASE surfaces, not one).
3. **§7.1 pass-discipline note (line 543→547)** — "the reference port's two LIVE P0 bugs" is STALE: **both fixed engine-side by Wave 4A** (commit `2a2473e` "texture-pool discipline, mask invert-once"). Verified in code: the singleton `return this._pongTexture;` is GONE (`apply`/`applyToTexture` now return caller-owned per-call outputs — `pipeline.ts:5204-5206` documents the Wave 4A contract change); the cross-clip-shared mask combine textures are GONE (`getMaskResult` per-call outputs — `mask-manager.ts:647-670`, "Wave 4A (P0.2)"). Rewritten: the normative rule STANDS, the port's fix is the rule realized.
4. **§11.1 Q1 engine sentence (line 728→732)** — "29 `rgba8unorm` sites across 7 GPU files" → **46 sites across 10 files under `src/lib/nle`** (32 in the 7 gpu/-family files) @ `b8c6f88`; the engine grew, the 8-bit baseline did not move.
5. **§13D (the engine reference table)** — the header's "37,958 LOC, 124 tests" SCOUT-04 snapshot → re-verified note (**440/440, ~54,700 LOC under `src/lib/nle`**). All 12 rows' file:line refs refreshed to `b8c6f88` (scout-era refs preserved in git history): player source upload → `compositor.ts:1797` (`uploadRgba`); ping-pong → `compositor.ts:1257`; canvas format → `device.ts:79`; blend table → `compositor.ts:33`; device loss → `device.ts:67`; 4096 cap → `device.ts:59`; transitions → `pipeline.ts:116`; `lut.ts:314` + `texture-pool.ts:190` unchanged (still exact). Two rows flipped status to **PORT-FIXED (Wave 4A)** (the P0.1 singleton return; the P0.2 mask double-invert). Two notes corrected: the TexturePool is no longer "dead code" (LIVE since Wave 4A — player + mask-manager acquire/release); the player dispatch row's quote updated to `clip.type !== 'video' && clip.type !== 'image'` (Wave 4C added image; text/composition dispatched since Waves 4C/4D — sticker/graphic still skipped). The ENGINE-GAP rows (device-loss recovery, 8K cap) stand with a Wave 5C nuance (init-state reset on loss).
6. **Header status line** — re-based to R23 (the re-verify's scope + the forward-work enumeration).

## GAP FLIPS / ADDS / RE-TAGS (the §0 register, per D24)

- **RE-TAGGED — the color instruments row**: `S-engine (∥ crawl, 4-6 wk … consumed + pinned at W-color)` → **consumed + pinned at r3 (was W-color)** — dual-tagged per battery_r23's pre-fleet-close vocabulary. Owner S-engine unchanged; the ZERO-code status re-verified (with the wire-schema-string nuance documented).
- **ADDED — the grade-math binding + parity row** (r3, was W-color): the plan's r3 row is "the W4 grade-math binding + parity; the S-engine instruments consumed"; acceptance = grade-math parity pins, max delta ≤ 1 LSB-equivalent. Cross-ref: the math-side row is spec 08 §0's binding row; this row owns the renderer-pipeline surface (§5.2/§16.3 linear-light working textures + §6.2 shaders). Previously the binding existed only as a BASE-side clause ("Its engine binding is W-color") — now a first-class GAP row.
- **ADDED — the projector parity corpus row** (the retired spec-14 §4.2 runtime row, re-homed per the posture law): the N1 composition-frame family's formal S4 suite (SceneTracks→ingestion translator + the parity corpus); S-engine ∥ crawl, additive at `src/lib/nle/` (the bridge family is the seed). Still OPEN engine-side: no S4 suite / parity corpus in the engine's tree or its PLAN queue (N1 is CLOSED as a seam, but the formal parity corpus is not authored).
- **RE-TAGGED — the CPU transition renderers row**: `R-engine-p2, non-blocking` → **r6 (was R-engine-p2), non-blocking** — the tail that also carries the ShapeItem/Lottie/Subtitle/Controller surfacing (the other half of the retired §4.2 bundle; ~1,450 LOC per the engine's own P2 backlog).
- **RE-TAGGED — the register pointer**: "spec 14 §4.2 + §3.2 W-color" → `IMPLEMENTATION-PLAN.md` §2 (r3's row + the D24 mapping) + §3 (the S-engine workstream); spec 14 is the redirect stub.
- Retired §4.2 rows NOT re-homed here (owned elsewhere, verified not spec-04's): the union façade (spec 05/12's W-ops→r1 domain), N2b keyframed volume (W-audio→r2 — **LANDED** engine `37cdd28` + app `c8c875e` per the engine PLAN.md; now reflected in BASE's landed list, not a GAP), N5 real media decode (r4, user-gated — spec 15's).

## REMAINS-OPEN

1. **The color instruments** (S-engine, ∥ crawl, 4-6 wk) — zero engine code at `b8c6f88`; consumed at r3. The wire schema already ADVERTISES `gpu-power-window`/`gpu-secondary-qualifier` (FreeCut contract parity) — the instruments GAP row now documents that trap (advertised-on-the-wire ≠ implemented).
2. **The grade-math binding + parity** (r3) — the W4 math exists mock-side only; no engine binding code.
3. **The projector parity corpus** (S-engine ∥ crawl) — the N1 family is landed + pinned (bridge-seams), but the formal S4 suite / parity corpus is not authored and not yet queued in the engine's PLAN.md (the IMPLEMENTATION-PLAN's S-engine row is its only home besides this §0 row).
4. **CPU transition renderers + the P2 surfacing tail** (r6) — the engine P2 backlog still lists "CPU transition renderers (~1,450 LOC, export fallback)" and "scopes (waveform/vectorscope)"; non-blocking by design.
5. **The ENGINE-GAP rows of §13D** (unchanged character at `b8c6f88`): no automatic device-loss recovery loop (Wave 5C added init-state reset only); the 4096 `maxTextureDimension2D` cap vs the spec's 8K tests.
6. **The engine's 8-bit GPU baseline** — 46 `rgba8unorm` sites: the spec's 10-bit scene-linear corrective path (§5/§16) remains entirely forward work (r3's binding is where the correction begins).

## NOTES

- **Dual phase tags** are used throughout §0 (new D24 vocabulary + the retired spec-14 term in parentheses) per battery_r23's pre-fleet-close convention ("both eras accepted pre-fleet-close, tighten at the close").
- **Count discipline**: the spec §0 cites the live variants count (1,521 @ `e89cd65`) with an explicit reconciliation note vs 00-master's 1470 (its own R23-pin-era figure, pre-W-C/D/E) — the battery re-baselines the pin world once, centrally, after the fleet; this file does not pre-empt it.
- **Sibling cross-refs kept as-is** (mapping-note law, D23 consequence 3): `shell-mini/docs/CORE-SEAMS.md`'s audited-absent row still says "(W-color binding)" — the sibling artifact gets a mapping note, not a rewrite; my §0 cites it with the r3 vocabulary applied on the spec side.
- **The OT placeholder compositor's contract seam** (worth registering for r3): its API is `setTracks()` + `renderFrame(time)` while the spec's renderer API (§7.1) is `renderFrame(FrameDescriptor)` — the placeholder's own header claims the real renderer "plugs in behind the same contract; only the painter changes." The adapter/contract alignment is part of the r3 binding work; the BASE row records the contract verbatim so the future binding row resolves against it deliberately.
- **No git commands run, no test suites run** (per the fleet rules — counts trusted, features verified by reading code). Only `04-renderer-color.md` was edited.
- §1-§16's FreeCut/OpenCut-classic teacher-repo analysis (the SCOUT-04 material) is historical by design and preserved untouched; §17's test plan is the intended contract, no live claims.

## VERDICT

**The spec's renderer/color thesis held completely — the engine is still an 8-bit FreeCut-baseline port with zero color instruments, and the mock-side reference strengthened (the W-B wave) — but every LIVE pin, count, and engine line-ref was stale, the port's two motivating P0 bugs were fixed engine-side (Wave 4A) without the spec knowing, and the r3 binding + the retired §4.2 projector-parity row were missing from the GAP register. All fixed; the §0 triad now matches the D24 ladder and the live repos.**
