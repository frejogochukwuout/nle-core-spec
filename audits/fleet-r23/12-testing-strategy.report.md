# R23 fleet audit — `12-testing-strategy.md` (the testing-strategy spec — the K-layer's spec-side home: virtual framebuffer / pixel+audio verification / WYSIWYG / CI configs)

**Agent:** R23-fleet-12 (fresh context). **File owned:** `12-testing-strategy.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs; counts trusted per the round's numbers, every suite inventory verified by listing test files):** nle-engine `b8c6f88` (440/440), opencut-timeline `222532c` (489/489 — `download/timeline-test-report.json` parsed live), web-daw-core `494f6ff` (759/759), nle-ui `85dcf57` (648), nle-test-app `70e99f0` (117/117); in-repo: `ui-mock/shell-mini` (355, the R23 seal) + `ui-mock/shell-variants` (60 test files, live count per NOTES). Context inputs: `audits/ARCH-R23-plan-and-bridge.md` (D23/D24/D25, v2), `IMPLEMENTATION-PLAN.md` (the S-spec row's K2-census item 2), the spec-14 redirect stub, the seal docs (CORE-SEAMS / LAW-NET-INVENTORY / OT-SEAMS).

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **The OT count + the report-json authority:** `download/timeline-test-report.json` parsed → `total 489 / passed 489 / failed 0`, **51 milestone suites** (M1-M46 + the R-suffixed real-mouse set; M45=12, M46=3 present). The runner `scripts/run-timeline-tests.mjs` verified: Phase 1 scrapes the in-page `window.__TIMELINE_TEST_REPORT__` (verified exposed in `src/app/page.tsx:155/:191`) then appends **14 real-mouse suites** (M17 + 13 R-suffixed — verified via each script's `id:` literal); `/view` exposes `window.__VIEW_TEST__` (`src/app/view/page.tsx:357`, with `.core`/`.layout` handles). OT's suite = the in-page UI-over-core runner exactly as D24's K2 row describes.
- **The engine inventory:** `tests/vitest/engine/` carries 11 test files (timeline-edit-ops, video-sync, bridge-seams, planner, api-surface, load-validation, persistence, render-abort, timeline-math, transform-resolver, undo-serialize — `api-surface.frozen.ts` is the snapshot helper, not a test) + `nle-bridge.test.ts`/`nle-bridge-realtime-midi.test.ts` at the `tests/vitest/` root → **13 suites**, and `"test:vitest": "vitest run"` is in `package.json` (the vitest tier LANDED — see FIXED #8 for the §13.7 stale claim). The K2 engine-side seed set matches the task text's inventory exactly.
- **The WDC inventory:** `src/test/` = 18 test files; the D24/plan-named integration family all present (audio-integration, dsp-bounce-parity, dsp-effects-integration, dsp-null-unity, dsp-pdc, dsp-sidechain, nle-audio-core-derisk, offline-parity-wiring, real-audio-e2e, meter-tap, varispeed, fft-analyzer) + the **Goertzel detection oracle** (`lib/daw/dsp/metrics.ts`, consumed by dsp-effects-integration/varispeed tests) + the offline render — "the combined test the audio core has done" is a live, greppable family.
- **The app's K3 seed:** 4 test files (engineSeam / sceneBridge / GluedShell / audioService), 117/117; the W3 JKL audio half verified in `timeline-port/hooks/use-timeline-actions.ts` (the tap-accel ladder) + `use-keybindings.ts`; nle-ui 648 across 37 test files (boundary-script-gated per 00-master).
- **The in-repo seal census:** shell-mini = 8 test files, and a declaration-level count (`it(`/`test(`) gives **exactly 355** — the census is self-consistent. `LAW-NET-INVENTORY.md` verified: **128 census units / 355 tests / 33 HOLDS tests (13 units) / 322 app-side authoring tests (115 GAP units)**; the testid census (§2.2) is **60 static source-side, of which 59 are APP-EMITTED (the 60th is harness-only) + 15 templated families** — the DOM-structural gate checks the 59 + 15. `CORE-SEAMS.md` = S1-S26 + the store partition (§2) + the transport map (§3); `OT-SEAMS.md` = 14 rows.
- **The VLM net (the R23-G visual-verification tooling):** `ui-mock/shell-variants/scripts/vlm-capture.mjs` / `vlm-review.mjs` / `vlm-run.sh` (+ `vlm-rubric.md`) all exist — the K3 visual net's adoption source is real, referenced from the variants' README/DESIGN-R20 (VLM PASS ×4 rounds, 102 stories).
- **The methodology body (§1-§11, §15-§18):** no live fleet claims beyond §0 — the SCOUT-12 Q&A/corrections/scripts/CI sections are historical-investigation records (kept per the round's rules); the three-tier methodology's current state = EXECUTING (K1 all green at the R23 pins), which the re-based §0 now states.

## FIXED (edits applied to `12-testing-strategy.md`)

1. **Status header:** the R22 framing → the R23 re-baseline: the D24 K1-K4 vocabulary declared as this spec's crawl verification tiers (the spec-side home), **the K2 combined-family census + registration named as THIS file's (and 17's) K2-entry deliverable**, the R23 count set, the C/W/R→K/w/r re-key, the spec-14 retirement + §4-row re-home. R22 kept as the supersession record.
2. **§0 BASE fleet row — the count re-pin (charge a):** `356/459/740/640/83 + variants 1,334` → **engine 440/440 @ `b8c6f88` (13 suites, enumerated) / OT 489/489 @ `222532c` (359 in-page + 130 real-mouse, 51 milestone suites, report-json authority, the runner mechanics) / WDC 759/759 @ `494f6ff` (18 suites, the integration family + Goertzel oracle) / nle-ui 648 @ `85dcf57` (37 files) / app 117/117 @ `70e99f0` (the 4 suites + W3 JKL/RR1-B) / mini 355 (8 files/128 units) / variants 1,542-live (60 files, the 00-master reconciliation note)**; consumer pins re-read live (engine consumed @ `4ef0147` behind — the re-pin wave queued; OT mirror @ `a4e971d` + the engine's OT submodule also `a4e971d`; WDC @ `f446512`; nle-ui @ HEAD).
3. **§0 BASE rows ADDED (charge e):** the R23 seal artifacts (CORE-SEAMS S1-S26 + LAW-NET census = K3's acceptance list) + the VLM net (the R23-G tooling) — the new upstream work now reflected spec-side.
4. **§0 tier map ADDED (charge e):** "The D24 verification ladder, spec-12-side" — K1 module nets / K2 combined nets (the seeds enumerated) / K3 app-behavior nets (the four sub-nets + the D25 ordering law) / K4 the crawl exit gate / the walk-run depth note (the [H:reason] registration + the r-phase pointers). The K-ladder now has its spec home in the testing strategy, as directed.
5. **§0 GAP register — the K2 row ADDED (charges b/c, THE row):** the combined-family census + registration (owner nle-core-spec, at K2 entry): the named family = engine-in-the-middle 4 + WDC's integration 4 (+ the offline-parity/real-audio pair) + OT's in-page runner; the missing pairs (OT-ops→engine-decode round-trips at the app's seam shapes, S-engine's worklist); acceptance = the census green + registered in spec 12/17 §13A.7 + battery-checked thereafter.
6. **§0 GAP register — three more rows + the re-tags (charges b/d):** the corpus row C4→**K3** (the D24 C4 split: corpus-check = K3, e2e = K4, side-by-side = w1-entry; D25.5's owner placement; the DOM-structural-after-swap ordering law; the composed ripple family 4 units/16 tests included); **the K3 offline audio pins row ADDED** (was C3's substance, now explicit — the Node-venue law + the S-wdc waveform-contract prerequisite); **the K3 VLM visual net row ADDED** (adopt-not-fork, the app's .storybook + OT's runner); **the K4 automated e2e row ADDED** (import→cut→play→export, zero mock paths — the crawl's completion proof; w2's re-gate note); W-audio→**r2**, W-color→**r3**, W-n5→**r4** (D6 pending), R-fcpxml→**r5** — all dual-tagged ("was X", the transitional form the round allows).
7. **§0 ACCEPTANCE & TEST PLAN:** the battery posture R22→R23 (battery_r23's key set: b8c6f88/440, 222532c/489, 494f6ff/759, 85dcf57/648, 70e99f0/117 + consumer pins); the K2/K3/K4 tier-gate pointers; the spec-17 §13A.7 pointer annotated as fleet-re-baselined (the sibling 17 round owns it).
8. **§13.7 (the engine reference table):** the LIVE stale claim fixed — "nle-engine's test reality is a single-tier, in-app harness (124 tests, No Vitest, no test script — ENGINE-GAP)" was true at SCOUT-12 and false at HEAD; the intro now scopes the table to the SCOUT-12 era (history kept) + an R23 annotation records the landed vitest tier (13 suites, 440/440, the K2 seeds, the legacy harness page). The table's rows themselves stay as the historical SCOUT record.
9. **The testid census wording:** "60-static + 15-templated" → "59-app-emitted-static + 15-templated (the 60th static id is harness-only — LAW-NET §2.2)" — matching the seal doc's own gate wording and D24's "59-emitted-static" phrasing.
10. **§0 heading:** "R22 posture" → "the R22 posture, R23-re-based" (the triad preserved; nothing else in §0's structure moved).

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **The K2 combined-family census + registration:** the ROW now exists (this round — the S-spec deliverable is registered); the census TEXT itself lands at K2 entry (the plan's S-spec item 2) and the **missing cross-module pairs (OT-ops→engine-decode round-trips at the app's seam shapes) are unstarted** — S-engine's worklist item.
- **The K3 corpus (322 authored / 115 units):** unstarted; the store/policy halves are unblocked NOW (∥ the D25 bridge, against the injection-point contracts), the DOM-structural half waits on the D25 swap + OT's testid layer.
- **The K3 offline audio pins:** blocked on S-wdc's waveform seam contract (the only K3 sub-half with an external prerequisite).
- **The K3 VLM net port:** the tooling exists (variants); the adoption onto the app's .storybook + OT's runner is unstarted (S-app item 5).
- **K4 the automated e2e:** does not exist as one test (the crawl's completion proof — authored after the swap, S-app item 4).
- **r2/r3/r4/r5:** the offline-parity null rig, the grade-math parity pins, the real-file decode corpus (user-gated, D6 re-affirmation pending), the FCPXML fixture corpus — all unstarted, now r-tagged.
- **Spec 17's §13A.7 re-tier row** still carries the R22 counts (356/459/740/640/83/1334) — the 17 fleet round owns that re-baseline; this spec points at it with the re-baseline annotation rather than pre-empting it (out of this file's edit scope).

## NOTES

- **The OT in-page/real-mouse split:** the fleet-standard **359 + 130** (ARCH-R23 F4; used by 00/03/05/06/09 and this round's task text) is kept in the spec for cross-file coherence. My independent re-derivation — the runner appends exactly 14 real-mouse suites (M17 + 13 R-suffixed, verified by each script's `id:` literal) summing to 129 tests, leaving 360 in-page — gives the same ±1 the 05/06 reports flagged; the TOTAL (489) is the count-discipline number and the report json is its authority. Immaterial to every claim.
- **The variants count:** three figures circulate — 00-master's pin-canon **1,470** (its R23-pin-era figure), the task text's **"1521+"**, and the 08 fleet report's **1,542/1,542 @ `acb2cf9`** (verified as the last variants-code commit's gate line). My declaration-level grep gives 1,539 (±3 — grep undercounts chained `it(`/`test(` forms). The spec declares **1,542-live with the 00-master reconciliation note**, mirroring the 04/07/08 convention; the battery's central re-baseline reconciles — this file does not pre-empt it.
- **The nle-bridge pair's location:** the task text's inventory grouped `nle-bridge`/`nle-bridge-realtime-midi` under `tests/vitest/engine/`; they actually live at the `tests/vitest/` root. The spec states the actual layout (13 suites, both directories named).
- **The spec-14 §4 recovery method (charge c):** this round ran NO git commands (per the rules — stricter than 06's read-only `git show`). The testing-domain rows were recovered from the D24 mapping table (ARCH-R23), the spec-14 stub's §-redirect table, and the seal docs; the D24 table is the authoritative re-homing map and every testing-domain row it names is now present in the §0 GAP register: the pre-C1 status flip (LANDED → K3's acceptance list), C3→K3 (the audio pins row), the C4 split (K3 corpus + K4 e2e + w1-entry side-by-side), W-audio/W-color/W-n5/R-fcpxml → r2/r3/r4/r5, plus the plan's K2-census row (new, not a spec-14 row). The C1(b,d,e,f) rows are the interaction domain's (spec 05/16 own the acceptance; this §0 carries the census/DOM-gate context in the corpus row — the same closing-pointer convention 06 used).
- **Battery/test discipline:** no battery runs, no test-suite runs, no git commands; all counts read from the report json, file listings, package.json scripts, and grep-verified inventories.
- **§0's structure preserved:** the BASE / GAP / ACCEPTANCE & TEST-PLAN triad (the R22 posture law) is intact; all historical contexts (SCOUT-12 Q&A, §13's /tmp-path teacher references, §14 corrections, §15-§16 scripts, the R22 lineage) untouched.
