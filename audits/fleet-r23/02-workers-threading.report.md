# fleet-r23 audit report — 02-workers-threading.md

**Agent:** R23-fleet-02 (spec-file auditor, fresh context). **Date:** 2026-09-07.
**File audited:** `nle-core-spec/02-workers-threading.md` (the workers/threading model spec).
**Pins verified against (live repos):** engine `b8c6f88` (440/440, tsc 0 — trusted count; features read) · OT `222532c` (489) · WDC `494f6ff` (759/759) · nle-ui `85dcf57` (648) · app `70e99f0` (117/117). Engine's vendored submodule pins at HEAD (read-only `git ls-tree`): OT `a4e971d`, WDC `f446512`.
**Context read first:** ARCH-R23-plan-and-bridge.md (D23/D24/D25), IMPLEMENTATION-PLAN.md, the spec file in full, the retired spec-14 stub + its v2.1 §4 registers (read-only `git show 7ba030a`).

---

## VERIFIED-STRONG (BASE re-verified at the live HEADs — every worker/threading claim checked by reading code)

1. **The audio-thread half of the spec is genuinely landed fleet-wide.** Verified in code:
   - **Engine** (`b8c6f88`): `src/lib/nle/audio/soundtouch-processor.worklet.ts` — the `nle-pitch-shifter` AudioWorklet (Blob-URL source-string load, 1-in/1-out, k-rate `pitchRatio` 0.5–2.0, always-returns-true, silence-on-no-input) consumed by `realtime-bridge.ts` (`ensurePitchShifterLoaded`/`createPitchShiftedSource`, the H8 insert seam, disposed with the segment). **W2 maintainPitch**: `bridge/segment-strip-adapter.ts` imports `retimeChannels`/`RETIME_RATE_MIN`/`RETIME_RATE_MAX` from `@/lib/daw/varispeed` (the vendored WDC), the three-site schedule law, the byte-capped 96 MB LRU retime span cache, the out-of-domain legacy-pair fallback (b7c7c2e); pins `W2a–W2k` in `tests/vitest/nle-bridge.test.ts` (incl. W2d the offline vertical — 440 Hz not 880 at 2×). **W1 meter bridge**: `realtime-bridge.ts` `syncTimeline` → `engine.reattachBusMeters()` (the wireBuses sever-all restore), `nle-bridge.test.ts` §"W1 meter taps on the realtime handle" (onMeter/onBusMeter, unknown-bus honest no-op, W1n reattach law). **H16 drift correction**: Player's `getScheduledClipSourceTime` + buffer-relative hard seek (`player.ts:3285-3324`). **N2b**: `gainAutomation` threaded player→bridge→adapter (`player.ts:3276-3294`, the chunk-relative fold; the adapter's dedicated automation gain node; the 2 REAL offline-render sample-level parity describes).
   - **WDC** (`494f6ff`): `lib/daw/soundtouch.ts` (682 LOC, the typed LGPL WSOLA port) + `lib/daw/varispeed.ts` (529 LOC — `VARI_SPEED_WORKLET_SOURCE` string-eval'd `daw-varispeed` processor with set-tempo/set-pitch/set-rate/flush port messages, all validated ≥ 0.01; `loadVarispeedWorklet` Blob-URL loader idempotent per ctx, never rejects; `retimeChannels` F1 flush law; the [1/32, 32] rate domain; degenerate-setter guards). `lib/daw/meter-tap.ts` (327 LOC — the tap loop, split-dispose law, the CR-P1 re-entrancy chain token, the offline guard). The worklet-serving surface: `public/worklets/{dsp-effects-worklet,biquad-processor,capture-worklet}.js` + `DSP_WORKLET_URL_CANDIDATES` root-base-first law + the in-flight addModule episode/bounded-retry/unity-passthrough fallback (`effects.ts:136-199`). The LGPL shipping obligation landed (COPYING.LESSER + THIRD-PARTY-NOTICES in `files`, CR round B).
   - **App** (`70e99f0`): `audioService.ts` consumes strip `onMeter` + engine-pushed master/bus taps; `sceneBridge.test.ts` W2 wiring pins (`el.speed` → `retime {rate, maintainPitch: true}` → varispeedRate + maintainPitch); the W3 JKL audio half (3f351bc — rate-change reschedule, one idempotence key, composed varispeedRate = segRate × tr, silent-reverse J); RR1-B (70e99f0 — the speed-identity hole, el.speed = retime.rate even at 1).
2. **The web-worker half is genuinely absent — re-verified by grep at `b8c6f88`:** zero `new Worker(`, zero `ManagedWorker` in the engine (only comment mentions of FreeCut's workers in `mixer.ts`/`export/index.ts`/`video-sync.ts`). OT and nle-ui: zero worker/worklet code (grep-verified). The engine's export path is main-thread by design (Stage 3, `gaps/audit/C-export-encode.md`: "worker offload w/ main-thread fallback (worker + managed-worker + render-pipeline port ~520 LOC)" — the §8.5 fallback family documented there matches the spec's).
3. **Spec-14 §4 register sweep for this domain (charge c):** read all six registers. Workers/threading-owned rows: §4.2 "CPU transition renderers; ShapeItem/Lottie/Subtitle/Controller surfacing" (→ present in §0, re-tagged) + N5 decode (→ present, rides r4); §4.3 "SoundTouch offline pitch" (→ LANDED as W2, reflected in BASE) + "Worklet asset serving (3 files, `/worklets/` root-base-only law)" (→ **was MISSING from this spec's §0 — a posture-law violation; ADDED**). §4.1/§4.4/§4.5/§4.6 carry no worker surfaces (the §4.5 media layer is spec 15's W-media/w2 row; this spec's decode-worker row already rides it via r4).

## FIXED (the stale/overclaimed LIVE claims — 11 BASE-class fixes + 6 GAP-class edits + 5 new-content notes)

1. **Status header**: R22 re-baseline language ("the worker laws landed + tested in the engine's 356/356 @ f68ab8c") → the R23 re-baseline with the honest audio-thread/web-worker split.
2. **§0 BASE engine**: `f68ab8c` 356/356, vendors OT `3420b5f`/WDC `5570321` → `b8c6f88` 440/440, vendors OT `a4e971d`/WDC `f446512`. **The R22 parenthetical "(§4 ManagedWorker/pool/session, §5 memory discipline, §7 AudioWorklet, §8 per-worker specs) are in-suite at this pin" was an OVERCLAIM** (§4/§5/§8 do not exist in the engine) — replaced with the precise landed inventory (§7's fleet-real form + W2/W1/H16/N2b) and the explicit "§4/§5/§8 UNIMPLEMENTED — zero `new Worker(`".
3. **§0 BASE WDC line ADDED** (the audio-thread DSP half of this spec is WDC-owned — W2 + W1 + the worklet-serving surface + the parity pins).
4. **§0 BASE app**: `e662759` 83/83 → `70e99f0` 117/117; the consumption proof extended from "crossfades + the WDC audio host" to the four-fold proof (host + W1 meters + W2 varispeed + W3 JKL/RR1-B).
5. **§0 GAP header**: "owner + phase per spec 14" → "per the D24 ladder, IMPLEMENTATION-PLAN.md §2 (the spec-14 vocabulary retired at R23)".
6. **GAP re-tags (charge d, dual-tag form)**: R-engine-p2 → **r6**; W-n5 → **r4** (user-gated D6); the new worklet-serving row tagged **K3** (was C3).
7. **GAP adds (3)**: worklet asset serving (K3 — the posture-law fix); export-render worker offload (r6 — engine Stage 3, was only in the engine's own gaps doc); the async pre-retime queue (r2 — NEW at these pins, see REMAINS-OPEN).
8. **§0 ACCEPTANCE & TEST PLAN**: the R22 battery's 356/356 posture check → the R23 battery (440/440, 759/759, 117/117) + the landed surfaces' own pin families enumerated.
9. **§3 zero-worker note**: re-verified annotation (`b8c6f88`, 2026-09-07) + the scoping correction (the corrective line is about the web-worker half; the audio-thread half is landed).
10. **§7.1 R23 fleet-reality note (NEW)**: the W2 in-fleet implementation (WDC soundtouch/varispeed; the engine consumes the offline `retimeChannels` form, not the worklet; the parity contract pinned; the remaining sync-pre-retime gap pointed at the §0 row).
11. **§7.2 R23 fleet-reality note (NEW)**: the two landed worklets are BOTH 1-in/1-out input-bus Blob-URL designs (engine `nle-pitch-shifter`; WDC `daw-varispeed`) — neither implements the 0-input + append-source port-push target; the target stands for the §7.1 preview surface.
12. **§13B preamble**: the stale "37,958 LOC, 124 tests" → 440/440 @ `b8c6f88` + the vendor pins.
13. **§13B row fixes**: §3 row re-verified annotation; §8.1 cite `player.ts:1059` → **`:1629`**; §11 Q12 cite `:81` → **`:83`** + the "all 9 mentions are comments only — zero imports" claim corrected (the MEDIA path is still comment-only, but the engine's EXPORT path genuinely imports mediabunny — `export/orchestrator.ts:232,822`, `export/settings.ts:302,332`, the engine's only runtime dep); §7.1/§7.2 rows amended with the W2 half-supersession.

## REMAINS-OPEN (the §0 GAP register at the R23 pins — 5 rows, all with owner + phase + acceptance)

1. **Worker-pool surfacing for the P2 engine features** — r6 (was R-engine-p2) — owner engine, non-blocking.
2. **The §8.1 decode worker** — rides r4 (was W-n5) — PENDING the user's D6 re-affirmation.
3. **Worklet asset serving** (K3, was C3 — the re-homed spec-14 §4.3 row): the app serves WDC's 3 worklet bundles from `public/`; today the app maps inserts (INSERT_MAP in `audioService.ts`) but serves NO worklets — verified `public/` has no `worklets/` dir, so the worklet-backed inserts (compressor/limiter/stereo-widener) silently run the unity-passthrough fallback in the browser venue. Acceptance: the 3 files at the root-base path + a K3 browser pin proving real DSP, not passthrough.
4. **Export-render worker offload** (r6): the engine's export is main-thread by design (Stage 3); acceptance = the ~520-LOC port per the engine's own gaps doc.
5. **The async pre-retime queue** (r2, NEW): the adapter's maintainPitch pre-retime is a synchronous O(span) WSOLA compute on the play edge — a 3-min stereo span at rate 0.5 ≈ seconds of main-thread jank (the WDC HANDOFF's own item 3; the fix is specced in `docs/design-w2-varispeed.md` §4: async pre-retime + legacy-fallback-while-pending). This is the purest live instance of this spec's §2 thesis ("what breaks if main-thread") inside the landed fleet — registered with owner (nle-engine adapter, design contract WDC) + acceptance (the jank pin).

**Out-of-domain notes (verified, not this spec's rows):** WDC's drift-gate law (`sync --check` fails-on-drift, de09c93) is a vendoring-hygiene gate, not a workers/threading law — it lives with the vendoring discipline (00-master/plan §5); noted here so the round's "new upstream work" checklist is complete. N2b/RR1-A's spec-side homes are 01/20 — this spec carries only their audio-thread surfaces. The K3 audio pins' S-wdc waveform-seam hard prerequisite (plan §4) is spec 12/17/20-adjacent; when the waveform WORKER (§8.3) lands at r4 it should consume that contract.

## NOTES (method + judgment calls)

- **The R22 §0 overclaim is the round's material finding**: the claim that "§4 ManagedWorker/pool/session, §5 memory discipline, §8 per-worker specs are in-suite" was false at f68ab8c and is false at b8c6f88 — the engine has zero web-worker code by design (D5 procedural media). The fix keeps the R22 posture-law triad intact while making the BASE honest (the audio-thread half landed; the web-worker half unimplemented, deferred r4/r6). No historical sections (§1–§14, the FreeCut/OpenCut-classic scout corpus) were touched — they are teacher-reference material, still accurate.
- **Read-only git usage**: `git log`/`git ls-tree`/`git show` on the engine/app/spec repos for pin verification and the retired spec-14 §4 register (charge c requires knowing the retired rows; the stub points at git history `7ba030a`). No state changes, no commits, no branch operations; no test suites run (counts trusted per the rules).
- **The dual-tag convention** ("r6 (was R-engine-p2)") applied per the D24 mapping; transitional form chosen since sibling artifacts (CORE-SEAMS, LAW-NET) still cite the C/W/R vocabulary.
- **The app's inserts-passthrough discovery** (GAP row 3) is the audit's actionable finding for the K3 audio half: it makes the spec-14 §4.3 row concrete (not just "copy 3 files" but "prove the real DSP runs") and it hard-depends the K3 audio pins on it.
- **Battery follow-up (for S-spec, not this file):** battery_r23's posture checks should assert the new §0 pin set (b8c6f88/494f6ff/70e99f0 + the vendor pins a4e971d/f446512) and the 5 GAP rows' phase vocabulary (r6/r4/K3/r2 — the D24 set, not the retired C/W/R).
