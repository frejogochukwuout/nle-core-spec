# R23 fleet audit — `15-wire-protocol.md` (the JSON wire protocol — the `EngineCommand`/`CommandResult`/`EngineEvent` contract)

**Agent:** R23-fleet-15 (fresh context). **File owned:** `15-wire-protocol.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs; counts trusted per the round's numbers, every FEATURE verified by reading code):** opencut-timeline `222532c` (489/489), nle-engine `b8c6f88` (440/440), nle-test-app `70e99f0` (117/117); the in-repo CORE-SEAMS S1/S10 seam context, `IMPLEMENTATION-PLAN.md` §2 (the r1 row: error-envelope FIRST, C7 at END), the 05/06 re-audited §0 rows (the coherence targets), and the retired spec-14 §4.1/§4.2 wire-domain rows (recovered read-only from git history) as the posture-law inputs.

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **HEADs + counts:** OT `main` = `222532c`, engine `b8c6f88`, app `70e99f0` (read-only). Fleet split kept as **359 in-page + 130 real-mouse across 51 milestone suites** (the 05/06-standard phrasing; the report json the count authority). App `vendor/nle-timeline/UPSTREAM.lock.json` → `upstreamHead: a4e971d` (the S3 seam pin — the mirror class).
- **The 30-name wire census (the round's core charge):** `headless/api.ts` carries **exactly 30 `applyInner` case arms** (:399-1505; 23 `timeline.*` + 7 `track.*`), with an exhaustive `default: never` guard at :1496-1504. Git archaeology confirms the census lineage: **24 @ the R15 pin `0412e41` → 28 @ the R22 pin `05584d8` → 30 @ `222532c`**. The growth 24→30 is exactly 6 names: `track.toggleLock` (S2, `1bda51e`) + the S1 transport trio `timeline.setPlaybackRate`/`setLoopRegion`/`advancePlayhead` (`6dc7004`) + `track.setAllLocked` (D-T3/M44, `eb2be3d`) + `track.setAllMuted` (S3 seam/M46, `a4e971d`). **BONUS FINDING: the R22 spec's "24/78 command coverage" was already stale at its own pin — 28 actual @ `05584d8`** (the S-round had landed `toggleLock` + the transport trio before that pin).
- **S3 LANDED (M46 @ `a4e971d`):** `setTracksMuted` at `ops/timeline-core.ts:2108` (the mute-all batch — the `setTracksLocked` D-T3 twin at `:2060`: ONE `withUndo` history entry, the every-track-at-target NOOP predicate both directions, the `"muted" in track` vacuous-at-target family law, not lock-gated) + the wire verb `track.setAllMuted` at `api.ts:1385` (boolean validation → INVALID_PARAMS; the A9 `ok:true` + `data:{changed, muted}` echo — never a failure code for the idempotent set, so `applyBatch` never aborts) + M46's 3 pins in `milestones-s3seam.ts`.
- **The F1 wire/echo laws ALL LANDED, each verified in source:** F1B-2 update-patch no-overlap → `CONFLICT` (`api.ts:202-272` — "the no-overlap invariant (spec 15) is enforced on the wire for insert, move, AND update"); **F1B-4 malformed/null/array params → `INVALID_PARAMS` (`api.ts:277-294` — "16/16 commands reported the wrong code" before)**; F1B-5 `setLoopRegion` `clear:false` never clears (`:1052-1065`); F1B-6 negative trims/starts rejected (`:871-881` for update patches; insert validates at `:509-534`); **F1B-7 failed `applyBatch` preserves the pre-batch redo stack (`api.ts:1542-1551` — `truncateRedo(redoPushed)` + `markTransactionRolledBack()`, transactions suspend per-commit future clears)**; **F1C-4 the strict wire-echo assert (`milestones-tround.ts:234` — a dropped `data.changed` payload FAILS the pin)**.
- **The S2 lock pre-checks: 10 commands emitting `TRACK_LOCKED`** — 8 `lockPreCheck` sites (move/trim/split/delete/rippleDelete/duplicate/updateElements/upsertKeyframe) + inline checks on `timeline.insert` (:414) and `track.remove` (:1476). **The `CommandResult.code` union is now 6 codes** (`api.ts:153-167`): INVALID_PARAMS/NOT_FOUND/CONFLICT/NOOP/**TRACK_LOCKED**/INTERNAL_ERROR — TRACK_LOCKED is an exact-match code already in §6.3's list.
- **The N5 loop invariant LANDED (S1 + F1B-5):** `api.ts:1094` — `endTicks > startTicks` else INVALID_PARAMS, non-negative finite ticks, `clear:true`-only. The §13.15 SetLoop row flipped CORRECTIVE→**ALIGNED** (r1 residual = the C7 fold only).
- **The insert landed-time echo:** `api.ts:552-562` — `data.startTime` returns where the element ACTUALLY landed (the main-track zero-anchor clamp); the §13.15 row's "spec's data should carry it" note is now landed OT-side.
- **The engine @ `b8c6f88`:** the 19-op JSON-RPC dispatch re-verified — `applyOp` at `src/lib/nle/headless/api.ts:785`, **still exactly 19 case arms** (addText…setTransform; the D12.2 INTERNAL-transport classification stands); the engine's own frozen-surface discipline live (`tests/vitest/engine/api-surface.frozen.ts` + `api-surface.test.ts`, the 453-name freeze + `gen-api-surface.mjs`); **N2b keyframed volume LANDED @ `37cdd28`** (git-verified: "the volume keyframe lane drives REAL audio end to end").
- **The app consumer side @ `70e99f0`:** the vendored OT core (`@vendor/timeline` imports across the `timeline-port` tree + `engineService`/`sceneBridge`/`audioService`), the **D28 edit-command dispatch** (`engineService.ts:204+` — store action → core-method dispatch → the GluedShell mirror store; consumes `core.setTracksMuted`/`setTracksLocked` at :169/:161 directly, pre-C7 op-level), the Wave-B S-round seam consumption (worklog:312), W3 JKL.
- **The §4.1A 78-member census arithmetic re-verified** (Timeline 17 + Track 7 + Playback 5 + Project 7 + Scene 4 + Bookmark 4 + Media 2 + Selection 4 + Marker 3 + Effect 5 + Mask 4 + Transition 3 + Keyframe 4 + Clipboard 3 + Undo/redo 2 + Snapshot 1 + Export 3 = 78 — the table's rows still sum correctly after the re-tag).
- **The §13.15 form-delta rows re-pinned at HEAD:** group-move `PlannedElementMove` at `:75-80`; the element-toggle engine ops at `timeline-core.ts:1341/:1385` (the spec's old `:906/:950` refs were stale — now matches 06's §0); the keyframe batch ops at `:1567/:1662/:1902` (old `:1210/:1437` stale).

## FIXED (edits applied to `15-wire-protocol.md`)

1. **Status header:** the R22 framing (S-round shape @ `05584d8`, app @ `e662759`, gap = C7/error-envelope/staircase) → the R23 fleet re-baseline (OT `222532c` + the **30-name census** + app `70e99f0` + engine `b8c6f88`; the gap = C7/error-envelope/union-façade → r1, staircase → w2).
2. **§0 BASE OT row:** `05584d8` 459/459 (329+130, 14 phases) → `222532c` **489/489 (359 in-page + 130 real-mouse, 51 milestone suites; report json the authority)** + the **30-name census with the full 6-name growth enumeration** (flagging that the R22 "24/78" was stale at its own pin — 28 @ `05584d8`) + **S3 LANDED** (M46 detail) + **the F1 hardening LANDED** (F1B-2/4/5/6/7 + F1C-4 + the S2 10-command lock pre-checks, each named) + the **27/78 union-counterpart count + the 3 OT-only verbs** (`advancePlayhead`/`setAllLocked`/`setAllMuted`).
3. **§0 BASE app row:** `e662759` 83/83 (Wave B) → `70e99f0` **117/117** + the vendor-mirror pin (`a4e971d`) + the D28 dispatch + the GluedShell mirror + the op-level batch-verb consumption.
4. **§0 BASE rows ADDED:** the engine row (`b8c6f88` 440/440 — the 19-op INTERNAL dispatch, the api-surface freeze, N2b LANDED) + the in-repo CORE-SEAMS S1/S10 row (S10 = the error-code→toast seam the r1 envelope feeds).
5. **§0 GAP register — re-tagged per D24 + the retired spec-14 rows re-homed (charges b/c/d):** C7 → **r1 END** (30 names; the one-business-day app migration + keymap-sync sub-gates named); error-envelope → **r1 FIRST** (per the plan's §2 r1 row) + the R23 coverage annotation (TRACK_LOCKED exact-match live, NOOP live, F1B-4's boundary law, F1B-2's CONFLICT — the coarseness remains); **ADDED the union-façade row** (r1; the retired spec-14 §4.2 row re-homed; the frozen-surface gate as acceptance); **ADDED the element-level `toggleElementMuted`/`toggleElementVisibility` wire-verbs row** (r1; the retired spec-14 §4.1 row re-homed; 06's §0 carries the ops-side half); the event staircase → **w2** (was W-media); routing-disposition verification → **r1**. The stale "per spec 14; the register is spec 14 §4.1" header → the D24 ladder + `IMPLEMENTATION-PLAN.md` §2 pointer.
6. **§0 ACCEPTANCE & TEST PLAN:** OT 459/app 83 → **OT 489 (359+130) + app 117 + engine 440**; the K1 regression role.
7. **§4.1A (preserved, battery-checked — updated in place):** the **R23 live reading** added to the header paragraph (30 census; 27/78 counterparts; 3 OT-only verbs; the dual-tag law stated); the **phase column dual-tagged per D24** on every row (A2→r1, A2.5→r1 wave 2, A3-media→w2, A3-selection→K3, A5→w3, A7a→r1 tail, A6→r5); the `setRate`/`setLoop` row notes **the S1-landed transport verbs**; the `toggleTrackLock` row notes **its S2-landed counterpart**; the element-toggle row's line refs re-pinned (`timeline-core.ts:1341/:1385`); the keyframe row's batch-op refs re-pinned (`:1567/:1662/:1902`); the closing reading paragraph gains the R23 reading (27/78; the façade = the r1 GAP row).
8. **§7.1A (preserved, battery-checked):** an **R23 refinement note added** — OT's landed F1B-7 law (transactions suspend per-commit redo clears; `truncateRedo` truncates only the rollback's own entries; the pre-batch redo stack survives a rollback) is the **controlling reading** of invariant 3's R8-era "redo stack cleared after rollback" letter.
9. **§13.15 (the C7 worklist — the charge-c centerpiece, updated to 30 names):** header → "re-refreshed Round 23 (the 30-name census @ `222532c`)"; the pin column → `222532c` with the **24/28/30 pin lineage** recorded; the **rename pass extended 24→30** (the 6 growth names: `toggleLock`→`toggleTrackLock`, `setPlaybackRate`→`setRate`, `setLoopRegion`→`setLoop`, + **3 C7-fold DECISION rows** for the union-less verbs `advancePlayhead`/`setAllLocked`/`setAllMuted`); every stale line ref refreshed (types `:41-151`, split `:734`, trim `:693`, insert `:401` + the landed-time echo `:552-562`, error codes `:153-167`, readouts `:1566-1588`, `applyBatch` `:1518-1562`, `rippleDelete` `:799`, `setLoopRegion` `:1051`, group-move `:75-80`); the **error-codes row → 6 codes** (TRACK_LOCKED absorbed exact-match; F1B-4 noted; the coarseness = the r1 FIRST refinement); the **coverage row → 27 of 78 + 3 OT-side verbs + 51 absent**, re-tagged r1, re-pointed from "spec 14 §4.1" to spec 06's §0 register; **the SetLoop N5 row flipped ALIGNED** (LANDED S1 + F1B-5); **a §9-events row ADDED** (the staircase register pointer — OT emits no `EngineEvent` union today); the binding convergence statement re-tagged (the façade at **r1**, the audio rows at **r2**, the C7 fold at **r1 END**).
10. **§13.14 (engine table):** an **R23 pin-drift note** added (the pin moved `f68ab8c`→`b8c6f88`; the 19-case dispatch re-verified at `:773-785`; the R7-era line refs drift accordingly; the api-surface freeze named as the r1 façade's gate). The table itself untouched (historical R7 citations).
11. **§9.5 (preserved, battery-checked):** a one-sentence R23 re-tag note appended (the full staircase = the **w2** GAP row; the GluedShell mirror-notify convention as the live seed) — the register's Round-15 text otherwise verbatim.
12. **The remaining "spec 14 §…" cross-refs re-based** (2 sites: the §0 GAP header; §13.15's coverage row) — the corpus sweep's file-local half closed.

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **C7 rename (r1 END + the two sub-gates):** all 30 prefixed names still on the wire; **NEW this round — the 3 union-less OT verbs (`advancePlayhead`/`track.setAllLocked`/`track.setAllMuted`) need a C7-fold DISISION** (absorb as OT-internal seam verbs, or add `advancePlayhead`/set-all union members) before the rename pass can be exhaustive. Registered as §13.15 decision rows.
- **Error-envelope coarseness (r1 FIRST):** only the classification half is BASE-covered (TRACK_LOCKED exact-match, NOOP, F1B-4's INVALID_PARAMS boundary, F1B-2's CONFLICT); `NOT_FOUND`/`CONFLICT`/`INVALID_PARAMS` remain undifferentiated vs the ~24 fine-grained §6.3 codes.
- **Union façade (r1):** the engine's wire surface is still the 19-op INTERNAL JSON-RPC dispatch; no 78-union service slice exists; the frozen api-surface gate awaits its first conscious union-façade re-freeze.
- **Element-level toggle wire verbs (r1):** absent from the 30-member union (ops live at `timeline-core.ts:1341/:1385`; routed via `updateElements` patches).
- **Event staircase at full scope (w2):** OT emits no `EngineEvent` union — only `core.subscribe()` readouts + the app mirror; the §9.5 mapping register is unauthored.
- **Routing-disposition verification (r1):** implemented §4.1A rows still cite no module pin SHAs; the DEFERRED families still dispatch nothing (nothing to dispatch yet).
- **The r1 op-family ports (spec 06's rows, mirrored in §4.1A's Timeline op-port rows):** roll/slip/slide/rateStretch + wave-2 retime/freezeFrame/rangeRemoval still OT-absent.

## NOTES

- **The briefing's "TRACK_MUTED wire code" is a misremembering — recorded accurately in the spec:** there is NO `TRACK_MUTED` error code anywhere in OT (grep-verified). S3's landing is the `setTracksMuted` op + the `track.setAllMuted` verb returning the **A9 `ok:true` + `changed` echo** (an idempotent set must never be a failure code — that's the design). The only NEW wire error code since R15 is **`TRACK_LOCKED` (S2)**. The spec's §0/§13.15 text states the truth.
- **The 05/06 §0 growth enumerations under-specify the 6th name:** both say "the family grew with the S1 transport trio + `track.setAllLocked` + `track.setAllMuted`" (5 items) while claiming the 24→30 total — `track.toggleLock` (S2) is the missing 6th (it was already live at the R22 pin, which is why `05584d8` counts 28, not 25+... the totals agree at 30; only the enumeration is short). My §0/§13.15 state the full 6-name growth; no sibling edit needed (their totals are right).
- **The counterpart arithmetic (27/78):** 24 R15-mapped (23 union-mapping 1:1 + the `rippleDelete` wrapper) + `toggleLock`→`toggleTrackLock` + `setPlaybackRate`→`setRate` + `setLoopRegion`→`setLoop` = 27 union members with live OT wire counterparts; the 3 union-less verbs sit outside the 78 until the C7-fold decides.
- **§7.1A's invariant 3 vs F1B-7:** resolved as a refinement note rather than a rewrite — the §7.1A block is battery-checked history (R8) and its letter ("redo stack cleared after rollback") is superseded-in-reading by the landed law; the note is marked "the controlling reading" so r1's envelope work cites the right semantics.
- **Battery compliance simulated read-only (no battery/test runs, per the round's rules):** all three spec-15 battery checks hold — "Routing-disposition table" present, "§9.5"+"staircase" present, "§13.15"+("W-ops"|"r1") present; the `live_stale` sweep for the four R15 pins (`f526e67`/`0412e41`/`374711c`/`d42693e`) = **0 live occurrences** (every surviving mention sits in a "Round 15"/"R15-era"/"superseded" historical context); the §0 triad shape intact.
- **Git usage:** read-only only (`git log`/`git show`/`git rev-parse` for HEAD confirmation + the census lineage + the retired spec-14 §4 row recovery at `4878827~1`) — no mutations, no commits, per the fleet protocol.
- **Coherence with the re-audited 05/06:** the §0 now matches their phrasing one-for-one on the shared rows — the 30-name C7 census, the r1-END + sub-gates, the r1-FIRST error envelope with the F1B-4 coverage annotation, the r1 element-level toggle verbs (06's ops-side row + this spec's §4.1A-side row, cross-pointed), the w2 staircase, and the D24 dual-tag vocabulary.
