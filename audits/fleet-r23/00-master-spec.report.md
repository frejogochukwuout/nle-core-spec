# 00-master-spec.md — the R23 per-file fleet audit (file 00 of 20)

**Agent:** R23-fleet-00 (fresh context, single-file owner). **File:** `00-master-spec.md` — the master decisions register + the fleet pin canon.
**Date:** 2026-09-07. **Inputs:** ARCH-R23 (v2, D23/D24/D25), `IMPLEMENTATION-PLAN.md`, the retired `14-implementation-phases.md` stub, 00 read in full, the five live repos at their 2026-09-07 HEADs.
**Method:** no git commands, no battery/test-suite runs; HEAD pins re-read as passive `.git/HEAD` file reads; features verified by reading code; counts re-derived statically where the count authority is a runner output (OT's report json read directly; vitest counts cross-checked by static `it/test` census).

---

## VERIFIED-STRONG (no edit needed)

1. **The orchestrator's v8.0 edits — coherence-verified, preserved untouched** (D23/D24/D25 bodies, §2A, the fleet re-pin, §10, D19's supersession bracket):
   - **All five HEAD pins re-read live:** engine `b8c6f88` ✓, OT `222532c` ✓, WDC `494f6ff` ✓, nle-ui `85dcf57` ✓, app `70e99f0` ✓ (`.git/HEAD` reads).
   - **The app's OT-mirror consumer pin `a4e971d`** verified against `nle-test-app/vendor/nle-timeline/UPSTREAM.lock.json` (`upstreamHead: a4e971d`, `mirroredAt: 2026-09-07`, `testing/` stripped, the single-path "no other local edits" sync law) ✓.
   - §2A's six standing laws present and matching ARCH-R23's D23.2/D20/D22/D25 provenance; D24's vocabulary (K1-K4 / w1-w3 / r1-r6) + estimates (crawl 6-12 / walk 10-15 / run 20-28, 18-25.5 without r4) match the plan + ARCH; D25's body carries the two-half grammar split, the vendoring mechanics summary, and the ordering law.
   - §10 re-pointed at `IMPLEMENTATION-PLAN.md` with the P0-P6 table kept as the "product-level shape (kept for the vision record)" — coherent with the stub's lineage tombstone + the plan's §7.
   - **25 decision rows** (battery-checked) ✓ — no `### Decision` headers added or removed by this audit.
2. **§0 fleet-row claims, feature-verified in code:**
   - **OT:** report json (`download/timeline-test-report.json`) = **489 total / 489 passed / 0 failed** — the count authority ✓. **M46** = "S3 seam round — setTracksMuted (the mute-all batch op: one history entry, the setTracksLocked twin) + the track.setAllMuted wire verb", 3 tests ✓; **M45** = T-round wave-F fixes, 12 tests ✓; **the F1 hardening** = `reviews/t-review-f1a/b/c.md` ✓; **the canonical tree = exactly 39 files / 7,702 LOC** (re-measured: `find src/components/timeline -type f | wc -l` = 39; total LOC = 7,702) ✓ — "zero next/* imports in components/" confirmed (05's auditor grep + ARCH agree).
   - **WDC:** 759/759 (README + HANDOFF); **W2 landed** (`src/lib/daw/soundtouch.ts` + `varispeed.ts`, `THIRD_PARTY-NOTICES.md` carries the LGPL header-preservation law, HANDOFF: "W2 CLOSED 2026-09-07, seam-round-s3, `de09c93`") ✓; **the drift-gate fails-on-drift** ✓ — `scripts/sync-from-upstream.mjs`: `--check  No writes: report drift + import violations, exit 1 on any`; HANDOFF: "`sync --check` now actually FAILS on drift".
   - **Engine:** **RR1-A verified by feature** — `tests/vitest/nle-bridge.test.ts`: "A4/RR1-A-1: the fade-clamp law at the flattener twin — clamps to the SEGMENT output span at emission (the native twins law)"; `clampAuthorFadeToSpan` imported at the bridge→audio edge (`scene-to-segments.ts:68`, documented in LAYER-SNAPSHOT) + `clampFade` in `transition-inputs.ts:185` ✓. **N2b closed @ `37cdd28`** ✓ (PLAN's N2 row: "N2b CLOSED too — seam round S2, 2026-09-07, engine 37cdd28... 18 bridge pins + 2 REAL offline-render sample-level parity pins"; HANDOFF: "N2b is CLOSED end to end"). Static vitest census = 442 `it/test` sites across 13 files ≈ declared 440 (±2 static-counting artifacts; declared count trusted per the round's canon).
   - **nle-ui:** static count = **648 exactly** ✓; **the kind-aware mute-all pin** ✓ — `timelineRouterDispatch.test.ts:241-270` (the muteAll convergence form; "a text-only overlay carries no mute in the engine model"); `shortcutMap.ts:28-29` (audio-mute "any kind" + audio-mute-all set-all batch). F3 verified in HANDOFF; F7 trusted from the round canon (repo-side worklog is R7-stale — see REMAINS-OPEN 3).
   - **App:** static count = **117 exactly** ✓; **the W3 JKL audio half** ✓ — `src/timeline-port/hooks/use-keybindings.ts` (tap-accel ladder, K-persists-rate), `GluedShell.test.tsx:113/:427` (JKL reverse + shuttle pins), `docs/design-jkl-audio-follow.md`; **timeline-port/ exists** (22 files — the fork D25 retires, with EngineMount) ✓; ProgramCanvas / engineService / audioService / sceneBridge all live ✓; the app's assembly claim (package AppShell + EngineMount + ProgramCanvas + the WDC audio host) consistent with the live file set + vendor layout.
   - **In-repo mocks:** mini 355 (the seal census; battery-scraped — trusted); variants features ✓ — W-A FX surface (`components/fx/*`), W-B color wave (`components/pages/color/*`), the VLM net (`scripts/vlm-capture.mjs` / `vlm-review.mjs` / `vlm-run.sh` + `vlm-rubric.md`).
3. **§4 tech-stack rows vs live package.jsons:** React 19 (OT ^19.0.0 / app+nle-ui ^19.2.8) ✓; Zustand 5 (nle-ui ^5.0.15) ✓; Tailwind 4 (^4.3.3, app + nle-ui) ✓; Zod 4 (engine ^4.0.2 — spec 15's schema layer) ✓; mediabunny 1.50.8 (engine, `src/lib/nle/export/*` + `media/metadata.ts`) ✓; SoundTouch (WDC W2) ✓; Vite (app) ✓. The contract-level rows (TanStack Router, Radix, OPFS, WebGPU-only) are un-contradicted by live code — kept as contract.
4. **§7 WYSIWYG** (pixel/state/audio, three invariants), **§6 constraints**, **§6A NFR budgets** (spec 17 §13A.1 pointers), **§9/§13 testability** (three tiers, asset palettes, property-based rows), **§11 open questions** (1-11 resolved / 12-14 open-by-design, correctly noted), **§12 glossary** — no staleness found. The glossary's "78 types as of Round 7" is a correctly-marked point-in-time claim (the spec-15 union remains 78; OT's prefixed census is now 30 — spec 15 §13.15's R23 refresh, reflected in D11's bracket after this audit's fix #8).
5. **§5 browser matrix** — time-stamped spec-level constraints (Firefox-stable-no-WebGPU "as of 2026"; Safari experimental) — no repo-verifiable staleness; kept.
6. **§8 stream map (rows 1-18)** — per-spec teachers/key-decisions verified against the current spec set (01-13, 15-20 all live files; spec 15's wire surface, spec 17's three-tier, spec 18's shell, spec 20's S/G/E all match their files' subjects). Only the spec-14 pointer sites were stale (fixed below).

## FIXED (this audit's edits — 20 sites, all in `00-master-spec.md` only)

**BASE re-verify fixes (charges a/e):**
1. **Status header + §0 fleet row: variants 1,470 → 1521+ (live; 1,470 at the 2026-09-07 pin-era reading — the sibling stream landed more post-re-pin).** Per the count-discipline law: the declared figure now states the live floor with the pin-era note (the task-sanctioned judgment call). Live re-measure: 1,535 static `it/test` sites + 1 `it.each` site in `ui-mock/shell-variants/src` — consistent with "1521+"; the exact figure stays the sibling track's round-wrap declaration (see NOTES).
2. **Date line:** 2026-09-02 → **2026-09-07**, and the revision ledger now carries the missing **v7 Round 22 2026-09-07 + v8 Round 23 2026-09-07** entries (the v8.0 status header previously had no Date-ledger counterpart — the ledger stopped at v6).
3. **§0 intro:** "the actual source code of two reference repos" → "the reference and core repos below" — the v1-era phrase contradicted the 8-row list it introduced (FreeCut, OpenCut-classic, engine, OT, WDC, nle-ui+app, cloudcut, web-daw).
4. **§0 engine row:** "the N1-N4 bridge seam family" → "(incl. **N2b keyframed volume @ `37cdd28`**)" — the round's landed feature now named (coherence with the canon; was subsumed, not wrong).
5. **§4 cross-origin row:** "COOP/CEOP" → "**COOP/COEP**" — CEOP is not a header name; specs 11/12/17 use COOP/COEP (spec 02/13 still spell "CEOP" — flagged for their own auditors, not edited here).
6. **D8 (cloud render):** "(phase 2)" → "(the run-tail **r6**, optional — unchartered unless the user re-scopes; was 'phase 2' in the v1 ladder)" — the phase re-tag per the D24 vocabulary (charge d).
7. **D10's supersession bracket:** re-pointed at §0's live fleet rows (`b8c6f88` 440 / `222532c` 489 / `494f6ff` 759) with the R15 figures explicitly "retired to history per Decision 21" — the old bracket pointed at "the repo table in §0" carrying R15 figures that §0 no longer shows.
8. **D11 clause-2 bracket (a living pointer, not a historical record):** "24 prefixed types since W5" → "**30 prefixed wire names at OT `222532c`** (24 at the R15 charter), **27/78 union counterparts live**" — the stale count refreshed to spec 15 §13.15's R23 census.
9. **D12 reasoning:** OT's React-tree figure refreshed — "React 3,773 LOC at R9 — now **39 files / ~7,702 LOC at the R23 pin**" (point-in-time claim kept, current figure added).
10. **D13 reasoning:** the 737/721 count chain extended — "superseded at R15 (721/721 PURE core) and again at R22/R23 (**759/759 @ `494f6ff`** — §0's fleet rows)".
11. **D13 implication:** "the lock file + 737-test gate make drift loud" → "the lock file + the suite gate make drift loud (**the sync `--check` gate now FAILS on drift — WDC's R23 CR rounds**)" — new upstream work reflected (charge e).
12. **D21 (the R22 pin record):** an **R23 note appended** — "the pin VALUES above are this decision's R22 record — the live canon is §0's fleet rows: engine `b8c6f88` 440 / OT `222532c` 489 / WDC `494f6ff` 759 / nle-ui `85dcf57` 648 / app `70e99f0` 117, consumer pins re-read live; the pin-CLASS law stands unchanged; the in-repo variants count reads 1521+ live vs the 1,334/1,470 point-in-time readings." All R22 pins in 00 now sit in explicitly-marked historical context (grep: f68ab8c/05584d8/fe05d85/dba8d52/e662759/ea10c42/5570321/752991d/1334 appear ONLY here).

**Stream-map / spec-set consistency fixes (charges b/c/e):**
13. **§0 companion table, spec-14 row (THE stale row):** "Phased rollout: playback → multitrack → ops → grading → export" → "**RETIRED at R23 (Decision 23)** — redirect stub; THE plan is `IMPLEMENTATION-PLAN.md` at the repo root." (Charge c: this was 00's only spec-14 description row, and it described the pre-retirement file.)
14. **§0 posture paragraph (line 23):** "the testing strategy (12/17) and implementation phases (`14`) define execution" → "...and the **implementation plan (`IMPLEMENTATION-PLAN.md` at the repo root — the ex-spec-14, retired at R23)** define execution."
15. **§0 companion table, spec-16 row:** "~180 keyboard bindings" → "**181** keyboard bindings (180 at v1.0 + Option+R)" — spec 16's current canon count (its §0: "181 bindings... (180 at v1.0 + Option+R)"; the old "~54 of ~178" claim retired there as stale on both sides).
16. **§0 companion table, spec-19 row:** reference-repo map widened to the fleet ("nle-engine + opencut-timeline + **web-daw-core + nle-ui + nle-test-app** + cloudcut UX-spec"); "corrective mapping (C1-**C8**)" → "(C1-**C9**)" — C9 exists in spec 19 §6 and is EXECUTED (audio-mix.ts + AudioMixer deleted @ R15); D10's body already said C1-C9, so 00 was internally inconsistent.
17. **§3 (the architecture diagram):** a "**Reading the diagram against the live fleet (D12 + D25)**" note added under the diagram — the Timeline UI box = OT's **canonical React tree** (`src/components/timeline/`, 39 files / ~7,702 LOC, the single-tree law, Decision 25; the app vendors it, the timeline-port fork retires); the Engine Core / Playback & Render boxes = nle-engine (RUNTIME); the audio DSP/mixing path (SoundTouch varispeed, channel strips, offline render) = web-daw-core (AUDIO, spec 20); the app composes them (D16/D18). (Charge e: the v1 diagram can no longer be read as contradicting the D25 canonical-tree law or the three-domain split.)
18. **§8 stream map, row 5 (Timeline):** teacher cell gains "+ **OT's canonical React tree (D25)**"; Key decisions "Decision 2" → "Decision **2, 25**" (freshness — the timeline row now points at the canonical tree).
19. **§8 stream map (after the table):** a spec-14 retirement note added — "*Spec 14 (`14-implementation-phases.md`) is RETIRED at R23 (Decision 23): its plan content moved to `IMPLEMENTATION-PLAN.md` at the repo root; the file remains as the redirect stub (the §-redirect table). The 18 rows above are the live stream map (specs 01-13, 15-20).*" (Charge c: 00's §8 now resolves the retired spec correctly — both the §0 companion row and §8's note point at the stub/plan, and the map's row set matches the live spec files.)
20. (Polish, same sites as 9/12: em-dash/readability alignment in D12's reasoning parenthetical.)

## REMAINS-OPEN

1. **The OT in-page/real-mouse split's ±1 (kept, flagged):** my independent per-test re-count of the report json (the declared count authority) gives **360 in-page + 129 real-mouse** (M17's 12 real-mouse tests + the 13 R-suffixed suites' 117 = 129; the 37 in-page milestones sum to 360; total = 489 exact). The corpus-standard **359 + 130** (ARCH-R23 review-1 F4; used by 00 §0, spec 05, spec 15) is KEPT in 00 for fleet coherence — the 05-timeline auditor re-derived the identical 360/129 and made the identical call ("the ±1 is a bucket-classification edge... immaterial, flagged here for the count-discipline law"). The battery-checked number is the total (489) and it verifies exactly. Resolve centrally at the fleet's close if the split itself is to be count-disciplined.
2. **The variants live count is a floor, not an exact:** "1521+" (3 sites: the status header, §0's fleet row, D21's note). The exact figure is the sibling track's round-wrap declaration (battery_r23's own NOTE: "the VARIANTS count is the sibling track's pin — they re-pin at their round wrap, not unilaterally bumped here"). When the sibling re-wraps, 00's three sites should take the exact figure (the pin-era 1,470 note can then drop or stay as lineage).
3. **F7's evidence is canon-only (not a 00 defect):** nle-ui's worklog/HANDOFF on disk end at the R7 era (638/638); F3 is verified in HANDOFF, F7 rests on the round's canon; the 648 static count verifies exactly, so 00's claim stands. Flagged so the next nle-ui-side session refreshes its own HANDOFF.
4. **The engine/app consumer pins (`4ef0147`, `f446512`) were not independently readable in this sandbox:** the engine's and app's submodule working copies are uninitialized (no gitlink file to read without git commands, which this audit is barred from). `a4e971d` (OT-mirror) IS verified via the app's UPSTREAM.lock.json; the other two are trusted from the task canon + the plan's own queue row ("the app's engine pin `4ef0147` is 14+ commits behind — the consumer re-pin wave"). Not a 00 defect; the battery's pin checks key the same canon.
5. **Specs 17/18/19/20 are the parallel fleet agents' files and still carry R22-era figures in their own §0s as of this audit's read** (e.g., spec 19's BASE line still says "engine 356 / OT 459 / WDC 740 / variants 1334"). Their auditors are in-flight; the battery's red rows are their TODO register. No 00-side action possible (single-file discipline).

## NOTES

- **Battery-critical invariants re-verified statically after the edits (the battery itself was NOT run, per the rules):** 25 `### Decision` headers (D1-D25); D23/D24/D25 headers present; §2A + "Port-then-swap, never swap-then-hope" present; §10's "SEPARATE document" present; the five HEAD pins + the three consumer pins (`a4e971d`/`f446512`/`4ef0147`) present; "440" (6 sites) + "489" (5 sites) present; **zero live R15 pins** (all four sit inside superseded/retired-to-history contexts — re-verified with the battery's own `live_stale` logic, ±260-char windows).
- **The stale-token sweep is clean:** f68ab8c / 05584d8 / fe05d85 / dba8d52 / e662759 / ea10c42 / 5570321 / 752991d / 1334 appear only inside D21's R22 record (now annotated); 356/459/740/640/83 (R22 counts) have zero occurrences; 1470/1,470 appears only as the annotated pin-era note; "phase 2" survives only as (i) D8's "was 'phase 2' in the v1 ladder" lineage note and (ii) the §3 diagram's ASCII box label — both deliberate vision-record keeps (the orchestrator's §10 preamble sets that precedent; D8's body now carries the r6 re-tag).
- **Preserved untouched (the orchestrator's v8.0 edit zone):** the status header's structure, D19's supersession bracket, §10 + its P0-P6 vision table, §2A's laws, the D23/D24/D25 bodies. The only touches inside the fresh-edit zone were the task-sanctioned ones: the variants count (header + fleet row) and the N2b naming addition in the engine fleet row — both count/coherence items explicitly directed or canon-backed, neither a restructuring.
- **Phase re-tags (charge d):** 00's living claims now use the D24 vocabulary (D8 → r6; §0/§2A/§10 as the orchestrator wrote them); historical decision bodies keep their point-in-time tags inside supersession brackets, which is the corpus convention (the battery accepts both vocabularies during the fleet window and tightens at its close).
- **What this audit did NOT restructure:** §2's decision order, §2A's placement, the §0 fleet-row layout, §10's table — all left as the orchestrator shaped them; every fix above is a local text correction inside an existing sentence/row.

---

**Verdict: FIXED (strong).** The file is R23-current: the fleet canon verifies live (all five HEADs re-read; the OT-mirror consumer pin lock-verified; every feature claim feature-checked in code — M46/S3, M45, F1, the 39-file/7,702-LOC canonical tree, W2 soundtouch/varispeed, the fails-on-drift gate, RR1-A's fade-clamp SPAN test, N2b @ 37cdd28, the kind-aware mute-all, JKL W3, the timeline-port fork), 20 staleness sites repaired (the spec-14 retirement now reflected in BOTH pointer surfaces, the R22/R15 count chains closed out, the D25 canonical-tree law added to §3 + the stream map), the battery invariants all hold — and the two honest residuals (the OT split's ±1 bucket edge, the variants live floor pending the sibling's wrap) are flagged, not hidden.
