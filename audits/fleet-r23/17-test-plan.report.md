# R23 fleet audit — `17-test-plan.md` (the overall test plan — the acceptance-executability layer: §0A the law + §13A the facet matrix + §17A the roof suites + §18.5 the engine reference)

**Agent:** R23-fleet-17 (fresh context). **File owned:** `17-test-plan.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs — verified via `.git/refs/heads/main`; counts trusted per the round's numbers, every suite inventory verified by listing files):** nle-engine `b8c6f88` (440/440 — 13 suites), opencut-timeline `222532c` (489/489 — `download/timeline-test-report.json` parsed live: total/passed/failed + 51 milestone suites), web-daw-core `494f6ff` (759/759 — 18 `src/test/` suites), nle-ui `85dcf57` (648), nle-test-app `70e99f0` (117/117); in-repo: `ui-mock/shell-mini` (355, the R23 seal — census re-read from `LAW-NET-INVENTORY.md`) + `ui-mock/shell-variants` (60 test files, live count per NOTES). Context inputs: `audits/ARCH-R23-plan-and-bridge.md` (D23/D24/D25, v2), `IMPLEMENTATION-PLAN.md` (§2 ladder, §8 fleet), the spec-14 redirect stub, the seal docs (CORE-SEAMS / LAW-NET-INVENTORY / OT-SEAMS), the sibling `12-testing-strategy.report.md` (the K2 twin row's wording), `00-master-spec.md` v8.0 (the re-pinned canon).

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **The §0A law's structure:** the four numbered laws + the BASE/GAP inventory shape are intact and battery-keyed (`battery_r23.py` checks the §0A heading + the "BASE rows state their regression role" phrase — both preserved verbatim).
- **The engine inventory:** 13 vitest suites exactly as the task text enumerates — `tests/vitest/engine/` carries 12 files (timeline-edit-ops, video-sync, bridge-seams, planner, api-surface, load-validation, persistence, render-abort, timeline-math, transform-resolver, undo-serialize + the `api-surface.frozen.ts` snapshot helper) and `tests/vitest/` carries the `nle-bridge.test.ts` + `nle-bridge-realtime-midi.test.ts` pair (96 + 8 declared pins at HEAD — the nle-bridge family grew from the R22-era 37-test citation; consistent with **N2b LANDED @ `37cdd28`** (18 bridge pins + 2 REAL offline-render sample-level parity pins, verified in the engine's `.agents/PLAN.md` + worklog) and the S/W-round growth). The 455-name API freeze verified (`api-surface.frozen.ts` + the worklog's "453→455 conscious freeze update").
- **The OT count + the report-json authority:** `download/timeline-test-report.json` parsed → **total 489 / passed 489 / failed 0, 51 milestone suites**; the M45/M46 suites present (+12/+3); 14 real-mouse suites (M17 + 13 R-suffixed) — see NOTES for the split's ±1.
- **The WDC inventory:** 18 `src/test/` suites — every D24/plan-named integration-family member present (audio-integration, dsp-bounce-parity, dsp-effects-integration, dsp-null-unity, dsp-pdc, dsp-sidechain, nle-audio-core-derisk, offline-parity-wiring, real-audio-e2e, meter-tap, varispeed, fft-analyzer + the rest); the Goertzel detection oracle at `lib/daw/dsp/` (metrics/oracle); the repo-root HANDOFF confirms **759/759 vitest + W2 varispeed CLOSED** ("W2 CLOSED (2026-09-07, seam-round-s3): lib/daw/soundtouch.ts + varispeed.ts") + the drift-gate now fails-on-drift.
- **The seal census (§0A.3's acceptance list):** `LAW-NET-INVENTORY.md` re-read → **8 files / 355 tests / 122 law families (122 describes → 128 census units); 13 HOLDS-on-OT units / 33 HOLDS tests; 322 app-side authoring tests across 115 GAP units** (§2.3/§2.4's audited arithmetic: 355−33 and 128−13) — exactly what §0A's GAP row and the D24 K3 row consume; **the testid census = 59 APP-EMITTED static ids + 15 templated families (the 60th static id is harness-only — §2.2)** — the DOM-structural gate wording now matches the seal doc + spec 12's convention. `CORE-SEAMS.md` S1-S26 verified present.
- **The VLM net:** `ui-mock/shell-variants/scripts/vlm-capture.mjs` / `vlm-review.mjs` / `vlm-run.sh` (+ `vlm-rubric.md`) all exist — the K3 visual net's adoption source is real.
- **The variants count:** 60 test files; declaration-level count 1,549 raw `it(/test(` matches (±grep slack) — the 1,542-live figure (the 08/12 fleet convention, the last variants-code gate line @ `acb2cf9`) is the declared live number with 00-master's 1,470 noted as its pin-era figure for the battery's central re-baseline.
- **The app's roof position:** 4 test files (engineSeam / sceneBridge / GluedShell / audioService) at `70e99f0`; the W3 JKL family + RR1-B per the round's task text (the app worklog's last in-file gate lines predate them — counts trusted per the round's numbers).
- **The three-tier methodology body (§1-§16):** no live fleet claims beyond the §0A/§13A/§17A/§18.5 surfaces — the matrix, template, NFR recipes, WYSIWYG, property strategy, CI composition, triage, and manual matrix are methodology canon (spec-shaped, not repo-shaped); the R15-era §17A S-suite SIZES stay estimates (annotated as such now).

## FIXED (edits applied to `17-test-plan.md`)

1. **Status header:** v1.3 → **v1.4** — the R23 fleet re-verify note prepended (the R23 count set, the D24 re-key C1/C4→K3/K4 + W-*→r1-r4 + R-fcpxml→r5, the K-tier map + the K2 census row additions, the N2b/W2/VLM/seal reflections, battery_r22→battery_r23, the plan/spec-14 split); the v1.3/v1.2/v1.1 ledger kept verbatim as the supersession record.
2. **§0A.1 (BASE regression-role example):** `OT @ 05584d8, 459/459 incl. 130 real-mouse` → **`OT @ 222532c, 489/489 — 359 in-page + 130 real-mouse; download/timeline-test-report.json the count authority`**.
3. **§0A.2 (GAP acceptance gate):** "the phase exit in spec 14" → **the plan's `IMPLEMENTATION-PLAN.md` §2 K/w/r ladder** (spec 14 retired; the stub's §-redirect table resolves the old citations).
4. **§0A.3 (law-net corpus):** the pre-C1/pre-C4 tag → **LANDED at the R23 seal**; the sealed census numbers added (128 units / 33 HOLDS / 322 authored / 115 GAP units / the 59-app-emitted + 15-templated testid census, the 60th static id harness-only); CORE-SEAMS S1-S26 added; the C4 exit → the D24 split (**K3** corpus-check + **K4** e2e + w1-entry human side-by-side).
5. **§0A.4 (count discipline):** battery → **battery_r23 (the battery_r22 successor)**; the drift ledger extended with the R22→R23 class (356→440, 459→489, 740→759, 640→648, 83→117, 1334→1542-live) + the count-consistency check's keying (engine 440 + OT 489 across 17+19+00) stated.
6. **§0A BASE row — the count re-pin (charge a):** `356/459/740/640/83 + mini 355 + variants 1334` → **engine 440/440 @ `b8c6f88` (13 suites enumerated, N2b LANDED `37cdd28` — 18 bridge pins + 2 offline-render parity pins, RR1-A, the 455-name freeze) / OT 489/489 @ `222532c` (359 in-page + 130 real-mouse across 51 milestone suites, report-json authority, the S-round + F1 + M45/M46, the canonical React tree per D25) / WDC 759/759 @ `494f6ff` (18 suites, the integration family + Goertzel, W2 LANDED, the drift-gate) / nle-ui 648 @ `85dcf57` / app 117/117 @ `70e99f0` / mini 355 sealed / variants 1,542-live (the VLM net; 00-master's 1,470 = its pin-era figure)**; consumer pins re-read live (engine @ `4ef0147` behind — the re-pin wave queued; OT mirror `a4e971d`; WDC `f446512`); 00-master named the pin canon (D21).
7. **§0A GAP register — the re-tag + the re-home (charges b/c/d):** the single R22 sentence → the 12-row register, dual-tagged per the D24 mapping: the LAW-NET row (LANDED — K3's acceptance list); **the K2 combined-family census + registration row ADDED (THE row — spec 12's twin; owner nle-core-spec at K2 entry; the named family enumerated; the missing OT-ops→engine-decode pairs; acceptance = census green + registered in 12/17 §13A.7 + battery-checked)**; the corpus authoring row (**K3**, was C4 — includes the composed ripple family 4 units/16 tests over `timeline.trim`/`rippleDelete` with r1's graduation, the D25 ordering law, the 59+15 DOM-structural half, the ∥ store/policy halves); **the K3 offline audio pins row ADDED** (was C3; the Node-venue law; S-wdc's waveform-contract prerequisite); **the K3 VLM visual net row ADDED** (adopt-not-fork onto the app's .storybook + OT's runner); **the K4 automated e2e row ADDED** (was C4's e2e half; zero mock paths; w2's re-gate); W-audio→**r2**, W-color→**r3**, W-n5→**r4** (D6 pending), R-fcpxml→**r5**; the battery row → **R23/battery_r23**.
8. **§0A — the D24 K-tier map ADDED (charge e):** the K1-K4 + w/r ladder, spec-17-side (where acceptance executes; the plan §2 cited as the executable gate table; K3 = 322 authored + 33 projection pins; the [H:reason] registration law for the walk).
9. **§13A.5:** the audio-domain seam bullet re-based — WDC `740/740 — R22 re-run @ fe05d85` → **759/759 @ `494f6ff`** (W2 + the Goertzel oracle noted); the engine bridge suite's "37 tests" → the C1/H8-H17 family grown to **~96 pins at the R23 pin** (the S/W rounds + **N2b LANDED @ `37cdd28`**: 18 bridge pins + 2 REAL offline-render sample-level parity pins) + the sibling `nle-bridge-realtime-midi.test.ts` (H18) added; the §13A.5 heading's spec-14 §3 citation re-pointed via the stub.
10. **§13A.7:** the re-tier row re-based to the R23 set (**engine 440 / OT 489 (359 in-page M1-M46 + 130 real-mouse, 51 suites) / WDC 759 / nle-ui 648 / app 117 / mini 355 sealed / variants 1,542-live**; the mock-split arithmetic updated nle-ui 648 + the crawl-app corpus **K3**; battery_r23-enforced; the in-page runner + the 18-suite integration family marked as the K2 seeds; post-K4/w1 retirement) — **this is the row that turns battery_r23's "engine 440 + OT 489 in 17" check green**; **the K2 combined-family census facet row ADDED** (spec 12's twin — the §13A.7 registration surface the K2 acceptance names); the two remaining "14 §3 A1" origin citations annotated "(retired → the plan's ladder)".
11. **§17A (the roof):** the R22 counts block → **R23** (440 / 489 incl. 130 real-mouse / 759 / 648 / **117 + the crawl's K3 law-net corpus** / mini 355 sealed / variants 1,542-live; post-K4/w1 retirement); S3(a)'s "OT's 120 real-mouse phases" → **the 130-test real-mouse family (the 14-suite R-family)**; S5's `battery_r9.py` → **the battery lineage (`battery_r23.py` today)**; the S-suite sizes annotated as R15-era estimates with the live counts pointed at §0A/§13A.7.
12. **§18.5 (the engine reference):** the header re-based — `R22 @ f68ab8c: 356/356; N1-N4 + W1; vendor OT @ 3420b5f, WDC @ 5570321; the 453-name freeze` → **`R23 @ b8c6f88: 440/440 (13 suites incl. the nle-bridge pair + nle-bridge-realtime-midi); N1-N4 + W1 + N2b (37cdd28) + RR1-A; vendor OT @ a4e971d, WDC @ f446512; the 455-name freeze`**; the Tier-1 row's "now runs a Node vitest tier: 274 tests" → "has run one since Round 15 (274 then, **440/440 across 13 suites at the R23 pin**)"; the "C4 re-tiering" phrase → the old C4→K4 mapping per D24.

## REMAINS-OPEN (gaps verified still-open at HEAD — no repo landed them)

- **The K2 combined-family census + registration:** the ROW now exists here + spec 12 (the S-spec deliverable is registered); the census TEXT itself lands at K2 entry (the plan's S-spec item 2) and the **missing cross-module pairs (OT-ops→engine-decode round-trips at the app's seam shapes) are unstarted** — S-engine's worklist item.
- **The K3 corpus (322 authored / 115 units):** unstarted; the store/policy halves are unblocked NOW (∥ the D25 bridge, against the injection-point contracts); the DOM-structural half waits on the D25 swap + OT's testid-emission layer (OT emits ZERO testids today — verified by the ARCH-R23 round; not re-verified file-by-file this round, taken from the ruling's evidence).
- **The K3 offline audio pins:** blocked on S-wdc's waveform seam contract (the only K3 sub-half with an external prerequisite).
- **The K3 VLM net port:** the tooling exists (variants); the adoption onto the app's `.storybook` + OT's runner is unstarted (S-app item 5).
- **K4 the automated e2e:** does not exist as one test (the crawl's completion proof — authored after the swap, S-app item 4).
- **r2/r3/r4/r5:** the offline-parity null rig, the grade-math parity pins, the real-file decode corpus (user-gated — D6 re-affirmation pending), the FCPXML fixture corpus — all unstarted, now r-tagged.
- **The battery's residue sweep:** spec 19 (the count twin) must land its own 440/489 re-baseline for the "in 17+19+00" check to go fully green — this file's half is done; 19 + 00 (00 already carries both) complete it. 00 already green (v8.0 verified); **the red row now hinges on 19 only**.

## NOTES

- **The OT in-page/real-mouse split:** the fleet-standard **359 + 130** (00-master v8.0, ARCH-R23 F4, the plan, spec 12, the task text) is kept in this spec for cross-file coherence. My independent re-derivation from the report json (14 real-mouse suites: M17 + 13 R-suffixed) sums the REAL-title suites to **129** tests, leaving **360** in-page — the same ±1 the 05/06/12 reports flagged. The TOTAL (489) is the count-discipline number and the report json is its authority; immaterial to every claim in this file.
- **The variants count:** three figures circulate — 00-master's **1,470** (pin canon), the task text's **"1521+"**, and the 08/12 convention's **1,542-live** (the last variants-code commit's gate line @ `acb2cf9`). This spec declares **1,542-live with the 00-master reconciliation note**, mirroring the 04/07/08/12 convention; my declaration-level grep gives 1,549 (±grep slack) — consistent. The battery's central re-baseline reconciles; this file does not pre-empt it.
- **The spec-14 §4 recovery method (charge c):** NO git commands were run (per the rules). The testing-domain rows were recovered from the D24 mapping table (ARCH-R23), the spec-14 stub's §-redirect table, and the seal docs. Every testing-domain row the D24 table names is now present in the §0A GAP register: the pre-C1 status flip (LANDED → K3's acceptance list), C3→K3 (the audio pins row), the C4 split (K3 corpus + K4 e2e + w1-entry), the GAP-W-ops compose row (inside the corpus row: 4 units/16 tests, r1 graduation), W-audio/W-color/W-n5/R-fcpxml → r2/r3/r4/r5, plus the plan's K2-census row (new, not a spec-14 row). The C1(b,d,e,f) rows are the interaction domain's — spec 05/16 own the acceptance; this §0A carries the census/DOM-gate context inside the corpus row (the same closing-pointer convention specs 06/12 used). The C4 annotakit review-loop config surfaces as the w1-entry half of the C4 split; the §4.6 mock-retirement triggers live in 00-master's standing laws + the post-K4/w1 notes.
- **Battery/test discipline:** no battery runs, no test-suite runs, no git commands; all counts read from the report json, file listings, `.git/refs` reads, the seal docs, and the repos' own HANDOFF/PLAN/worklog records. The battery's spec-17-relevant string checks were verified by inspection (the §0A heading + the regression-role phrase preserved; "440" and "489" present; no "358"; no live 333-pattern — the two "333" occurrences are the v1.3 ledger's "333 at the R22 re-baseline" and the R15→R22 drift arithmetic, both historical-context, neither matching the sweep's patterns).
- **The §0A triad/law preservation:** the §0A law's four numbered clauses, the BASE/GAP inventory shape, and the historical ledger (v1.1-v1.3, the R15→R22 drift examples, the ARCH-R22 Ruling B citation) are all preserved; the R22 "A3/A4-v1/A4-v2/m24-m26" lineage labels inside §17A stay (ARCH-R15 vocabulary, resolvable via the lineage tables — the D24 mapping governs new tags, not the historical record).
- **The §2-§16 methodology body untouched:** the three-tier matrix, per-module template, asset design, WYSIWYG, property strategy, performance methodology, CI composition, regen process, triage, and manual matrix carry no fleet-count claims (verified by sweep); their audit trail (§18.1-§18.4) stays as-is.
