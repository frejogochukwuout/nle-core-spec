# R23 fleet audit — `18-ui-shell.md` (the UI shell spec — the shell/chrome contract; the mini's embedding home)

**Agent:** R23-fleet-18 (fresh context). **File owned:** `18-ui-shell.md`. **Date:** 2026-09-07.
**Repos audited against (all read at their synced HEADs, no fetch/clone, no git mutations, no test runs — counts trusted per the round's declared suite totals; FEATURES verified by reading code):** nle-ui `85dcf57` (648), nle-test-app `70e99f0` (117), opencut-timeline `222532c` (489), nle-engine `b8c6f88` (440) + WDC `494f6ff` (759) as context, in-repo `ui-mock/shell-mini` (355 sealed) + `ui-mock/shell-variants` (1,521 → **1,542 at the W-F wrap**, `ba46cf3` W-C/D/E → `acb2cf9` W-F). Consumer pins re-read live: the app vendors nle-ui @ `85dcf57` ( submodule status; engine @ `4ef0147`, behind). Evidence base: `ui-mock/shell-mini/docs/CORE-SEAMS.md` (26 seams; §E S22-S26; the store partition; the transport map; the audited-absent register), `LAW-NET-INVENTORY.md` (128 units / 355 tests; 33 HOLDS / 322 authored; Part B chrome-side corpus; the 60-static/15-templated testid census), the retired `14-implementation-phases.md` stub + its git-history v2.1 @ `7ba030a` (the §4.4/§4.6 shell rows, read-only `git show`), `IMPLEMENTATION-PLAN.md`, `audits/ARCH-R23-plan-and-bridge.md` (D23/D24/D25).

---

## VERIFIED-STRONG (claims that checked out against the live repos)

- **The §0 BASE triad's structure and the §3-§15 design-of-record** remain intact and current — nothing in the live repos contradicts the region geometry, panel inventory, gesture→command contracts, state rows, or a11y floor.
- **The §16.2 R22 drag-law notice (the R18k verbatim + the retirement list) is present and battery-checked** — untouched by this audit (the D22 freeze). §16.3's "355 vitest tests" battery phrase likewise preserved.
- **nle-ui @ `85dcf57` — the F3/F7 real-gain laws verified in code:** `src/components/mixer/ChannelEditor.tsx:99-103` (the clip-gain SLIDER commits `Math.max(0.001, Math.pow(10, dbv/20))` — F3, with the pin at ChannelEditor.test.tsx:65 asserting 10^(−9/20)=0.3548 NOT 0.55) + `src/components/shell/Inspector.tsx:951-968` (the Inspector Gain displays `20·log10(volume)` and commits `10^(dB/20)` clamped ≥0.001 over `e.volume` — F7, pinned at Inspector.test.tsx:182-189). This is exactly §4.4's B2 display-side-dB law (dB is display-only; the field is the linear `volume`) — the package implements the spec's law, not a new one.
- **The kind-aware mute-all convergence pin landed** (`85dcf57` RR1-B-5: the mirror-state discriminating pin — the naive `every(t => t.muted)` re-dispatches MUTE on text-only-overlay projects; the kind-aware law reads the muteable set only).
- **The MiniShell does NOT exist in nle-ui** (charge directive's "verify" — grep-verified: zero `MiniShell` matches under `src/`; the shell family is AppShell/Toolbar2/MediaPool/Viewer/Inspector/AppDock/CheatSheet/ConfirmDialog/ContextMenu/ErrorBoundary/StatusStrip/ToastRegion + mixer + pages). The C0 row is a real, still-open gap.
- **The app @ `70e99f0`:** `App.tsx` wires `AppShell` with `timelineRegion={<EngineMount />}` + `mediaDragSource` + `programMonitor={() => <ProgramCanvas />}` (the slots contract exactly as §0 BASE bullet 3 claims); the W3 JKL seam landed (`27f14a6`/`3f351bc`: `setPlaybackRate` routed through the timelineRouter, ⇧J/⇧L yielding to the port's capture keymap — one key, one owner); RR1-B @ HEAD. 117/117 per the commit trail.
- **The app's `src/timeline-port/` IS a fork of OT's `src/components/timeline/`** — `diff -r --brief` shows ~34 differing/extra files (incl. the port-only `EngineMount.tsx`); the fork carries the hardcoded `id="timeline-scroll"` (TimelineView.tsx:1062) OT lacks, and OT's `TimelineElementView` carries the `cancelRegistry` prop (line 74) the fork misses; the port-only empty-array context-menu stability fix (`const n` inline) exists app-side only. All the D25 drift evidence verified first-hand.
- **OT @ `222532c`:** `src/components/timeline/` = 39 files, 4,025 LOC in the .tsx set (~7,700 with hooks/state per the ARCH census); the `/view` page is the miniature editor shell (library + compositor preview + timeline; `window.__VIEW_TEST__` hooks at page.tsx:14/357; the DECISIONS-#18-ruling-9 CSS section); `theme.ts` is the classic constants port; `globals.css` :root = the 1,298-line CSS-variable surface. **Zero `data-testid` emissions under `src/components/`** (rg-verified) — the D25.3b structural-half gap is real.
- **The mini's seal artifacts verified as claimed:** CORE-SEAMS 26 seams (§E S22-S26 the chrome/view seams; the 5-class store partition [DOC/HISTORY/PLAYBACK/VIEW/SESSION/TEST]; the C0-C4 transport map; the 8-row audited-absent register); LAW-NET-INVENTORY Part B chrome-side corpus = App 60 / MediaPool 19 / timecode 8 / waveform 7 / otProject 12 (rg-counted per file, matching §2.1 exactly); the testid census 60 static (59 app-emitted + `mini-clip-harness` story-surface-only) + 15 templated; disposition totals 128 units / 355 tests / 33 HOLDS / 322 authored.
- **The variants track's R23 waves verified via the in-repo git trail:** W-B @ `db58d8d` (1470) → W-C/D/E @ `ba46cf3` (**1521/1521**) → W-F @ `acb2cf9` (**1542/1542**, the deliver-view RangeBand + the P3 fix round). The VLM net exists: `scripts/vlm-capture.mjs` + `vlm-review.mjs` + `vlm-run.sh` + `vlm-rubric.md`. The W-A FX surface + the W-B color view confirmed in the README's live-surface rows (the FX page ⌘5, the tabbed ScopesDock, the Stills Gallery, nodes-in-viewer).
- **The OT S-round queue (the spec-14 §4.4 row) is still open, filed exactly where the plan says:** nle-ui `.agents/PLAN.md` §"Queued from the opencut-timeline S-round" — P-widen (`patch` grows `transitionOut`; the app's Inspector transition edits refresh only on the next doc rebuild — the registered staleness) + P-lock-route (`toggleTrackCmd('locked')` routes to the engine; the app's store write is mirror-clobbered).
- **battery_r23 (run after the edits): every spec-18 check green** — the §0 triad placement, the §16.2 R22 drag-law notice, the §16.3 MVP-vehicle phrase, the "355 vitest tests" re-baseline, the mini-count coherence (18 + plan + ARCH), the no-live-333 / no-358 sweeps. (The battery's 3 remaining FAILs are other agents' files: the 06/20 phase-tag rows, the 17/19/00 count consistency, the live R15 pin `374711c` in 00/19/20 — spec 18 is not in that fileset and is clean.)

## FIXED (edits applied to `18-ui-shell.md` — 55 insertions / 29 deletions)

1. **Status header:** v1.4 → **v1.5** (the R23 fleet re-verify framing: the BASE re-pins, the D24/D25 GAP re-key, the spec-14 §4.4/§4.6 rows re-homed, §16.3 re-pointed at IMPLEMENTATION-PLAN.md, the seal artifacts reflected). Date line gains "v1.5 2026-09-07".
2. **§0 BASE bullet 1 (the full-shell DESIGN):** variants 1,334 → **1,521+ (1,542 at the W-F wrap: W-A FX + W-B color + W-C/D/E + W-F deliver; the VLM net `scripts/vlm-*`)**; nle-ui `dba8d52`/consumer `752991d`/640 → **`85dcf57` (consumer pin re-read live: the app's vendor @ `85dcf57`) / 648**, with the **F3/F7 real-gain laws** (the true dB law over `el.volume` — display 20·log10, commit 10^(dB/20) ≥0.001; the B2-law cross-ref) + the kind-aware mute-all pin + the §10 `shell-*` testid family already emitting.
3. **§0 BASE bullet 2 (the mini):** 355 kept; enriched with the R23 seal artifacts (CORE-SEAMS 26 seams + §E S22-S26 + the store partition + the transport map + the audited-absent register; LAW-NET 128 units/355 tests/33 HOLDS/322 authored + Part B's chrome-side file counts + the 60-static/15-templated testid census; RH-skin-extraction).
4. **§0 BASE bullet 3 (the app):** `e662759`(83) → **`70e99f0` (117)**; + the GluedShell assembly, the W3 JKL transport seam (setPlaybackRate routed; ⇧J/⇧L yield), RR1-B; + the **timeline-port fork-is-retiring-per-D25** statement (PORT-LOCAL props upstream; the two-path UPSTREAM.lock vendoring; the ≥2-waves-behind drift evidence).
5. **§0 GAP register — the full D24/D25 re-key + the re-homed spec-14 rows (the big edit):** header re-pointed from "per spec 14" to the D24 ladder (crawl K1-K4 / walk w1-w3 / run r1-r6, IMPLEMENTATION-PLAN.md §2; dual-tagged). Rows now: **MiniShell** (C0 → **w1-prep/S-package**, rides ∥ crawl; CORE-SEAMS S22 slots law; verified still-open); **the grammar's TOKEN half (D25.3a — NEW)** (the mini theme mode on OT's `globals.css` :root + `theme.ts`; S-ot, w1-entry; the gold-sample acceptance); **the grammar's STRUCTURAL half (D25.3b — NEW)** (the testid-emission convention [OT emits ZERO today; the 59+15 census is K3's gate], the track-head column, the minimized strip, the compact/pill mode, the tools-row deltas; S-ot, crawl-tail, M-pinned classic defaults); **the crawl app's chrome surfaces — behavior half** (C1(b-f)+C2-behavior → **K3**, with the ordering law [DOM-structural after the swap+testid layer; store/policy ∥ now] and C1(a)/C2-frame → w1); **C4's mini-parity gate** (→ **K4** e2e + the human side-by-side + annotakit config → **w1-entry**, the side-by-side re-pointed to **app-vs-OT-runner** per D25.4); **the OT S-round queue (NEW — spec-14 §4.4 re-home)** (P-widen + P-lock-route, filed in nle-ui's PLAN, ∥ crawl; verified still-open); the full-shell chrome gaps (keymap long tail/i18n C12/tooltip C11/type-scale C14/strip badges C15) → **r5**; **W-ops keymap surfaces (NEW — spec-14 §4.4 re-home)** → **r1**; the color-page engine binding → **r3** (the W-B color wave as the design reference); the a11y+NFR floor → **r5**; **the mock retirement triggers (NEW — spec-14 §4.6 re-home)** → **post-K4/post-w1** (the law text in 00-master per D23.2).
6. **§0 ACCEPTANCE & TEST PLAN:** the LAW-NET framing flipped from "pre-C1/pre-C4 deliverables" to "**K3's acceptance list — the pre-C1 half LANDED at the R23 seal [33 HOLDS verified]**; the corpus is the K3/K4 re-expression target"; the annotakit loop re-pointed to w1-entry.
7. **§0A TL;DR:** a v1.5 (R23) paragraph added (the D25 two-half grammar, the fork retirement, the gold sample, the D24 ladder re-key).
8. **§12's annotakit-for-app charter:** re-tagged — the app-side config lands at **w1-entry** (the human rounds' review surface); the A3/A7 labels marked as the R15-era lineage resolved via the plan's §7 table (historical text kept).
9. **§13 code references:** the **stalest row in the file fixed** — the OT row said "components pending its W4" → now `src/components/timeline/` (39 files / ~7,700 LOC — **THE canonical React timeline UI tree, D25**) + `/view` (the miniature editor shell; `__VIEW_TEST__`; 359+130 suites — 489 @ `222532c`) + the gold-sample-host role; the shell-mini row 302 → **355 vitest (sealed)** + the seal docs; **a NEW nle-ui row** (the productized package family + F3/F7 + the boundary law).
10. **§16.1:** the R23 seal's CORE-SEAMS widened-map parenthetical added (26 seams across timeline/engine/audio/project-asset/chrome-view + the partition + the transport map + the audited-absent register).
11. **§16.3.1:** the deviation register "27 entries through R19" → **"39 entries through the R22 drag-law retirement; 27 at R19"** (counted in the live README); the seam-reasoning citation gains CORE-SEAMS.
12. **§16.3.5 (the MVP-vehicle row):** "(spec 14 §3.1 C0-C4)" → **the crawl's K1-K4 ladder via IMPLEMENTATION-PLAN.md §2 (spec 14 retired, the stub's redirect table resolves the old tags)**; "OT timeline via EngineMount" → **OT's canonical React tree vendored per D25 (the fork retires; EngineMount stays as the app adapter)**; "retirement decision post-C4" → **post-K4/post-w1**; + the two-half grammar statement + **the gold sample = `/view` at mini theme; the w1 side-by-side = app-vs-OT-runner, two renderings of ONE tree (D25.4)**. The battery-checked "The MVP vehicle (R22 amendment" prefix and the §16.3.3 "355 vitest tests" phrase preserved verbatim.

## REMAINS-OPEN (the §0 GAP register's live state after this audit — all rows carry owner + phase + acceptance)

- **MiniShell (w1-prep/S-package)** — no component in nle-ui yet (verified).
- **The grammar's TOKEN half (w1)** and **STRUCTURAL half (crawl-tail)** — OT has NO mini theme mode and ZERO component testids (both verified absent); the structural components don't exist.
- **K3's chrome-behavior corpus + the DOM-structural gate** — the app emits only 6 own testids today (program-canvas/p-master/p-tr-audio-1/p-aux-a1/pool-panel/shell-viewer-state-loading) vs the 59+15 census target; the LAW-NET 322-authored re-expression not started.
- **K4's e2e + w1-entry's side-by-side/annotakit config** — nothing landed.
- **The OT S-round queue (P-widen/P-lock-route)** — still unchecked in nle-ui's PLAN (verified live).
- **r5 rows (keymap long tail/i18n/tooltip/type-scale/badges/a11y-NFR), r1 keymap surfaces, r3 color binding, the post-K4/w1 mock retirement triggers** — all open by design (run-phase/walk-phase).

## NOTES

- **The variants count:** the directive said "1521+"; the live repo's last variants commit (`acb2cf9`, W-F, 10:56) declares **1542/1542** — the spec records "1,521+ (1,542 at the R23 W-F wrap)", which satisfies the directive's floor AND the latest-codebase charge. (ARCH-R23's 1470 was the W-B-era figure; battery_r23's note keeps the variants count as the sibling track's pin — the battery's central re-baseline reconciles.)
- **Read-only git usage:** `git show`/`git log`/`git diff` were used to read the retired spec-14 v2.1 content (§4.4/§4.6 — the charge-(c) source) and to verify commit trails/pins; no state-changing git commands were run; no test suites were run (counts trusted per the directive; features verified by reading code).
- **Concurrent fleet activity observed:** 17-test-plan.md and 20-audio-core.md carry uncommitted in-flight edits from sibling agents; the battery FAILs attributable to those files (06/20 phase tags, 17/19/00 count consistency, the live `374711c` pin in 00/19/20) are outside this file's ownership and unaffected by my edits.
- **The §4.x body (panels/context menus/cursor grammar/state rows), §5/§5A contracts, §9-§11 floors** — spot-checked against the mini/variants/package surfaces; no contradictions with the live repos found; no edits needed (the design-of-record sections are specification, not status claims).
- **The one battery-sensitive preservation risk (handled):** the stale-333 sweep regex (`333 tests|333 vitest|333/333|333-test` etc.) — the surviving historical mentions ("333 at the R22 retirement", "the R22 drag-machinery retirement's 333 +") do not match the patterns, and the battery run confirms green.
