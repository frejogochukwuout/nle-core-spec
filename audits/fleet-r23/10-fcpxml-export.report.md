# Fleet R23 — Spec 10 (FCPXML export) audit report

**Agent:** R23-fleet-10 · **File owned:** `10-fcpxml-export.md` · **Date:** 2026-09-07
**Pins audited against:** nle-engine `b8c6f88` (440/440) · nle-test-app `70e99f0` (117/117) · opencut-timeline `222532c` (489/489) · nle-ui `85dcf57` (648) · WDC `494f6ff` (759, checked for FCPXML-zero only)
**Context:** ARCH-R23 D23 (plan separation — spec 14 retired to stub), D24 (crawl K1-K4 / walk w1-w3 / run r1-r6; R-fcpxml→r5; C4's export wiring→K4), D25 (OT canonical tree) + `IMPLEMENTATION-PLAN.md` §2 r5 row.

## VERIFIED-STRONG (BASE re-verified against live code — no edit needed)

1. **The greenfield posture core survives the fleet move:** `rg -i fcpxml` at the pins → **0 matches in nle-engine/src, nle-test-app/src, opencut-timeline/src, web-daw-core**. No FCPXML writer, parser, or fixture corpus exists anywhere. The whole module remains greenfield; the spec's design-of-record role (DTD-not-XSD, §4 mappings, §6 validation, §8 limitations) is untouched by upstream work.
2. **The serialization source (D25's doc model) is live and matches the spec's mapping 1:1:** OT `src/lib/timeline/types/index.ts` — `SceneTracks { overlay: OverlayTrack[]; main: VideoTrack (singleton); audio: AudioTrack[] }` maps exactly onto §4's main→spine / overlay→lanes>0 / audio→lanes<0; `TimelineCore.toJSON()`/`fromJSON` (timeline-core.ts:2381) with round-trip pinned by OT's M-suites. Time base: integer ticks (TICKS discipline), consistent with spec 09's MediaTime = 120,000 ticks/sec that §4's rational-time conversion assumes.
3. **Cross-refs resolve:** spec 15 §4.2's export row + §4.3.74 `ExportFCPXMLCommand` (with the "engine export orchestrator exists (mediabunny A/V); FCPXML is A6 greenfield" split — exactly this spec's seam posture), spec 15 §4.3.76-78 export family, spec 01 §14.11/ExportManager, spec 17 §13A FCPXML matrix rows + §12 M1-M4 manual tests, spec 18 §4.8 Deliver page (FCPXML via `exportFCPXML` command). All present as cited.
4. **The spec's Tier-3 EngineCommand dispatch story** (command → `engine.export.exportFCPXML` function path; WYSIWYG byte-equality keyboard vs API) is consistent with spec 15's dispatcher design — still purely contractual (nothing implemented), correctly so.

## FIXED (stale LIVE claims → corrected at the pins)

1. **§0 BASE pin/claims (was: "nle-engine @ `f68ab8c` (356/356 vitest, tsc 0) exports mp4/webm A/V only"):** engine is now `b8c6f88` (440/440, tsc 0) and its export subsystem is **landed and decode-verified** — `src/lib/nle/export/` (orchestrator/settings/audio-mixdown/contracts): Wave 5B Export M1 (m24, 13/13 green @ `8122a21`) + Stage 2 audio (m24 24.11-13 @ `b9acda5`) + m29 A/V mux; real MP4/WebM/MKV + audio-only via WebCodecs+mediabunny; decode-verified incl. **pixel parity vs `renderFrameOffscreen`** in the browser m24/m25/m29 milestones (engine `gaps/audit/MASTER.md` rows 5B/5B-S2). Rewrote the row as the export-seam split: **media half LANDED engine-side / format half still zero** — FCPXML is the interchange FORMAT, the engine's export is the MEDIA render.
2. **"no FCPXML … exists anywhere (app included)" — refined:** the only FCPXML-shaped code in the fleet is two **honest-mock deliver rails** that explicitly defer to this spec: nle-ui `src/components/pages/DeliverPage.tsx` (preset `fcpxml` active by default, "Bundle media with FCPXML" checkbox, `shell-deliver-btn-export-fcpxml` CTA, mock queue rows `…v3.fcpxml`) and `ui-mock/shell-variants/src/components/pages/DeliverPage.tsx` (the R22 W5 grammar: presets left / queue replaces preview while rendering / deliver inspector right), plus the app's `GluedDeliver` story rendering the package rail around the engine timeline; `useShortcuts` ⌘E toasts "FCPXML export lands with spec 10 — the Deliver page carries the mock queue". The zero-writer/parser/fixture claim stays true; the mock surfaces are now named as the reference (BASE + Tier-2 note).
3. **§12.8 (Round-7 engine code references):** kept the historical table verbatim, added an **R23 re-verification note**: zero-FCPXML re-verified at HEAD; the "engine export roadmap is MP4/WebM only" note superseded (see FIXED-1); `MediaMetadata` still has **no colorSpace field** at HEAD (the "highest-value pre-export engine addition" ENGINE-GAP row is still open — verified by reading `metadata.ts`); the Round-7 "37,958 LOC, 124 tests" snapshot annotated as historical.
4. **Tier 2 `export-fcpxml-via-ui-triggers-download-event`:** added the R23 live-mock-reference note (the two DeliverPages + the app story; the real CTA→engine wiring = the K4 GAP row).
5. **Status line:** re-baselined to Round 23 (r5 greenfield, D24 re-tag; R22/SCOUT lineage preserved).

## GAP flips / adds / re-tags (the §0 register, D24 vocabulary)

- **RE-TAGGED** the whole-module row: R-fcpxml → **r5** (dual-tagged "was R-fcpxml"); the phase-entry artifact (parser+fixture-corpus choice, decided at entry) **unchanged in substance**, but its recording home re-pointed from "spec 14" (retired stub, D23) → **`IMPLEMENTATION-PLAN.md` §2's r5 row** — verified the plan's r5 row carries exactly this artifact + the "validates vs the chosen reference parser on the corpus" exit gate. Acceptance (corpus validation + deliver e2e) intact. Owner stays nle-test-app (matches the plan §3 S-app worklist ending "r5 fcpxml").
- **ADDED** the export-seam wiring row (charge d — the C4 export wiring): phase **K4** (was C4, dual-tagged), owner nle-test-app, CORE-SEAMS S15 as the seam register — verified S15 (`ui-mock/shell-mini/docs/CORE-SEAMS.md`: "the engine's real A/V export (app-side wiring is C4)" → D24 re-tag) and verified the wiring state in code: the app has **no** `renderTimeline`/`renderProject`/`NleExportSettings` consumer in `engineService.ts`; the deliver CTA is mock-only. Acceptance: CTA → engine export call + the K4 e2e's import→cut→play→export leg, zero mock paths.
- **ADDED** a new GAP row (new upstream work — charge e): **keyframed-volume mapping**. N2b LANDED (engine `37cdd28` + app `c8c875e`: `animations["volume"]` → `gainAutomation` breakpoints, lane-replaces-static, dB-domain exp-ramp law — verified in the engine's `.agents/PLAN.md` N2/N2b closure + `bridge/` sources) has **no v1 FCPXML 1.10 mapping**: v1 exports static `<adjust-volume>` + fades and lists `gainAutomation` in `KNOWN_LOSSY_FIELDS`; the v2 `keyframeAnimation` mapping rides §8's existing DEFERRED decision.
- **R-polish → r5:** not present in this file (keymap/i18n/a11y rows live in specs 16/18) — nothing to re-tag here; noted for those files' owners.
- **Charge (c) — retired spec-14 §4 export rows present in §0:** the R-fcpxml row's four elements (greenfield / parser choice at entry / validates vs the chosen reference parser on the corpus / deliver e2e) all present ✓; the C4 export-wiring element now present ✓. No orphans.

## REMAINS-OPEN (no action this round — registered)

1. **The parser+fixture-corpus choice itself** — still undecided by design (the phase-entry artifact at r5; the R22 posture holds). Not a spec bug.
2. **Engine `MediaMetadata` colorSpace** — still absent at `b8c6f88`; the §12.8 ENGINE-GAP row ("highest-value pre-export engine addition") re-verified open. Owner: S-engine, consumed at r5.
3. **The engine-export remainder** (worker offload + OPFS Stage 3, smart-copy/remux Stage 5, per `gaps/audit/C-export-encode.md`) — media-half depth, orthogonal to FCPXML; rides S-engine's queue, not this spec.
4. **M1-M4 manual flagship-NLE tests** — unchanged, gating at the phase's deliver gate (spec 17 §12); cannot be automated (licensed desktop NLEs).
5. ** battery_r23's posture checks for this spec** — "no suite exists to hold" remains true; the phase-entry corpus creates the net. The battery re-baselines pins centrally (not per-file).

## NOTES

- OT emits ZERO testids and its components tree is the D25 canonical tree — irrelevant to this spec's own tests (they target the app shell's deliver surface + the exporter), but the K4 wiring row's deliver-rail surface will consume nle-ui's already-emitted `shell-deliver-*` testids (verified present in `DeliverPage.test.tsx`) — no OT dependency.
- The spec's §4 `MediaTime` 120K-ticks assumption cross-checks against spec 09 (TICKS_PER_SECOND = 120_000, the OpenCut-classic v22→v23 migration) and OT's live tick discipline — no drift.
- Edit discipline honored: only `10-fcpxml-export.md` touched; no git commands; no test runs (counts trusted per the round context; features verified by reading code); §0 triad preserved; historical sections (§0A scout notes, §12.8 Round-7 rows, §13 corrections) kept verbatim with re-verification annotations.

**Verdict: VERIFIED-STRONG with BASE refresh — the greenfield posture survives the fleet move intact (zero FCPXML writer/parser/fixture anywhere); the export seam's media half is newly landed engine-side (decode-verified m24/m29) and is now correctly registered as the K4 wiring row, the R-fcpxml row re-tagged r5 with the phase-entry artifact re-pointed to the plan, and N2b's keyframed volume registered as v1-known-lossy.**
