# SCOUT-R8-C — cloudcut UX-Spec Scouting Report (Round 8)

**Agent:** SCOUT-R8-C
**Date:** 2026-09-02
**Mission:** Read the full cloudcut-nle `ux-spec` branch spec set (source material), map it against our canon spec set (00-18), and produce an integration plan for the coordinator.
**Canon hierarchy:** our spec set (nle-core-spec 00-18) is canon; the ux-spec is source material. Where they contradict, OURS wins. Our scope (00-master-spec.md §1-§2): browser NLE engine core + timeline + FCPXML handoff — a rough-cut editor with export. NOT cloud-collaboration-first, NOT AI-first, NOT monetization.
**Source:** `origin/ux-spec:docs/ux-spec/` on the local cloudcut-nle clone (read-only; accessed via `git show`, extracted to `/tmp/uxspec/` for reading). Reference code surveyed on the `main` working copy at `/home/z/my-project/cloudcut-nle`.

**Note on file count:** the task brief said 27 files; the branch actually carries **28 files** — `00-INDEX.md` plus §01-§26, with **two §16 files** (`16-ai-first.md` and `16-5-screen-camera-capture.md`, the latter numbered "16.5" in the index). Total **7,933 LOC**. All 28 were read in full.

---

## 1. File-by-File Digest

**Set-level maturity.** This is an unusually well-governed spec set: v1.0 → v1.3.5 across six revisions (2026-06-29 → 2026-09-02); a uniform 9-part internal structure per file (purpose / as-built / flows / component contract / states / edge-cases / a11y / gap-vs-DaVinci box / open questions); an explicit wired/disabled/missing vocabulary; a consolidated **154-item P0 roadmap** with S/M/L/XL effort estimates and a launch checklist (§22); and a strikethrough audit trail for scope strikes (§26) with honest "the v1.3 commit only edited 9 files" audit notes. Review discipline: 3 sub-agent review rounds (v1.3.2-v1.3.5) closing at "0 MAJOR remaining." It is grounded in an actual code audit of the cloudcut-nle prototype (Next.js 16, ~12k LOC of UI code) cross-referenced with DaVinci/Premiere/FCP/CapCut. Quality signal: **HIGH for UI-layer UX detail; LOW-to-NONE for engine internals** (no color math, no threading, no wire protocol — it is purely an app-UX spec).

| File (LOC) | Summary | Maturity/quality signals |
|---|---|---|
| `00-INDEX.md` (183) | Reading guide, 27-section roadmap table, role-based reading paths, and a **cross-cutting conventions** section (§3): SMPTE NDF time format, cursor-name vocabulary, selection/disabled/rec colors, tooltip latency target (500 ms delay + 100 ms fade), snapping threshold (8 px screen-space), undo coalescing window (250 ms), autosave debounce (1500 ms), keyboard text-input guard. | Change log v1.0→v1.3.5; review-round annotations; P0-count reconciliation notes. The conventions block is the single densest cross-cutting artifact in the set. |
| `01-overview-personas.md` (227) | Product thesis: AI-first, virtual-production-only browser NLE; 5 personas (solo creator, podcast editor, agency, educator, student); 8 JTBDs; competitive positioning; **7 design principles** (§6) incl. "muscle memory over novelty," "one undo step per user intent," "the empty state teaches." | v1.3 scope-declaration header; persona rewrites with strike notes; current-state assessment table (~40% of vision built). |
| `02-information-architecture.md` (435) | 6-page DaVinci-style IA (Media/Edit/Fusion/Color/Fairlight/Deliver — Cut struck v1.3); as-built shell anatomy (MenuBar with 13 cosmetic menus, SecondaryToolbar, MediaPool/Viewer/Inspector upper row, Timeline/Meter/Mixer lower row, AppDock); resizable-panel ranges; workspace presets (P1); fullscreen modes; F6 panel-focus cycling; the full UI-store field table. | As-built verified against `TopHeader.tsx`/`SecondaryToolbar.tsx`/`AppDock.tsx` with line refs; v1.3.2-v1.3.4 corrections (menu list, 1-active-tab + 5 disabled, 21→19 toolbar controls); gap box with 9 items. |
| `03-visual-language.md` (598) | Complete visual system: 30+ color tokens (`--c0`…`--c9`, `--tx`…`--tx4`, `--bd`…`--bd3`, semantic + meter tokens) with **WCAG-verified contrast ratios**; 6-step type scale; font stacks (Inter + JetBrains Mono); Lucide icon vocabulary with per-concept size table; 4px spacing scale; **5 motion tokens + rules** ("no motion for state the user is tracking," reduced-motion discipline); panel anatomy; 9-row state-visualization table; light-theme token mapping; 17-item gap box. | Tokens quoted verbatim from `globals.css`; contrast table computed against `--c2`; icon inventory "verified against codebase"; v1.3.2-5 strike/audit annotations. The most mature file in the set. |
| `04-media-pool.md` (377) | Media-pool UX: as-built component contract (grid/list/search/sort, footer); asset types & thumbnail rules (incl. `#t=0.1` video-poster trick); selection model; 4 user flows (upload MISSING, browse, drag-to-timeline MISSING, SRT import library-only); states — empty/loading-skeleton/search-no-results (all missing); metadata display; bin tree + smart bins (P0 minimum "All Files" + "Recent"); drag feedback spec (ghost, `copy`/`not-allowed` cursors); a11y; 16-item gap box. | As-built verified against `MediaPool.tsx` (357 lines); bug callouts (footer counts static catalog); P0.42-P0.51 in §22; strikes for sync-bin. |
| `05-viewer-transport.md` (415) | Viewer + transport: 4-zone anatomy (toolbar/video-frame/scrub/transport); canvas compositor explanation; transport contract table incl. JKL speed ladder, mark in/out, loop, next/prev edit; 7 flows; empty/fallback/playing/looping/marked states; zoom (no-op bug); on-screen overlay spec (§6); dual-viewer struck v1.3; 17-item gap box incl. a11y (canvas aria-label 1 Hz, TC live region). | Verified against `Viewer.tsx` (427 lines) + `usePreviewEngine.ts`; bug annotations (scrubber uses hardcoded TOTAL_DUR=399 s); v1.3 strike of dual-viewer with "single Viewer is permanent." |
| `06-timeline-tracks.md` (381) | Timeline UX: anatomy with 160 px headers; ruler as-built (static ticks — bug) + zoom-adaptive target; **track-header component contract** (grip, label, volume dB input, lock, R/S/M, expand, per-track meter, height handle); track types; playhead behavior (auto-scroll at 70%); zoom ladder; snapping (8 px, candidates = playhead + all edges + ruler ticks); markers; track reorder + height resize; 6 flows; 19-item gap box (add/delete/rename track UI, empty state, in/out + loop ranges on ruler, TC tooltips). | Verified against `Timeline.tsx`/`TrackHeader.tsx`/`Ruler.tsx` + hooks; notes the UI-type vs engine-type bridge data-loss issue; v1.3 strike on webcam recording. |
| `07-clips-operations.md` (455) | Clip ops UX: the EDL data model as-built; 46-action operation table (add/move/trim/ripple/slip/nudge/duplicate/split/razor/replace…); collision modes (noCollision ripple vs free); multi-track compositing transform model; fades; speed (pitch-loss bug); color labels; 11 flows incl. keyboard insert/overwrite (`,/./`); clip visual states (hover cursor zones, selected, dragging ghost, locked 50%); 20-item gap box (text-clip UI, replace-asset UI, effect badges, fade triangles, drag tooltips, context menu). | Verified against `timeline-store.ts` (~950 lines, 46+ actions); Prisma silent-data-loss callout; v1.3 strikes (multicam, match-frame/Source-viewer). |
| `08-inspector.md` (396) | Inspector UX: 6-tab topology (Video/Audio/Effects/Transition/Image/File) as-built with per-tab contents; common sections (label+color, source-asset card, In/Out NumberFields, duration, track assignment, quick-seek buttons, split/delete actions); 8 flows; states (no-selection project view, clip deleted while inspecting, tab switching, disabled tabs); a11y; 20-item gap box (timecode parsing, live preview standardization, keyframing, reset, paste-props, tab visibility by clip type). | Verified against `InspectorPanel.tsx` (699 lines — "largest component"); WCAG failure callouts on `--tx4`; state-only controls flagged. |
| `09-toolbar-tools.md` (306) | Toolbar UX: 34 px NLEToolbar, 7 groups / 19 controls (post-strike); tool behaviors (Select/Trim/DynamicTrim/Blade/Hand/Transform with the T-key miswire bug); edit toggles (snap/link/lock); zoom cluster (hardcoded zoom-fit bug); master-audio cluster incl. **DIM chip** (temporary 25% dip); 16-item gap box. | Verified against `NLEToolbar.tsx` (391 lines); multiple bug fixes promoted to P0.1-P0.8; v1.3 Sync-group strike. |
| `10-keyboard-shortcuts.md` (378) | Full shortcut map: registry architecture (priority + condition, single keydown listener, text-input guard, Ctrl/Cmd interchangeable); 72 shortcuts in 11 category tables with context + notes; conflicts (zero hard, `[`/`]` mutual exclusion); the undo-outside-registry exception; Mac display bug; untested-shortcut list (10); overlay spec; customization roadmap (v1 none / P1 overrides / P2 profiles). | Verified against `shortcut-registry.ts` (525 lines) + e2e tests; the S/M track-hijack historical bug with regression-test note; hard counts (69+3). |
| `11-pointer-modifiers.md` (374) | Pointer grammar: click/shift/ctrl/alt click table across 9 surfaces; double-click semantics table (incl. reset-to-default); **9 implemented + 3 gap drag gestures** (honestly recounted after v1.1 inflation); wheel/trackpad semantics; modifier-in-drag expectations (Ctrl+drag duplicate, Alt+drag slip, Shift+drag axis-lock — all missing); full **cursor vocabulary** table per context (with gaps flagged); **5 complete right-click context-menu specs** (clip/track/ruler/media-pool/empty-timeline) with shortcuts shown; 18-item gap box incl. keyboard-accessible menus (Shift+F10). | v1.1 amendment note ("drag gesture count was inflated — restated as 9 + 3 gaps"); as-built verified against 6 drag hooks. |
| `12-audio.md` (298) | Audio UX: MixerPanel anatomy (5 channel strips, 45+ controls); PluginModal (12 knobs, 4 groups — verified accurate in v1.1 amendment); MeterPanel; the real Web Audio metering pipeline; 4 flows (fader, solo, plugin, voiceover record partially wired); 18-item gap box (EQ/dynamics mock, no real mixing, LUFS, no voiceover getUserMedia). | Component-contract tables with "Wired?" column; strike notes for dual-system sync + surround; reframed LUFS rationale (web, not broadcast). Most of this is mock — low build value, decent UX-shape value. |
| `13-color-grading.md` (242) | Color UX: ColorPanel as-built (5 tabs, only Primaries + Qualifier functional, state-only); 4-wheel primaries model (Lift/Gamma/Gain/Offset + luma/sat masters); qualifier model; scopes (MISSING — P0.62 waveform); LUTs (P1); 18-item gap box; Log/RAW/CDL/HDR struck v1.3. | Verified against `ColorPanel.tsx`; P0.63 access-path reconciliation notes across 3 review rounds. Thin vs our spec 08 — engine depth lives in OUR spec, not here. |
| `14-effects-transitions.md` (202) | Effects/transitions/keyframing UX: EffectsPanel missing; Inspector effects tab as-built (4 types); transitions MISSING (target list: cross-dissolve, dip-to-color, push, slide, wipe); keyframing MISSING (DaVinci diamond-toggle model described); 14-item gap box with P0↔P1 corrections. | v1.3.4 annotations (ordering/bypass priority corrections flagged "needs roadmap decision"). |
| `15-export-render.md` (249) | Export UX: 4 formats (MP4 server-side / FCPXML / EDL / JSON client-side); ExportDialog anatomy; render-job lifecycle diagram (queued→rendering→complete/failed, atomic claim, 10 s heartbeat, 5-min timeout, 3 retries); download-UX bugs (two-click download, no cancel button, no queue); 16-item gap box (presets, destinations, MP3/WAV P0.70, frame export, background render, blob: URL trap). | Verified against `ExportDialog.tsx`/`export*.ts`/render-worker; broadcast/CEA strikes v1.3; GPU-rendering priority reconciliation v1.3.4-5. |
| `16-ai-first.md` (344) | AI flows: the product's center of gravity (per v1.3). Transcript-based editing (P0): transcribe flow, Transcript Panel wireframe, edit-by-deleting-text, auto-captions, silence detection; P1 set (auto-cut with proposal review, smart trim, scene detect, text-to-edit, voiceover gen); generated-media pipeline (§4.6: avatar/B-roll/stock P1); **AI UX principles §5** (explicit / inspectable / reversible / respectful + spark badge). | 16-item gap box with P0.73-82; cost-transparency promoted P0.80; consent-gate question. Well-shaped UX constraints even if the feature is out of our scope. |
| `16-5-screen-camera-capture.md` (335) | Screen capture (camera struck v1.3): 3 sources (screen/mic/system-audio); capture dialog + floating preview window wireframes; pause/resume/cancel flows; post-capture asset creation + auto-prompt to transcribe; states incl. permission-denied and stream-ended; 15-item gap box; P0.136-144. | Heavy strike annotations throughout (camera blocks struck); effort re-estimates L→M. |
| `17-project-bin-files.md` (199) | Project/bin/file management: no project concept as-built; target flows (project list page, new-project dialog, switcher, settings modal, relink, archive/restore); UI-state persistence gap; blob: URL trap; 15-item gap box. | v1.3 narrowing note (relink narrowed to moved/renamed uploads; proxy generation simplified). |
| `18-collaboration-cloud.md` (199) | Cloud sync/collab UX: auto-save as-built (1500 ms debounce) with the **silent data-loss CRITICAL bug** (Prisma schema drops transform/speed/text fields); target save-status indicator ("Saving…/Saved 2s ago/Save failed/Offline — N pending"); review links, frame comments, version history (P1); conflict resolution last-write-wins (P1). | Bug verified end-to-end; P0.87-P0.94; strikes absent (cloud is in THEIR scope, out of OURS). |
| `19-onboarding-help.md` (175) | Onboarding/empty-states/help: first-run modal + interactive tour + sample project (P0); per-panel empty-state CTA targets; shortcut-overlay gaps; tooltip delay; settings modal (6 tabs); help menu; tutorials; 14-item gap box. | Short but unusually dense in CTA patterns; every empty state specified with copy. |
| `20-undo-history-errors.md` (183) | Undo/errors: 250 ms coalesced full-EDL snapshot stack; flush-before-undo; history panel (missing); error-state table (network/render/asset/validation/overlap — mostly silent today); toast conventions (success 4 s / error persist / warning 6 s); 15-item gap box (stack cap 100, error boundary, validation, destructive confirms, beforeunload). | Small file, high leverage; the error-UX vocabulary is the most portable part. |
| `21-a11y-i18n-perf.md` (208) | Cross-cutting quality: **12 enumerated WCAG 2.2 AA P0 fixes** (contrast, tablist pairing, canvas aria-label 1 Hz, grid arrow-nav, F6, reduced-motion global, focus rings, decorative-SVG aria-hidden, label sizes, skip link, live regions, Shift+F10 menus); i18n plan (string extraction, 7 languages, timecode-format picker, RTL P2); perf UX (virtualization, memoization, code-splitting, skeleton states, **performance budget**: first paint <1 s, TTI <3 s, 60 fps @ 1080p/50 clips). | Current-state audit split good/bad; P0.112-P0.122; WebGL2 disambiguation note v1.3.5. |
| `22-gap-roadmap.md` (509) | The consolidated roadmap: methodology (impact/blocking/cost/strategic value); **154 P0 items** in 16 sub-tables with effort + section cross-refs; P1 (81 live bullets) and P2 (32) lists; v1 launch checklist; JTBD coverage matrix; risk assessment + dependency graph; 3-phase ship plan. | Multiple hard-recount corrections (v1.3.2: "81 live P1 bullets… the old ~76 estimate never reconciled"); dependency notes (auth blocks rate-limits). The set's index of record. |
| `23-monetization.md` (253) | Monetization UX: Free/Pro/Team tiers with limits tables; watermark policy (canonical: Free CAN export >720p with watermark); paywall trigger flows; usage metering displays; credit system; team management. P0.125-135. | v1.3.4 reconciliation note (hard-cap vs watermark phrasing corrected). Entirely out of OUR scope. |
| `24-multi-camera.md` (262) | Multi-cam editing workflows — **struck entirely v1.3** (preserved for audit trail): multi-cam clip model, sync dialog (timecode/waveform/manual/marker), multi-cam viewer grid, live switching. | Header strike note + "DO-NOT-BUILD" note added v1.3.3. Out of our scope too (and their own). |
| `25-freecut-integration.md` (488) | Strategic addendum: fork FreeCut (~60k LOC WebGPU browser NLE) as engine foundation; coverage analysis of the 154 P0s (16% covered / 40% extends / 44% custom, ~33% effort savings); **UX preservation verdict** (keep CloudCut's 20 components: 11 KEEP / 2 REPLACE / 7 MERGE / 0 DISCARD) via the `nle-store.tsx` bridge seam; 7-step migration path; custom-tech gaps; WebGPU-only rendering strategy. | 4 parallel subagent analyses cited; multiple "NOT APPLIED" audit notes (v1.3.2) where §25 recommendations never landed in §22. Interesting as a *decision record*; contradicts OUR Decision 1 (rebuild, don't fork). |
| `26-virtual-production-scope.md` (262) | Scope-correction addendum: strikes all physical-production scope (multi-cam, camera capture, RAW, broadcast, CDL, HDR, surround, live streaming); consolidates the strike list with section refs; promotes AI generation features to P1/P2; honest audit note that the original v1.3 commit edited only 9 of the claimed files. | The strike/audit-trail governance itself is the interesting artifact — a model for how WE should record scope changes. |

---

## 2. Applicability Matrix

Verdicts: **ADOPT** = fills a real gap in ours (named). **ADAPT** = partially useful, reshape for our smaller scope. **REJECT** = out of our scope or contradicted by canon. "Informs" lists our spec files that the material would land in.

| ux-spec file | Informs (ours) | Verdict | What to take / what to reshape / why reject |
|---|---|---|---|
| 00-INDEX | 18, 05, 06, 16 | **ADAPT** | Take the cross-cutting conventions block (§3) as a checklist: tooltip latency 500 ms + 100 ms fade + 4 s auto-dismiss; snapping 8 px screen-space threshold (matches `SNAP_THRESHOLD` practice; ours 05 §9 has `SNAP_THRESHOLD_SECONDS` — add the px↔time rule); cursor-name vocabulary; disabled = 40 % opacity + `cursor: default`. **Reshape/reject:** 250 ms time-window undo coalescing (ours is command-based coalescing — 06 §4.6 `previewElements`/`commitPreview`), 1500 ms cloud PUT autosave (ours is OPFS local — 09 §6.1), dual text-input guards (theirs is a bug; ours 16 §8.5 single handler). |
| 01-overview-personas | 18 (preamble), 00 | **REJECT** (with 3 extractable principles) | Product thesis, personas, JTBDs, competitive map are all AI-first/cloud-first — contradicted by 00-master §1 ("NOT AI-first," rough-cut + FCPXML handoff). Extract: design principle 6 ("one undo step per user intent" — already implied by 06 §4.4 coalescing; worth stating as a shell-wide UX rule), principle 7 ("the empty state teaches" — the governing rule for the empty-state work below), principle 3 ("timeline is source of truth / no shadow state" — restates our Decision 9). |
| 02-information-architecture | 18 | **REJECT core / ADAPT fringe** | Core contradicted: 6-page IA + wired 13-menu MenuBar (ux-spec §3, P0.9) vs our 3-page dock + menu bar removed by user directive (18 §3.1, §8.1). **Take:** F6 panel-focus cycling + `Ctrl+F6` reverse (02 §7 — genuine a11y gap in our 18 §11); panel-toggle binding pattern (theirs `Shift+1..5`; ours `Option+1..4` — ours wins, no change); resizable-range precedents (15-60 % / 20-80 %, double-click reset — ours 18 §3.2 has defaults+reset already; their numeric ranges are a useful sanity check); the "visible-but-disabled tabs" rationale (02 §3) as a future-consideration note against our 18 §8 ledger. |
| 03-visual-language | 18 §9 (primary), 05, 08, 17 | **ADOPT** | Fills: spec 18 §9 has 8 tokens + spacing; ux-spec 03 has 30+ tokens **with WCAG contrast verification** (03 §3.2), a 6-step type scale (§4.1), a per-concept icon size table (§5.2), 5 motion tokens + anti-cute motion rules (§7), panel anatomy (§8.1), a 9-row state-visualization table (§9.1), disabled-language rule (§8.2 #4), skeleton-pulse loading pattern (§7.2 #5), light-theme mapping (03 §3.3 — ours says single theme; keep as deferred note). Best single integration target in the whole set. Map their token names onto our `--bg-*`/`--accent-*` names (ours wins on naming; theirs wins on coverage + the contrast table). |
| 04-media-pool | 18 §4.2, 09 §7, 17 | **ADOPT** | Fills: our 18 §4.2 media pool is 5 bullets; no states, no sort/search contract, no drag feedback. Take: empty/loading-skeleton/search-no-results states (04 §4); grid/list + sort contract (04 §2.1); drag feedback spec — ghost clip, `copy`/`not-allowed` cursors, target-track highlight (04 §8); footer counts (live, not static — their bug is our warning); a11y row set (aria-activedescendant, arrow keys, live region — 04 §9.2); metadata columns (04 §5 — bounded by spec 09's `MediaRecord` fields). **Reshape/reject:** bin tree + smart bins (04 §6) → defer to v2 (our 09 project model is per-project flat `media[]`; a bin field would be a 09 schema change — coordinator call); proxy badge (we have no proxy pipeline — reject); offline badge + relink (see §4 gap inventory — ADAPT as an error-state, defer the relink flow); SRT import (captions are out of our v1 — reject, note for v2). |
| 05-viewer-transport | 18 §4.3, 03 | **ADAPT** | Take: on-screen overlay spec (05 §6) — top-left clip name + TC, top-right res/fps (we have chips in the toolbar but no in-canvas overlay); empty-state CTA + drag-drop target (05 §4.1); scrubber richness (gap 3) — in/out + loop range shading, click-to-seek; distinct go-to-start vs prev-edit icons (gap 17); canvas `aria-label` updated at 1 Hz + throttled TC live region (05 §8.2); fallback source-preview mode (05 §4.2 — relevant to our 18 §8.5 dual-viewer deferral as a cheap v1 substitute). **Reject:** JKL ladder semantics (10 §3.2 — ours 16 §3.1 multi-tap + K+J/K+L chords win), 640×360 canvas advice (ours is WebGPU/DPR-aware — 18 §3.3), dual-viewer (both specs agree it's out). |
| 06-timeline-tracks | 18 §4.7, 05 §10-§11, 09 | **ADAPT** | Take: track-header affordance inventory (06 §2.3) — inline rename, height resize + presets, expand/collapse, per-track mini meter (defer), grip reorder (our 15 wire has `ReorderTrack` — the UI gesture was unspecified); add/delete/rename-track context menu + "+" button (06 flow 6.1 — our commands exist, the UI doesn't); in/out + loop range rendered on ruler (06 gaps 13-14); playhead TC tooltip during drag (gap 11); empty timeline state (gap 5); playhead auto-scroll 70 % rule (06 §2.5 — verify against 05; 05 doesn't state the % — adopt); track-type color coding (06 gap 7 → reshape onto our token set). **Reject:** record-arm `R` + recording workflow (06 flow 6.5 — no recording in our scope, 00 §1); per-track volume dB input in header (our gain model lives in inspector Audio tab per 18 §4.4 + spec 03 §9 — keep it there; document the divergence). |
| 07-clips-operations | 05 §7-§8, 06, 18 §5 | **ADAPT** | Take: clip visual-state vocabulary (07 §4) — hover cursor zones (body `move`, edges `ew-resize`), selected outline, dragging = 50 % original + ghost, locked = `pointer-events:none` + 50 % opacity; effect badges on clips (gap 7: F/T/S/♪) — we have effects/transitions/speed in the model, no on-clip indication; fade triangle visualization (gap 6) — pairs with spec 05 §7 clip visual structure; clip color-label strip (gap 5) — `updateElements` covers the field, rendering unspecified; drag TC tooltip with In/Out/Dur (gap 9); cross-track-type move prevention with `not-allowed` cursor (gap 17 — a real contract our 05 §8.3 should state); blade cut-line cursor feedback (gap 15 in 07 §6 OQ5). **Reject:** keyboard `,`/`.` insert/overwrite from pool selection (our `,`/`.` are slip/nudge — 16 §3.4/3.6 wins); replace-asset UI (no such command in our 15 §4.1 union — would need a wire addition; defer); razor-all shortcut (ours is `Cmd+Shift+B` — 16 §3.4). |
| 08-inspector | 18 §4.4, 09 | **ADAPT** | Take: common-sections pattern (08 §2.4) — source-asset card with thumbnail, In/Out NumberField grid, duration row, track-assignment display, **quick-seek buttons (→ In / → Out / → Mid)** — cheap, high-value, absent from ours; timecode parsing in numeric fields (gap 5 — accept `HH:MM:SS:FF` and seconds); uniform commit semantics (gap 8: all property edits live-preview, debounced 50 ms — ours 18 §4.4 has commit-on-release/enter; add the NumberField case); reset-per-property-group (gap 14); disabled-tab visibility by clip type (gap 16 — ours 18 §4.4 has "visible when" column, same idea — theirs adds hiding irrelevant tabs); tabpanel ARIA pairing (§5.2). **Reject:** 6-tab topology (ours = 4 tabs, 18 §4.4 — image/file struck with rationale §8.6-8.7); EDL-JSON project fallback view (ours shows "Nothing to inspect"). |
| 09-toolbar-tools | 18 §4.5, 16 §3.2 | **REJECT core / ADAPT fringe** | Core contradicted: tool set + keys (theirs V/B/T/H + miswired transform; ours V/B/H/Z/T/Y/U/R/N per 16 §3.2 — ours wins; also their T-key bug validates our single-ShortcutMap design). **Take:** DIM chip concept (09 §2.8 — temporary monitor dip; cheap pro nicety, optional); disabled-button visual language (09 §4.3 — opacity 40 % + `not-allowed`); group-separator discipline; the "no toolbar button for a tool = undiscoverable" principle (09 gap 1) — apply as a checklist to our 16 §3.2 tool set vs 18 §4.5 tool cluster. |
| 10-keyboard-shortcuts | 16 | **REJECT (conflicts) / ADAPT mechanics** | The binding tables conflict with our 16 at nearly every row (see §3 contradictions — ours wins throughout). **Take mechanics only:** the priority + condition registry architecture (10 §2.1 — compatible with our 16 §8.6 ShortcutMap; their resolved-conflict examples are good test cases); platform-aware `⌘`/`Ctrl` display in the cheat sheet (gap 2 — ours 16 §7.3 doesn't specify platform display; adopt); shortcut-overlay searchability (already ours — 16 §7.3); untested-shortcut tracking as a test-suite discipline (10 §7 → our 17 §16 test matrix); Mac/Windows `Ctrl`/`Cmd` interchange normalization (16 §8 has layout handling — theirs confirms the need). |
| 11-pointer-modifiers | 18 §5, 05 §8 | **ADOPT** | Fills the biggest single UX gap relative to spec 18: a complete pointer+modifier grammar. Take: the click/shift/ctrl/alt table per surface (11 §2.1 — reshape: our multi-select is shift/cmd-click per 18 §4.2; keep, add ctrl-click alias decision); double-click reset semantics (fader/knob/NumberField → default — 11 §2.2); wheel semantics incl. **Ctrl/Cmd+wheel zoom toward cursor** (11 §2.4 — absent from ours); the 5 **context-menu specs** (11 §4.1-4.5) — clip, track-header, ruler, media-pool, empty-timeline, each fully enumerated with shortcuts; the full **cursor vocabulary** table (11 §3) — directly testable, cheap; keyboard context menus via Shift+F10 (gap 18). **Reshape:** their modifier-drag set (Ctrl+drag duplicate / Alt+drag slip / Shift+drag axis-lock) vs our Alt-drag duplicate (18 §5) — ours wins; adopt **Shift+drag axis-constrain** (compatible, unspecified in ours); slip stays key/tool-driven in ours. |
| 12-audio | 18 §8.4, 03 §9 | **REJECT** | Mixer panel (45+ controls), PluginModal, EQ/dynamics, LUFS metering, voiceover record, ducking — all Fairlight-class audio post, explicitly out of our scope (18 §8.4 removed the Fairlight page; 00 §1 "no broadcast audio routing"). Our audio surface is inspector Audio tab + master mute/volume (18 §4.5) + spec 03 §9's graph. Nothing here to adopt beyond the meter `prefers-reduced-motion` 60 Hz→10 Hz fallback (03 already respects ours via token rule). |
| 13-color-grading | 08, 18 §4.8 | **REJECT UI shape / (engine covered by ours)** | Their ColorPanel is state-only mock UI; our spec 08 owns the real wheels/curves/LUT/qualifier/window/scopes math and 18 §4.8 owns the page layout (open question 3). **Minor adapt:** before/after split-view toggle (13 gap 17 — nice grading UX, unspecified in ours); keyboard shortcuts for color ops (Ctrl+1..4 wheels — 16 §3.11 may already have some; reconcile); wheel reset + numeric readout conventions (13 §2.3). Scopes priority (waveform first, P0.62) matches our 08 §11 ordering — no change needed. |
| 14-effects-transitions | 07, 18 §4.4 | **ADAPT (small)** | Our model already has effects/transitions/keyframes (15 wire §4.1 effect/transition/keyframe commands; 07 §6.3's 27-transition registry). Take: the effects-list UX contract (add/remove/reorder/toggle + param editors — ours 18 §4.4 Effects tab has exactly this; theirs adds drag-reorder + bypass-eye conventions, 14 gaps 7-9); transition picker contents (cross-dissolve/dip/push/slide/wipe + duration + alignment — ours 18 §4.4 Transition tab says "presentation picker (27 registry entries)" — their 5-name shortlist is a sane default view); keyframe diamond-toggle UX description (14 §2.5 — ours has keyframe commands; the inspector-side diamond affordance is unspecified). Reject: OpenFX, templates, motion blur (out of scope). |
| 15-export-render | 10, 15-wire, 11, 18 §4.8 | **ADAPT** | Ours wins on architecture (exportFCPXML/exportMaster/exportFrame via `apply()`, 15 §4.3.74-76; no in-browser master encode — 00 §1). **Take for the Deliver page (18 §4.8):** render-preset dropdown concept (15 gap 4 — bounded to our 2-3 export commands: FCPXML bare vs bundleMedia, master format/destination); **cancel button** (gap 2 — we have a job queue per 11-cloud; the UI cancel affordance is unspecified); progress row + ETA (their lifecycle states map 1:1 to our `renderProgress` events — 15 §9); auto-download/failure retry presentation (gaps 1, 7); a11y for the progress UI (gap 16: `role="progressbar"` + `aria-valuenow`). **Reject:** MP4-in-browser, EDL/JSON/MP3/WAV export formats (would need new wire commands — note as future 15-wire candidates, not v1); destinations (YouTube/S3); timecode burn-in; watermark. |
| 16-ai-first | — | **REJECT** | AI-first is the cloudcut thesis, explicitly not ours (00 §1: "AI features beyond transcription" excluded; transcription itself optional v1). Keep one breadcrumb for the coordinator: **if/when transcription ships**, their §5 principles (explicit / inspectable / reversible / one-undo-batch) + the Transcript Panel shape (16 §3.2) are the right UX constraints, and they compose cleanly with our command architecture (a transcript edit = `batch` of `delete` commands, one undo step — 15 §7). |
| 16-5-screen-camera-capture | — | **REJECT** | Screen/mic capture, MediaRecorder pipeline, PiP layouts — capture is a media-creation feature, out of our rough-cut scope (00 §1 has no capture anywhere). The permission-denied / stream-ended error-state patterns (16-5 §6.7) are nice but only matter if capture ever ships. |
| 17-project-bin-files | 09, 18 | **ADAPT** | Our project model (09) covers persistence/OPFS/autosave/migrations far better. **Take:** project-settings modal fields (17 flow 17.3 — name/fps/resolution + the "changing fps after clips exist" warning; our 09 §3.1 `UpdateProjectSettings` command exists, the dialog UX doesn't); UI-state persistence discipline (17 gap 2 — ours 09 §3.1 `ProjectUIState` persists opaquely per 18 §14; their gap is our warning: persist it, don't rebuild); save-status indicator (17 gap 15 → see §4); offline-asset handling as error state (17 gap 6/9 — badge + red clip stripe; **defer the relink flow itself** — no command for it, and OPFS-local media rarely goes "offline"; note as a media-asset-missing error state instead). **Reject:** project list page / multi-project routing (ours is single-project-per-session v1 — `CreateProject`/`LoadProject`/`SwitchScene` cover the model), archive/restore, teams. |
| 18-collaboration-cloud | — | **REJECT core / ADAPT one item** | Cloud sync, review links, comments, presence, conflict resolution — all out of scope (00 §6 #6: "no multi-user collaboration, deferred indefinitely"). **One adaptation:** the save-status indicator pattern (18 flow 18.1 target: "Saving…" / "Saved 2s ago" / "Save failed — retrying") applies verbatim to our **local** OPFS autosave (09 §6.1) — a status-strip chip, not a cloud feature. Their silent-data-loss bug is a general lesson our 09 §5.3 migration rules already encode. |
| 19-onboarding-help | 18, 17 | **ADOPT (slimmed)** | Fills a genuine hole: our spec set has zero onboarding/empty-state content. Take: per-panel empty-state CTA copy (19 §2.2/flow 19.4) — media pool / viewer / timeline / deliver, each with an action button; **sample project** (flow 19.2 — pre-built 30 s demo; doubles as a Tier-2/3 test fixture, aligning with 17-test-plan asset design); tooltip latency rule (19 gap 5, from 03 §7). **Reject/defer:** first-run tour, settings modal (18 §8.12 defers settings — agree), tutorials, docs site, help menu (no menu bar), demo mode. |
| 20-undo-history-errors | 18 §6, 06, 15 | **ADOPT** | Ours owns the undo *mechanism* (command-based, 15 §6.2 `UndoInfo`; 06 §4.6 coalescing) — their snapshot-stack is rejected. **Take:** the error-state table concept (20 §2.3) reshaped onto our typed `CommandError` (15 §6.3): presentation per error class (command rejected → toast + `not-allowed` cursor; render job failed → Deliver row + Retry; asset missing → pool badge); **toast conventions** (success 4 s auto-dismiss / error persists / warning 6 s — 20 §2.4); invalid-operation feedback (gap 6: "Cannot move — blocked by locked clip" + cursor); React error boundary + reload/report UI (gap 9); input validation presentation (gap 10: red border + message — pairs with inspector NumberField work); destructive-action confirmations (gap 14: deleteScene already confirmed per 18 §4.6; add delete-track-with-clips + multi-delete); beforeunload warning (gap 15 — our 09 §6.3 handles unload persistence; add the browser-prompt UX); undo-stack cap (gap 4 — 100 entries; verify against 01 CommandManager limits). History panel (gap 2) — our 16 §3.10 already reserves `Cmd+Option+Z` for it; adopt their list/jump UX as the v2 shape. |
| 21-a11y-i18n-perf | 18 §11-§12, 00, 17 | **ADOPT** | The deepest cross-cutting fill: our 18 §11 is 4 paragraphs; theirs is a 12-item P0 floor + i18n + perf. Take: the **12 WCAG 2.2 AA fixes** as our a11y floor (contrast-verified tokens, tablist pairing, canvas 1 Hz label, grid arrow-nav + aria-activedescendant, F6 cycling, global reduced-motion CSS, visible focus rings, decorative-SVG aria-hidden, 11 px minimum label size, skip link, selection live regions, Shift+F10 menus); screen-reader test pass (NVDA/VoiceOver/JAWS) as a 17-test-plan Tier-3 item; **performance budget** (21 gap 14: first paint < 1 s, TTI < 3 s, 60 fps @ 1080p 50 clips) + the P0 perf list (React.memo on TrackHeader/TrackRow/clip cards, memoized derived values, 200 ms search debounce, code-split heavy modals, skeleton states); i18n discipline (string extraction from day 1, even shipping English-only — cheap now, expensive later; timecode-format picker REJECT for v1, 05 owns format). |
| 22-gap-roadmap | 14, 17 (methodology) | **ADAPT (methodology only)** | The 154 P0 items are ~80 % out of our scope (auth, monetization, capture, AI, collab). **Take the machinery:** the prioritization rubric (impact × blocking × cost × strategic value), the effort-tagged item table format, the launch checklist, the dependency graph (18 §8.2-style), and the hard-recount discipline ("the '~76' estimate never reconciled"). This is a strong template for a **Round-8/9 UI gap ledger** derived from §4 below. |
| 23-monetization | — | **REJECT** | Plans/billing/quotas/watermarks — out of scope by task definition and by 00 §1. Nothing portable. |
| 24-multi-camera | — | **REJECT** | Struck even in their own spec (v1.3 §26); multi-cam is out of our scope (00 §1). No action. |
| 25-freecut-integration | 00 (Decision 1), 19 | **REJECT decision / ADAPT evidence** | Their verdict — fork FreeCut, keep the cloudcut UI via the `nle-store.tsx` bridge — **directly contradicts our Decision 1** ("rebuild from scratch — do not fork") and Decision 10 (nle-engine is the reference, not a fork base). Ours wins. **Adapt as evidence:** their coverage math (16 % covered / 40 % extends / 44 % custom) independently corroborates our Decision 1 pruning (we adopt 10 of FreeCut's 21 workers etc.); their §4.2 component KEEP/REPLACE/MERGE table is a useful cross-check for 19-code-references' UI side (they keep 11 components we're deliberately NOT building — mixer, meters, TopHeader — confirming our 18 §8 ledger is the smaller, right-sized surface); the vendored `src/freecut/` in their main branch is a *stale* FreeCut snapshot — spec 19's nle-engine mapping supersedes it as our code reference. |
| 26-virtual-production-scope | 00, 18 §8 | **REJECT content / ADOPT the governance pattern** | Their scope strike list is theirs (AI-first virtual production). **Adopt the pattern:** the strike-marker format + consolidated strike table + "honest audit note" (26 §6) is exactly how our 18 §8 Chrome Removal Ledger should evolve if Round 8+ removes more surface — a `SCOPE-CHANGE` addendum with per-item rationale and an applied/not-applied audit note. |

---

## 3. Contradiction List (ours wins in every row)

| # | Topic | ux-spec says | Ours says | Resolution |
|---|---|---|---|---|
| C1 | Page IA / chrome | `02 §3`: 6-page IA (Media/Edit/Fusion/Color/Fairlight/Deliver), Cut struck; "wire all 13 MenuBar dropdowns" (P0.9) | `18 §3.1 + §8.1-8.4`: 3 pages (Edit/Color/Deliver); menu bar REMOVED (user directive); feature access = toolbar + shortcuts + context menus | Ours wins. Do not reintroduce the menu bar or 6-page dock; ux-spec's "visible-but-disabled tabs" (02 §3) may be revisited only via the 18 §8 ledger. |
| C2 | Inspector tabs | `08 §2.1`: 6 tabs (Video/Audio/Effects/Transition/Image/File) | `18 §4.4`: 4 tabs; image/file dropped with rationale (§8.6-8.7) | Ours wins. Their per-tab field inventories remain useful *content* for our 4 tabs. |
| C3 | Tool keys | `09 §2.3 + 10 §3.1`: V/A select, B blade, T trim (miswired), H hand; no slip/slide/roll keys | `16 §3.2`: V select, B razor, H hand, Z zoom, T **roll**, Y slip, U slide, R ripple-toggle, N snap-toggle | Ours wins (FCP-convention rationale, 16 §3.2 note). Their T-key bug is a warning about dual sources of tool truth — we have one ShortcutMap (16 §8.6). |
| C4 | Snap toggle key | `09 §2.5`: snapping toggled with `S` | `16 §3.2`: `N` = snap toggle; `S` = split at playhead (16 §3.4) | Ours wins. |
| C5 | JKL shuttle | `10 §3.2`: SPEED_LADDER `[0.25, 0.5, 1, 2, 4, 8]`, "NO J+K or K+L chord — registry doesn't support chords" | `16 §3.1`: J/L multi-tap to 4×; `K`+`J`/`K`+`L` held = ½× slow-mo | Ours wins. (Their P1 "add chord support" suggests chords are the pro-correct endpoint — ours already specifies them.) |
| C6 | Edit-point navigation | `10 §3.3`: `ArrowUp`/`ArrowDown` = prev/next edit | `16 §3.1`: `PageUp`/`PageDown` = prev/next edit; `Up`/`Down` = marker nav | Ours wins. |
| C7 | Shift+Arrow granularity | `10 §3.2`: Shift+Arrow = ±1 second | `16 §3.1`: Shift+Left/Right = ±10 frames (frame-rate aware per §9.4) | Ours wins. |
| C8 | Delete vs ripple-delete defaults | `07 §3.4 + 10 §3.4`: `Delete` = **ripple delete** (close gap); `Shift+Delete` = leave gap | `16 §3.4`: `Delete` = leave gap; `Backspace`/`Cmd+Delete` = ripple | Ours wins — **opposite defaults**; flag for the cheat-sheet so nobody "fixes" it toward DaVinci behavior. |
| C9 | Slip keys | `07 §3.4`: `[` / `]` = slip 1 frame | `16 §3.4/§3.6`: `,` / `.` = slip (slip tool) or nudge (select mode); `[` / `]` = trim-to-playhead | Ours wins. |
| C10 | Q / W semantics | `07 §3.3`: Q/W jump to clip start/end; Shift+Q/W ripple-trim to playhead | `16 §3.4`: Q/W = splitAndRemove left/right half | Ours wins. |
| C11 | Modifier-drag duplicate | `11 §2.5`: Ctrl/Cmd+drag = duplicate; Alt+drag = slip | `18 §5`: Alt-drag = duplicate (then `move`, batched); slip via slip tool/keys | Ours wins. Adopt their **Shift+drag axis-constrain** (compatible, unspecified in ours); do NOT adopt Ctrl+drag-duplicate or Alt+drag-slip. |
| C12 | Undo architecture | `20 §2.1`: full-EDL snapshot stack, 250 ms time-window coalescing, in-memory, separate keydown listener | `06 §4.4-4.6 + 15 §6.2`: command-based undo (`UndoInfo` per command), drag coalescing via `previewElements()`→`commitPreview()`, nudge coalescing via 400 ms Option-hold (16 §3.10), `undo`/`redo` are wire commands | Ours wins. Do not adopt time-window snapshot undo — it is incompatible with the command/`CommandResult` protocol and the WYSIWYG replay model (15 §10.4). |
| C13 | Timeline-tab semantics | `02 §4.4`: sequence tabs; New Sequence dialog; multi-timeline store | `18 §4.6 + 09 §6`: **scene** tabs (multi-scene project); `+` = `createScene`; close = `deleteScene` (confirm) | Ours wins. Their "New Sequence dialog (name, fps, resolution)" shape is reusable for a New Scene dialog (fps/resolution come from project settings — simplify). |
| C14 | Export surface | `15 §2.1 + §22 P0.64-72`: 4 formats (MP4/FCPXML/EDL/JSON), MP3/WAV P0, presets, destinations, render queue panel | `15-wire §4.3.74-76 + 18 §4.8 + 00 §1`: `exportFCPXML` (canonical), `exportMaster` (cloud, optional), `exportFrame`; **no in-browser master encode, no EDL/JSON/MP3** | Ours wins. EDL/JSON/MP3/WAV are future wire-protocol candidates only — log, don't build. Their Deliver-page UX (presets/cancel/progress/retry) is adoptable within our 3-command surface. |
| C15 | Audio scope | `12`: Fairlight-class mixer (45+ controls), EQ/dynamics/LUFS, voiceover record P0.123 | `18 §8.4` (Fairlight page removed) + `00 §1` (no broadcast audio routing) + `03 §9` (audio graph) | Ours wins. Reject the mixer; audio params live in the inspector Audio tab + timeline-toolbar master controls. |
| C16 | Color panel topology | `13 §2.1`: floating ColorPanel, 5 tabs (Log struck → 4) | `18 §4.8 + 08`: Color page, single-column panel stack hosting spec 08's wheels/curves/LUT/qualifier/window/scopes; layout = open question 3 | Ours wins. Take only the before/after split and wheel-reset conventions (13 gaps 17, §2.3). |
| C17 | Engine strategy | `25 §4.1`: fork FreeCut as engine foundation; KEEP cloudcut UI via `nle-store.tsx` bridge; 32-variant `NLEAction` bus | `00 Decision 1 + Decision 9 + 10`: rebuild in pure TS; UI = new DaVinci-derived shell (18); `EngineCommand` 78-type union (15); nle-engine is de-risking reference, not a fork base | Ours wins. Their coverage analysis is corroborating evidence for our pruned-worker adoption; the vendored `src/freecut/` snapshot is not a reference for us (spec 19 owns reference mapping). |
| C18 | Autosave mechanism | `00 §3 + 18 §2.1`: 1500 ms debounce → `PUT /api/timelines` (cloud) | `09 §6.1`: debounced autosave to OPFS (local); `saveProject` command flush | Ours wins. Only the *status indicator* UX is portable (see §4 G10). |
| C19 | Text-input guard | `10 §4.3`: two hooks with different guards — flagged as a bug (Ctrl+Z in contenteditable double-fires) | `16 §8.5`: single text-input handling in one handler | Ours wins; their bug is a validating test case for us. |
| C20 | Keyboard customization | `10 §8`: v1 none, P1 user overrides, P2 profiles | `16 §7.4`: v1 none, v2 overrides via `ShortcutMap` + localStorage | No conflict — identical stance. Listed to confirm alignment. |
| C21 | Dual viewer / Cut page | `05 §7`: struck v1.3, "single Viewer is permanent" | `18 §8.5`: dual viewers deferred to v2 (seal-round question) | Compatible (they strike permanently; we defer). Note: if source-preview ever ships for us, their fallback `<video>` source-preview mode (05 §4.2) is the cheap interim. |
| C22 | Track-header contents | `06 §2.3`: lock/R(rec-arm)/S/M/expand + volume dB input + grip + height handle + meter | `05 §10 + 18 §4.7`: M/S/lock/visibility buttons; no rec-arm, no per-track volume input | Ours wins on scope (no recording). Their affordance set is the adapt target minus R and minus volume-input (gain via inspector, per C23). |
| C23 | Per-track gain control | `06 §2.3`: per-track dB input in the header (click-to-edit) | `18 §4.4`: gain/pan/fades in the inspector Audio tab; master volume in timeline toolbar | Ours wins (keeps headers cheap; gain is model-backed in 09). Document the divergence so a future "add volume to headers" PR cites it. |
| C24 | In/out-point model | `05 §3.3 + 06`: `markInSec`/`markOutSec` store fields, used for insert/overwrite/loop/export | `16 §3.4 note + 15 §4.3.29 + 03 §3.4`: no dedicated commands; in/out = `setLoop` start/end halves | Ours wins (wire-protocol minimality). UI must render the in/out range from loop state — see §4 G12. |
| C25 | Viewer canvas tech | `05 §2.2`: 2D-canvas compositor with per-clip `<video>` elements; 640×360 internal | `18 §3.3 + 04`: WebGPU canvas, DPR-aware backing store, 10-bit pipeline | Ours wins (Decision 4/5). Their compositing rationale ("why canvas, not DOM") is still a good explainer for blend-mode preview needs. |

---

## 4. Gap-Fill Inventory for Spec 18 (+ project-level logic)

Spec 18 (`18-ui-shell.md`, 320 lines) covers: derivation/simplification (§2), layout + sizing (§3), panel inventory (§4), gesture→command contracts (§5), state binding (§6), rendering strategy (§7), chrome-removal ledger (§8), theming tokens (§9), testids (§10), 4-paragraph a11y (§11), three-tier tests (§12). What it LACKS, section by section, with ux-spec citations:

**G1. Per-panel state rows: empty / loading / error (fills 18 §4).** Ours specifies happy-path contents only (exception: "Nothing to inspect", §4.4). ux-spec has production-depth state specs for every panel: media pool empty/skeleton/search-no-results (04 §4.1-4.3), viewer no-media + fallback (05 §4.1-4.2), timeline empty (06 §4.1), inspector no-selection (08 §4.1), export no-clips (19 §2.2). Each with CTA copy. **This is the single biggest structural gap.**

**G2. Context menus, enumerated (fills 18 §4/§5 + resolves 18 §15.2 Q2).** Ours: "context menus are implied but not enumerated." ux-spec 11 §4.1-4.5 enumerates all five (clip / track-header / ruler / media-pool / timeline-empty) down to separators and shortcut labels, plus the keyboard route (Shift+F10, 11 gap 18). Needs reshaping to our command set (15 §4.1) and our keymap (16).

**G3. A11y floor (fills 18 §11 — currently 4 paragraphs).** Missing from ours, present in ux-spec: F6 panel-focus cycling with 2 px focus outline (02 §7); grid/listbox arrow-key navigation + `aria-activedescendant` (04 §9.2); selection live region (04 gap 15); tablist↔tabpanel `aria-controls` pairing (08 §5.2); canvas `aria-label` at 1 Hz + throttled TC live region (05 §8.2); global `prefers-reduced-motion` CSS zeroing (21 §2.2 #6); visible focus rings on sliders and all interactive elements (21 §2.2 #7, 09 gap 14); decorative-SVG `aria-hidden` + `tabindex=-1` (05 gap 14); 11 px minimum label size (08 gap 12); skip-to-content (21 §2.2 #10); Shift+F10 context menus (11 gap 18); screen-reader test pass NVDA/VoiceOver/JAWS (21 gap 12 → our 17 Tier 3).

**G4. Error & notification presentation (fills 18 §6).** Ours: typed `CommandError` (15 §6.3) + "minimal live region" (18 §11.4) — no presentation layer. ux-spec 20 §2.3-2.4: error-class → presentation table (network/render/asset/validation/overlap), toast conventions (success 4 s / warning 6 s / error persist), invalid-op feedback ("Cannot move — blocked by locked clip" + `not-allowed` cursor, 20 gap 6), render-failure retry (20 gap 7), React error boundary (20 gap 9), input validation borders (20 gap 10), destructive confirms (20 gap 14 — ours confirms only `deleteScene`), beforeunload (20 gap 15, pairs with 09 §6.3).

**G5. Visual-language depth (fills 18 §9).** Ours: 8 tokens + 8 px grid + control heights. Missing: WCAG-verified contrast table incl. the `--tx4`-forbidden-as-body-text rule (03 §3.2); 6-step type scale (03 §4.1); icon size/stroke conventions (03 §5.1); **motion tokens + rules** — no-motion-while-tracking, no playhead animation, 1 Hz pulse loading, reduced-motion global (03 §7); tooltip latency 500 ms + 100 ms fade + 4 s dismiss (03 gap 2, 19 gap 5); disabled = 40 % opacity + `not-allowed` (03 §8.2 #4); state-visualization table (03 §9.1 — hover/selected/drag/error/disabled per control class); panel anatomy incl. optional footer with counts (03 §8.1); track-type color strip (03 §9.3, 06 gap 7); clip color-label strip + effect badges + fade triangles (03 §9.2, 07 gaps 5-7) — the latter three are 05 §7 territory but the token rules live in 18 §9.

**G6. i18n posture (fills 18 §9 or a new note).** Ours: silent on i18n entirely. ux-spec 21 §3: extract strings to a locale file from day 1, ship English, plan DF/seconds/frames TC formats (P1), RTL deferred. Cheap-now/expensive-later discipline worth one normative paragraph even for an English-only v1.

**G7. Performance budget (fills 18 §12 + 00-master non-functional).** Ours has no numeric budget anywhere (00 §9 is testability, not perf targets). ux-spec 21 §4.2 + P0.116b: first paint < 1 s, TTI < 3 s, preview 60 fps @ 1080p with 50 clips; plus the P0 perf list — React.memo on track headers/rows/clip cards, memoized derived values, 200 ms search debounce, code-splitting + React.lazy for heavy modals, skeleton states. Pairs with existing 05 §13 perf notes (which cover drag/scroll/canvas, not load-time).

**G8. Media-pool depth (fills 18 §4.2).** Ours: 5 bullets. Missing: grid/list view contract + sort modes (04 §2.1); drag feedback (ghost + `copy`/`not-allowed` cursors + track highlight, 04 §8); footer counts (04 §2.5); metadata-on-hover/list-columns bounded to 09's `MediaRecord` (04 §5); bin tree minimum (04 §6 — **needs a 09 schema decision**, defer to v2 if rejected); offline/missing-asset error state (04 OQ5, 17 gap 6/9 — reshape: no relink flow v1, just badge + clip stripe + typed error).

**G9. Viewer & transport affordances (fills 18 §4.3).** Missing: in-canvas overlays (clip name + TC top-left; res/fps top-right — 05 §6); scrubber richness (in/out + loop ranges, clip-boundary ticks, click-to-seek — 05 gap 3); empty-state CTA + drag-drop target (05 §4.1); distinct jump-start vs prev-edit icons (05 gap 17); fallback source-preview mode as the v1 stand-in for deferred dual-viewer (05 §4.2, ties to 18 §8.5/§15.2 Q1).

**G10. Save-status indicator (fills 18 §6.3 event→render map).** Ours maps `stateChanged`/`timeupdate`/`renderProgress` but has no autosave-status surface beyond the scene-tab dirty dot (§4.6). ux-spec 18 flow 18.1 target: "Saving…" / "Saved 2s ago" / "Save failed — retrying" chip; portable verbatim to our OPFS autosave (09 §6.1) as a status-strip element (18 §3.1 has the 12 px status strip — currently unowned content).

**G11. Track-header affordances (fills 18 §4.7 + 05 §10).** Missing: inline rename (06 gap 3), add/delete track via context menu + bottom "+" (06 flow 6.1 — commands `addTrack`/`deleteTrack` exist in 15), height presets + resize (06 §2.10/gap 17), expand/collapse (06 §4.8). Explicitly REJECT: rec-arm, per-track volume input, per-track meter (see C22/C23).

**G12. Ruler & drag feedback (fills 05 §9-§11 + 18 §5 preview column).** Missing: in/out + loop range shading on ruler/scrubber (06 gaps 13-14); playhead + clip-drag TC tooltips ("In/Out/Dur", 06 gaps 11-12, 07 gap 9); snap-engagement highlight on the snap target (06 gap 19 — 200 ms `--accent` flash); blade-tool cut-line cursor (07 OQ5, 09 gap 8); auto-scroll-at-70 % rule (06 §2.5 — verify vs 05).

**G13. Inspector field contracts (fills 18 §4.4).** Missing: timecode parsing in numeric fields (08 gap 5); uniform live-preview commit incl. NumberField (08 gap 8); reset-per-group (08 gap 14); quick-seek buttons → In/Out/Mid (08 §2.4 #6); source-asset card with thumbnail (08 OQ5); multi-select "mixed" indicators (already ours) — confirmed aligned.

**G14. Pointer & cursor grammar completion (fills 18 §5).** Missing: the full cursor table (11 §3 — 16 rows, directly testable); wheel semantics incl. Ctrl+wheel zoom-to-cursor (11 §2.4); double-click resets (11 §2.2); Shift+drag axis-constrain (11 gap 4); cross-track-type move prevention + `not-allowed` (07 gap 17).

**G15. Deliver-page UX detail (fills 18 §4.8).** Missing: preset dropdown over our 3 export commands; cancel affordance for `exportMaster` jobs (11-cloud has the queue; the button is unspecified); progress row + ETA + `role="progressbar"` (15 gap 16); failure retry; artifact-download presentation for `exportFCPXML` (`CommandResult.data` → file save, spec 15 §14.11 — the save-dialog UX is unspecified).

**G16. Onboarding & sample project (fills 18, new small section).** Missing entirely: sample project (19 flow 19.2 — doubles as a 17-test-plan fixture); empty-state CTA copy set (19 §3.4). Reject: tour, settings modal, help menu (agree with our 18 §8.12 deferral).

**G17. History panel shape (fills 16 §3.10's reserved binding).** `Cmd+Option+Z` opens a history panel in ours, but no spec describes it. ux-spec 20 §2.2 (list every action + timestamp, click to jump) is the v2-ready shape.

**G18. Scope-change governance (fills 00/18 §8 process).** Ours has the Chrome Removal Ledger; ux-spec 26 demonstrates the stronger pattern: strike markers with rationale + consolidated strike table + applied/not-applied audit note + hard recount discipline. Worth adopting as the amendment format for future scope changes.

**Non-gaps verified (ours already covers or deliberately rejects):** minimum-window overlay (18 §3.2 = 01 §8); viewer zoom/DPR handling (18 §3.3 > 05 §5); transport/tool contracts (18 §5 ≫ 09/10); theme single-dark (18 §9, theirs P1 — agreed defer); keyboard customization deferral (16 §7.4); testid conventions (18 §10 vs theirs — ours is more systematic); state-WYSIWYG testing (17 §6.1 — ours is categorically stronger; theirs has no equivalent); undo mechanism (06/15 — ours stronger); autosave mechanics (09 §6 — ours stronger + their critical bug validates our schema-first approach); render lifecycle (11-cloud — ours stronger, theirs presentation-only).

---

## 5. cloudcut-nle Reference-Code Survey (main branch)

The repo (Next.js 16 + Prisma + Zustand + Tailwind 4; `src/components/nle/` 20 components ~6.6k LOC, `src/hooks/` 19 hooks, `src/lib/` 17 libs, `src/store/` 4 stores, plus a vendored `src/freecut/` engine snapshot, a `mini-services/render-worker/`, and 57 test files). The UX spec's §5 source-of-truth table maps 1:1 onto these files. Character: the **UI-surface code is genuinely useful as UX/app-scope reference**; the **state/engine code embodies the anti-patterns our spec set exists to correct** (store-mutation UI, snapshot undo, cloud round-trip data loss, mock panels).

| Area | Files | Usable as UX/app-scope reference? | Notes / anchors |
|---|---|---|---|
| **Drag state machine** | `src/hooks/use-timeline-drag.ts` (281) | ✅ **Best single file.** | `DragMode = 'move' \| 'trim-start' \| 'trim-end'` (line 31); edge hit-zones, snap integration, multi-clip drag as move-config. Direct reference for 05 §8 + 18 §5 contracts. |
| **Snap computation** | `src/lib/nle-constants.ts` (169) | ✅ | `SNAP_THRESHOLD_PX = 8` (line 22), `snapTime()` (line 68) — candidate collection (playhead + all edges + ruler ticks). Matches ux-spec 00 §3. |
| **Shortcut registry** | `src/lib/shortcut-registry.ts` (525) + `src/hooks/use-keyboard-shortcuts.ts` | ✅ | Priority + condition architecture (registry line 40 `priority?`); single listener + text-input guard. Reference for 16 §8.6 ShortcutMap mechanics. Their undo-outside-registry bug is the counter-example. |
| **Undo UX** | `src/hooks/use-undo-redo.ts` (252) | ⚠️ pattern only | `flushPending()` (line 157), 250 ms coalescing. The *UX rule* (one drag = one undo entry) matches our 06 §4.6; the *mechanism* (snapshot stack) is rejected (C12). |
| **Media pool UI** | `src/components/nle/MediaPool.tsx` (356) | ✅ | Grid/list toggle, sort menu with `menuitemradio` + Escape/outside-close, ARIA listbox/option, search+clear, footer counts. Reference for 18 §4.2 expansion (G8) — including its footer-count bug as a test case. |
| **Track header UI** | `src/components/nle/TrackHeader.tsx` (314) | ✅ (minus recording) | Volume dB input click-to-edit (Flow 6.6), grip reorder, height handle, expand/collapse. Reference for G11 — minus R-arm and volume input per C22/C23. |
| **Viewer chrome** | `src/components/nle/Viewer.tsx` (439) | ✅ chrome / ❌ engine | Transport button cluster, scrub-row, TC chip, empty-state text. The canvas compositor (`use-preview-engine.ts`, 450) is 2D-canvas + `<video>` — superseded by our WebGPU pipeline (C25); do not use as engine reference. |
| **Inspector** | `src/components/nle/InspectorPanel.tsx` (698) | ⚠️ mixed | Good: In/Out NumberField grid, duration row, quick-seek buttons, label+color, source-asset card (G13 targets). Bad: 6-tab topology (rejected), direct `useTimelineStore.setState` bypass (08 gap 9), EDL-JSON fallback view. |
| **Context-menu / pointer** | — (none exist) | ❌ nothing to reference | Zero context menus in the codebase (ux-spec 11 §4 is design-only). The shadcn `ContextMenu` primitive exists under `src/components/ui/` for us to use instead. |
| **Collision logic** | `src/lib/collision.ts` (333) | ⚠️ | Ripple/free collision modes; useful as a behavior reference for 06 §5 ops, though our op layer is pure-function (06 §4.6). |
| **Export** | `src/lib/export-fcpxml.ts` (227), `export-edl.ts`, `ExportDialog.tsx` (263) | ✅ secondary | A second FCPXML serializer (ours: spec 10 with OpenCut-classic refs — `exportFcpxml()` line 49). ExportDialog is a clean modal + progress reference for G15. The render worker (`mini-services/render-worker/`) lifecycle (atomic claim, heartbeat, retries) is well-verified — presentation reference for our 11-cloud job rows. |
| **Virtualization** | `src/hooks/use-virtualized-clips.ts` | ✅ | Clip-window virtualization w/ integration tests; corroborates 05 §6. |
| **Toolbar / shell chrome** | `NLEToolbar.tsx` (390), `SecondaryToolbar.tsx`, `TopHeader.tsx`, `AppDock.tsx`, `TimelineTabs.tsx` | ⚠️ partial | Tool-group + toggle-cluster layout and `.toggled` state language are reusable; TopHeader's 13 cosmetic menus and AppDock's non-navigating tabs are the "fake UI" anti-pattern our 18 §5/§12 contract tests exist to prevent. |
| **Audio / mixer** | `MixerPanel.tsx` (493), `PluginModal.tsx` (364), `Knob.tsx`, `Meter.tsx`, `use-real-audio-meter.ts` (235) | ❌ mock / ⚠️ widget-level only | EQ/dynamics/plugin knobs are state-only mocks; metering pipeline (AnalyserNode + reduced-motion 10 Hz fallback) is real and decent. Knob/fader drag UX (Shift+drag fine mode, double-click reset) is the only portable part. Out of our v1 scope anyway (C15). |
| **Color panel** | `ColorPanel.tsx` (516) | ❌ | Wheels + qualifier are state-only; our spec 08 + nle-engine own this. |
| **State / bridge / stores** | `src/store/*.ts`, `src/lib/nle-store.tsx` (bridge), `src/lib/nle-bridge.ts` | ❌ **avoid** | The 4-store bridge + 32-variant action bus is the architecture ux-spec 25 §4.3 wanted to keep and our Decision 9 rejects (UI must emit `EngineCommand`, never touch stores). Also home of the silent data-loss bug (18 §2.2). |
| **Cloud sync** | `src/hooks/use-cloud-sync.ts` (202) | ❌ | 1500 ms PUT debounce; out of scope; its failure modes validate 09 §6. |
| **Vendored FreeCut** | `src/freecut/{runtime,infrastructure,shared}/` | ❌ stale snapshot | gpu-compositor/effects/scopes/transitions vendored from FreeCut. Our reference mapping is spec 19 (nle-engine + FreeCut upstream); this snapshot is frozen and second-hand. |
| **Tests** | `tests/unit/` (~30), `tests/e2e/` (~20 spec files) | ✅ selectively | e2e specs like `keyboard-insert-workflow.spec.ts`, `multi-clip-drag.spec.ts`, `chronological-editing-session.spec.ts` are good UX-flow test *shapes* for our 17 Tier 3; the DOM-scrape assertion style is what our state-WYSIWYG harness replaces (17 §6.1). |

**Bottom line:** treat `use-timeline-drag.ts`, `nle-constants.ts`, `shortcut-registry.ts`, `MediaPool.tsx`, `TrackHeader.tsx`, `Viewer.tsx` (chrome), `InspectorPanel.tsx` (selective), and `ExportDialog.tsx` as the UX/app-scope reference set — exactly the files the ux-spec audited. Avoid the stores/bridge, mixer/color mock panels, and `src/freecut/`.

---

## 6. Prioritized Integration Plan

Ranked by gap-severity × leverage ÷ effort. Destination = the spec file the material lands in. "18+Δ" means a section-level amendment to spec 18 (bump to v1.1) rather than a new file.

| # | Action | Source (ux-spec) | Destination | Effort | Notes |
|---|---|---|---|---|---|
| 1 | **Context-menu enumeration** — 5 menus (clip/track-header/ruler/media-pool/timeline-empty), each item mapped to our 15-wire commands and 16 keymap; Shift+F10 keyboard route; resolves 18 §15.2 Q2. | 11 §4.1-4.5, gap 18 | 18 §4/§5 amendment (new §4.9 "Context Menus") | **M** | Highest-value single fill; every item must be command-backed (18 §5 rule); tier-3 testable via testids. |
| 2 | **Per-panel state rows** — empty/loading/search-no-results/error per panel with CTA copy; sample-project CTA in media-pool empty state. | 04 §4, 05 §4, 06 §4.1, 19 §2.2/§3.4 | 18 §4 (add a States row per panel) + §6 | **S** | Pairs with #10 (status strip). |
| 3 | **A11y floor expansion** — adopt the 12-item WCAG 2.2 AA list + F6 cycling + tabpanel pairing + canvas 1 Hz label + reduced-motion global; add Tier-3 a11y spot tests beyond the current 1 line in 18 §12. | 21 §2.2, 02 §7, 04 §9.2, 05 §8.2, 08 §5.2 | 18 §11 (expand ~4→~15 items) + 17 Tier-3 additions | **M** | Mostly normative-list writing; contrast table belongs with #5. |
| 4 | **Error & toast presentation layer** — error-class→presentation table over `CommandError` codes; toast conventions (4 s/6 s/persist); invalid-op cursor + message; error boundary; destructive confirms (extend beyond deleteScene); beforeunload prompt. | 20 §2.3-2.4, gaps 6/9/10/14/15 | 18 new §"Error & Notification UX" (or §6.4) + small 15/11 cross-refs | **S** | Pure UX-layer; zero engine impact. |
| 5 | **Visual-language deepening** — WCAG-verified contrast rules; 6-step type scale; icon conventions; motion tokens + anti-motion rules; tooltip latency; disabled language; state-visualization table; track-type + clip-label color strips. | 03 §3-§9 | 18 §9 (expand) + 05 §7 (clip badges/labels/fade triangles) | **S** | Map their token names to ours (ours wins on naming); theirs wins on coverage. |
| 6 | **Pointer/cursor grammar completion** — full cursor table; wheel semantics (Ctrl+wheel zoom-to-cursor); double-click resets; Shift+drag axis-constrain; cross-track-type prevention. | 11 §2-§3 | 18 §5 (extend contract table) + 05 §8 | **S** | Assert-through-commands per 17 rules; cursor states are DOM-level, testable via `data-cursor` or computed style. |
| 7 | **Perf budget + perf-UX list** — first paint < 1 s, TTI < 3 s, 60 fps @ 1080p/50 clips; memoization list, search debounce, code-split heavy modals, skeletons. | 21 §4.2, gap 14 | 18 §12 preamble + 00-master §"Non-functional requirements" (new short section) | **S** | Numbers are testable in Tier-2 smoke; also closes the "00 has no NFRs" hole. |
| 8 | **Media-pool depth** — grid/list+sort contract, drag feedback (ghost/cursors/track highlight), footer counts, metadata display bounded to 09 `MediaRecord`, missing-asset error state (badge + clip stripe; relink deferred with rationale). | 04 §2/§4/§5/§8, 17 gap 6/9 | 18 §4.2 (expand) + 09 §7 note | **M** | Bin tree = explicit v2 rejection (schema change); document in 18 §8-style ledger. |
| 9 | **Drag/ruler feedback** — TC tooltips during clip/trim drag (In/Out/Dur), snap-target flash, blade cut-line, in/out+loop ranges on ruler+scrubber, auto-scroll 70 % rule. | 06 gaps 11-14/19, 07 gap 9, 09 gap 8 | 05 §8/§9/§11 + 18 §5 preview column | **S** | All overlay-rendering; keep in 05's ownership, 18 cross-references. |
| 10 | **Save-status indicator + status-strip ownership** — "Saving…/Saved 2s ago/Save failed" chip in the 12 px status strip; scene dirty-dot stays. | 18(flow 18.1) §2.4 target, 17 gap 15 | 18 §4 (status strip) + §6.3 event map (autosave events from 09 §6) | **S** | Local-OPFS only; explicitly NOT cloud sync (record the rejection). |
| 11 | **Inspector field contracts** — timecode parsing, uniform commit semantics, reset-per-group, quick-seek buttons, source-asset card, tab-visibility rules. | 08 §2.4, gaps 5/8/14/16 | 18 §4.4 (expand) | **S** | All within the existing 4-tab ceiling (C2). |
| 12 | **Viewer affordances** — in-canvas overlays (clip name+TC; res/fps), scrubber richness (ranges, ticks, click-to-seek), empty-state CTA, fallback source-preview mode as the dual-viewer stand-in. | 05 §4/§6, gap 3 | 18 §4.3 (expand) + 05 cross-ref | **S** | Fallback mode partially answers 18 §15.2 Q1 without new commands. |
| 13 | **Deliver-page UX** — presets over the 3 export commands, cancel button for master jobs, progress rows + progressbar ARIA, retry, artifact-save presentation. | 15 §2.2/§4, gaps 2/4/16 | 18 §4.8 (expand) | **S** | EDL/JSON/MP3 formats REJECTED — log as future 15-wire candidates in the 18 §8 ledger. |
| 14 | **Sample project / demo fixture** — 30 s, 3 clips, 1 text, 1 audio; used by empty-state CTA and Tier-2/3 tests. | 19 flow 19.2 | 17 test-plan assets + 18 empty-state | **S** | Cheapest item with test ROI; aligns with 17's committed-assets rule (00 §13.7). |
| 15 | **Scope-change governance + gap-ledger machinery** — adopt the strike-marker/audit-note amendment format; build a Round-8 UI gap ledger from §4 above using the 22-style rubric (impact × blocking × cost), with hard counts. | 26 §3/§6, 22 §2/§3 | 00 (amendment process note) + new `audits/`-derived ledger for the coordinator | **S** | No product change; process insurance against re-litigating C1-C25. |

**Explicit rejection register (record, don't integrate):** 13-menu menu bar & 6-page IA (C1) · 6-tab inspector (C2) · cloudcut keymap rows (C3-C10) · Ctrl+drag-duplicate / Alt+drag-slip (C11) · snapshot undo (C12) · sequence tabs (C13) · MP4/EDL/JSON/MP3 export (C14) · Fairlight mixer (C15) · floating ColorPanel topology (C16) · FreeCut fork strategy (C17) · cloud sync/collab/auth (scope) · AI features incl. transcript v1 (scope; keep §16-principles breadcrumb) · screen/mic capture (scope) · monetization (scope) · multi-cam (scope + their own strike) · recording/voiceover (scope) · bins/smart bins (v2, schema) · relink flow (defer) · settings modal (18 §8.12 stands) · light theme (defer) · tour/docs-site (reject) · LUFS/EQ/dynamics (reject).

---

## 7. Verification Notes

- All 28 ux-spec files were read in full from `origin/ux-spec:docs/ux-spec/` (extracted read-only to `/tmp/uxspec/`; the cloudcut-nle repo was not modified — verified `git status` clean on `main` apart from pre-existing changes; only `git show`/`git ls-tree` were used against `origin/ux-spec`).
- Our side: `00-master-spec.md` (full), `18-ui-shell.md` (full), `16-keyboard-shortcuts.md` (§0, §3.1-§3.10, §7.3), `15-wire-protocol.md` (§4.1 union + structure), `05-timeline.refined.md` (§9-§11 + structure), `06-nle-ops.refined.md` (§4.4-§4.7), `09-project-model.refined.md` (§3.1 + structure), `17-test-plan.md` (§6.1 refs), `11-cloud-render.refined.md` (structure + progress refs). Section numbering for other specs is cited from header scans — **UNVERIFIED beyond the sections listed** where a §X claim is made without a direct read (notably: 03 §3.4/§4 timecode format, 08 §11/§15 scope ordering, 14-implementation-phases.md).
- cloudcut-nle code anchors (`DragMode` line 31, `SNAP_THRESHOLD_PX` line 22, `snapTime` line 68, `flushPending` line 157, registry `priority` line 40, `exportFcpxml` line 49) were verified by direct grep on the `main` working copy. Line numbers are as of commit `4ed97e2` (HEAD) and will drift.
- The ux-spec's own as-built claims about cloudcut-nle code were spot-checked against the working tree where cited above and held up (component line counts, registry architecture, mock-status of mixer/color panels).

---

**End of SCOUT-R8-C report.**
