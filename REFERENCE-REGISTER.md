# REFERENCE-REGISTER — the mock-surface reference register (Decision 35)

**What this is:** the standing index of WHAT the two in-tree shell mocks answer that the spec corpus asks — one row per censused mock surface family, cross-spec-owned (NOT 18 §0 — 18 is one owner among 08/09/16/05/12; the register is cross-cutting). Seeded (R25) from the shell-mock cross-cut's 19-family census; grown to 21 families at R27 (the mixer + view-state rows). **Every count and verdict below is the census's own** (`audits/fleet-r25/xcut-shell-mocks.md`, read from source — do not re-derive; the battery re-derives live per 35.2). The edit-mode family's deeper evidence is the ten `audits/fleet-r25/mode-*.md` reports. Law: `audits/ARCH-R25-edit-mode-completeness.md` **D35**.

**The law (D35):**

- **35.1 — the row form:** `surface | mock home (component+store) | test pins (count) | owning spec | citation form | divergence rule`. Mock homes are relative to `ui-mock/shell-variants/` and `ui-mock/shell-mini/`; store line numbers as censused R25. `citation form` ∈ {**DESIGN-REFERENCE** (the 08 §0 W-B class), **LAW-REGISTER** (the K3 acceptance class), **EXECUTABLE-WITNESS** (a spec row's named implementation evidence — the mode-audit class), **PROPOSAL** (C-ledger, awaiting adoption)}. `divergence rule` ∈ {**ENGINE-WINS** (D22b — the engine/package world wins; the mock yields or registers), **MOCK-YIELDS-REGISTERED**, **SPEC-WINS-UNADOPTED** (the pending ruling is NAMED in the row), **MOCK-IS-REFERENCE** (not law)}.
- **35.2 — battery-enforced freshness:** every row's test-pin counts re-derive live at every spec round (the battery scrapes the mocks' suites); **a count drift without a register amendment is a battery failure.** The suite-count pins at the top are the baseline.
  - **35.2-rider — the WRAP-gated re-key (the R27 coordination law, mock-verdicts §3.2, ratified 00 §2A.11):** the sibling's R24-2 (mini) + R25 (variants) waves land continuously; **per-wave movements are RECORDED, never chased**; the register re-keys a suite-count pin ONLY at the sibling's round-WRAP declaration, recording both the declared and the scraped figure; rows touched by in-flight waves carry a **REGISTER-PENDING** marker and NEVER a premature LANDED flip (the flip is the D26.4 retirement key). Current markers: variants **WRAPPED @ `0c7bf01`** (R25, 2026-09-13 — re-keyed R28: declared 1,950 runner-count / 126 stories; scraped 1,939/68 battery-method, both recorded per the rider); mini **IN FLIGHT** (the R24-2 wrap re-key at 495/12; the stream continues — retirement is post-K4/w1).
- **35.3 — the C-ledger intake discipline:** every proposal in the mocks' `.agents/SPEC-REVISION-CANDIDATES.md` (C33-C58 today) gets either a register row (ADOPTED / QUEUED-with-owner) or an explicit DECLINE with a reason — **no proposal ages untracked** (the root cause the R25 sweep found: five-round-old proposals re-derived by every fleet). The intake table below closes it.
- **35.4 — the four doc-level rulings land this round** (register adoption, not new design): SourceEditBar (18 §4.3's rows — register row 2, PROPOSAL→ADOPTED: the source-edit surface the D34 keyboard family depends on), SourceRangeBar (the source in/out marks — row 3, ADOPTED: the 3-point family's missing UI primitive; the marks are surface state, ops stay caller-supplied), the deliver RangeBand's spec rows (row 4, ADOPTED: the R23 W-F surface, never specced), and the 3-vs-5 page ruling (row 8 — a registered OPEN decision at D35.4, closed R28-D50; the table below is the record); marker v2 vs 09-A2 + captions stayed registered OPEN decisions (rows 5-6 + the OPEN table) until R28 closed them (D48/D49) — the conflicts stayed VISIBLE here instead of buried.
- **The K3/VLM + retirement tie (the register's operating law, from the xcut proposal):** the register is the INDEX side of the R23 K3 adoption — **the register rows are the verification worklist**: each DESIGN-REFERENCE/LAW-REGISTER row names what the crawl's VLM pass must see; each EXECUTABLE-WITNESS row names what the r1 port must match. The D26.4 mock-retirement triggers (post-K4/w1) key on register rows **flipping LANDED** (a mock surface retires when its owning spec row lands AND its row flips). 18 §0 + 12/17's K3 rows gain a one-line pointer at their next natural edit.

**The suite-count pins (the battery's freshness baseline — R28):** `ui-mock/shell-variants` **1,939 it-blocks / 68 test files REGISTERED** (the **R28 WRAP re-key** — the sibling's R25 round WRAPPED 2026-09-13 @ `0c7bf01`: 19/19 threads resolved, exit gates PASS (0 P1/0 P2; console 28/28 clean), tsc 0, build green; **the WRAP's declared figures: 1,950/1,950 tests (the runner count — the battery's line-start method reads 1,939; the 11-test gap = the `it.each(REPLAY_CASES)` 7-case fan-out @ insertPlan.test.ts:458 + the 4 extra loop iterations of the AA5_SITES-registered it @ appLayers.test.ts:131; 1,939+7+4=1,950 exactly) + 126 stories** (live source count: 11 .stories.tsx files, 123 StoryObj + 3 PresetStory exports — the declared public count confirmed; the R24-era 124 and the mid-flight reads 1,773/1,796/1,874 superseded; the canon "1521+" already retired at the R27 edit)) · `ui-mock/shell-mini` **495 / 12 test files — IN FLIGHT, sibling-maintained live-current** (their R24-2 waves W0-W5 + the W4-fix wrap re-key landed 2026-09-13: 357→377→396→415→428→441→445→495; census 163 units / 150 GAP / 462 authored — LAW-NET-INVENTORY §2.3's own header). A drift in the mini pin vs live, or a WRAP-declared variants figure left un-re-keyed for more than one round, = a battery failure; a mid-flight variants drift WITHOUT the REGISTER-PENDING marker = a battery failure.

**The census's headline (the report's own count, not re-derived):** 19 families — **11 aligned / 3 diverging / 8 carrying mock-only content** — the classes **OVERLAP by design** (compound verdicts: two families count in both their aligned/diverging class and the mock-only class). *(The R25 census's own count; rows 20/21 extend the census to 21 families — both in the mock-only class.)*

---

## The register (the 21 censused families — the R25 19 + R27's mixer + view-state additions)

| # | Surface family (census verdict) | Mock home (component + store) | Test pins (count) | Owning spec | Citation form | Divergence rule |
|---|---|---|---|---|---|---|
| 1 | **Edit-mode family** — the 7 insert modes (ops + preview grammar + keys) · **MOCK-ONLY** (the ten mode audits' filed ground — G1/G5-class rows; not re-litigated by the census) | variants: `src/components/shell/SourceEditBar.tsx` + `src/hooks/useInsertPreview.ts` + `src/lib/insertPlan.ts` (+ `editModeIcons.tsx`, `Timeline.tsx`'s preview layer) · store: `src/state/useUiStore.ts` insert family (:705-714) | **30** (insertPlan.test — the plan/apply split, "preview == commit by construction"; the battery's line-start method is THE method — the loose-rg 31 was a `split('` false positive at insertPlan.test.ts:490; one method, everywhere, the R27 census-method law) | 06 §5.9 / 15 / 16 / 18 §4.3 | EXECUTABLE-WITNESS (the mode-audit class — "the natural r1 reference") | MOCK-IS-REFERENCE (the registered reference, not spec law; the D30 mode-matrix + D31's 06 §5.9/§5.9B-F land the law THIS round; the four absent families are r1-scheduled spec-first per D30.2) |
| 2 | **Source-preview chrome** — viewerMode, the SourceEditBar law (one-shot, 150ms dwell/focus preview, honest refusal tips, wrong-asset law, roving tabindex, stay-in-source) + the W5b one-row law (h-8, no wrap, overflow-x-auto, 24px hit floor — superseded in-round by W1's priority ladder: the edit bar is the transport row's PRIORITY consumer, the readout/trim degrade first, all 7 buttons visible at every width ≥ the shell's min; a horizontal-scroll bar is a defect, r25:114-119) + the R25-A1 contract LANDED as their W1 (`a83d4d2`: stills get the full transport with 5s pseudo-duration; the SourceRangeBar merges into a real scrub strip — playhead + I/O bracket flags + strip dimming; play stops at out; JKL join the program grammar) · **MOCK-ONLY** | variants: `src/components/shell/Viewer.tsx` (viewerMode) + `src/components/shell/SourceEditBar.tsx` (293 ln) · store: `useUiStore.ts:652-653` (viewerMode/sourceMediaId) + `:1253` (enter/exitSourcePreview) | **18** (SourceEditBar.test) + **73** (useShortcuts.test — the source-mode `,`/`.` rows; W1's transport pins) | 18 §4.3 (ONE fallback paragraph; the C46 "18 §4.3 v1.2" anchor is a spec-QUEUE that never landed — **18 has ZERO source-edit rows**) + 18 §4.3's W6 amendments (V1-V6) absorb the stills-transport + strip-flags + priority-ladder laws — **LANDED (R27-W6)** | PROPOSAL→ADOPTED (C46; D35.4's SourceEditBar rows LANDED in 18 §4.3 v1.7 @ R25 — XMOCK-1/D1) | **LANDED-SPEC-R25** (18 v1.7's §4.3 rows carry the chrome contract; the wire posture: documented composites, no OT-parity claim) |
| 3 | **Source in/out marks** — the source scrub strip (the W1 merge: playhead + per-media I/O bracket flags + out-of-range dimming ON THE STRIP, never the poster; the duration readout switches to the [in,out] range; stills load at a 5s pseudo-duration — the honest static band clause is DEAD, V1) + the B7/B8 gesture laws (pointercancel/lostpointercapture, aria-orientation, Home/End on either handle, absolute drag) · **MOCK-ONLY** | variants: `src/components/shell/SourceRangeBar.tsx` (157 ln) · store: `useUiStore.ts:658-662` (`sourceRanges: Record<mediaId,{in,out}>` + setSourceRangeIn/Out/clear) | **20** (SourceRangeBar.test — W1's scrub-strip merge, mock-verdicts V5) | 18 §4.3 (the R25 rows + R27-W6's V1/V2/V5 amendments — the stills-transport + strip-flags laws) | PROPOSAL→ADOPTED (**D35.4's SourceRangeBar rows landed R25** — the 3-point family's input surface; the marks are surface state, ops stay caller-supplied — XMOCK-2/D2) | **LANDED-SPEC-R27** (18 §4.3's V1/V2/V5 amendments landed R27-W6; the mock stays the reference implementation; the marks remain surface state, ops caller-supplied) |
| 4 | **Deliver RangeBand + the coexistence law** — the 22px read-only ruler UNCONDITIONAL in TimelineCompact's head stack + the 32px band BELOW it on the deliver branch (54px total; the ruler's read-only in/out bracket FLAGS `shell-timeline-compact-flag-in/out`); the loop-seam three-writer law (ruling-21 drag, R14 ordering, [0,duration] clamp) with the SPLIT semantics: MOUSE preview clamps against the opposite LIVE edge (release commits exactly the preview); KEYBOARD keeps the R14 drag-along verbatim · **MOCK-ONLY** | variants: `src/components/timeline/RangeBand.tsx` (210 ln, below the compact ruler's head row) | **31** (TimelineCompact.test — incl. the band's clone) | 18 §4.8 (the coexistence-law rewrite LANDED R27-W6) + 15 §13.15 (the one-line pointer row rides 15's next natural edit — the P3 tail) | PROPOSAL→ADOPTED (**D35.4's deliver-RangeBand rows landed R25** — XMOCK-3/D3) | **LANDED-SPEC-R27** (18 §4.8's coexistence law landed R27-W6 — the R23 W-F surface finally specced) |
| 5 | **Marker v2** (range duration/notes + the REJECTED keyword) + **clip markers** (re-queued r5-entry) + the MarkerInspector rail-swap · **RULED-R28 (D48 — the OT-aligned subset)** | variants: `src/components/panels/MarkerInspector.tsx` + `src/lib/mockData.ts:106-117` (Marker) + `:75-76` (ElementJSON.markers) · store: `useUiStore.ts` (`addClipMarker` :1209 / `removeClipMarker` :1224) | **10** (MarkerInspector.test — re-keyed with the D48 flip) + the useUiStore/Clip marker families (:1900-1935 / :987-1007) | 09 §3.1A **A2 widened R28-D48: `Marker {id,time,label?,color?,duration?,notes?}` — point/range by duration; notes owns OT's `Bookmark.note`, label doc-side-synthesized** + 05 §11.1 + 16 §3.7 + 18 §4.4/§4.9 | PROPOSAL→ADOPTED-SUBSET (D48/R28 — C33 closed: duration+notes adopted; keyword REJECTED-registered; clip markers re-queued r5) | **LANDED-SPEC-R28** (09-A2's family widened per D48; the deviation text: the mock's keyword field retires with its **~3-pin keyword churn** — no OT home, no consumer; the clip markers ride r5-entry as ONE field+re-offset-laws bundle carrying **C33b's r5 acceptance pins — the 4 Clip.test pins + the insertPlan trim/split re-offset laws** — the C35 precedent's STRONGER form: the field alone corrupts offsets on split/trim) |
| 6 | **Caption track system** (`kind:'caption'`, addCaption first-free-slot, CaptionInspector + computed CPS) · **RULED-R28 (D49 — the track-kind hybrid)** | variants: `src/components/panels/CaptionInspector.tsx` + `src/lib/mockData.ts:12` (TrackKind) · store: `useUiStore.ts:1233-1239` (addCaption) | **10** (CaptionInspector.test — re-keyed with the D49 flip) | 09 (R28-D49: `SceneTracksJSON.captions: CaptionTrackJSON[]` — 0..n, one per language (BCP-47, descriptive v1), carrying `type:'text'` elements; `SubtitleSegmentItem` element path REJECTED v1) + 05 §7.3/§12.1/§12.2 + 18 §4.4/§4.7 + 10 §8 + 15 §4.3.22 (the addTrack `'caption'` kind + `language?`) | PROPOSAL→ADOPTED (D49/R28 — C34 closed: the track-kind + body-field hybrid, both halves) | **LANDED-SPEC-R28** (the FULL OT r1 cost stated: TrackType + `SceneTracks.captions` + the `TextElement` body field + the addTrack machinery branches; the body's home = first-class `ElementJSON.text`, retiring 10:544's `params?.text` stand-in; lane position TOP; one-track-per-language DESCRIPTIVE v1 — enforced uniqueness registered to r5) |
| 7 | **FX page** (fxMode one-flag law, FxBrowser row grammar + Fade rows, FxInspector, FX tool) · **RULED-R28 (D50 — the fifth page per the one-flag law)** | variants: `src/components/fx/FxBrowser.tsx` + `src/components/fx/FxInspector.tsx` · store: `useUiStore.ts:489-493` (fxMode — written ONLY by setTool/setPage) + `:210` (the fx density exemption) | **26** (FxBrowser 12 + FxInspector 14 — the W3 DnD routing pins) + **31** (TimelineCompact.test — the `auto\|on\|off` density law; the density note re-keys to the popover, row 21) | 16 §0/§3.8 (⌘5 row + the FX-tool radio row LANDED) + 18 §4.8 R28-D50 (**five pages — FX = the fifth page**; the §4.11 rail grammar rides the same fold) | PROPOSAL→ADOPTED (D50/R28 — the five-page ruling; XMOCK-6's ask closed) | **LANDED-SPEC-R28** (D50 — DECISION-closed vs IMPL-half distinct: the spec row is LANDED; the nle-ui FX-page absorption + the app's page-key tests ride XMOCK-6's r5/K3; the adopt-on-ruling content: the frozen `application/x-nle-effect` MIME contract, the Fade-rows→`setFade` routing law, the 27-transition registry, the fx-selection domain, ruling 22's delete-transition-first; **+ the R24-A1 DnD routing canon: replace-never-stack (a presentation swap retains duration+alignment; identical short-circuits with the 'Already X' toast), one law three doors (clip body / SeamZone / occupied TransitionBox), effect rows stack w/ ×N toast, empty-lane PURE no-op, adjacency refusal (SEAM_EPSILON 1ms), occupied-seam ⇄ surface (14→24px drop floor), dbl-click apply** — the W6 18 §4.11a absorption is the spec-side home) |
| 8 | **Page system: the 5-page dock** (Audio-toggle + FX) · **RULED-R28 (D50 — five pages, two pinned as modes)** | variants: `src/components/shell/AppDock.tsx` (:11-19; the Audio page is a TOGGLE — re-click exits, :53) + `AppShell.tsx` · `src/lib/shortcutMap.ts:84-91` (⌘1-⌘5) | **6** (AppDock.test — the audio-toggle + fxMode coupling rows grew) | 18 §4.8 R28-D50 (**five pages, not seven** — Edit/Color/Audio/FX/Deliver; Audio = a focus mode) + §8.4/§8.8 (the removal ledger re-keyed by the same fold); 16 §0's K3 row (the ⌘3/⌘5 reconciliation lands in the same edit) | PROPOSAL→ADOPTED (D50/R28 — the 3-vs-5 OPEN decision closed: five pages; XMOCK-7's ask closed) | **LANDED-SPEC-R28** (the 3-vs-5 divergence retired by D50 — 18 §4.8 amended to the five-page dock; the nle-ui FX-page absorption rides XMOCK-6/7's r5/K3 impl half; 20's M2-remainder still owns the real mixer surface at r2) |
| 9 | **Cheat-sheet surface** · **SPEC'D-ALIGNED, both directions thin** | variants: `src/components/shell/CheatSheet.tsx` + `src/lib/shortcutMap.ts` (63 rows — the single source CheatSheet and useShortcuts both read) | **14 + 73** (CheatSheet.test + useShortcuts.test) | 16 §7.3 (:660-671) | EXECUTABLE-WITNESS | MOCK-YIELDS-REGISTERED (the mock-stale half: 16's conflict-badges + Cmd+E JSON export are NOT mock-implemented; the mock's z-layer law, load-sample footer, live-count readout are registered mock-only niceties) |
| 10 | **Context-menu grammar** · **SPEC'D-ALIGNED + MOCK-ONLY micro-grammar** | variants: `src/components/shell/ContextMenu.tsx` + 5 hosts | **8** (ContextMenu.test) | 18 §4.9 (the five menus + chrome: 220px/28px/Ec/outside-click/focus-return/Shift+F10) | EXECUTABLE-WITNESS (+ the micro-grammar as PROPOSAL — D8's fold into 18 §4.9 at D1's edit) | MOCK-IS-REFERENCE (the micro-grammar — one-menu-open-at-a-time, Tab-dismisses, menuitemcheckbox/custom rows, keyboard-route placement, viewport clamp, close-then-dispatch — becomes contract at the app port) |
| 11 | **Pool multi-select grammar** (mediaSelection, listbox a11y, keyboard parity, hover-autoplay C42/C49) · **SPEC'D-UNDERDEFINED** | variants: `src/components/shell/MediaPool.tsx` · store: `useUiStore.ts` (mediaSelection, sorts, search, `mediaDrag`, `poolModeFilter`) | **31** (MediaPool.test — the R27 W6-C live read; not censused R25) | 18 §4.2 (ONE line: single-select + shift/cmd-click; the a11y listbox law is its last bullet) | EXECUTABLE-WITNESS + PROPOSAL (C42/C49 autoplay — C-ledger only) | MOCK-IS-REFERENCE (the mock's fuller grammar is the reference for the thin spec row) |
| 12 | **Save/load surface** (save chip, idle theater, failure drill) · **SPEC'D-ALIGNED** | variants: `src/components/shell/StatusStrip.tsx` (the save-state machine + the `simulateSaveFail` drill) · store: `useUiStore.ts:753` (saveNow/retrySave/simulateSaveFail); mini: none | — (not censused R25; inside useUiStore.test) | 18 §6.3 + 09 §6.1 | EXECUTABLE-WITNESS (W2.3 productized the dual-world machine) | MOCK-IS-REFERENCE (the mock's idle world is the registered discriminator) |
| 13 | **Undo history surface** (withHistory, one entry per intent) · **SPEC'D-ALIGNED** | variants: `useUiStore.ts:904` (withHistory, past/future snapshots); mini: the snapshot undo family (`src/state/useMini.ts`) | — (not censused R25; inside useUiStore.test 2006+) | 15 §6.2 strategy 2 + 06 §7 | EXECUTABLE-WITNESS | **ENGINE-WINS** (D22b — engine history first; the mock's store history yields) |
| 14 | **Tool-cursor system + trim-edge grammar** (8px hit strips, w/e-resize, selection-accent) · **SPEC'D-DIVERGES** | variants: `src/components/timeline/Clip.tsx` (the trim-edge hit-strip grammar) | — (the mode audits' ROLL-1/G5 rows own the conflict) | 18 §5A (16 cursor rows) | EXECUTABLE-WITNESS (the mode-audit class) | **ENGINE-WINS** (D22b — the three-way color/cursor conflict; the mock yields; mode-roll ROLL-1 + mode-slide G5 file it) |
| 15 | **Transport** (JKL shuttle, I/O-as-loop, marker nav) · **SPEC'D-ALIGNED** | variants: `useUiStore.ts:1130` (setShuttle); mini: the scrub bar + `seekToClipHead` (`src/state/useMini.ts`) | — (not censused R25) | 16 §3.1 + 03 §3.4 | EXECUTABLE-WITNESS (M28R is the engine-world BASE) | **ENGINE-WINS** (D22b — the mock's store-world shuttle yields; 16 §0 registers it verbatim) + the source-mode half LANDED mock-side (W1: J/K/L drive the source playhead; play stops at out) — registered spec-side by 18 §4.3's V4 amendment (LANDED R27-W6) |
| 16 | **Color surfaces** (the W2+W3 composition: the color console row = thin TAB strip [Timeline \| Nodes \| Scopes] with the Toolbar2 buttons activating their tabs — `ConsoleTabs.tsx`, the under-viewer pane RETIRED, region [2] ALWAYS viewer-led; scopes react to the PLAYHEAD FRAME (always-on, never selection); Gallery in the left dock (apply = REPLACE not merge, one setGrade patch); the YRGB curves rebuild (CurveSet {y,r,g,b}, y∘ch composition, ~64-bin histogram behind the grid); the wheels per R25-A3 (relative/accumulating drags, live preview + live YRGB readouts, master = horizontal dial + Ctrl/Cmd+drag, Shift = absolute jump, dbl-click = color-only reset, corner button = full reset, one undo per gesture); the 3-chip target breadcrumb [clip · track] ▸ [Clip grade \| Timeline grade] ▸ [Node n]; the timeline-grade copy 'applies to every clip in this timeline, after clip grades') · **SPEC'D-AS-REFERENCE** | variants: `src/components/pages/ColorPage.tsx` + the `pages/color/*` family · state: `src/state/mockGrades.ts` + the color store slice | **ColorPage 50 · ScopesDock 22 · StillsPanel 19 · ColorInspector 16 · GradedViewerCanvas 16 · WheelsPanel 17 · ConsoleTabs 8 · controls 6 · mockGrades 24 · lib/color 172** (colorSpace 31 / gradeMath 57 / gradedImage 26 / qualifierMath 32 / scopesMath 20 / index 6 — the W3 color wave + the F1 audit-fix, R28 W4 live read) | 08 (§0's W-B rows + §4.3a/§17.B/§20/§21 — the R27-W6 law set) | **DESIGN-REFERENCE** (the R23 W-B ruling — the precedent form itself; the R25-A2/A3 registrations LANDED-MOCK `b08b83a` — 08's W6 amendments F1/F8/F9 the spec-side absorption, LANDED R27-W6) | MOCK-IS-REFERENCE (not law — the reference class; the A3 model question = the OPEN table's 5th row, **RULED — D39**; the A2 supersession registered forward: R23 W-B (console row beside MixerDock) → R24-A2 (under-viewer pane, `0196c22`) → **R25-A2 (the TAB strip, LANDED `b08b83a` — the pane never lands in a spec)**) |
| 17 | **Deliver page** (presets/settings/queue) · **SPEC'D-ALIGNED on the W2.4 contract + MOCK-ONLY halves** | variants: `src/components/pages/DeliverPage.tsx`; the W2.4 surface in nle-ui | **34** (DeliverPage.test) + **13** (deliverViewStore.test — the W5 module `src/state/deliverViewStore.ts`) | 18 §4.8/§5 + 10 + 11 | EXECUTABLE-WITNESS + PROPOSAL (the #89 queue-replaces-preview layout ruling + the #88 preview-default) | MOCK-IS-REFERENCE (the mock-only halves ride XMOCK-3's r5 deliver polish; + the deliverViewStore laws (module zustand: jobs/showQueue single-writer, the store-owned 4×500ms mock render timer, unmount-survival); the W5 halves registered mock-only (the export-summary → console-row panel = placement detail; the custom JSON preset = DECLINE-class absent a 10/11 export-target ask, V18)) |
| 18 | **Mini: the embedding window, layout states, the R18k drag law, gap-fit, RH cuts, the C1/C16 key laws** · **SPEC'D-ALIGNED** | mini: `src/state/useMini.ts` + `src/hooks/useKeys.ts` (the C16 `e.repeat` gate; the C1 Space-yields law) + `src/shell/*` | **495** (12 test files — sibling-maintained, live-current; LAW-NET-INVENTORY: 163 units / 495 tests / 150 GAP / 462 authored — K3's acceptance list; the R24-2 waves W0-W5 + the W4-fix wrap landed 2026-09-13) | 18 §16.1-16.2 + OT-SEAMS | **LAW-REGISTER** (18 §16 IS the mini's spec) | MOCK-YIELDS-REGISTERED (the deviations live in README "What's OUT" + OT-SEAMS + CORE-SEAMS; retirement post-K4/w1 keys on this row flipping LANDED; the C16 gate is registered in 16 §0 as still-needed in canonical trees — a live gap row) |
| 19 | **Review loop (annotakit) + the VLM net** · **SPEC'D-ALIGNED** | mini: the vendored `vendor/storybook-annotakit` (the live review surface); variants: `scripts/vlm-*` (capture/review/rubric) | — (tooling; no census count) | 18 §12 note (the w1-entry charter) + 12/17's K3 rows | **LAW-REGISTER** (the R23 K3 adoption: "the VLM net adopts the mocks as the crawl's visual-verification tooling") | MOCK-IS-REFERENCE (the adopted tooling class; the register rows are the verification worklist per 35.4) |
| 20 | **Mixer family** — the audio-page mixer dock (binary toggle w/ lastVisual memory, the <280px pure-render floor, strip tiers T0-T2, aux-returns BOTH buses a1+a2, the de-esser Freq log-domain slider (√(2k·9k)≈4.24kHz midpoint), stripFocus clearing on scene switch, SoundLibrary) · **MOCK-ONLY** (18 §8.8 dropped the mixer panels under the 3-page ruling — the audio half of the 3-vs-5 decision, CLOSED-D50) | variants: `src/components/mixer/{MixerDock,ChannelEditor,ChannelStrip,MixerPrimitives,StripGraphs,SoundLibrary}.tsx` · store: `src/state/mockMixer.ts` (the module sidecar — never on ElementJSON, C50's precedent) | **MixerDock 45 + ChannelEditor 30 + ChannelStrip 50 + MixerPrimitives 49 + SoundLibrary 11 + mockMixer 15** (the R28 W4 live read, battery line-start method) | 20 (§7's M2 design round — r2-pending) | EXECUTABLE-WITNESS + PROPOSAL (the M2 queue) | MOCK-YIELDS-REGISTERED (rides row 8's 3-vs-5 decision — CLOSED-D50, the Audio focus-mode page is the mixer dock's home; 20's M2-remainder owns the real mixer surface at r2; C40's strip-chrome queue grows the element-visibility toggles, V17) |
| 21 | **Timeline view-state / ViewOptionsPopover** — the APG popover (Compact-tracks menuitemcheckbox DOM-ABSENT on fx, Clip style Filmstrip\|Block menuitemradio, Audio waveforms menuitemcheckbox w/ honest aria-disabled + reason while compact) + `setAllTrackWaveforms` (ONE withHistory write converges every audio track's flag — a converged no-change mints nothing) + the per-track flag→Clip-body render gate (flag false = no waveform band; thumbnail + name stay) · **MOCK-ONLY** (C57's B3 height-home seam) | variants: `src/components/timeline/ViewOptionsPopover.tsx` (+ TimelineToolbar's hamburger) · store: `useUiStore.ts` (the resolver + per-track flags + the batch :775/:1634) | **TimelineToolbar 38 + AppShell 78 (incl. the 7 density describes re-pointed W1) + TimelineCompact 31** (the R28 W4 live read, battery line-start method) | 18 §4.5/§4.7 (the view-options contract) + 05 §7.2/§12.2 (the render gate + the height home) | PROPOSAL (C57's B3 height-home rides; the popover contract is a W6 absorption item — landed 18 §4.7's F-12, R27-W6) | MOCK-IS-REFERENCE |

---

## The OPEN decisions (registered open items — the conflicts are VISIBLE here, not buried)

| Open decision | Register row | One-line disposition |
|---|---|---|
| **Marker v2 vs 09-A2** (C33 — the flat per-scene marker vs range/notes/keyword + clip markers) | row 5 | **CLOSED-D48** — the OT-aligned subset: duration + notes join 09-A2's family; keyword REJECTED-registered (no OT home/consumer — the mock's ~3-pin keyword churn named in row 5's deviation text); clip markers re-queued r5-entry as one field+re-offset-laws bundle (C33b's acceptance pins carried: the 4 Clip.test pins + the insertPlan re-offset laws) |
| **Captions C34** (the track-kind vs element-type model decision) | row 6 | **CLOSED-D49** — the track-kind hybrid: `SceneTracks.captions` (one per language) carrying `type:'text'` elements with the body field; the FULL OT r1 cost stated at row 6 (TrackType + `SceneTracks.captions` + the `TextElement` body field + the addTrack machinery branches; the body's home = first-class `ElementJSON.text`, retiring 10:544's `params?.text` stand-in); `SubtitleSegmentItem` rejected v1 |
| **The 3-vs-5 page shape** (18 §4.8 "three pages" vs the live 5-page dock) | row 8 | **CLOSED-D50** — five pages (Edit/Color/Audio/FX/Deliver; Audio = a focus mode, FX = the fifth page per the one-flag law); the 16 ⌘3/⌘5 reconciliation lands in the same edit; the nle-ui FX-page absorption + the app's page-key tests ride the r5/K3 impl half |
| **The FX-page grammar adoption** (18 §4.8/§4.11 amendment vs FX-as-a-mode of Edit) | row 7 | **CLOSED-D50** — LANDED-SPEC-R28 (the fifth page per the one-flag law; the FxBrowser row grammar + the fx-selection domain + ruling 22 land with it); DECISION-closed vs IMPL-half distinct: the nle-ui FX-page absorption + the app's page-key tests ride XMOCK-6's r5/K3 |
| **A3 wheels: 4 live EDITABLE YRGB fields vs 08 §16.A's verified (hue, amount)+scalar 28-f32 uniform** (r25:210-212; spec-08's F2) | row 16 (+ row 21 adjacent) | **RULED (R27 — D39: WIDEN-VIA-PROJECTION)** — Y writes the scalar, R/G/B solve (hue,amount) via the luma-neutral derivation, zero shader change; the projection-snap law + the round-trip law + the 2-to-1 convention + the offset dual-unit are the ruling's riders (08 §4.3a the law home, 00 D39 the register). The model question is CLOSED; r3-entry gates the parity fixtures' consumption |

*(R28 round-final: **0 open design rows** — marker v2 CLOSED-D48 · captions CLOSED-D49 · page-shape + FX grammar CLOSED-D50; the A3 row stands RULED (D39, R27) with r3-entry gating its parity fixtures' consumption.)*

---

## The C-ledger intake (D35.3 — every C33-C58 proposal dispositioned; no proposal ages untracked)

| # | The ledger entry (one line) | Register row | Disposition |
|---|---|---|---|
| C33 | Marker v2 model (range/clip markers, the inspector rail) | row 5 | **ADOPTED-SUBSET (R28-D48)** — duration + notes join 09-A2's family; keyword REJECTED-registered; clip markers re-queued r5-entry (the C33b field+re-offset-laws bundle); the XMOCK-4 ruling ask closed |
| C34 | Captions track kind + body field | row 6 | **ADOPTED (R28-D49)** — the track-kind + body-field hybrid (both halves; the full OT r1 cost registered at row 6); the XMOCK-5 ruling ask closed |
| C35 | Per-clip audio params (pan/pitchSemitones/eq on ElementJSON) | — (below census granularity; the Inspector audio tab rides no censused family) | **QUEUED** — the r2 audio round owns the field homes (20 §3-§4 scopes mixing per-track; the mock's fields are display-state, the engine consumes none); NOTE (R28-D48): the clip-marker re-queue cites this precedent in the STRONGER form — a field landing ahead of its laws corrupts offsets on split/trim, so r5's C33b bundle is field + re-offset-laws together |
| C36 | Color node graph layout (18 §15 item 3, deferred) | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23); the family's DESIGN-REFERENCE form |
| C37 | Deliver whole-view page (3-region layout) | row 17 | **QUEUED** — owner S-spec 18 (the mock-only layout halves: the #88 preview-default + the #89 queue-replaces-preview ruling; r5 deliver polish per XMOCK-3) |
| C38 | Fullscreen-viewer toggle REMOVED (spec text should drop the clause) | — | **QUEUED** — owner S-spec 18 §4.1 (drop the clause at the next natural edit; verified R25: 18:137 still lists the toggle; the mock removed it per reviewer th_mtoyu8bl) |
| C39 | Source-preview trigger widened (single pool-card selection entry) | row 2 | **QUEUED** — owner S-spec 18 §4.3 (verified R25: the v1.2 chrome contract landed WITHOUT the trigger list — 18's source-preview rows still name only the clip-menu + source-card triggers; fold the widened list in at the next natural edit; B4's one-gesture-one-meaning law kept; the mock's widened trigger stays the registered reference) |
| C40 | Strip chrome beyond the G-surface (record-arm/inserts/input rows) | row 8 | **QUEUED** — 20's M2 mixer design round (r2; deviation-registered mock-side) + the R13 element-visibility toggles (FX sends/pan/meter strips) — V17 |
| C41 | Meter palette (reference's continuous green→yellow) | — | **DECLINED** — keep-ours (the R19 decision stands, documented at the ledger row: the mock keeps the 3-zone palette + clip semantics at the reference's 14px width; 20 leaves meter styling unpinned — no spec change) |
| C42 | Pool hover-to-autoplay (poster frames, never full-res) | row 11 | **QUEUED** — the engine round's poster-frame family (18 §4.2's autoplay is C-ledger only; the decode surface is 03/09) |
| C43 | Scopes 2×2 dock (seeded traces) | row 16 | **SUPERSEDED** — by C53 (the ledger's own append-only note; the stopgap deleted in R20-W4b) |
| C44 | Inspector empty-state → active-track fallback | — | **QUEUED** — owner S-spec 18 §4.4 (the bless-or-blank ruling; un-ruled) + the refresh law rides V19's 18 §4.4 amendment (LANDED R27-W6 — F-10): on any view/editor-mode transition the inspector re-resolves its target; unresolvable = the empty state, never stale data |
| C45 | 7-mode source edit-function family | row 1 | **ADOPTED** — THIS round (D30/D31: the mode-matrix census in 06 §0 + the §5.9B-F family sections; the four absent families r1-scheduled spec-first) |
| C46 | Source-transport insertion-mode cluster (the SourceEditBar) | row 2 | **ADOPTED** — THIS round (D35.4: the 18 §4.3 rows — the source-edit surface the D34 keyboard family depends on) |
| C47 | Audio-source track routing law (media-type overrides selection-kind) | rows 1-2 | **QUEUED** — owner S-spec 06 §5.9/18 §4.3 (rides the mode-matrix authoring; the mock's `insertPlan.ts` resolveTargetTrack is the evidence) |
| C48 | Hover-placement preview (the plan/apply split) | rows 1-2 | **ADOPTED** — THIS round (D35.4's preview-geometry contract + D33's grammar register; "preview == commit by construction" lands in the 06/05 half) |
| C49 | Audio hover-dwell autoplay = waveform motion (extends C42) | row 11 | **QUEUED** — rides C42's engine round |
| C50 | mockGrades sidecar + undo snapshot extension | row 16 | **LANDED** — app-side (the W2.5 grade-sidecar extension `41f5bc9`, promoted 09 §0 BASE at R24; the `'timeline'` post-clip second pass stays a README-registered deviation); row 16 carries the reference form |
| C51 | ColorConsole layout (incl. the clip rail) | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23) |
| C52 | CPU preview pipeline | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23) |
| C53 | Real scopes (plots real graded-buffer data) | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23; supersedes C43) |
| C54 | Qualifier HSL keying | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23) |
| C55 | Curves tab (monotone spline + LUT bake) | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23) |
| C56 | Node-graph → grade binding | row 16 | **LANDED** — via 08 §0's W-B reference rows (R23) |
| C57 | Per-track height overrides + per-track FX home | — | **QUEUED** — owner S-spec 05 §12.2/18 §4.9 (the B3 height-home ruling; the mock's view-state answer — never snapshotted — is the reference) + the R3 per-page view-state memory + the R5 per-kind compact (compact-video / compact-audio / compact-all) — row 21's design round carries them (the W6 view-state row registered, 18 §4.7 F-12) |
| C58 | Project-level properties (the read-only ProjectSheet) | — | **DECLINED** — descoped per D4.4 (the ledger's own row: the full per-stage design is pending; the read-only sheet + the 18 §4.4/§11.6 tab-strip deviation note stand as the registered state; re-open only at 09's project round on user ask) |

**Intake state: 26 rows — 5 ADOPTED (R25: C45/C46/C48; R28: C33-subset/C34) · 8 LANDED (reference/W-B/app-side) · 10 QUEUED-with-owner · 2 DECLINED-with-reason · 1 SUPERSEDED (C43→C53). Zero untracked.**

*C59+ (the mock's own README deviation-ledger family — e.g. C59 the .drx/PowerGrade render-round boundary + the clip-level-stills gap) are MOCK-side gap labels, not C-ledger rows; corpus citations must name the ledger they mean.*

---

## Maintenance (who keeps this current)

- **The spec-round fleets maintain it** (the R25 xcut sweep seeded it): every fleet's mock-touching findings fold here; every landing that closes a row's gap flips the row **LANDED** (the flip is the D26.4 retirement trigger's key).
- **The battery enforces it** (35.2): the suite-count pins at the top + every row's test-pin count re-derive live at each round; drift without amendment = failure. The battery's register checks per ARCH-R25 §7, re-keyed battery_r27 §O': the file exists, the **21-family census** (19 + mixer + view-state), the C-ledger dispositions complete + the C59 mock-ledger footnote, the OPEN table (5 rows incl. the A3-wheels D39 ruling — the R28 round-final: the four design rows CLOSED-D48/D49/D50, 0 open design rows), the two suite-count pins (the WRAP-gated pair — the mini pin == live; the variants pin carries the R28 WRAP re-key: the scraped headline 1,939/68 + the declared pair 1,950 runner-count / 126 stories recorded).
- **The C-ledger intake re-runs every round** (35.3): new C-rows enter the intake table with a disposition; XMOCK-9's law — zero un-tagged C-rows older than two rounds.
- **Owners pointer-row at their next natural edit:** 18 §0 + 12/17's K3 rows each gain the one-line pointer to this file (D35.4).
