# Integration Review R15 — Cross-Stream Consistency (Round 15 Final Gate)

**Reviewer:** general-purpose (INTEGRATION-R15, Task ID R15-INT, fresh context, read-only)
**Date:** 2026-09-06
**Specs reviewed:** the Round-15 landing set — 00-master v6.0 (D15/D16/D17 + repo table + posture) · 14 (FULL REWRITE as the assembly plan) · 15 (§4.1A/§4.1B + §13.15 refresh + marker-note/181-bindings/R-example fixes) · 09/05/16/18/20 (A1-A6 + B1-B4 + N1-N15 amendments, agents AM1/AM2) · 19 v2.1 + 17 v1.2 (R15 re-baselines + roof suite, agent AM3) · `audits/ARCH-R15-assembly-and-path.md` + `REVIEW-R15-ARCH.md`/`REVIEW-R15-EXEC.md` + `SCOUT-R15-A/B/C/D` + `.agents/SPEC-REVISION-CANDIDATES.md` processed header.
**Method:** Per-check verification against HEAD `2b986f9` (diff base `d42693e`; round range `aa54c81..2b986f9`), grep across the whole canon (`*.md`, `audits/`, `.agents/`), side-by-side decision-text reads, a programmatic markdown table-pipe/fence battery over the 10 amended files, and numeric re-verification of the 19/17 re-baselines against the four SCOUT files. Record files (`INTEGRATION-REVIEW*.md`, `audits/*` except ARCH-R15's status line, `*-SIGNOFF.md`, `ui-mock/docs`) are treated as historical records per the review charter — old counts inside them are not flagged. Files opened at the cited lines; commit messages used only as pointers.

## Summary

- Cross-stream checks performed: 8 (each with verdict + evidence below)
- Issues found: 13 (0 BLOCKING, 6 MAJOR, 7 MINOR)
- Decision-text fidelity: D15/D16/D17 in 00-master match ARCH-R15 §1/§2/§3 clause-for-clause (projector home, pin-lockset, D12.2 re-typing, event staircase, multi-scene, roof suites — no drift found)
- Re-baseline fidelity: every numeric fact in 19/17/00 (engine 274+265+318 @f526e67, ~52k/51,968 LOC; OT 423=303+120 @0412e41 with the exact LOC split; WDC 721/30 files @374711c; mock 596/34 files @d42693e; pin distances 3420b5f→0412e41 = 35, 5243c49→374711c = 1) matches the scouts exactly — zero numeric drift
- Table continuity: 0 real pipe-count mismatches across the 10 amended files (one escaped-pipe false positive in 09, same class as R8's); code fences even everywhere
- The round's own centerpiece instrument (spec 15 §4.1A routing-disposition table) is the source of the two worst defects: it covers only **73 of the 78 union members**, misnames the Scene family, and its §4.1B companion claims a §6.3 registration that never landed

## Verdict: ❌ HOLD PUSH — 0 BLOCKING + 6 MAJOR issues must land before push (all mechanical, ~1-2 h of surgical edits)

The Round-15 architecture itself is sound and unusually well-verified: the decision texts are drift-free, the scouts' numbers carried into canon without a single digit of error, the 14 rewrite is exit-gate-faithful to ARCH-R15 §3.4, and both peer reviews re-reviewed and signed off with the named folds verifiably landed. The failures are all completion/propagation class — the same class R7 caught (an amendment whose companion edits didn't land): the routing table's member census, the NOT_IMPLEMENTED registration, the A1/A6 delete-chord propagation into 05/06, the stale-count counter-sweep (README wholesale, 19 §2.4, 05 §16.5, 06 §10.4/§10.5), 19's R9-era projector-home text, and the dangling `spec 14 §2.1` pointers the same round's 14-rewrite created.

---

## Per-check results

### Check 1: Decision-text consistency (00-master D15/D16/D17 vs ARCH-R15 §1/§2/§3)

**Status:** ✅ PASS

Side-by-side read of 00 `:357-379` vs ARCH-R15 `§1/§2/§3`:

- **D15 (evolve-in-place):** "four foundational repos… the product's code, not its references… fork-in/inherit, then close the spec-identified gaps in place" ↔ ARCH §1.4 verbatim ruling; the ~65-70% union figure, the evidence base (274+265+318 / 423 incl. 120 real-mouse / 721 / 596), the greenfield-ledger reference (ARCH §1.2), the REVIEW-R15-ARCH §4 steelman citation, the reversal condition ("independently probed and not found"), and the **D10 supersession note** ("repos are re-typed from references to product; Decision 10's citation discipline (§2.5.2) survives unchanged") all match. FCPXML/cloud greenfield-by-nature framing identical.
- **D16 (assembly):** all five laws match — (1) projector **ENGINE-home** at `nle-engine/src/lib/nle/projector/` (ARCH §2.2), additive under D9, SceneTracks TYPE-ONLY per the `scene-to-segments.ts:43` precedent, `Timeline` = parity oracle then permanent internal test substrate; (2) commands DOWN one union with the routing-disposition table at §4.1A + the **D12.2 re-typing** ("the engine's JSON-RPC+$ref surface is re-typed as INTERNAL transport… the 'retire JSON-RPC' clause of D12.2 is superseded" ↔ ARCH §2.3 pt 3 word-for-word); (3) telemetry UP a separate staircase (ARCH §2.3bis) incl. the playhead-ownership ruling (Clock = truth, imperative mirror not `core.seek`, commit on pause/scrub); (4) **multi-scene app-level** with per-core-undo-never-crosses and history budget (ARCH §2.4 sub-gates a/b); (5) no domain code in the app. The **pin-lockset** (ARCH §2.5: app OT/WDC pins ≥ engine pin's SHAs, move only WITH the engine pin, S5 parses `git submodule status`) appears in 00 D16 and in 17 §17A.1 S5 identically. Alternatives-weighed (§2.1bis) referenced from both.
- **D17 (four walls one roof):** 00's S1-S5 descriptions (tiers, contents, sizes' roles, CI composition: fast lane = tsc+S1/S2/S5 <5 min, S3 on PR+nightly, S4 nightly + HEAD-follow bump PRs) match ARCH §3.2/§3.3 clause-for-clause; the regression-continuity law (port-then-swap) is stated in all three of 00 D17 / ARCH §3.3 / 17 §17A.2.
- The 22-27 wk solo / 13-16 two-dev / demo 11-13 calendar is consistent across ARCH §3.4 and 14 §3 (see Check 6).

No drift found. (ARCH-R15's *status line* is a separate hygiene defect — MINOR-6.)

### Check 2: Routing-table integrity (spec 15 §4.1A / §4.1B / §13.15 / §6.3)

**Status:** ❌ TWO DEFECTS (Issues #1, #2) — homes story otherwise consistent

**(a) Member census: 73 of 78, under a "Members (78)" header (Issue #1 — the R7 Issue-#2 class).** The §4.1 code-block union (`15:161-256`, "Total: 78" at `:259`) and the Zod registry carry 78 members; the §4.1A table body (`15:272-297`) accounts for **73**:

- **The 4 Bookmark commands** (`ToggleBookmark/RemoveBookmark/UpdateBookmark/MoveBookmarkCommand`, union `15:205-208`, §4.3.39-42, Zod `15:4373-4376`) have **no routing row at all.** The Marker row (`15:289`) covers only `addMarker/updateMarker/deleteMarker` (3) and gestures at unification, but the union still carries both families (7 members) and every member-count story in canon stays at 78 — so 4 members are simply unowned. This violates §4.1A's own law ("**Every union member** is assigned ONE home") and fails the round's own new battery clause — 17 §13A.7's routing row (`17:2324`) asserts "zero unowned union members", which is false against the table as shipped.
- **`UpdateKeyframeCurvesCommand`** (union `15:240`, §4.3.67, Zod `15:4408`) is missing from the Keyframe row (`15:293`).
- **Scene row names three phantom members:** `createScene/loadScene/closeScene/updateScene` (`15:287`) — the union/§4.2/§4.3/Zod all say `createScene/deleteScene/renameScene/switchScene` (`15:348-350`, `:1286-1327`, `:3920-3935`). `loadScene`, `closeScene`, `updateScene` do not exist anywhere in spec 15; the three real members are absent.
- **Keyframe row cites a phantom plural:** `retimeKeyframes` (`15:293`, and again in §13.15's rename row `15:4862`) — the union's type is `RetimeKeyframeCommand` → `'retimeKeyframe'` (singular; `15:239`, §4.2 `:378`, Zod `:4222`). Upsert/remove are plural in the spec; retime is not — three sites in 15 repeat the wrong form.

The internals that DO hold: the "implemented = 24" reading (`15:299`) reconciles with the rows (7+1+4+3+1+3+3+2 = 24, all OT) and with §13.15's "24 of 78 (23 map 1:1 + rippleDelete wrapper)" (`15:4871`); the DEFERRED-families arithmetic (effects 5 + masks 4 + transitions 3 + clipboard 3 = 15 ≈ "≈15 members", `15:299`) is right; homes are consistent with §13.15's OT-as-editing-home + engine-façade story and D16 laws 2/4 (project/media/scene/selection-controller/setRate-setLoop/clipboard/snapshot → APP; effects/masks/transitions/export → ENGINE; editing ops → OT); and the preamble's pin baseline (OT @`0412e41`) matches the scouts.

**(b) `NOT_IMPLEMENTED` registration claimed but not landed (Issue #2).** §4.1B's own header says "registers into **§6.3's** error-code table" (`15:301`), and §4.1A's preamble points at "**§6.3A**" (`15:270`) — a section that does not exist anywhere in the corpus (grep: exactly one hit, the pointer itself). §6.3's `CommandError.code` doc-block (`15:2833-2884`, standard-codes list `:2842-2864`) was **not amended** — no `NOT_IMPLEMENTED` entry (its ~24 codes are the spec-15 set; the 5-code union §4.1B contrasts against — `INVALID_PARAMS/NOT_FOUND/CONFLICT/NOOP/INTERNAL_ERROR` — is OT's, which also does not appear in §6.3 as names). An implementer reading §6.3 (the canonical machine-readable list) never sees the new code. Related under-delivery vs ARCH-R15 §4's own impact-map row for spec 15 (promised: §4.1A ✓, §6.3 ✗, §13.15 ✓, "**§9 event mapping (engine-name↔spec-name register)**" ✗ — §9 `15:3283+` has no R15 marker, yet 17's new facet row `:2323` cites "15 §9" as the mapping register's home; "**§15.1 versioning-at-bus note**" ✗ — §15.1 `15:5035` is the unchanged file-layout section; ProtocolVersion actually lives at §10.1, which ARCH §2.3 itself miscites as "§15.1's ProtocolVersion envelope").

### Check 3: Amendment cross-refs (A/N/B across 09/05/16/18/20/15)

**Status:** ⚠️ PASS with two residuals (Issues #7, #8 — MINOR)

Every A/N/B resolution carries its candidates ID and a "(Round 15 amendment)" marker, and the named cross-spec pairs were opened on both sides:

- **A2 marker-per-scene:** 09 §3.1/§3.1A (`markers: Marker[]` per scene, Bookmark deleted) ↔ 15 §13.3's amended note (`15:4771`, "markers are now PER SCENE… project-level markers are retired") ↔ 16 §3.7's M/⌥⇧M rows (`addMarker`) + N8 note ↔ 18's transport-row/ruler-menu (`addMarker`, batched `deleteMarker`) — all four agree on the per-scene, one-family model. **Residual (Issue #7):** 16 §3.7's remaining rows still emit `removeBookmark` (Shift+M) and `updateBookmark` (⌥M), and 09's A2 says "16 §3.7's toggleBookmark/removeBookmark/updateBookmark trio… RENAME into this family" while 16's N8 note says those verbs "retire into the marker family via the A2 unification (09-side R15 pass)" — **each side names the other as the rename owner; neither side's rows were renamed** (16's "Color is stored on the bookmark (`Bookmark.color`)" line likewise). 15 §4.3.49-51's "Maps to: `engine.timeline.updateTracks(...)`" prose also still contradicts §13.3's R15 note ("mappings target the ACTIVE scene's markers… not a project-level array").
- **B1 linkedTo/sync-lock:** 09 §3.1A B1 (`ElementJSON.linkedTo?`, `TrackJSON.syncLock?` default true) ↔ 18 §4.5 N4 (link-OFF gates BOTH 05 §12.3 linked-selection propagation AND 06 §6 sync-lock move-following; view-level flag vs doc-level links) — both directions resolve (05 §12.3 exists `05:692`; 06 §6 exists `06:2035`); 16 §3.4/§6.1 #13 `toggleAVLink` present.
- **B2 pan/preservePitch:** 09 §3.1A B2 (pan −100..100, preservePitch, volume LINEAR 0..1 persisted) ↔ 18 §4.4's reword ("Volume %, pan (−100..100)… 'Gain dB' is withdrawn — no dB field exists in 09") — exact agreement, cross-refs bidirectional.
- **A4 mute/solo & A5 visibility:** 09 §3.1A ↔ 20 §4.2's G-surface authoring contract ↔ 18 §4.7's both-directions correction ↔ 15 §4.1A's Track row ("solo semantics per spec 20 §4.2 — see 09's A4 amendment", `15:281`) — one consistent story.
- **05 §11.2 retirement vs 16 §3.1:** 05 §11.2 ("N12 — SUPERSEDED", the `InOutPoints` model struck) ↔ 16 §3.1's N12 note ("this is the canonical in/out model: 05 §11.2 is RETIRED by this resolution") ↔ 09 §3.1A's N12/N5 note ↔ 18 §4.3/§4.9 (halves form; citation corrected §3.4→§3.1) — closed loop. 03's `clearInOutPoints` (`03:1112/:2013`) is the acknowledged playback-side consumer — correctly left alone.
- **05 §16.5A projector vs 14 A1 vs 00 D16 law 1:** 05 §16.5A (engine-home, additive/fenced, TYPE-ONLY per `scene-to-segments.ts:43`, parity-oracle-then-substrate, op-port waves 1-2 table) ↔ 14 §3's A1 row ↔ 00 D16 law 1 ↔ ARCH §2.2 — one ruling, four statements, zero drift (this also resolves REVIEW-R15-ARCH m1's "spec 05 §16.5A does not exist" — the section now exists).
- Also verified: N3 (09 `MediaRecord.importedAt`/numeric size ↔ 18 §4.2's formatting note), N10 (05 §11.1 ruler-SEEKS ↔ 18 §4.9's ruler menu note), N15 (16 §3.4 trim targeting ↔ 14 §4.1's rows), N13 (05 §5.2 zoom-ladder registration), the A1 delete-chord and A6 R-key resolutions in 16/18/15 (see Check 4 for the specs they did NOT reach), and 15 §13.5's 181-bindings sync + R→ripple example fix (`15:4782`, `:4793`) against 16's Appendix-A census (mechanically counted: **181 `kbd-` rows**, `16:2265` — the census claim is true).

### Check 4: Counter-claim sweep (the R7-style greps)

**Status:** ❌ REAL RESIDUE (Issues #3, #4, #5 + MINORs) — full census:

| Stale claim | Should be (R15 canon) | Live-canon hits |
|---|---|---|
| "202/202" engine tests | 274 vitest + 265 browser rows + 318 probes @f526e67 | `README:44` (live row, also ~47k LOC + 25/25 milestones); `00:290` (D10 body, ~47k/202); `06:2369` (live §10.4 preamble, engine @624a76b, 202, 25/25); `06:4` (status ledger — properly contextualized R9 note, pass); `19:287` (RESOLVED note, properly contextualized, pass) |
| "297/297" OT | 423/423 = 303+120 @0412e41 | `README:45`; `05:1170` (live §16.5 preamble: @`4e39b67`, 297/297, pre-W8 LOC census, "W4 components have LANDED"); `06:2402` (live §10.5 preamble: @`4e39b67`, 297/297); `19:55` (§2.4.3: "OT's 297-test suite" as the port acceptance bar) |
| "737/737" WDC | 721/721 PURE core @374711c (bridge relocated M1.6) | `README:46` (also still "plus the NLE bridge" — the bridge is now engine-side); `00:341` (D13 body); `20:7` (the old "Baseline:" header field, adjacent to the R15 re-baseline line) |
| "18 types"/"18 prefixed" C7 worklist | 24 since W5 | `00:304` (D12 law 2 body); `05:1199` (§16.5 table row "18-type prefixed union" — **direct contradiction with 15 §13.15's refreshed 24-type row, same corpus**); `19:54` (§2.4.2 "renames its **18** prefixed types… C7 worklist **unchanged**" — the worklist was refreshed this round); `19:239` (§6 C7 row: "all **18** headless types… **60 of 78 commands absent**" — both numbers now wrong: 24 implemented, 54 absent) |
| `Backspace` = ripple-delete | A1: Delete/Backspace plain-delete aliases; ⇧⌫/Shift+Delete the ONLY ripple chord | `05:567` (table row "Backspace \| Ripple delete"); `05:1659-1661` (`keyboard-backspace-ripple-delete` test: Backspace issues delete ripple=true); `05:1671` ("Cmd+X … same SceneState as Cmd+C followed by Backspace (ripple delete…)"); `05:1720` ("params.ripple = true (regardless of Backspace vs Delete)"); `06:3297-3302` (`keyboard-ripple-delete-backspace` + "Shift+Delete is the **alt** binding… same EngineCommand as Backspace") — all contradict 16 §3.4's A1 note and 18 §4.9's aligned row; 17 `:726-729` is consistent with A1 (Backspace ripple=false, ⇧ chord ripple) modulo a pre-existing `ids:`-vs-`elements` param nit |
| `R` = ripple-mode toggle | A6: R = ripple TOOL; ⌥R = mode | `05:558` (table row "`R` \| Ripple mode toggle"); `05:1718-1720` (`keyboard-r-toggles-ripple-mode`: R flips `TimelineViewState.rippleMode`); `05:61` (component tree "ripple mode toggle") |
| "8-13 wk" estimate | superseded by 22-27/13-16 + demo 11-13 | All canon occurrences carry the explicit supersession (ARCH §3.4; 14 §3 "The R9 estimate (8-13 wk) is superseded") — **PASS**; 00/17 carry no stale copy |
| "greenfield shell"/"one greenfield surface" | assembly layer + FCPXML | `README:81` (D14 row, live); `00:352` + `00:355` (D14 body: "the one greenfield surface… the only greenfield code is the shell" — contradicts 00's own §1 posture at `00:23`); ARCH-R9/SCOUT records exempt |
| "nle-bridge" at WDC | engine `src/lib/nle/bridge/` + `tests/vitest/nle-bridge.test.ts` | `20:30` (§3.1 "de-risked by `test/nle-bridge.test.ts`" in a web-daw-core-scoped parenthetical — 20's own §5/§11 notes carry the M1.6 correction; this one spot was missed) |
| `InOutPoints` as live canon | retired (setLoop halves) | `05:663` properly struck; 09/16/18 carry the resolution; 03's Clock-level API is the acknowledged consumer — **PASS** (only 03's `clearInOutPoints` remains, by design) |
| "project-level markers" as CURRENT canon | per-scene (A2) | `10:1665` ("For project-level markers (not attached to a specific clip)…" — descriptive FCPXML-export wording presuming the superseded model) |

**(Issue #3)** is the A1/A6 propagation column: 05 (in this round's amendment set!) and 06 (untouched) still carry the pre-A1/A6 keyboard contracts as live Tier-3 test definitions — an implementer writing 05's `keyboard-backspace-ripple-delete` or 06's `keyboard-ripple-delete-backspace` asserts the exact behavior 16 §3.4 now forbids. **(Issue #4)** is the README/stale-count column. **(Issue #5)** is 19 §2.4.1's projector home (below, Check 6). Records (`TESTABILITY-SIGNOFF`, `TEST-INTEGRATION-REVIEW`, SCOUT-R8-*, ARCH-R9, TEST-02 audit) are exempt per charter.

### Check 5: Table continuity (the R8 lesson)

**Status:** ✅ PASS

Programmatic battery over the 10 amended files (00/05/09/14/15/16/17/18/19/20): pipe-count-per-row vs header with code-span neutralization **and** escaped-pipe (`\|`) handling — **0 real mismatches** (09's migration table flagged once; the hit is the `color\|blur` escaped pipe inside `09:2193` — false positive, same class R8 documented). Code-fence parity: even in all 10 (54 fences in 16, 322 in 15, 120 in 09, etc.). No mid-table note insertions detected; all R15 insertions sit between sections or after tables. 16's Appendix A census re-counted mechanically: 181 rows, matching the header claim.

### Check 6: 14-rewrite integrity + dangling P-phase references

**Status:** ⚠️ EXIT GATES FAITHFUL; DANGLING REFERENCES FOUND (Issues #5, #6)

- **Exit gates vs ARCH-R15 §3.4:** row-for-row agreement — week−1 "both spikes green" / A0 "tsc clean; TimelineView renders in-app + HMR round-trip on a vendored file; fast lane measured" / A1 "parity green (audio + stills) on corpus; S1 projector laws; engine CI green; app pin bump" / A2 "S1 dispatch-complete + typed NOT_IMPLEMENTED honest; OT 423 + new op tests green" / A3 "S2+S3(a-c) green; DEMO: import(virtual) → cut → play → export mp4" / A4-v1, A4-v2, A5, A6, A7a, A7b gates and all wk estimates identical; the totals (22-27 solo / 13-16 two-dev / demo 11-13 / full-serial 29-30) and the 8-13 supersession sentence match verbatim.
- **Gap registers cite pins:** §4.1 OT @`0412e41`, §4.2 engine @`f526e67`, §4.3 WDC @`374711c` + engine bridge @`f526e67`, §4.4 ui-mock @`d42693e` — all four match the scouts; rows carry file:line cites (timeline.ts:4143/4246/2984/3155, timeline-core.ts:906/:950, use-timeline-zoom.ts:70). (§4.1A's per-row pin-SHA clause is a forward A2-time obligation — the R15 table carries the pin at preamble level only; NIT, folded into Issue #1's fix.)
- **P→A map:** §5 covers P0/P1/P-A/P2/P3/P4/P5/P6 with fates — internally consistent with §3.
- **Dangling references to the retired 14 §2.1/P-phase sections from OTHER specs (Issue #6):** the rewrite retired §2.1 (posture table) and P0-P6, but — `00:355` (D14 reasoning: "tabulated in **spec 14 §2.1**" — section gone; the same paragraph's "the only greenfield code is the shell" contradicts 00 §1); `06:2424` ("port into it per **spec 14 §2.1's** port plan" — now 14 §4.1 + 05 §16.5A); `17:2701` + `17:2322` (**the round's own new §17A/facet text** cites "00 D12.1 + **14 §2.1** + SCOUT-A §4" — a citation the same round's 14-rewrite invalidated; ARCH §3.2's S1 row carries the same now-dangling cite); `19:53` ("spec **14 P1**"); `17:2265` (§13A.5 header, "spec 14 P1's mandatory deliverable" — historical framing); `00:311` (D11 body, "spec 14 P1 gains the seam-integration phase" — point-in-time); `README:27` ("Phased rollout: **P0 (playback) → P6 (cloud render)**" — the retired ordering, in the entry document).
- **19 §2.4.1 projector home (Issue #5):** `19:53` still rules the projector's "implementation home: `src/scene/scene-projector.ts`, **spec 14 P1**" — the R9-era app-repo path and the retired phase — in a file this round re-baselined. This directly contradicts D16 law 1 / ARCH §2.2 / 05 §16.5A / 19's own §3.1 ("the remaining engine-side convergence duties are the union façade + projector (Decision 16…)"). The §6 C8 row (`19:240`) repeats "re-typed as the one-way PROJECTOR (`scene-projector.ts`)".

### Check 7: 19/17 re-baseline accuracy vs the SCOUT files

**Status:** ✅ PASS — 6/6 fact families exact

| Claim in 19/17/00 | SCOUT evidence | Match |
|---|---|---|
| engine 274/274 vitest = 237 engine + 37 bridge; 265/265 browser rows across 31 milestones (real WebGPU, `isSwiftShader: false`, ~8 min; `EXPECTED_MILESTONE_COUNT=31`/`MIN_TOTAL_TEST_ROWS=260`); 318 probe checks (`probe-p114-undo.ts` 318 PASS / 0 FAIL) @`f526e67` | SCOUT-A §1 table (`:21-22`), §10 verification (`:283` "TRUE on CI; locally conditional… 237 engine + 37 bridge") | ✅ exact (19 §3.1 tests row; 17 §17A preamble; 00 repo table) |
| ~52k LOC (51,968) across 58 files; engine-own timeline 8,936 (timeline.ts 7,480 + keyframe-store 817 + sync-lock 639) | SCOUT-A §4 + corrections (`:312` "51,968 LOC across 58 files in `src/lib/nle` (wc)") | ✅ exact |
| OT 423/423 = 303 in-page (32 suites) + 120 real-mouse (13 phases) = 45 milestone entries, 218.7 s, tsc 0 @`0412e41` (82 commits; `4e39b67` + 53) | SCOUT-B `:10`, `:23`, `:222` (executed, not trusted from docs; README "420/420" noted one round stale) | ✅ exact (19 §3.2; 00; 15 §13.15's "24 of 78" baseline) |
| OT LOC split: engine 9,651 / controllers 3,190 (11 files) / React view 7,413 (15 .tsx + 21 .ts) ≈ 20.2k + fixtures 1,988 + in-page harness 12,357 + real-mouse scripts 6,450 | SCOUT-B `:16-21` table, digit-for-digit | ✅ exact |
| WDC 721/721 (30 files, ~149 s wall / 53.65 s tests-phase), tsc 0, zero runtime deps, PURE core @`374711c` = `5243c49` + 1 docs-only; bridge relocated M1.6 (7 files, 6 verbatim @b837d60); 752→721 (31 bridge tests moved, none lost) | SCOUT-C `:16`, `:214` ("CONFIRMED by running"), §4/§7 | ✅ exact (19 §3.3; 20 header; 17 §13A.5's "721/721 — Round 15 re-run") |
| mock 596/596 (34 files, 218.9 s), tsc 0, 71 stories, PR #1 OPEN @`d42693e`, no CI workflow, ~8.9k impl LOC | SCOUT-D `:225` ("vitest run 34/34 files, 596/596 tests"), §1/§10 | ✅ exact (19 §3.5 NEW; 00's ui-mock row) |

Pin distances also verified: engine's `vendor/opencut-timeline @3420b5f` = 35 commits behind `0412e41` (SCOUT-A `:81`, SCOUT-B `:179` — "NOT pre-W8; it is the W8-d endpoint") and `vendor/web-daw-core @5243c49` = 1 docs-only commit behind `374711c` — 19 §3.1's submodule row states both correctly. Zero numeric drift found anywhere in the re-baselined text.

### Check 8: ARCH-R15 internal consistency (v2.1 folds)

**Status:** ✅ PASS (one hygiene defect — Issue #12)

- **Section numbering:** §2.1 → §2.1bis → §2.2 → §2.3 → §2.3bis → §2.4 → §2.5 → **§2.6** (punch list, `audits/ARCH…:150`) → §3 → §3.1-§3.5 → §4 → §5 — the re-review's "§2.5 jumps to §2.7" numbering gap is fixed (fold #1 landed).
- **The five named re-review folds all landed:** numbering (above); **demo derivation** — §3.4 now carries "(derivation: week−1 + A0 + A1 + A2 serial-critical-chain ≈ 10.5-13 wk; chrome-port work inside A3 overlaps A1/A2; the demo exit itself is serially gated on A1+A2)" + "A2.5 riding PARALLEL to A3 (full-serial high end ≈ 29-30)" (EXEC N1's arithmetic caveat resolved in substance; the reviewer's optional "op wave 1 after the demo" sentence did not land verbatim — NIT); **playhead punch item** — §2.6 "Plus the imperative playhead surface (A3, ~0.5d): TimelinePlayhead.tsx:50 renders from core.getCurrentTime() — without a new imperative set-mirror API, the OT playhead FREEZES during engine playback" (EXEC N2 line-itemed; 14 §4.1 carries the matching gap-register row); **S3a exception** — §3.1 "with the one sanctioned exception of S3(a), which re-runs OT's real-mouse phases specifically to verify the VIEW COMPONENT in the app's host context"; **viewer pull** — §2.3bis "**Viewer frames are a PULL, not a push** — the viewer subscribes to time events and calls renderFrame/renderFrameOffscreen (player.ts:2995) on its own cadence."
- **Both re-review sections exist and are signed off:** REVIEW-R15-ARCH §RE-REVIEW (R15-PR1b, "Amendment score: 7/7 LANDED… SIGNED OFF — D15/D16/D17 may land") and REVIEW-R15-EXEC §RE-REVIEW (R15-PR2b, "BUILDABLE — SIGNED OFF", N1/N2 folded as required-on-landing — both verified landed above).
- **Residual (Issue #12):** ARCH-R15's status line still reads "**v2** — post peer-review… all amendments folded; **re-review gate pending**" (`audits/ARCH…:3`) — but both re-reviews are done, signed off, and folded; the round's own commit messages and the task ledger call the file v2.1. The document's self-description is one gate behind its actual state. (Also ARCH §2.3's "spec 15 §15.1's ProtocolVersion envelope" pointer — the envelope is §10.1; §15.1 is file layout.)

---

## Issues list

| # | Severity | Issue | Where |
|---|---|---|---|
| 1 | **MAJOR** | §4.1A routing-disposition table covers **73 of 78** union members: the 4 `*BookmarkCommand` members (§4.3.39-42, Zod) and `UpdateKeyframeCurvesCommand` (§4.3.67) have NO row — violating the table's own "every union member is assigned ONE home" law and 17 §13A.7's "zero unowned union members" battery clause (which fails against the spec's own table as shipped). Scene row names 3 phantom members (`loadScene/closeScene/updateScene`; union says `deleteScene/renameScene/switchScene`); Keyframe row cites phantom plural `retimeKeyframes` (union: `retimeKeyframe`) and omits `updateKeyframeCurves`; preamble points at nonexistent **§6.3A** | `15:270-297` vs `15:161-256, 348-354, 378-379, 205-208, 240`; `17:2324` |
| 2 | **MAJOR** | §4.1B claims "registers into §6.3's error-code table" — §6.3 was never amended (no `NOT_IMPLEMENTED` in the `CommandError.code` list). ARCH-R15 §4's impact-map row for spec 15 is ~2.5/5 landed: §9 event-mapping register and §15.1 versioning-at-bus note never landed (17's new facet row `:2323` cites "15 §9" as the register's home; ARCH §2.3 itself miscites ProtocolVersion as §15.1 — it is §10.1) | `15:301-303` vs `15:2833-2884`; `audits/ARCH…§4`; `17:2323` |
| 3 | **MAJOR** | A1/A6 resolutions not propagated: 05 (in this round's amendment set) and 06 still carry the pre-A1/A6 keyboard contracts as live Tier-3 test definitions — `Backspace`=ripple-delete (05:567, 1659-1661, 1671, 1720; 06:3297-3302 incl. "Shift+Delete is the alt binding… same EngineCommand as Backspace") and `R`=ripple-mode-toggle (05:558, 1718-1720, 61) — directly contradicting 16 §3.4's A1/A6 rulings and 18 §4.9's aligned row | `05:61, 558, 567, 1659-1720`; `06:3297-3302` vs `16:72, 208-223`; `18:197` |
| 4 | **MAJOR** | Counter-claim sweep residue vs the R15 re-baseline: README wholesale stale (202/202+47k+25/25; 297/297; 737/737 + WDC-bridge claim; "14 architectural decisions"; "Phased rollout: P0 → P6"; "~180 bindings"; D14-only decisions list; "seal round remains" status) — README untouched this round; 19 §2.4.2/§2.4.3/§6-C7 ("18 prefixed… worklist unchanged", "297-test suite", "all 18 headless types… 60 of 78 absent"); 05 §16.5 preamble + row (@`4e39b67`, 297/297, "18-type prefixed union" — a live 05-vs-15 contradiction); 06 §10.4/§10.5 preambles (engine @`624a76b` 202/25; OT @`4e39b67` 297/297) | `README:13, 27, 29, 44-46, 60, 81, 111-130`; `19:54, 55, 239`; `05:1170, 1199`; `06:2369, 2402` |
| 5 | **MAJOR** | 19 §2.4.1 still rules the projector's implementation home as `src/scene/scene-projector.ts`, spec 14 P1 — the retired R9 app-repo path/phase — contradicting D16's ENGINE-home ruling (`nle-engine/src/lib/nle/projector/`), 05 §16.5A, 14's A1 row, and 19's own §3.1; §6's C8 row repeats `scene-projector.ts` | `19:53, 240` vs `00:367-370`; `05:§16.5A`; `14:§3 A1` |
| 6 | **MAJOR** | Dangling references to retired 14 §2.1 / P-phase sections — created by this round's own 14-rewrite: 00's D14 reasoning paragraph ("tabulated in spec 14 §2.1" + "the only greenfield code is the shell", contradicting 00 §1's posture); 06:2424 (port plan); **17:2701 + 17:2322 (the round's own new §17A/facet rows cite "14 §2.1")**; 19:53 ("spec 14 P1"); README:27 ("P0 → P6") | `00:352-355`; `06:2424`; `17:2322, 2701`; `19:53`; `README:27` |
| 7 | MINOR | A2 marker-family rename is a mutual deferral: 09's A2 says 16 §3.7's bookmark trio "RENAME into this family"; 16's N8 note says the verbs "retire into the marker family via the A2 unification (09-side R15 pass)" — neither side renamed the live rows: 16 §3.7 Shift+M still emits `removeBookmark`, ⌥M `updateBookmark`, "Bookmark.color" line stands; 15 §4.3.49-51's "Maps to: engine.timeline.updateTracks(...)" contradicts §13.3's R15 note; the union's 4 Bookmark members still need a disposition (folds into Issue #1's fix) | `16:270-275`; `09:305`; `15:1527-1559` |
| 8 | MINOR | 18 §4.3's N5 cross-ref points at "15 §4.3.29's R15 invariant (end > start validated / halves swapped)" — §4.3.29 was never amended; the invariant lives in §13.15's SetLoop row (and in 05 §11.2/16 §3.1/09 correctly) | `18:146` vs `15:1157-1171, 4872` |
| 9 | MINOR | Decision-body counts not refreshed/annotated: D10 body (~47k LOC, 202/202), D13 body (737/737), D12 law 2 ("renames its 18 prefixed types") — R9's own precedent (INTEGRATION-REVIEW-R9 MINOR-2) fixed D10's body in-session | `00:290, 304, 341` |
| 10 | MINOR | 20 §3.1's web-daw-core-scoped "de-risked by `test/nle-bridge.test.ts`" citation missed the M1.6 relocation (20's own §5/§11 notes carry it); 20's old "Baseline: 737/737" header field sits unannotated above the R15 line | `20:30, 7` |
| 11 | MINOR | 10:1665 describes marker emission in FCPXML terms presuming "project-level markers" — A2's per-scene ruling supersedes the model (descriptive wording, needs an A2-consistent pass at A6) | `10:1665` |
| 12 | MINOR | ARCH-R15's status line still "v2 … re-review gate pending" — both re-reviews (PR1b/PR2b) are signed off and the folds verifiably landed; the file everyone cites as v2.1 self-describes one gate behind | `audits/ARCH…:3` |
| 13 | MINOR | NIT-class: §4.1A's per-row pin-SHA battery clause not yet satisfiable (pin only in the preamble — forward A2 obligation); 17:2265's "spec 14 P1" historical framing; 17:726's `deleteElements({ids:…})` param naming (pre-existing); 16's Appendix-A footer still says 15 §13.5's citation "predates the R15 pass — flagged for the 15-side sync" (the sync landed); ARCH §2.3's "§15.1's ProtocolVersion" pointer (§10.1); 05:1170's "W4 components have LANDED" (W8 superseded) | `15:270`; `17:2265, 726`; `16:2265`; `audits/ARCH…§2.3`; `05:1170` |

## Resolution table (recommended fixes — coordinator)

| # | Fix | Files / lines |
|---|---|---|
| 1 | Add the 5 missing §4.1A rows: one Bookmark row (home OT, "renamed into the unified marker family per A2 → A2", or fold the 4 members into the Marker row with an explicit disposition note); one `updateKeyframeCurves` entry in the Keyframe row; fix the Scene row's names to `createScene/deleteScene/renameScene/switchScene`; fix `retimeKeyframes` → `retimeKeyframe` (3 sites: §4.1A, §13.15's rename row, and the "(plural batch forms)" parenthetical); repoint the preamble's "§6.3A" → "§4.1B"; re-run the member census (must sum to 78) | `15:270-297, 4862` |
| 2 | Add `NOT_IMPLEMENTED` to §6.3's standard-codes list (one doc-block line, cross-ref §4.1B); EITHER land the §9 event-mapping register + §15.1 versioning-at-bus note as ARCH §4 promised, OR annotate ARCH §4's impact-map row + 17:2323 with "deferred to A2" so the impact map matches the landing; fix ARCH §2.3's ProtocolVersion pointer to §10.1 | `15:2842-2864, §9, §15.1`; `audits/ARCH…§2.3, §4`; `17:2323` |
| 3 | Sweep the A1/A6 propagation: 05's table row 567 + test rows 1659-1661/1671/1720 + `R` rows 558/61/1718-1720 and 06's rows 3297-3302 → re-write to the A1/A6 forms (Backspace = plain-delete alias; ⇧⌫ the only ripple chord; R = ripple tool, ⌥R = mode; cut = the `cut` command, not "Cmd+C followed by Backspace") — 05 was in the round's amendment set, so this is a completion fix, not a new amendment | `05:61, 558, 567, 1659-1720`; `06:3297-3302` |
| 4 | R15 README milestone (the R9 review's README-fix precedent): repo-table rows → R15 SHAs/counts (274+265+318/52k; 423/423; 721/721 pure-core + bridge-at-engine; +ui-mock row); "14 architectural decisions" → 17; 14's row → "assembly plan (week −1 + A0-A7b + gap registers)"; "~180" → 181; decisions list + status → D15/16/17 + ARCH-R15; drop the "seal round remains" line. Plus the four stale canon spots: 19:54/55/239 (24 prefixed / 423-suite / 54-of-78-absent), 05:1170+1199 (@`0412e41`, 423/423, 24-type), 06:2369/2402 (@`f526e67`, 274+265+318; @`0412e41`, 423) | `README:13-130`; `19:54, 55, 239`; `05:1170, 1199`; `06:2369, 2402` |
| 5 | 19 §2.4.1: replace "implementation home: `src/scene/scene-projector.ts`, spec 14 P1" with the D16 ruling (ENGINE-home `nle-engine/src/lib/nle/projector/`, A1; one-way; Timeline = oracle); same for the C8 row's `scene-projector.ts` mention (keep the R9 history note if wanted, marked superseded) | `19:53, 240` |
| 6 | Repoint the dangling 14 references: 00:355's paragraph → add a D14 supersession note (D15/D16 own posture + greenfield surfaces; the per-repo posture table is retired — see 14 §2/ARCH §1) and drop "spec 14 §2.1"; 06:2424 → "spec 14 §4.1 / 05 §16.5A's op-port table"; 17:2701+2322 → "00 D12.1 + 14 §3 A1 + SCOUT-A §4" (or "05 §16.5A"); 19:53 → 14 §3/ARCH §2.2; README:27 → the assembly-plan row | `00:352-355`; `06:2424`; `17:2322, 2701`; `19:53`; `README:27` |
| 7 | Finish the A2 rename on 16's side (Shift+M → `deleteMarker`, ⌥M's save verb → `updateMarker`, "Bookmark.color" → "Marker.color") or land a single dated note in BOTH 09 §3.1A and 16 §3.7 naming ONE owner; align 15 §4.3.49-51's Maps-to prose with §13.3's R15 note; give the union's Bookmark block its disposition at the next union-version bump (with Issue #1's row) | `16:270-275`; `09:305`; `15:1527-1559` |
| 8 | 18's N5 pointer: "15 §4.3.29's R15 invariant" → "15 §13.15's N5 row (§4.3.29 unamended)" — or amend §4.3.29 with the one-line invariant | `18:146` |
| 9 | Decision-body hygiene pass (R9 precedent): annotate D10/D12/D13 bodies with "(counts superseded at R15 — see the repo table)" or re-type the numbers | `00:290, 304, 341` |
| 10 | 20:30's citation → `nle-engine/tests/vitest/nle-bridge.test.ts` (the §5 note's form); mark 20:7's Baseline field "(R9-era; superseded by the R15 line below)" | `20:30, 7` |
| 11 | 10:1665 → reword to scene-scoped markers at the A6 pass (nano-fix now if convenient) | `10:1665` |
| 12 | ARCH-R15 status line → "v2.1 — re-review gate PASSED (R15-PR1b SOUND/SIGNED OFF + R15-PR2b BUILDABLE/SIGNED OFF; all folds landed)" | `audits/ARCH…:3` |
| 13 | Optional polish: the six NITs in Issue #13's row | as listed |

## Final verdict

**❌ HOLD PUSH.** 0 BLOCKING + 6 MAJOR + 7 MINOR. The Round-15 architecture — evolve-in-place (D15), the nle-app assembly with ENGINE-home projector + two-staircase seam (D16), four-walls-one-roof (D17), the 14 assembly plan, and the scout-verified re-baselines — is coherent, decision-for-decision drift-free, and numerically exact against its evidence base. The failures are completion failures inside the round's own landing: the §4.1A member census (73/78 + phantom names + a §6.3 registration that never landed), the A1/A6 propagation into 05/06, the counter-claim sweep (README wholesale; 19/05/06 stale counts contradicting the re-baseline), 19's R9-era projector-home text, and the dangling `14 §2.1` pointers the 14-rewrite created. Every fix above is mechanical and scoped (~1-2 h); none re-opens a ruling.

**Recommended next actions:**
1. Coordinator lands fixes for Issues #1-#6 before push (Issues #1/#2 are the round's own normative instrument — the S5 battery's "zero unowned union members" check fails against the spec as shipped).
2. MINORs #7-#12 in the same pass where cheap; #13 can ride the A2/A6 phases.
3. Re-run this review's mechanical battery after the fixes: §4.1A census = 78 (mechanically countable); `NOT_IMPLEMENTED` present in §6.3's code list; `loadScene|closeScene|updateScene|retimeKeyframes` zero-hits in 15; `Backspace.*ripple|ripple-delete-backspace` zero-hits in 05/06; `202/202|297/297|737/737|18 prefixed|18-type` zero-hits in live canon (README/19/05/06); `spec 14 §2.1|14 P1` zero-hits outside historical framings; `scene-projector.ts` zero-hits in 19.
4. Fold a one-line addendum into the R15 worklog/ARCH status recording this review + the fixes (per the R7 precedent).
5. Add the missing impact-map items (§9 event register, §15.1 versioning note) to the A2 phase's deliverable list if not landed now (ARCH §4's 15-row currently over-promises vs the landing).

## Addendum — fix wave (R15-FIX, 2026-09-06)

All 13 issues from the resolution table landed in the R15-FIX surgical pass (Task ID R15-FIX): §4.1A census re-counted to 78/78 (Bookmark row + `updateKeyframeCurves` + Scene-name fix + `retimeKeyframe` singular at all 3 sites + §4.1B pointer); `NOT_IMPLEMENTED` registered in §6.3; §9.5 event-mapping register + §10.1 versioning-at-bus note landed; A1/A6 propagated into 05/06 (table rows, test contracts, R-rows, component tree — all marked "(Round 15 amendment: A1/A6 propagation)"); README fully re-baselined (repo table R15 SHAs/counts, 17 decisions, assembly-plan row, 181 bindings, status → "spec final push landed R15; assembly plan chartered"); 19/05/06 stale-count residue re-typed; 19 §2.4.1 + C8 row moved to the D16 ENGINE-home projector ruling; dangling 14 §2.1/P1 references repointed (00 D14-body supersession note, 06:2424, 17:2265/2301/2322/2701, ARCH-R15 §3.2); MINORs #7-#12 landed (16 §3.7 marker verbs + the dated single-owner note shared by 09 §3.1A, 15 §4.3.49-51 Maps-to alignment, 18's N5 pointer → §13.15, 00 D10/D12/D13 body annotations, 20:30 citation + 20:7 baseline annotation, 10:1665 scene-scoped reword, ARCH-R15 status → v2.1 re-review gate PASSED); NITs #13 where trivial (16's Appendix-A footer, 17:726 param naming). Verification battery re-run: §4.1A census = 78 (programmatic, union-vs-table cross-check); `loadScene|closeScene|updateScene|retimeKeyframes` zero-hits in 15; `Backspace.*ripple` remaining hits in 05/06 assert the A1-correct contracts (Backspace = plain-delete alias / ripple-MODE semantics); `202/202|297/297|737/737|18 prefixed|18-type` remaining live-canon hits all carry R15 supersession annotations (records exempt); `spec 14 §2.1` remaining hit (00 D14 body) carries the supersession note; 00-master = exactly 17 decisions; table pipe-continuity + fence parity green across all 13 changed files. Verdict flipped: **HOLD PUSH → CLEARED.**

*End of Round-15 Integration Review Report.*
