# 19 — Code References & Engine Reconciliation: Canon Hierarchy, Reference Map, Insight Ledger

**Stream:** Cross-cutting (reference architecture)
**Spec file:** `19-code-references.md`
**Status:** v3.2 (Round 24 — the fleet re-pin + the D26/D27/D28/D29 reflection: ALL assets re-pinned to the 2026-09-08 R24 HEADs — nle-engine `5036387` **458/458** (the W2.5/D29 effects sidecar LANDED @ `8a0b7fe` + the R8-REV blur-radius clamp @ `4b2dfd1`; N2b @ `37cdd28` + RR1-A + the MIDI path stand; vendors OT @ `c15a629` + WDC @ `494f6ff`); opencut-timeline HEAD `ded43c4`, **code pin `c15a629`** (src diff empty — the last three commits are docs/runner-artifacts only) **536/536 — RUN LIVE by the module audit** (59 milestone entries = 386 in-page + 150 real-mouse; W10F hardening + **W11 the complete-UI round** — `use-wire-dispatch` + the M49C coverage gate (24 routed wire verbs + 6 exceptions) + M49T/H/R/G; **the canonical React timeline UI tree per Decision 25** — `src/components/timeline/`, **40 files / 8,604 LOC**; the app's vendored `src/lib/timeline` lock-copy **byte-exact** @ `c15a629`); web-daw-core `85b81b0` **759/759** (docs-only seal-round wrap past `494f6ff`; W2 + whole-CR + the S4 seal stand; the drift-gate fails-on-drift; engine consumes @ `494f6ff`, app @ `85b81b0`); `nle-ui` `fc4cc35` **674 tests** (the D29 seams: saveState + exportRequest + SceneGrade; consumer pin `fc4cc35` — the app at package HEAD); `nle-test-app` `c885ece` **174/174** (THE APP — the R8/D29 REAL wiring (persistenceService/deliverService/sidechain-ducking/effects+grade) + the R9 W-A re-pins; consumes engine @ `5036387`); the in-repo mocks unchanged (mini **355 sealed** — CORE-SEAMS 26 seams + LAW-NET-INVENTORY 128-unit census, §3.5/§3.6; variants **1,521+** current-tracked — the VLM net `vlm-capture/vlm-review/vlm-run`); **D26 ruled this round: the adopt-wholesale convergence census (the app's `timeline-port/` = 39 mirrors + host; 7 byte-exact + 25 mechanical + 7 carriers + `use-wire-dispatch` pending W-C) supersedes D25.2's two-path mechanism — the D25.1 single-tree canon stands; D27 keeps nle-ui + the app; D28 evolve-in-place; D29 adopts the W11 instruments ladder-wide**; spec 14 retired (D23 — phase tags D24). v3.1 history: Round 23 (the R23 pins `b8c6f88`/`222532c`/`494f6ff`/`85dcf57`/`70e99f0` — superseded by this round); v3.0: Round 22; v2.1: Round 15; v2.0: Round 8/9.)
**Date:** 2026-09-08 (Round 24 re-baseline; the R23/R22/R15 re-baselines are history below)
**Consumers:** Implementation team, nle-engine workstream, opencut-timeline workstream, all stream specs (they carry per-spec code-reference sections that this doc governs)

---

## 0. FORWARD INVENTORY (the R22-posture law, standing — what needs to be done; the BASE is accepted, not re-explained)

**BASE (accepted, pinned 2026-09-08 @ R24):** the canon hierarchy (§2); the four-domain reference map with every asset pinned + gated (§3, re-baselined this round); the insight ledger (§5, 33 rows — the never-drop law); the C-series corrective deltas (§6, live until each lands). All gates re-run R24: engine 458 / OT 536 (live-run by the module audit) / WDC 759 / nle-ui 674 / app 174 / mini 355 (sealed — the R23 seal census, unchanged) / variants 1,521+ (current-tracked). Consumer pins: engine→OT `c15a629` + WDC `494f6ff`; app→engine `5036387` + OT-lock-copy `c15a629` (byte-exact, live-verified) + nle-ui `fc4cc35` + WDC `85b81b0`.

**GAP (the work — owner + phase per the D24 ladder, the phase vocabulary of `IMPLEMENTATION-PLAN.md`; spec 14 is retired to the stub — acceptance in parentheses):**
- C7 rename → r1 at END (op depth; acceptance: spec 15 §13.15 rows ALIGNED + the one-day app migration sub-gate). The union is 30 prefixed types at `c15a629` (S/T/S3 grew it past the 24 of R15; the D29 re-base splits the census 24 routed + 6 exceptions — §3.2's refresh block).
- ~~The consumer re-pin wave~~ **EXECUTED (R24, the R9 W-A fleet-coherence wave)**: the engine's OT vendor `a4e971d` → `c15a629` (engine HEAD `5036387`; 458/458 green against the new pin) and the app's engine pin `4ef0147` → `5036387` + OT-lock-copy `a4e971d` → `c15a629` (byte-exact, live-verified) + nle-ui `85dcf57` → `fc4cc35` + WDC → `85b81b0` — all landed. Residual: the engine's WDC vendor `494f6ff` sits one docs-only commit behind WDC HEAD `85b81b0` (zero code delta — opportunistic, pin-lockset law).
- **The D26 census discipline (the single-tree migration, re-ruled — D26 amends D25.2's mechanism; D25.1's canon stands)** → S-app joint with D30's W-C..W-G (crawl; acceptance: the port census recomputed app-side by the CI-gated register-equality script vs the vendored pin, import-specifier-normalized — register-inequality fails the build): the CORE tree stays the vendored lock-copy (`vendor/nle-timeline`, byte-exact minus `testing/` @ `c15a629`, UPSTREAM.lock — live-verified); the COMPONENTS tree lives in the app's `timeline-port/` as a **converging mirror: 40 files = 39 mirrors of OT's 40 + 1 app host (EngineMount)** — 7 byte-exact + 25 mechanical-exact (the `@/lib/timeline`↔`@vendor/timeline` specifier only) = **32 zero-action + 7 documented carriers** (TimelineView, use-timeline-actions, use-keybindings, use-element-interaction, TimelineToolbar — the 5 W-C/W-D pending adoptions; TimelineContextMenu + use-playback-ticker — the 2 port-local keeps) + `use-wire-dispatch` pending W-C. Every upstream wave re-pins via 3-way merge, no-conflict files adopt-wholesale; a carrier without a divergence rationale is a census violation. Fork retirement now MEANS the carrier count reaches D30's declared irreducible set (tracked by the census, not by a file-deletion event); the R8/R9 waves already absorbed the F1/P2-6/P2-8/P2-9/lock-menu deltas (pinned app-side).
- The OT-side **carrier-reduction program** (the D25.2 props-upstreaming re-chartered per D26.3 — no longer gates anything; K3's halves gate on the coverage vocabulary) → S-ot, crawl, **ONE work order** (the plan re-write carries its text; the filing lands at S-ot execution): the PORT-LOCAL props upstreamed as optional injectable props (`initialSelectedIds`/`onSelectionChange`/`selectionIds`, `zoom`/`onZoomChange`, `confirmDelete`, ElementView's `cancelRegistry` — injectable, today minted internally at TimelineView.tsx:715-719, the `timeline-scroll` anchor; `EMPTY_CONTEXT_ITEMS` as the stability bug-fix) — each landing flips a carrier toward exact + the **data-test stability contract** (OT emits ZERO testids today — `data-test` is the only harness attribute, 68 component-tree sites (62 static + 6 templated incl. the W11 `label-lock`/`rate-seg` families), zero `data-testid`; the D26.5 re-point: the census gap is coverage-of-existing-vocabulary — freeze it + close the gaps, do NOT add a new testid family) + the **view-config surface** (zoom-ladder config + ripple-toggle semantics exposure — the retired spec-14 §4.1 C1 OT-side halves, re-homed here). OT's own injection points the app rides instead: the W11 `wire`/`onSaveScene`/`onLoadScene` props.
- FCPXML zero-refs (verified again at `5036387`/`c885ece` — zero implementation refs both repos; the app's R8 deliverService carries only the honest guard + the page mock) → r5 (app greenfield; acceptance: the phase-entry parser choice + the deliver e2e).
- The projector parity corpus (the N1 composition-frame family's formal S4 suite) → S-engine, crawl-window queue (the projector itself — Decision 16's `src/lib/nle/projector/` — is still NOT LANDED at `5036387`, verified by directory inspection; acceptance: parity green on the corpus).
- The K2 combined-family census + registration (engine-in-the-middle + WDC integration family + OT's runner — §3's R24 refresh blocks carry the file inventories; **D29.1: OT's M49C coverage gate is now K2's OT-side completeness instrument**, 24/24 green) → S-spec at K2 entry (spec 12/17 extend; acceptance: the census registered + the missing pairs closed).
- §3.5/§3.6 mock retirement → post-K4/w1 (the port-then-swap law, text in 00-master's standing laws; acceptance: the crawl/walk parity gates).
- The §9 watch list's open residuals → S-spec rolling (acceptance: each re-baselined at its next round).

**ACCEPTANCE & TEST PLAN:** every §3 asset row carries its pin + suite + count (the regression role); every GAP row above carries its D24-ladder phase (the forward gate); the battery's stale-pin check (HEAD-pins AND consumer-pins, both classes separately) enforces the pin world — count consistency: engine 458 and OT 536 appear identically in specs 00/17/19.

---

## 0A. TL;DR

This spec answers one question for everyone who touches the project after Round 24: **which code is authoritative, which code is a reference, and what must never be lost when we link to reference code instead of inlining it.** The spec set (00-20) is canon — the FORWARD contract layer over a seven-repo fleet (Decision 14 + ARCH-R22 Ruling A; the plan/spec separation is Decision 23 — `IMPLEMENTATION-PLAN.md` owns HOW/WHEN, the specs own WHAT/acceptance). Three private repos are **domain cores** (Decision 12): **nle-engine** (runtime domain — clean-room FreeCut port, **458/458 vitest @ `5036387`** (R24), real A/V export, the N1-N4 bridge seam family + W1 meter taps + **N2b keyframed-volume automation (LANDED @ `37cdd28`)** + the MIDI/instrument (BGM scoring) path + the RR1-A fade-clamp SPAN round landed + **the W2.5/D29 effects sidecar (the honest CSS-filter preview subset, `buildElementFilterString` @ `8a0b7fe` + the R8-REV blur-radius clamp @ `4b2dfd1`)**; vendors OT @ `c15a629` + WDC @ `494f6ff`; inherits FreeCut patterns the spec corrects), **opencut-timeline** (editing domain — clean-room OpenCut-classic distill, **536/536 @ `c15a629` (OT HEAD `ded43c4` — docs-only past the code pin)** (R24; RUN LIVE by the module audit — 59 milestone entries: 386 in-page + 150 real-mouse), the T-round + W10F hardening + **the W11 complete-UI round LANDED — `use-wire-dispatch` + the M49C coverage gate (24 routed wire verbs + 6 documented exceptions, tsc-lockstep) + the JKL/rate/loop transport + track-head lock-all/mute-all + save/load affordances**; **carries THE canonical React timeline UI tree** — `src/components/timeline/`, 40 files / 8,604 LOC, Decision 25, zero `next/*` imports, zero testids; carries prefixed command names on a refuted premise, C7 → r1 at end), and **web-daw-core** (audio domain — extracted from the living web-daw upstream, **759/759 @ `85b81b0`** (R24; one docs-only seal-wrap commit past the R23 pin `494f6ff`; engine consumes @ `494f6ff`, app @ `85b81b0`), null-test-hardened PURE core, W1 meter taps + **W2 SoundTouch/varispeed LANDED** + the whole-CR rounds with the drift-gate now failing-on-drift). **TWO tier-4 assets:** **nle-ui** (the engine-free chrome PACKAGE — shell-variants productized, 674 tests, boundary-script-gated, `fc4cc35`, consumed @ `fc4cc35` — the app at package HEAD; the D29 seam waves — W2.3 saveState + W2.4 exportRequest + W2.5 SceneGrade — + the kind-aware mute-all pin) and **nle-test-app** (**THE APP** — the assembly: AppShell + EngineMount (OT timeline) + ProgramCanvas (engine N1) + WDC audio host + the W3 JKL transport + **the R8/D29 REAL wiring (persistenceService, deliverService, sidechain ducking, effects/color preview)**, 174/174, `c885ece`; consumes engine @ `5036387` + the OT lock-copy @ `c15a629` (byte-exact, minus `testing/`); its `timeline-port/` is a **CONVERGING MIRROR per D26** (the census: 39 mirrors = 32 zero-action + 7 documented carriers; `use-wire-dispatch` pending D30's W-C) — the app-layer validation stream that superseded ARCH-R15's "fifth repo to be created" plan (the R15-era spec-14 §3, retired to the stub). In-repo: the **cloudcut UX-spec** (integrated ours-wins) + the two MOCKS — **ui-mock/shell-mini** (355 tests, SEALED — the R23 seal census; the crawl's law register: OT-SEAMS + CORE-SEAMS (26 seams) + LAW-NET-INVENTORY (128-unit/355-test census: 13 HOLDS units / 115 GAP units → 33 HOLDS tests / 322 authored)) and **ui-mock/shell-variants** (1,521+ tests current-tracked — the R23 W-A FX surface + W-B color waves + the VLM net `vlm-capture/vlm-review/vlm-run`; the walk's design reference + the VLM tooling source) — design references + review surfaces, NOT shipping code (§3.5/§3.6). The Round-9 seam resolution (§2.4) stands: **one state model, one wire protocol, ONE algorithm home, one render seam, one audio seam.** The rule throughout: **where reference code and the spec conflict, the spec wins; the delta is the documentation.** §5's insight-preservation ledger is the enforcement mechanism for the "never drop a hard-won insight" requirement.

---

## 1. Purpose

Six refinement rounds produced a spec set grounded in two public teacher repos. Since then, private workstreams began producing *executable* code from the same lineage: nle-engine (2026-08-22 → active) and opencut-timeline (landed Round 8 as the timeline-side core, supersedes the earlier timeline-distill plan). Code moves faster than prose and carries more detail than any spec can hold — but it also silently encodes the exact legacy patterns the spec set was designed to correct. Without an explicit hierarchy, every future contributor faces the same ambiguity that this round inherited: "the engine does X, the spec says Y — which wins?" This document makes that decision once, records the history that produced the ambiguity, maps every spec to its reference code with status tags, and maintains the ledger of implementation insights that must survive any future re-linking, re-forking, or re-distilling of the reference repos.

## 2. Canon Hierarchy & Workstream History

### 2.1 The hierarchy

| Tier | Artifact | Role | Authority |
|---|---|---|---|
| 1 — **Canon** | This spec set (`00`-`20`; the plan lives OUTSIDE it per D23 — `IMPLEMENTATION-PLAN.md`) | The design of record: **the decision ledger through Decision 29** (9 seed + D10-D14 R7-R9 + D15-D17 R15 + D18-D22 R22 + D23-D25 R23 + **D26-D29 R24 — the adopt-wholesale census, the repo topology, evolve-in-place, the W11 instruments**), contracts, gap register, test methodology | **Wins every conflict** |
| 2 — **Domain core: runtime** | `bearachprema/nle-engine` (private, **458/458 vitest + tsc 0 — R24 @`5036387`**; vendors OT @ `c15a629` + WDC @ `494f6ff` — WDC REGISTERED one docs-only commit behind HEAD `85b81b0`, moving with the pin-lockset law) | Clean-room FreeCut port: player, WebGPU compositor, transitions/effects/text stacks, export, media, fonts — the RUNTIME-domain normative core (Decision 12) + the audio bridge + the N1-N4 + N2b seam family (composition-frame, flattener, transitions, av-link, volume-keyframe automation — **+ the W2.5/D29 effects sidecar: `CompositionElementParams.effects` + `buildElementFilterString`, the honest CSS-filter preview subset**) + the MIDI/instrument (BGM scoring) path | Domain core; corrective deltas in §6 |
| 3 — **Domain core: editing** | `bearachprema/opencut-timeline` (private, **536/536 = 386 in-page + 150 real-mouse (RUN LIVE by the module audit; the report json at HEAD records it), tsc 0 — R24 @`c15a629` (OT HEAD `ded43c4` — docs-only past the code pin)**; the app's vendored `src/lib/timeline` lock-copy byte-exact @ `c15a629` (minus `testing/`); **the canonical React view tree lives IN-REPO at `src/components/timeline/` — 40 files / 8,604 LOC, Decision 25; the app's `timeline-port/` is a CONVERGING MIRROR per D26 (the census: 39 mirrors = 7 byte-exact + 25 mechanical + 7 documented carriers; `use-wire-dispatch` pending W-C), riding OT's W11 injection points (`wire`/`onSaveScene`/`onLoadScene`)**) | OpenCut-classic distilled as the EDITING-domain normative core (SceneTracks, ops, controllers, headless editing commands, the T/W10F hardening + W11 wire-dispatch rounds: transport policy/track lock/lock-all/mute-all/transitionOut/bookmarks/JKL-rate-loop/save-load, the M49C coverage gate) — the single algorithm home (Decision 12.3) | Domain core; corrective deltas in §6 (C7 → r1) |
| 3A — **Domain core: audio** | `bearachprema/web-daw-core` (private, **759/759, tsc 0, zero runtime deps — R24 @`85b81b0` (docs-only seal wrap past the R23 pin `494f6ff`); consumed engine @ `494f6ff` + app @ `85b81b0`**) | web-daw's engine extracted as a **PURE core** (the NLE bridge relocated to nle-engine per M1.6; W1 canonical meter taps landed; **W2 SoundTouch/varispeed LANDED @ `de09c93`** — `lib/daw/soundtouch.ts` + `varispeed.ts`; the whole-CR + S4 seal rounds landed, the drift-gate FAILS-on-drift) — the AUDIO-domain normative core (Decision 13 / spec 20); manifest-synced from the living web-daw upstream | Domain core; contract = spec 20 |
| 4 — **Teachers (public, read-only)** | `walterlow/freecut` (~250k LOC, MIT), `opencut-app/opencut-classic` (archived MIT), `bearachprema/web-daw` (private, LIVING upstream) | Original sources of the architecture; cited with file:line throughout specs 01-12; web-daw feeds web-daw-core continuously via sync | Historical authority; superseded as code, canonical as insight origin (web-daw: active) |
| 5 — **Visual reference** | `ui-mock/davinci_resolve_ui_mock.html` (this repo) | Layout/identity reference for the UI shell (spec 18) | Visual only; not code |
| 5A — **Shell design surfaces (in-repo; R15 origin, state unchanged R24)** | `ui-mock/shell-variants` (**1,521+ current-tracked, tsc 0** at the R23 state — the R22 `1,334` and R15 `d42693e`/596 states are history; the R23 W-A FX surface + W-B color waves + the VLM net `scripts/vlm-capture.mjs`/`vlm-review.mjs`/`vlm-run.sh`/`vlm-rubric.md` landed) + `ui-mock/shell-mini` (**355 SEALED** — the R23 seal census; the crawl's law register: OT-SEAMS + CORE-SEAMS (26 seams) + LAW-NET-INVENTORY (128 census units / 355 tests)) | The SHELL design + chrome layer for spec 18 — productized as the `nle-ui` package (tier 4, above) with the app as consumer; the mocks stay design references + law registers + review surfaces (annotakit) + the VLM tooling source (adopt, don't fork) | Design reference + review surface; not canon, not shipping code |

### 2.2 The history (why the engine is not spec-conformant)

The spec set and nle-engine began as parallel workstreams, both dated 2026-08-22. Cross-pollination between them was **real but shallow**: an early snapshot of the spec set was handed to the engine workstream mid-build with an instruction to apply any gaps; some did apply, but by then a large portion of the engine code was already written, and the engine workstream was consumed by its own complexity — delta analysis against original FreeCut (its Wave 1 read every relevant FreeCut source file; its own 7-track audit A-G re-verified claims against `/home/z/my-project/freecut/`). Consequences:

1. The engine's 13 architectural decisions (`.agents/DECISIONS.md`) were written **without** reference to our 9 decisions. Where they overlap (clock, registry patterns, structural/presentation split) they converge independently — strong signal. Where they diverge (wire protocol, color depth, API surface, testing, media, workers) they diverge **toward FreeCut**, because FreeCut was the engine's only teacher.
2. Whatever spec knowledge landed in the engine is partial and cannot be treated as canon-conformance. The engine is "closer to FreeCut, cleaned up" — which is exactly its value: it drags the implementation risk of the whole system down while leaving the *corrective* work (the part these specs exist for) explicit and well-scoped.
3. Direction of travel, therefore: **the engine converges toward the spec, never the reverse.** Round 7 starts that convergence with §7 (answers to the engine's own open decisions D1-D5) and the corrective mapping (§6). The engine's active gap-closure waves (see §9) are tracked so the seal round can re-baseline.

### 2.3 What "spec wins" means in practice

- A reference-citation that contradicts a spec contract is tagged **CORRECTIVE** in the per-spec code-reference sections and the delta is documented — never silently adopted.
- The engine's *verified bug findings* (its gaps/audit register) are evidence **for** spec discipline, not against spec content: e.g. its P0.1 texture-singleton bug is the live proof of spec 04 §7's pool-discipline rule.
- New insights discovered in reference code (e.g. FreeCut's occlusion cutoff, found in the engine's scene-assembly and missing from spec 07 §5.2) flow **up into the spec** via amendment — Round 7 did this for spec 07 §6.1 (cut-centered transitions) and §5.2 (occlusion) — and only then become canon.

### 2.4 The seam resolution — Round 8 form, Round 9 amendment (one state model, one wire protocol, one algorithm home, one render seam, one audio seam)

opencut-timeline landed 2026-09-02 (at the R15 pin `0412e41` — Round 15 re-baseline, historical: **423/423 tests (303 in-page + 120 real-mouse, re-run), tsc 0** at that pin; the W8 classic-parity UI round + W8-e polish + the W9 full-repo review are all landed and terminal — **zero open P1/P2** (SCOUT-R15-B §1/§6). The R8-era final-round hardening per its DECISIONS #15 (3 P1 engine divergences incl. a data-loss class, SSR-500, dead playhead-handle drag, 8 untested wire commands — all fixed with regression suites; marquee + follow-scroll shipped; its own SEAMS.md is the repo-side layer map) was the 297/297 state) as the **editing-domain engine core**. Its overlap with nle-engine (6,972-LOC `Timeline`, 102 methods, own snapshot undo, own headless surface) was resolved in Round 8 by a binding architecture (master Decision 11), **amended in Round 9 by Decision 12** after the user's challenge — the "two algorithm homes" clause and the bidirectional adapter were permanent bridge-code cost without product gain (ARCH-R9 §2):

1. **One state model — single truth (R9).** `SceneTracks {overlay[], main (singleton), audio[]}` (opencut-timeline `types/index.ts:116-119` at `c15a629`) is the editing state of record **including persistence** (its `toJSON/fromJSON`; the app shell owns when/where per its SEAMS.md §6). nle-engine's flat `TimelineData {fps, tracks[], clips[], transitions, markers, keyframes, compositions}` (`core/types.ts:1354-1406`) is re-specified as a **one-way derived projection** for render scheduling: `project(scene) → TimelineData`, deterministic and idempotent (implementation home per Decision 16: **ENGINE-home at `nle-engine/src/lib/nle/projector/`** — still NOT LANDED at `5036387`, verified by directory inspection; additive under the engine's API-freeze rules, SceneTracks TYPE-ONLY import per the bridge-family precedent — the convention is now FOUR type-only sites in the engine's `src/lib/nle/bridge/` (scene-to-segments.ts:67, av-link.ts:49, transition-inputs.ts:70, composition-frame.ts:100, all `import type`), one-way for editing state, assembly phase A1; the engine's own `Timeline` class is the parity oracle then a permanent internal test substrate — see 05 §16.5A; supersedes the R9-era `src/scene/scene-projector.ts` app-repo path). **Editing never reads back from engine state.** The R8 bidirectional adapter (`src/scene/scene-adapter.ts`) and its round-trip property tests are retired — the projector's idempotence property replaces them.
2. **One wire protocol (R8, C2 scoped in R9).** Spec 15's bare `EngineCommand` (78 types) is the only protocol. Convergence work: opencut-timeline **renames its prefixed types to canon** (24 at W5 → **30 at `c15a629`** (24 routed + 6 exceptions per the W11 census — see §3.2); the S/T/S3 rounds added `timeline.setPlaybackRate`/`setLoopRegion`/`advancePlayhead`, `track.toggleLock`/`setAllLocked`, `track.setAllMuted`, `timeline.toggleBookmark` color/note, undo/redo inside the batch guard, etc.; the R8-era "18" was stale; C7 worklist last refreshed to the 24-command reality in spec 15 §13.15 at R15 — needs the 30-refresh at the rename) and becomes the implementation home of the **EDITING command subset**; nle-engine owes only the **RUNTIME command subset** (render/export/media/scenes — Decision 12.2's scope-down; D12.2's "retire JSON-RPC" clause is superseded by D16's re-typing of the JSON-RPC+$ref surface as INTERNAL transport — §3.1). No repo may invent a second protocol. — Note: the premise behind opencut-timeline's DECISIONS #9 remains **refuted** (00-master:234/:562 show bare names; what its authors saw is spec 15 §4.2's manager-method column).
3. **One algorithm home — was two (R9).** opencut-timeline's ops layer is the normative editing-algorithm home: it already owns placement / zero-anchor / ripple-diff / split-snap-once / group-move / interaction controllers (spec 05 §14.4-14.6, spec 06 §5.1-5.4, §5.9-5.10). nle-engine's FreeCut-side families (roll / slip / slide / rateStretch / retime / freezeFrame / insert-edit-3-point / rangeRemoval / sync-lock — spec 06 §5.5-5.8, §5.11-5.14, §6) are **port-scheduled INTO it** as pure functions over SceneTracks (acceptance = OT's 423-test suite — the R15 re-baseline; 297 at R9 — + the ported engine tests; the pipeline proves out on `roll` first — ARCH-R9 §7.3). Until each port lands, the engine's version is its internal fallback; the editing wire never routes through it. Transitions/linked-groups/sync-lock take SceneTracks shapes per specs 06/07.
4. **One render seam (R8, unchanged).** `setTracks()/renderFrame(t)` (opencut-timeline `render/placeholder-compositor.ts:116`) is the compositor contract; nle-engine's WebGPU compositor plugs in behind it (after its C1 8-bit corrective work). Every UI/test pixel assertion flows through this seam; the projector feeds `setTracks`.
5. **One undo family (R8, OT's in R9).** Both repos are snapshot-based (spec 15 §6.2 strategy 2); OT's transaction machinery (eviction-suspended batches, depth-anchored rollback, redo clear) is absorbed into spec 15 §7.1A; nle-engine's now-keyframe-aware `snapshotsEqual` (P0.6 fixed) is the live reference for ledger #12 and **retires with the op-family port**.
6. **One audio seam (R9, new).** The S/G/E three-layer contract (spec 20): structure, signal, and engine share only `trackId`; the bridge implements the engine's own Wave-5A semantics. Nothing bidirectional exists to maintain — this is the structural difference from the timeline duplicate the user rejected (Decision 13's reasoning).

**Undo-layer note:** the cloudcut UX-spec's time-windowed EDL-snapshot undo (ux-spec 20 §2.1) is REJECTED as canon-incompatible — see the Round-8 rejection register (SCOUT-R8-C §6) and spec 18 §8.

## 3. Reference Repo Map

### 3.1 nle-engine — `github.com/bearachprema/nle-engine` (private)

**What it is:** a minimal NLE engine built from scratch with FreeCut as architectural reference (its Decision 1), **~54.8k LOC (54,791) across 62 files** (the R24 census, `wc -l` over `src/lib/nle/**`; the R23 census was 54,721/62 — the W2.5 sidecar grew composition-frame; the R15 census was 51,968/58) of pure TypeScript engine under `src/lib/nle/` (no React in the core — its Decision 9; the surrounding Next.js app is a test harness), plus the deepest FreeCut delta-analysis in existence (its `gaps/` directory: 30+ wave-gap files + a 12-file audit of its own). **Runtime-domain normative core per Decision 12.**

**Verified state (Round 15 re-baseline — 2026-09-05, @`f526e67`, range `624a76b..f526e67` = 61 commits; SCOUT-R15-A ran every gate in-sandbox — vitest, tsc, lint-layering, 2 probes, the full browser milestone run — and used commit messages only as pointers):**

| Surface | State | Evidence (file:line) |
|---|---|---|
| LOC / files (Round 15) | **~52k (51,968) across 58 files** in `src/lib/nle/` (replaces the R8-era 37,958/29-file census); engine-own timeline = **8,936 LOC** (timeline.ts 7,480 + keyframe-store.ts 817 + sync-lock.ts 639) | `wc -l` census, SCOUT-R15-A §4 + corrections |
| Tests (Round 15) | **274/274 vitest** = 237 engine + 37 bridge (the bridge H10 render test needs `--testTimeout=240000` on 4 GB sandboxes — CI's 16 GB venue is authoritative); **+ 265/265 browser milestone rows across 31 milestones** (fresh real-WebGPU re-run: `isSwiftShader: false`, 0 failures, ~8 min; runner pins `EXPECTED_MILESTONE_COUNT = 31` / `MIN_TOTAL_TEST_ROWS = 260`); **+ 318 probe checks** (`probe-p114-undo.ts` → 318 PASS / 0 FAIL; `probe-fx4.ts` all green); `tsc --noEmit` exit 0; CI = one workflow, 3 jobs (typecheck / vitest / milestones), green through `508c838` + 2 docs-only commits | `vitest.config.ts:21-22`, `scripts/run-nle-tests.mjs:41,51`, `.github/workflows/ci.yml`, SCOUT-R15-A §1 (all executed) |
| NLE ops (Round 15) | Timeline class: the **59 undo-wrapped ops** + link/sync-lock/marker/sub-comp families (~20 op families matching spec 06 §5; timeline.ts 7,480 LOC; undo-serialize 15 vitest) — **port-scheduled into OT per Decision 12.3 (A2 charter, not started)** | `timeline/timeline.ts`, probe-p114, SCOUT-R15-A §10 P2 |
| GPU effects (Round 15) | 44 registry entries (16 color-category + chromaKey keying; LUT3D pipeline live per Wave 3E) — the old 43-vs-44 HANDOFF drift is superseded | `effects/pipeline.ts` + `effects/lut.ts:323`, SCOUT-R15-A §10 P4 |
| Transitions | 27 presentations, cut-centered planner, handle clamping (unchanged) | `transitions/registry.ts:2249`, `planner.ts:21/:27`, `handle-utils.ts:302` |
| Clock / video sync | Faithful FreeCut port (unchanged) | `core/clock.ts` (:550, :612, :661), `playback/video-sync.ts` (:180, :224, :925) |
| Compositor | Ping-pong pipeline, 25 blend modes — **still 8-bit `rgba8unorm`** (C1 stands); BUT pool discipline LANDED and pixel-verified (m20.1/20.2/20.5) | `gpu/compositor.ts` (:32, :981) |
| Audio (Round 15) | **One audio engine, end-to-end**: realtime (Player — `player.ts:3247` `scheduleClipWithFades` through `RealtimeAudioBridge`) AND export (`orchestrator.ts:765` `renderAudioMixdown` = `new SceneMixer(...).renderOffline(...)`) both drive **one SceneMixer/ChannelStrip/master-chain graph** on web-daw-core DSP; the freecut `AudioMixer` **class is DELETED** @`20fa266` (0 hits `class AudioMixer`; only the shared pure law library survives in `audio/mixer.ts`) and the direct mix `export/audio-mix.ts` is DELETED @`abdf9ee`; parity evidence is **composed**: H3 (offline≡realtime null ≥ 60 dB, Node) + 29.1 (both offline export paths sample-identical, < 1e-6) + m30 realtime behavioral pins | `bridge/` (per-file table below), `playback/player.ts:107,209,3092,3247`, `export/orchestrator.ts:586-620,729-800`, SCOUT-R15-C §5-6 |
| LUT | 3D `.cube` parser — 8-bit quantized, no linear↔sRGB conversion (inherited FreeCut bug stands) | `effects/lut.ts` (:314, :337) |
| Headless API (Round 15) | JSON-RPC-style + `$ref`: **19 op names + 8 API methods** (`renderTimeline`, `renderProject`, `renderFrame`, `dumpLayout`, `editProject`, `normalizeProject`, `probeMedia`, `createProject`; Zod-strict per-op schemas); canonical op semantics live in `headless/timeline-adapter.ts` — **re-typed INTERNAL transport per Decision 16** (the 78-union contract is enforced at the app bus + the engine's union façade; D12.2's "retire JSON-RPC" clause is superseded) | `headless/api.ts:378-406` (op names), `:2025-2044` (`EDIT_OPERATION_NAMES`), `:110-130` (methods), SCOUT-R15-A §5 |
| Persistence | **Wave 4B LANDED (1,174 LOC): serialize/hydrate/migrate/normalize + ~27-code warnings channel + real `editProject` round-trip — but serializes the engine-native "v2" shape (D1 resolved AGAINST the spec answer; see C8 + §7 D1 escalation)**. No Zod, no scenes/media, no storage plane (D5 open) | `persistence/index.ts` (:5-9, :65-67, :162-183, :352-387, :411-585), `api.ts:1070-1130` |
| Render loop | **Wave 4C LANDED: per-type branches — CompositionItem (recursive `_buildLayersForData` + offscreen nested composite, m19.2 pixel-verified), AdjustmentItem + ImageItem end-to-end (m21, 7 pixel tests). The `player.ts:1038` non-video-skip counter-example is RETIRED** | `playback/player.ts`, `playback/scene-assembly.ts`, SCOUT-R8-B §3 |
| Known bugs (its own register) | **P0.1 texture-singleton (FIXED), P0.2 mask double-invert (FIXED), P0.6 keyframe-blind undo equality (FIXED)** — and at Round 15 the review ledger is **CLOSED: ZERO P2s remain open** (`REVIEW-TRACKER.md:117`); only the by-choice P3 register remains (TEST-9..14, DOCS-8..12, UX-5..8, ARCH-12..14, CODE-6..8) | `gaps/audit/MASTER.md`, `gaps/audit/REVIEW-TRACKER.md:117`, SCOUT-R15-A §7 |
| Vendored submodules (Round 15) | `vendor/opencut-timeline @3420b5f` (the W8-d endpoint, 379/379) — **35 commits behind OT HEAD `0412e41`**; `vendor/web-daw-core @5243c49` (the M1.6 relocation commit) — **1 docs-only commit behind WDC HEAD `374711c`**; OT is TYPE-ONLY in src/ (`bridge/scene-to-segments.ts:43`; the only runtime OT import is the vitest bridge suite) while `@/lib/daw` is a real runtime dependency (5 import sites in src/) | `.gitmodules` (2 entries), `tsconfig.json` paths, SCOUT-R15-A §2 |
| API freeze (Round 15) | **453 frozen names** (additions need a same-commit list update, removals/renames a DECISIONS entry — regenerate via `scripts/gen-api-surface.mjs`) + **52-edge layer fence** over the 14 top-level layers of `src/lib/nle` (`lint-layering.mjs` in CI; `bridge` is its own fenced layer) | `api-surface.frozen.ts` (453 entries), `gaps/audit/LAYER-SNAPSHOT.txt` (52 edges), SCOUT-R15-A §9 |
| Text stack | **Wave 4D-B/C LANDED: typography core (6 files, 1,436 LOC) + GlyphAtlasTextPipeline (996 LOC) + text texture LRU + per-axis scale — text renders pixels (fonts bundled per its D3, no CDN race)** | `text/`, `gpu/`, `core/types.ts:936` |
| Export (Round 15) | **Works end-to-end** (browser-verified: milestones m24/m26/m28/m29 in the 265/265 run, incl. real A/V MP4 decode); codecs `avc\|hevc\|vp8\|vp9\|av1`, containers `mp4\|mov\|webm\|mkv`, audio `aac\|opus\|mp3\|pcm-s16` with fallback ladders; mediabunny@1.50.8 is the only runtime dep, lazy-imported strictly inside `export/`; **NO FCPXML — zero refs repo-wide (spec 10 unstarted)**; subtitle export on the P2 backlog; worker offload + OPFS streaming explicitly not done | `export/settings.ts:30-76`, `export/index.ts:6-8`, SCOUT-R15-A §6 |
| UI (Round 15) | No React timeline, no shell — `src/app/page.tsx` is an **11,206-LOC single-page browser TEST RUNNER** (31 milestone definitions inline, U1-U4 runner UX, imports zero shadcn components); `/api` GET status route; layout.tsx metadata still says "Z.ai Code Scaffold" (cosmetic D7 residue) | `src/app/page.tsx:129`, SCOUT-R15-A §8 |
| Workers | **Zero** Web Workers (unchanged) | grep-verified |
| Media (Round 15) | Procedural `VirtualVideo`/`VirtualAudio` remain the e2e standard (6A real media DEPRIORITIZED per its D6); mediabunny is now the lone runtime dependency but is confined to `export/` — no decode pipeline yet | `media/virtual-media.ts`, SCOUT-R15-A §6-7 |

**The audio bridge (`src/lib/nle/bridge/`, 7 files, 2,078 LOC) — Round 15** (M1.6 relocated 6 files VERBATIM from WDC@`8016aeb` at commit `b837d60`; `conversions.ts` is engine-born, review-R1 fix A4; the bridge is a fenced layer of its own and stays out of the public barrel — only `RealtimeAudioBridge` is exported, `index.ts:46-48`):

| File (LOC) | One-liner (SCOUT-R15-C §4) |
|---|---|
| `conversions.ts` (189) | the "one law, two consumers" PURE converter: `TimelineData` → bridge segments + strip settings (fades clamped to play span, folded volumeDb→level, masterBusDb→masterVolume, rate-aware pre-roll skip) |
| `fade-curve.ts` (174) | the author fade-curve law (power curves through an exactly-solved control point; consumed as 16-breakpoint piecewise-linear ramps so bridge ⇄ offline-mix agree sample-level) |
| `mixer-track-model.ts` (191) | the **G layer** of S/G/E: `MixerTrackSettings` keyed by trackId (fader/pan/mute/solo/inserts/sends/outputBus/instrument); `toDawTrack()` lossless round-trip |
| `scene-mixer.ts` (380) | the conductor: `materializeStrips` + `renderOffline` on the one graph; idempotent materialization, one-engine-per-instance guard, canonical bus send/return, solo coordination |
| `scene-to-segments.ts` (369) | the S-layer converter + merge-law engine (split-merge H1/H9 click-free continuity, equal-power transition windows H1b, varispeed H8) + the opencut flattener — the repo's **only OT type-only import** |
| `segment-strip-adapter.ts` (588) | the scheduler: per segment `AudioBufferSourceNode → envelope Gain → strip.input`; 8 ms anti-zipper ramps, H13 level folding, H16 drift/seek surface, H8 worklet insert seam, H0 pan law |
| `realtime-engine.ts` (187) | `EngineHandle` on a LIVE AudioContext — the realtime twin of the offline handle; the SAME shared `buildMasterChain` ("one chain across playback, bounce, offline render, and the NLE bridge's realtime path") |

**Its roadmap of record (Round 15):** `gaps/audit/MASTER.md` §0 — everything ✅ through the SEAL row; only "6A–6C real media" (DEPRIORITIZED per its D6) and "7 P2 backlog" are non-DONE. Open P2 backlog (per `.agents/PLAN.md:82-89`): ShapeItem SDF, LottieItem (D4 open), text motion + presets, scopes (waveform/vectorscope), ControllerItem, SubtitleSegmentItem, CPU transition renderers, compute-shader pixel-sort, decoder prewarm, filmstrip, VFR policy. `Item` census: the `Clip` union is **6 types** (`core/types.ts:1030`; Shape/Lottie/Controller/Subtitle absent). **The M1.5/M1.6 audio convergence duty per Decision 13 is EXECUTED and verified** (SCOUT-R15-C); the remaining engine-side convergence duties are the union façade + projector (Decision 16, app A1/A2).

**R24 re-baseline (the live register @ `5036387` — 2026-09-08, this round's fresh-grep pass; the R15 table above is the point-in-time history; the R23 register rows are subsumed):**

| Surface | State (R24) | Evidence (file:line) |
|---|---|---|
| Tests | **458/458 vitest** (13 test files: 2 top-level bridge suites + 11 under `tests/vitest/engine/`; static `it/test` census verifies the count exactly — the live-run venue is GHA CI), tsc 0, CI 3 jobs; browser milestone rows + probes per the R15 venues (unchanged mechanism); lineage 440 @ `b8c6f88` (R23) → +18 (the W2.5 round `8a0b7fe` +17 + the R8-REV `4b2dfd1` +1) | `tests/vitest/` (13 files), commit `5036387` |
| The bridge family | **11 files / 4,334 LOC** (4,264 at R23): the N1-N4 seam family LANDED — `composition-frame.ts` (459 — the W2.5/D29 effects sidecar LANDED @ `8a0b7fe`: `CompositionElementParams.effects` :119 + `CompositionElementEffect` :124 + `CompositionDrawOp.filter` :165 + `buildElementFilterString` :223, the honest CSS-filter preview subset (Gaussian Blur → `blur(radius px)`; Vignette/Glow/Film Grain/Motion Blur honest-skip; the scene GRADE stays consumer-side final pass BY DESIGN) + the R8-REV #5 blur-radius clamp @ `4b2dfd1`), `av-link.ts` (201), `transition-inputs.ts` (313) + the MIDI path `scored-notes.ts` (400) + the R15 six (conversions 228, scene-mixer 398, scene-to-segments 649, segment-strip-adapter 1,087, realtime-engine 234, fade-curve 174, mixer-track-model 191); **N2b LANDED @ `37cdd28`** — the flattener's volume-keyframe fold (dB-domain, lane-replaces-static, merge-block) + the adapter's dedicated automation gain node + the NATIVE core path (`audio-scene.ts` lane attach + `foldVolumeKeyframesToAutomation`); the review rounds R1-R3 converged the whole codebase to **zero open P0/P1/P2** | `src/lib/nle/bridge/` (wc -l), `gaps/audit/design-n2b-gain-automation.md` |
| The audio graph | ONE SceneMixer graph end-to-end (unchanged law); realtime `player.ts:3287 scheduleClipWithFades` via `audio/realtime-bridge.ts` (714 LOC, the m30 port) AND export `orchestrator.ts:796 renderAudioMixdown`; **RR1-A: the fade-clamp SPAN round LANDED** — `clampAuthorFadeToSpan` extracted as the one player↔export law | `playback/player.ts:3287`, `export/orchestrator.ts:796`, `audio/audio-scene.ts:803` |
| MIDI/instrument (BGM) | **VALIDATED end to end** (P1.20 CLOSED): `bridge/scored-notes.ts` (clip-relative beats + clip-carried TempoMap, split/merge rebase identity) + the export path note-aware + realtime `scheduleScoredNotes` with the KILL-WINDOW law (H18a-f + 5 review regressions); the downstream W7 proposal prepared (`gaps/specs/midi-bgm-track-proposal.md`) | `bridge/scored-notes.ts`, `tests/vitest/nle-bridge-realtime-midi.test.ts` |
| W2 consumption | **LANDED same round as WDC's W2** (the engine consumer @ `df6eadf`): the flattener `maintainPitch` thread (`scene-to-segments.ts` :246/:305 the option fields, `:611`/`:638` the `el.retime?.maintainPitch → source.maintainPitch` thread) + merge predicate; the adapter's pre-retime branch with the three-site schedule law + the byte-capped LRU span cache; 10 bridge pins | `bridge/scene-to-segments.ts`, WDC HANDOFF (W2 record) |
| K2 combined nets (engine-side) | The cross-module family EXISTS in-repo: `tests/vitest/engine/timeline-edit-ops.test.ts` (vendored-OT ops × engine composition), `video-sync.test.ts`, `bridge-seams.test.ts`, `planner.test.ts` — the K2 census's engine-in-the-middle seeds (register row §0 GAP) | `tests/vitest/engine/` |
| API freeze / layer fence | **455 frozen names** (unmoved since `c6dc8f5` — the W2.5 round's `buildElementFilterString` lives in `bridge/composition-frame.ts`, OUTSIDE the barrel `src/lib/nle/index.ts` by design (the bridge is app-level G-layer code, deep-imported); `api-surface.frozen.ts`), **52-edge layer fence** over 13 top-level layers + the barrel (unchanged count; the audit's `LAYER-SNAPSHOT.txt` carries 9 documented inversions) | `tests/vitest/engine/api-surface.frozen.ts`, `gaps/audit/LAYER-SNAPSHOT.txt` |
| Vendored submodules | OT @ `c15a629` + WDC @ `494f6ff` — the FULL OT repo as a git submodule, CORE-ONLY discipline enforced by the tsconfig/vitest alias (`opencut-timeline` → `vendor/opencut-timeline/src/lib/timeline/`, `vitest.config.ts:44-50`), not by tree filtering; OT is TYPE-ONLY in src/ (4 sites, all `import type`); `@/lib/daw` a real runtime dependency; **pin state: OT @ `c15a629` is the CODE tip (HEAD `ded43c4` — src identical, docs/runner-artifacts only past it); WDC @ `494f6ff` is one docs-only commit behind HEAD `85b81b0` (zero code delta)** — residuals moving with the pin-lockset law | `.gitmodules`, `vitest.config.ts`, §0 GAP |
| FCPXML | **Still ZERO implementation refs repo-wide** (grep @ `5036387`; at the app only the R8 deliverService's honest guard + page mock) → the r5 greenfield stands | grep-verified this round |
| Projector (D16) | **NOT LANDED** — `src/lib/nle/projector/` does not exist (directory inspection @ `5036387`); the A1 duty + the projector parity corpus live in §0 GAP | directory census |

### 3.2 opencut-timeline — `github.com/bearachprema/opencut-timeline` (private, LANDED Round 8; **editing-domain core per Decision 12**)

**What it is:** the timeline-side counterpart to nle-engine — OpenCut-classic distilled as the **editing-domain engine core** (Round 15 @`0412e41`, `main`, clean, 82 commits — `4e39b67` + W5 domains + W6/W7 seal + W8 UI round + W8-e polish + W9 full-repo review, 53 commits on the R8 baseline). LOC by area (`wc -l`, SCOUT-R15-B §1): **engine (pure lib) 9,651 / controllers 3,190 (11 files) / React view 7,413 (15 .tsx + 21 .ts/hooks)** ≈ 20.2k non-test + app-shell fixtures 1,988 + in-page harness 12,357 + real-mouse scripts 6,450; **423/423 tests (303 in-page across 32 suites + 120 real-mouse across 13 phases = 45 milestone entries; re-run R15, 218.7 s), tsc 0**. Unlike the "UI-only, leave the types out" shape §3.2 originally recommended, it deliberately ports the full type system + ops engine + controllers + React components + headless API (the user's own framing: "more like timeline / multi-track engine core"). It was built **with spec awareness** — its README cites Decision-2 type adoptions and the spec-15 wire protocol — and its `TimelineCore` method surface **already matches spec 15 §4.2's manager-method column 1:1** (`splitElements`, `moveElements`, `trimElements`, `deleteElements`, `insertElement`, `duplicateElements`, `updateElements`, `toggleTrackMute`, `addTrack`, `removeTrack`, …). Deps: next 16.1.1 + react/react-dom ^19 + playwright ^1.62.1 (dev) — **no state library: the engine itself replaced classic's Zustand store** (`core.subscribe()` + preview-layered `getTracks()`; DECISIONS #2). **It is the single algorithm home per Decision 12.3 and the port target for the engine's FreeCut-side op families (A2, not started).**

**R24 re-baseline (the live register @ `c15a629` — OT HEAD `ded43c4`, the src diff from the code pin is EMPTY; 2026-09-08, this round's fresh-grep pass + the module audit's LIVE test run; the R15 rows below are the point-in-time history):**

| Surface | State (R24) | Evidence (file:line) |
|---|---|---|
| Tests | **536/536 — RUN LIVE by the module audit** (dev server + `node scripts/run-timeline-tests.mjs`; the report json at HEAD records `total: 536, passed: 536, failed: 0`; **59 milestone entries = 41 in-page entries (386 tests) + 18 real-mouse entries (150 tests)**), tsc 0. Lineage: 489 @ `222532c` (R23) → +M45/M47 (W10F) + M48 + the W11 M49 family (M49 + M49T/H/R/G + the M49C gate) | `download/timeline-test-report.json`, `scripts/run-timeline-tests.mjs` |
| The runner (K2's in-page family) | `scripts/run-timeline-tests.mjs` (the in-page driver → the report json) + **18 real-mouse scripts** (the `m17-…m43-*-real-mouse.mjs` classics + the W11 `m49-transport/trackheads/residuals/gestures` set) + `capture-w8-screens.mjs`; the runner page exposes `window.__TIMELINE_TEST_REPORT__` + `[data-test="suite-complete"]` — the contract the app's `/dev/view-fixture` replicates; **M49C the coverage gate** (reads `WIRE_COMMAND_TYPES` LIVE from the page, accumulates the wire-coverage Set across phases, asserts every verb minus the exception registry fired through the real UI — 24/24 PASS) | `scripts/` |
| The rounds since `222532c` | **T-round + W10F hardening** (D-T3 `setTracksLocked` lock-all + D-T7 same-position TRUE-NOOP + M44/M45/M47; DECISIONS #24 — the button-family law, commit-before-notify, pair-scoped validation, wire-owned invariants) + **M48** (`setTracksMuted`/`track.setAllMuted` ported, the F1C-1-normalized predicate) + **W11 the complete-UI round**: W11-a engine widenings (`HeadlessTimelineApi` attach with SA-5 fps/instance validation, the dispatch recorder (ring 500 + coverage Set), `WIRE_COMMAND_TYPES` tsc-lockstep, `applyBatch data.results`, keyframe-verb `lockPreCheck`→TRACK_LOCKED) + W11-b the dispatch migration (`use-wire-dispatch` + command-verb migration across toolbar/menus/labels/bookmarks; the wire-error chip) + W11-c transport (JKL ladder, rate control, loop region + overlays, M49T) + W11-d track heads (label-lock + lock-all/mute-all + track.add, M49H) + W11-e residual affordances (bookmark remove, library batch insert, save/load swap protocol, M49R) + W11-f gesture commits (move/patch/bookmark/insert cross the wire, M49G) + W11-g the coverage gate (M49C); DECISIONS #25 + the SEAMS W11 seam map; the W11 testing module is `testing/milestones-w11.ts` (the M49 family's pins); **the op-surface parity audit at HEAD (`ded43c4`, `.agents/OP-COVERAGE.md`): 11/11 NLE edit verbs ported vs opencut-classic, extensions lock/loop/rate/JKL/rippleDelete-first-class/save-load** | `headless/api.ts`, `ops/timeline-core.ts:2145` (`setTracksMuted`), `hooks/use-wire-dispatch.ts`, `testing/milestones-w11.ts`, `.agents/OP-COVERAGE.md` |
| **The CANONICAL React tree (Decision 25)** | `src/components/timeline/` — **40 files / 8,604 LOC** (39/7,702 at R23): TimelineView 1,516 + TimelineElementView 883 + use-timeline-actions 580 / Toolbar 391 / use-bookmark-drag 382 / use-keyframe-box-select 353 / icons 326 / BookmarksRow 257 / Track 184 / Ruler 172 / ContextMenu 148 / AudioWaveform 204 / AudioVolumeLine 225 / Playhead 96 / DragLine 63 / SnapIndicator 41 / Tick 57 + **20 hooks (use-wire-dispatch.ts 90 — NEW W11-b)** + audio-state/expanded-layout/layout/theme/use-committed-ref; **ZERO `next/*` imports in components/ (pure React — the Next.js coupling lives only in `src/app/`); ZERO `data-testid` emission — the harness attribute is `data-test` (68 component-tree sites: 62 static + 6 templated families incl. the W11 `label-lock-{trackId}`/`rate-seg-{rate}x`; repo-wide 100 sites)**; the host seam `TimelineViewProps` at `TimelineView.tsx:177-201` (`core` REQUIRED + `fps` + optional snapping/initialZoom/isShiftHeld/dragSource/mediaAssets/mediaLookup + **W11: `wire`/`onSaveScene`/`onLoadScene`**); the `/view` page is the miniature editor shell with the stable `window.__VIEW_TEST__` (field-assignment + revision counter, `src/app/view/page.tsx:454`; the file 610 LOC) | `src/components/timeline/` (wc -l + census), `src/app/view/page.tsx` |
| Headless union | **30 prefixed types** (23 `timeline.*` + 7 `track.*` — `WIRE_COMMAND_TYPES` at `api.ts:182-213`, tsc-lockstep both directions `:216-230`; 24 at W5 — C7's worklist needs the 30-refresh); **the W11 census: 24 UI-routed verbs + 6 documented exceptions** (`timeline.selectElements`, `timeline.upsertKeyframe`/`removeKeyframe`/`retimeKeyframe` (singular verbs — keyframes author through the batch gesture paths), `timeline.advancePlayhead` (rAF-local), `timeline.trim` (uniform-delta; UI resize commits as updateElements patches)); `apply()` never-throws, `applyBatch` atomic (`api.ts:1739`, undo/redo rejected inside batches `:1745`, per-command `data.results`), NOOP≠NOT_FOUND (`api.ts:921`); `track.setAllLocked` `:1605` / `track.setAllMuted` `:1624` | `headless/api.ts:41` (union), `:182-213`, `:1739`, `:921`, `:1605`/`:1624` |
| Module areas (LOC, R24 census) | core 445 / types 382 / snapping 167 / placement 854 (`enforceMainTrackStart` :175) / ripple 395 / **ops 4,562** (`timeline-core.ts` 2,890 — was 2,873 at R23) / controllers 3,419 / media 160 / render 385 / headless 1,806 / view 542 / bookmarks 208 / animations 1,019 / selection 212 / testing 16,731 (28 files, the in-page harness) | `wc -l` per area |
| C7 rename | **NOT STARTED, still deliberate** (its DECISIONS #9); re-chartered per D24 → **r1 at END** (the one-day app migration sub-gate); lineage per D29: 24 (the R15 charter) → 28 (the R22 pin) → 30 (R23) → **the 24-routed + 6-exceptions split at `c15a629`** | its `.agents/DECISIONS.md`, §0 GAP |

**Verified state (SCOUT-R8-A, 12/12 citation checks passed; module rows re-verified Round 15 by SCOUT-R15-B where marked):**

| Module (LOC) | Contents | Spec value |
|---|---|---|
| `core/` (395) | `MediaTime` 120k branded ticks (:21), rational `FrameRate` (:9), deterministic id counter (:10) | Decision-2/3 type layer executable; `resetIdCounterForTests` (:15) is the test-replay pattern |
| `types/` (299) | `SceneTracks {overlay[], main singleton, audio[]}` (:95-99), 5 TrackTypes (:43), 7-way element union (:155), drag/drop models (:240-271) | **The runtime state model of record** (§2.4.1); spec 06 §4.7 executable |
| `snapping/` (151) | zoom-aware threshold `(10px/pps)*TICKS` (:48), closest-wins (:73), lazy sources, keyframe-source seam (:44) | Spec 05 §14.6 reference, exact |
| `placement/` (807) | 5-strategy union (:43-54), reject-not-shift overlap, **magnetic main-track zero-anchor** (`enforceMainTrackStart` :167) | Spec 05 §14.5 + spec 06 §5.9; zero-anchor semantics = ledger #26 |
| `ripple/` (395) | diff-based ripple (vacated−joined interval arithmetic, `computeRippleAdjustments` :121) | Spec 06 §5.4's adopted algorithm, executable |
| `ops/` (2,434) | group-resize (snap-once trim), group-move (`PlannedElementMove` :69 — **exact spec 15 §4.3.3 match**), split (retainSide), retime math | Spec 06 §5.1-5.3 algorithm layer |
| `ops/timeline-core.ts` (2,213 — Round 15) | `TimelineCore` class: ~50-method public surface (readouts, ops, bookmarks ×6, keyframes ×6, tracks, preview overlay ×4, history ×10, `toJSON`/`fromJSON` :1769/:1773) + preview/commit + `UndoStack` w/ transactions | The engine.timeline manager surface; spec 06 §4.6 preview/commit reference |
| `view/` (501) | 50 px/s base, exponential zoom (MIN 0.1/MAX 100), DPR-snapped pixels, ruler interval tables, layout constants | Spec 05 §5/§14.3 view math; ledger #28 |
| `controllers/` (3,190, 11 files — Round 15) | framework-free state machines: resize, playhead scrub, element drag (`TIMELINE_DRAG_THRESHOLD_PX = 5` :51), zoom, drop-target | **Spec 05 §14.4's controller architecture, verified 1:1** |
| `media/` (158) | virtual solid-color video + pure-tone audio + Goertzel detection (:107) | Engine-compatible test media (spec 17 §5 pattern) |
| `render/` (323) | `PlaceholderCompositor` Canvas2D, `setTracks` (:116)/`renderFrame(t)`, waveform peaks + drawn-column measurement | **The render seam (§2.4.4)**; M12/M13 = tri-invariant pixel/audio legs |
| `headless/` (948 — Round 15) | **24-command union** (`api.ts:39-125`), `CommandResult {ok, code…}` (:127-140), never-throws `apply()` (:154-165), atomic `applyBatch` (:886-923), out-of-band readouts (:925-937); all 24 pinned by the M29 parade (21 tests) | Spec-15 skeleton, wrong names (C7, NOT STARTED — deliberate per its DECISIONS #9) + 5 coarse codes vs §6.3's ~24; 24 of 78 types (~31%) |
| `testing/` (12,357, 20 files — Round 15) | M1–M41: **303 in-page tests across 32 suites**; custom in-page harness (NOT Vitest); M11 = state-WYSIWYG via synthetic events; M14 = command-path == direct-path | Spec 17 re-tier source (M1-M10 are Vitest-ready pure TS) |

**Its decisions:** 15 in `.agents/DECISIONS.md` — pure-TS Rust-port time system (D1, citing our Decision 3), TimelineCore composition, snapshot undo, determinism ids, the Canvas2D placeholder compositor as seam, virtual media following the nle-engine pattern, NOOP-vs-NOT_FOUND split, framework-free event types, the **false-premise prefixed command names** (D9 — see §2.4.2 and C7), leaf-channel keyframe distillation (#13), and the final-round hardening record (#15: 3-wave review — engine wave fixed fromJSON keyframe-id seeding (data-loss class), the shared update-pipeline seam, classic boundary defaults; UI wave fixed SSR-500, dead handle-drag, rAF zombies, Escape-cancels, classic constants; test wave closed coverage holes).

**The algorithm home (Decision 12.3, replaces the R8 division of labor):** opencut-timeline owns placement/zero-anchor/ripple-diff/split-snap-once/group-move/controllers as landed; the engine's FreeCut-side families port into it (§2.4.3, chartered at A2 — not started). The R8-era P3 polish list (zoom two-regime anchor, edge auto-scroll, drop-line, keyframe lanes) is **ALL LANDED as of W8** (Round 15) — the engine + interaction semantics are complete and verified.

**The W8 UI surface as-landed (Round 15, `src/components/timeline/` — every item source-verified by SCOUT-R15-B §2):**

- **Theming tokens** — per-type element bg hexes, waveform/burn/bookmark colors: `theme.ts:6-20`.
- **Per-type clip content renderers** — video|image|sticker tiled media thumbs; text/graphic/effect glyphs; audio waveform+volume line: `TimelineElementView.tsx:145-209` (MediaLookup seam `:62`).
- **Waveform** — 1 px bars every 2 px, viewport-virtualized canvas, DPR-scaled, classic dB curve, orange burn cap: `TimelineAudioWaveform.tsx:1-16`.
- **Volume line** — perceptual dB↔position mapping, absolute-jump drag, 0.1 dB snap, preview→commit, portal tooltip, suppressed when volume keyframes exist: `AudioVolumeLine.tsx:1-14` + `theme.ts:64-93`.
- **Snap indicator** — 2 px translucent line at snapped time, scroll-corrected: `TimelineSnapIndicator.tsx:1-11`, wired from resize/drag/bookmark seams (`TimelineView.tsx:152-160`).
- **Toolbar** — play/pause, split ×3, duplicate, delete, undo/redo, bookmark, snapping, ripple-mode toggle, log-mapped zoom slider — all M24 `data-test` hooks preserved: `TimelineToolbar.tsx:1-25`.
- **Track labels** — type icon + mute/visibility toggles behind classic gates, selected-track highlight: `TimelineView.tsx:745-817`.
- **Context menus** — controlled, portal-free, collision clamping/flip, Escape/outside/wheel/blur close; element + track menus since W8-d: `TimelineContextMenu.tsx:1-15`.
- **Keyframe lanes** — expanded per-property rows (classic PROPERTY_GROUPS ordering, 20 px lanes): `expanded-layout.ts:1-10`; lane box-select + diamond drag-to-retime in `TimelineElementView.tsx` + `hooks/use-keyframe-box-select.ts`.
- **Drag line** — horizontal insertion indicator at drop-target track Y, TWO mounted (library DnD + element drag): `TimelineDragLine.tsx:1-14`.
- **Edge auto-scroll** — rAF loop, proximity-speed ramps, wired for element/bookmark/playhead drags (marquee deliberately excluded): `hooks/use-edge-auto-scroll.ts:1-13`.
- **Two-regime zoom** — classic playhead-anchored layout (anchor above slider 0.15) + rAF-coalesced wheel: `hooks/use-timeline-zoom.ts:8-17`, its DECISIONS #17, M36.
- Also: virtualized ruler ticks, declarative+imperative playhead, bookmarks row (seek + drag-to-move), marquee box + ratchet merge, drag-time preview transforms, selection ring, selection journal for undo/redo restore, classic action semantics (Q/W split-side, keyframe-first delete, escape ladder).
- Not attempted (documented): graph-editor popover (W8-f stretch), bookmark popover (app-shell chrome) — the two remaining UI residuals.

**The host seam (Round 15):** `TimelineViewProps` (`TimelineView.tsx:115-127`) — `core: TimelineCore` (REQUIRED, the SSOT instance prop) + `fps` + optional `snappingEnabled`/`initialZoom`/`isShiftHeld`/`dragSource`/`mediaAssets`/`mediaLookup`. The host supplies a constructed core (round-trip via `toJSON`/`fromJSON`), a media registry for clip visuals, a drag-source for library DnD; the `onViewStateChange` view-state seam exists at the HOOK level only (not yet a prop — an assembly-time gap to close at A3). The `/view` fixture (`src/app/view/page.tsx`) is the miniature editor shell — dark panel grid + Library/Preview/Timeline — and exposes `window.__VIEW_TEST__` {core, ids, keyframeIds, dragSource, registry, fps, layout} (`view/page.tsx:356-373`); the runner page exposes `window.__TIMELINE_TEST_REPORT__` + `[data-test="suite-complete"]` — the contract the app's `/dev/view-fixture` must replicate to reuse the 120-test real-mouse suite. **The regression-harness attribute is `data-test`, NOT `data-testid`: the R24 repo-wide census is 73 static values + 8 dynamic families** (`library-item-{assetId}`, `resize-handle-{side}`, `label-mute/visibility/lock-{trackId}`, `context-item-{key}`, `rate-seg-{rate}x`, `milestone-{id}` — the W11 additions `label-lock`/`rate-seg`; 100 sites repo-wide, 68 in the components tree) — the final app's harness must target the same attribute or the view must dual-emit.

**The W9 review state (Round 15): TERMINAL with review consensus** — 4 facet audits + 3 peer rounds + 2 external rounds (CodeRabbit + Codex) feed its 270-row REVIEW-TRACKER; **zero open P1/P2** (only 3 dispositioned P3 Trivials: keyframe snap-point caching perf, keyboard volume a11y); suite 423/423, tsc clean (confirmed by execution). The deliberate divergences from classic are ledgered: **DECISIONS #20 (10 rulings** — engine-seek frame-snap, commitElements re-runs, bookmark `B` keybinding, playhead-focus arrow guard, discrete-channel keyframe removal, selection journal scope, split-left ripple-seek clause, depth-keyed journal, scroller-null hardening, keyframe-indicator sort) **+ #21** (bookmark move collision → reject → engine NOOP, stricter than classic; M19-pinned). **C7 rename: NOT STARTED, deliberately** — its DECISIONS #9 defers it as an nle-core-spec internal conflict; the spec-side prerequisite is now supplied (spec 15 §13.15 refreshed to the 24-command reality, Round 15), and the rename + op-family growth are chartered at A2 (ARCH-R15 §3.4).

### 3.3A web-daw-core — `github.com/bearachprema/web-daw-core` (private, LANDED Round 9; **re-baselined Round 15 as a PURE core**) — the audio-domain core

**What it is:** the AUDIO-domain normative core (Decision 13 / spec 20) — web-daw's DAW-grade engine extracted as a package: channel strips with insert chains + aux sends + sidechain, 20+ DSP effects (convolution reverb, worklet-backed compressor/limiter/stereo-widener), PDC, offline render with bounce parity, WAM/WASM plugin hosting, and the real-DSP waveform test harness (nullDepthDb AES null-test gates). **721/721 tests (30 files — re-run Round 15; wall clock ~149 s on the 4 GB sandbox, tests-phase 53.65 s; CI's 30-min timeout ample), tsc clean, zero runtime deps** (the only non-relative imports in `src/` are vitest in tests + `node:fs`/`node:path` in a build script; all five package deps are devDependencies). Extracted from web-daw `main@913d0d7` (the only LIVING ancestor — upstream unchanged since the sync, ls-remote verified 2026-09-04). @`374711c` = `5243c49` (the M1.6 relocation commit) + 1 docs-only wrap. **M1.6 made it a PURE core: the NLE bridge RELOCATED verbatim to nle-engine `src/lib/nle/bridge/` (§3.1's per-file table) — the old `nle-bridge` references that placed the bridge inside web-daw-core are superseded; the bridge tests live at nle-engine `tests/vitest/nle-bridge.test.ts` (37 tests, C1/H8–H17), and 752→721 with the move (31 bridge tests moved, none lost).** Zero submodules; CI = exactly one GHA `gate` job (vitest + tsc).

**The three file classes (the whole repo design — Round 15 census):** **copy** (75 src files + 4 root assets — verbatim upstream bytes, NEVER hand-edited; they update by re-sync), **shims** (11 files at upstream import paths so the closure resolves without the DAW app; the instrument ones double as the `setInstrumentProvider()` injection seam — verbatim `channel-strip.ts` can drive the real app's instruments), **coreOwned** (**5** files: the barrel `index.ts`, `test/index.ts`, `test/setup.ts`, the de-risk suite, the instrument-provider test — the NLE bridge is NO LONGER in this set).

**Spec value:** the three-layer track model (S structure / G signal / E engine, sharing only `trackId`) is spec law (spec 20 §3; `docs/track-model.md` is the contract doc, updated to name the bridge's new home). No timeline/NLE types leak into `src/lib/daw/**` — verified by grep (the only hit is a doc comment). The S-facing interface is 9 fields (`StructuralAudioSource`) — timeline-agnostic by construction. **M1 ✅ / M1.5 ✅ / M1.6 ✅ COMPLETE** (extraction; nle-engine wiring incl. bridge + H1-H16 pins + parity gate + realtime Player path closed; bridge relocation). **M2's SoundTouch offline half is LANDED (W2 @ `de09c93`): `src/lib/daw/soundtouch.ts` (the typed LGPL WSOLA port, bit-exact vs freecut's reference) + `src/lib/daw/varispeed.ts` (retimeChannels — the F1 flush law, EXACT round(N/rate); the [1/32, 32] rate domain) + `src/test/varispeed.test.ts`/`varispeed-worklet.test.ts`, consumers landed the same round (engine `df6eadf` + app `0ac3f15`); the M2 REMAINDER (the mixer surface: live parametric EQ/reverb sends/inserts/solo-mute/sidechain ducking/PDC + the 4 deferred upstream test ports) → r2 per the D24 mapping.** The UPSTREAMING story is bidirectional: sync-from-upstream is LIVE (hash-verified verbatim copies, fail-loudly import validation — "seams must be conscious"), Phase B (web-daw consumes the core back via `setInstrumentProvider` before the first ChannelStrip) is READY, and Phase C (upstream deletes its copies; the sync direction INVERTS) is specified, not started. Full contract: **spec 20** (canon for this repo per Decision 13).

**R24 re-baseline (the live register @ `85b81b0`):** **759/759 tests (33 test files), tsc 0, zero runtime deps** (devDeps only: types/jsdom/typescript/vitest/web-audio-api) — **RUN LOCALLY by the module audit** (frozen-lockfile install + `vitest run`, 92 s). Exactly ONE commit past the R23 pin `494f6ff`, docs-only (4 files: SKILL/HANDOFF/PLAN/worklog — the seal-round wrap): **the S4 seal round CLOSED** (PLAN S4 + the honest remaining ledger + SKILL §11's six new laws; the seal's content — 2 fresh-context cross-repo audits → 3 P1 + 8 P2 fixed with pins incl. the A1/A2/A4 flattener/adapter/fade-clamp twins, F1 el.speed end-to-end, **W3 the JKL audio half CLOSED** (composed varispeed = element × transport, fades ÷ rate; the design doc lives APP-side), F3/F7 dB-law convergence, A6 barrel hygiene — landed BEFORE `494f6ff`, first fully documented at `85b81b0`; SEAL-READY verdict, P3/P4-only). Consumers DIVERGED: engine @ `494f6ff` (one docs-only commit behind — zero code delta), app @ `85b81b0` (opportunistic freshness). The **whole-CR rounds A+B landed** (only P3-class findings remain) with two gate upgrades: **`sync --check` now FAILS on drift** (CR-B1 — it was exit-0-blind for the repo's whole life) and a repo-root import escape is now a loud SEAM violation. The K2 WDC-side combined family (the "combined test that audio core has done before", the user's words): `src/test/audio-integration.test.ts`, `dsp-bounce-parity.test.ts`, `dsp-effects-integration.test.ts`, `nle-audio-core-derisk.test.ts`, `real-audio-e2e.test.ts`, `offline-parity-wiring.test.ts` + the meter-tap/varispeed suites — the K2 census's audio half. Design docs: `docs/design-w1-meter-taps.md`, `docs/design-w2-varispeed.md` (+ its §10 CR addendum).

### 3.3B nle-ui — `github.com/bearachprema/nle-ui` (private, NEW register row Round 23 — the tier-4 asset previously carried only in §0A/§9; this closes the missing §3 row)

**What it is:** the PRODUCTIZED shell-variants package — the engine-free chrome layer (Decision 18/21, **KEPT per D27**): AppShell + panels + `useUiStore` + the `TimelineEditCommand`/`dispatch` router seam (D28) + the `shell-*` testid contract, gated by its own vitest + `tsc` + `scripts/check-boundary.mjs` (the zero-engine-imports law) + the Storybook build (10 story files, 74 stories). **674 tests, tsc 0, boundary OK @ `fc4cc35` — RUN LIVE by the module audit** (37 suites; 648 @ `85dcf57` at R23). The D29 seam waves LANDED: **W2.3 saveState** (`useUiStore.ts:122`/`597` — the autosave lifecycle: idle/dirty/saving/saved, view-state, no history entry; the ⋘S real-dirty gate + beforeunload + StatusStrip dual-world), **W2.4 DeliverPage exportRequest** (`DeliverPage.tsx:37-91` — the 5 TYPE-ONLY barrel shapes; absent = honest mock) + **W2.5 per-scene grade sidecar** (`SceneGrade` `useUiStore.ts:42` + `setGrade` :199 — CSS-filter domain, neutral-seeded merges; ColorPage Contrast/Saturation LIVE) + the R8-REV #1 retryJob blob-URL revoke. **The kind-aware mute-all pin** stands (`src/state/timelineRouter.ts:59` `setAllTracksMuted` — the mute twin of the D-T3 lock-all: ALL tracks, ONE engine history entry via OT's S3 `setTracksMuted` batch op, engine-owned track-muted per the single-writer law; the mixed-state convergence target is kind-aware — a mixed track set converges to the mute-all target over MUTEABLE tracks, pinned in `timelineRouterDispatch.test.ts`). AppShell's FOUR optional prop seams (each absent = mock world): `timelineRegion`/`mediaDragSource`/`programMonitor` + `exportRequest` (`AppShell.tsx:208-227`, W2.4). Consumed by the app @ `fc4cc35` (the app at package HEAD — no lag). Its queue: the W2.3 Load/Save-As file affordance, the upstream-back of the bf9be13 hardening hunks, the frozen-grammar re-pin batch, the W3 remainder + the T1 view-math port; **the C0 MiniShell chrome family is its crawl-window deliverable (the plan's S-package row) — STILL NOT STARTED (no MiniShell component in src/; no C0 row in its own PLAN — the filing gap)**. Orientation: `.agents/` (SKILL → HANDOFF → PLAN → DECISIONS D22-D28 — D29/D30 not yet filed package-side).

### 3.3C nle-test-app — `github.com/bearachprema/nle-test-app` (private, NEW register row Round 23 — THE APP, same closure as §3.3B)

**What it is:** THE APP — the assembly roof that validates the whole topology (AppShell (nle-ui) + EngineMount (OT timeline) + ProgramCanvas (engine N1 composition-frame) + the WDC audio host + GluedShell store family + `engineService.ts`/`sceneBridge.ts`/`audioService.ts` glues) — **KEPT and evolving IN PLACE per D27/D28**. **174/174, tsc 0 @ `c885ece`** (static census + commit claims: GluedShell 70 + audioService 40 + sceneBridge 25 + persistence 17 + deliver 15 + engineSeam 7; 117 @ `70e99f0` at R23) — the W3 JKL transport (`audioService.ts:374-382` the S3-C4 reschedule edge: a JKL rate change while playing reschedules at the composed rate, IDEMPOTENT via `lastScheduledTr`; the honest NEGATIVE degradation — WebAudio cannot play backward — `audioService.test.tsx:871/:923`; design doc `docs/design-jkl-audio-follow.md`) + RR1-B + the N2b/W2 app-side waves + **the R8/D29 REAL wiring LANDED**: `persistenceService.ts` (380 LOC — engine-free localStorage autosave, 2 s debounce, boot hydrate, ⋘S/beforeunload, Save-As/Load snapshots), `deliverService.ts` (344 LOC — the REAL master export via mediabunny@1.50.8 + `renderCompositionCore` + `SceneMixer.renderOffline`; ranged/abort/frame-PNG paths), the W2.1 sidechain ducking (`insertEffects` → external-sidechain compressor −30 dB; `rewireSidechains()` after every materializeStrips), and the W2.5 effects/color preview (`ProgramCanvas.tsx` 242 LOC — `buildElementFilterString` per element + the per-scene GRADE final pass: contrast/saturate/brightness/hue-rotate, one filter-draw after compose). **The vendoring record (the consumer pins, R24):** `vendor/nle-timeline` @ `c15a629` — **the byte-exact LOCK-COPY** (`UPSTREAM.lock.json` upstreamHead `c15a629`; live-verified vs `git archive c15a629 src/lib/timeline` minus `testing/`: 0 diff / 0 missing / 0 extra, 14,912 LOC both sides; upstreamPath `src/lib/timeline`, `testing/` stripped, "no other local edits permitted — engine is EXACT upstream"; the `@vendor/timeline` alias at vite:33/tsconfig:17) + `vendor/nle-engine` @ `5036387` (the R9 W-A re-pin — EXECUTED, at engine HEAD) + `vendor/nle-ui` @ `fc4cc35` (file: link, the closed exports map) + `vendor/web-daw-core` @ `85b81b0` (the R9 W-A tail). **`src/timeline-port/` (40 files / 8,556 LOC) is a CONVERGING MIRROR per D26 — the adopt-wholesale census (see §0 GAP's D26 row)**: 39 mirrors of OT's 40 component files = 7 byte-exact + 25 mechanical-exact (the import specifier only) = 32 zero-action + 7 documented carriers (5 pending D30 W-C/W-D adoptions: TimelineView, use-timeline-actions, use-keybindings, use-element-interaction, TimelineToolbar; 2 port-local keeps: TimelineContextMenu's functional `setPos`, use-playback-ticker) + the host `EngineMount.tsx` (306 LOC); `hooks/use-wire-dispatch.ts` (90 LOC) NOT yet ported — D30 W-C, in flight. The app has NO `.agents/` yet (**D27/F17: create it BEFORE W-C — the cross-repo lockstep needs the notification surface**).

### 3.3 cloudcut UX-spec + cloudcut-nle — `github.com/frejogochukwuout/cloudcut-nle` branch `ux-spec` (integrated Round 8)

**What it is:** the prior cloudcut iteration's UX knowledge, distilled into a mature app-layer UX spec set (`docs/ux-spec/`, 28 files / 7,933 LOC, v1.3.5, three review rounds, 0 MAJOR remaining) — the user's "fullest" source on app shell and application/project-level logic. Integrated per the canon rule: **where it contradicts the spec set, the spec set wins** (25 contradictions logged in SCOUT-R8-C §3 with resolutions; the rejection register lives in SCOUT-R8-C §6 and spec 18 §8). Its adoptions landed in spec 18 v1.1 (context menus, per-panel states, a11y floor, error/toast presentation, visual language, pointer grammar, perf budgets, media-pool depth, drag/ruler feedback, save-status chip, inspector field contracts, viewer affordances, Deliver-page UX) and in 00-master's new non-functional requirements section. The cloudcut-nle repo itself (main branch) serves as the **UX/app-scope reference codebase** (tier-5-style: messier, lower quality, but the working as-built for app-shell/project flows — cite for UX patterns, never for engine patterns; engine reference stays nle-engine). **R24 (D27.3): dispositioned the DORMANT PREDECESSOR (June-July 2026) — no code migration, no repo changes; its postmortem (the bridge/adapter failure pattern — "the mapping surface grows quadratically with features" — vs. the reskin/adopt recommendation) is D26's cited evidence; its hybrid-storage RFC (three-tier storage, the 480p-proxy pattern) is registered as a w2/r4 media-design reference only.**

### 3.4 Teachers (public)

FreeCut and OpenCut-classic remain the citable, public sources of record. Every claim in specs 01-12 carries their file:line citations, verified by the 16 audit reports in `audits/`. They stay in tier 4 even as tiers 2-3 grow, because they are the only *public* evidence chain — valuable for anyone outside the private repos.

### 3.5 ui-mock/shell-variants — in-repo (NEW, Round 15)

**What it is:** the SHELL design + chrome layer, living in THIS repo (the `d42693e`/PR-#1 state is R15 history — the R22/R23 states follow) — the R15-era description follows with its point-in-time counts: 27 component implementation files (shell 12, timeline 6, mixer 5, pages 2, debug 2) + `App.tsx`/`main.tsx`, **~8.9k impl LOC (17.6k incl. tests)**; **596/596 vitest (34 files, re-run Round 15, 218.9 s), `tsc --noEmit` exit 0**; **71 stories across 10 story files** on **Storybook 10.6** with the vendored `storybook-annotakit` review surface (pin → thread → GH-mirror workflow; the strongest single reusable asset — a config change, not a port, per ARCH-R15). Stack = exactly the 00-master §4 row (react 19.2.8, zustand 5, tailwind 4, vite 8, vitest 5, TS 7). **No CI workflow — the suite is the per-round local gate** (the mock is ported into the app at A3 and retires as a repo after A7 per ARCH-R15 §3.1 — the R15-era plan, superseded by the post-K4/w1 retirement trigger). **PR #1 state: OPEN** (base `ui-baseline`, head == `d42693e` — the R15 point-in-time state; 146 files +35,087/−32, 40 commits, mergeable clean at that pin) — down to **2 P3 inline comments** (a HANDOFF doc wording; a dead story args generic); R14's own audit: 90 review entries ≈ 75 unique findings, all closed.

**R23 state (the live register; UNCHANGED at R24 — no new mock rounds; my R24 static census reads 1,588 tests / 61 files, above the tracked floor):** **1,521+ tests current-tracked (60 test files), tsc 0** — the lineage 596 (R15) → 1,334 (R22, W0-W6: real color math + real scopes data) → 1,470 (the R23 W-B color wave, the ARCH-R23 evidence base) → **1,521+ (current-tracked; the battery re-baselines centrally)**. The R23 additions: the **W-A FX surface wave**, the **W-B color wave**, and the **R23-G VLM net** — `scripts/vlm-capture.mjs` + `vlm-review.mjs` + `vlm-run.sh` + `vlm-rubric.md` — the programmatic visual-review tooling that K3/w1 ADOPT (don't fork) onto the app's `.storybook` and OT's runner (the D26/K3 ordering law's VLM half).

**Its role in the hierarchy:** ≈**55% of the final app's UI-layer risk de-risked** (range 45-65%, SCOUT-R15-D §10 — "decisions made + pitfalls already paid + scaffolding reusable, NOT code-that-ships"). What survives as code: the chrome layer (Toolbar2/dock/status/toasts/dialogs/cheat-sheet/context-menu primitive), the view-state store slice (`useUiStore.ts` — spec 18 §6.2's field list transfers near-verbatim; the doc slice + snapshot-undo + mock mixer sidecar are discarded), the `shell-*` testid contract (98 unique base names; all 11 spec-18 §10 panel roots honored), and the review infra. **The timeline region (Timeline/Clip/Ruler, 1,326 LOC) is NOT shipping code per Decision 12** — a design reference + law register; its behavioral laws transfer as tests/spec text. The mock's compounding asset is the negative space: 29 C-registrations + 15 N-findings + 6 A-conflicts + 4 B-gaps — the spec set's UI ambiguities surfaced before the real build.

### 3.6 ui-mock/shell-mini — in-repo (NEW row, Round 23 — previously cited as §3.6 in §0/§0A without a section; this closes the dangling reference)

**What it is:** the minimal-UX mock that the R22 user directive re-typed as **the crawl's law register** — the miniature shell (~29 source files vs the variants' 100+, 13 stories) whose test corpus was SEALED at the R23 seal round and whose value is the re-expression worklist, not the code. **355/355 tests (8 files, vitest/jsdom), tsc 0, SEALED** — the lineage 358 (R21e) → 333 (the R22 drag-machinery retirement, user directive) → 343 (the R22 review-loop nets) → **355 (the R23 seal + fix rounds)**.

**The seal artifacts (the register this row exists to carry):**
- **`docs/OT-SEAMS.md`** — the timeline-OPS-only seam map (the pre-C1 seam study; the mini↔OT projection laws).
- **`docs/CORE-SEAMS.md`** — the R23 seal round's WHOLE-surface seam audit (the user's directive: "audit of all the ui vs. core system seams with NLE engine / timeline / audio core / project asset etc."): **26 seam rows** across 5 domains (A timeline / B engine / C audio / D project-asset / E chrome-view-infra — the targets cited at the R22 pins `05584d8`/`f68ab8c`/`fe05d85`, the seal's point-in-time evidence base) + the store partition (§2, the transport map for `useMini`) + §3 the file→phase transport map + §4 the audited-absent negative-result register + §5 the gap register.
- **`docs/LAW-NET-INVENTORY.md`** — the law corpus as the crawl's acceptance lists: **8 files / 355 tests / 128 census units** (122 describes + 6 split families) — **13 HOLDS-on-OT units (33 tests) / 115 GAP units (322 authored tests)**; the four GAP classes (GAP-app-C0/C1/C2 view+policy 102 units/282 tests; GAP-C2/C3 engine/audio boundary 7/21; GAP-W-ops composed-ripple 4/16; GAP-verify-C1 2/3). K3's app-side re-expression consumes this row-by-row (the 322 authored + the 33 kept near-verbatim as projection-conformance pins — ARCH-R23's arithmetic).
- Plus `docs/RH-skin-extraction.md` (C0's input) and `docs/DESIGN-mvp.md`.

**Its role in the hierarchy:** design reference + the crawl's acceptance-list source — NOT shipping code; the D25 swap retires it per the port-then-swap law (trigger post-K4/w1, text in 00-master's standing laws). The mock's grammar feeds the canonical tree in TWO halves (D25.3): the TOKEN half (a mini theme mode on OT's CSS-variable surface — w1-entry) and the STRUCTURAL half (the track-head column, minimized strip, compact/pill mode, tools-row deltas, the testid-emission convention — OT-side additive components in the crawl window; the mini's 59-static + 15-templated `data-testid` census is K3's DOM-structural gate, while OT today emits ZERO testids).

## 4. Link vs Inline Policy (recommendation)

The spec set's options were: link reference code and slim the prose, or keep inlining distilled versions. Round 7 adopts **link + distilled-insight callout** as the default, with three mandatory-inline cases:

1. **Default — LINK + callout**: a per-spec "Code References — nle-engine (reference, NOT canon)" table (inserted in every stream spec this round) maps spec section → `file:line` → one-line verified quote → status tag → note. The link carries the detail; the note carries the delta.
2. **Inline whenever the insight is a FreeCut-iteration lesson not visible in reference code** — e.g. why `<video>` decode was rejected (spec 03 §5.1), the CDP ArrayBuffer serialization cost (spec 11), the kimdogyeom persistence bugs (spec 09 §13). These exist only as prose; linking would orphan them.
3. **Inline every correction of a FreeCut pattern** — the "why we diverge" text (Decision 5's color-space reasoning, spec 15 §14.11's output-command design). The correction is the spec's own content; the reference repo's code can never carry it.
4. **Inline when contract precision requires code-level shape** — Zod schemas, type unions, method signatures (spec 15 §4/§11, spec 09 §3). Consumers implement against these directly.

**The enforcement mechanism is §5's ledger**: every crown-jewel insight is enumerated with its spec home; any future pass that rewrites or slims a spec must relocate each ledger row, not delete it. A link that replaces an inlined insight must point at where the insight now lives — the ledger row is the tripwire.

## 5. Insight-Preservation Ledger (never drop these)

Years of FreeCut iterations plus six rounds of distillation produced specific, hard-won implementation insights. Each row below is load-bearing: it either corrects a real bug pattern, encodes a non-obvious API reality, or pins a numeric constant somebody bled for. The "visible in" column shows where reference code *also* demonstrates it — that column is a convenience, never a substitute for the spec text.

| # | Insight | Spec home | Visible in (reference) | Class |
|---|---|---|---|---|
| 1 | `AudioContext.currentTime` is the only trustworthy clock; `performance.now()` drifts vs audio; monotonic `Math.max` guard; 100ms `timeupdate` throttle; re-anchor on seek/rate/visibility | 03 §3.2-3.4 | nle-engine `core/clock.ts` (:137 the 100 ms throttle, :568/:591 the monotonic guard, :659/:701 the re-anchor paths) — near-verbatim (R23 grep) | Corrects drift |
| 2 | Six sync plans with exact thresholds: 0.2s large drift, 0.15 rate cap, ±5%/±15% rVFC bands, -0.2s/+80ms behind debounce, +0.5s far-ahead, 1ms coalesced-seek epsilon; hard seeks only on target discontinuity | 03 §6 | engine `video-sync.ts` (:181-225 constants; :907-913 the ±5%/±15% correction bands — R23 grep) | Constants |
| 3 | Sync plans drive *decode rate*, not `<video>.playbackRate` — WebCodecs frame timestamps are the actuator (mediabunny `VideoSampleSink`) | 03 §5.1, §6.5 | engine ports plans but targets DOM elements (CORRECTIVE) | Corrects FreeCut |
| 4 | mediabunny exposes NO `pixelFormat` option (`VideoSinkDecoderOptions` = hardwareAcceleration + optimizeForLatency only); 10-bit arrives as `I420P10`-family from the browser's VideoDecoder | 03 §5.2/§14.D, 04 §14.C, 00 glossary | — (API reality) | API reality |
| 5 | 10-bit YUV values are MSB-aligned in 16-bit cells — extract via `u16 >> 6`, never `& 0x3FF` | 00 §12 glossary, 04 §5.1 | — | Corrects bit-packing |
| 6 | Scene-linear is the only correct compositing space; FreeCut applies exposure on sRGB-encoded values (`c *= pow(2.0, exposure)` at color.ts:611) — the canonical "what not to do" | 00 D5, 04, 08 | engine reproduces the 8-bit sRGB baseline (29 `rgba8unorm` sites) | Corrects FreeCut |
| 7 | LUT sampling needs linear↔sRGB conversion around the lookup; FreeCut (and the engine port) sample raw — banding + wrong colors | 08 §7.4 | engine `lut.ts:323-337` (bug preserved — no conversion in the shader, R23 grep) | Corrects FreeCut |
| 8 | Ping-pong textures must be pool-acquired per consumer; returning a pass-owned singleton corrupts the second effected/masked clip in the same frame | 04 §7, 07 §7.3/§8.3 | engine P0.1/P0.2 (the counter-example sites now documented at pipeline.ts:5135/:5206 — the file is 5,320 LOC at `5036387`, unchanged) | Discipline |
| 9 | Render loop must composite ALL item types; `if (clip.type !== 'video') continue` silently renders blank output for comps/images/adjustments | 04 §7.1, 07 §12.2 | engine Wave-4C landed the per-type branches — the counter-example is RETIRED (§3.1's R15 row records the retirement); the rule stands as the law | Discipline |
| 10 | Transitions: structural type (cut-centered adjacency, clips don't move) split from presentation (shader); hidden source handles + alignment math + binary-search max duration | 07 §6 (Round-7 amendment) | engine planner.ts:27, handle-utils.ts:302, registry.ts:2249 | Adopted from FreeCut |
| 11 | Scene assembly walks DESC (occlusion cutoff at first fully-opaque top item); compositor paints array order; player reverses — the reversal lives in the player, not assembly | 07 §5 (Round-7 amendment) | engine scene-assembly.ts:1249-1255 (the reversal contract) + :729 (R23 grep) | Adopted |
| 12 | Undo snapshot equality must cover the FULL field set (17 fields incl. keyframes, compositions, backgroundColor) — keyframe-only edits silently stop being undoable | 06 §7, 15 §14.1 | engine P0.6 (timeline.ts:1521 `snapshotsEqual` — R23 grep) | Discipline |
| 13 | Batch commands must roll back on partial failure (atomic all-or-nothing) — aborting mid-batch and keeping prior ops corrupts state | 15 §7.1 | engine api.ts:1057-1060 (the batch abort keeps prior ops — the counter-example stands, R23 grep) | Discipline |
| 14 | Commands are pure JSON; input I/O happens pre-command (probe/persist before `importMedia`); output artifacts ride `CommandResult.data` (§14.11 output exception); `$ref`-style intra-batch pointers make commands non-replayable — pass IDs by value | 15 §2/§5.4/§14.2/§14.11 | engine api.ts:543-556 `resolveOperationRefs` (uses `$ref` — CORRECTIVE; R24 grep) | Corrects FreeCut |
| 15 | Deterministic replay needs `idSeed` on ID-generating commands (split/insert/duplicate/…) | 15 §14.6 | — | Testability |
| 16 | Three-tier testing + UI-interaction-tax rules; engine-only single-tier suites miss error paths (engine: ~9% error-path coverage, 1 throw assertion in 124 tests) | 17 §2/§13, 12 | engine gaps/audit/G | Methodology |
| 17 | WYSIWYG tri-invariant (state/pixel/audio) is CI-blocking, not aspirational | 00 §7/§13, 17 §6 | — | Methodology |
| 18 | AudioWorklet varispeed topology: 0-input worklet, push via port, so it runs inside `OfflineAudioContext` (audio WYSIWYG); granular pitch shift is the simplified fallback when full WSOLA isn't ported | 02 §7.2, 03 §8.2 | engine worklet is 1-in/1-out (`:47`) — CORRECTIVE | Corrects topology |
| 19 | FCPXML 1.10 is DTD-validated (not XSD); most attributes are CDATA — Zod must layer type/range checks; `colorSpace` needs the triplet format + Display P3 requires 1.11+ fallback to `1-1-1 (Rec. 709)` | 10 §3/§11/§13 | — (format reality) | API reality |
| 20 | mediabunny is MPL-2.0 (weak file-level copyleft) — safe via npm, modifications must be shared | 00 §4 stack row | — | License |
| 21 | Worker pool discipline: lazy creation + idle reuse + main-thread fallback for export; FreeCut's 21 workers prune to 10 (the survivors are the load-bearing set) | 02 §3 | engine: 0 workers (counter-example) | Adopted from FreeCut |
| 22 | `snapshotsEqual` + orphan cleanup: every removal op must prune orphaned keyframes; weak `restore()` paths need sanitize/prune/re-sort/overlap-repair with warnings | 06 §7, 09 §5 | engine P1.11/P1.12 | Discipline |
| 23 | CPU-side FFT verification (fft.js, not fftw-wasm which doesn't exist on npm); distinct-frequency tones + solid-color clips make pixel/audio assertions exact | 12 §14, 17 §5 | — | Testability |
| 24 | GPU readback (not rendering) is the cloud-render bottleneck: ~80fps 4K / ~20fps 8K; pipeline 3 frames in flight | 11 §15 | — | Constants |
| 25 | `Xvfb` is mandatory for headless Chrome WebGPU even with `--headless=new` (requestAdapter returns null otherwise); SwiftShader flags `--use-vulkan=swiftshader` + `--enable-unsafe-swiftshader` work in-container (engine-validated); lavapipe is the FreeCut-pattern canon choice | 12 §3.2/§14.A | engine run-nle-tests.mjs:8-35, DECISIONS #12 | Infra reality |
| 26 | **Magnetic main-track zero-anchor**: empty main → element lands at ZERO; requested start ≤ earliest element → clamp to ZERO; sole main element cannot group-move off 0 (`moveElements` on raw track data is the escape hatch) | 05 §8.3/§14.5 (R8), 06 §5.9 | opencut-timeline placement/index.ts:175-197 (R23 grep); M5 fixtures | Adopted from OpenCut |
| 27 | **Insert startTime-override**: first element lands exactly at requested `startTime`, later elements keep relative offsets, then zero-anchor may shift the whole batch; the element's own `startTime` field is ignored on insert | 06 §5.9 (R8) | opencut-timeline SKILL gotcha #2; api.ts:558-718 returns actual landed time (the data block at :712-718; R24 grep) | Adopted |
| 28 | **View-math constants**: 50 px/s base zoom, zoom-multiplier [0.1, 100] with dynamic zoom-to-fit minimum, DPR-snapped pixel math, drag threshold 5px strict-`>`, ruler interval tables [2,3,5,10,15]/[1,2,3,5,10,15] + 120px/18px min spacings, playhead line 2px | 05 §5.2/§14.3 (R8) | opencut-timeline scale.ts:6-8, pixel-utils.ts:13-15/:63, element-interaction-controller.ts:53 (also box-select-controller.ts:34), ruler-utils.ts:17-38 (R23 grep; re-verified unchanged R24) | Constants |
| 29 | **Coordinate-space contract**: `clientX/Y` are viewport-space; `elementRectLeft` must be `getBoundingClientRect().left` (not content-space) — mixing the two is the classic drag-drift bug | 05 §8.3 (R8) | opencut-timeline SKILL gotcha #3, DECISIONS #8 | Discipline |
| 30 | **NOOP is not NOT_FOUND**: a trim clamped to a no-op (at source bounds) returns `ok:false` with a NOOP-class code, distinct from element-not-found; tests assert the distinction | 15 §6.3 (R8), 06 §5.2 | opencut-timeline api.ts:921 (hasAnyRef ? NOOP : NOT_FOUND — the trim case; R24 grep), split-edge NOOP :986; M16 | Testability |
| 31 | **Atomic-batch transaction semantics beyond all-or-nothing**: eviction suspended during transactions, depth-anchored rollback (no history drain), redo-stack cleared after rollback, undo/redo rejected inside batches, intra-batch overlap guard for insert batches | 15 §7 (R8) | opencut-timeline DECISIONS #10 addendum, applyBatch api.ts:1739 (undo/redo in-batch rejection at :1745, verified R24); 3 review rounds | Discipline |
| 32 | **Fake-commit bug class**: an op that returns the original object (not a new reference) defeats snapshot-diff undo — split/join/persistence ops must return structurally-new state | 06 §5.x constraints (R8), 09 §5 | opencut-timeline worklog Task 4; engine's editProject bug (retired) | Discipline |
| 33 | **Command-path == direct-path determinism test**: the WYSIWYG replay invariant is testable as `f(command path) === f(direct manager path)` without any UI — M14 pattern; plus `resetIdCounterForTests` for id determinism (align to spec 15's `idSeed` at the wire boundary) | 17 (R8), 15 §14.6 | opencut-timeline milestones M14; core/id.ts:15 | Testability |

## 6. Corrective Mapping — FreeCut patterns the engine inherited and the spec corrects

These are the systemic deltas (verified in reference source this round). C1-C6 are the FreeCut patterns the engine inherited; C7-C8 are Round-8 additions (opencut-timeline's protocol naming; the engine's persistence shape). Each row: what the reference does (with citation) → what the spec mandates → the convergence work.

| # | Engine (inherited FreeCut pattern) | Spec mandate | Convergence |
|---|---|---|---|
| C1 | 8-bit sRGB `rgba8unorm` end-to-end (29 sites across compositor/effects/masks/transitions/player/lut) | Decision 5: 10-bit pipeline, scene-linear working space, `r16uint` plane uploads, `rgba16float` compositing, `rgba10a2unorm`+display-p3 canvas | Re-texture every pass (formats + shader math); the effect *algorithms* port, the color space is rebuilt |
| C2 | Headless protocol = 19-op JSON-RPC + `$ref` field pointers (`api.ts:1065` `editProject`; `resolveOperationRefs` :543; `EDIT_OPERATION_NAMES` :2025 — R24 grep) | Spec 15: 78-type `EngineCommand` discriminated union, Zod-canonical, single `engine.command.apply()` dispatcher, replayable by value | Build the command layer as a **dispatcher adapter over the existing 102-method class surface** (the methods are the algorithm layer); retire `$ref` (pass IDs by value) |
| C3 | Class-based `Timeline` manager API is the primary mutation surface | Decision 9: every mutation flows through `apply()`; managers become the implementation layer commands dispatch to | Same adapter as C2 — spec 15 §4.2's mapping table is the contract |
| C4 | Single-tier Playwright suite (124 tests in-app, DOM-scraped results, ~9% error-path — the R7-era pattern) | Spec 17 three-tier (Vitest T1 / render T2 / UI T3) + property-based + WYSIWYG invariants | Re-tier per the engine's own G-charter (it independently recommends extracting ~60 CPU tests to Vitest — converging). **Round 15: largely converged engine-side** — its CI now runs 3 venues (274 vitest + 265 browser rows + 318 probes, SCOUT-A §1); the remaining re-tier surface is the app's (spec 17 §17A) |
| C5 | Procedural `VirtualVideo`/`VirtualAudio` media; zero mediabunny imports | Spec 03: mediabunny + WebCodecs decode path, 10-bit, in decode worker | Build spec 03's path fresh (engine's own Wave 6A converges; `MediaRegistry` is the seam it was designed for) |
| C6 | Zero Web Workers (all decode/audio/waveform on main thread) | Spec 02: 10-worker pool + `ManagedWorker` (lazy create, idle reuse, export fallback) | Build spec 02 fresh; engine main-thread code becomes the worker *bodies* |
| C7 | **opencut-timeline prefixed command names** — all 30 headless types at `c15a629` (24 at W5; 18 at the R8 sweep) are `timeline.*`/`track.*`-prefixed on the false premise that 00-master shows `{type:'timeline.split'}` (refuted: 00-master:234/:562 are bare; the namespaced surface its authors saw is spec 15 §4.2's manager-method column) | Spec 15: bare command types, 78-member union | Mechanical rename pass (30 types — the R15 worklist's 24-command refresh is itself stale; spec 15 §13.15 needs the 30-refresh at execution; the W11 census is 24 UI-routed + 6 exceptions) + param-shape alignment per SCOUT-R8-A §3.2 / SCOUT-R15-B §4; `PlannedElementMove`/`PlannedTrackCreation` need zero work. Also: 5 coarse result codes vs §6.3's ~24; no `stateChange` payload; the 78-vs-30 coverage gap (the S/T/S3/W11 rounds closed transport/lock/bookmark/mute/JKL/loop/save-load verbs — the remaining gap is the r1 op family). Re-chartered per D24 → **r1 at END** |
| C8 | **Engine persistence serializes the engine-native "v2" shape** (flat `id/name/createdAt` + `timeline{fps, clips, RGBA-array backgroundColor}`, `schemaVersion: 2`; MASTER §0 records D1 resolved "engine-native v2") — directly contradicting §7's D1 answer; now THREE competing project shapes in the engine (v2, FreeCut wire, headless-normalized) | Spec 09 §3: `ProjectJSON` (`metadata`/`settings`/`scenes`/`schemaVersion`, Zod-validated) | **R9 re-type: persistence follows SceneTracks (Decision 12.1 — OT's `toJSON/fromJSON` is the serialization of record; the app shell owns storage)**. The engine's v2 stays internal intermediate; the OLD C8 "persistence adapter" is re-typed as the one-way PROJECTOR — **ENGINE-home per Decision 16: `nle-engine/src/lib/nle/projector/`** (supersedes the R9-era `src/scene/scene-projector.ts` app-repo path; §9.2 carries the A1 duty) — the engine store is a render-scheduling cache, so its persisted shape is an internal concern that dies with the engine-native editing surface. The migrate/normalize/warnings semantics remain spec-09-§5-compatible and portable |
| C9 | **Engine `export/audio-mix.ts` (Wave-5B, 275 LOC) was a second offline mixdown path** — written so A/V mux had audio before the audio core was wired; risked calcifying into a third mixer next to AudioMixer and web-daw-core's `SceneMixer` | Spec 20 §6: the offline mixdown is the bridge's `SceneMixer` + `buildAudioSegments` (post-M1.6: the bridge lives at **nle-engine `src/lib/nle/bridge/`**), one path | **EXECUTED (Round 15)** — the gate passed and both retirements are code-verified: `audio-mix.ts` DELETED @`abdf9ee` (2026-09-02, AFTER its 26.11 bridge⇄direct null gate held on CI — the successor consistency pin is 29.1) and the `AudioMixer` class DELETED @`20fa266` (2026-09-04, −1,243 lines; only the shared law library survives in `audio/mixer.ts`); realtime AND export now run through the one SceneMixer graph (SCOUT-R15-C §5, tombstones verified). Parity evidence is composed: H3 + 29.1 + m30 (SCOUT-C §6) — the pins re-verified at `player.ts:3287` / `orchestrator.ts:796` (R24) |

Plus two engine-local patterns worth naming because they are the spec's strongest validation: the render-loop item-type skip (P0.3-class, ledger #9) and the singleton-texture returns (ledger #8) are precisely the failure modes the spec's discipline rules exist to prevent — keep them as documented counter-examples even after the engine fixes them.

## 7. Answers to the Engine's Open Decisions (D1-D5)

The engine's master gap charter (§8) lists 5 blocking decisions. The spec set already answers four of them; this section is the Round-7 cross-pollination the workstreams never had.

| ID | Engine's question (MASTER §8) | Spec answer | Notes |
|---|---|---|---|
| D1 | Canonical project wire shape: engine-native (inline fps, `clips`) vs FreeCut's (metadata, `timeline.items`) | **Neither — spec 15's `ProjectJSON`** (spec 09 §3: `metadata`/`settings`/`scenes`/`schemaVersion`, Zod-validated). **R8 escalation: the engine's Wave 4B resolved D1 as "engine-native v2" — AGAINST this answer** (C8). Convergence path (Round-8 decision, Scout-B §8.1 option a): hold the spec line; the engine's v2 shape is an internal intermediate; a spec-09 serializer adapter over its clean serialize/hydrate seams is the bounded convergence task. Do NOT bless the v2 shape — it lacks scenes/media/displayMode/ISO times and would create a third wire shape | Also resolved by spec 09 C1-C3 fixes (schemaVersion literal, migrate-then-parse) |
| D2 | mediabunny as first runtime dependency vs zero-dep WebCodecs hand-roll | **mediabunny** (master spec §4 stack; spec 03 §5.2 API surface verified). Lazy-import is fine; keep the procedural path dep-free for tests (engine's own recommendation aligns) | Matches the engine's recommendation; MPL-2.0 ledger #20 |
| D3 | Font assets for text stack: bundled vs CDN vs caller-provided | **Not spec'd — engine-local.** Spec coverage of text = FCPXML title export (spec 10 §4) only. Recommendation: bundled 2 weights + caller registry (engine's own lean), and flag text-stack *spec coverage* as a seal-round question (the engine's A2 port plan is de facto the reference) | Spec gap acknowledged |
| D4 | Lottie renderer adapter vs skip | **Skip for v1** (out of scope — master §1; no spec consumes lottie). Injectable adapter default-off is acceptable if already built; do not let it grow scope | — |
| D5 | Storage: engine-internal OPFS port vs host-provided `MediaStore` interface | **Interface in the engine, host provides impl — OPFS as the reference implementation** (spec 09 §4.1: `OPFSStorage implements Storage`; Goal 3 swappable). The engine's Wave 4B persistence module should target spec 09's schema/migration/normalization contracts directly | Spec 09 §5 is the port target |

## 8. Engine Subsystem ROI — refactor vs rebuild

Per-subsystem decision table (the "selective grafting hybrid"). "Verdict" is the Round-7 recommendation; the seal round re-baselines after the engine's waves land (§9).

| Subsystem | Engine state (verified) | Spec target | Verdict | Rationale |
|---|---|---|---|---|
| NLE op algorithms (split/trim/ripple/roll/slip/slide/insert/range-…) | ~20 families, 102 methods, tests green | 06 §5 + 15 command layer | **Refactor (graft)** | Math is ALIGNED and test-covered; wrap in command layer + `SceneTracks` model adaptation |
| Transition planner + handles + 27 presentations | Complete, cut-centered, handle clamping | 07 §6 (amended) | **Refactor (graft)** | Round-7 spec amendment already adopted the engine's model — they now match by construction |
| Clock + 6 sync plans + constants | Near-verbatim FreeCut port | 03 §3/§6 | **Refactor (graft)** | Strongest ALIGNED pair in the repo; swap the actuator (DOM→decode) + MediaTime at boundaries |
| Audio mixer / EQ / pitch worklet | **RETIRED-EXECUTED (Round 15)**: AudioMixer class deleted @`20fa266`, direct mix `audio-mix.ts` deleted @`abdf9ee`; realtime + export both render through the one SceneMixer bridge graph (`player.ts:3287` / `orchestrator.ts:796` at the R24 pins); EQ survives as the shared bake law | 20 §5/§6 (the three-layer bridge) | **DONE — retire executed (R15; was RETIRE-AT-GATE)** | Decision 13's premise held: the mixing graph moved to the WDC DSP + engine bridge (reverb/sends/sidechain/PDC/WAM = has-vs-has-not); the remaining M2 surface is the G-wiring → **r2 per the D24 mapping** (W2's SoundTouch half is already LANDED — §3.3A) |
| GPU effect shaders (43-44) | Algorithms match FreeCut; 8-bit | 08 §4/§15 | **Port the math, rebuild the pipeline** | WGSL functions survive; textures/formats/conversion wrap rebuilt per Decision 5 |
| Compositor (ping-pong, 25 blends) | 8-bit formats remain; BUT pool discipline LANDED and pixel-verified (m20.1/20.2/20.5); nested composites exercised (m19.2) | 04 §5/§7 | **Rebuild formats, keep structure + now-proven discipline** (R8 softening) | Only the 10-bit/scene-linear rebuild remains (ledger #6/#8's format half); the discipline half is landed and regression-tested |
| Headless API / command surface | 19-op JSON-RPC + `$ref` (unchanged at Round 15; 19 ops + 8 methods, SCOUT-A §5) | 15 §4 (78 types) | **Rebuild (adapter bridge) — re-typed R15**: per Decision 16 the JSON-RPC surface is INTERNAL transport; the adapter is the engine's union façade enforced at the app bus (D12.2's retire clause superseded) | The dispatcher is new code over the method surface; `$ref` stays internal to the headless/cloud venue |
| Undo/redo | Snapshot machinery, zero callers, keyframe-blind equality | 15 §14.1, 06 §7 | **Refactor** | Extend equality (17 fields), wire callers via commands — engine P0.6 already chartered |
| Media pipeline | Procedural only | 03 §5, 02 §8 | **Rebuild fresh** | `MediaRegistry` seam was designed for this; engine Wave 6A converges independently |
| Persistence | **Wave 4B LANDED (1,174 LOC)**: real round-trip, migrate gate, ~27-code warnings, freecut-parity normalization — but engine-native v2 shape (C8), no Zod, no storage plane (D5 open) | 09 §4-§5 | **Re-shape via adapter; keep semantics** (R8 change from "build fresh") | The migrate/normalize/warnings semantics are spec-09-§5-compatible and portable; the JSON shape is not. A spec-09-shaped serializer can be written as an adapter over `serializeTimeline`/`hydrateTimeline` rather than from scratch |
| FCPXML export | Zero surface (grep-verified) | 10 (entire) | **Build fresh** | Pure JSON→XML, no GPU/media dependency — can land ahead of engine waves |
| Timeline UI | None in engine; **OT W8 TimelineView + real-mouse suite LANDED (Round 15 — theming, waveform, volume line, context menus, keyframe lanes, two-regime zoom; SCOUT-B §2)**; mock timeline region = design reference only (D12) | 05 + 18 | **Build fresh — superseded by D25/D26: OT's `src/components/timeline/` (40 files / 8,604 LOC) is THE canonical tree; the app's `timeline-port/` is a CONVERGING MIRROR under the D26 census (32/40 zero-diff at `c15a629`; 7 documented carriers; `use-wire-dispatch` pending D30 W-C — the carrier count reaches D30's irreducible set, tracked by the census)** | Engine contributes nothing UI-side; the mock's Timeline/Clip/Ruler laws transfer as tests/spec text, not code; the app re-converges the mirror per the census discipline (the §0 GAP D26 row) |
| UI shell | **≈55% de-risked by ui-mock/shell-variants (Round 15)** — chrome + view-state store + testid contract + review infra port at A3; doc slice discarded | 18 | **Build fresh (risk re-priced)** | Mockup + OpenCut conventions + the mock's chrome layer and negative-space findings (SCOUT-D §10) |
| Workers | Zero | 02 (10 workers) | **Build fresh** | Engine main-thread bodies become worker internals |
| Test infrastructure | **Three venues at Round 15 (SCOUT-A §1)**: 274 vitest (Node, 237 engine + 37 bridge) + 265 browser milestone rows (real-WebGPU re-run) + 318 probes, CI = 3 jobs; pixel-truth regression milestones proven at scale | 17 three-tier | **Re-tier (largely converged)** | The engine's own CI now runs the Node tier + browser tier + probes — the C4 re-tiering is de facto done engine-side; remaining work is the app-tier roof suites (spec 17 §17A) |
| Text stack | Throws (advertised op) | 10 (export) only | **Engine-led** (A2 plan) | Spec coverage gap — seal-round decision (§12) |

## 9. Reference Status & Watch List (re-baselined Round 24, 2026-09-08 — this round's fresh-grep pass over the live repos; SCOUT-R15-A/B/C/D remain the R15 evidence)

**R24 fleet re-baseline (2026-09-08):** **nle-engine** @`5036387`: **458/458 vitest (static-census-verified; live venue GHA CI), tsc 0** — the R23 state (N2b @ `37cdd28`, the MIDI path, W2 consumption, RR1-A `clampAuthorFadeToSpan` @ audio-scene.ts:803) now joined by **the W2.5/D29 effects sidecar @ `8a0b7fe`** (`CompositionElementParams.effects` + `buildElementFilterString` — the honest CSS-filter preview subset; the scene GRADE stays consumer-side BY DESIGN) + **the R8-REV #5 blur-radius clamp @ `4b2dfd1`**; API surface 455 (unmoved — the sidecar is bridge-level, outside the barrel), layer fence 52; vendors OT @ `c15a629` (the CODE tip — HEAD `ded43c4`'s src is identical) + WDC @ `494f6ff` (one docs-only commit behind `85b81b0`); FCPXML still zero implementation refs. **opencut-timeline** @ HEAD `ded43c4` / **code pin `c15a629`**: **536/536 — RUN LIVE by the module audit, tsc 0** (59 milestone entries = 386 in-page + 150 real-mouse) — the W10F hardening (M44/M45/M47, DECISIONS #24) + M48 + **the W11 complete-UI round** (use-wire-dispatch; the M49C coverage gate: 24 routed wire verbs + 6 documented exceptions, tsc-lockstep; M49T/H/R/G; DECISIONS #25) + **the op-surface parity audit @ HEAD (11/11 NLE edit verbs vs opencut-classic, `.agents/OP-COVERAGE.md`)**; **the canonical React tree per D25** (`src/components/timeline/`, 40 files / 8,604 LOC, zero testids, `data-test` 68 component-tree sites); C7 rename still NOT STARTED (deliberate → **r1 at end**; the union is 30 types, split 24 routed + 6 exceptions). **web-daw-core** @`85b81b0`: **759/759 RUN LOCALLY, tsc 0** — ONE docs-only commit past `494f6ff` (the S4 seal-round wrap: the seal verdict SEAL-READY P3/P4-only, the honest ledger, SKILL §11's six new laws; the seal's fixes — incl. W3 the JKL audio half CLOSED — landed pre-pin, documented at HEAD); the drift-gate FAILS-on-drift; the M2 remainder (mixer surface + the 4(=5)-named deferred ports + sidechain/PDC/automation) → r2. **nle-ui** @`fc4cc35`: **674 tests RUN LIVE, tsc 0, boundary OK** — the D29 seam waves (W2.3 saveState, W2.4 exportRequest, W2.5 SceneGrade + ColorPage LIVE sliders) + the R8-REV #1 + the kind-aware mute-all pin (`timelineRouter.ts:59`); the C0 MiniShell still NOT STARTED (the PLAN filing gap); consumed by the app @ `fc4cc35` (the app at package HEAD). **nle-test-app** @`c885ece` (THE APP): **174/174, tsc 0** — the R8/D29 REAL wiring (persistenceService + deliverService + sidechain ducking + the effects/color preview with ProgramCanvas's grade final pass) + **the R9 W-A fleet re-pins** (engine `5036387`; OT lock-copy `c15a629` byte-exact — 0 diffs/0 missing/0 extra, 14,912 LOC; WDC `85b81b0`) + W-B (7 no-conflict adopt-wholesale files); **`timeline-port/` (40 files / 8,556 LOC) is a CONVERGING MIRROR per D26** (the census: 7 byte-exact + 25 mechanical + 7 carriers + the host + `use-wire-dispatch` pending W-C); the D30 W-C..W-G waves mid-flight. **Mocks (in-repo):** shell-variants **1,521+** (unchanged; the VLM net `vlm-capture/vlm-review/vlm-run`); shell-mini **355 SEALED** (CORE-SEAMS 26 seams + LAW-NET-INVENTORY 128 units: 13 HOLDS / 115 GAP → 33/322 tests).

**(The R23 fleet paragraph, kept as lineage: R23 pinned engine `b8c6f88` 440 / OT `222532c` 489 (359+130) / WDC `494f6ff` 759 / nle-ui `85dcf57` 648 / app `70e99f0` 117 — the N2b/MIDI/W2/RR1-A + S/T/S3/F1 + D29-seam waves of that round are all subsumed in the R24 rows above.)**

**(The R22 fleet paragraph, kept as lineage: R22 pinned engine `f68ab8c` 356 / OT `05584d8` 459 (329+130) / WDC `fe05d85` 740 / nle-ui `dba8d52` 640 (consumer `752991d`) / app `e662759` 83 / mini 355 / variants 1,334 — the N1-N4 + W1 + S-round + consumer-migration waves of that round are all subsumed in the rows above.)**

Watch list (re-verify at seal — ARCH-R9 §7 is the authoritative version):

1. ~~Waves 4A/4B/4C/4D-B/5A/5B/5C~~ **ALL LANDED** — see §3.1's re-baselined table; counter-example texts stay per §6's rule.
2. ~~Test count drift~~ **RESOLVED (re-confirmed Round 15)** — the R8-era 202/202 truth-fixes held and grew to the R15 three-venue baseline: 274 vitest + 265 browser rows + 318 probes, all re-run (SCOUT-R15-A §1). The engine CI is 3 jobs — the C4 single-tier caveat is history engine-side; the remaining re-tier duty moved to the app (spec 17 §17A).
3. ~~Effects count drift~~ **RESOLVED (Round 15)** — 44 registry entries at `f526e67` per SCOUT-R15-A §10 (16 color-category + chromaKey; the 43-vs-44 HANDOFF drift is superseded).
4. ~~D2 (mediabunny)~~ **LANDED** — its D2: adopted `mediabunny@1.50.8`, lazy-imported, confined behind `export/` (matches spec 03 §5.2's direction; m24 decode-verified).
5. ~~opencut-timeline W4/W5~~ **LANDED (and superseded by W8/W9 — Round 15)** — components + interaction domain + final-round hardening landed (R9 code-ref upgrades stand); W8 added the classic-parity UI, W9 closed the full-repo review at zero open P1/P2.
6. **opencut-timeline C7 rename pass** — **NOT STARTED (re-verified R24 @ `c15a629`)**, deliberately (its DECISIONS #9 treats it as an nle-core-spec internal conflict). The spec-side prerequisite is SUPPLIED (spec 15 §13.15) but its 24-command worklist is now STALE — the union is 30 types (24 routed + 6 exceptions per the W11 census). The rename itself remains mechanically small (single file + the M29 parade + the real-mouse phases) and is re-chartered per D24 → **r1 at END** with the op-family growth (the one-day app migration sub-gate).
7. ~~three competing project shapes~~ **DISSOLVED by re-type** — C8 is now the one-way projector (Decision 12.1); persistence follows SceneTracks; the engine's v2 shape is internal-intermediate only.
8. ~~NEW — M1.5 wire-up lands~~ **DONE and VERIFIED (Round 15)** — the bridge landed in the engine (`src/lib/nle/bridge/`, 7 files, 2,078 LOC — §3.1's per-file table); player routing (`player.ts:3247` via `RealtimeAudioBridge`) + export offline render (`orchestrator.ts:765`) verified through ONE SceneMixer graph; the AudioMixer retirement is EXECUTED (@`20fa266`), and audio-mix.ts is DELETED (C9 resolved, @`abdf9ee`); parity is the composed H3 + 29.1 + m30 gate set — SCOUT-R15-A §3 + SCOUT-R15-C §5-6. (M1.6 followed: WDC is now the PURE core; the engine vendors both.)
9. **op-family port kickoff** — **NOT STARTED (re-verified R24)**; the R15-era A2 charter is superseded by the D24 mapping: **the composed ripple family (GAP-W-ops: cutHead/cutTail/ripple-trim/quantize — 4 census units / 16 tests) composes APP-SIDE at K3 over OT's `timeline.trim`/`rippleDelete`; r1 graduates the OT interval-diff family** (slip/slide/roll/rateStretch INTO OT's engine layer, algorithms from the engine Timeline class, tests carried). The engine Timeline class remains the parity oracle; its ops are verified by the undo probe. The K3∥D26 ordering law governs (ARCH-R24: the DOM-structural half gates on the data-test mapping row + D30's W-C — not on a swap+testid layer).
10. **NEW — web-daw upstream drift** (ARCH-R9 §7.4): run `bun run sync -- --check` in web-daw-core — the only LIVING ancestor; the lock file + 721-test gate make drift loud. **Round 15: upstream main UNCHANGED since the `913d0d7` sync (ls-remote verified 2026-09-04); `sync --check` remains a local-checkout-only duty by design** (SCOUT-R15-C §8).
11. **NEW — seam-doc cross-consistency** (ARCH-R9 §7.7): OT's SEAMS.md and web-daw-core's track-model.md are normative-adjacent — verify this spec set's seam statements (§2.4, spec 20 §3) match them exactly at seal.
12. ~~spec-05 inline-block classification~~ **RULE LANDED, AUDIT DEFERRED** — 00-master §2.5.2 governs; the ~180 legacy blocks classify at seal (§12 item).

## 10. Usage Rules for Implementers

1. **Spec wins.** Any engine/distill code that contradicts a spec contract is a bug in the reference (or a CORRECTIVE delta to document), never a reason to amend the spec silently. Amendments flow through the spec's own process (like Round 7's spec 07 §6.1 amendment — evidence up, decision documented, canon updated).
2. **Cite, don't copy, the corrective list.** Anything in §6 (C1-C8) may be read as executable pseudocode but must not be pasted as architecture: the 8-bit formats, the `$ref` protocol, the class-API-as-surface, the single-tier harness, procedural media, main-thread-everything, the prefixed command names, and the engine-native persistence shape are the eight patterns to undo.
3. **Engine methods are the algorithm layer.** The command layer (spec 15 §4.2 mapping) dispatches to them; nobody calls `timeline.splitClip(...)` from UI/cloud/test code directly.
4. **Graft verdicts come with tests.** Any subsystem adopted per §8 keeps its tests only if they re-tier cleanly (spec 17); otherwise the spec's per-module Testing section governs.
5. **Every new citation must be verified.** New code-reference rows added in future rounds carry file:line + a one-line quoted snippet that was actually opened (the anti-fabrication rule that kept this round's 53/54 citation check clean; see `audits/ROUND-7-AUDITS.md`).

## 11. Relationship to Other Specs (where the reference sections live)

Round 8 adds a second column: **ot** = opencut-timeline code-references (added this round where marked; specs 05/06/16 gain them via the R8 revision pass). Engine code-refs carry Round-7 line numbers unless re-baselined — SCOUT-R8-B §6's drift table is the translator where lines moved. **R24 note: the per-file audit fleet re-bases the per-spec code-ref line numbers at the current pins (this file's own §3/§5/§6 anchors were re-grepped this round; the sibling specs' anchors are their agents' charge).**

| Spec | nle-engine code-refs | opencut-timeline code-refs (R8) | Highlights |
|---|---|---|---|
| 01 core engine | §13A | — | Singleton/dispatcher deltas, EventEmitter + id (ALIGNED), undo zero-callers |
| 02 workers | §13B | — | Zero-worker counter-example, worklet topology correction |
| 03 playback | §13E | — | clock.ts + video-sync.ts — the strongest ALIGNED pair |
| 04 renderer/color | §13D | render seam (§2.4.4 of this spec) | 8-bit totality; pool discipline now landed (R8) |
| 05 timeline | §16.4 | §16.5 (new R8) | Controllers/view/placement/snapping executable reference + zero-anchor semantics |
| 06 NLE ops | §10.4 | §10.5 (new R8) | Division of labor: opencut = placement/ripple/split/group-move; engine = roll/slip/slide/rateStretch/sync-lock |
| 07 composition | §12A | — | Planner/handles/occlusion (adopted); render-loop fix landed (R8) |
| 08 color grading | §15A | — | Registry pattern (ALIGNED), 8-bit/LUT conversion (corrective), scopes absent |
| 09 project model | §10.3 | — | Persistence LANDED but shape-divergent (C8/D1); D5 open |
| 10 FCPXML | §12.8 | — | Zero-surface verification + field-presence map |
| 11 cloud render | §14R | — | Xvfb infra (ALIGNED), cloud pipeline SPEC-ONLY |
| 12 testing strategy | §13.7 | M1-M16 mapping (R8) | Single-tier reality + G-charter convergence; ot's M1-M10 = Vitest-ready T1 |
| 15 wire protocol | §13.14 | §13.15 (new R8) | C2 (engine JSON-RPC) + C7 (ot prefixed names) — the two protocol corrections |
| 16 keyboard | appendix | — | No keyboard layer in either repo — SPEC-ONLY |
| 17 test plan | §18.5 (header re-baselined R15) | §18.6 (new R8) | Re-tier evidence both repos; M11/M12/M13/M14 patterns; **§17A app-tier roof suites (new R15)** |
| 18 UI shell | §13 | — (W4 pending) | No shell code; mockup + ux-spec adoptions (v1.1) |
| 00 / 13 / 14 | Decision 10 + 11 + phases + process | — | This spec's hierarchy + seam feed the master's Decisions 10/11; **spec 14 is RETIRED (D23) — its plan content lives in `IMPLEMENTATION-PLAN.md`, its §4 gap rows in the owning specs' §0 registers, the lineage in the plan's §7** |

## 12. Seal-Round Checklist (open items this round deliberately left)

1. ~~Re-baseline §9's watch list after the engine's waves land~~ — **DONE in Round 8, re-done in Round 9, re-done again in Round 15 — re-done at R22 — re-done at R23 — and re-done at R24** (§9 re-baselined at the R24 pins `5036387`/`c15a629` (HEAD `ded43c4`)/`85b81b0`/`fc4cc35`/`c885ece` + the in-repo mocks; all gates re-run (OT/WDC/nle-ui live, engine/app census-verified); all watch items current; the R23/R15 pins and the R22 pins are history).
2. ~~timeline-distill exists? → wire §3.2~~ — **DONE in Round 8; W4 component upgrade DONE in Round 9** (spec 05 §16.5 re-baselined @4e39b67, components landed).
3. ~~Text-stack spec coverage decision (D3)~~ — **RESOLVED by events**: 4D-B/C landed with bundled fonts (its D3), typography core + GlyphAtlas pixel-verified; spec-side coverage question becomes a citation pass (item 6).
4. Source-preview question (spec 18 §15.1) — affects the wire protocol if a source-playback surface is added. The ux-spec's fallback `<video>` source-preview mode (ux-spec 05 §4.2) is the cheap interim if we ever ship one (SCOUT-R8-C §3 C21).
5. Full decision reconciliation (the engine's D1-D10+ ledger / **opencut-timeline's 25 decisions** (through #25, the W11 wire-dispatch rulings) / this spec set's **29**): §7 answered engine D1-D5 (D1 dissolved by C8's R9 re-type); the remaining engine decisions + all OT decisions need "no spec conflict" sign-off lines — C7 (its D9) and its D1/D2 (our Decisions 3/2 — aligned by construction) already covered. **web-daw-core's own doc-set (SKILL/track-model/UPSTREAMING) needs the same sign-off pass per 00-master §2.5.3.**
6. Final citation sweep of all per-spec code-ref tables (rule §10.5) + regenerate audit totals — **THIS file's sweep is DONE at R24 (the §3 refresh blocks + the §5/§6 ledger anchors re-grepped at the live pins); the sibling specs' sweeps are the R24 audit fleet's charge**; regenerate audit totals at the next seal. **Includes the spec-05 inline-block classification audit (~180 legacy blocks per 00-master §2.5.2).**
7. ~~opencut-timeline C7 rename pass + W5/W6 command growth~~ — **W5/W6 + hardening DONE; C7 rename REMAINS (§9 item 6, re-verified R24: NOT STARTED, deliberately)**: when landed, update §3.2's headless row and spec 15 §13.15. The spec-side prerequisite is SUPPLIED but its 24-command worklist is STALE — **the union has been 30 types since the S/T/S3 rounds, now split 24 routed + 6 exceptions at `c15a629` (R24, §3.2's refresh block)**, not the 24 of R15 nor the 18 the old worklist enumerated; execution is re-chartered per D24 → **r1 at END** (the one-day app migration sub-gate).
8. ~~the engine's C8 persistence adapter + C2 command-layer adapter~~ — **RE-SCOPED in Round 9, re-typed Round 15 per Decision 16**: C8 is re-typed as the one-way projector (§6 C8 row) — now **ENGINE-HOME** (`nle-engine/src/lib/nle/projector/`, an additive fenced module per the bridge family's type-only precedent; ARCH-R15 §2.2); C2 narrows to the runtime command subset with the JSON-RPC surface re-typed INTERNAL transport (the union contract enforced at the app bus + engine façade). Verify the PROJECTOR + the union façade when they land (**still NOT LANDED at `5036387` — R24 directory verification, §3.1's refresh block**); the editing-subset duty moved to OT.
9. cloudcut UX-spec maintenance: the ux-spec branch is now an input-of-record; if it keeps evolving, re-run the applicability matrix deltas (SCOUT-R8-C is the baseline).
10. ~~NEW (R9) — the three ARCH-R9 §7 verifications~~ — **all resolved at Round 15**: (a) M1.5 wire-up + executed AudioMixer retirement — **DONE/EXECUTED, verified** (§9 item 8; SCOUT-A §3 + SCOUT-C §5-6); (b) the `roll` op-family port kickoff into OT — **NOT STARTED, re-chartered as A2 wave 1** (§9 item 9); (c) `bun run sync -- --check` in web-daw-core — upstream unchanged since `913d0d7` (SCOUT-C §8; local-checkout-only duty stands). The `.refined.md` rename's zero-dangling-reference battery check (§7.5) and seam-doc cross-consistency (§7.7) remain seal-round duties.
11. **NEW (R9) — spec 20's open questions** close at seal: automation-curve shape, sidechain source-selection UX (needs a spec 18 mixer-panel section) — both remain open; **the D24 mapping re-keys their home to r2** (the audio-depth phase; the R15-era "app A4" charter is superseded); ~~audio-mix.ts end-state note (C9 resolution)~~ **RESOLVED at Round 15 by deletion** (@`abdf9ee`; tombstones verified by SCOUT-R15-C §5 — spec 20's §12.4 row is superseded).
12. **Post-re-baseline residuals (updated R24)**: (a) ~~the consumer re-pin wave~~ **EXECUTED at R24** (the R9 W-A fleet-coherence wave: engine OT `a4e971d`→`c15a629`, app engine `4ef0147`→`5036387`, app OT-lock-copy→`c15a629` byte-exact, nle-ui→`fc4cc35`, app WDC→`85b81b0`; the only residual is the engine's WDC vendor `494f6ff` — one docs-only commit behind, zero code delta) — the NEW open residuals are the app's D30 W-C..W-G completion (the interlocked carriers + `use-wire-dispatch`), the app's `.agents/` creation (D27/F17, before W-C), and the cross-repo filing gaps (nle-ui's C0/MiniShell + D29/D30 DECISIONS not filed package-side; the engine's meta-docs lag their last 3 commits); (b) engine cosmetic folds at its next commit (layout.tsx "Z.ai Code Scaffold" metadata; doc count drifts — ARCH-R15 §2.6) — status unchanged at R24; (c) the mock's README count drift — closed by the R23 seal artifacts (§3.6); the variants' README count ("33 files / 510 tests" — the R15 text) remains behind the 1,521+ current state, folds into the next mock commit or the C0 port.

---

**End of `19-code-references.md`.** Round 7 established the canon hierarchy; Round 8 made it three-repo; **Round 9 made it three-DOMAIN (§2.4) with the spec set re-typed as contract + gap + acceptance (Decision 14); Round 15 re-baselined all four code assets at their verified SHAs and added the shell-mock tier (§3.5); R22 re-pinned the fleet + added the tier-4 rows; R23 re-pinned the fleet at the D23/D24/D25 pins, made OT's `src/components/timeline/` THE canonical React tree (Decision 25), added the §3.6 mini row, and re-grepped every live anchor; R24 re-pinned the fleet at the D26-D29 pins (458/536/759/674/174), registered the W11/W2.5/D29 surfaces + the D26 port census (the converging mirror + the byte-exact lock-copy), and re-grepped every live anchor again.** Specs 00/13 and the README carry the pointers (spec 14 is retired to the stub; the plan lives at the repo root).